stage: synthesis
tool: fake_tool
id: two_examples_with_full
title: An error with two examples each having minimal and full versions
regex: >-
  known error pattern here
matching_mode: specific
issue_novelty: new
issue_link: https://example.com/4
maintainers_response: bug
examples:
  - example_1:
      first_found: 01.01.2025
      minified_example: |
        module broken1(output o);
          assign o = ;
        endmodule
      full_example: |
        // Full version with more context
        module broken1_full(input clk, output o);
          assign o = ;
        endmodule
  - example_2:
      first_found: 15.02.2025
      minified_example: |
        module broken2(output o);
          assign o = ;
        endmodule
      full_example: |
        // Full version with more context
        module broken2_full(input clk, input rst, output o);
          assign o = ;
        endmodule
