stage: synthesis
tool: fake_tool
id: still_reproducing_error
title: An error that is still present
regex: >-
  known error pattern here
matching_mode: specific
issue_novelty: new
issue_link: https://example.com/1
maintainers_response: bug
examples:
  - example_still_reproducing_1:
      first_found: 01.01.2025
      minified_example: |
        module broken(output o);
          assign o = ;
        endmodule
      full_example: |
        // full version
        module broken_full(output o);
          assign o = ;
        endmodule
