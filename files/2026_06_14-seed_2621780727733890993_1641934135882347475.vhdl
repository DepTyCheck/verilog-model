-- Seed: 2621780727733890993,1641934135882347475



entity lcgxkoxt is
  port (ykyaf : linkage severity_level; hgpyopff : buffer time_vector(4 to 3));
end lcgxkoxt;



architecture kkdbjseirl of lcgxkoxt is
  
begin
  
end kkdbjseirl;

library ieee;
use ieee.std_logic_1164.all;

entity uke is
  port (alieh : out boolean; a : buffer std_logic_vector(1 downto 0); mewuvby : inout real; aord : in character);
end uke;



architecture pl of uke is
  signal llm : time_vector(4 to 3);
  signal jupwriuab : time_vector(4 to 3);
  signal zputn : time_vector(4 to 3);
  signal rgssrddcu : severity_level;
begin
  ozykphxy : entity work.lcgxkoxt
    port map (ykyaf => rgssrddcu, hgpyopff => zputn);
  gz : entity work.lcgxkoxt
    port map (ykyaf => rgssrddcu, hgpyopff => jupwriuab);
  aylxcsjws : entity work.lcgxkoxt
    port map (ykyaf => rgssrddcu, hgpyopff => llm);
end pl;

library ieee;
use ieee.std_logic_1164.all;

entity dykhwptl is
  port (efc : buffer std_logic; vndveurnfa : buffer real; toqwlmg : inout integer; ujfguyjoa : out severity_level);
end dykhwptl;

library ieee;
use ieee.std_logic_1164.all;

architecture hxj of dykhwptl is
  signal dvceejszzp : time_vector(4 to 3);
  signal xnyx : time_vector(4 to 3);
  signal gcj : severity_level;
  signal ekpyvvsn : time_vector(4 to 3);
  signal o : character;
  signal dkramxni : real;
  signal l : std_logic_vector(1 downto 0);
  signal duvlv : boolean;
begin
  dikhky : entity work.uke
    port map (alieh => duvlv, a => l, mewuvby => dkramxni, aord => o);
  qdoqgp : entity work.lcgxkoxt
    port map (ykyaf => ujfguyjoa, hgpyopff => ekpyvvsn);
  rmc : entity work.lcgxkoxt
    port map (ykyaf => gcj, hgpyopff => xnyx);
  nxjfq : entity work.lcgxkoxt
    port map (ykyaf => ujfguyjoa, hgpyopff => dvceejszzp);
end hxj;

library ieee;
use ieee.std_logic_1164.all;

entity jlhihroa is
  port (bewkevgwxh : in std_logic_vector(1 to 1); qeiag : buffer time; f : in std_logic_vector(3 to 4); onyba : out std_logic);
end jlhihroa;

library ieee;
use ieee.std_logic_1164.all;

architecture eqfmbl of jlhihroa is
  signal tvhjku : time_vector(4 to 3);
  signal vdemldem : severity_level;
  signal dxtgxzvz : character;
  signal gtdz : real;
  signal phg : std_logic_vector(1 downto 0);
  signal lcz : boolean;
begin
  zt : entity work.uke
    port map (alieh => lcz, a => phg, mewuvby => gtdz, aord => dxtgxzvz);
  are : entity work.lcgxkoxt
    port map (ykyaf => vdemldem, hgpyopff => tvhjku);
end eqfmbl;



-- Seed after: 14238725082539266249,1641934135882347475
