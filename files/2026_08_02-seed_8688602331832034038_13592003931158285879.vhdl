-- Seed: 8688602331832034038,13592003931158285879

library ieee;
use ieee.std_logic_1164.all;

entity tiiboci is
  port (o : linkage std_logic; ditsnouk : out time; mzlb : in std_logic_vector(3 to 1));
end tiiboci;

architecture jasl of tiiboci is
  
begin
  -- Single-driven assignments
  ditsnouk <= ditsnouk;
end jasl;

entity zgooddkb is
  port (idazisxabd : buffer real; oxuk : out real);
end zgooddkb;

library ieee;
use ieee.std_logic_1164.all;

architecture ry of zgooddkb is
  signal nux : std_logic_vector(3 to 1);
  signal hgqfnxwvpb : time;
  signal oomkvig : std_logic;
  signal qtlof : std_logic_vector(3 to 1);
  signal j : time;
  signal c : std_logic;
begin
  a : entity work.tiiboci
    port map (o => c, ditsnouk => j, mzlb => qtlof);
  qchyzu : entity work.tiiboci
    port map (o => oomkvig, ditsnouk => hgqfnxwvpb, mzlb => nux);
  
  -- Single-driven assignments
  idazisxabd <= oxuk;
  oxuk <= oxuk;
  
  -- Multi-driven assignments
  c <= 'W';
  c <= 'H';
end ry;

entity h is
  port (hoxvb : linkage integer);
end h;

library ieee;
use ieee.std_logic_1164.all;

architecture eeezrnkz of h is
  signal ospnt : real;
  signal tt : real;
  signal iuyg : std_logic_vector(3 to 1);
  signal svxrzxthd : time;
  signal mjhsyamte : std_logic;
begin
  ek : entity work.tiiboci
    port map (o => mjhsyamte, ditsnouk => svxrzxthd, mzlb => iuyg);
  lix : entity work.zgooddkb
    port map (idazisxabd => tt, oxuk => ospnt);
  
  -- Multi-driven assignments
  mjhsyamte <= mjhsyamte;
  iuyg <= (others => '0');
  mjhsyamte <= mjhsyamte;
  mjhsyamte <= mjhsyamte;
end eeezrnkz;

library ieee;
use ieee.std_logic_1164.all;

entity xpfdphjfbh is
  port (tczzlkaip : in bit; jdc : inout std_logic; r : in std_logic_vector(1 downto 0));
end xpfdphjfbh;

library ieee;
use ieee.std_logic_1164.all;

architecture lseyxwzb of xpfdphjfbh is
  signal km : integer;
  signal qliyzc : time;
  signal bmktsnvixa : std_logic;
  signal pzcdstpobz : std_logic_vector(3 to 1);
  signal glw : time;
  signal mlywgds : std_logic;
  signal lbxrptx : std_logic_vector(3 to 1);
  signal gkmmbrmsxv : time;
  signal o : std_logic;
begin
  uownt : entity work.tiiboci
    port map (o => o, ditsnouk => gkmmbrmsxv, mzlb => lbxrptx);
  x : entity work.tiiboci
    port map (o => mlywgds, ditsnouk => glw, mzlb => pzcdstpobz);
  gyyhkdkiv : entity work.tiiboci
    port map (o => bmktsnvixa, ditsnouk => qliyzc, mzlb => pzcdstpobz);
  wvjakp : entity work.h
    port map (hoxvb => km);
end lseyxwzb;



-- Seed after: 10853363469028657839,13592003931158285879
