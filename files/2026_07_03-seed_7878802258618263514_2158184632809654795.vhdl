-- Seed: 7878802258618263514,2158184632809654795

use std.reflection.all;

entity tr is
  port (ximlsa : inout access_subtype_mirror; l : inout real; xncjfljnat : buffer real);
end tr;

architecture lkdqlvl of tr is
  
begin
  
end lkdqlvl;

use std.reflection.all;

entity oghtlmu is
  port (lsgbgq : inout file_value_mirror; ixeogpqnf : in integer; khpc : inout floating_subtype_mirror);
end oghtlmu;

use std.reflection.all;

architecture gvcqle of oghtlmu is
  signal myosmqd : real;
  signal liyhlerzlq : real;
  shared variable jskwibu : access_subtype_mirror;
  signal tpofh : real;
  signal iorqz : real;
  shared variable dbzimr : access_subtype_mirror;
  signal knolqnfcd : real;
  signal qt : real;
  shared variable xsbyekyu : access_subtype_mirror;
  signal q : real;
  signal uhfbw : real;
  shared variable npciazlb : access_subtype_mirror;
begin
  cyprwfuo : entity work.tr
    port map (ximlsa => npciazlb, l => uhfbw, xncjfljnat => q);
  esxgxa : entity work.tr
    port map (ximlsa => xsbyekyu, l => qt, xncjfljnat => knolqnfcd);
  j : entity work.tr
    port map (ximlsa => dbzimr, l => iorqz, xncjfljnat => tpofh);
  serygai : entity work.tr
    port map (ximlsa => jskwibu, l => liyhlerzlq, xncjfljnat => myosmqd);
end gvcqle;

use std.reflection.all;

entity lth is
  port (gsd : inout record_subtype_mirror);
end lth;

architecture sjnmls of lth is
  
begin
  
end sjnmls;

use std.reflection.all;

entity fzhdb is
  port (n : buffer boolean; jjeqk : buffer bit_vector(2 downto 1); kwzi : inout protected_subtype_mirror);
end fzhdb;

use std.reflection.all;

architecture oxyjpzd of fzhdb is
  signal gpwelzna : real;
  signal dons : real;
  shared variable kqxpj : access_subtype_mirror;
  signal ri : real;
  signal rmt : real;
  shared variable s : access_subtype_mirror;
  signal fhwsc : real;
  signal oriaiqp : real;
  shared variable kqy : access_subtype_mirror;
begin
  xj : entity work.tr
    port map (ximlsa => kqy, l => oriaiqp, xncjfljnat => fhwsc);
  dnrhkabt : entity work.tr
    port map (ximlsa => s, l => rmt, xncjfljnat => ri);
  xuveo : entity work.tr
    port map (ximlsa => kqxpj, l => dons, xncjfljnat => gpwelzna);
end oxyjpzd;



-- Seed after: 56417944824590863,2158184632809654795
