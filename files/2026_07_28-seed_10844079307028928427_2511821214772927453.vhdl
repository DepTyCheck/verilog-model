-- Seed: 10844079307028928427,2511821214772927453

entity thbv is
  port (dex : out real; sn : buffer integer_vector(2 to 1));
end thbv;

architecture cwpgjoy of thbv is
  
begin
  -- Single-driven assignments
  dex <= 4_0.244;
end cwpgjoy;

library ieee;
use ieee.std_logic_1164.all;

entity muvg is
  port (kovzg : linkage integer; dqeul : inout std_logic; mldwifmonk : linkage integer);
end muvg;

architecture lqx of muvg is
  signal sv : integer_vector(2 to 1);
  signal zkjnyc : real;
  signal zrotjmw : integer_vector(2 to 1);
  signal slm : real;
  signal ey : integer_vector(2 to 1);
  signal m : real;
  signal kqcswt : integer_vector(2 to 1);
  signal tdfhaqgtp : real;
begin
  nqyu : entity work.thbv
    port map (dex => tdfhaqgtp, sn => kqcswt);
  wcp : entity work.thbv
    port map (dex => m, sn => ey);
  jkd : entity work.thbv
    port map (dex => slm, sn => zrotjmw);
  yqhmxv : entity work.thbv
    port map (dex => zkjnyc, sn => sv);
  
  -- Multi-driven assignments
  dqeul <= '-';
end lqx;



-- Seed after: 18218839993656006826,2511821214772927453
