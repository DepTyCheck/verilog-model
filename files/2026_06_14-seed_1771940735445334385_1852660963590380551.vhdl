-- Seed: 1771940735445334385,1852660963590380551

library ieee;
use ieee.std_logic_1164.all;

entity sb is
  port (g : inout boolean; gobfasfqn : inout real; uantpccech : inout std_logic);
end sb;



architecture rshv of sb is
  
begin
  
end rshv;

library ieee;
use ieee.std_logic_1164.all;

entity bfjbtq is
  port (fw : in integer; cdar : inout real; mzum : out std_logic_vector(3 downto 4); svmuk : buffer std_logic);
end bfjbtq;

library ieee;
use ieee.std_logic_1164.all;

architecture hjt of bfjbtq is
  signal hxbirct : std_logic;
  signal mfw : boolean;
  signal gl : real;
  signal mdwhtewtp : boolean;
  signal eloluettv : std_logic;
  signal obgnvvyoec : real;
  signal kt : boolean;
  signal raekfvt : std_logic;
  signal abjsoqxzt : real;
  signal cm : boolean;
begin
  ywvkttbiki : entity work.sb
    port map (g => cm, gobfasfqn => abjsoqxzt, uantpccech => raekfvt);
  lekjpyawbq : entity work.sb
    port map (g => kt, gobfasfqn => obgnvvyoec, uantpccech => eloluettv);
  qmo : entity work.sb
    port map (g => mdwhtewtp, gobfasfqn => gl, uantpccech => eloluettv);
  qflxoe : entity work.sb
    port map (g => mfw, gobfasfqn => cdar, uantpccech => hxbirct);
end hjt;



-- Seed after: 14904700191292701826,1852660963590380551
