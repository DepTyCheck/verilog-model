-- Seed: 11835193948175062561,10463297573877745897

entity iv is
  port (kywy : linkage integer; scjj : in integer; i : out real; sz : linkage real);
end iv;

architecture db of iv is
  
begin
  
end db;

library ieee;
use ieee.std_logic_1164.all;

entity oburl is
  port (gim : out std_logic_vector(2 downto 3); clydr : inout severity_level; vaujpm : buffer integer; sjct : in time);
end oburl;

architecture oidrlu of oburl is
  signal nxdvuqbo : real;
  signal ym : real;
  signal dndkelyqm : integer;
  signal zjgr : real;
  signal nftrkz : real;
  signal mb : integer;
  signal gmrijkk : integer;
  signal cgtooamvx : real;
  signal ixkhgsgjdn : real;
  signal thujz : integer;
  signal p : real;
  signal lltzhuysmm : real;
  signal fytpgkvqsd : integer;
  signal ayyi : integer;
begin
  oowvjh : entity work.iv
    port map (kywy => ayyi, scjj => fytpgkvqsd, i => lltzhuysmm, sz => p);
  kagv : entity work.iv
    port map (kywy => thujz, scjj => vaujpm, i => ixkhgsgjdn, sz => cgtooamvx);
  yehadzuwar : entity work.iv
    port map (kywy => gmrijkk, scjj => mb, i => nftrkz, sz => zjgr);
  pjcfa : entity work.iv
    port map (kywy => dndkelyqm, scjj => ayyi, i => ym, sz => nxdvuqbo);
  
  -- Single-driven assignments
  vaujpm <= 2_2_1;
  mb <= dndkelyqm;
  fytpgkvqsd <= 4;
  clydr <= clydr;
  
  -- Multi-driven assignments
  gim <= (others => '0');
  gim <= "";
  gim <= gim;
  gim <= "";
end oidrlu;



-- Seed after: 11710076851602595750,10463297573877745897
