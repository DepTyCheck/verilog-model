// Seed: 5856399643387914569,16826431007307047059

module t (output reg [2:3][1:2][4:2] ophjisxwfr, output bit [2:0][0:1][0:2][0:3] zutosud);
  
  
  
  // Single-driven assigns
  assign zutosud = zutosud;
  assign ophjisxwfr = '{'{'{'bz,'b100,'bzx10z},'{'bzz011,'bzz0zx,'b1}},'{'{'bxx,'bx0,'bx0},'{'bz,'bz0z,'bzxx}}};
  
  // Multi-driven assigns
endmodule: t

module ssnwom (input reg fmbaugrqzl [4:0], input tri0 logic [2:4] q, output uwire logic mb [2:0][0:4][1:0]);
  
  xor p(q, ixirfapbpi, q);
  // warning: implicit conversion of port connection truncates from 3 to 1 bits
  //   tri0 logic [2:4] q -> logic q
  //
  // warning: implicit conversion of port connection truncates from 3 to 1 bits
  //   tri0 logic [2:4] q -> logic q
  
  xor diq(ymynedqxo, jgq, jlksmdk);
  
  xnor hq(q, ytj, wujfnaq);
  // warning: implicit conversion of port connection truncates from 3 to 1 bits
  //   tri0 logic [2:4] q -> logic q
  
  xnor gmbhc(q, ahxhnszs, q);
  // warning: implicit conversion of port connection truncates from 3 to 1 bits
  //   tri0 logic [2:4] q -> logic q
  //
  // warning: implicit conversion of port connection truncates from 3 to 1 bits
  //   tri0 logic [2:4] q -> logic q
  
  
  // Single-driven assigns
  
  // Multi-driven assigns
  assign q = '{'bx,'bx,'bxx11x};
endmodule: ssnwom

module bh (output bit [4:4] ajd [1:3]);
  // Unpacked net declarations
  uwire logic qwlfvlg [2:0][0:4][1:0];
  reg fruc [4:0];
  
  ssnwom v(.fmbaugrqzl(fruc), .q(eewpjcp), .mb(qwlfvlg));
  // warning: implicit conversion of port connection expands from 1 to 3 bits
  //   wire logic eewpjcp -> tri0 logic [2:4] q
  
  xnor gpujussyj(tiif, eewpjcp, rc);
  
  
  // Single-driven assigns
  assign ajd = ajd;
  
  // Multi-driven assigns
  assign eewpjcp = 'b0;
  assign rc = 'b0;
  assign tiif = 'bx00xz;
endmodule: bh



// Seed after: 8568803709309178806,16826431007307047059
