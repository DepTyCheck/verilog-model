-- Seed: 15459536099867736759,7808623373429384027

entity tg is
  port (dipccgiu : out integer; dmgyexeu : buffer bit; pxxhzbtw : inout integer_vector(1 to 2); kwiiwluuar : out integer_vector(3 to 3));
end tg;

architecture afaz of tg is
  
begin
  -- Single-driven assignments
  pxxhzbtw <= (2_2_0_4, 4204);
  dipccgiu <= dipccgiu;
end afaz;

entity cvdxaqbv is
  port (o : out character; n : in integer);
end cvdxaqbv;

architecture xsuh of cvdxaqbv is
  signal kcwrsdi : integer_vector(3 to 3);
  signal fqesigfvch : integer_vector(1 to 2);
  signal xlphxdrp : bit;
  signal dccdrzbs : integer;
  signal bdlru : integer_vector(3 to 3);
  signal szveczl : integer_vector(1 to 2);
  signal xxozzeu : bit;
  signal m : integer;
  signal irvitqdw : integer_vector(3 to 3);
  signal gwbj : integer_vector(1 to 2);
  signal zs : bit;
  signal cilj : integer;
begin
  lixexorgfd : entity work.tg
    port map (dipccgiu => cilj, dmgyexeu => zs, pxxhzbtw => gwbj, kwiiwluuar => irvitqdw);
  xrame : entity work.tg
    port map (dipccgiu => m, dmgyexeu => xxozzeu, pxxhzbtw => szveczl, kwiiwluuar => bdlru);
  lfjv : entity work.tg
    port map (dipccgiu => dccdrzbs, dmgyexeu => xlphxdrp, pxxhzbtw => fqesigfvch, kwiiwluuar => kcwrsdi);
  
  -- Single-driven assignments
  o <= o;
end xsuh;



-- Seed after: 4079993558205768948,7808623373429384027
