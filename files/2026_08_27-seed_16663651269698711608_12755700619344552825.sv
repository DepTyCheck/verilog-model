// Seed: 16663651269698711608,12755700619344552825

module twjop (output tri0 logic [2:0] lo, input reg [3:4][1:1][2:2] npfrryq, input logic [2:4] palxjczd [1:4]);
  not wjlqfta(lo, lo);
  // warning: implicit conversion of port connection truncates from 3 to 1 bits
  //   tri0 logic [2:0] lo -> logic lo
  //
  // warning: implicit conversion of port connection truncates from 3 to 1 bits
  //   tri0 logic [2:0] lo -> logic lo
  
  or xfq(lo, npfrryq, ifx);
  // warning: implicit conversion of port connection truncates from 3 to 1 bits
  //   tri0 logic [2:0] lo -> logic lo
  //
  // warning: implicit conversion of port connection truncates from 2 to 1 bits
  //   reg [3:4][1:1][2:2] npfrryq -> logic npfrryq
  
  xor acsitrjlhh(lo, ifx, ioiveja);
  // warning: implicit conversion of port connection truncates from 3 to 1 bits
  //   tri0 logic [2:0] lo -> logic lo
  
  
  // Multi-driven assignments
  assign lo = lo;
  assign lo = lo;
  assign lo = lo;
  assign lo = lo;
endmodule: twjop



// Seed after: 16767496690488578297,12755700619344552825
