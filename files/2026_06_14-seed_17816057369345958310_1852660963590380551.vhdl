-- Seed: 17816057369345958310,1852660963590380551

library ieee;
use ieee.std_logic_1164.all;

entity n is
  port (uxznp : linkage bit; bzyfu : out std_logic_vector(2 downto 2); kktfjxw : out integer);
end n;



architecture h of n is
  
begin
  
end h;



entity fdraemum is
  port (ovhsgwa : inout real; eokraucuue : linkage severity_level);
end fdraemum;

library ieee;
use ieee.std_logic_1164.all;

architecture uje of fdraemum is
  signal ufsuq : integer;
  signal jfkajpher : std_logic_vector(2 downto 2);
  signal pibjmn : bit;
  signal gys : integer;
  signal dahpb : std_logic_vector(2 downto 2);
  signal kicfiq : bit;
  signal twnzsxu : integer;
  signal deyfqgr : std_logic_vector(2 downto 2);
  signal cw : bit;
begin
  goht : entity work.n
    port map (uxznp => cw, bzyfu => deyfqgr, kktfjxw => twnzsxu);
  tesm : entity work.n
    port map (uxznp => kicfiq, bzyfu => dahpb, kktfjxw => gys);
  z : entity work.n
    port map (uxznp => pibjmn, bzyfu => jfkajpher, kktfjxw => ufsuq);
end uje;



entity tidcmwm is
  port (o : in integer);
end tidcmwm;

library ieee;
use ieee.std_logic_1164.all;

architecture hfwozqaeqp of tidcmwm is
  signal wrshfotm : integer;
  signal pts : std_logic_vector(2 downto 2);
  signal bcg : bit;
  signal rb : severity_level;
  signal fcqumnbl : real;
begin
  xtns : entity work.fdraemum
    port map (ovhsgwa => fcqumnbl, eokraucuue => rb);
  ujzkyqje : entity work.n
    port map (uxznp => bcg, bzyfu => pts, kktfjxw => wrshfotm);
end hfwozqaeqp;



entity nv is
  port (tstnojpvj : linkage integer);
end nv;

library ieee;
use ieee.std_logic_1164.all;

architecture hg of nv is
  signal wnwtbztko : integer;
  signal vedgwb : std_logic_vector(2 downto 2);
  signal km : bit;
begin
  nogqwx : entity work.n
    port map (uxznp => km, bzyfu => vedgwb, kktfjxw => wnwtbztko);
end hg;



-- Seed after: 13630094191437269192,1852660963590380551
