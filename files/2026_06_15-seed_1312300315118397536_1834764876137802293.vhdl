-- Seed: 1312300315118397536,1834764876137802293

entity vigsy is
  port (mhotdrrfgo : linkage time; vaf : inout integer);
end vigsy;

architecture cgbamcu of vigsy is
  
begin
  -- Single-driven assignments
  vaf <= 16#7#;
end cgbamcu;

entity wid is
  port (fpv : buffer severity_level);
end wid;

architecture dw of wid is
  signal suom : integer;
  signal czaj : time;
  signal wncj : integer;
  signal shglpuo : time;
  signal bayvjjqdyw : integer;
  signal pcrvhneu : time;
begin
  unpojjiluo : entity work.vigsy
    port map (mhotdrrfgo => pcrvhneu, vaf => bayvjjqdyw);
  yrllorsrff : entity work.vigsy
    port map (mhotdrrfgo => shglpuo, vaf => wncj);
  orsdv : entity work.vigsy
    port map (mhotdrrfgo => czaj, vaf => suom);
end dw;

entity zakqin is
  port (zlfcisa : inout character; rmcyuafnpc : buffer real);
end zakqin;

architecture mp of zakqin is
  signal xrfnnonrf : integer;
  signal xw : time;
  signal la : integer;
  signal ber : time;
  signal ewg : integer;
  signal bev : time;
  signal dkucfbsxvq : integer;
  signal jyyziflgls : time;
begin
  v : entity work.vigsy
    port map (mhotdrrfgo => jyyziflgls, vaf => dkucfbsxvq);
  abzo : entity work.vigsy
    port map (mhotdrrfgo => bev, vaf => ewg);
  a : entity work.vigsy
    port map (mhotdrrfgo => ber, vaf => la);
  ymsoo : entity work.vigsy
    port map (mhotdrrfgo => xw, vaf => xrfnnonrf);
  
  -- Single-driven assignments
  rmcyuafnpc <= 0_2_1_4.3_0;
  zlfcisa <= 't';
end mp;



-- Seed after: 11134754097959358789,1834764876137802293
