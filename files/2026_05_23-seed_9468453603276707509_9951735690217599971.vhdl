-- Seed: 9468453603276707509,9951735690217599971



entity s is
  port (hwymvwnb : inout severity_level; vd : buffer boolean; iziksai : buffer time_vector(1 to 4));
end s;



architecture cvbmck of s is
  
begin
  
end cvbmck;

library ieee;
use ieee.std_logic_1164.all;

entity ofbmi is
  port (yxclrkhvw : in std_logic; ngtpasal : inout std_logic_vector(3 to 3));
end ofbmi;



architecture kn of ofbmi is
  signal kfkxvo : time_vector(1 to 4);
  signal fpjkrioip : boolean;
  signal rmto : severity_level;
  signal lsfxhgklvn : time_vector(1 to 4);
  signal kelm : boolean;
  signal mjcpadd : severity_level;
begin
  qixi : entity work.s
    port map (hwymvwnb => mjcpadd, vd => kelm, iziksai => lsfxhgklvn);
  g : entity work.s
    port map (hwymvwnb => rmto, vd => fpjkrioip, iziksai => kfkxvo);
end kn;

library ieee;
use ieee.std_logic_1164.all;

entity iawbf is
  port (wkw : linkage std_logic; n : linkage character; msaodshtgr : inout time);
end iawbf;

library ieee;
use ieee.std_logic_1164.all;

architecture qndxxepvvt of iawbf is
  signal gpu : time_vector(1 to 4);
  signal prnakdk : boolean;
  signal lnqoglvi : severity_level;
  signal lrfx : std_logic_vector(3 to 3);
  signal wegsq : std_logic;
  signal o : time_vector(1 to 4);
  signal y : boolean;
  signal nr : severity_level;
begin
  hwxm : entity work.s
    port map (hwymvwnb => nr, vd => y, iziksai => o);
  jbmds : entity work.ofbmi
    port map (yxclrkhvw => wegsq, ngtpasal => lrfx);
  mysszyo : entity work.s
    port map (hwymvwnb => lnqoglvi, vd => prnakdk, iziksai => gpu);
end qndxxepvvt;



entity nvvezo is
  port (jiku : in real);
end nvvezo;

library ieee;
use ieee.std_logic_1164.all;

architecture ioi of nvvezo is
  signal kmyvctjcfl : time;
  signal p : time;
  signal kddlpr : std_logic;
  signal vxft : time;
  signal lxwjd : character;
  signal xnpkb : std_logic;
  signal v : std_logic_vector(3 to 3);
  signal fqnkvkcze : std_logic;
begin
  uiwuan : entity work.ofbmi
    port map (yxclrkhvw => fqnkvkcze, ngtpasal => v);
  sli : entity work.iawbf
    port map (wkw => xnpkb, n => lxwjd, msaodshtgr => vxft);
  o : entity work.iawbf
    port map (wkw => kddlpr, n => lxwjd, msaodshtgr => p);
  recp : entity work.iawbf
    port map (wkw => kddlpr, n => lxwjd, msaodshtgr => kmyvctjcfl);
end ioi;



-- Seed after: 7376012992557497793,9951735690217599971
