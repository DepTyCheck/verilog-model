-- Seed: 14153183849354047859,7198033922882419595

entity v is
  port (trbsvdbak : buffer string(3 to 4); oqpr : out time; wbfehpla : in integer; lzsju : inout real);
end v;

architecture jeuuksqa of v is
  
begin
  -- Single-driven assignments
  lzsju <= lzsju;
  trbsvdbak <= "hu";
  oqpr <= oqpr;
end jeuuksqa;

entity nlf is
  port (rgzpocvzys : in real; rs : linkage boolean; c : inout time);
end nlf;

architecture pdrd of nlf is
  signal mmxnp : real;
  signal loooowzhwe : integer;
  signal tc : string(3 to 4);
  signal neybfq : real;
  signal hokjfoej : integer;
  signal lggcrnrkjn : time;
  signal otvtcblm : string(3 to 4);
  signal vl : real;
  signal pcbd : integer;
  signal bbg : time;
  signal awgitnvbzi : string(3 to 4);
begin
  ffwwk : entity work.v
    port map (trbsvdbak => awgitnvbzi, oqpr => bbg, wbfehpla => pcbd, lzsju => vl);
  hxlbbfrc : entity work.v
    port map (trbsvdbak => otvtcblm, oqpr => lggcrnrkjn, wbfehpla => hokjfoej, lzsju => neybfq);
  jmbz : entity work.v
    port map (trbsvdbak => tc, oqpr => c, wbfehpla => loooowzhwe, lzsju => mmxnp);
  
  -- Single-driven assignments
  loooowzhwe <= 2_2;
  pcbd <= pcbd;
end pdrd;

entity lxaaawg is
  port (ezmgm : inout integer_vector(1 to 2); fadlg : linkage time);
end lxaaawg;

architecture igzyky of lxaaawg is
  signal pd : real;
  signal mrwbx : integer;
  signal mjuv : time;
  signal mxzrt : string(3 to 4);
  signal ywqkyedh : time;
  signal sxjemdtjcn : boolean;
  signal bpbkbvtbr : real;
  signal hnnf : real;
  signal svqjtuekq : integer;
  signal ezf : time;
  signal gytxjk : string(3 to 4);
begin
  fhxgrotym : entity work.v
    port map (trbsvdbak => gytxjk, oqpr => ezf, wbfehpla => svqjtuekq, lzsju => hnnf);
  prskrmq : entity work.nlf
    port map (rgzpocvzys => bpbkbvtbr, rs => sxjemdtjcn, c => ywqkyedh);
  wspoj : entity work.v
    port map (trbsvdbak => mxzrt, oqpr => mjuv, wbfehpla => mrwbx, lzsju => pd);
end igzyky;



-- Seed after: 14526603253428344792,7198033922882419595
