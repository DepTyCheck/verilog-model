-- Seed: 10061273582228226735,5306691039457971049

library ieee;
use ieee.std_logic_1164.all;

entity tfh is
  port (c : out real; b : out std_logic_vector(1 to 1));
end tfh;

architecture axwn of tfh is
  
begin
  -- Single-driven assignments
  c <= c;
end axwn;

entity avzekladmi is
  port (spp : linkage boolean);
end avzekladmi;

library ieee;
use ieee.std_logic_1164.all;

architecture opnulwn of avzekladmi is
  signal noc : std_logic_vector(1 to 1);
  signal hqzjdsyqe : real;
  signal pjyknv : std_logic_vector(1 to 1);
  signal cvvfqp : real;
  signal dznw : std_logic_vector(1 to 1);
  signal vxrkxyeeai : real;
  signal iynndl : std_logic_vector(1 to 1);
  signal znnt : real;
begin
  iyxja : entity work.tfh
    port map (c => znnt, b => iynndl);
  vvo : entity work.tfh
    port map (c => vxrkxyeeai, b => dznw);
  wpmrkn : entity work.tfh
    port map (c => cvvfqp, b => pjyknv);
  dlowcdslnq : entity work.tfh
    port map (c => hqzjdsyqe, b => noc);
end opnulwn;



-- Seed after: 4862711038142601548,5306691039457971049
