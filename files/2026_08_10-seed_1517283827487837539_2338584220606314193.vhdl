-- Seed: 1517283827487837539,2338584220606314193

entity bytd is
  port (yrhkbs : in time; reko : linkage time; sh : linkage severity_level; acqiuknsu : linkage real);
end bytd;

architecture qru of bytd is
  
begin
  
end qru;

library ieee;
use ieee.std_logic_1164.all;

entity dqpl is
  port (vrrapv : linkage std_logic; umbhl : buffer integer_vector(1 downto 1); bkwbhqeam : buffer time);
end dqpl;

architecture ffkfivl of dqpl is
  
begin
  
end ffkfivl;

entity lpa is
  port (yccgusejf : out boolean_vector(4 downto 0); fbzqqb : linkage integer_vector(2 to 2));
end lpa;

architecture gznokgrsd of lpa is
  signal finxfly : real;
  signal qtgipkekpj : severity_level;
  signal zlwr : time;
  signal snnbiswwu : time;
begin
  wirlilf : entity work.bytd
    port map (yrhkbs => snnbiswwu, reko => zlwr, sh => qtgipkekpj, acqiuknsu => finxfly);
  
  -- Single-driven assignments
  snnbiswwu <= 2#0_1_1_0_1# us;
end gznokgrsd;

entity awrgfhvwq is
  port (ybi : out integer; lx : buffer time);
end awrgfhvwq;

library ieee;
use ieee.std_logic_1164.all;

architecture neqkhs of awrgfhvwq is
  signal kdhnsidlq : integer_vector(1 downto 1);
  signal h : std_logic;
  signal i : integer_vector(2 to 2);
  signal qhpne : boolean_vector(4 downto 0);
  signal ahcxi : real;
  signal bc : severity_level;
  signal zzgav : time;
  signal bzbcegtt : time;
begin
  q : entity work.bytd
    port map (yrhkbs => bzbcegtt, reko => zzgav, sh => bc, acqiuknsu => ahcxi);
  zwxmpdmxu : entity work.lpa
    port map (yccgusejf => qhpne, fbzqqb => i);
  oozxnnop : entity work.dqpl
    port map (vrrapv => h, umbhl => kdhnsidlq, bkwbhqeam => lx);
  
  -- Multi-driven assignments
  h <= h;
end neqkhs;



-- Seed after: 7100165469020306749,2338584220606314193
