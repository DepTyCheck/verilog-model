-- Seed: 14679185302782095160,14094562573555574003

library ieee;
use ieee.std_logic_1164.all;

entity grlmhlo is
  port (kfnlftlf : buffer severity_level; kqz : out std_logic_vector(2 to 3); hlp : linkage time_vector(3 to 0); pkltfqckhp : inout time);
end grlmhlo;

architecture fmvkzs of grlmhlo is
  
begin
  -- Single-driven assignments
  pkltfqckhp <= pkltfqckhp;
  kfnlftlf <= FAILURE;
  
  -- Multi-driven assignments
  kqz <= ('0', '-');
  kqz <= kqz;
  kqz <= ('L', 'L');
  kqz <= "1X";
end fmvkzs;

library ieee;
use ieee.std_logic_1164.all;

entity pbvoqqbbx is
  port (chvvvgue : out boolean; r : linkage std_logic);
end pbvoqqbbx;

library ieee;
use ieee.std_logic_1164.all;

architecture gdbxrt of pbvoqqbbx is
  signal so : time;
  signal mt : time_vector(3 to 0);
  signal hbktnqjeyf : severity_level;
  signal xzbjkzvnf : time;
  signal xqvvazpj : time_vector(3 to 0);
  signal mfuzux : std_logic_vector(2 to 3);
  signal ginxf : severity_level;
  signal rgvcekun : time;
  signal pbvku : time_vector(3 to 0);
  signal ahqch : severity_level;
  signal xpzqqvc : time;
  signal gbz : time_vector(3 to 0);
  signal ehtvptu : std_logic_vector(2 to 3);
  signal isdjpbqq : severity_level;
begin
  iycmpsowd : entity work.grlmhlo
    port map (kfnlftlf => isdjpbqq, kqz => ehtvptu, hlp => gbz, pkltfqckhp => xpzqqvc);
  sluawka : entity work.grlmhlo
    port map (kfnlftlf => ahqch, kqz => ehtvptu, hlp => pbvku, pkltfqckhp => rgvcekun);
  dllrxumv : entity work.grlmhlo
    port map (kfnlftlf => ginxf, kqz => mfuzux, hlp => xqvvazpj, pkltfqckhp => xzbjkzvnf);
  e : entity work.grlmhlo
    port map (kfnlftlf => hbktnqjeyf, kqz => ehtvptu, hlp => mt, pkltfqckhp => so);
end gdbxrt;

entity nkpvcgtsr is
  port (zoxi : out real);
end nkpvcgtsr;

library ieee;
use ieee.std_logic_1164.all;

architecture dbgeoyahvt of nkpvcgtsr is
  signal uchkimokyl : time;
  signal toguekxeh : time_vector(3 to 0);
  signal jihbekpk : std_logic_vector(2 to 3);
  signal gz : severity_level;
  signal nnw : std_logic;
  signal vltwxybgxl : boolean;
begin
  rrvdzjp : entity work.pbvoqqbbx
    port map (chvvvgue => vltwxybgxl, r => nnw);
  qankmoj : entity work.grlmhlo
    port map (kfnlftlf => gz, kqz => jihbekpk, hlp => toguekxeh, pkltfqckhp => uchkimokyl);
  
  -- Single-driven assignments
  zoxi <= 0.1_4_0_0_4;
  
  -- Multi-driven assignments
  nnw <= nnw;
end dbgeoyahvt;

entity hyhjy is
  port (ys : linkage character);
end hyhjy;

library ieee;
use ieee.std_logic_1164.all;

architecture cfuj of hyhjy is
  signal ulec : time;
  signal vbwkxnh : time_vector(3 to 0);
  signal thxvhz : severity_level;
  signal x : std_logic;
  signal ozkdbiz : boolean;
  signal uokiw : time;
  signal axzjnfwvd : time_vector(3 to 0);
  signal rgzfazm : std_logic_vector(2 to 3);
  signal d : severity_level;
  signal phb : time;
  signal dhh : time_vector(3 to 0);
  signal w : std_logic_vector(2 to 3);
  signal tuehsvt : severity_level;
begin
  ssnfascg : entity work.grlmhlo
    port map (kfnlftlf => tuehsvt, kqz => w, hlp => dhh, pkltfqckhp => phb);
  jdahysmtx : entity work.grlmhlo
    port map (kfnlftlf => d, kqz => rgzfazm, hlp => axzjnfwvd, pkltfqckhp => uokiw);
  urczakp : entity work.pbvoqqbbx
    port map (chvvvgue => ozkdbiz, r => x);
  tqnjshl : entity work.grlmhlo
    port map (kfnlftlf => thxvhz, kqz => rgzfazm, hlp => vbwkxnh, pkltfqckhp => ulec);
  
  -- Multi-driven assignments
  w <= "UH";
  rgzfazm <= ('H', '0');
  w <= w;
  w <= w;
end cfuj;



-- Seed after: 2353906916104041190,14094562573555574003
