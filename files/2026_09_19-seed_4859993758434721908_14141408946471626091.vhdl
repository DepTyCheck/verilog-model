-- Seed: 4859993758434721908,14141408946471626091

entity cydckeu is
  port (mbhtrncvos : buffer boolean; fbgrauxxd : linkage integer; lx : linkage time; wuoh : in time_vector(2 to 0));
end cydckeu;

architecture kgnl of cydckeu is
  
begin
  -- Single-driven assignments
  mbhtrncvos <= mbhtrncvos;
end kgnl;

entity rbauc is
  port (gsjshz : linkage real; osnzfgrn : out time);
end rbauc;

architecture oa of rbauc is
  signal asi : integer;
  signal qhto : boolean;
  signal owtv : time_vector(2 to 0);
  signal eteg : time;
  signal rdn : integer;
  signal dr : boolean;
  signal apxak : time_vector(2 to 0);
  signal x : time;
  signal eijnyryccw : integer;
  signal otnxhku : boolean;
begin
  ohg : entity work.cydckeu
    port map (mbhtrncvos => otnxhku, fbgrauxxd => eijnyryccw, lx => x, wuoh => apxak);
  tijmahmnws : entity work.cydckeu
    port map (mbhtrncvos => dr, fbgrauxxd => rdn, lx => eteg, wuoh => owtv);
  fcljkyhcu : entity work.cydckeu
    port map (mbhtrncvos => qhto, fbgrauxxd => asi, lx => osnzfgrn, wuoh => apxak);
  
  -- Single-driven assignments
  owtv <= (others => 0 ns);
  apxak <= apxak;
end oa;

library ieee;
use ieee.std_logic_1164.all;

entity kdqsxzow is
  port (bbitvjmlr : out std_logic_vector(0 to 1); mufxxbhj : linkage std_logic; fcx : out std_logic);
end kdqsxzow;

architecture qm of kdqsxzow is
  signal zuz : time_vector(2 to 0);
  signal xer : time;
  signal yplolm : integer;
  signal qs : boolean;
  signal dsx : time_vector(2 to 0);
  signal wjoixhto : time;
  signal luxluzxymh : integer;
  signal opudnqc : boolean;
begin
  suiitm : entity work.cydckeu
    port map (mbhtrncvos => opudnqc, fbgrauxxd => luxluzxymh, lx => wjoixhto, wuoh => dsx);
  lrr : entity work.cydckeu
    port map (mbhtrncvos => qs, fbgrauxxd => yplolm, lx => xer, wuoh => zuz);
  
  -- Single-driven assignments
  zuz <= dsx;
end qm;



-- Seed after: 2184470868421580104,14141408946471626091
