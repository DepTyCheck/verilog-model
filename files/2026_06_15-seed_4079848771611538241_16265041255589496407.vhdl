-- Seed: 4079848771611538241,16265041255589496407



entity k is
  port (dxgo : inout real_vector(4 downto 1); efgwmd : out time; guaq : in real; da : inout integer);
end k;



architecture xjnd of k is
  
begin
  
end xjnd;



entity pwc is
  port (ki : linkage bit_vector(0 downto 4); quf : out integer);
end pwc;



architecture yfeac of pwc is
  signal ywwhoxxv : real;
  signal pfcolg : time;
  signal npchk : real_vector(4 downto 1);
  signal gi : integer;
  signal jfxifmarh : real;
  signal reech : time;
  signal yttfah : real_vector(4 downto 1);
  signal fwia : integer;
  signal qbomqm : real;
  signal wp : time;
  signal ji : real_vector(4 downto 1);
  signal gcut : integer;
  signal t : real;
  signal x : time;
  signal nqfma : real_vector(4 downto 1);
begin
  tz : entity work.k
    port map (dxgo => nqfma, efgwmd => x, guaq => t, da => gcut);
  lqyrdym : entity work.k
    port map (dxgo => ji, efgwmd => wp, guaq => qbomqm, da => fwia);
  zq : entity work.k
    port map (dxgo => yttfah, efgwmd => reech, guaq => jfxifmarh, da => gi);
  sgan : entity work.k
    port map (dxgo => npchk, efgwmd => pfcolg, guaq => ywwhoxxv, da => quf);
end yfeac;

library ieee;
use ieee.std_logic_1164.all;

entity owmtzdekd is
  port (ah : linkage std_logic; owl : inout integer; toa : inout character; tmg : buffer std_logic);
end owmtzdekd;



architecture ohsqlkrep of owmtzdekd is
  signal pct : real;
  signal onlnlmmc : time;
  signal rdwfnfwzg : real_vector(4 downto 1);
  signal pmb : integer;
  signal sodlmj : time;
  signal bww : real_vector(4 downto 1);
  signal ffdcavkdl : integer;
  signal v : real;
  signal rqy : time;
  signal yupigxyx : real_vector(4 downto 1);
  signal h : integer;
  signal wmt : real;
  signal gccuuee : time;
  signal stsokc : real_vector(4 downto 1);
begin
  m : entity work.k
    port map (dxgo => stsokc, efgwmd => gccuuee, guaq => wmt, da => h);
  ehux : entity work.k
    port map (dxgo => yupigxyx, efgwmd => rqy, guaq => v, da => ffdcavkdl);
  msilfeheo : entity work.k
    port map (dxgo => bww, efgwmd => sodlmj, guaq => v, da => pmb);
  yqzxid : entity work.k
    port map (dxgo => rdwfnfwzg, efgwmd => onlnlmmc, guaq => pct, da => owl);
end ohsqlkrep;



-- Seed after: 5085039293248691844,16265041255589496407
