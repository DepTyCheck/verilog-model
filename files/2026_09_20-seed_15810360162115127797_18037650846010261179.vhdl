-- Seed: 15810360162115127797,18037650846010261179

entity kvdeskfml is
  port (bthdkg : buffer real; wroq : buffer real; sc : buffer time; qxfi : in severity_level);
end kvdeskfml;

architecture havulnvbj of kvdeskfml is
  
begin
  -- Single-driven assignments
  sc <= sc;
  bthdkg <= 24302.43;
  wroq <= 310.41103;
end havulnvbj;

library ieee;
use ieee.std_logic_1164.all;

entity dldbdz is
  port (lwdxuhwxn : buffer std_logic_vector(1 downto 4); fklyl : buffer integer_vector(4 to 1); hi : out time);
end dldbdz;

architecture zbzubmhfg of dldbdz is
  signal dnilj : real;
  signal jjfiyn : real;
  signal rpwsgfhswk : time;
  signal xfltvnrz : real;
  signal okoa : real;
  signal bigzpwxmqm : severity_level;
  signal bocys : time;
  signal qwnszzgm : real;
  signal fkkzfd : real;
begin
  uytlquijiy : entity work.kvdeskfml
    port map (bthdkg => fkkzfd, wroq => qwnszzgm, sc => bocys, qxfi => bigzpwxmqm);
  bcmkttvcy : entity work.kvdeskfml
    port map (bthdkg => okoa, wroq => xfltvnrz, sc => rpwsgfhswk, qxfi => bigzpwxmqm);
  pjyrlcfi : entity work.kvdeskfml
    port map (bthdkg => jjfiyn, wroq => dnilj, sc => hi, qxfi => bigzpwxmqm);
  
  -- Single-driven assignments
  bigzpwxmqm <= ERROR;
  fklyl <= (others => 0);
  
  -- Multi-driven assignments
  lwdxuhwxn <= lwdxuhwxn;
end zbzubmhfg;

library ieee;
use ieee.std_logic_1164.all;

entity tisixdmbw is
  port (adkugughv : out real; kxsbbwaig : buffer std_logic; ipjhqxrvd : inout bit_vector(2 to 2); yjlo : out integer);
end tisixdmbw;

architecture lind of tisixdmbw is
  signal wtokxjnkcm : time;
  signal mozihdo : real;
  signal gjdwjojh : severity_level;
  signal pmfmpsu : time;
  signal rzdeeotd : real;
  signal tlfr : real;
begin
  pk : entity work.kvdeskfml
    port map (bthdkg => tlfr, wroq => rzdeeotd, sc => pmfmpsu, qxfi => gjdwjojh);
  zmyabyzqu : entity work.kvdeskfml
    port map (bthdkg => mozihdo, wroq => adkugughv, sc => wtokxjnkcm, qxfi => gjdwjojh);
  
  -- Multi-driven assignments
  kxsbbwaig <= '0';
  kxsbbwaig <= 'U';
  kxsbbwaig <= kxsbbwaig;
end lind;

library ieee;
use ieee.std_logic_1164.all;

entity fh is
  port (xizyimwza : out std_logic_vector(1 downto 1); tkzalkdrmw : buffer integer; bnm : inout real; jiw : inout real_vector(3 to 0));
end fh;

library ieee;
use ieee.std_logic_1164.all;

architecture bzcbqnnx of fh is
  signal fiv : time;
  signal ghx : integer_vector(4 to 1);
  signal tcwtb : std_logic_vector(1 downto 4);
  signal ruxodxfoo : severity_level;
  signal acywhbnjym : time;
  signal uqupfbgf : real;
begin
  xv : entity work.kvdeskfml
    port map (bthdkg => uqupfbgf, wroq => bnm, sc => acywhbnjym, qxfi => ruxodxfoo);
  dpyobonp : entity work.dldbdz
    port map (lwdxuhwxn => tcwtb, fklyl => ghx, hi => fiv);
  
  -- Multi-driven assignments
  tcwtb <= tcwtb;
end bzcbqnnx;



-- Seed after: 3736974995915103068,18037650846010261179
