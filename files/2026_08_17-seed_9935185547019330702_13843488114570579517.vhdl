-- Seed: 9935185547019330702,13843488114570579517

entity aaporhex is
  port (bthf : out time);
end aaporhex;

architecture jllqn of aaporhex is
  
begin
  -- Single-driven assignments
  bthf <= bthf;
end jllqn;

library ieee;
use ieee.std_logic_1164.all;

entity sbpbxhnab is
  port (wc : buffer std_logic_vector(1 downto 0); cblczk : in severity_level; dtzywslvbq : in time; jfz : buffer string(5 to 1));
end sbpbxhnab;

architecture iqlabe of sbpbxhnab is
  signal zbs : time;
  signal rkeyfv : time;
  signal lx : time;
  signal pddstk : time;
begin
  pnpfjmobqy : entity work.aaporhex
    port map (bthf => pddstk);
  uy : entity work.aaporhex
    port map (bthf => lx);
  tlfbeinb : entity work.aaporhex
    port map (bthf => rkeyfv);
  ubjlqugt : entity work.aaporhex
    port map (bthf => zbs);
  
  -- Multi-driven assignments
  wc <= wc;
  wc <= wc;
  wc <= wc;
end iqlabe;

library ieee;
use ieee.std_logic_1164.all;

entity n is
  port (biwwmnqy : inout real; fwxak : buffer std_logic_vector(4 downto 0); coov : linkage real; ovltph : in integer_vector(4 to 0));
end n;

library ieee;
use ieee.std_logic_1164.all;

architecture eshecybngg of n is
  signal nfqtqeykq : string(5 to 1);
  signal cqcux : time;
  signal mfc : severity_level;
  signal wet : std_logic_vector(1 downto 0);
  signal dmsxde : time;
  signal ssialteir : time;
  signal kmkv : time;
begin
  arttqvi : entity work.aaporhex
    port map (bthf => kmkv);
  iz : entity work.aaporhex
    port map (bthf => ssialteir);
  sdfwr : entity work.aaporhex
    port map (bthf => dmsxde);
  gywgwo : entity work.sbpbxhnab
    port map (wc => wet, cblczk => mfc, dtzywslvbq => cqcux, jfz => nfqtqeykq);
  
  -- Single-driven assignments
  biwwmnqy <= biwwmnqy;
  mfc <= mfc;
  cqcux <= 16#BA.9_7_2_1# fs;
end eshecybngg;

entity xofbqd is
  port (tdjlifz : out boolean);
end xofbqd;

library ieee;
use ieee.std_logic_1164.all;

architecture vn of xofbqd is
  signal xz : integer_vector(4 to 0);
  signal axk : real;
  signal i : real;
  signal sese : string(5 to 1);
  signal f : time;
  signal v : severity_level;
  signal xtwxdioh : std_logic_vector(1 downto 0);
  signal agz : integer_vector(4 to 0);
  signal fjfwgh : real;
  signal aeigd : std_logic_vector(4 downto 0);
  signal b : real;
begin
  e : entity work.n
    port map (biwwmnqy => b, fwxak => aeigd, coov => fjfwgh, ovltph => agz);
  kodqsktnov : entity work.sbpbxhnab
    port map (wc => xtwxdioh, cblczk => v, dtzywslvbq => f, jfz => sese);
  gewpm : entity work.n
    port map (biwwmnqy => i, fwxak => aeigd, coov => axk, ovltph => xz);
  mskndhc : entity work.aaporhex
    port map (bthf => f);
  
  -- Single-driven assignments
  xz <= (others => 0);
  v <= NOTE;
  agz <= agz;
  tdjlifz <= tdjlifz;
  
  -- Multi-driven assignments
  aeigd <= aeigd;
end vn;



-- Seed after: 2641855393221338664,13843488114570579517
