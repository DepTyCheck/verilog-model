-- Seed: 8388407380329347861,8927267689619684183

entity nbvoctkwil is
  port (zses : linkage time; dibrmde : in integer_vector(1 to 1); hzoy : out bit);
end nbvoctkwil;

architecture rhux of nbvoctkwil is
  
begin
  -- Single-driven assignments
  hzoy <= hzoy;
end rhux;

entity eakotsz is
  port (mpw : out integer_vector(3 to 4); dqsosi : out integer_vector(3 to 1));
end eakotsz;

architecture hdrge of eakotsz is
  signal wroqnmnzw : bit;
  signal zwixxulhe : time;
  signal sdgay : bit;
  signal o : integer_vector(1 to 1);
  signal fepg : time;
  signal dwpe : bit;
  signal jqswfl : time;
  signal sexhqzsaz : bit;
  signal fpvs : integer_vector(1 to 1);
  signal hbhrccuum : time;
begin
  ndsur : entity work.nbvoctkwil
    port map (zses => hbhrccuum, dibrmde => fpvs, hzoy => sexhqzsaz);
  uep : entity work.nbvoctkwil
    port map (zses => jqswfl, dibrmde => fpvs, hzoy => dwpe);
  wwhsqp : entity work.nbvoctkwil
    port map (zses => fepg, dibrmde => o, hzoy => sdgay);
  qdbism : entity work.nbvoctkwil
    port map (zses => zwixxulhe, dibrmde => o, hzoy => wroqnmnzw);
  
  -- Single-driven assignments
  fpvs <= (others => 0_0_0_2);
  mpw <= (16#4_7_3#, 2#0_0_1#);
  dqsosi <= dqsosi;
end hdrge;

entity grgegpe is
  port (o : out integer; uavzac : buffer integer; sscqe : inout boolean);
end grgegpe;

architecture xg of grgegpe is
  signal nwvpdoudiz : bit;
  signal avn : integer_vector(1 to 1);
  signal r : time;
begin
  gcwj : entity work.nbvoctkwil
    port map (zses => r, dibrmde => avn, hzoy => nwvpdoudiz);
end xg;



-- Seed after: 8358444598130598523,8927267689619684183
