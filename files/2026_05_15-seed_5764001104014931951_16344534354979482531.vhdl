-- Seed: 5764001104014931951,16344534354979482531



entity xcof is
  port (nxvbtpydq : inout severity_level; uhx : buffer real; pgsrbd : buffer boolean);
end xcof;



architecture upufvglx of xcof is
  
begin
  
end upufvglx;

library ieee;
use ieee.std_logic_1164.all;

entity u is
  port (ppax : inout std_logic);
end u;



architecture b of u is
  signal ayjpolzhp : boolean;
  signal boethf : real;
  signal wjipwwc : severity_level;
  signal agztc : boolean;
  signal gzgctthdvm : real;
  signal ptlkwsx : severity_level;
  signal porzwgjzj : boolean;
  signal jglga : real;
  signal bnrgkimxc : severity_level;
  signal jhyqrln : boolean;
  signal htbmpuyp : real;
  signal qtem : severity_level;
begin
  yziysr : entity work.xcof
    port map (nxvbtpydq => qtem, uhx => htbmpuyp, pgsrbd => jhyqrln);
  sqpjliiv : entity work.xcof
    port map (nxvbtpydq => bnrgkimxc, uhx => jglga, pgsrbd => porzwgjzj);
  elcg : entity work.xcof
    port map (nxvbtpydq => ptlkwsx, uhx => gzgctthdvm, pgsrbd => agztc);
  ktjkyhmbaq : entity work.xcof
    port map (nxvbtpydq => wjipwwc, uhx => boethf, pgsrbd => ayjpolzhp);
end b;



-- Seed after: 11535627647629713744,16344534354979482531
