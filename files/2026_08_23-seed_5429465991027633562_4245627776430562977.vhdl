-- Seed: 5429465991027633562,4245627776430562977

library ieee;
use ieee.std_logic_1164.all;

entity idjgrk is
  port (jz : out boolean; i : buffer std_logic);
end idjgrk;

architecture rrknflo of idjgrk is
  
begin
  -- Single-driven assignments
  jz <= jz;
end rrknflo;

library ieee;
use ieee.std_logic_1164.all;

entity k is
  port (rkcvh : linkage std_logic; dbuucfve : out integer_vector(2 to 3); aeijtl : out time);
end k;

library ieee;
use ieee.std_logic_1164.all;

architecture iq of k is
  signal y : std_logic;
  signal bdsixmn : boolean;
  signal o : boolean;
  signal elxwf : std_logic;
  signal l : boolean;
begin
  pdwhwlzzyi : entity work.idjgrk
    port map (jz => l, i => elxwf);
  pwieupil : entity work.idjgrk
    port map (jz => o, i => elxwf);
  xv : entity work.idjgrk
    port map (jz => bdsixmn, i => y);
  
  -- Single-driven assignments
  aeijtl <= aeijtl;
  dbuucfve <= (2#100#, 0);
  
  -- Multi-driven assignments
  y <= y;
end iq;

library ieee;
use ieee.std_logic_1164.all;

entity ma is
  port (spkkn : in integer; el : out std_logic_vector(2 downto 2); kmslvvdj : in real; eisyd : buffer real);
end ma;

library ieee;
use ieee.std_logic_1164.all;

architecture lcqopinlwv of ma is
  signal ovjj : std_logic;
  signal e : boolean;
  signal svtzfwjkep : boolean;
  signal kgedkje : time;
  signal zpilgjr : integer_vector(2 to 3);
  signal n : std_logic;
  signal mdmca : std_logic;
  signal eimmtnsbw : boolean;
begin
  pluyve : entity work.idjgrk
    port map (jz => eimmtnsbw, i => mdmca);
  cvurr : entity work.k
    port map (rkcvh => n, dbuucfve => zpilgjr, aeijtl => kgedkje);
  ew : entity work.idjgrk
    port map (jz => svtzfwjkep, i => mdmca);
  gg : entity work.idjgrk
    port map (jz => e, i => ovjj);
  
  -- Single-driven assignments
  eisyd <= 3_3_3.2;
  
  -- Multi-driven assignments
  el <= el;
  ovjj <= 'U';
  mdmca <= mdmca;
  mdmca <= 'H';
end lcqopinlwv;



-- Seed after: 4759581911053379072,4245627776430562977
