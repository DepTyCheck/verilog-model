-- Seed: 17424273283909322365,12143220691580258643

entity cgikosis is
  port (jvduwa : out bit_vector(1 to 0); zmtjlqdst : buffer bit_vector(3 downto 2));
end cgikosis;

architecture emvcionz of cgikosis is
  
begin
  -- Single-driven assignments
  zmtjlqdst <= zmtjlqdst;
  jvduwa <= (others => '0');
end emvcionz;

library ieee;
use ieee.std_logic_1164.all;

entity wvrvuruco is
  port (nzrwymo : out std_logic);
end wvrvuruco;

architecture fawe of wvrvuruco is
  signal gufwd : bit_vector(3 downto 2);
  signal kn : bit_vector(1 to 0);
begin
  hyzhlf : entity work.cgikosis
    port map (jvduwa => kn, zmtjlqdst => gufwd);
  
  -- Multi-driven assignments
  nzrwymo <= 'X';
  nzrwymo <= nzrwymo;
  nzrwymo <= nzrwymo;
  nzrwymo <= nzrwymo;
end fawe;

library ieee;
use ieee.std_logic_1164.all;

entity kne is
  port (kbhx : buffer std_logic_vector(2 to 0));
end kne;

architecture kfzq of kne is
  signal vsu : bit_vector(3 downto 2);
  signal kdvtnbzfy : bit_vector(1 to 0);
begin
  v : entity work.cgikosis
    port map (jvduwa => kdvtnbzfy, zmtjlqdst => vsu);
  
  -- Multi-driven assignments
  kbhx <= (others => '0');
end kfzq;

library ieee;
use ieee.std_logic_1164.all;

entity oaihpn is
  port (elov : inout std_logic; goipisia : linkage std_logic);
end oaihpn;

library ieee;
use ieee.std_logic_1164.all;

architecture ezm of oaihpn is
  signal xrxl : bit_vector(3 downto 2);
  signal zvtfjrlxg : bit_vector(1 to 0);
  signal msxud : std_logic_vector(2 to 0);
  signal dkyd : bit_vector(3 downto 2);
  signal ziupkqs : bit_vector(1 to 0);
begin
  nmpsxdyk : entity work.cgikosis
    port map (jvduwa => ziupkqs, zmtjlqdst => dkyd);
  bj : entity work.kne
    port map (kbhx => msxud);
  x : entity work.cgikosis
    port map (jvduwa => zvtfjrlxg, zmtjlqdst => xrxl);
  
  -- Multi-driven assignments
  elov <= 'H';
  elov <= 'X';
  elov <= 'H';
end ezm;



-- Seed after: 14914844080282052722,12143220691580258643
