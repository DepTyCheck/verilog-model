-- Seed: 201050065867258698,8068158652091157513

entity lzuijys is
  port (l : buffer character; mttbwebq : linkage severity_level; he : in time);
end lzuijys;

architecture szzmba of lzuijys is
  
begin
  -- Single-driven assignments
  l <= l;
end szzmba;

library ieee;
use ieee.std_logic_1164.all;

entity ssrrteiz is
  port (gafurqigh : linkage integer; mbnmbvimx : linkage std_logic_vector(4 downto 0); giuvgwwuk : linkage bit; yrdppnfd : out time);
end ssrrteiz;

architecture gvzosgyi of ssrrteiz is
  signal guqqrhmbnn : severity_level;
  signal rbnjxtgydo : character;
begin
  ncfkh : entity work.lzuijys
    port map (l => rbnjxtgydo, mttbwebq => guqqrhmbnn, he => yrdppnfd);
  
  -- Single-driven assignments
  yrdppnfd <= 1 hr;
end gvzosgyi;

library ieee;
use ieee.std_logic_1164.all;

entity ivktjfletj is
  port (dpomjwzpm : buffer std_logic_vector(0 downto 2); ud : inout real; ndeaerz : buffer integer);
end ivktjfletj;

library ieee;
use ieee.std_logic_1164.all;

architecture knrgb of ivktjfletj is
  signal uwv : time;
  signal jfxoxoh : severity_level;
  signal xyp : character;
  signal kob : time;
  signal esd : severity_level;
  signal vtkkbwl : character;
  signal slwgyuh : bit;
  signal lsphzxvv : std_logic_vector(4 downto 0);
  signal vv : integer;
  signal quim : time;
  signal ah : severity_level;
  signal vssh : character;
begin
  dqodyh : entity work.lzuijys
    port map (l => vssh, mttbwebq => ah, he => quim);
  ubcdilb : entity work.ssrrteiz
    port map (gafurqigh => vv, mbnmbvimx => lsphzxvv, giuvgwwuk => slwgyuh, yrdppnfd => quim);
  dzhlrqums : entity work.lzuijys
    port map (l => vtkkbwl, mttbwebq => esd, he => kob);
  bjoizwakbx : entity work.lzuijys
    port map (l => xyp, mttbwebq => jfxoxoh, he => uwv);
  
  -- Single-driven assignments
  ndeaerz <= 0;
  uwv <= quim;
  kob <= quim;
  ud <= 02.044;
  
  -- Multi-driven assignments
  dpomjwzpm <= (others => '0');
  dpomjwzpm <= dpomjwzpm;
end knrgb;



-- Seed after: 3505271564400278261,8068158652091157513
