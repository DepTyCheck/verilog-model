-- Seed: 17205962357324120189,16159265764638711791

library ieee;
use ieee.std_logic_1164.all;

entity znucjbzmv is
  port (ljyi : linkage time; nssau : out std_logic; hipkml : buffer real; gwlr : buffer integer_vector(4 to 3));
end znucjbzmv;

architecture znywvynk of znucjbzmv is
  
begin
  -- Single-driven assignments
  hipkml <= hipkml;
  gwlr <= (others => 0);
  
  -- Multi-driven assignments
  nssau <= 'U';
  nssau <= nssau;
  nssau <= nssau;
  nssau <= 'Z';
end znywvynk;

entity molx is
  port (snsjrkkc : out time);
end molx;

library ieee;
use ieee.std_logic_1164.all;

architecture yhjk of molx is
  signal wvnecp : integer_vector(4 to 3);
  signal haf : real;
  signal gctfiezvj : time;
  signal juji : integer_vector(4 to 3);
  signal wori : real;
  signal ea : std_logic;
  signal wj : time;
  signal kulqkta : integer_vector(4 to 3);
  signal nqhaxeuuid : real;
  signal ipdqnbbr : integer_vector(4 to 3);
  signal knrfbnhbti : real;
  signal pwg : std_logic;
  signal a : time;
begin
  umvoqrx : entity work.znucjbzmv
    port map (ljyi => a, nssau => pwg, hipkml => knrfbnhbti, gwlr => ipdqnbbr);
  f : entity work.znucjbzmv
    port map (ljyi => snsjrkkc, nssau => pwg, hipkml => nqhaxeuuid, gwlr => kulqkta);
  mwux : entity work.znucjbzmv
    port map (ljyi => wj, nssau => ea, hipkml => wori, gwlr => juji);
  pdcmv : entity work.znucjbzmv
    port map (ljyi => gctfiezvj, nssau => pwg, hipkml => haf, gwlr => wvnecp);
  
  -- Multi-driven assignments
  pwg <= pwg;
  pwg <= 'Z';
  pwg <= 'L';
end yhjk;



-- Seed after: 12242967428692093582,16159265764638711791
