-- Seed: 26986304201813661,6882842853887419669

entity hig is
  port (kukyize : in character; x : inout real);
end hig;

architecture jqb of hig is
  
begin
  -- Single-driven assignments
  x <= 2320.3_1;
end jqb;

library ieee;
use ieee.std_logic_1164.all;

entity udwrmaxz is
  port (ozgkpk : buffer integer; yb : in time; cjyogwnau : buffer integer; qbpwnlp : out std_logic_vector(0 to 3));
end udwrmaxz;

architecture sshakwir of udwrmaxz is
  signal n : real;
  signal fsljoncf : real;
  signal ltiqp : character;
begin
  ijhnk : entity work.hig
    port map (kukyize => ltiqp, x => fsljoncf);
  jnknlvl : entity work.hig
    port map (kukyize => ltiqp, x => n);
  
  -- Single-driven assignments
  cjyogwnau <= 3;
  ltiqp <= 'e';
  
  -- Multi-driven assignments
  qbpwnlp <= "UXL1";
end sshakwir;

entity kl is
  port (g : inout time; fbmr : buffer time; dsuxkygsg : out integer);
end kl;

library ieee;
use ieee.std_logic_1164.all;

architecture hmfadpp of kl is
  signal cedsurw : std_logic_vector(0 to 3);
  signal mtij : integer;
  signal nh : time;
  signal horylziloz : real;
  signal zcniytfsf : real;
  signal sepgrwo : real;
  signal wsdz : character;
begin
  pvouqhlmp : entity work.hig
    port map (kukyize => wsdz, x => sepgrwo);
  vrbnzeayxa : entity work.hig
    port map (kukyize => wsdz, x => zcniytfsf);
  kly : entity work.hig
    port map (kukyize => wsdz, x => horylziloz);
  qasj : entity work.udwrmaxz
    port map (ozgkpk => dsuxkygsg, yb => nh, cjyogwnau => mtij, qbpwnlp => cedsurw);
  
  -- Single-driven assignments
  wsdz <= 'l';
  nh <= 2 ms;
  g <= 16#D.8A14# us;
  
  -- Multi-driven assignments
  cedsurw <= ('W', 'X', 'Z', '-');
  cedsurw <= ('W', '1', '0', 'X');
end hmfadpp;

library ieee;
use ieee.std_logic_1164.all;

entity n is
  port (uzzvaoetcy : out std_logic_vector(0 to 4));
end n;

library ieee;
use ieee.std_logic_1164.all;

architecture ixf of n is
  signal bc : integer;
  signal m : integer;
  signal ciufgzd : time;
  signal geqxqvl : integer;
  signal pcpfu : std_logic_vector(0 to 3);
  signal hbajcy : integer;
  signal b : time;
  signal rowodrkzlg : integer;
begin
  amxncvqtup : entity work.udwrmaxz
    port map (ozgkpk => rowodrkzlg, yb => b, cjyogwnau => hbajcy, qbpwnlp => pcpfu);
  zlfdqdsj : entity work.udwrmaxz
    port map (ozgkpk => geqxqvl, yb => ciufgzd, cjyogwnau => m, qbpwnlp => pcpfu);
  xs : entity work.kl
    port map (g => b, fbmr => ciufgzd, dsuxkygsg => bc);
  
  -- Multi-driven assignments
  uzzvaoetcy <= ('H', 'Z', 'Z', 'U', '1');
  pcpfu <= ('0', 'U', 'L', 'W');
  pcpfu <= "--WW";
  uzzvaoetcy <= "11UUL";
end ixf;



-- Seed after: 3430288674877144600,6882842853887419669
