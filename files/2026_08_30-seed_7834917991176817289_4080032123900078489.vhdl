-- Seed: 7834917991176817289,4080032123900078489

entity yhqcrxryb is
  port (mhrwzst : buffer bit_vector(3 to 2));
end yhqcrxryb;

architecture squfgmgef of yhqcrxryb is
  
begin
  -- Single-driven assignments
  mhrwzst <= mhrwzst;
end squfgmgef;

entity znjyhjt is
  port (z : in severity_level; qnfrirgxlw : inout real; prbybj : out real);
end znjyhjt;

architecture mww of znjyhjt is
  signal ytjucelq : bit_vector(3 to 2);
  signal qwqp : bit_vector(3 to 2);
  signal fpng : bit_vector(3 to 2);
begin
  mn : entity work.yhqcrxryb
    port map (mhrwzst => fpng);
  tunq : entity work.yhqcrxryb
    port map (mhrwzst => qwqp);
  eym : entity work.yhqcrxryb
    port map (mhrwzst => ytjucelq);
  
  -- Single-driven assignments
  prbybj <= 4.3222;
end mww;

entity qjhvah is
  port (eksgnbnp : buffer character; igujbvt : out real; v : inout time; rbwegx : linkage real);
end qjhvah;

architecture nudpshusuk of qjhvah is
  signal xlyyyytviq : bit_vector(3 to 2);
  signal hevrwp : real;
  signal uftzm : severity_level;
begin
  tm : entity work.znjyhjt
    port map (z => uftzm, qnfrirgxlw => hevrwp, prbybj => igujbvt);
  zwfpy : entity work.yhqcrxryb
    port map (mhrwzst => xlyyyytviq);
end nudpshusuk;

entity bezaqa is
  port (uv : out real);
end bezaqa;

architecture br of bezaqa is
  
begin
  -- Single-driven assignments
  uv <= uv;
end br;



-- Seed after: 12523857445377001100,4080032123900078489
