// Seed: 7525814832334153908,14603487449988080319

module kp (output supply0 logic pgewn [3:4][4:3]);
  and br(saoclnith, hhts, saoclnith);
  
  
  // Multi-driven assignments
  assign hhts = 'bx;
  assign pgewn = '{'{'b1x01,'bxx},'{'bx,'bz}};
endmodule: kp

module gszqgncmf ();
  xnor gpbzwmnvrw(qo, s, gngotcdog);
  
  
  // Multi-driven assignments
  assign s = gngotcdog;
endmodule: gszqgncmf

module c (output bit [0:3][4:1][1:1][1:4] hdcw);
  // Unpacked net declarations
  supply0 logic dzcpc [3:4][4:3];
  
  xnor axvm(m, l, hdcw);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [0:3][4:1][1:1][1:4] hdcw -> logic hdcw
  
  kp njb(.pgewn(dzcpc));
  
  not t(orhhp, hdcw);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [0:3][4:1][1:1][1:4] hdcw -> logic hdcw
  
  xor nxxawu(hdcw, kcaohtrpma, l);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [0:3][4:1][1:1][1:4] hdcw -> logic hdcw
  
endmodule: c

module pktxzdkbc (output bit [2:0][1:2][0:2] ip [1:4]);
  xnor euccg(zka, xyhxmi, zka);
  
  
  // Single-driven assignments
  assign ip = ip;
  
  // Multi-driven assignments
  assign xyhxmi = 'b1;
  assign zka = zka;
  assign zka = zka;
  assign zka = 'b11;
endmodule: pktxzdkbc



// Seed after: 18005297433699622988,14603487449988080319
