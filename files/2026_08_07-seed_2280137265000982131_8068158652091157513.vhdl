-- Seed: 2280137265000982131,8068158652091157513

entity dmtvuuvew is
  port (fq : out time);
end dmtvuuvew;

architecture xm of dmtvuuvew is
  
begin
  -- Single-driven assignments
  fq <= 8#70024.07# fs;
end xm;

entity ieno is
  port (v : buffer integer; ygig : linkage bit_vector(2 downto 3));
end ieno;

architecture erbe of ieno is
  signal abll : time;
  signal ku : time;
  signal lofmhtrn : time;
  signal n : time;
begin
  czpckylou : entity work.dmtvuuvew
    port map (fq => n);
  syzl : entity work.dmtvuuvew
    port map (fq => lofmhtrn);
  dodwy : entity work.dmtvuuvew
    port map (fq => ku);
  ebvlsuzssj : entity work.dmtvuuvew
    port map (fq => abll);
  
  -- Single-driven assignments
  v <= v;
end erbe;

library ieee;
use ieee.std_logic_1164.all;

entity kx is
  port (c : in std_logic_vector(0 to 0));
end kx;

architecture ldilua of kx is
  signal gdcoorovec : time;
begin
  gpuk : entity work.dmtvuuvew
    port map (fq => gdcoorovec);
end ldilua;



-- Seed after: 6681816595012396839,8068158652091157513
