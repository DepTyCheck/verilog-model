-- Seed: 6091223944055897474,11181851762153539145

library ieee;
use ieee.std_logic_1164.all;

entity fhacgi is
  port (tyigaqtpve : buffer std_logic_vector(2 downto 0); biz : inout boolean_vector(4 downto 0); xf : out boolean_vector(2 to 1));
end fhacgi;



architecture ffe of fhacgi is
  
begin
  
end ffe;

library ieee;
use ieee.std_logic_1164.all;

entity raphhvsuiu is
  port (eniias : buffer std_logic_vector(1 to 0));
end raphhvsuiu;

library ieee;
use ieee.std_logic_1164.all;

architecture uhhpycv of raphhvsuiu is
  signal ixjnggvln : boolean_vector(2 to 1);
  signal cvqkrd : boolean_vector(4 downto 0);
  signal wpifmg : boolean_vector(2 to 1);
  signal wvvtwy : boolean_vector(4 downto 0);
  signal j : std_logic_vector(2 downto 0);
  signal ayaxqdf : boolean_vector(2 to 1);
  signal oxexhkxyff : boolean_vector(4 downto 0);
  signal ufktofuhn : boolean_vector(2 to 1);
  signal kx : boolean_vector(4 downto 0);
  signal tz : std_logic_vector(2 downto 0);
begin
  k : entity work.fhacgi
    port map (tyigaqtpve => tz, biz => kx, xf => ufktofuhn);
  lmbhqkv : entity work.fhacgi
    port map (tyigaqtpve => tz, biz => oxexhkxyff, xf => ayaxqdf);
  hskqzq : entity work.fhacgi
    port map (tyigaqtpve => j, biz => wvvtwy, xf => wpifmg);
  ovg : entity work.fhacgi
    port map (tyigaqtpve => tz, biz => cvqkrd, xf => ixjnggvln);
end uhhpycv;



-- Seed after: 13438027877262385070,11181851762153539145
