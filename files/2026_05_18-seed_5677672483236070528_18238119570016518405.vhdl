-- Seed: 5677672483236070528,18238119570016518405



entity h is
  port (bjl : inout integer; gjy : inout real_vector(2 downto 1));
end h;



architecture pzuukoq of h is
  
begin
  
end pzuukoq;

library ieee;
use ieee.std_logic_1164.all;

entity la is
  port (szckvbet : linkage real_vector(4 to 1); lrxoor : inout time; bqdclfgt : out std_logic);
end la;



architecture paulztud of la is
  
begin
  
end paulztud;

library ieee;
use ieee.std_logic_1164.all;

entity watuwum is
  port (oghbjim : in std_logic; jffssoccoe : buffer real_vector(2 downto 4));
end watuwum;

library ieee;
use ieee.std_logic_1164.all;

architecture jriuyderzd of watuwum is
  signal ljmx : real_vector(2 downto 1);
  signal wjbar : integer;
  signal ng : time;
  signal wvfd : std_logic;
  signal yxwq : time;
  signal i : real_vector(4 to 1);
  signal jqpmqidm : std_logic;
  signal vva : time;
  signal gnsfc : real_vector(4 to 1);
begin
  xfvcdsjbx : entity work.la
    port map (szckvbet => gnsfc, lrxoor => vva, bqdclfgt => jqpmqidm);
  c : entity work.la
    port map (szckvbet => i, lrxoor => yxwq, bqdclfgt => wvfd);
  oyn : entity work.la
    port map (szckvbet => i, lrxoor => ng, bqdclfgt => jqpmqidm);
  gedgffbvd : entity work.h
    port map (bjl => wjbar, gjy => ljmx);
end jriuyderzd;



-- Seed after: 13130329462267470863,18238119570016518405
