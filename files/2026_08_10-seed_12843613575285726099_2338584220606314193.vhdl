-- Seed: 12843613575285726099,2338584220606314193

library ieee;
use ieee.std_logic_1164.all;

entity xldadsb is
  port (aevtjmaie : linkage real; okdp : buffer std_logic_vector(0 downto 4); p : inout character; kprz : linkage integer_vector(4 to 0));
end xldadsb;

architecture sonoc of xldadsb is
  
begin
  -- Single-driven assignments
  p <= 't';
  
  -- Multi-driven assignments
  okdp <= okdp;
  okdp <= okdp;
  okdp <= "";
  okdp <= (others => '0');
end sonoc;

entity naksy is
  port (xwndcc : buffer real; frivvv : buffer real; akx : buffer time; imwh : out integer);
end naksy;

library ieee;
use ieee.std_logic_1164.all;

architecture k of naksy is
  signal uavphftic : integer_vector(4 to 0);
  signal wjecqszz : character;
  signal a : std_logic_vector(0 downto 4);
  signal jmlojiuul : real;
  signal xjwrlclnm : integer_vector(4 to 0);
  signal edck : character;
  signal vviarney : real;
  signal mdlhx : integer_vector(4 to 0);
  signal bgl : character;
  signal yc : integer_vector(4 to 0);
  signal ugltrctn : character;
  signal dzjubrbcbd : std_logic_vector(0 downto 4);
  signal r : real;
begin
  fni : entity work.xldadsb
    port map (aevtjmaie => r, okdp => dzjubrbcbd, p => ugltrctn, kprz => yc);
  zqpcbxrdw : entity work.xldadsb
    port map (aevtjmaie => frivvv, okdp => dzjubrbcbd, p => bgl, kprz => mdlhx);
  g : entity work.xldadsb
    port map (aevtjmaie => vviarney, okdp => dzjubrbcbd, p => edck, kprz => xjwrlclnm);
  htdlv : entity work.xldadsb
    port map (aevtjmaie => jmlojiuul, okdp => a, p => wjecqszz, kprz => uavphftic);
  
  -- Single-driven assignments
  akx <= akx;
  xwndcc <= frivvv;
  imwh <= 2#0_1#;
  
  -- Multi-driven assignments
  a <= dzjubrbcbd;
  dzjubrbcbd <= (others => '0');
  a <= "";
  dzjubrbcbd <= (others => '0');
end k;



-- Seed after: 5374802788017681313,2338584220606314193
