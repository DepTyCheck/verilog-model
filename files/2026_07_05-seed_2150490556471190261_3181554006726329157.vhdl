-- Seed: 2150490556471190261,3181554006726329157

use std.reflection.all;

entity xdvkvpmaok is
  port ( lhlu : inout protected_value_mirror
  ; qatzwhby : inout subtype_mirror
  ; mbgnbroj : inout integer_value_mirror
  ; iepfwdlcd : inout record_subtype_mirror
  );
end xdvkvpmaok;

architecture fycoyknneq of xdvkvpmaok is
  
begin
  
end fycoyknneq;

use std.reflection.all;

entity f is
  port (xz : inout subtype_mirror);
end f;

use std.reflection.all;

architecture gdy of f is
  shared variable lakly : record_subtype_mirror;
  shared variable fsyhpglq : integer_value_mirror;
  shared variable yd : protected_value_mirror;
  shared variable t : record_subtype_mirror;
  shared variable qdagvfhv : integer_value_mirror;
  shared variable mn : subtype_mirror;
  shared variable yoivlx : protected_value_mirror;
  shared variable mx : record_subtype_mirror;
  shared variable rsqtvkap : integer_value_mirror;
  shared variable m : subtype_mirror;
  shared variable inaqw : protected_value_mirror;
  shared variable xiyquoqoa : record_subtype_mirror;
  shared variable lvt : integer_value_mirror;
  shared variable xujhmfkz : subtype_mirror;
  shared variable fayjnhi : protected_value_mirror;
begin
  debuxkuvh : entity work.xdvkvpmaok
    port map (lhlu => fayjnhi, qatzwhby => xujhmfkz, mbgnbroj => lvt, iepfwdlcd => xiyquoqoa);
  gu : entity work.xdvkvpmaok
    port map (lhlu => inaqw, qatzwhby => m, mbgnbroj => rsqtvkap, iepfwdlcd => mx);
  sflk : entity work.xdvkvpmaok
    port map (lhlu => yoivlx, qatzwhby => mn, mbgnbroj => qdagvfhv, iepfwdlcd => t);
  ckulvm : entity work.xdvkvpmaok
    port map (lhlu => yd, qatzwhby => xz, mbgnbroj => fsyhpglq, iepfwdlcd => lakly);
end gdy;

use std.reflection.all;

entity jmf is
  port (ckryxupx : buffer severity_level; ecaaxpjuif : inout value_mirror);
end jmf;

use std.reflection.all;

architecture snqx of jmf is
  shared variable osotliv : record_subtype_mirror;
  shared variable htjuombegr : integer_value_mirror;
  shared variable fgh : subtype_mirror;
  shared variable ahs : protected_value_mirror;
  shared variable dsedyp : record_subtype_mirror;
  shared variable icivbbcjy : integer_value_mirror;
  shared variable ha : subtype_mirror;
  shared variable aaml : protected_value_mirror;
  shared variable pxf : record_subtype_mirror;
  shared variable ngbbsx : integer_value_mirror;
  shared variable s : subtype_mirror;
  shared variable yqnilb : protected_value_mirror;
begin
  imxlvxqit : entity work.xdvkvpmaok
    port map (lhlu => yqnilb, qatzwhby => s, mbgnbroj => ngbbsx, iepfwdlcd => pxf);
  vu : entity work.xdvkvpmaok
    port map (lhlu => aaml, qatzwhby => ha, mbgnbroj => icivbbcjy, iepfwdlcd => dsedyp);
  hbqvixubi : entity work.xdvkvpmaok
    port map (lhlu => ahs, qatzwhby => fgh, mbgnbroj => htjuombegr, iepfwdlcd => osotliv);
  
  -- Single-driven assignments
  ckryxupx <= ERROR;
end snqx;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity dncrcqxje is
  port (tesejflu : inout protected_value_mirror; fgjinbll : inout physical_subtype_mirror; nhg : inout std_logic_vector(0 downto 2); zrx : in time);
end dncrcqxje;

architecture dtmr of dncrcqxje is
  
begin
  -- Multi-driven assignments
  nhg <= nhg;
  nhg <= nhg;
  nhg <= "";
  nhg <= (others => '0');
end dtmr;



-- Seed after: 14610447119571490908,3181554006726329157
