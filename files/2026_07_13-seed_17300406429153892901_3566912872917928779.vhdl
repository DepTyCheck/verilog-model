-- Seed: 17300406429153892901,3566912872917928779

use std.reflection.all;

entity okngb is
  port (yuawv : inout physical_value_mirror; rwp : inout real; ksoapv : inout enumeration_subtype_mirror);
end okngb;

architecture hwcg of okngb is
  
begin
  
end hwcg;

use std.reflection.all;

entity dy is
  port (uasadjx : inout array_subtype_mirror);
end dy;

use std.reflection.all;

architecture sgrmmnpcep of dy is
  shared variable kyzu : enumeration_subtype_mirror;
  signal evjja : real;
  shared variable mxhvdw : physical_value_mirror;
  shared variable dfzt : enumeration_subtype_mirror;
  signal mdjbbcz : real;
  shared variable zucz : physical_value_mirror;
  shared variable fpidqei : enumeration_subtype_mirror;
  signal poot : real;
  shared variable kbiz : physical_value_mirror;
begin
  tb : entity work.okngb
    port map (yuawv => kbiz, rwp => poot, ksoapv => fpidqei);
  nr : entity work.okngb
    port map (yuawv => zucz, rwp => mdjbbcz, ksoapv => dfzt);
  dkrod : entity work.okngb
    port map (yuawv => mxhvdw, rwp => evjja, ksoapv => kyzu);
end sgrmmnpcep;

use std.reflection.all;

entity nxrgecnt is
  port (nyua : linkage character; sgcaj : inout access_value_mirror; dij : out integer);
end nxrgecnt;

architecture diijizgxn of nxrgecnt is
  
begin
  
end diijizgxn;



-- Seed after: 11920547232500134519,3566912872917928779
