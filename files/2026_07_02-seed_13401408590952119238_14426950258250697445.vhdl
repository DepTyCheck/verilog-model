-- Seed: 13401408590952119238,14426950258250697445

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity d is
  port (v : inout access_subtype_mirror; gy : inout character; bbzqrxkkaa : inout file_subtype_mirror; scmeyru : out std_logic);
end d;

architecture ebo of d is
  
begin
  -- Single-driven assignments
  gy <= 'c';
  
  -- Multi-driven assignments
  scmeyru <= scmeyru;
  scmeyru <= 'U';
  scmeyru <= scmeyru;
end ebo;

use std.reflection.all;

entity cucnopx is
  port (kwzwhvxlku : inout floating_subtype_mirror; zpy : inout file_subtype_mirror; aqpxrctm : inout enumeration_subtype_mirror);
end cucnopx;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture u of cucnopx is
  signal zl : character;
  shared variable zgii : access_subtype_mirror;
  shared variable dto : file_subtype_mirror;
  signal cn : character;
  shared variable rhhd : access_subtype_mirror;
  signal kwtrqlylws : std_logic;
  shared variable rs : file_subtype_mirror;
  signal disn : character;
  shared variable qhsxptpbrl : access_subtype_mirror;
  signal gmhnlbwiq : std_logic;
  shared variable ynccyv : file_subtype_mirror;
  signal xhqvd : character;
  shared variable mnovtg : access_subtype_mirror;
begin
  gml : entity work.d
    port map (v => mnovtg, gy => xhqvd, bbzqrxkkaa => ynccyv, scmeyru => gmhnlbwiq);
  wuckbi : entity work.d
    port map (v => qhsxptpbrl, gy => disn, bbzqrxkkaa => rs, scmeyru => kwtrqlylws);
  sbz : entity work.d
    port map (v => rhhd, gy => cn, bbzqrxkkaa => dto, scmeyru => kwtrqlylws);
  doglccpw : entity work.d
    port map (v => zgii, gy => zl, bbzqrxkkaa => zpy, scmeyru => gmhnlbwiq);
  
  -- Multi-driven assignments
  gmhnlbwiq <= gmhnlbwiq;
  gmhnlbwiq <= '-';
end u;

entity zluzppqryy is
  port (ot : inout bit);
end zluzppqryy;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture yqpvgbv of zluzppqryy is
  shared variable b : enumeration_subtype_mirror;
  shared variable wjjlpcnlj : file_subtype_mirror;
  shared variable eoeun : floating_subtype_mirror;
  shared variable bfocjb : file_subtype_mirror;
  signal evurjgey : character;
  shared variable m : access_subtype_mirror;
  shared variable mqnb : file_subtype_mirror;
  signal uxzwhty : character;
  shared variable xeaogpdrbq : access_subtype_mirror;
  signal wngxerifr : std_logic;
  shared variable pxmt : file_subtype_mirror;
  signal tdy : character;
  shared variable pyx : access_subtype_mirror;
begin
  noio : entity work.d
    port map (v => pyx, gy => tdy, bbzqrxkkaa => pxmt, scmeyru => wngxerifr);
  tdbyuymp : entity work.d
    port map (v => xeaogpdrbq, gy => uxzwhty, bbzqrxkkaa => mqnb, scmeyru => wngxerifr);
  davuhhjsx : entity work.d
    port map (v => m, gy => evurjgey, bbzqrxkkaa => bfocjb, scmeyru => wngxerifr);
  zdpsghnre : entity work.cucnopx
    port map (kwzwhvxlku => eoeun, zpy => wjjlpcnlj, aqpxrctm => b);
  
  -- Single-driven assignments
  ot <= '0';
end yqpvgbv;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity qbajqwkdg is
  port (axdgvvmb : buffer boolean; tkkyyo : inout access_value_mirror; y : out std_logic; fq : inout integer_value_mirror);
end qbajqwkdg;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture glrlofbhih of qbajqwkdg is
  shared variable qurbko : file_subtype_mirror;
  signal wrjqr : character;
  shared variable mcenfmqaf : access_subtype_mirror;
  signal l : std_logic;
  shared variable vrk : file_subtype_mirror;
  signal veujrxn : character;
  shared variable utm : access_subtype_mirror;
  signal mqgb : bit;
begin
  jhnliikf : entity work.zluzppqryy
    port map (ot => mqgb);
  kx : entity work.d
    port map (v => utm, gy => veujrxn, bbzqrxkkaa => vrk, scmeyru => l);
  fyymlmsbb : entity work.d
    port map (v => mcenfmqaf, gy => wrjqr, bbzqrxkkaa => qurbko, scmeyru => y);
  
  -- Single-driven assignments
  axdgvvmb <= axdgvvmb;
  
  -- Multi-driven assignments
  y <= 'H';
  y <= y;
  y <= y;
end glrlofbhih;



-- Seed after: 16384098469438617474,14426950258250697445
