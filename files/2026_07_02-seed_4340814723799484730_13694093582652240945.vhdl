-- Seed: 4340814723799484730,13694093582652240945

entity a is
  port (oq : inout severity_level);
end a;

architecture ozypq of a is
  
begin
  
end ozypq;

entity mcmnas is
  port ( kmdhxm : in real
  ; ukzstmxuna : linkage integer_vector(0 to 2)
  ; zbylwwooq : linkage time_vector(4 downto 3)
  ; fpocdhkj : out real_vector(4 downto 4)
  );
end mcmnas;

architecture msaviddi of mcmnas is
  signal itfigw : severity_level;
  signal dfjpqkhuf : severity_level;
  signal h : severity_level;
  signal t : severity_level;
begin
  sqo : entity work.a
    port map (oq => t);
  jzeceh : entity work.a
    port map (oq => h);
  idkwb : entity work.a
    port map (oq => dfjpqkhuf);
  fc : entity work.a
    port map (oq => itfigw);
  
  -- Single-driven assignments
  fpocdhkj <= (others => 0_4_3_2_3.2_1_4_2);
end msaviddi;

entity tcpy is
  port (i : out real);
end tcpy;

architecture skxflx of tcpy is
  signal ajljcy : severity_level;
  signal dzwki : severity_level;
  signal doein : severity_level;
  signal jan : real_vector(4 downto 4);
  signal crs : time_vector(4 downto 3);
  signal rcp : integer_vector(0 to 2);
  signal mbh : real;
begin
  xawclqpq : entity work.mcmnas
    port map (kmdhxm => mbh, ukzstmxuna => rcp, zbylwwooq => crs, fpocdhkj => jan);
  yhgjkqv : entity work.a
    port map (oq => doein);
  s : entity work.a
    port map (oq => dzwki);
  kn : entity work.a
    port map (oq => ajljcy);
  
  -- Single-driven assignments
  i <= 8#2.0#;
end skxflx;

library ieee;
use ieee.std_logic_1164.all;

entity cplybzczc is
  port (yzyqjpy : inout integer; qfeevarsat : in std_logic_vector(2 to 1));
end cplybzczc;

architecture tymvichirx of cplybzczc is
  signal clobkbvc : severity_level;
  signal sfxlfal : real_vector(4 downto 4);
  signal wfkuxfox : time_vector(4 downto 3);
  signal i : integer_vector(0 to 2);
  signal s : real;
  signal ioen : severity_level;
begin
  jstzlydtfo : entity work.a
    port map (oq => ioen);
  fd : entity work.mcmnas
    port map (kmdhxm => s, ukzstmxuna => i, zbylwwooq => wfkuxfox, fpocdhkj => sfxlfal);
  wdsm : entity work.a
    port map (oq => clobkbvc);
  
  -- Single-driven assignments
  yzyqjpy <= 43411;
  s <= 314.211;
end tymvichirx;



-- Seed after: 4298047436655425316,13694093582652240945
