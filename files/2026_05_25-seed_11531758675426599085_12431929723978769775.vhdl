-- Seed: 11531758675426599085,12431929723978769775

library ieee;
use ieee.std_logic_1164.all;

entity l is
  port (fl : buffer std_logic; apiff : out integer; z : in time_vector(3 to 3));
end l;



architecture vt of l is
  
begin
  
end vt;

library ieee;
use ieee.std_logic_1164.all;

entity mzwetld is
  port (mn : buffer boolean; qbqnxt : inout std_logic_vector(3 downto 2); jiq : in severity_level);
end mzwetld;

library ieee;
use ieee.std_logic_1164.all;

architecture mbhouss of mzwetld is
  signal gpuc : time_vector(3 to 3);
  signal hgtuchezm : integer;
  signal vx : std_logic;
  signal deotnrmq : time_vector(3 to 3);
  signal trmpawv : integer;
  signal zjqpgmod : std_logic;
begin
  itjedzfxc : entity work.l
    port map (fl => zjqpgmod, apiff => trmpawv, z => deotnrmq);
  zstkcudxu : entity work.l
    port map (fl => vx, apiff => hgtuchezm, z => gpuc);
end mbhouss;

library ieee;
use ieee.std_logic_1164.all;

entity szexkrbkzj is
  port (orxmoemhvv : inout bit_vector(3 to 4); fjuchka : inout real; smty : linkage std_logic_vector(2 to 0));
end szexkrbkzj;

library ieee;
use ieee.std_logic_1164.all;

architecture beigyq of szexkrbkzj is
  signal fig : time_vector(3 to 3);
  signal nkpksoomla : integer;
  signal koc : std_logic;
  signal tjwyzpn : time_vector(3 to 3);
  signal iqxtevjdvn : integer;
  signal vi : std_logic;
  signal shebkqkxog : time_vector(3 to 3);
  signal jeham : integer;
  signal nkw : std_logic;
begin
  dwvawpb : entity work.l
    port map (fl => nkw, apiff => jeham, z => shebkqkxog);
  fwo : entity work.l
    port map (fl => vi, apiff => iqxtevjdvn, z => tjwyzpn);
  hxpgbdj : entity work.l
    port map (fl => koc, apiff => nkpksoomla, z => fig);
end beigyq;



-- Seed after: 1132173498345992900,12431929723978769775
