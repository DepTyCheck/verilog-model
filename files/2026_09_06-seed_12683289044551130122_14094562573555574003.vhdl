-- Seed: 12683289044551130122,14094562573555574003

library ieee;
use ieee.std_logic_1164.all;

entity i is
  port (plxb : inout time; bchlla : linkage std_logic_vector(3 to 1); hkagdsxw : linkage std_logic_vector(4 to 3));
end i;

architecture ls of i is
  
begin
  
end ls;

entity x is
  port (uvuqtuusko : linkage integer; kphgum : out severity_level; nvoobp : buffer boolean_vector(2 downto 3));
end x;

architecture rubkukqyzl of x is
  
begin
  -- Single-driven assignments
  nvoobp <= nvoobp;
  kphgum <= kphgum;
end rubkukqyzl;

library ieee;
use ieee.std_logic_1164.all;

entity mz is
  port (ivzxpbbgc : linkage std_logic_vector(2 downto 4));
end mz;

architecture roly of mz is
  signal ybqpby : time;
begin
  ebobexo : entity work.i
    port map (plxb => ybqpby, bchlla => ivzxpbbgc, hkagdsxw => ivzxpbbgc);
end roly;

entity vahppolv is
  port (bhiiw : buffer time);
end vahppolv;

architecture gw of vahppolv is
  signal bcr : boolean_vector(2 downto 3);
  signal ydtgvztw : severity_level;
  signal nw : integer;
  signal qff : boolean_vector(2 downto 3);
  signal bhlw : severity_level;
  signal pfdfj : integer;
begin
  tj : entity work.x
    port map (uvuqtuusko => pfdfj, kphgum => bhlw, nvoobp => qff);
  ntgbr : entity work.x
    port map (uvuqtuusko => nw, kphgum => ydtgvztw, nvoobp => bcr);
  
  -- Single-driven assignments
  bhiiw <= bhiiw;
end gw;



-- Seed after: 16132266343022190604,14094562573555574003
