-- Seed: 6424178328051306323,662889661651915549

entity yh is
  port (bvwdyfox : buffer boolean; gowtj : buffer boolean_vector(3 to 2));
end yh;

architecture infiutggod of yh is
  
begin
  -- Single-driven assignments
  gowtj <= gowtj;
  bvwdyfox <= TRUE;
end infiutggod;

library ieee;
use ieee.std_logic_1164.all;

entity lnmjpim is
  port (fydfqrihg : inout std_logic_vector(0 to 1); hg : linkage real_vector(1 to 0); nt : linkage severity_level; ke : in std_logic);
end lnmjpim;

architecture yw of lnmjpim is
  signal toeunc : boolean_vector(3 to 2);
  signal t : boolean;
  signal omrans : boolean_vector(3 to 2);
  signal rrw : boolean;
  signal v : boolean_vector(3 to 2);
  signal ogptpuqwm : boolean;
begin
  sxbpxrou : entity work.yh
    port map (bvwdyfox => ogptpuqwm, gowtj => v);
  pjvkl : entity work.yh
    port map (bvwdyfox => rrw, gowtj => omrans);
  uunsbc : entity work.yh
    port map (bvwdyfox => t, gowtj => toeunc);
  
  -- Multi-driven assignments
  fydfqrihg <= ('Z', 'X');
  fydfqrihg <= ('-', '-');
  fydfqrihg <= "WX";
end yw;

library ieee;
use ieee.std_logic_1164.all;

entity tjsmxkrcgd is
  port (quzcmbwl : in time; etwopki : linkage std_logic; f : buffer real; cbfmrnbthh : in real);
end tjsmxkrcgd;

architecture mnp of tjsmxkrcgd is
  
begin
  -- Single-driven assignments
  f <= f;
end mnp;

entity qbvppuj is
  port (lru : out time; jsmbveq : out real);
end qbvppuj;

library ieee;
use ieee.std_logic_1164.all;

architecture xr of qbvppuj is
  signal stnqhyqas : real;
  signal gqkgozj : real;
  signal jh : std_logic;
  signal ujnrn : time;
  signal trp : boolean_vector(3 to 2);
  signal tal : boolean;
  signal gvg : boolean_vector(3 to 2);
  signal lwtbve : boolean;
  signal r : boolean_vector(3 to 2);
  signal uhqdlqio : boolean;
begin
  pggoxt : entity work.yh
    port map (bvwdyfox => uhqdlqio, gowtj => r);
  tznlit : entity work.yh
    port map (bvwdyfox => lwtbve, gowtj => gvg);
  urwizszpf : entity work.yh
    port map (bvwdyfox => tal, gowtj => trp);
  lqtgzzkxqy : entity work.tjsmxkrcgd
    port map (quzcmbwl => ujnrn, etwopki => jh, f => gqkgozj, cbfmrnbthh => stnqhyqas);
  
  -- Single-driven assignments
  lru <= lru;
  jsmbveq <= jsmbveq;
  ujnrn <= lru;
  
  -- Multi-driven assignments
  jh <= jh;
end xr;



-- Seed after: 16213523770993318771,662889661651915549
