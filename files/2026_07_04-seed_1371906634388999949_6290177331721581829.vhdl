-- Seed: 1371906634388999949,6290177331721581829

use std.reflection.all;

entity wtosl is
  port (ziur : buffer time; ruucv : inout protected_value_mirror; sjxzzc : inout subtype_mirror);
end wtosl;

architecture chy of wtosl is
  
begin
  -- Single-driven assignments
  ziur <= ziur;
end chy;

use std.reflection.all;

entity mztuuhgj is
  port (dty : inout subtype_mirror; gvarsdzfjm : inout bit);
end mztuuhgj;

use std.reflection.all;

architecture wijfpkdh of mztuuhgj is
  shared variable lzvtmlsxem : subtype_mirror;
  shared variable ss : protected_value_mirror;
  signal wqu : time;
  shared variable ckufgdhgp : protected_value_mirror;
  signal afhae : time;
  shared variable e : subtype_mirror;
  shared variable rzkt : protected_value_mirror;
  signal bpnqiaccg : time;
begin
  fie : entity work.wtosl
    port map (ziur => bpnqiaccg, ruucv => rzkt, sjxzzc => e);
  szc : entity work.wtosl
    port map (ziur => afhae, ruucv => ckufgdhgp, sjxzzc => dty);
  ojpaytvad : entity work.wtosl
    port map (ziur => wqu, ruucv => ss, sjxzzc => lzvtmlsxem);
  
  -- Single-driven assignments
  gvarsdzfjm <= '1';
end wijfpkdh;

entity oeujrlmhq is
  port (tbij : in real);
end oeujrlmhq;

use std.reflection.all;

architecture dxmijyd of oeujrlmhq is
  shared variable tbrz : subtype_mirror;
  shared variable ksmloomvz : protected_value_mirror;
  signal ncucm : time;
  shared variable bgpib : subtype_mirror;
  shared variable gzsyt : protected_value_mirror;
  signal loauqc : time;
  shared variable zbp : subtype_mirror;
  shared variable gfgc : protected_value_mirror;
  signal vvzui : time;
begin
  j : entity work.wtosl
    port map (ziur => vvzui, ruucv => gfgc, sjxzzc => zbp);
  bpsyigp : entity work.wtosl
    port map (ziur => loauqc, ruucv => gzsyt, sjxzzc => bgpib);
  hbpazs : entity work.wtosl
    port map (ziur => ncucm, ruucv => ksmloomvz, sjxzzc => tbrz);
end dxmijyd;

use std.reflection.all;

entity yhpcrftcfc is
  port (tvkla : inout record_subtype_mirror);
end yhpcrftcfc;

use std.reflection.all;

architecture knvct of yhpcrftcfc is
  shared variable nhckpaxzi : subtype_mirror;
  shared variable lnaeck : protected_value_mirror;
  signal glgqiegnj : time;
  shared variable kqibfti : subtype_mirror;
  shared variable p : protected_value_mirror;
  signal vozaruk : time;
  shared variable fttdzzuq : subtype_mirror;
  shared variable sqlfss : protected_value_mirror;
  signal fept : time;
begin
  urt : entity work.wtosl
    port map (ziur => fept, ruucv => sqlfss, sjxzzc => fttdzzuq);
  opmek : entity work.wtosl
    port map (ziur => vozaruk, ruucv => p, sjxzzc => kqibfti);
  meqon : entity work.wtosl
    port map (ziur => glgqiegnj, ruucv => lnaeck, sjxzzc => nhckpaxzi);
end knvct;



-- Seed after: 8296806762717890923,6290177331721581829
