-- Seed: 787557374899884026,5805648483995786113

library ieee;
use ieee.std_logic_1164.all;

entity dmaktq is
  port (xm : out real; rzatoxsz : buffer std_logic; etphp : in time; lixp : in character);
end dmaktq;

architecture vc of dmaktq is
  
begin
  -- Single-driven assignments
  xm <= xm;
  
  -- Multi-driven assignments
  rzatoxsz <= '-';
  rzatoxsz <= rzatoxsz;
  rzatoxsz <= 'H';
  rzatoxsz <= 'Z';
end vc;

library ieee;
use ieee.std_logic_1164.all;

entity eo is
  port (plpqdgq : in time; zowe : out real; c : linkage std_logic_vector(1 downto 2); ny : out time);
end eo;

library ieee;
use ieee.std_logic_1164.all;

architecture ifdell of eo is
  signal rqzhpx : character;
  signal wxywdflcd : real;
  signal ampui : character;
  signal gqov : time;
  signal m : std_logic;
  signal sdfwacfwr : real;
begin
  rkwckwgbfm : entity work.dmaktq
    port map (xm => sdfwacfwr, rzatoxsz => m, etphp => gqov, lixp => ampui);
  aknwyj : entity work.dmaktq
    port map (xm => wxywdflcd, rzatoxsz => m, etphp => ny, lixp => rqzhpx);
  
  -- Multi-driven assignments
  m <= 'X';
  m <= m;
  m <= m;
end ifdell;

entity ypkocj is
  port (zmsmevm : inout boolean; s : inout time; gl : out time; daff : out boolean);
end ypkocj;

library ieee;
use ieee.std_logic_1164.all;

architecture l of ypkocj is
  signal dxrcu : character;
  signal hupgj : time;
  signal dd : std_logic;
  signal nnhdmk : real;
begin
  spmlifl : entity work.dmaktq
    port map (xm => nnhdmk, rzatoxsz => dd, etphp => hupgj, lixp => dxrcu);
  
  -- Single-driven assignments
  gl <= 8#5416.7# us;
  zmsmevm <= FALSE;
  
  -- Multi-driven assignments
  dd <= dd;
  dd <= '-';
  dd <= '-';
end l;

library ieee;
use ieee.std_logic_1164.all;

entity u is
  port (mupdjywguu : linkage integer; bdnhvqjwc : buffer real; bpikn : in std_logic_vector(4 to 3));
end u;

library ieee;
use ieee.std_logic_1164.all;

architecture lsdqdsqr of u is
  signal ye : boolean;
  signal tuismpchby : time;
  signal y : time;
  signal arxap : boolean;
  signal tzauh : time;
  signal mbdy : character;
  signal ug : time;
  signal e : std_logic;
  signal ucxwlpiaac : real;
  signal wcnuvoqmv : time;
  signal enhvbhhitv : real;
  signal zfe : time;
begin
  ytlob : entity work.eo
    port map (plpqdgq => zfe, zowe => enhvbhhitv, c => bpikn, ny => wcnuvoqmv);
  oemlyn : entity work.dmaktq
    port map (xm => ucxwlpiaac, rzatoxsz => e, etphp => ug, lixp => mbdy);
  ffbdg : entity work.eo
    port map (plpqdgq => tzauh, zowe => bdnhvqjwc, c => bpikn, ny => zfe);
  rdbprzsgl : entity work.ypkocj
    port map (zmsmevm => arxap, s => y, gl => tuismpchby, daff => ye);
  
  -- Single-driven assignments
  ug <= zfe;
  tzauh <= zfe;
  mbdy <= mbdy;
  
  -- Multi-driven assignments
  e <= 'U';
  e <= 'L';
  e <= e;
  e <= e;
end lsdqdsqr;



-- Seed after: 6844833122749965645,5805648483995786113
