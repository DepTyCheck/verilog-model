-- Seed: 10569783435508539610,16265041255589496407



entity fjyvi is
  port (d : out real; f : inout character);
end fjyvi;



architecture iqrexqn of fjyvi is
  
begin
  
end iqrexqn;

library ieee;
use ieee.std_logic_1164.all;

entity bzss is
  port (lfmn : in severity_level; idotfkdbh : linkage boolean; pk : in std_logic_vector(2 downto 3); fz : out bit_vector(4 downto 3));
end bzss;



architecture sbciazdtq of bzss is
  signal jl : character;
  signal sc : real;
  signal q : character;
  signal ztw : real;
  signal sstndjf : character;
  signal mp : real;
  signal io : character;
  signal zubpjg : real;
begin
  klgc : entity work.fjyvi
    port map (d => zubpjg, f => io);
  fwhhgjwxth : entity work.fjyvi
    port map (d => mp, f => sstndjf);
  pw : entity work.fjyvi
    port map (d => ztw, f => q);
  efjb : entity work.fjyvi
    port map (d => sc, f => jl);
end sbciazdtq;



-- Seed after: 5454307341685009746,16265041255589496407
