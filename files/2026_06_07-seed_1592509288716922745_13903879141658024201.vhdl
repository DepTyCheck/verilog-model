-- Seed: 1592509288716922745,13903879141658024201

library ieee;
use ieee.std_logic_1164.all;

entity val is
  port (tuw : linkage boolean_vector(4 downto 3); gvb : linkage std_logic; d : out std_logic_vector(1 downto 1); zpmdt : in time);
end val;



architecture vzjxdm of val is
  
begin
  
end vzjxdm;



entity g is
  port (iyh : buffer boolean_vector(3 downto 1); mqroepiz : in severity_level; ctqecjkn : linkage integer);
end g;

library ieee;
use ieee.std_logic_1164.all;

architecture xb of g is
  signal szxo : time;
  signal zluurpwq : std_logic_vector(1 downto 1);
  signal opjkhxxid : std_logic;
  signal jfr : time;
  signal q : std_logic_vector(1 downto 1);
  signal bypf : std_logic;
  signal oxcszggbv : boolean_vector(4 downto 3);
begin
  dihojfbetn : entity work.val
    port map (tuw => oxcszggbv, gvb => bypf, d => q, zpmdt => jfr);
  jbltwed : entity work.val
    port map (tuw => oxcszggbv, gvb => opjkhxxid, d => zluurpwq, zpmdt => szxo);
end xb;



-- Seed after: 14358896561145481508,13903879141658024201
