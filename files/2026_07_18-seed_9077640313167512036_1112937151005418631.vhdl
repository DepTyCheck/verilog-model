-- Seed: 9077640313167512036,1112937151005418631

library ieee;
use ieee.std_logic_1164.all;

entity fx is
  port (nac : buffer std_logic; bu : in integer_vector(0 to 0); nxaykklog : buffer time; rgiqeipla : in integer_vector(1 downto 4));
end fx;

architecture so of fx is
  
begin
  -- Single-driven assignments
  nxaykklog <= nxaykklog;
  
  -- Multi-driven assignments
  nac <= nac;
end so;

entity yapz is
  port (jshdwg : inout bit);
end yapz;

library ieee;
use ieee.std_logic_1164.all;

architecture cotdkcqrr of yapz is
  signal ykzya : time;
  signal imfmw : integer_vector(0 to 0);
  signal kjqtef : std_logic;
  signal q : integer_vector(1 downto 4);
  signal e : time;
  signal koy : integer_vector(0 to 0);
  signal tlvpjjl : std_logic;
begin
  yrztmkayaf : entity work.fx
    port map (nac => tlvpjjl, bu => koy, nxaykklog => e, rgiqeipla => q);
  dqlgkzk : entity work.fx
    port map (nac => kjqtef, bu => imfmw, nxaykklog => ykzya, rgiqeipla => q);
  
  -- Single-driven assignments
  q <= q;
  
  -- Multi-driven assignments
  tlvpjjl <= '-';
  tlvpjjl <= tlvpjjl;
  tlvpjjl <= tlvpjjl;
  tlvpjjl <= 'H';
end cotdkcqrr;

entity zoapi is
  port (txgt : in real; zjzwhovhun : buffer bit; pnekxf : inout time);
end zoapi;

library ieee;
use ieee.std_logic_1164.all;

architecture fsensmf of zoapi is
  signal skysiqifl : integer_vector(0 to 0);
  signal oaz : bit;
  signal uifzyeju : integer_vector(1 downto 4);
  signal znqdfs : time;
  signal h : integer_vector(0 to 0);
  signal hsn : std_logic;
begin
  hehkue : entity work.fx
    port map (nac => hsn, bu => h, nxaykklog => znqdfs, rgiqeipla => uifzyeju);
  fdtipcruh : entity work.yapz
    port map (jshdwg => oaz);
  igjhmgd : entity work.fx
    port map (nac => hsn, bu => skysiqifl, nxaykklog => pnekxf, rgiqeipla => uifzyeju);
  
  -- Single-driven assignments
  h <= (others => 3_0_1_1);
  skysiqifl <= skysiqifl;
  uifzyeju <= (others => 0);
  zjzwhovhun <= '0';
  
  -- Multi-driven assignments
  hsn <= hsn;
  hsn <= hsn;
end fsensmf;



-- Seed after: 14078258092676628801,1112937151005418631
