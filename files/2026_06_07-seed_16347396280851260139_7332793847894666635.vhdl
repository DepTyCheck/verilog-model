-- Seed: 16347396280851260139,7332793847894666635

library ieee;
use ieee.std_logic_1164.all;

entity e is
  port (tdiumvhlgx : out integer; rlxwldg : buffer std_logic_vector(2 to 2); nct : out std_logic_vector(3 to 1); qryipueg : buffer std_logic);
end e;



architecture a of e is
  
begin
  
end a;

library ieee;
use ieee.std_logic_1164.all;

entity ksgllr is
  port (frffv : buffer std_logic; izjw : out bit_vector(3 to 2));
end ksgllr;

library ieee;
use ieee.std_logic_1164.all;

architecture tpkl of ksgllr is
  signal oum : std_logic;
  signal dxritsfcf : std_logic_vector(3 to 1);
  signal coxljm : std_logic_vector(2 to 2);
  signal rtunmyanai : integer;
  signal yhjjyynqbb : std_logic_vector(3 to 1);
  signal ixofh : integer;
  signal yclo : std_logic_vector(3 to 1);
  signal syu : std_logic_vector(2 to 2);
  signal znp : integer;
begin
  rme : entity work.e
    port map (tdiumvhlgx => znp, rlxwldg => syu, nct => yclo, qryipueg => frffv);
  rxngx : entity work.e
    port map (tdiumvhlgx => ixofh, rlxwldg => syu, nct => yhjjyynqbb, qryipueg => frffv);
  qqsdxkqpo : entity work.e
    port map (tdiumvhlgx => rtunmyanai, rlxwldg => coxljm, nct => dxritsfcf, qryipueg => oum);
end tpkl;



-- Seed after: 14044856510322962065,7332793847894666635
