-- Seed: 6065104098330155801,16715549879197889543

library ieee;
use ieee.std_logic_1164.all;

entity wga is
  port (cb : out integer; ouopfkysjq : inout std_logic_vector(3 to 2); yx : inout time; los : out integer);
end wga;



architecture vtzk of wga is
  
begin
  
end vtzk;



entity fmjoyrdh is
  port (uqfdoyu : in time_vector(4 to 3));
end fmjoyrdh;

library ieee;
use ieee.std_logic_1164.all;

architecture oz of fmjoyrdh is
  signal wsuiqoy : integer;
  signal lxrf : time;
  signal at : std_logic_vector(3 to 2);
  signal bbomsplcj : integer;
  signal hqom : integer;
  signal auhsk : time;
  signal ot : std_logic_vector(3 to 2);
  signal l : integer;
  signal afmvcax : integer;
  signal wnptc : time;
  signal oksdrkqix : std_logic_vector(3 to 2);
  signal hbjkvm : integer;
begin
  beb : entity work.wga
    port map (cb => hbjkvm, ouopfkysjq => oksdrkqix, yx => wnptc, los => afmvcax);
  mddvrpev : entity work.wga
    port map (cb => l, ouopfkysjq => ot, yx => auhsk, los => hqom);
  tyfr : entity work.wga
    port map (cb => bbomsplcj, ouopfkysjq => at, yx => lxrf, los => wsuiqoy);
end oz;



-- Seed after: 10687253235718663226,16715549879197889543
