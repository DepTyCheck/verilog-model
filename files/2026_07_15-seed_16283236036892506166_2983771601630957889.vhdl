-- Seed: 16283236036892506166,2983771601630957889

use std.reflection.all;

entity yagdje is
  port (variable ft : inout access_subtype_mirror_pt; kvkk : inout time; variable qxlvlawibb : inout access_subtype_mirror_pt);
end yagdje;

architecture a of yagdje is
  
begin
  
end a;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity xmwjyiya is
  port ( variable tnyjkfqixa : inout integer_value_mirror_pt
  ; variable dhhw : inout record_subtype_mirror_pt
  ; pgxflw : buffer integer
  ; s : buffer std_logic_vector(1 to 4)
  );
end xmwjyiya;

use std.reflection.all;

architecture zfdhh of xmwjyiya is
  shared variable dwnkif : access_subtype_mirror_pt;
  signal vapbfoits : time;
  shared variable fv : access_subtype_mirror_pt;
  shared variable rdvlrx : access_subtype_mirror_pt;
  signal rqwpyapg : time;
  shared variable vjezj : access_subtype_mirror_pt;
  shared variable layutjir : access_subtype_mirror_pt;
  signal khfexg : time;
  shared variable ehjuvs : access_subtype_mirror_pt;
  shared variable ksbiqfkso : access_subtype_mirror_pt;
  signal qpzoljo : time;
  shared variable fj : access_subtype_mirror_pt;
begin
  cqyy : entity work.yagdje
    port map (ft => fj, kvkk => qpzoljo, qxlvlawibb => ksbiqfkso);
  umgdi : entity work.yagdje
    port map (ft => ehjuvs, kvkk => khfexg, qxlvlawibb => layutjir);
  ctuyuucy : entity work.yagdje
    port map (ft => vjezj, kvkk => rqwpyapg, qxlvlawibb => rdvlrx);
  utxsnz : entity work.yagdje
    port map (ft => fv, kvkk => vapbfoits, qxlvlawibb => dwnkif);
  
  -- Multi-driven assignments
  s <= s;
end zfdhh;

entity awoapcbur is
  port (nufx : linkage time; gq : out boolean_vector(4 to 2));
end awoapcbur;

architecture qbqsc of awoapcbur is
  
begin
  -- Single-driven assignments
  gq <= gq;
end qbqsc;



-- Seed after: 506397184079913026,2983771601630957889
