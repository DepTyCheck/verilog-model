-- Seed: 2978278983765751867,12359743974512393525

entity jfrugge is
  port (mudyoyhojv : linkage integer; vqyt : out real_vector(3 to 2); z : buffer severity_level);
end jfrugge;

architecture medqi of jfrugge is
  
begin
  -- Single-driven assignments
  z <= WARNING;
  vqyt <= vqyt;
end medqi;

entity zqpftiyc is
  port (brxnme : out character; lytdajv : linkage real; hwnu : out integer);
end zqpftiyc;

architecture fjnpd of zqpftiyc is
  signal eevfxed : severity_level;
  signal nj : real_vector(3 to 2);
  signal itkfeaxs : severity_level;
  signal dzx : real_vector(3 to 2);
  signal uzsqot : integer;
  signal o : severity_level;
  signal cult : real_vector(3 to 2);
  signal dzcdvpymod : integer;
begin
  zdkgxxrfme : entity work.jfrugge
    port map (mudyoyhojv => dzcdvpymod, vqyt => cult, z => o);
  s : entity work.jfrugge
    port map (mudyoyhojv => uzsqot, vqyt => dzx, z => itkfeaxs);
  u : entity work.jfrugge
    port map (mudyoyhojv => hwnu, vqyt => nj, z => eevfxed);
  
  -- Single-driven assignments
  brxnme <= 'j';
end fjnpd;

entity takk is
  port (cl : inout time; zmp : buffer time_vector(2 to 3); bekfs : in real);
end takk;

architecture rsaslr of takk is
  signal ieoobqa : integer;
  signal vm : real;
  signal abqzo : character;
begin
  dpbhun : entity work.zqpftiyc
    port map (brxnme => abqzo, lytdajv => vm, hwnu => ieoobqa);
  
  -- Single-driven assignments
  cl <= cl;
  zmp <= zmp;
end rsaslr;

entity wsmhshml is
  port (sijtvvis : buffer character);
end wsmhshml;

architecture wwhdrtrana of wsmhshml is
  signal enuhzpceaq : severity_level;
  signal zqu : real_vector(3 to 2);
  signal lqo : integer;
  signal mzzspaxcl : integer;
  signal yk : real;
  signal jaxlheqtkr : integer;
  signal wvgdnnfw : real;
  signal xw : character;
begin
  ctvkpzxw : entity work.zqpftiyc
    port map (brxnme => xw, lytdajv => wvgdnnfw, hwnu => jaxlheqtkr);
  jbw : entity work.zqpftiyc
    port map (brxnme => sijtvvis, lytdajv => yk, hwnu => mzzspaxcl);
  hxuept : entity work.jfrugge
    port map (mudyoyhojv => lqo, vqyt => zqu, z => enuhzpceaq);
end wwhdrtrana;



-- Seed after: 13865548619198171421,12359743974512393525
