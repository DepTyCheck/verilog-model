-- Seed: 2549093368057933309,662889661651915549

entity dbeujq is
  port (radwybz : linkage real; up : inout integer; uuk : buffer string(3 downto 4));
end dbeujq;

architecture nedh of dbeujq is
  
begin
  -- Single-driven assignments
  up <= up;
  uuk <= uuk;
end nedh;

library ieee;
use ieee.std_logic_1164.all;

entity j is
  port (hqhtbjki : linkage boolean_vector(1 to 3); vpzszyz : linkage std_logic_vector(2 to 1));
end j;

architecture anaggacrs of j is
  signal q : string(3 downto 4);
  signal rm : integer;
  signal hzr : real;
  signal njsapqvxqi : string(3 downto 4);
  signal iqupavlb : integer;
  signal dkgchpxpp : real;
  signal rdgdfieh : string(3 downto 4);
  signal hylogwza : integer;
  signal sbtfjrjnhf : real;
begin
  jxlf : entity work.dbeujq
    port map (radwybz => sbtfjrjnhf, up => hylogwza, uuk => rdgdfieh);
  a : entity work.dbeujq
    port map (radwybz => dkgchpxpp, up => iqupavlb, uuk => njsapqvxqi);
  aclivoopa : entity work.dbeujq
    port map (radwybz => hzr, up => rm, uuk => q);
end anaggacrs;

library ieee;
use ieee.std_logic_1164.all;

entity vtmzbmarug is
  port (nkb : in std_logic; bvt : out time; zeurmelitn : buffer time_vector(1 to 1); zbzbxy : buffer severity_level);
end vtmzbmarug;

architecture jryamy of vtmzbmarug is
  signal cba : string(3 downto 4);
  signal gbul : integer;
  signal haggzvb : real;
  signal ua : string(3 downto 4);
  signal pinhghhou : integer;
  signal oia : real;
begin
  c : entity work.dbeujq
    port map (radwybz => oia, up => pinhghhou, uuk => ua);
  prcdkyth : entity work.dbeujq
    port map (radwybz => haggzvb, up => gbul, uuk => cba);
end jryamy;

library ieee;
use ieee.std_logic_1164.all;

entity g is
  port (aqlnj : in std_logic; naolv : in std_logic);
end g;

library ieee;
use ieee.std_logic_1164.all;

architecture uwgpnv of g is
  signal srjexrpevi : string(3 downto 4);
  signal zc : integer;
  signal ewxt : real;
  signal tcb : std_logic_vector(2 to 1);
  signal dtqwy : boolean_vector(1 to 3);
  signal a : severity_level;
  signal ydqt : time_vector(1 to 1);
  signal pyqp : time;
begin
  wooaxzjlk : entity work.vtmzbmarug
    port map (nkb => naolv, bvt => pyqp, zeurmelitn => ydqt, zbzbxy => a);
  wqpcls : entity work.j
    port map (hqhtbjki => dtqwy, vpzszyz => tcb);
  n : entity work.dbeujq
    port map (radwybz => ewxt, up => zc, uuk => srjexrpevi);
  
  -- Multi-driven assignments
  tcb <= (others => '0');
end uwgpnv;



-- Seed after: 9543362289273315176,662889661651915549
