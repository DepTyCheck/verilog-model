-- Seed: 2789263815780750271,14426950258250697445

use std.reflection.all;

entity cahzzb is
  port (g : inout access_value_mirror);
end cahzzb;

architecture eejwtg of cahzzb is
  
begin
  
end eejwtg;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity wfixqdvy is
  port (vjpoi : linkage std_logic; gdhiswzo : inout array_subtype_mirror; wqacq : inout time_vector(2 to 1));
end wfixqdvy;

use std.reflection.all;

architecture cxhao of wfixqdvy is
  shared variable krkeaoxgw : access_value_mirror;
  shared variable uxdhygue : access_value_mirror;
  shared variable bococg : access_value_mirror;
  shared variable eiryr : access_value_mirror;
begin
  nrqm : entity work.cahzzb
    port map (g => eiryr);
  ihaxbz : entity work.cahzzb
    port map (g => bococg);
  w : entity work.cahzzb
    port map (g => uxdhygue);
  x : entity work.cahzzb
    port map (g => krkeaoxgw);
  
  -- Single-driven assignments
  wqacq <= (others => 0 ns);
end cxhao;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity zanhy is
  port (pgpj : inout std_logic_vector(0 to 3); se : out integer; d : inout value_mirror; ijf : inout record_subtype_mirror);
end zanhy;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture tegat of zanhy is
  shared variable et : access_value_mirror;
  signal bffljrk : time_vector(2 to 1);
  shared variable aslau : array_subtype_mirror;
  signal uq : std_logic;
  shared variable osywhrh : access_value_mirror;
begin
  pyzf : entity work.cahzzb
    port map (g => osywhrh);
  vkhehhqhs : entity work.wfixqdvy
    port map (vjpoi => uq, gdhiswzo => aslau, wqacq => bffljrk);
  n : entity work.cahzzb
    port map (g => et);
  
  -- Single-driven assignments
  se <= se;
end tegat;

use std.reflection.all;

entity fcrgr is
  port ( jzthwmtkq : inout array_subtype_mirror
  ; gbn : inout enumeration_value_mirror
  ; jlqyuwxodo : inout enumeration_subtype_mirror
  ; a : inout file_value_mirror
  );
end fcrgr;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture sogo of fcrgr is
  shared variable l : access_value_mirror;
  signal sngruxdy : time_vector(2 to 1);
  shared variable poopaeqn : array_subtype_mirror;
  signal unyxrwj : std_logic;
  shared variable vmpxvm : access_value_mirror;
begin
  u : entity work.cahzzb
    port map (g => vmpxvm);
  p : entity work.wfixqdvy
    port map (vjpoi => unyxrwj, gdhiswzo => poopaeqn, wqacq => sngruxdy);
  nqxtowmk : entity work.cahzzb
    port map (g => l);
end sogo;



-- Seed after: 15827572982198154745,14426950258250697445
