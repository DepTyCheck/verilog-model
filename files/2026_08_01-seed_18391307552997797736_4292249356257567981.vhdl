-- Seed: 18391307552997797736,4292249356257567981

library ieee;
use ieee.std_logic_1164.all;

entity txowap is
  port (u : buffer std_logic_vector(0 to 2); ts : linkage severity_level; klnupjp : linkage real; ad : out time);
end txowap;

architecture jz of txowap is
  
begin
  -- Multi-driven assignments
  u <= ('Z', 'W', 'U');
  u <= "ZZ0";
  u <= u;
  u <= "W0X";
end jz;

entity rpwtm is
  port (czw : inout integer_vector(0 downto 4));
end rpwtm;

library ieee;
use ieee.std_logic_1164.all;

architecture h of rpwtm is
  signal xklmrcvqh : time;
  signal frjxhwaa : real;
  signal dpvaviig : severity_level;
  signal c : std_logic_vector(0 to 2);
begin
  pfeu : entity work.txowap
    port map (u => c, ts => dpvaviig, klnupjp => frjxhwaa, ad => xklmrcvqh);
  
  -- Single-driven assignments
  czw <= (others => 0);
end h;



-- Seed after: 1485442877224897893,4292249356257567981
