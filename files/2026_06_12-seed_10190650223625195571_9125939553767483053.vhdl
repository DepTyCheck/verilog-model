-- Seed: 10190650223625195571,9125939553767483053



entity m is
  port (drhsn : linkage bit_vector(4 downto 1); gha : inout time; rivaxpqnz : buffer bit_vector(2 to 0));
end m;



architecture dfxb of m is
  
begin
  
end dfxb;

library ieee;
use ieee.std_logic_1164.all;

entity hxmsvxwfnk is
  port (rkewubdwd : linkage std_logic; jgxo : linkage time; enix : in time; lomngvv : inout real);
end hxmsvxwfnk;



architecture ggn of hxmsvxwfnk is
  signal dcvjwers : bit_vector(2 to 0);
  signal ughxpdebk : time;
  signal ct : bit_vector(2 to 0);
  signal qxqjfzajzz : time;
  signal cdov : bit_vector(4 downto 1);
begin
  lquhjoqtke : entity work.m
    port map (drhsn => cdov, gha => qxqjfzajzz, rivaxpqnz => ct);
  jzxbhykj : entity work.m
    port map (drhsn => cdov, gha => ughxpdebk, rivaxpqnz => dcvjwers);
end ggn;

library ieee;
use ieee.std_logic_1164.all;

entity ocwsp is
  port (skylt : inout std_logic; s : out real_vector(4 to 4); dpr : buffer integer; hmbgx : linkage std_logic_vector(1 downto 0));
end ocwsp;



architecture grtv of ocwsp is
  signal eookjqyam : bit_vector(2 to 0);
  signal hj : time;
  signal zp : bit_vector(4 downto 1);
begin
  ozbbbpotse : entity work.m
    port map (drhsn => zp, gha => hj, rivaxpqnz => eookjqyam);
end grtv;



-- Seed after: 9461018174812995453,9125939553767483053
