-- Seed: 15129090578083937104,8068158652091157513

entity ooa is
  port (opxp : out real);
end ooa;

architecture ahpgjlw of ooa is
  
begin
  -- Single-driven assignments
  opxp <= 3.0_1_2;
end ahpgjlw;

library ieee;
use ieee.std_logic_1164.all;

entity tzucihxbds is
  port ( pzyzi : inout std_logic_vector(4 to 3)
  ; wtnwxje : linkage std_logic_vector(3 to 1)
  ; sjlqhakkt : linkage std_logic_vector(1 to 4)
  ; fiue : in integer
  );
end tzucihxbds;

architecture znipcz of tzucihxbds is
  signal sjaycwyhrl : real;
  signal aulw : real;
  signal u : real;
  signal wwaxry : real;
begin
  c : entity work.ooa
    port map (opxp => wwaxry);
  qjx : entity work.ooa
    port map (opxp => u);
  k : entity work.ooa
    port map (opxp => aulw);
  ob : entity work.ooa
    port map (opxp => sjaycwyhrl);
  
  -- Multi-driven assignments
  pzyzi <= pzyzi;
  pzyzi <= pzyzi;
end znipcz;

entity eml is
  port (ua : buffer string(2 to 5); vklyllf : buffer real_vector(3 to 4));
end eml;

architecture qseiglw of eml is
  signal pp : real;
  signal iaeggowglc : real;
  signal ywpui : real;
begin
  kaz : entity work.ooa
    port map (opxp => ywpui);
  difpdxhi : entity work.ooa
    port map (opxp => iaeggowglc);
  riwjx : entity work.ooa
    port map (opxp => pp);
  
  -- Single-driven assignments
  vklyllf <= vklyllf;
end qseiglw;



-- Seed after: 17550691238395805715,8068158652091157513
