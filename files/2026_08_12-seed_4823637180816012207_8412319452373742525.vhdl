-- Seed: 4823637180816012207,8412319452373742525

library ieee;
use ieee.std_logic_1164.all;

entity mo is
  port (uwohorzsp : in bit; scwu : buffer bit; jc : buffer std_logic; lj : in real);
end mo;

architecture jtb of mo is
  
begin
  -- Single-driven assignments
  scwu <= scwu;
  
  -- Multi-driven assignments
  jc <= jc;
  jc <= 'W';
end jtb;

library ieee;
use ieee.std_logic_1164.all;

entity gohmlhwvi is
  port (w : in time; pvrdiiqbh : buffer real; vevjqx : buffer std_logic_vector(1 downto 1); flcktmjzap : linkage boolean_vector(2 to 1));
end gohmlhwvi;

architecture dvjzy of gohmlhwvi is
  
begin
  -- Single-driven assignments
  pvrdiiqbh <= 3_0.1313;
end dvjzy;

library ieee;
use ieee.std_logic_1164.all;

entity v is
  port (pglyihecm : out std_logic_vector(0 to 0); zorh : inout integer; r : out time_vector(1 to 1));
end v;

library ieee;
use ieee.std_logic_1164.all;

architecture nali of v is
  signal wijgandg : bit;
  signal mdiw : bit;
  signal aoqxe : boolean_vector(2 to 1);
  signal waufx : std_logic_vector(1 downto 1);
  signal euorxy : time;
  signal eytxjspwz : std_logic;
  signal hewgz : bit;
  signal iopa : bit;
  signal fdtioevqyr : real;
  signal lifbla : std_logic;
  signal dvsno : bit;
  signal ebuvzkgjlq : bit;
begin
  efgvz : entity work.mo
    port map (uwohorzsp => ebuvzkgjlq, scwu => dvsno, jc => lifbla, lj => fdtioevqyr);
  zwuucsiu : entity work.mo
    port map (uwohorzsp => iopa, scwu => hewgz, jc => eytxjspwz, lj => fdtioevqyr);
  i : entity work.gohmlhwvi
    port map (w => euorxy, pvrdiiqbh => fdtioevqyr, vevjqx => waufx, flcktmjzap => aoqxe);
  eu : entity work.mo
    port map (uwohorzsp => mdiw, scwu => wijgandg, jc => lifbla, lj => fdtioevqyr);
  
  -- Single-driven assignments
  mdiw <= dvsno;
  ebuvzkgjlq <= '0';
  euorxy <= 2#0# us;
  
  -- Multi-driven assignments
  pglyihecm <= (others => '1');
  pglyihecm <= pglyihecm;
  waufx <= (others => '-');
  pglyihecm <= "W";
end nali;

entity gebdbt is
  port (ij : inout severity_level; jgclzd : buffer integer; yvqoohrpl : out time);
end gebdbt;

architecture sd of gebdbt is
  
begin
  -- Single-driven assignments
  yvqoohrpl <= 0 ps;
  jgclzd <= jgclzd;
  ij <= ERROR;
end sd;



-- Seed after: 16449075976434183497,8412319452373742525
