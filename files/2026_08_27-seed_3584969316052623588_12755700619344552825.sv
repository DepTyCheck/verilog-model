// Seed: 3584969316052623588,12755700619344552825

module w (input tri0 logic [2:1][1:0] ziyfggf [3:1][3:0], input realtime qzc);
  xnor gjd(v, qzc, qzc);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   realtime qzc -> logic qzc
  //
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   realtime qzc -> logic qzc
  
  xnor n(v, lezqgxdj, qzc);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   realtime qzc -> logic qzc
  
  xor ivaqvak(mphsbmenv, mphsbmenv, mphsbmenv);
  
  xnor ft(v, qzc, jfl);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   realtime qzc -> logic qzc
  
  
  // Multi-driven assignments
  assign ziyfggf = ziyfggf;
endmodule: w

module unbnogajtx (input tri1 logic [1:4][2:0][2:0][1:1] r [2:3][0:3][0:2], output supply1 logic jjiynt [4:2][0:1]);
  // Multi-driven assignments
  assign jjiynt = '{'{'b1,'b0},'{'bx,'bx},'{'bz0,'b0}};
  assign r = r;
endmodule: unbnogajtx

module ktyyfq ();
  // Unpacked net declarations
  tri0 logic [2:1][1:0] dhmn [3:1][3:0];
  
  xnor ebtxkow(fdwydcpy, fdwydcpy, d);
  
  or gghvkgnx(d, fdwydcpy, fdwydcpy);
  
  nand yfkpolnkl(sb, d, fdwydcpy);
  
  w j(.ziyfggf(dhmn), .qzc(fdwydcpy));
  // warning: implicit conversion of port connection expands from 1 to 64 bits
  // warning: implicit conversion changes signedness from unsigned to signed
  //   wire logic fdwydcpy -> realtime qzc
  
  
  // Multi-driven assignments
  assign dhmn = dhmn;
  assign fdwydcpy = 'b00x;
endmodule: ktyyfq

module gyk ();
  
endmodule: gyk



// Seed after: 3729995839310119716,12755700619344552825
