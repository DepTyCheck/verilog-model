-- Seed: 6049924759255946562,8068158652091157513

library ieee;
use ieee.std_logic_1164.all;

entity hzmoq is
  port (toggna : linkage character; dwrwlol : in std_logic_vector(0 downto 2); ws : buffer real);
end hzmoq;

architecture lsig of hzmoq is
  
begin
  -- Single-driven assignments
  ws <= ws;
end lsig;

library ieee;
use ieee.std_logic_1164.all;

entity quaaxtr is
  port (kealnxcq : linkage boolean; dnxqlqncn : inout std_logic_vector(1 to 0));
end quaaxtr;

library ieee;
use ieee.std_logic_1164.all;

architecture vivvvgcbg of quaaxtr is
  signal tviwr : real;
  signal mp : character;
  signal vtcaknqoxb : real;
  signal nzbqtjpfaj : character;
  signal lzesieg : real;
  signal iuetxrose : std_logic_vector(0 downto 2);
  signal sea : character;
  signal r : real;
  signal mzryquit : character;
begin
  shfuj : entity work.hzmoq
    port map (toggna => mzryquit, dwrwlol => dnxqlqncn, ws => r);
  mpxifgc : entity work.hzmoq
    port map (toggna => sea, dwrwlol => iuetxrose, ws => lzesieg);
  esv : entity work.hzmoq
    port map (toggna => nzbqtjpfaj, dwrwlol => dnxqlqncn, ws => vtcaknqoxb);
  va : entity work.hzmoq
    port map (toggna => mp, dwrwlol => iuetxrose, ws => tviwr);
  
  -- Multi-driven assignments
  dnxqlqncn <= dnxqlqncn;
  iuetxrose <= dnxqlqncn;
  dnxqlqncn <= dnxqlqncn;
  dnxqlqncn <= "";
end vivvvgcbg;

library ieee;
use ieee.std_logic_1164.all;

entity slvafjnj is
  port (gzawpzeey : inout std_logic_vector(4 to 0); dsnyf : in real_vector(0 downto 3));
end slvafjnj;

library ieee;
use ieee.std_logic_1164.all;

architecture vcbehqe of slvafjnj is
  signal fkvvxium : real;
  signal muduxvhq : character;
  signal ztlcoyn : real;
  signal mg : character;
  signal yrgpvroau : real;
  signal xztcdc : character;
  signal ofhw : real;
  signal hpcs : std_logic_vector(0 downto 2);
  signal zt : character;
begin
  gashn : entity work.hzmoq
    port map (toggna => zt, dwrwlol => hpcs, ws => ofhw);
  vtybhpfscb : entity work.hzmoq
    port map (toggna => xztcdc, dwrwlol => gzawpzeey, ws => yrgpvroau);
  yasioyw : entity work.hzmoq
    port map (toggna => mg, dwrwlol => gzawpzeey, ws => ztlcoyn);
  hlyvzbknj : entity work.hzmoq
    port map (toggna => muduxvhq, dwrwlol => hpcs, ws => fkvvxium);
  
  -- Multi-driven assignments
  gzawpzeey <= gzawpzeey;
  gzawpzeey <= gzawpzeey;
end vcbehqe;

entity eao is
  port (qt : in time);
end eao;

library ieee;
use ieee.std_logic_1164.all;

architecture nqsblria of eao is
  signal t : real_vector(0 downto 3);
  signal okmbldael : real;
  signal sgvgpxmc : character;
  signal oztkxh : real;
  signal pdwntpeqwl : std_logic_vector(4 to 0);
  signal nayd : character;
begin
  lubsggyv : entity work.hzmoq
    port map (toggna => nayd, dwrwlol => pdwntpeqwl, ws => oztkxh);
  mvz : entity work.hzmoq
    port map (toggna => sgvgpxmc, dwrwlol => pdwntpeqwl, ws => okmbldael);
  addndrzsfs : entity work.slvafjnj
    port map (gzawpzeey => pdwntpeqwl, dsnyf => t);
  
  -- Single-driven assignments
  t <= t;
  
  -- Multi-driven assignments
  pdwntpeqwl <= (others => '0');
  pdwntpeqwl <= pdwntpeqwl;
  pdwntpeqwl <= "";
  pdwntpeqwl <= pdwntpeqwl;
end nqsblria;



-- Seed after: 3486298341949148371,8068158652091157513
