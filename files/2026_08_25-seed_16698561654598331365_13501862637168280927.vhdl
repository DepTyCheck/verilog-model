-- Seed: 16698561654598331365,13501862637168280927

library ieee;
use ieee.std_logic_1164.all;

entity wul is
  port (s : inout std_logic; sa : linkage time; d : buffer integer; qvy : in time);
end wul;

architecture w of wul is
  
begin
  -- Single-driven assignments
  d <= d;
end w;

library ieee;
use ieee.std_logic_1164.all;

entity a is
  port (nooij : in character; htcxpyqt : out std_logic);
end a;

library ieee;
use ieee.std_logic_1164.all;

architecture gg of a is
  signal iqah : time;
  signal mfhsvhny : integer;
  signal op : time;
  signal wwplnusxpf : integer;
  signal ee : time;
  signal hyqcdahu : integer;
  signal d : std_logic;
  signal xvowtuvv : time;
  signal rfrbz : integer;
  signal x : time;
begin
  fur : entity work.wul
    port map (s => htcxpyqt, sa => x, d => rfrbz, qvy => xvowtuvv);
  bp : entity work.wul
    port map (s => d, sa => xvowtuvv, d => hyqcdahu, qvy => ee);
  dvgmhbg : entity work.wul
    port map (s => d, sa => ee, d => wwplnusxpf, qvy => op);
  h : entity work.wul
    port map (s => htcxpyqt, sa => op, d => mfhsvhny, qvy => iqah);
  
  -- Single-driven assignments
  iqah <= 232.44 fs;
  
  -- Multi-driven assignments
  htcxpyqt <= d;
end gg;



-- Seed after: 6257463914884108782,13501862637168280927
