-- Seed: 8082542953301903304,13468520519399083457

library ieee;
use ieee.std_logic_1164.all;

entity lwrgmykn is
  port (isw : buffer time; vxpy : inout severity_level; fl : inout std_logic);
end lwrgmykn;



architecture lqcxhr of lwrgmykn is
  
begin
  
end lqcxhr;

library ieee;
use ieee.std_logic_1164.all;

entity wbmuy is
  port (ywkhrlri : buffer std_logic; bbykqaksbw : out std_logic; cgrf : buffer real; kwaezrmlzj : buffer boolean);
end wbmuy;

library ieee;
use ieee.std_logic_1164.all;

architecture js of wbmuy is
  signal zum : std_logic;
  signal kq : severity_level;
  signal rb : time;
  signal cnjhowz : severity_level;
  signal uwzl : time;
begin
  verbr : entity work.lwrgmykn
    port map (isw => uwzl, vxpy => cnjhowz, fl => bbykqaksbw);
  l : entity work.lwrgmykn
    port map (isw => rb, vxpy => kq, fl => zum);
end js;



entity qqmhc is
  port (irdi : linkage integer);
end qqmhc;

library ieee;
use ieee.std_logic_1164.all;

architecture hmri of qqmhc is
  signal vmqpmanhou : std_logic;
  signal iykvafbdo : severity_level;
  signal ipjh : time;
  signal yvnhub : std_logic;
  signal jixuthbib : severity_level;
  signal ghcdmdxfir : time;
begin
  i : entity work.lwrgmykn
    port map (isw => ghcdmdxfir, vxpy => jixuthbib, fl => yvnhub);
  gvx : entity work.lwrgmykn
    port map (isw => ipjh, vxpy => iykvafbdo, fl => vmqpmanhou);
end hmri;



-- Seed after: 942449705941383554,13468520519399083457
