-- Seed: 4230628714156452943,3108530264173481209

library ieee;
use ieee.std_logic_1164.all;

entity ktodpav is
  port (xcgxsbv : in std_logic_vector(3 to 2); d : linkage real; cqchvstxw : in real_vector(3 to 4); nudb : out std_logic);
end ktodpav;

architecture m of ktodpav is
  
begin
  -- Multi-driven assignments
  nudb <= 'Z';
  nudb <= '1';
  nudb <= '-';
end m;

library ieee;
use ieee.std_logic_1164.all;

entity atv is
  port (xuaz : out std_logic);
end atv;

library ieee;
use ieee.std_logic_1164.all;

architecture xnrgiwmwx of atv is
  signal eyhkvres : std_logic;
  signal nyzo : real_vector(3 to 4);
  signal qzxnjvjt : real;
  signal ddnrr : std_logic_vector(3 to 2);
  signal l : std_logic;
  signal ycfytuysl : real_vector(3 to 4);
  signal hmuaiyh : real;
  signal rkqwrhuipm : std_logic_vector(3 to 2);
  signal uhn : real_vector(3 to 4);
  signal aghfuowm : real;
  signal hwegoz : std_logic;
  signal nxlj : real_vector(3 to 4);
  signal eegv : real;
  signal ogbn : std_logic_vector(3 to 2);
begin
  ouoly : entity work.ktodpav
    port map (xcgxsbv => ogbn, d => eegv, cqchvstxw => nxlj, nudb => hwegoz);
  c : entity work.ktodpav
    port map (xcgxsbv => ogbn, d => aghfuowm, cqchvstxw => uhn, nudb => xuaz);
  fcovbifbd : entity work.ktodpav
    port map (xcgxsbv => rkqwrhuipm, d => hmuaiyh, cqchvstxw => ycfytuysl, nudb => l);
  cw : entity work.ktodpav
    port map (xcgxsbv => ddnrr, d => qzxnjvjt, cqchvstxw => nyzo, nudb => eyhkvres);
  
  -- Multi-driven assignments
  hwegoz <= 'Z';
end xnrgiwmwx;



-- Seed after: 7802494724402543287,3108530264173481209
