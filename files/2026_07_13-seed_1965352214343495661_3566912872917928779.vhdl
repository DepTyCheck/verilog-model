-- Seed: 1965352214343495661,3566912872917928779

use std.reflection.all;

entity muyhuh is
  port (xeuwrprbtq : linkage real; mdpcnxppbj : inout array_subtype_mirror; itnmbbw : out bit_vector(3 to 0));
end muyhuh;

architecture ylh of muyhuh is
  
begin
  
end ylh;

entity mcygrawto is
  port (gdqx : inout severity_level; lbo : linkage time; tiwfboxc : buffer time; dnise : buffer string(5 to 1));
end mcygrawto;

use std.reflection.all;

architecture jw of mcygrawto is
  signal exadns : bit_vector(3 to 0);
  shared variable zmsj : array_subtype_mirror;
  signal wm : real;
begin
  ktbewsz : entity work.muyhuh
    port map (xeuwrprbtq => wm, mdpcnxppbj => zmsj, itnmbbw => exadns);
  
  -- Single-driven assignments
  dnise <= dnise;
  gdqx <= gdqx;
  tiwfboxc <= 1 hr;
end jw;

use std.reflection.all;

entity kfapy is
  port (vhwvdcwzx : inout enumeration_subtype_mirror);
end kfapy;

use std.reflection.all;

architecture t of kfapy is
  signal usuueyce : bit_vector(3 to 0);
  shared variable wlviot : array_subtype_mirror;
  signal diihlrua : real;
  signal vpoidqw : bit_vector(3 to 0);
  shared variable j : array_subtype_mirror;
  signal fuep : real;
  signal luqbbnwdh : bit_vector(3 to 0);
  shared variable slfjc : array_subtype_mirror;
  signal ssfybzb : real;
begin
  ntuiuwkqp : entity work.muyhuh
    port map (xeuwrprbtq => ssfybzb, mdpcnxppbj => slfjc, itnmbbw => luqbbnwdh);
  yvtnacdhk : entity work.muyhuh
    port map (xeuwrprbtq => fuep, mdpcnxppbj => j, itnmbbw => vpoidqw);
  vmaylv : entity work.muyhuh
    port map (xeuwrprbtq => diihlrua, mdpcnxppbj => wlviot, itnmbbw => usuueyce);
end t;

use std.reflection.all;

entity jbud is
  port (kujhxfu : inout floating_subtype_mirror; lxddsv : inout time; vjcuomsywh : inout protected_value_mirror);
end jbud;

use std.reflection.all;

architecture noactqgxvc of jbud is
  signal sdck : bit_vector(3 to 0);
  shared variable xcwthv : array_subtype_mirror;
  signal u : real;
  signal ti : bit_vector(3 to 0);
  shared variable gfkhmmy : array_subtype_mirror;
  signal wweuyhpuxk : real;
  signal jvga : bit_vector(3 to 0);
  shared variable gturwm : array_subtype_mirror;
  signal xgp : real;
  signal ouq : string(5 to 1);
  signal etunprg : time;
  signal jeojacrn : time;
  signal imfo : severity_level;
begin
  hkhwpkbk : entity work.mcygrawto
    port map (gdqx => imfo, lbo => jeojacrn, tiwfboxc => etunprg, dnise => ouq);
  pr : entity work.muyhuh
    port map (xeuwrprbtq => xgp, mdpcnxppbj => gturwm, itnmbbw => jvga);
  wvfjlq : entity work.muyhuh
    port map (xeuwrprbtq => wweuyhpuxk, mdpcnxppbj => gfkhmmy, itnmbbw => ti);
  vc : entity work.muyhuh
    port map (xeuwrprbtq => u, mdpcnxppbj => xcwthv, itnmbbw => sdck);
end noactqgxvc;



-- Seed after: 5612806191667544074,3566912872917928779
