-- Seed: 17577060895705407633,5983430343285687595

library ieee;
use ieee.std_logic_1164.all;

entity rffapcmzp is
  port (bmyz : inout real; tcpdxp : in std_logic_vector(3 to 3); q : linkage time; kbsjaosx : in time);
end rffapcmzp;

architecture gbhkazbetu of rffapcmzp is
  
begin
  -- Single-driven assignments
  bmyz <= bmyz;
end gbhkazbetu;

library ieee;
use ieee.std_logic_1164.all;

entity gggrt is
  port (uzyqjzriq : buffer std_logic_vector(2 to 2); kre : linkage integer; p : buffer real);
end gggrt;

library ieee;
use ieee.std_logic_1164.all;

architecture uabjf of gggrt is
  signal zddgammn : time;
  signal yjswnrb : time;
  signal hcsrzdo : std_logic_vector(3 to 3);
  signal oezrikg : real;
  signal kqmicq : time;
  signal l : std_logic_vector(3 to 3);
  signal ijsdtkkmfv : real;
  signal uatggo : std_logic_vector(3 to 3);
  signal kzmlm : real;
  signal xu : time;
  signal uiadbmbr : time;
  signal keelsvutkj : std_logic_vector(3 to 3);
begin
  r : entity work.rffapcmzp
    port map (bmyz => p, tcpdxp => keelsvutkj, q => uiadbmbr, kbsjaosx => xu);
  j : entity work.rffapcmzp
    port map (bmyz => kzmlm, tcpdxp => uatggo, q => xu, kbsjaosx => xu);
  zzi : entity work.rffapcmzp
    port map (bmyz => ijsdtkkmfv, tcpdxp => l, q => kqmicq, kbsjaosx => uiadbmbr);
  vcfchratt : entity work.rffapcmzp
    port map (bmyz => oezrikg, tcpdxp => hcsrzdo, q => yjswnrb, kbsjaosx => zddgammn);
  
  -- Single-driven assignments
  zddgammn <= 16#32# ns;
end uabjf;



-- Seed after: 8560642754538307495,5983430343285687595
