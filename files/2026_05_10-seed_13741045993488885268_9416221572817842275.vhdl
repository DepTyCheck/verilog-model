-- Seed: 13741045993488885268,9416221572817842275

library ieee;
use ieee.std_logic_1164.all;

entity icb is
  port (dirgo : in std_logic; mauq : buffer std_logic; nsml : buffer time; etmnezpsvt : buffer severity_level);
end icb;



architecture utefiovf of icb is
  
begin
  
end utefiovf;

library ieee;
use ieee.std_logic_1164.all;

entity nh is
  port (vfkxjgjlal : in std_logic; ruvrp : inout real; ukaw : buffer severity_level; eyqlomcfje : linkage real);
end nh;

library ieee;
use ieee.std_logic_1164.all;

architecture r of nh is
  signal fjbbpmml : time;
  signal vzkegfitra : std_logic;
  signal gpyhsgo : severity_level;
  signal s : time;
  signal wg : std_logic;
  signal ymdzktly : std_logic;
  signal kf : severity_level;
  signal tydppdys : time;
  signal sjru : std_logic;
  signal fxhcomsaub : std_logic;
  signal dypksmwwny : severity_level;
  signal qnicnb : time;
  signal a : std_logic;
  signal pbmoss : std_logic;
begin
  jluzzi : entity work.icb
    port map (dirgo => pbmoss, mauq => a, nsml => qnicnb, etmnezpsvt => dypksmwwny);
  jaukhuymz : entity work.icb
    port map (dirgo => fxhcomsaub, mauq => sjru, nsml => tydppdys, etmnezpsvt => kf);
  id : entity work.icb
    port map (dirgo => ymdzktly, mauq => wg, nsml => s, etmnezpsvt => gpyhsgo);
  gg : entity work.icb
    port map (dirgo => pbmoss, mauq => vzkegfitra, nsml => fjbbpmml, etmnezpsvt => ukaw);
end r;

library ieee;
use ieee.std_logic_1164.all;

entity zxqbk is
  port (vhvmfiy : out std_logic);
end zxqbk;

library ieee;
use ieee.std_logic_1164.all;

architecture a of zxqbk is
  signal upnrre : severity_level;
  signal h : time;
  signal wu : severity_level;
  signal gafo : time;
  signal imaizpv : std_logic;
  signal rgsyqlh : severity_level;
  signal fhkzt : real;
  signal kzstulm : std_logic;
  signal wbgrrt : severity_level;
  signal hwkifvnag : time;
  signal benboins : std_logic;
begin
  elaoyh : entity work.icb
    port map (dirgo => vhvmfiy, mauq => benboins, nsml => hwkifvnag, etmnezpsvt => wbgrrt);
  bimaydb : entity work.nh
    port map (vfkxjgjlal => kzstulm, ruvrp => fhkzt, ukaw => rgsyqlh, eyqlomcfje => fhkzt);
  veuckdq : entity work.icb
    port map (dirgo => vhvmfiy, mauq => imaizpv, nsml => gafo, etmnezpsvt => wu);
  zmf : entity work.icb
    port map (dirgo => imaizpv, mauq => imaizpv, nsml => h, etmnezpsvt => upnrre);
end a;



-- Seed after: 10990793094611122231,9416221572817842275
