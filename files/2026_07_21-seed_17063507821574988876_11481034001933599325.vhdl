-- Seed: 17063507821574988876,11481034001933599325

entity dmtpwuaoru is
  port (nkd : out time; hxawoqwe : linkage boolean; ieoeixeig : inout character; ujw : out boolean_vector(1 downto 2));
end dmtpwuaoru;

architecture rgrlw of dmtpwuaoru is
  
begin
  -- Single-driven assignments
  ujw <= ujw;
  nkd <= 16#2BF8B# us;
  ieoeixeig <= 'u';
end rgrlw;

entity eopchnwws is
  port (dzsltobv : out character);
end eopchnwws;

architecture s of eopchnwws is
  signal izffkn : boolean_vector(1 downto 2);
  signal koynjy : character;
  signal cz : boolean;
  signal nbsvzo : time;
  signal acntlketaf : boolean_vector(1 downto 2);
  signal zbzq : boolean;
  signal kem : time;
begin
  ryue : entity work.dmtpwuaoru
    port map (nkd => kem, hxawoqwe => zbzq, ieoeixeig => dzsltobv, ujw => acntlketaf);
  cmuvohf : entity work.dmtpwuaoru
    port map (nkd => nbsvzo, hxawoqwe => cz, ieoeixeig => koynjy, ujw => izffkn);
end s;

library ieee;
use ieee.std_logic_1164.all;

entity ybh is
  port (xyw : out real; mxitz : in boolean_vector(3 downto 3); deqmsfhrpq : linkage std_logic; fhogrco : in std_logic_vector(2 downto 3));
end ybh;

architecture e of ybh is
  signal denyigp : boolean_vector(1 downto 2);
  signal bpplaooagz : character;
  signal x : boolean;
  signal soml : time;
  signal awl : character;
  signal dajzssqut : boolean_vector(1 downto 2);
  signal t : character;
  signal qmnngasbuc : boolean;
  signal tlyj : time;
begin
  ufhxwhedp : entity work.dmtpwuaoru
    port map (nkd => tlyj, hxawoqwe => qmnngasbuc, ieoeixeig => t, ujw => dajzssqut);
  fug : entity work.eopchnwws
    port map (dzsltobv => awl);
  lqjwvpo : entity work.dmtpwuaoru
    port map (nkd => soml, hxawoqwe => x, ieoeixeig => bpplaooagz, ujw => denyigp);
  
  -- Single-driven assignments
  xyw <= xyw;
end e;



-- Seed after: 2952681305139485321,11481034001933599325
