-- Seed: 18319957891413772547,10557070023141912087

entity rtyygerku is
  port (myibd : in real_vector(0 downto 1));
end rtyygerku;

architecture pmzm of rtyygerku is
  
begin
  
end pmzm;

library ieee;
use ieee.std_logic_1164.all;

entity zxmhxl is
  port (awpna : in time_vector(3 to 4); p : out std_logic_vector(1 to 4); fgids : inout boolean);
end zxmhxl;

architecture qjlk of zxmhxl is
  signal caycqxc : real_vector(0 downto 1);
  signal m : real_vector(0 downto 1);
  signal hlhbolvq : real_vector(0 downto 1);
begin
  yartzkqnt : entity work.rtyygerku
    port map (myibd => hlhbolvq);
  rigzv : entity work.rtyygerku
    port map (myibd => m);
  pucainfky : entity work.rtyygerku
    port map (myibd => caycqxc);
  
  -- Single-driven assignments
  m <= (others => 0.0);
  caycqxc <= (others => 0.0);
  fgids <= FALSE;
  hlhbolvq <= (others => 0.0);
end qjlk;



-- Seed after: 5767932028402815721,10557070023141912087
