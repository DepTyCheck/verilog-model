// Seed: 15338762050277892139,15659934682226287687

module ezjwamfsi (output real ltytbyjmv [4:4], output reg [3:3] knfpw [0:2]);
  xor idbrpobui(ghefh, difyg, ghefh);
  
  xnor ilegvjsgw(difyg, difyg, pzme);
  
  
  // Single-driven assignments
  assign knfpw = knfpw;
  assign ltytbyjmv = ltytbyjmv;
  
  // Multi-driven assignments
  assign pzme = pzme;
endmodule: ezjwamfsi

module vvdkr (output bit [1:4] vi);
  xnor enofap(zilyatibkm, vi, blrv);
  // warning: implicit conversion of port connection truncates from 4 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [1:4] vi -> logic vi
  
  xor kplvvouwf(mmd, usm, usm);
  
  xor eijmowiyst(vi, blrv, vi);
  // warning: implicit conversion of port connection truncates from 4 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [1:4] vi -> logic vi
  //
  // warning: implicit conversion of port connection truncates from 4 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [1:4] vi -> logic vi
  
  xor g(a, usm, a);
  
  
  // Multi-driven assignments
  assign zilyatibkm = 'bz0;
  assign mmd = 'b1z1;
  assign zilyatibkm = 'b1;
endmodule: vvdkr



// Seed after: 12023387524046867565,15659934682226287687
