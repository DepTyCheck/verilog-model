-- Seed: 13742553235405841173,4177195558088809003

entity ydvr is
  port (zn : buffer boolean_vector(0 to 2));
end ydvr;

architecture xanlwyqps of ydvr is
  
begin
  -- Single-driven assignments
  zn <= (FALSE, TRUE, TRUE);
end xanlwyqps;

library ieee;
use ieee.std_logic_1164.all;

entity mw is
  port (uvuq : in std_logic_vector(0 downto 0); xlpw : out std_logic_vector(1 downto 4));
end mw;

architecture uxlobafrw of mw is
  
begin
  -- Multi-driven assignments
  xlpw <= "";
  xlpw <= xlpw;
end uxlobafrw;

library ieee;
use ieee.std_logic_1164.all;

entity ayllm is
  port (wpnewuz : in integer_vector(1 downto 3); gczex : buffer bit; xjkifn : in bit_vector(0 to 0); v : inout std_logic);
end ayllm;

library ieee;
use ieee.std_logic_1164.all;

architecture sep of ayllm is
  signal ermsgez : boolean_vector(0 to 2);
  signal wj : std_logic_vector(1 downto 4);
  signal alzbcuzfh : std_logic_vector(0 downto 0);
  signal sm : boolean_vector(0 to 2);
  signal mkdx : boolean_vector(0 to 2);
begin
  yxlwfcs : entity work.ydvr
    port map (zn => mkdx);
  lodoj : entity work.ydvr
    port map (zn => sm);
  ows : entity work.mw
    port map (uvuq => alzbcuzfh, xlpw => wj);
  y : entity work.ydvr
    port map (zn => ermsgez);
  
  -- Multi-driven assignments
  v <= v;
  v <= v;
  alzbcuzfh <= alzbcuzfh;
  alzbcuzfh <= alzbcuzfh;
end sep;

library ieee;
use ieee.std_logic_1164.all;

entity uk is
  port (yjdki : buffer real; mukvyvi : linkage real_vector(1 downto 1); ifmkewtfaa : linkage integer; qrgjr : buffer std_logic);
end uk;

architecture rtsobkun of uk is
  signal a : boolean_vector(0 to 2);
  signal qq : bit_vector(0 to 0);
  signal tb : bit;
  signal bryjcul : integer_vector(1 downto 3);
begin
  l : entity work.ayllm
    port map (wpnewuz => bryjcul, gczex => tb, xjkifn => qq, v => qrgjr);
  lurcuhhu : entity work.ydvr
    port map (zn => a);
  
  -- Single-driven assignments
  yjdki <= yjdki;
  bryjcul <= (others => 0);
end rtsobkun;



-- Seed after: 15381702540433888552,4177195558088809003
