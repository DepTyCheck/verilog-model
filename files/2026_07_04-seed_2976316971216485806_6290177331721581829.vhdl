-- Seed: 2976316971216485806,6290177331721581829

use std.reflection.all;

entity zidfxw is
  port (txan : in real; xz : inout physical_subtype_mirror);
end zidfxw;

architecture xaiw of zidfxw is
  
begin
  
end xaiw;

use std.reflection.all;

entity txqexwfix is
  port (kpmgvcsmgp : inout physical_value_mirror; ys : buffer string(1 downto 5));
end txqexwfix;

use std.reflection.all;

architecture smyolemz of txqexwfix is
  shared variable jaywh : physical_subtype_mirror;
  signal w : real;
  shared variable vrly : physical_subtype_mirror;
  signal yvexb : real;
  shared variable ukrkr : physical_subtype_mirror;
  signal sfwdhwq : real;
begin
  c : entity work.zidfxw
    port map (txan => sfwdhwq, xz => ukrkr);
  vfvvsbqhz : entity work.zidfxw
    port map (txan => yvexb, xz => vrly);
  uzigw : entity work.zidfxw
    port map (txan => w, xz => jaywh);
  
  -- Single-driven assignments
  w <= w;
  yvexb <= sfwdhwq;
  sfwdhwq <= w;
  ys <= (others => ' ');
end smyolemz;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity tebgyyne is
  port ( acqbt : inout protected_subtype_mirror
  ; v : out std_logic_vector(3 downto 4)
  ; bkw : inout protected_value_mirror
  ; u : inout floating_subtype_mirror
  );
end tebgyyne;

use std.reflection.all;

architecture qryigeej of tebgyyne is
  shared variable vbuk : physical_subtype_mirror;
  signal rzhaagyicz : real;
  signal z : string(1 downto 5);
  shared variable qtlohgte : physical_value_mirror;
begin
  ctrvgvizqx : entity work.txqexwfix
    port map (kpmgvcsmgp => qtlohgte, ys => z);
  vceknhbjwb : entity work.zidfxw
    port map (txan => rzhaagyicz, xz => vbuk);
  
  -- Single-driven assignments
  rzhaagyicz <= rzhaagyicz;
end qryigeej;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity rukpd is
  port (dylbzcijh : out std_logic_vector(2 downto 0); r : inout protected_subtype_mirror; v : out boolean);
end rukpd;

use std.reflection.all;

architecture ukwqd of rukpd is
  signal qntvhkwb : string(1 downto 5);
  shared variable nqsg : physical_value_mirror;
begin
  zqtr : entity work.txqexwfix
    port map (kpmgvcsmgp => nqsg, ys => qntvhkwb);
  
  -- Single-driven assignments
  v <= TRUE;
end ukwqd;



-- Seed after: 7319663623344482153,6290177331721581829
