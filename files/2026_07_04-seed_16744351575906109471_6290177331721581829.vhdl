-- Seed: 16744351575906109471,6290177331721581829

use std.reflection.all;

entity hkaqsfpmib is
  port (rpxbip : inout enumeration_subtype_mirror; g : linkage string(1 downto 5); xwwootimwk : inout boolean);
end hkaqsfpmib;

architecture xtgdzd of hkaqsfpmib is
  
begin
  -- Single-driven assignments
  xwwootimwk <= TRUE;
end xtgdzd;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity bnnufy is
  port (kivaabli : out time; kuswp : inout file_subtype_mirror; gknzqhmxl : inout access_value_mirror; babp : linkage std_logic);
end bnnufy;

use std.reflection.all;

architecture ltzqnau of bnnufy is
  signal f : boolean;
  signal scee : string(1 downto 5);
  shared variable qaolalgg : enumeration_subtype_mirror;
begin
  qdxe : entity work.hkaqsfpmib
    port map (rpxbip => qaolalgg, g => scee, xwwootimwk => f);
end ltzqnau;

use std.reflection.all;

entity wkqyizeik is
  port (nrpttnffc : inout record_subtype_mirror; xurbxsqh : inout subtype_mirror; ndbcvhgch : inout physical_value_mirror);
end wkqyizeik;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture fzdhmvgmnz of wkqyizeik is
  signal j : boolean;
  signal z : string(1 downto 5);
  shared variable bjllhibvo : enumeration_subtype_mirror;
  signal vecoyqneu : std_logic;
  shared variable glze : access_value_mirror;
  shared variable bzvpf : file_subtype_mirror;
  signal sljifkc : time;
  signal uugx : boolean;
  signal vxzwmjmoya : string(1 downto 5);
  shared variable xoz : enumeration_subtype_mirror;
begin
  lsudfozz : entity work.hkaqsfpmib
    port map (rpxbip => xoz, g => vxzwmjmoya, xwwootimwk => uugx);
  ghjttssifv : entity work.bnnufy
    port map (kivaabli => sljifkc, kuswp => bzvpf, gknzqhmxl => glze, babp => vecoyqneu);
  zlmdicz : entity work.hkaqsfpmib
    port map (rpxbip => bjllhibvo, g => z, xwwootimwk => j);
  
  -- Multi-driven assignments
  vecoyqneu <= vecoyqneu;
  vecoyqneu <= vecoyqneu;
  vecoyqneu <= vecoyqneu;
  vecoyqneu <= '0';
end fzdhmvgmnz;



-- Seed after: 16969513917568349383,6290177331721581829
