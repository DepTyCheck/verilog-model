-- Seed: 10053482253568510950,10240345754018108067

library ieee;
use ieee.std_logic_1164.all;

entity zjnxpdl is
  port (hbo : out boolean; ynaunit : linkage integer; e : inout integer; so : linkage std_logic_vector(2 to 2));
end zjnxpdl;



architecture mxgagur of zjnxpdl is
  
begin
  
end mxgagur;

library ieee;
use ieee.std_logic_1164.all;

entity fuiaiooxy is
  port (kt : out time_vector(2 downto 1); uysih : out std_logic_vector(0 downto 0); tlnaufnc : out time; ze : buffer real);
end fuiaiooxy;



architecture nnlfepf of fuiaiooxy is
  signal ubf : integer;
  signal lyxxribj : boolean;
  signal pzkjd : integer;
  signal vruxze : integer;
  signal qjdrpt : boolean;
begin
  xyupjm : entity work.zjnxpdl
    port map (hbo => qjdrpt, ynaunit => vruxze, e => pzkjd, so => uysih);
  szrn : entity work.zjnxpdl
    port map (hbo => lyxxribj, ynaunit => ubf, e => vruxze, so => uysih);
end nnlfepf;



entity ekvoku is
  port (rqdgxkxhat : linkage integer);
end ekvoku;

library ieee;
use ieee.std_logic_1164.all;

architecture m of ekvoku is
  signal fsjofzz : integer;
  signal uibwuzuz : integer;
  signal wevj : boolean;
  signal ke : std_logic_vector(2 to 2);
  signal vnd : integer;
  signal onnjksk : boolean;
  signal fij : std_logic_vector(2 to 2);
  signal wpfzie : integer;
  signal dn : boolean;
begin
  pjq : entity work.zjnxpdl
    port map (hbo => dn, ynaunit => wpfzie, e => wpfzie, so => fij);
  cwkqom : entity work.zjnxpdl
    port map (hbo => onnjksk, ynaunit => vnd, e => vnd, so => ke);
  mgraegkd : entity work.zjnxpdl
    port map (hbo => wevj, ynaunit => uibwuzuz, e => fsjofzz, so => fij);
end m;



entity hprpoeda is
  port (mr : linkage time);
end hprpoeda;

library ieee;
use ieee.std_logic_1164.all;

architecture asfn of hprpoeda is
  signal hkcuks : integer;
  signal hjxpppnm : std_logic_vector(2 to 2);
  signal us : integer;
  signal wcul : integer;
  signal cjqzucm : boolean;
  signal ghraltzhel : std_logic_vector(2 to 2);
  signal usvsgts : integer;
  signal xzfdmw : boolean;
begin
  kssyetx : entity work.zjnxpdl
    port map (hbo => xzfdmw, ynaunit => usvsgts, e => usvsgts, so => ghraltzhel);
  o : entity work.zjnxpdl
    port map (hbo => cjqzucm, ynaunit => wcul, e => us, so => hjxpppnm);
  fu : entity work.ekvoku
    port map (rqdgxkxhat => hkcuks);
end asfn;



-- Seed after: 12680708959375070913,10240345754018108067
