-- Seed: 6425139196746475756,9372850727389630639

library ieee;
use ieee.std_logic_1164.all;

entity dx is
  port (wczwube : linkage real; pv : inout real; lizzdaxfhd : buffer boolean; ehlbcld : buffer std_logic);
end dx;



architecture wt of dx is
  
begin
  
end wt;

library ieee;
use ieee.std_logic_1164.all;

entity ydcmibh is
  port (ntfbrtzx : in integer; fiqgmq : in std_logic);
end ydcmibh;

library ieee;
use ieee.std_logic_1164.all;

architecture zdde of ydcmibh is
  signal cyl : boolean;
  signal e : std_logic;
  signal jxafmk : boolean;
  signal p : real;
  signal vxuquqpr : boolean;
  signal kdtacccag : real;
  signal ar : std_logic;
  signal vrwwdk : boolean;
  signal rcenaws : real;
  signal sthdesnk : real;
begin
  jut : entity work.dx
    port map (wczwube => sthdesnk, pv => rcenaws, lizzdaxfhd => vrwwdk, ehlbcld => ar);
  okvnub : entity work.dx
    port map (wczwube => kdtacccag, pv => sthdesnk, lizzdaxfhd => vxuquqpr, ehlbcld => ar);
  gbtffykhb : entity work.dx
    port map (wczwube => sthdesnk, pv => p, lizzdaxfhd => jxafmk, ehlbcld => e);
  qzo : entity work.dx
    port map (wczwube => sthdesnk, pv => kdtacccag, lizzdaxfhd => cyl, ehlbcld => e);
end zdde;



-- Seed after: 7716310890147694075,9372850727389630639
