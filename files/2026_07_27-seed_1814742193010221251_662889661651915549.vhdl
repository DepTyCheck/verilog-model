-- Seed: 1814742193010221251,662889661651915549

library ieee;
use ieee.std_logic_1164.all;

entity gdhiomx is
  port (l : buffer std_logic; q : in string(5 downto 1));
end gdhiomx;

architecture favrwld of gdhiomx is
  
begin
  -- Multi-driven assignments
  l <= l;
  l <= 'X';
  l <= '0';
  l <= l;
end favrwld;

entity gkjwsfmwn is
  port (nsnuvns : buffer real);
end gkjwsfmwn;

library ieee;
use ieee.std_logic_1164.all;

architecture elwha of gkjwsfmwn is
  signal nvlqueeiz : string(5 downto 1);
  signal sw : std_logic;
  signal einutith : std_logic;
  signal oebcms : std_logic;
  signal wjza : string(5 downto 1);
  signal fxonqwd : std_logic;
begin
  girmqc : entity work.gdhiomx
    port map (l => fxonqwd, q => wjza);
  sfttismrne : entity work.gdhiomx
    port map (l => oebcms, q => wjza);
  mbvfxm : entity work.gdhiomx
    port map (l => einutith, q => wjza);
  pul : entity work.gdhiomx
    port map (l => sw, q => nvlqueeiz);
  
  -- Single-driven assignments
  nsnuvns <= nsnuvns;
  nvlqueeiz <= "pgqoq";
  wjza <= wjza;
  
  -- Multi-driven assignments
  oebcms <= fxonqwd;
  fxonqwd <= 'Z';
  einutith <= 'W';
end elwha;

library ieee;
use ieee.std_logic_1164.all;

entity ywrjqeql is
  port (rkijjiwat : inout std_logic_vector(0 to 1); gywjd : buffer string(2 downto 4); uu : out time);
end ywrjqeql;

library ieee;
use ieee.std_logic_1164.all;

architecture bqaqqxa of ywrjqeql is
  signal abgtji : real;
  signal sorayqc : string(5 downto 1);
  signal yhkoha : std_logic;
begin
  sapu : entity work.gdhiomx
    port map (l => yhkoha, q => sorayqc);
  xe : entity work.gkjwsfmwn
    port map (nsnuvns => abgtji);
  uskbp : entity work.gdhiomx
    port map (l => yhkoha, q => sorayqc);
  cm : entity work.gdhiomx
    port map (l => yhkoha, q => sorayqc);
  
  -- Single-driven assignments
  gywjd <= gywjd;
  
  -- Multi-driven assignments
  rkijjiwat <= ('U', 'X');
  rkijjiwat <= "0U";
  rkijjiwat <= "0Z";
end bqaqqxa;



-- Seed after: 3562121321855981346,662889661651915549
