-- Seed: 13016008579875044083,8068158652091157513

entity hcnkt is
  port (odhekh : linkage bit_vector(2 to 2));
end hcnkt;

architecture itbybhxm of hcnkt is
  
begin
  
end itbybhxm;

library ieee;
use ieee.std_logic_1164.all;

entity gcvosq is
  port (cbwpfaev : linkage std_logic_vector(3 downto 0); bgeebumt : out time_vector(4 to 4));
end gcvosq;

architecture d of gcvosq is
  signal htedjkdz : bit_vector(2 to 2);
  signal ppzknwttbq : bit_vector(2 to 2);
  signal wwsi : bit_vector(2 to 2);
  signal ordhsm : bit_vector(2 to 2);
begin
  zuetpufed : entity work.hcnkt
    port map (odhekh => ordhsm);
  y : entity work.hcnkt
    port map (odhekh => wwsi);
  wkexy : entity work.hcnkt
    port map (odhekh => ppzknwttbq);
  pr : entity work.hcnkt
    port map (odhekh => htedjkdz);
  
  -- Single-driven assignments
  bgeebumt <= (others => 16#F_D_0_5.2B2E5# ns);
end d;



-- Seed after: 13310427097450508105,8068158652091157513
