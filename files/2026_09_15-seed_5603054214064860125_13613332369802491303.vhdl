-- Seed: 5603054214064860125,13613332369802491303

entity zbh is
  port (q : inout integer; rcpstzuvt : buffer time; bold : buffer integer_vector(0 downto 2));
end zbh;

architecture an of zbh is
  
begin
  -- Single-driven assignments
  q <= 12304;
end an;

library ieee;
use ieee.std_logic_1164.all;

entity kfmodll is
  port (jxkp : out real; fqkrlvvgn : linkage severity_level; apyc : in std_logic_vector(3 to 1));
end kfmodll;

architecture qwa of kfmodll is
  signal wq : integer_vector(0 downto 2);
  signal kdnka : time;
  signal mo : integer;
  signal lbvjfrin : integer_vector(0 downto 2);
  signal vswmhc : time;
  signal dicry : integer;
begin
  shibpuzoyx : entity work.zbh
    port map (q => dicry, rcpstzuvt => vswmhc, bold => lbvjfrin);
  tj : entity work.zbh
    port map (q => mo, rcpstzuvt => kdnka, bold => wq);
  
  -- Single-driven assignments
  jxkp <= 3.2403;
end qwa;



-- Seed after: 11464099047509864238,13613332369802491303
