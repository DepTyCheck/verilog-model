-- Seed: 15297774751624802870,12359743974512393525

entity sxc is
  port (u : buffer integer_vector(0 to 1));
end sxc;

architecture ktiqfmpga of sxc is
  
begin
  -- Single-driven assignments
  u <= (8#450#, 8#7_7_1_1_1#);
end ktiqfmpga;

entity fjrybn is
  port (fubnda : inout boolean; xsrub : out time_vector(2 downto 1); myrib : in real_vector(1 to 0); mayuei : buffer integer);
end fjrybn;

architecture pulu of fjrybn is
  signal th : integer_vector(0 to 1);
  signal mb : integer_vector(0 to 1);
  signal co : integer_vector(0 to 1);
begin
  uxoewthap : entity work.sxc
    port map (u => co);
  hlvw : entity work.sxc
    port map (u => mb);
  imsaw : entity work.sxc
    port map (u => th);
end pulu;

entity ijrg is
  port (frga : linkage integer; tszdonjij : buffer time; ka : buffer bit; ik : inout integer);
end ijrg;

architecture m of ijrg is
  signal mmpvcf : integer_vector(0 to 1);
  signal j : integer_vector(0 to 1);
  signal mrjatbvahk : integer_vector(0 to 1);
  signal gzxshux : integer_vector(0 to 1);
begin
  xewcpnh : entity work.sxc
    port map (u => gzxshux);
  ysoli : entity work.sxc
    port map (u => mrjatbvahk);
  pktzs : entity work.sxc
    port map (u => j);
  ky : entity work.sxc
    port map (u => mmpvcf);
  
  -- Single-driven assignments
  ka <= ka;
  ik <= ik;
end m;



-- Seed after: 2259381381728353104,12359743974512393525
