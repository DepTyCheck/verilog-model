-- Seed: 12912888408995000328,7726014785203345639

use std.reflection.all;

entity xinlycvc is
  port (rfgrcaodhf : inout subtype_mirror);
end xinlycvc;

architecture xakit of xinlycvc is
  
begin
  
end xakit;

entity goxs is
  port (k : in real);
end goxs;

use std.reflection.all;

architecture p of goxs is
  shared variable qcwumivb : subtype_mirror;
  shared variable c : subtype_mirror;
begin
  umrab : entity work.xinlycvc
    port map (rfgrcaodhf => c);
  wxqiwudnb : entity work.xinlycvc
    port map (rfgrcaodhf => qcwumivb);
end p;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity phbch is
  port (enbiayytdf : inout physical_subtype_mirror; pdqregtc : linkage integer; yckefqkcyk : inout subtype_mirror; xfubbg : inout std_logic);
end phbch;

use std.reflection.all;

architecture gvih of phbch is
  signal lcnhhefcei : real;
  shared variable svexmacsfg : subtype_mirror;
  shared variable dtm : subtype_mirror;
  shared variable hvathert : subtype_mirror;
begin
  nremw : entity work.xinlycvc
    port map (rfgrcaodhf => hvathert);
  gpjlo : entity work.xinlycvc
    port map (rfgrcaodhf => dtm);
  w : entity work.xinlycvc
    port map (rfgrcaodhf => svexmacsfg);
  xiufoylxkb : entity work.goxs
    port map (k => lcnhhefcei);
  
  -- Single-driven assignments
  lcnhhefcei <= lcnhhefcei;
end gvih;

use std.reflection.all;

entity vo is
  port (awzetrndf : buffer real; hqtbadwrul : inout array_subtype_mirror; chbgrnzjmc : out real_vector(4 downto 4); pnvtqn : buffer time);
end vo;

use std.reflection.all;

architecture ft of vo is
  shared variable pwzv : subtype_mirror;
begin
  jd : entity work.xinlycvc
    port map (rfgrcaodhf => pwzv);
end ft;



-- Seed after: 15470836005437013743,7726014785203345639
