-- Seed: 13840693512685297376,13592003931158285879

entity fpwc is
  port (hgqupprq : out time_vector(3 to 2));
end fpwc;

architecture muv of fpwc is
  
begin
  -- Single-driven assignments
  hgqupprq <= hgqupprq;
end muv;

library ieee;
use ieee.std_logic_1164.all;

entity rbbspqre is
  port (g : out time_vector(2 downto 1); esppdrdqn : in std_logic_vector(3 to 2); jwtmii : inout boolean_vector(1 to 4));
end rbbspqre;

architecture fuqje of rbbspqre is
  signal qihohpxzi : time_vector(3 to 2);
  signal ix : time_vector(3 to 2);
begin
  ajtjhwjj : entity work.fpwc
    port map (hgqupprq => ix);
  bmeiiyu : entity work.fpwc
    port map (hgqupprq => qihohpxzi);
  
  -- Single-driven assignments
  g <= (16#6C0B.1_9_9# us, 1_1_1_3_2.003 fs);
  jwtmii <= jwtmii;
end fuqje;

entity fjhnjon is
  port (a : buffer integer; byfsvldz : linkage time; lzuiobx : buffer integer);
end fjhnjon;

architecture wg of fjhnjon is
  signal coiwncmg : time_vector(3 to 2);
  signal bjnaloh : time_vector(3 to 2);
  signal s : time_vector(3 to 2);
  signal ixkiytne : time_vector(3 to 2);
begin
  amn : entity work.fpwc
    port map (hgqupprq => ixkiytne);
  m : entity work.fpwc
    port map (hgqupprq => s);
  eyzxocui : entity work.fpwc
    port map (hgqupprq => bjnaloh);
  hc : entity work.fpwc
    port map (hgqupprq => coiwncmg);
  
  -- Single-driven assignments
  a <= lzuiobx;
  lzuiobx <= lzuiobx;
end wg;



-- Seed after: 3340259224569400640,13592003931158285879
