-- Seed: 5625199334477845791,3316342841050048249

entity yormgkl is
  port (ymzkgihm : out time; oq : out time);
end yormgkl;

architecture g of yormgkl is
  
begin
  -- Single-driven assignments
  oq <= 1 min;
  ymzkgihm <= 1 hr;
end g;

library ieee;
use ieee.std_logic_1164.all;

entity r is
  port (uttkhal : buffer character; bk : inout std_logic_vector(0 to 1); z : linkage severity_level; rcdfb : buffer bit);
end r;

architecture wkrp of r is
  signal kpcfnbwktg : time;
  signal nyakjr : time;
  signal byvodf : time;
  signal sw : time;
begin
  zgfy : entity work.yormgkl
    port map (ymzkgihm => sw, oq => byvodf);
  e : entity work.yormgkl
    port map (ymzkgihm => nyakjr, oq => kpcfnbwktg);
  
  -- Single-driven assignments
  rcdfb <= rcdfb;
  uttkhal <= uttkhal;
  
  -- Multi-driven assignments
  bk <= bk;
end wkrp;

library ieee;
use ieee.std_logic_1164.all;

entity cslds is
  port (jscdyb : inout std_logic_vector(1 to 0); fjkzytslx : in integer);
end cslds;

architecture bojuyk of cslds is
  
begin
  -- Multi-driven assignments
  jscdyb <= jscdyb;
  jscdyb <= jscdyb;
  jscdyb <= jscdyb;
end bojuyk;

entity y is
  port (vtjcd : in real; clfiktsid : linkage character);
end y;

library ieee;
use ieee.std_logic_1164.all;

architecture wajuhih of y is
  signal rhrzcemezj : time;
  signal vwlmsozlis : time;
  signal op : bit;
  signal qguwps : severity_level;
  signal x : std_logic_vector(0 to 1);
  signal tleyximih : character;
  signal hqkyvh : time;
  signal skbmwsbovi : time;
  signal oa : time;
  signal uk : time;
begin
  dth : entity work.yormgkl
    port map (ymzkgihm => uk, oq => oa);
  g : entity work.yormgkl
    port map (ymzkgihm => skbmwsbovi, oq => hqkyvh);
  u : entity work.r
    port map (uttkhal => tleyximih, bk => x, z => qguwps, rcdfb => op);
  f : entity work.yormgkl
    port map (ymzkgihm => vwlmsozlis, oq => rhrzcemezj);
end wajuhih;



-- Seed after: 13312355057497298867,3316342841050048249
