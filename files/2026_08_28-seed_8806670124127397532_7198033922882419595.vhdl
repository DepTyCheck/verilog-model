-- Seed: 8806670124127397532,7198033922882419595

entity gro is
  port (yxxibltlp : in integer; uel : in boolean; p : buffer real);
end gro;

architecture newwsuq of gro is
  
begin
  -- Single-driven assignments
  p <= p;
end newwsuq;

library ieee;
use ieee.std_logic_1164.all;

entity fmpzly is
  port (nhtn : out string(4 to 4); jsx : out time; wfzoiimxzx : in std_logic_vector(3 downto 2); rhypwybem : in std_logic_vector(3 to 4));
end fmpzly;

architecture czcaupcp of fmpzly is
  signal fyssitjm : real;
  signal dmq : real;
  signal qa : integer;
  signal e : real;
  signal w : boolean;
  signal cktbk : integer;
  signal gwirsxsod : real;
  signal nk : boolean;
  signal hwe : integer;
begin
  hgycubnquh : entity work.gro
    port map (yxxibltlp => hwe, uel => nk, p => gwirsxsod);
  nzggaa : entity work.gro
    port map (yxxibltlp => cktbk, uel => w, p => e);
  x : entity work.gro
    port map (yxxibltlp => qa, uel => nk, p => dmq);
  xhdp : entity work.gro
    port map (yxxibltlp => hwe, uel => nk, p => fyssitjm);
  
  -- Single-driven assignments
  nhtn <= (others => 'g');
  qa <= hwe;
  hwe <= hwe;
  jsx <= 42103.3 fs;
end czcaupcp;



-- Seed after: 2361335831361839370,7198033922882419595
