-- Seed: 16237846089432144445,12269339630485015285

entity lynyos is
  port (ji : out time);
end lynyos;

architecture zed of lynyos is
  
begin
  -- Single-driven assignments
  ji <= 8#52063.2_4_2# ns;
end zed;

library ieee;
use ieee.std_logic_1164.all;

entity rmrweax is
  port (k : inout std_logic_vector(0 to 1); hkq : buffer time; wod : out time);
end rmrweax;

architecture pisw of rmrweax is
  signal txo : time;
  signal mj : time;
begin
  tjfitsjmcz : entity work.lynyos
    port map (ji => wod);
  kdodsznydh : entity work.lynyos
    port map (ji => mj);
  su : entity work.lynyos
    port map (ji => txo);
  
  -- Single-driven assignments
  hkq <= wod;
  
  -- Multi-driven assignments
  k <= k;
  k <= "XW";
end pisw;

entity suye is
  port (ycklyrmfo : linkage character);
end suye;

architecture bvu of suye is
  signal mtp : time;
  signal dt : time;
  signal rlbyjux : time;
  signal giiecf : time;
begin
  fx : entity work.lynyos
    port map (ji => giiecf);
  xkbo : entity work.lynyos
    port map (ji => rlbyjux);
  pnjqmnm : entity work.lynyos
    port map (ji => dt);
  cjgmswxqaa : entity work.lynyos
    port map (ji => mtp);
end bvu;



-- Seed after: 18063099246578324401,12269339630485015285
