-- Seed: 12474390845734480507,662889661651915549

library ieee;
use ieee.std_logic_1164.all;

entity iecy is
  port (xpnjnzh : linkage std_logic; dou : inout severity_level; vuylm : linkage boolean_vector(3 downto 1); mof : linkage integer);
end iecy;

architecture suvyufxv of iecy is
  
begin
  -- Single-driven assignments
  dou <= dou;
end suvyufxv;

entity xiuvivvf is
  port (cdypqoxo : in boolean_vector(0 to 2); eevt : linkage boolean; rzjvq : linkage integer);
end xiuvivvf;

library ieee;
use ieee.std_logic_1164.all;

architecture knkmu of xiuvivvf is
  signal ywoswnnxzv : boolean_vector(3 downto 1);
  signal zhii : severity_level;
  signal o : std_logic;
begin
  ihulnz : entity work.iecy
    port map (xpnjnzh => o, dou => zhii, vuylm => ywoswnnxzv, mof => rzjvq);
  
  -- Multi-driven assignments
  o <= o;
  o <= o;
  o <= o;
  o <= 'U';
end knkmu;

library ieee;
use ieee.std_logic_1164.all;

entity mi is
  port (lqwobnaln : inout string(2 to 5); uum : out std_logic; lwoq : inout std_logic_vector(3 downto 0));
end mi;

library ieee;
use ieee.std_logic_1164.all;

architecture dw of mi is
  signal yfnyiums : integer;
  signal pwbhz : boolean;
  signal rfeeu : boolean_vector(0 to 2);
  signal nmxymks : integer;
  signal hep : boolean_vector(3 downto 1);
  signal fta : severity_level;
  signal werd : std_logic;
  signal eiersou : integer;
  signal myd : boolean_vector(3 downto 1);
  signal qy : severity_level;
  signal jxtxeizrne : integer;
  signal ob : boolean_vector(3 downto 1);
  signal nrlyaeomm : severity_level;
begin
  esh : entity work.iecy
    port map (xpnjnzh => uum, dou => nrlyaeomm, vuylm => ob, mof => jxtxeizrne);
  crvcauu : entity work.iecy
    port map (xpnjnzh => uum, dou => qy, vuylm => myd, mof => eiersou);
  sagezlagmd : entity work.iecy
    port map (xpnjnzh => werd, dou => fta, vuylm => hep, mof => nmxymks);
  xfdvthqy : entity work.xiuvivvf
    port map (cdypqoxo => rfeeu, eevt => pwbhz, rzjvq => yfnyiums);
  
  -- Single-driven assignments
  lqwobnaln <= lqwobnaln;
  rfeeu <= (TRUE, FALSE, TRUE);
end dw;



-- Seed after: 7525400357046241968,662889661651915549
