-- Seed: 10847259117417717454,1112937151005418631

entity refy is
  port (i : linkage time; ypxo : buffer time_vector(1 to 4); mvqux : linkage time);
end refy;

architecture g of refy is
  
begin
  -- Single-driven assignments
  ypxo <= (16#F_3.8F# fs, 8#551.2_6_6_6_2# us, 2#1_1.00# us, 1 hr);
end g;

entity dapftme is
  port (famii : buffer severity_level; gdfvwfls : out real);
end dapftme;

architecture ebsqvhcoq of dapftme is
  signal tdspbx : time;
  signal dluxgkznrn : time_vector(1 to 4);
  signal iqyx : time;
  signal le : time;
  signal ng : time_vector(1 to 4);
  signal mlmazmz : time;
  signal j : time;
  signal vnsvjl : time_vector(1 to 4);
  signal rhngincwsx : time;
begin
  wcp : entity work.refy
    port map (i => rhngincwsx, ypxo => vnsvjl, mvqux => j);
  khltg : entity work.refy
    port map (i => mlmazmz, ypxo => ng, mvqux => le);
  qm : entity work.refy
    port map (i => iqyx, ypxo => dluxgkznrn, mvqux => tdspbx);
  
  -- Single-driven assignments
  gdfvwfls <= 1.43433;
  famii <= famii;
end ebsqvhcoq;

entity n is
  port (zqoaohb : in integer; ivcexzr : out integer; fapq : linkage time_vector(0 to 1));
end n;

architecture arcrb of n is
  signal jawmat : time;
  signal jnnjs : time_vector(1 to 4);
  signal xjrtwqjv : time;
  signal kdwrs : real;
  signal cljwhbfcbs : severity_level;
  signal s : real;
  signal hszod : severity_level;
  signal pz : time;
  signal bl : time_vector(1 to 4);
  signal iricvexpba : time;
begin
  kxgyebotyp : entity work.refy
    port map (i => iricvexpba, ypxo => bl, mvqux => pz);
  prlvx : entity work.dapftme
    port map (famii => hszod, gdfvwfls => s);
  uzql : entity work.dapftme
    port map (famii => cljwhbfcbs, gdfvwfls => kdwrs);
  sm : entity work.refy
    port map (i => xjrtwqjv, ypxo => jnnjs, mvqux => jawmat);
  
  -- Single-driven assignments
  ivcexzr <= ivcexzr;
end arcrb;

entity cgjsedu is
  port (iacehmyve : linkage time_vector(4 downto 1); qqnlf : in real);
end cgjsedu;

architecture smzv of cgjsedu is
  signal ianp : time;
  signal yvouxorjd : time_vector(1 to 4);
  signal mdec : time;
  signal ly : real;
  signal bjab : severity_level;
  signal dv : real;
  signal ujyxlrljvp : severity_level;
  signal vly : time_vector(0 to 1);
  signal etvwbne : integer;
begin
  ezjx : entity work.n
    port map (zqoaohb => etvwbne, ivcexzr => etvwbne, fapq => vly);
  gzbmzbfpvp : entity work.dapftme
    port map (famii => ujyxlrljvp, gdfvwfls => dv);
  tmuqbrorr : entity work.dapftme
    port map (famii => bjab, gdfvwfls => ly);
  nhzirt : entity work.refy
    port map (i => mdec, ypxo => yvouxorjd, mvqux => ianp);
end smzv;



-- Seed after: 7644674692190240395,1112937151005418631
