-- Seed: 11917639238352243119,1630680796402093529

library ieee;
use ieee.std_logic_1164.all;

entity so is
  port (qriqoad : buffer integer_vector(3 downto 1); j : out std_logic_vector(4 to 3); bf : in boolean; wjzi : inout bit_vector(4 to 2));
end so;



architecture ic of so is
  
begin
  
end ic;

library ieee;
use ieee.std_logic_1164.all;

entity oyrpyo is
  port (argzwiau : out integer; sgyrel : buffer std_logic_vector(4 to 1));
end oyrpyo;

library ieee;
use ieee.std_logic_1164.all;

architecture vnspzojis of oyrpyo is
  signal bgypcocu : bit_vector(4 to 2);
  signal omx : std_logic_vector(4 to 3);
  signal y : integer_vector(3 downto 1);
  signal nztkoafeyi : bit_vector(4 to 2);
  signal vlduqz : boolean;
  signal yua : std_logic_vector(4 to 3);
  signal hgy : integer_vector(3 downto 1);
  signal an : bit_vector(4 to 2);
  signal sojei : boolean;
  signal ifpemsta : std_logic_vector(4 to 3);
  signal wsbe : integer_vector(3 downto 1);
  signal vukazceqse : bit_vector(4 to 2);
  signal ld : boolean;
  signal kmzdvl : integer_vector(3 downto 1);
begin
  vqiyrh : entity work.so
    port map (qriqoad => kmzdvl, j => sgyrel, bf => ld, wjzi => vukazceqse);
  laeycddet : entity work.so
    port map (qriqoad => wsbe, j => ifpemsta, bf => sojei, wjzi => an);
  hunpvkxjb : entity work.so
    port map (qriqoad => hgy, j => yua, bf => vlduqz, wjzi => nztkoafeyi);
  re : entity work.so
    port map (qriqoad => y, j => omx, bf => ld, wjzi => bgypcocu);
end vnspzojis;

library ieee;
use ieee.std_logic_1164.all;

entity amht is
  port (ey : in std_logic_vector(2 to 3); gthe : inout std_logic_vector(1 downto 1));
end amht;



architecture suh of amht is
  
begin
  
end suh;



-- Seed after: 7206441809633460894,1630680796402093529
