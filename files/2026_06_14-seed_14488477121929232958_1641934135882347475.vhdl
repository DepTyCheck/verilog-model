-- Seed: 14488477121929232958,1641934135882347475



entity fn is
  port (xmkpykokk : linkage real_vector(2 to 3); fczujsokjj : inout real; sje : out real; mphlb : out integer);
end fn;



architecture uxn of fn is
  
begin
  
end uxn;

library ieee;
use ieee.std_logic_1164.all;

entity m is
  port (locn : out integer; xpfalfhw : in integer; svlindd : buffer std_logic);
end m;



architecture qzfm of m is
  signal uwndiq : real;
  signal tfjrqk : real;
  signal yb : real_vector(2 to 3);
  signal t : integer;
  signal ku : real;
  signal wselqddgy : real;
  signal ksyu : real_vector(2 to 3);
begin
  ukje : entity work.fn
    port map (xmkpykokk => ksyu, fczujsokjj => wselqddgy, sje => ku, mphlb => t);
  z : entity work.fn
    port map (xmkpykokk => yb, fczujsokjj => tfjrqk, sje => uwndiq, mphlb => locn);
end qzfm;



entity roffxziya is
  port (o : linkage boolean_vector(4 to 2); twndhceklo : linkage real; dtbwlvi : inout character; iqgzhvgaqb : buffer bit_vector(1 downto 0));
end roffxziya;



architecture yqcx of roffxziya is
  signal q : integer;
  signal wamp : real;
  signal gyjgppnjpm : real;
  signal sqfbv : real_vector(2 to 3);
begin
  ovxqaf : entity work.fn
    port map (xmkpykokk => sqfbv, fczujsokjj => gyjgppnjpm, sje => wamp, mphlb => q);
end yqcx;



-- Seed after: 4594464921743696396,1641934135882347475
