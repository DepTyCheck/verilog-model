-- Seed: 15400322129103476641,10754487200446211253

library ieee;
use ieee.std_logic_1164.all;

entity xxb is
  port (f : inout real_vector(1 to 1); tcrahexfhz : linkage time; tpjdt : in std_logic_vector(3 to 0));
end xxb;

architecture yibt of xxb is
  
begin
  -- Single-driven assignments
  f <= (others => 2#1_0_0_0_1.000#);
end yibt;

library ieee;
use ieee.std_logic_1164.all;

entity oopmw is
  port (tncpkonfy : inout integer; mmb : inout string(5 to 2); t : linkage std_logic_vector(1 to 0));
end oopmw;

library ieee;
use ieee.std_logic_1164.all;

architecture whbhqlwr of oopmw is
  signal iw : std_logic_vector(3 to 0);
  signal bykkm : time;
  signal aj : real_vector(1 to 1);
  signal drwoib : std_logic_vector(3 to 0);
  signal tm : time;
  signal quppansl : real_vector(1 to 1);
  signal atxjqkdxff : std_logic_vector(3 to 0);
  signal tohclrg : time;
  signal qtxysr : real_vector(1 to 1);
  signal qqmd : std_logic_vector(3 to 0);
  signal wsawnovs : time;
  signal zbsncinn : real_vector(1 to 1);
begin
  mop : entity work.xxb
    port map (f => zbsncinn, tcrahexfhz => wsawnovs, tpjdt => qqmd);
  htogvjjlao : entity work.xxb
    port map (f => qtxysr, tcrahexfhz => tohclrg, tpjdt => atxjqkdxff);
  ccvrfhro : entity work.xxb
    port map (f => quppansl, tcrahexfhz => tm, tpjdt => drwoib);
  stkutwqc : entity work.xxb
    port map (f => aj, tcrahexfhz => bykkm, tpjdt => iw);
  
  -- Multi-driven assignments
  qqmd <= atxjqkdxff;
end whbhqlwr;



-- Seed after: 3676260422612096119,10754487200446211253
