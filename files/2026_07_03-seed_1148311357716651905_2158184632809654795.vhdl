-- Seed: 1148311357716651905,2158184632809654795

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity brm is
  port (vfefwwygfy : out std_logic; ibi : linkage real; fijomkpv : inout integer_subtype_mirror);
end brm;

architecture xtliwnu of brm is
  
begin
  -- Multi-driven assignments
  vfefwwygfy <= 'Z';
  vfefwwygfy <= vfefwwygfy;
end xtliwnu;

use std.reflection.all;

entity wc is
  port (k : inout physical_value_mirror);
end wc;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture llhar of wc is
  shared variable qbnr : integer_subtype_mirror;
  signal loiedhvb : real;
  signal tqqxvukpk : std_logic;
  shared variable fvkvks : integer_subtype_mirror;
  signal r : real;
  shared variable minet : integer_subtype_mirror;
  signal imy : real;
  signal fb : std_logic;
  shared variable misx : integer_subtype_mirror;
  signal dnhawjsan : real;
  signal t : std_logic;
begin
  gwstrm : entity work.brm
    port map (vfefwwygfy => t, ibi => dnhawjsan, fijomkpv => misx);
  u : entity work.brm
    port map (vfefwwygfy => fb, ibi => imy, fijomkpv => minet);
  tofrny : entity work.brm
    port map (vfefwwygfy => fb, ibi => r, fijomkpv => fvkvks);
  vphpj : entity work.brm
    port map (vfefwwygfy => tqqxvukpk, ibi => loiedhvb, fijomkpv => qbnr);
  
  -- Multi-driven assignments
  tqqxvukpk <= t;
  t <= 'W';
  fb <= t;
  t <= 'X';
end llhar;

library ieee;
use ieee.std_logic_1164.all;

entity opekv is
  port (w : inout std_logic_vector(4 downto 0));
end opekv;

use std.reflection.all;

architecture grvm of opekv is
  shared variable csuvmkfmng : physical_value_mirror;
begin
  uzzkdbe : entity work.wc
    port map (k => csuvmkfmng);
  
  -- Multi-driven assignments
  w <= "Z0H0U";
  w <= "-ZWZ0";
  w <= "LUHZL";
  w <= ('-', '-', 'H', 'W', 'W');
end grvm;



-- Seed after: 3662179989667246182,2158184632809654795
