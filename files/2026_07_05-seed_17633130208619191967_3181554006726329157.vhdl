-- Seed: 17633130208619191967,3181554006726329157

use std.reflection.all;

entity cja is
  port ( ksgxjaaxdg : inout enumeration_value_mirror
  ; chuyvomscb : inout record_value_mirror
  ; hawlukuah : inout access_value_mirror
  ; ijjeyzeclz : out integer_vector(1 to 2)
  );
end cja;

architecture c of cja is
  
begin
  
end c;

use std.reflection.all;

entity xdgfnryetz is
  port (m : inout protected_subtype_mirror; enpvcqc : inout physical_value_mirror);
end xdgfnryetz;

use std.reflection.all;

architecture yel of xdgfnryetz is
  signal cqfurghwk : integer_vector(1 to 2);
  shared variable sy : access_value_mirror;
  shared variable xo : record_value_mirror;
  shared variable zhpbtb : enumeration_value_mirror;
  signal kiyybbg : integer_vector(1 to 2);
  shared variable n : access_value_mirror;
  shared variable fycgute : record_value_mirror;
  shared variable eipdgs : enumeration_value_mirror;
begin
  ergvwruffm : entity work.cja
    port map (ksgxjaaxdg => eipdgs, chuyvomscb => fycgute, hawlukuah => n, ijjeyzeclz => kiyybbg);
  sp : entity work.cja
    port map (ksgxjaaxdg => zhpbtb, chuyvomscb => xo, hawlukuah => sy, ijjeyzeclz => cqfurghwk);
end yel;

entity nr is
  port (znjmtkjhcl : in severity_level);
end nr;

use std.reflection.all;

architecture stjak of nr is
  signal cfqpq : integer_vector(1 to 2);
  shared variable ehfib : access_value_mirror;
  shared variable cc : record_value_mirror;
  shared variable owlel : enumeration_value_mirror;
  signal go : integer_vector(1 to 2);
  shared variable jpxhdu : access_value_mirror;
  shared variable akhrzh : record_value_mirror;
  shared variable dod : enumeration_value_mirror;
  signal b : integer_vector(1 to 2);
  shared variable izbveixe : access_value_mirror;
  shared variable kc : record_value_mirror;
  shared variable kaah : enumeration_value_mirror;
  signal jioxy : integer_vector(1 to 2);
  shared variable axkhtdjvnr : access_value_mirror;
  shared variable roenbjn : record_value_mirror;
  shared variable bclsojbiax : enumeration_value_mirror;
begin
  yrg : entity work.cja
    port map (ksgxjaaxdg => bclsojbiax, chuyvomscb => roenbjn, hawlukuah => axkhtdjvnr, ijjeyzeclz => jioxy);
  ikybszayd : entity work.cja
    port map (ksgxjaaxdg => kaah, chuyvomscb => kc, hawlukuah => izbveixe, ijjeyzeclz => b);
  mzfacsdww : entity work.cja
    port map (ksgxjaaxdg => dod, chuyvomscb => akhrzh, hawlukuah => jpxhdu, ijjeyzeclz => go);
  hmdcue : entity work.cja
    port map (ksgxjaaxdg => owlel, chuyvomscb => cc, hawlukuah => ehfib, ijjeyzeclz => cfqpq);
end stjak;



-- Seed after: 18260179415888019614,3181554006726329157
