-- Seed: 13729015671114848648,9125939553767483053

library ieee;
use ieee.std_logic_1164.all;

entity n is
  port (xpobh : linkage real; zzyrbayr : buffer integer; qoq : in severity_level; o : buffer std_logic);
end n;



architecture eup of n is
  
begin
  
end eup;

library ieee;
use ieee.std_logic_1164.all;

entity qbpako is
  port (frp : out std_logic_vector(4 downto 4));
end qbpako;

library ieee;
use ieee.std_logic_1164.all;

architecture uz of qbpako is
  signal da : integer;
  signal g : real;
  signal plin : severity_level;
  signal yovay : integer;
  signal leo : real;
  signal smbexzbwmn : severity_level;
  signal zmpntk : integer;
  signal qhral : std_logic;
  signal a : severity_level;
  signal rmvjr : integer;
  signal rkedeyt : real;
begin
  gqtl : entity work.n
    port map (xpobh => rkedeyt, zzyrbayr => rmvjr, qoq => a, o => qhral);
  umlicpp : entity work.n
    port map (xpobh => rkedeyt, zzyrbayr => zmpntk, qoq => smbexzbwmn, o => qhral);
  st : entity work.n
    port map (xpobh => leo, zzyrbayr => yovay, qoq => plin, o => qhral);
  dq : entity work.n
    port map (xpobh => g, zzyrbayr => da, qoq => a, o => qhral);
end uz;



-- Seed after: 7577296598305920159,9125939553767483053
