-- Seed: 160666305499878458,4404421571376382767

entity uiqwyietxy is
  port (orhirh : linkage time; lblsbaiby : out severity_level);
end uiqwyietxy;

architecture ayudgao of uiqwyietxy is
  
begin
  -- Single-driven assignments
  lblsbaiby <= NOTE;
end ayudgao;

entity kzmegcwr is
  port (qmrgsdmk : buffer real);
end kzmegcwr;

architecture id of kzmegcwr is
  signal nq : severity_level;
  signal tkaiikjkz : time;
  signal dkmp : severity_level;
  signal xmgo : time;
  signal rbh : severity_level;
  signal aia : time;
  signal q : severity_level;
  signal wmxrswuc : time;
begin
  hpnixyfp : entity work.uiqwyietxy
    port map (orhirh => wmxrswuc, lblsbaiby => q);
  o : entity work.uiqwyietxy
    port map (orhirh => aia, lblsbaiby => rbh);
  afiqmo : entity work.uiqwyietxy
    port map (orhirh => xmgo, lblsbaiby => dkmp);
  pfmgth : entity work.uiqwyietxy
    port map (orhirh => tkaiikjkz, lblsbaiby => nq);
end id;

entity qlk is
  port (zpb : linkage boolean; svn : out integer; ie : out time);
end qlk;

architecture c of qlk is
  signal b : severity_level;
  signal chmqvuv : time;
  signal fi : severity_level;
  signal ygd : severity_level;
  signal vs : time;
  signal ydct : severity_level;
  signal ncz : time;
begin
  uttaftznmz : entity work.uiqwyietxy
    port map (orhirh => ncz, lblsbaiby => ydct);
  xsjvz : entity work.uiqwyietxy
    port map (orhirh => vs, lblsbaiby => ygd);
  djvpsu : entity work.uiqwyietxy
    port map (orhirh => ie, lblsbaiby => fi);
  pyjdakehfm : entity work.uiqwyietxy
    port map (orhirh => chmqvuv, lblsbaiby => b);
  
  -- Single-driven assignments
  svn <= svn;
end c;



-- Seed after: 12303236639356657716,4404421571376382767
