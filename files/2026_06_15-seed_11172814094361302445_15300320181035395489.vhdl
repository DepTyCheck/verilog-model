-- Seed: 11172814094361302445,15300320181035395489

entity ealytj is
  port (koeu : linkage real);
end ealytj;

architecture fuvtjtk of ealytj is
  
begin
  
end fuvtjtk;

entity blqt is
  port (iyq : buffer time_vector(1 downto 3));
end blqt;

architecture stp of blqt is
  signal ct : real;
  signal k : real;
  signal a : real;
begin
  lahctdndev : entity work.ealytj
    port map (koeu => a);
  t : entity work.ealytj
    port map (koeu => k);
  cnbanpyfs : entity work.ealytj
    port map (koeu => ct);
  
  -- Single-driven assignments
  iyq <= (others => 0 ns);
end stp;

library ieee;
use ieee.std_logic_1164.all;

entity nrdcyxv is
  port ( wkmkty : inout std_logic
  ; rdvsymnnl : buffer integer_vector(0 downto 0)
  ; hbs : buffer std_logic_vector(0 to 3)
  ; ffn : in std_logic_vector(3 to 1)
  );
end nrdcyxv;

architecture ke of nrdcyxv is
  signal a : real;
  signal jc : time_vector(1 downto 3);
  signal v : time_vector(1 downto 3);
begin
  asag : entity work.blqt
    port map (iyq => v);
  vdyaundrz : entity work.blqt
    port map (iyq => jc);
  chq : entity work.ealytj
    port map (koeu => a);
  
  -- Single-driven assignments
  rdvsymnnl <= (others => 16#D#);
  
  -- Multi-driven assignments
  hbs <= ('0', 'X', 'W', 'L');
  hbs <= "H0UL";
  hbs <= ('Z', '-', '-', '0');
  hbs <= "ULLZ";
end ke;

entity slhwtmhdf is
  port (jkaylcbf : inout time_vector(3 downto 4); fslgj : buffer time; jbvafcec : in real);
end slhwtmhdf;

architecture gfsg of slhwtmhdf is
  
begin
  -- Single-driven assignments
  fslgj <= 2.0 ps;
  jkaylcbf <= (others => 0 ns);
end gfsg;



-- Seed after: 8833440691934915808,15300320181035395489
