-- Seed: 4976452777851758496,1112937151005418631

entity xbaly is
  port (dn : in boolean_vector(3 downto 2); i : in real; fsxsen : inout integer);
end xbaly;

architecture jm of xbaly is
  
begin
  -- Single-driven assignments
  fsxsen <= fsxsen;
end jm;

library ieee;
use ieee.std_logic_1164.all;

entity nubrzwjnns is
  port (v : buffer std_logic_vector(4 to 2); xskt : buffer real; btwsi : inout real; gj : in time);
end nubrzwjnns;

architecture lsygadzqzz of nubrzwjnns is
  signal ibidi : integer;
  signal uaczxcrnr : boolean_vector(3 downto 2);
  signal g : integer;
  signal bjo : boolean_vector(3 downto 2);
begin
  vy : entity work.xbaly
    port map (dn => bjo, i => btwsi, fsxsen => g);
  loh : entity work.xbaly
    port map (dn => uaczxcrnr, i => btwsi, fsxsen => ibidi);
  
  -- Single-driven assignments
  btwsi <= btwsi;
  xskt <= 1.4;
end lsygadzqzz;

library ieee;
use ieee.std_logic_1164.all;

entity nma is
  port (xcoj : in std_logic_vector(2 downto 2); exhvzjfv : inout real; bz : in boolean);
end nma;

library ieee;
use ieee.std_logic_1164.all;

architecture wkshond of nma is
  signal tkdrq : integer;
  signal a : real;
  signal wfyolxk : boolean_vector(3 downto 2);
  signal hjrmx : time;
  signal wjtxswycp : real;
  signal ghuklmzezy : std_logic_vector(4 to 2);
  signal zehnau : time;
  signal wbjfp : real;
  signal ddn : real;
  signal dsmw : time;
  signal ojdjs : real;
  signal l : real;
  signal atsy : std_logic_vector(4 to 2);
begin
  nuaw : entity work.nubrzwjnns
    port map (v => atsy, xskt => l, btwsi => ojdjs, gj => dsmw);
  hl : entity work.nubrzwjnns
    port map (v => atsy, xskt => ddn, btwsi => wbjfp, gj => zehnau);
  toxdldqkn : entity work.nubrzwjnns
    port map (v => ghuklmzezy, xskt => wjtxswycp, btwsi => exhvzjfv, gj => hjrmx);
  ititlfe : entity work.xbaly
    port map (dn => wfyolxk, i => a, fsxsen => tkdrq);
  
  -- Single-driven assignments
  a <= 2.1121;
  zehnau <= hjrmx;
end wkshond;

entity yxkeku is
  port (tfwv : buffer real_vector(1 to 4); hftihnaly : inout severity_level; veqesz : in boolean_vector(4 to 3); hsjhldmtqr : inout real);
end yxkeku;

library ieee;
use ieee.std_logic_1164.all;

architecture ptcygyrwwb of yxkeku is
  signal ftgynxeiff : boolean;
  signal xwfqzowzz : std_logic_vector(2 downto 2);
  signal cgbq : integer;
  signal vzdj : boolean_vector(3 downto 2);
  signal fmwmvgez : integer;
  signal g : real;
  signal nlxkgog : boolean_vector(3 downto 2);
  signal cfntrhxzh : integer;
  signal qxlcsl : real;
  signal bdfnpqry : boolean_vector(3 downto 2);
begin
  aqyl : entity work.xbaly
    port map (dn => bdfnpqry, i => qxlcsl, fsxsen => cfntrhxzh);
  tvicekju : entity work.xbaly
    port map (dn => nlxkgog, i => g, fsxsen => fmwmvgez);
  ijwgyp : entity work.xbaly
    port map (dn => vzdj, i => qxlcsl, fsxsen => cgbq);
  r : entity work.nma
    port map (xcoj => xwfqzowzz, exhvzjfv => g, bz => ftgynxeiff);
  
  -- Single-driven assignments
  nlxkgog <= bdfnpqry;
  hsjhldmtqr <= qxlcsl;
  ftgynxeiff <= ftgynxeiff;
end ptcygyrwwb;



-- Seed after: 15825635494492409497,1112937151005418631
