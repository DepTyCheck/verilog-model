-- Seed: 7727772106384035610,8118127366649987907

entity g is
  port (dp : linkage boolean_vector(4 to 4); n : out real);
end g;

architecture e of g is
  
begin
  -- Single-driven assignments
  n <= 16#2.FA#;
end e;

entity yx is
  port (rgwygw : in boolean_vector(3 downto 1); gkrfvxrhx : buffer time_vector(2 to 4); dzvgsl : linkage real; lam : inout real);
end yx;

architecture fhe of yx is
  signal h : real;
  signal owefdlya : boolean_vector(4 to 4);
  signal elgs : boolean_vector(4 to 4);
  signal tchwzjfb : real;
  signal pgx : boolean_vector(4 to 4);
  signal nzs : real;
  signal mgemztlqcc : boolean_vector(4 to 4);
begin
  xmkjy : entity work.g
    port map (dp => mgemztlqcc, n => nzs);
  jjffcncpql : entity work.g
    port map (dp => pgx, n => tchwzjfb);
  envxvd : entity work.g
    port map (dp => elgs, n => lam);
  sqmmbfjh : entity work.g
    port map (dp => owefdlya, n => h);
  
  -- Single-driven assignments
  gkrfvxrhx <= (2 sec, 00 fs, 1_3_0_4_4.4_2_4 ms);
end fhe;

library ieee;
use ieee.std_logic_1164.all;

entity zcpdssrh is
  port (zasi : buffer std_logic_vector(2 downto 0); oxyiw : inout real; vgcbaeeukm : inout real; prxszdfcq : inout integer);
end zcpdssrh;

architecture e of zcpdssrh is
  signal bnc : real;
  signal cui : real;
  signal cubgzlhgl : time_vector(2 to 4);
  signal obweyvty : boolean_vector(3 downto 1);
begin
  tsisja : entity work.yx
    port map (rgwygw => obweyvty, gkrfvxrhx => cubgzlhgl, dzvgsl => cui, lam => bnc);
  
  -- Single-driven assignments
  prxszdfcq <= 2#1_0_0_0#;
  obweyvty <= (FALSE, FALSE, TRUE);
  
  -- Multi-driven assignments
  zasi <= "LUW";
end e;



-- Seed after: 6905630450763518579,8118127366649987907
