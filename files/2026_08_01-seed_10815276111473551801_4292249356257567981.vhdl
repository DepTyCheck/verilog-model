-- Seed: 10815276111473551801,4292249356257567981

entity fcg is
  port (bvba : linkage real);
end fcg;

architecture qtbfyark of fcg is
  
begin
  
end qtbfyark;

entity bos is
  port (cw : in boolean; ejimv : buffer time; nsiepju : inout time);
end bos;

architecture jyc of bos is
  signal ozh : real;
  signal it : real;
  signal sgleezbi : real;
  signal ccj : real;
begin
  ekslasum : entity work.fcg
    port map (bvba => ccj);
  opxj : entity work.fcg
    port map (bvba => sgleezbi);
  azjfae : entity work.fcg
    port map (bvba => it);
  zxt : entity work.fcg
    port map (bvba => ozh);
  
  -- Single-driven assignments
  ejimv <= nsiepju;
end jyc;

library ieee;
use ieee.std_logic_1164.all;

entity xge is
  port (lx : in std_logic_vector(2 downto 0));
end xge;

architecture hydrqa of xge is
  signal ou : real;
  signal gjqhohe : real;
  signal fwblfep : real;
begin
  oqocaqsqx : entity work.fcg
    port map (bvba => fwblfep);
  ywqq : entity work.fcg
    port map (bvba => gjqhohe);
  wghcfulp : entity work.fcg
    port map (bvba => ou);
end hydrqa;

entity d is
  port (dywaib : buffer boolean; qpndjgpf : out integer);
end d;

architecture dgpzewje of d is
  signal wnondt : real;
  signal lj : real;
  signal rgfjtowsmk : real;
begin
  jeyfoi : entity work.fcg
    port map (bvba => rgfjtowsmk);
  bygscovd : entity work.fcg
    port map (bvba => lj);
  xkhjhu : entity work.fcg
    port map (bvba => wnondt);
  
  -- Single-driven assignments
  qpndjgpf <= qpndjgpf;
  dywaib <= dywaib;
end dgpzewje;



-- Seed after: 1083513172372980828,4292249356257567981
