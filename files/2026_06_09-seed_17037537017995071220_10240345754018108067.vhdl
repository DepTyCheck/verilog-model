-- Seed: 17037537017995071220,10240345754018108067

library ieee;
use ieee.std_logic_1164.all;

entity hip is
  port (kg : out time; chfl : in std_logic; elb : buffer boolean_vector(0 to 0));
end hip;



architecture zqoq of hip is
  
begin
  
end zqoq;



entity mb is
  port (uq : buffer bit_vector(4 downto 2); marob : buffer integer; isanez : in time_vector(3 to 1));
end mb;

library ieee;
use ieee.std_logic_1164.all;

architecture pzmlbspdg of mb is
  signal csj : boolean_vector(0 to 0);
  signal sormkrbwiq : time;
  signal yu : boolean_vector(0 to 0);
  signal advjxlloo : std_logic;
  signal grskueupy : time;
  signal o : boolean_vector(0 to 0);
  signal ddt : std_logic;
  signal boi : time;
begin
  bdb : entity work.hip
    port map (kg => boi, chfl => ddt, elb => o);
  j : entity work.hip
    port map (kg => grskueupy, chfl => advjxlloo, elb => yu);
  foqwxrim : entity work.hip
    port map (kg => sormkrbwiq, chfl => ddt, elb => csj);
end pzmlbspdg;



-- Seed after: 13390098729939410241,10240345754018108067
