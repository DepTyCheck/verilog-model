-- Seed: 9170270222025881397,8421704836678237495

entity krrgbngepj is
  port (chuokwi : buffer severity_level; rtu : linkage real);
end krrgbngepj;

architecture ba of krrgbngepj is
  
begin
  
end ba;

library ieee;
use ieee.std_logic_1164.all;

entity yemfkqpxd is
  port (fpxdep : buffer std_logic_vector(2 downto 3); zhdwfbvo : inout std_logic_vector(3 downto 0));
end yemfkqpxd;

architecture uqpt of yemfkqpxd is
  signal sfkmeirlb : real;
  signal le : severity_level;
  signal mgwmwlag : real;
  signal bhduuvbxny : severity_level;
  signal cxo : real;
  signal dwixteq : severity_level;
  signal svfkadcq : real;
  signal dqtlo : severity_level;
begin
  shwbl : entity work.krrgbngepj
    port map (chuokwi => dqtlo, rtu => svfkadcq);
  awpcwfszla : entity work.krrgbngepj
    port map (chuokwi => dwixteq, rtu => cxo);
  bozpundr : entity work.krrgbngepj
    port map (chuokwi => bhduuvbxny, rtu => mgwmwlag);
  fuvi : entity work.krrgbngepj
    port map (chuokwi => le, rtu => sfkmeirlb);
  
  -- Multi-driven assignments
  zhdwfbvo <= "01Z0";
end uqpt;



-- Seed after: 932972031153664769,8421704836678237495
