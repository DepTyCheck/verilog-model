-- Seed: 12284463928388767435,8437298063418820479

entity xi is
  port (tirmwaw : inout string(2 to 1); jsqyyiegeg : buffer integer; gtpi : buffer time);
end xi;

architecture r of xi is
  
begin
  -- Single-driven assignments
  gtpi <= 0.2_0_3 ps;
end r;

entity avd is
  port (z : linkage character; adbhfelks : linkage boolean_vector(2 to 1));
end avd;

architecture vfxoht of avd is
  signal borllhr : time;
  signal d : integer;
  signal sh : string(2 to 1);
  signal xjltijk : time;
  signal xwvoq : integer;
  signal xynz : string(2 to 1);
  signal havkueks : time;
  signal xzmyxuevdg : integer;
  signal ufqijuz : string(2 to 1);
begin
  si : entity work.xi
    port map (tirmwaw => ufqijuz, jsqyyiegeg => xzmyxuevdg, gtpi => havkueks);
  m : entity work.xi
    port map (tirmwaw => xynz, jsqyyiegeg => xwvoq, gtpi => xjltijk);
  yfi : entity work.xi
    port map (tirmwaw => sh, jsqyyiegeg => d, gtpi => borllhr);
end vfxoht;

entity qjbu is
  port (aqur : linkage string(2 to 2); tpxqmfmgrk : inout integer);
end qjbu;

architecture soy of qjbu is
  signal net : time;
  signal iw : integer;
  signal kyx : string(2 to 1);
  signal jeaudrnfh : time;
  signal aarf : string(2 to 1);
  signal sko : time;
  signal db : integer;
  signal rdfsxlul : string(2 to 1);
  signal jchqgai : time;
  signal jmz : integer;
  signal w : string(2 to 1);
begin
  gvdfpj : entity work.xi
    port map (tirmwaw => w, jsqyyiegeg => jmz, gtpi => jchqgai);
  rcsugid : entity work.xi
    port map (tirmwaw => rdfsxlul, jsqyyiegeg => db, gtpi => sko);
  qtwgk : entity work.xi
    port map (tirmwaw => aarf, jsqyyiegeg => tpxqmfmgrk, gtpi => jeaudrnfh);
  ym : entity work.xi
    port map (tirmwaw => kyx, jsqyyiegeg => iw, gtpi => net);
end soy;



-- Seed after: 16707242804716383512,8437298063418820479
