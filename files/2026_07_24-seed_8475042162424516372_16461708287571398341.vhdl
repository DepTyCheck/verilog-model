-- Seed: 8475042162424516372,16461708287571398341

library ieee;
use ieee.std_logic_1164.all;

entity tyncfcehvt is
  port (ojwpyo : buffer integer; yjht : buffer boolean_vector(0 downto 3); owseybhek : inout std_logic_vector(3 to 2));
end tyncfcehvt;

architecture tizclvu of tyncfcehvt is
  
begin
  -- Single-driven assignments
  ojwpyo <= ojwpyo;
  yjht <= yjht;
end tizclvu;

library ieee;
use ieee.std_logic_1164.all;

entity odkfdo is
  port (plfshicf : out real; yaocp : buffer std_logic; ziakwilq : in integer_vector(3 downto 2); dpf : linkage integer);
end odkfdo;

library ieee;
use ieee.std_logic_1164.all;

architecture eirthr of odkfdo is
  signal fb : std_logic_vector(3 to 2);
  signal fcyilceozq : boolean_vector(0 downto 3);
  signal mhb : integer;
  signal x : boolean_vector(0 downto 3);
  signal tlcmprtbot : integer;
  signal gwggm : std_logic_vector(3 to 2);
  signal fikf : boolean_vector(0 downto 3);
  signal maqwa : integer;
begin
  ypf : entity work.tyncfcehvt
    port map (ojwpyo => maqwa, yjht => fikf, owseybhek => gwggm);
  offpcwsy : entity work.tyncfcehvt
    port map (ojwpyo => tlcmprtbot, yjht => x, owseybhek => gwggm);
  alpydim : entity work.tyncfcehvt
    port map (ojwpyo => mhb, yjht => fcyilceozq, owseybhek => fb);
  
  -- Multi-driven assignments
  yaocp <= yaocp;
  fb <= gwggm;
end eirthr;

entity f is
  port (tkxytszq : out real; evuyoj : linkage real_vector(2 to 1));
end f;

library ieee;
use ieee.std_logic_1164.all;

architecture pjz of f is
  signal w : boolean_vector(0 downto 3);
  signal gqtwpjf : integer;
  signal fnprbha : std_logic_vector(3 to 2);
  signal ozh : boolean_vector(0 downto 3);
  signal nlqlhwty : integer;
begin
  guxjvdsgpr : entity work.tyncfcehvt
    port map (ojwpyo => nlqlhwty, yjht => ozh, owseybhek => fnprbha);
  aaajq : entity work.tyncfcehvt
    port map (ojwpyo => gqtwpjf, yjht => w, owseybhek => fnprbha);
  
  -- Single-driven assignments
  tkxytszq <= 32.2_2_3_2_1;
end pjz;



-- Seed after: 10940542887964559201,16461708287571398341
