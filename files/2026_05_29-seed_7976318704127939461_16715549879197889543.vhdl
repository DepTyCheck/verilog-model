-- Seed: 7976318704127939461,16715549879197889543



entity onbxugxd is
  port (abliauiazu : out time);
end onbxugxd;



architecture fpvqrtenry of onbxugxd is
  
begin
  
end fpvqrtenry;

library ieee;
use ieee.std_logic_1164.all;

entity v is
  port (z : buffer bit; ybzbx : inout std_logic);
end v;



architecture kvax of v is
  signal hoegvtb : time;
  signal gdgf : time;
  signal pitkxieeax : time;
  signal vmgkx : time;
begin
  rvzedymo : entity work.onbxugxd
    port map (abliauiazu => vmgkx);
  qqyfeo : entity work.onbxugxd
    port map (abliauiazu => pitkxieeax);
  asabr : entity work.onbxugxd
    port map (abliauiazu => gdgf);
  nwuozfqi : entity work.onbxugxd
    port map (abliauiazu => hoegvtb);
end kvax;

library ieee;
use ieee.std_logic_1164.all;

entity jtsu is
  port (pbemldi : buffer std_logic_vector(1 to 3); zihjb : inout std_logic; wl : buffer bit; tod : buffer std_logic);
end jtsu;



architecture vevbb of jtsu is
  
begin
  
end vevbb;

library ieee;
use ieee.std_logic_1164.all;

entity w is
  port (zjfml : inout std_logic; hosklaxjnq : inout integer_vector(1 to 0); uiddwrmsp : inout real_vector(0 downto 4));
end w;

library ieee;
use ieee.std_logic_1164.all;

architecture liywdbnju of w is
  signal rnuvbekt : time;
  signal mftgcuu : time;
  signal cmx : std_logic;
  signal nowsnlrh : bit;
  signal ft : bit;
  signal kms : std_logic_vector(1 to 3);
begin
  xmuefv : entity work.jtsu
    port map (pbemldi => kms, zihjb => zjfml, wl => ft, tod => zjfml);
  he : entity work.jtsu
    port map (pbemldi => kms, zihjb => zjfml, wl => nowsnlrh, tod => cmx);
  flhrjeke : entity work.onbxugxd
    port map (abliauiazu => mftgcuu);
  iyzucuoflu : entity work.onbxugxd
    port map (abliauiazu => rnuvbekt);
end liywdbnju;



-- Seed after: 12747575914952342476,16715549879197889543
