-- Seed: 1892685080595981185,14094562573555574003

entity zcv is
  port (fcvc : in string(5 downto 4); rvwatpne : inout real; fqa : out real);
end zcv;

architecture eahrhqav of zcv is
  
begin
  -- Single-driven assignments
  fqa <= 16#7.B7AF#;
  rvwatpne <= rvwatpne;
end eahrhqav;

library ieee;
use ieee.std_logic_1164.all;

entity olygsbdmug is
  port (xbitgzdjy : out std_logic_vector(1 to 4));
end olygsbdmug;

architecture rorosss of olygsbdmug is
  signal ir : real;
  signal k : real;
  signal lyez : real;
  signal u : real;
  signal jh : string(5 downto 4);
  signal bg : real;
  signal ss : real;
  signal unnsree : string(5 downto 4);
  signal kc : real;
  signal ud : real;
  signal kluj : string(5 downto 4);
begin
  bgvuje : entity work.zcv
    port map (fcvc => kluj, rvwatpne => ud, fqa => kc);
  kmprcqylep : entity work.zcv
    port map (fcvc => unnsree, rvwatpne => ss, fqa => bg);
  deivyyh : entity work.zcv
    port map (fcvc => jh, rvwatpne => u, fqa => lyez);
  mf : entity work.zcv
    port map (fcvc => unnsree, rvwatpne => k, fqa => ir);
  
  -- Single-driven assignments
  unnsree <= kluj;
  jh <= ('p', 'j');
  kluj <= "wz";
  
  -- Multi-driven assignments
  xbitgzdjy <= "-LLX";
  xbitgzdjy <= "ZWU1";
  xbitgzdjy <= xbitgzdjy;
  xbitgzdjy <= ('-', 'W', 'U', '1');
end rorosss;



-- Seed after: 10080538181790297325,14094562573555574003
