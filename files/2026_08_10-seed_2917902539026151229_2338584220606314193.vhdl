-- Seed: 2917902539026151229,2338584220606314193

entity iorvvf is
  port (japzu : out time; pwgjp : in character; rkl : out bit; slagajqzw : out real);
end iorvvf;

architecture xdq of iorvvf is
  
begin
  
end xdq;

library ieee;
use ieee.std_logic_1164.all;

entity wrvyxfpk is
  port (snw : buffer std_logic; zl : linkage std_logic_vector(2 downto 0); wlcnrhwuo : buffer integer);
end wrvyxfpk;

architecture rahyq of wrvyxfpk is
  signal tq : real;
  signal irg : bit;
  signal a : character;
  signal vr : time;
  signal wnyq : real;
  signal x : bit;
  signal btlxhu : time;
  signal cjkmipto : real;
  signal vqlyccdi : bit;
  signal jsa : character;
  signal sanclgh : time;
  signal xebqjij : real;
  signal yehomx : bit;
  signal lgl : character;
  signal ihhjfftwn : time;
begin
  zncp : entity work.iorvvf
    port map (japzu => ihhjfftwn, pwgjp => lgl, rkl => yehomx, slagajqzw => xebqjij);
  ruptju : entity work.iorvvf
    port map (japzu => sanclgh, pwgjp => jsa, rkl => vqlyccdi, slagajqzw => cjkmipto);
  jzkleqvwlh : entity work.iorvvf
    port map (japzu => btlxhu, pwgjp => lgl, rkl => x, slagajqzw => wnyq);
  ouzxtl : entity work.iorvvf
    port map (japzu => vr, pwgjp => a, rkl => irg, slagajqzw => tq);
  
  -- Single-driven assignments
  a <= lgl;
  
  -- Multi-driven assignments
  snw <= '-';
  snw <= 'W';
end rahyq;

library ieee;
use ieee.std_logic_1164.all;

entity mmqgcrvr is
  port (akz : in boolean_vector(3 to 0); y : out string(2 downto 5); qhurc : inout character; pvqqjpin : buffer std_logic);
end mmqgcrvr;

architecture yvysi of mmqgcrvr is
  signal fk : real;
  signal zkqolwce : bit;
  signal nhwxvk : time;
  signal mzuwd : real;
  signal tn : bit;
  signal vrcrx : time;
  signal zevv : real;
  signal vdlhta : bit;
  signal biuqkkpc : character;
  signal xegatm : time;
  signal idhcdica : real;
  signal sudrtv : bit;
  signal gkasnu : character;
  signal ovmjqfoejo : time;
begin
  k : entity work.iorvvf
    port map (japzu => ovmjqfoejo, pwgjp => gkasnu, rkl => sudrtv, slagajqzw => idhcdica);
  byzguw : entity work.iorvvf
    port map (japzu => xegatm, pwgjp => biuqkkpc, rkl => vdlhta, slagajqzw => zevv);
  apadejfhqi : entity work.iorvvf
    port map (japzu => vrcrx, pwgjp => qhurc, rkl => tn, slagajqzw => mzuwd);
  xwgb : entity work.iorvvf
    port map (japzu => nhwxvk, pwgjp => gkasnu, rkl => zkqolwce, slagajqzw => fk);
  
  -- Single-driven assignments
  biuqkkpc <= biuqkkpc;
  gkasnu <= qhurc;
  y <= y;
  qhurc <= biuqkkpc;
  
  -- Multi-driven assignments
  pvqqjpin <= 'L';
  pvqqjpin <= '-';
  pvqqjpin <= pvqqjpin;
  pvqqjpin <= 'X';
end yvysi;



-- Seed after: 4929546585871372043,2338584220606314193
