-- Seed: 10675965236375240512,7332793847894666635

library ieee;
use ieee.std_logic_1164.all;

entity wtafthb is
  port (hgsn : in std_logic; qelmofxczz : linkage real; z : linkage integer);
end wtafthb;



architecture mquaxufjeg of wtafthb is
  
begin
  
end mquaxufjeg;

library ieee;
use ieee.std_logic_1164.all;

entity lpellorcqk is
  port (a : out real; jzeeb : inout time_vector(3 to 3); pskyotuft : out std_logic; afou : buffer character);
end lpellorcqk;

library ieee;
use ieee.std_logic_1164.all;

architecture bs of lpellorcqk is
  signal cqzm : integer;
  signal yr : std_logic;
  signal kf : real;
  signal kdlzguj : std_logic;
  signal obrlm : integer;
  signal bg : real;
  signal frixveczap : std_logic;
begin
  gzzjj : entity work.wtafthb
    port map (hgsn => frixveczap, qelmofxczz => bg, z => obrlm);
  pyoaddff : entity work.wtafthb
    port map (hgsn => kdlzguj, qelmofxczz => a, z => obrlm);
  mnggflwsk : entity work.wtafthb
    port map (hgsn => pskyotuft, qelmofxczz => kf, z => obrlm);
  sq : entity work.wtafthb
    port map (hgsn => yr, qelmofxczz => bg, z => cqzm);
end bs;



entity afqyiteg is
  port (lxduldby : in time);
end afqyiteg;

library ieee;
use ieee.std_logic_1164.all;

architecture ovnzv of afqyiteg is
  signal ptsuqha : integer;
  signal uvnrd : real;
  signal dpxmzdndd : character;
  signal oisuxir : std_logic;
  signal ae : time_vector(3 to 3);
  signal ondecgj : real;
  signal ninidge : character;
  signal rdk : std_logic;
  signal hksviqu : time_vector(3 to 3);
  signal gzobtodzri : real;
begin
  ibcafix : entity work.lpellorcqk
    port map (a => gzobtodzri, jzeeb => hksviqu, pskyotuft => rdk, afou => ninidge);
  vo : entity work.lpellorcqk
    port map (a => ondecgj, jzeeb => ae, pskyotuft => oisuxir, afou => dpxmzdndd);
  gdafz : entity work.wtafthb
    port map (hgsn => oisuxir, qelmofxczz => uvnrd, z => ptsuqha);
end ovnzv;

library ieee;
use ieee.std_logic_1164.all;

entity oirfdr is
  port (envybw : out std_logic_vector(3 downto 3); zbwmeuzi : out boolean_vector(2 to 1); cx : buffer integer);
end oirfdr;

library ieee;
use ieee.std_logic_1164.all;

architecture lhaayy of oirfdr is
  signal x : std_logic;
  signal bagwyueqsm : real;
  signal wziwh : integer;
  signal ni : std_logic;
  signal icnpoqb : character;
  signal fjzgbtk : std_logic;
  signal qtg : time_vector(3 to 3);
  signal n : real;
begin
  t : entity work.lpellorcqk
    port map (a => n, jzeeb => qtg, pskyotuft => fjzgbtk, afou => icnpoqb);
  tpkoktghj : entity work.wtafthb
    port map (hgsn => ni, qelmofxczz => n, z => wziwh);
  uoc : entity work.wtafthb
    port map (hgsn => fjzgbtk, qelmofxczz => bagwyueqsm, z => cx);
  fpd : entity work.wtafthb
    port map (hgsn => x, qelmofxczz => n, z => cx);
end lhaayy;



-- Seed after: 12324090958942426420,7332793847894666635
