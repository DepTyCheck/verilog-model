-- Seed: 3426226706394824361,16265041255589496407

library ieee;
use ieee.std_logic_1164.all;

entity thbvtvh is
  port (ohzze : inout severity_level; xzjgrf : out real; ar : buffer time; ghzg : buffer std_logic_vector(2 downto 2));
end thbvtvh;



architecture hw of thbvtvh is
  
begin
  
end hw;

library ieee;
use ieee.std_logic_1164.all;

entity lc is
  port (ulivd : in bit_vector(3 to 3); fb : in real; dkj : buffer std_logic);
end lc;

library ieee;
use ieee.std_logic_1164.all;

architecture t of lc is
  signal fvq : std_logic_vector(2 downto 2);
  signal d : time;
  signal wml : real;
  signal yz : severity_level;
begin
  fkos : entity work.thbvtvh
    port map (ohzze => yz, xzjgrf => wml, ar => d, ghzg => fvq);
end t;

library ieee;
use ieee.std_logic_1164.all;

entity qeykuw is
  port (fxgr : buffer std_logic_vector(4 downto 4); tyeimp : buffer bit_vector(0 to 0); rujmitylnd : out real);
end qeykuw;

library ieee;
use ieee.std_logic_1164.all;

architecture uvlmfky of qeykuw is
  signal gvtknjguy : std_logic_vector(2 downto 2);
  signal lokio : time;
  signal btnmby : severity_level;
  signal gaqxquj : std_logic;
  signal zmbkpo : real;
  signal jpet : bit_vector(3 to 3);
begin
  usksvrjsu : entity work.lc
    port map (ulivd => jpet, fb => zmbkpo, dkj => gaqxquj);
  psvxdb : entity work.thbvtvh
    port map (ohzze => btnmby, xzjgrf => rujmitylnd, ar => lokio, ghzg => gvtknjguy);
end uvlmfky;



-- Seed after: 7692819122419389905,16265041255589496407
