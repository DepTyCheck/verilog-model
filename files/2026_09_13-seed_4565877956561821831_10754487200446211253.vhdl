-- Seed: 4565877956561821831,10754487200446211253

library ieee;
use ieee.std_logic_1164.all;

entity msaixeir is
  port (u : out std_logic; a : out integer; st : out integer_vector(0 downto 0));
end msaixeir;

architecture omtmq of msaixeir is
  
begin
  -- Single-driven assignments
  a <= 14001;
  st <= (others => 8#02705#);
end omtmq;

entity mxatojewqa is
  port (nu : in time; u : buffer severity_level; sqcbds : linkage boolean);
end mxatojewqa;

library ieee;
use ieee.std_logic_1164.all;

architecture qblr of mxatojewqa is
  signal dufdmf : integer_vector(0 downto 0);
  signal qngks : integer;
  signal ti : integer_vector(0 downto 0);
  signal hrz : integer;
  signal y : std_logic;
begin
  nofedajxn : entity work.msaixeir
    port map (u => y, a => hrz, st => ti);
  lqxar : entity work.msaixeir
    port map (u => y, a => qngks, st => dufdmf);
  
  -- Single-driven assignments
  u <= NOTE;
  
  -- Multi-driven assignments
  y <= 'U';
  y <= 'L';
  y <= y;
end qblr;



-- Seed after: 23089512574502272,10754487200446211253
