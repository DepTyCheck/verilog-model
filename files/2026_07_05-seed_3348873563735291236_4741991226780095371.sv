// Seed: 3348873563735291236,4741991226780095371

module fgjl ();
  xnor fglitfoha(cje, cje, pqgxowjszk);
  
  xnor qymgsq(xlwyd, cje, ascaawve);
  
  or fxtol(xlwyd, dgwlqiv, ehwbuvfni);
  
  not lxmvlkmqvo(xlwyd, yinxszkyd);
  
  
  // Multi-driven assignments
  assign cje = 'b1x1;
  assign pqgxowjszk = 'b11z;
endmodule: fgjl

module fgsii (input tri logic [0:4][4:1][2:2] zmajvpdtn, input integer ldzy, inout tri logic [1:3][2:1] efp [0:1][2:0], output byte heml [4:1]);
  xnor lulgnvpme(zmajvpdtn, ldzy, ldzy);
  // warning: implicit conversion of port connection truncates from 20 to 1 bits
  //   tri logic [0:4][4:1][2:2] zmajvpdtn -> logic zmajvpdtn
  //
  // warning: implicit conversion of port connection truncates from 32 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   integer ldzy -> logic ldzy
  //
  // warning: implicit conversion of port connection truncates from 32 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   integer ldzy -> logic ldzy
  
  and mf(gup, ldzy, zmajvpdtn);
  // warning: implicit conversion of port connection truncates from 32 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   integer ldzy -> logic ldzy
  //
  // warning: implicit conversion of port connection truncates from 20 to 1 bits
  //   tri logic [0:4][4:1][2:2] zmajvpdtn -> logic zmajvpdtn
  
  
  // Single-driven assignments
  assign heml = heml;
  
  // Multi-driven assignments
  assign zmajvpdtn = zmajvpdtn;
  assign efp = efp;
endmodule: fgsii



// Seed after: 99377504967775953,4741991226780095371
