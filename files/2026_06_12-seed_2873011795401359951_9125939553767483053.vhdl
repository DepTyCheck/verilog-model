-- Seed: 2873011795401359951,9125939553767483053



entity uhcedlzsua is
  port (fhrbcl : in real; yxiimijlg : buffer integer; ky : buffer time);
end uhcedlzsua;



architecture tmins of uhcedlzsua is
  
begin
  
end tmins;

library ieee;
use ieee.std_logic_1164.all;

entity dgejlvmeoc is
  port (ihtbbdv : linkage character; mcuzx : out std_logic; o : inout integer);
end dgejlvmeoc;



architecture ovuodwlt of dgejlvmeoc is
  signal nyul : time;
  signal xlx : integer;
  signal zyurqsjr : real;
  signal iuulydvsg : time;
  signal lyhpgj : time;
  signal pfxe : integer;
  signal pdo : time;
  signal h : integer;
  signal ve : real;
begin
  thywm : entity work.uhcedlzsua
    port map (fhrbcl => ve, yxiimijlg => h, ky => pdo);
  rgelsuhlsm : entity work.uhcedlzsua
    port map (fhrbcl => ve, yxiimijlg => pfxe, ky => lyhpgj);
  edijqd : entity work.uhcedlzsua
    port map (fhrbcl => ve, yxiimijlg => o, ky => iuulydvsg);
  jgadpgrizd : entity work.uhcedlzsua
    port map (fhrbcl => zyurqsjr, yxiimijlg => xlx, ky => nyul);
end ovuodwlt;



entity qiqdw is
  port (yutomxbdey : out integer; jbg : inout time; tba : linkage integer; upnwkmnul : linkage real);
end qiqdw;



architecture e of qiqdw is
  signal whnnnn : integer;
  signal czlrhokka : real;
  signal mayzrph : time;
  signal ijliltuob : real;
begin
  xrwgm : entity work.uhcedlzsua
    port map (fhrbcl => ijliltuob, yxiimijlg => yutomxbdey, ky => mayzrph);
  ivr : entity work.uhcedlzsua
    port map (fhrbcl => czlrhokka, yxiimijlg => whnnnn, ky => jbg);
end e;



entity ecmtolea is
  port (qvevnckfv : inout boolean_vector(2 downto 3); lsrxbi : in severity_level; domzf : out integer; hdakm : in integer);
end ecmtolea;



architecture zkogkcer of ecmtolea is
  signal esw : time;
  signal kbrowmnggy : real;
begin
  iqrpnnolk : entity work.uhcedlzsua
    port map (fhrbcl => kbrowmnggy, yxiimijlg => domzf, ky => esw);
end zkogkcer;



-- Seed after: 12336113010729158219,9125939553767483053
