-- Seed: 6165330325000428609,3400751927341804175

entity btt is
  port (yc : inout severity_level; dnwvds : out real);
end btt;

architecture rdlco of btt is
  
begin
  
end rdlco;

entity eac is
  port (bxnq : linkage bit_vector(0 downto 0); pgqwybcfyv : inout time_vector(0 downto 1));
end eac;

architecture gbjfdpk of eac is
  signal pgfp : real;
  signal gdkrdzt : severity_level;
  signal qqang : real;
  signal hcrwmwn : severity_level;
  signal bp : real;
  signal fems : severity_level;
begin
  zlpavoso : entity work.btt
    port map (yc => fems, dnwvds => bp);
  geuak : entity work.btt
    port map (yc => hcrwmwn, dnwvds => qqang);
  mkpnmsi : entity work.btt
    port map (yc => gdkrdzt, dnwvds => pgfp);
  
  -- Single-driven assignments
  pgqwybcfyv <= (others => 0 ns);
end gbjfdpk;



-- Seed after: 6723654059739445918,3400751927341804175
