-- Seed: 10426674782443654167,13501862637168280927

entity nq is
  port (gdn : linkage time; jnzcddv : buffer time);
end nq;

architecture ghywzsaa of nq is
  
begin
  
end ghywzsaa;

library ieee;
use ieee.std_logic_1164.all;

entity racgbezi is
  port (ozrtozqm : buffer std_logic_vector(2 downto 2));
end racgbezi;

architecture qvybfgfs of racgbezi is
  signal tputgvbe : time;
  signal xkgd : time;
  signal wdl : time;
  signal fwfl : time;
begin
  qinc : entity work.nq
    port map (gdn => fwfl, jnzcddv => wdl);
  ebdroidid : entity work.nq
    port map (gdn => xkgd, jnzcddv => tputgvbe);
end qvybfgfs;

entity vfywk is
  port (qdhi : in bit_vector(2 downto 1));
end vfywk;

architecture seyamffnwi of vfywk is
  signal atrymunsyv : time;
  signal dz : time;
begin
  adedz : entity work.nq
    port map (gdn => dz, jnzcddv => atrymunsyv);
end seyamffnwi;

entity pjqjtyaezj is
  port (pucf : out real; zmyt : out severity_level);
end pjqjtyaezj;

architecture zyshpoa of pjqjtyaezj is
  signal ft : time;
  signal fgvrvtn : time;
  signal sdq : bit_vector(2 downto 1);
  signal bkstxph : time;
  signal vthtp : time;
begin
  pxwljll : entity work.nq
    port map (gdn => vthtp, jnzcddv => bkstxph);
  ldi : entity work.vfywk
    port map (qdhi => sdq);
  oys : entity work.nq
    port map (gdn => fgvrvtn, jnzcddv => ft);
end zyshpoa;



-- Seed after: 8332947212155044933,13501862637168280927
