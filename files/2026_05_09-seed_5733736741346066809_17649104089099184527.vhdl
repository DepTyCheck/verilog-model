-- Seed: 5733736741346066809,17649104089099184527

library ieee;
use ieee.std_logic_1164.all;

entity x is
  port (lwmt : out severity_level; verlkwjg : out std_logic; qirfpodyw : inout time; nnus : out time);
end x;



architecture grknbcefm of x is
  
begin
  
end grknbcefm;



entity az is
  port (tmyaat : buffer real; flegajnm : buffer real; jydwhsoku : out integer);
end az;

library ieee;
use ieee.std_logic_1164.all;

architecture edcyni of az is
  signal blciwqich : time;
  signal xvuc : time;
  signal djh : std_logic;
  signal uraeebuup : severity_level;
  signal uujbzk : time;
  signal ocqam : time;
  signal qsah : std_logic;
  signal ic : severity_level;
  signal kjvbyz : time;
  signal yxebaefo : time;
  signal opcgrbxenv : severity_level;
  signal gbqbdd : time;
  signal mfkvsce : time;
  signal ercrdemfau : std_logic;
  signal sxnehj : severity_level;
begin
  mslwkbha : entity work.x
    port map (lwmt => sxnehj, verlkwjg => ercrdemfau, qirfpodyw => mfkvsce, nnus => gbqbdd);
  neiefx : entity work.x
    port map (lwmt => opcgrbxenv, verlkwjg => ercrdemfau, qirfpodyw => yxebaefo, nnus => kjvbyz);
  r : entity work.x
    port map (lwmt => ic, verlkwjg => qsah, qirfpodyw => ocqam, nnus => uujbzk);
  kpq : entity work.x
    port map (lwmt => uraeebuup, verlkwjg => djh, qirfpodyw => xvuc, nnus => blciwqich);
end edcyni;

library ieee;
use ieee.std_logic_1164.all;

entity bubay is
  port (s : linkage std_logic);
end bubay;

library ieee;
use ieee.std_logic_1164.all;

architecture r of bubay is
  signal oueniqz : time;
  signal qgrwq : time;
  signal rxfmtmn : std_logic;
  signal cfn : severity_level;
  signal jldclmcz : time;
  signal dargfj : time;
  signal xdkrkonlr : severity_level;
  signal ffmthl : time;
  signal lw : time;
  signal mtbyxh : std_logic;
  signal e : severity_level;
begin
  pnngidaj : entity work.x
    port map (lwmt => e, verlkwjg => mtbyxh, qirfpodyw => lw, nnus => ffmthl);
  uayj : entity work.x
    port map (lwmt => xdkrkonlr, verlkwjg => mtbyxh, qirfpodyw => dargfj, nnus => jldclmcz);
  huj : entity work.x
    port map (lwmt => cfn, verlkwjg => rxfmtmn, qirfpodyw => qgrwq, nnus => oueniqz);
end r;



entity gaz is
  port (bwfgamul : out integer);
end gaz;

library ieee;
use ieee.std_logic_1164.all;

architecture z of gaz is
  signal dxanarrqvu : integer;
  signal h : real;
  signal qsjzbfo : real;
  signal yfdm : time;
  signal f : time;
  signal oywozzi : std_logic;
  signal lt : severity_level;
  signal ynaq : integer;
  signal pawjxracm : real;
  signal vjc : real;
begin
  ftscmb : entity work.az
    port map (tmyaat => vjc, flegajnm => pawjxracm, jydwhsoku => ynaq);
  lve : entity work.x
    port map (lwmt => lt, verlkwjg => oywozzi, qirfpodyw => f, nnus => yfdm);
  xgglcxym : entity work.az
    port map (tmyaat => qsjzbfo, flegajnm => h, jydwhsoku => dxanarrqvu);
end z;



-- Seed after: 16989713228994961858,17649104089099184527
