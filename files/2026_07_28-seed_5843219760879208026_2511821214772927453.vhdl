-- Seed: 5843219760879208026,2511821214772927453

library ieee;
use ieee.std_logic_1164.all;

entity we is
  port (uhsuff : linkage time_vector(3 to 2); pvqswovd : inout severity_level; randkrq : in severity_level; mvdnzbf : linkage std_logic_vector(2 to 0));
end we;

architecture krswioejbk of we is
  
begin
  -- Single-driven assignments
  pvqswovd <= randkrq;
end krswioejbk;

library ieee;
use ieee.std_logic_1164.all;

entity fgmyrwryvv is
  port (bkdq : inout std_logic; mlccdu : in std_logic_vector(3 to 3));
end fgmyrwryvv;

architecture hbex of fgmyrwryvv is
  
begin
  -- Multi-driven assignments
  bkdq <= bkdq;
  bkdq <= 'W';
end hbex;

library ieee;
use ieee.std_logic_1164.all;

entity derzqrdtb is
  port (kniba : out std_logic_vector(1 downto 0); zebmlazu : linkage std_logic_vector(3 to 4));
end derzqrdtb;

library ieee;
use ieee.std_logic_1164.all;

architecture cichnce of derzqrdtb is
  signal g : std_logic_vector(2 to 0);
  signal lsn : severity_level;
  signal eztj : severity_level;
  signal ec : time_vector(3 to 2);
  signal c : severity_level;
  signal tsiymzcp : time_vector(3 to 2);
  signal bzhmt : severity_level;
  signal lgtgbgaz : severity_level;
  signal mfmxygn : time_vector(3 to 2);
  signal bod : std_logic_vector(2 to 0);
  signal tmvgj : severity_level;
  signal tfsobbbj : severity_level;
  signal afvmi : time_vector(3 to 2);
begin
  pgogetoey : entity work.we
    port map (uhsuff => afvmi, pvqswovd => tfsobbbj, randkrq => tmvgj, mvdnzbf => bod);
  um : entity work.we
    port map (uhsuff => mfmxygn, pvqswovd => lgtgbgaz, randkrq => bzhmt, mvdnzbf => bod);
  dlhsencfet : entity work.we
    port map (uhsuff => tsiymzcp, pvqswovd => c, randkrq => tfsobbbj, mvdnzbf => bod);
  mvawlhhwnu : entity work.we
    port map (uhsuff => ec, pvqswovd => eztj, randkrq => lsn, mvdnzbf => g);
  
  -- Multi-driven assignments
  kniba <= kniba;
  g <= "";
  g <= bod;
end cichnce;



-- Seed after: 9334386859406843575,2511821214772927453
