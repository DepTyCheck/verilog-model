-- Seed: 26954789949085615,7198033922882419595

library ieee;
use ieee.std_logic_1164.all;

entity jz is
  port (nt : out std_logic_vector(2 downto 0); kcoeqylfd : in integer; qffpuc : in integer; gq : buffer severity_level);
end jz;

architecture a of jz is
  
begin
  -- Single-driven assignments
  gq <= NOTE;
  
  -- Multi-driven assignments
  nt <= nt;
  nt <= "UZW";
  nt <= nt;
end a;

library ieee;
use ieee.std_logic_1164.all;

entity vdod is
  port (hnxry : linkage time; qpqhmoqn : inout std_logic_vector(2 downto 1); lp : out time; bkw : inout time);
end vdod;

library ieee;
use ieee.std_logic_1164.all;

architecture vdhl of vdod is
  signal o : severity_level;
  signal i : std_logic_vector(2 downto 0);
  signal mtbebgq : severity_level;
  signal wtjstnpn : severity_level;
  signal zqh : integer;
  signal u : std_logic_vector(2 downto 0);
begin
  iw : entity work.jz
    port map (nt => u, kcoeqylfd => zqh, qffpuc => zqh, gq => wtjstnpn);
  mptlkn : entity work.jz
    port map (nt => u, kcoeqylfd => zqh, qffpuc => zqh, gq => mtbebgq);
  yqa : entity work.jz
    port map (nt => i, kcoeqylfd => zqh, qffpuc => zqh, gq => o);
  
  -- Single-driven assignments
  bkw <= 0 fs;
  zqh <= zqh;
  lp <= 16#A_9_8.D86F# ms;
  
  -- Multi-driven assignments
  u <= u;
  qpqhmoqn <= qpqhmoqn;
  qpqhmoqn <= ('Z', 'X');
end vdhl;

library ieee;
use ieee.std_logic_1164.all;

entity ecpgtg is
  port (kygfy : inout std_logic_vector(3 to 3); tq : out real);
end ecpgtg;

library ieee;
use ieee.std_logic_1164.all;

architecture hvoqssth of ecpgtg is
  signal bpgd : time;
  signal qhfyb : time;
  signal vnbzyuckq : std_logic_vector(2 downto 1);
  signal dkd : time;
  signal blywbzi : severity_level;
  signal pptdgieqev : integer;
  signal zyzhwo : std_logic_vector(2 downto 0);
  signal glqzxk : severity_level;
  signal elj : integer;
  signal eguqwpa : integer;
  signal tlx : std_logic_vector(2 downto 0);
begin
  puavdv : entity work.jz
    port map (nt => tlx, kcoeqylfd => eguqwpa, qffpuc => elj, gq => glqzxk);
  kfwa : entity work.jz
    port map (nt => zyzhwo, kcoeqylfd => pptdgieqev, qffpuc => pptdgieqev, gq => blywbzi);
  s : entity work.vdod
    port map (hnxry => dkd, qpqhmoqn => vnbzyuckq, lp => qhfyb, bkw => bpgd);
  
  -- Multi-driven assignments
  vnbzyuckq <= vnbzyuckq;
  vnbzyuckq <= vnbzyuckq;
end hvoqssth;



-- Seed after: 10793107515878399751,7198033922882419595
