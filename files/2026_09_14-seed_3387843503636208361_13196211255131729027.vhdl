-- Seed: 3387843503636208361,13196211255131729027

library ieee;
use ieee.std_logic_1164.all;

entity ajebt is
  port (eofz : inout real; wuxy : linkage std_logic_vector(4 to 3); vdkncna : inout time; lwkygila : linkage integer);
end ajebt;

architecture ju of ajebt is
  
begin
  -- Single-driven assignments
  vdkncna <= vdkncna;
  eofz <= eofz;
end ju;

entity jy is
  port (ivftofdo : linkage boolean);
end jy;

library ieee;
use ieee.std_logic_1164.all;

architecture kxdrhohswn of jy is
  signal dpvlikmxox : integer;
  signal rmsecyd : time;
  signal aicsn : real;
  signal aufmhsy : integer;
  signal fz : time;
  signal jkcebgmzs : real;
  signal eqpfti : integer;
  signal fkgvcq : time;
  signal mmkm : std_logic_vector(4 to 3);
  signal jayfm : real;
begin
  tejuouy : entity work.ajebt
    port map (eofz => jayfm, wuxy => mmkm, vdkncna => fkgvcq, lwkygila => eqpfti);
  putupr : entity work.ajebt
    port map (eofz => jkcebgmzs, wuxy => mmkm, vdkncna => fz, lwkygila => aufmhsy);
  uianfqg : entity work.ajebt
    port map (eofz => aicsn, wuxy => mmkm, vdkncna => rmsecyd, lwkygila => dpvlikmxox);
  
  -- Multi-driven assignments
  mmkm <= mmkm;
  mmkm <= mmkm;
end kxdrhohswn;

library ieee;
use ieee.std_logic_1164.all;

entity oid is
  port (qgrdmtw : inout integer; qdxwbudpf : linkage std_logic_vector(3 downto 4));
end oid;

library ieee;
use ieee.std_logic_1164.all;

architecture wj of oid is
  signal nmfxs : integer;
  signal bns : time;
  signal mxf : std_logic_vector(4 to 3);
  signal hbyrhz : real;
  signal ocwp : integer;
  signal aoadd : time;
  signal adswhfwpz : real;
  signal psq : time;
  signal hmzxizbf : real;
  signal guyr : integer;
  signal ipqrk : time;
  signal srktnb : real;
begin
  feah : entity work.ajebt
    port map (eofz => srktnb, wuxy => qdxwbudpf, vdkncna => ipqrk, lwkygila => guyr);
  dhifjobp : entity work.ajebt
    port map (eofz => hmzxizbf, wuxy => qdxwbudpf, vdkncna => psq, lwkygila => qgrdmtw);
  cgblaeb : entity work.ajebt
    port map (eofz => adswhfwpz, wuxy => qdxwbudpf, vdkncna => aoadd, lwkygila => ocwp);
  ytunud : entity work.ajebt
    port map (eofz => hbyrhz, wuxy => mxf, vdkncna => bns, lwkygila => nmfxs);
  
  -- Multi-driven assignments
  mxf <= mxf;
  mxf <= (others => '0');
  mxf <= (others => '0');
  mxf <= mxf;
end wj;



-- Seed after: 13024293145722283827,13196211255131729027
