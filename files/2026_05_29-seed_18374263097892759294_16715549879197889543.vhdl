-- Seed: 18374263097892759294,16715549879197889543

library ieee;
use ieee.std_logic_1164.all;

entity bxkvx is
  port (pjmmqf : inout std_logic; rxm : inout real_vector(3 downto 0); f : buffer std_logic_vector(2 downto 2); svckwfkli : out character);
end bxkvx;



architecture ea of bxkvx is
  
begin
  
end ea;

library ieee;
use ieee.std_logic_1164.all;

entity evon is
  port (asno : buffer time_vector(3 downto 0); wysks : linkage std_logic_vector(0 downto 4));
end evon;

library ieee;
use ieee.std_logic_1164.all;

architecture owisglgi of evon is
  signal swzce : character;
  signal gzy : std_logic_vector(2 downto 2);
  signal zye : real_vector(3 downto 0);
  signal vjmncjky : std_logic;
  signal dkl : character;
  signal ij : real_vector(3 downto 0);
  signal kunvh : character;
  signal l : real_vector(3 downto 0);
  signal giso : character;
  signal jzpd : std_logic_vector(2 downto 2);
  signal rmmlngvihe : real_vector(3 downto 0);
  signal u : std_logic;
begin
  hwzv : entity work.bxkvx
    port map (pjmmqf => u, rxm => rmmlngvihe, f => jzpd, svckwfkli => giso);
  ym : entity work.bxkvx
    port map (pjmmqf => u, rxm => l, f => jzpd, svckwfkli => kunvh);
  ywckae : entity work.bxkvx
    port map (pjmmqf => u, rxm => ij, f => jzpd, svckwfkli => dkl);
  jy : entity work.bxkvx
    port map (pjmmqf => vjmncjky, rxm => zye, f => gzy, svckwfkli => swzce);
end owisglgi;



entity nzk is
  port (nzjilau : linkage boolean);
end nzk;



architecture hfd of nzk is
  
begin
  
end hfd;



entity lm is
  port (wj : inout time; st : buffer time_vector(2 to 1); gotfgvtn : inout boolean_vector(2 to 0));
end lm;

library ieee;
use ieee.std_logic_1164.all;

architecture mekz of lm is
  signal uudmxtkz : character;
  signal qxhttp : std_logic_vector(2 downto 2);
  signal sxebj : real_vector(3 downto 0);
  signal dfpszsjcbs : std_logic;
  signal aegwgs : character;
  signal qdxgat : real_vector(3 downto 0);
  signal dgcicz : std_logic;
  signal qutggp : std_logic_vector(0 downto 4);
  signal pwplmyla : time_vector(3 downto 0);
  signal hi : character;
  signal lalok : std_logic_vector(2 downto 2);
  signal emqhogcmf : real_vector(3 downto 0);
  signal e : std_logic;
begin
  u : entity work.bxkvx
    port map (pjmmqf => e, rxm => emqhogcmf, f => lalok, svckwfkli => hi);
  owrcpv : entity work.evon
    port map (asno => pwplmyla, wysks => qutggp);
  pbbvdtpzih : entity work.bxkvx
    port map (pjmmqf => dgcicz, rxm => qdxgat, f => lalok, svckwfkli => aegwgs);
  tkoccvk : entity work.bxkvx
    port map (pjmmqf => dfpszsjcbs, rxm => sxebj, f => qxhttp, svckwfkli => uudmxtkz);
end mekz;



-- Seed after: 11963219357522016519,16715549879197889543
