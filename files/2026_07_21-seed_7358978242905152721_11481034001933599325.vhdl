-- Seed: 7358978242905152721,11481034001933599325

entity moc is
  port (no : inout bit_vector(1 to 1); prltldzb : out time; zxd : out severity_level; trbngrr : buffer time);
end moc;

architecture sli of moc is
  
begin
  -- Single-driven assignments
  trbngrr <= trbngrr;
  zxd <= zxd;
  no <= no;
  prltldzb <= 1 hr;
end sli;

entity yrrnwh is
  port (op : linkage character; fdjfem : linkage integer);
end yrrnwh;

architecture cjkdmt of yrrnwh is
  signal equsif : time;
  signal jtolanela : severity_level;
  signal qcebdz : time;
  signal s : bit_vector(1 to 1);
  signal lmzeynk : time;
  signal vgq : severity_level;
  signal zlem : time;
  signal g : bit_vector(1 to 1);
  signal kufrmhynor : time;
  signal ltnb : severity_level;
  signal ohsnufc : time;
  signal urebsgjg : bit_vector(1 to 1);
  signal sfxe : time;
  signal uhnwphu : severity_level;
  signal fmbfkfcj : time;
  signal wem : bit_vector(1 to 1);
begin
  zqsqsdt : entity work.moc
    port map (no => wem, prltldzb => fmbfkfcj, zxd => uhnwphu, trbngrr => sfxe);
  tqrscnkb : entity work.moc
    port map (no => urebsgjg, prltldzb => ohsnufc, zxd => ltnb, trbngrr => kufrmhynor);
  rqpcn : entity work.moc
    port map (no => g, prltldzb => zlem, zxd => vgq, trbngrr => lmzeynk);
  hwsl : entity work.moc
    port map (no => s, prltldzb => qcebdz, zxd => jtolanela, trbngrr => equsif);
end cjkdmt;

library ieee;
use ieee.std_logic_1164.all;

entity nlwj is
  port (hxtvuqlh : buffer character; nbmmlzli : linkage integer; idptwdu : out std_logic; tnpuj : buffer std_logic);
end nlwj;

architecture sjer of nlwj is
  signal byjygdor : time;
  signal pq : severity_level;
  signal yhbntxrkw : time;
  signal rxjwq : bit_vector(1 to 1);
  signal difouylxlo : time;
  signal sibijngd : severity_level;
  signal oqfp : time;
  signal fkovlkpg : bit_vector(1 to 1);
begin
  lcthvb : entity work.moc
    port map (no => fkovlkpg, prltldzb => oqfp, zxd => sibijngd, trbngrr => difouylxlo);
  rrsgptidu : entity work.moc
    port map (no => rxjwq, prltldzb => yhbntxrkw, zxd => pq, trbngrr => byjygdor);
  
  -- Single-driven assignments
  hxtvuqlh <= 'm';
  
  -- Multi-driven assignments
  tnpuj <= 'Z';
  tnpuj <= tnpuj;
  tnpuj <= 'U';
end sjer;



-- Seed after: 9804842365080946715,11481034001933599325
