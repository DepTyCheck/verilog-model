-- Seed: 4594464921743696396,1641934135882347475

library ieee;
use ieee.std_logic_1164.all;

entity tb is
  port (rqt : buffer boolean; foas : linkage std_logic; l : in boolean_vector(3 downto 1));
end tb;



architecture ckzgue of tb is
  
begin
  
end ckzgue;



entity s is
  port (ks : out character; lxrla : buffer real);
end s;

library ieee;
use ieee.std_logic_1164.all;

architecture ndufo of s is
  signal bh : std_logic;
  signal wnhw : boolean;
  signal lk : boolean_vector(3 downto 1);
  signal pafxqowfx : boolean;
  signal yutx : boolean_vector(3 downto 1);
  signal afuya : std_logic;
  signal ofnubex : boolean;
begin
  oxboqd : entity work.tb
    port map (rqt => ofnubex, foas => afuya, l => yutx);
  ucxsvjclpl : entity work.tb
    port map (rqt => pafxqowfx, foas => afuya, l => lk);
  ywpyclukl : entity work.tb
    port map (rqt => wnhw, foas => bh, l => yutx);
end ndufo;

library ieee;
use ieee.std_logic_1164.all;

entity cunjrpc is
  port (rsivxso : out std_logic);
end cunjrpc;



architecture wudnugvzd of cunjrpc is
  signal gatgezads : boolean_vector(3 downto 1);
  signal zkxusurlpn : boolean;
  signal mzlkv : boolean_vector(3 downto 1);
  signal kbqwwxan : boolean;
begin
  jknubs : entity work.tb
    port map (rqt => kbqwwxan, foas => rsivxso, l => mzlkv);
  zkrljamwtq : entity work.tb
    port map (rqt => zkxusurlpn, foas => rsivxso, l => gatgezads);
end wudnugvzd;



-- Seed after: 18128746921384213027,1641934135882347475
