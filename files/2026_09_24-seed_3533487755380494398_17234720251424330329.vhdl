-- Seed: 3533487755380494398,17234720251424330329

entity zfoqq is
  port (g : out time; tav : out boolean_vector(0 to 2); pfofjb : out integer);
end zfoqq;

architecture dwss of zfoqq is
  
begin
  -- Single-driven assignments
  pfofjb <= pfofjb;
  g <= g;
  tav <= tav;
end dwss;

entity key is
  port (ydzhvx : out character; yvsuilbd : out bit);
end key;

architecture uspxrmcnup of key is
  signal eudspm : integer;
  signal l : boolean_vector(0 to 2);
  signal e : time;
  signal ekre : integer;
  signal tirtuag : boolean_vector(0 to 2);
  signal zmurzrp : time;
  signal qn : integer;
  signal sljetywzfw : boolean_vector(0 to 2);
  signal hkeodeh : time;
begin
  b : entity work.zfoqq
    port map (g => hkeodeh, tav => sljetywzfw, pfofjb => qn);
  eahe : entity work.zfoqq
    port map (g => zmurzrp, tav => tirtuag, pfofjb => ekre);
  ehkie : entity work.zfoqq
    port map (g => e, tav => l, pfofjb => eudspm);
  
  -- Single-driven assignments
  yvsuilbd <= '0';
end uspxrmcnup;

entity kvyj is
  port (qextizksh : out time; dka : buffer string(1 to 2));
end kvyj;

architecture qhgqb of kvyj is
  signal lwteecrz : integer;
  signal e : boolean_vector(0 to 2);
  signal nsvouepr : integer;
  signal rqyj : boolean_vector(0 to 2);
  signal eypdafpq : time;
  signal h : integer;
  signal mvi : boolean_vector(0 to 2);
  signal axdfu : time;
  signal oobsk : integer;
  signal vtqhrru : boolean_vector(0 to 2);
  signal ldx : time;
begin
  agjgp : entity work.zfoqq
    port map (g => ldx, tav => vtqhrru, pfofjb => oobsk);
  qkjiz : entity work.zfoqq
    port map (g => axdfu, tav => mvi, pfofjb => h);
  mxzlirqg : entity work.zfoqq
    port map (g => eypdafpq, tav => rqyj, pfofjb => nsvouepr);
  hbzfers : entity work.zfoqq
    port map (g => qextizksh, tav => e, pfofjb => lwteecrz);
end qhgqb;



-- Seed after: 17695778063727264629,17234720251424330329
