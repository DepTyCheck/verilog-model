-- Seed: 7865222501561891349,8089241273282434469

library ieee;
use ieee.std_logic_1164.all;

entity txnifj is
  port (m : inout std_logic);
end txnifj;



architecture ghwhq of txnifj is
  
begin
  
end ghwhq;

library ieee;
use ieee.std_logic_1164.all;

entity ot is
  port (ifgz : buffer std_logic_vector(4 to 3); gsenuhas : linkage real; easpnvcta : buffer bit_vector(4 downto 1); eqmujvs : in integer);
end ot;

library ieee;
use ieee.std_logic_1164.all;

architecture hnqelpgj of ot is
  signal u : std_logic;
  signal grqyzez : std_logic;
begin
  fzdtnxuhpm : entity work.txnifj
    port map (m => grqyzez);
  lybo : entity work.txnifj
    port map (m => u);
end hnqelpgj;



entity hsvwqu is
  port (mkxdgr : linkage boolean_vector(1 to 2));
end hsvwqu;

library ieee;
use ieee.std_logic_1164.all;

architecture jp of hsvwqu is
  signal x : integer;
  signal io : bit_vector(4 downto 1);
  signal mam : std_logic_vector(4 to 3);
  signal cdqbqcivm : std_logic;
  signal ui : integer;
  signal iifil : bit_vector(4 downto 1);
  signal jnuvke : integer;
  signal j : bit_vector(4 downto 1);
  signal bb : real;
  signal nnghf : std_logic_vector(4 to 3);
begin
  bcyqr : entity work.ot
    port map (ifgz => nnghf, gsenuhas => bb, easpnvcta => j, eqmujvs => jnuvke);
  zea : entity work.ot
    port map (ifgz => nnghf, gsenuhas => bb, easpnvcta => iifil, eqmujvs => ui);
  pmqkftpk : entity work.txnifj
    port map (m => cdqbqcivm);
  qwunkstma : entity work.ot
    port map (ifgz => mam, gsenuhas => bb, easpnvcta => io, eqmujvs => x);
end jp;



entity tgushcokju is
  port (fuootvmhe : out integer; xitqtdev : in time_vector(0 downto 0); t : in boolean; nqrymkvyt : linkage severity_level);
end tgushcokju;

library ieee;
use ieee.std_logic_1164.all;

architecture yksmzt of tgushcokju is
  signal dlxslrighk : boolean_vector(1 to 2);
  signal nj : std_logic;
  signal mfk : std_logic;
  signal dvewjru : std_logic;
begin
  qknrza : entity work.txnifj
    port map (m => dvewjru);
  x : entity work.txnifj
    port map (m => mfk);
  kd : entity work.txnifj
    port map (m => nj);
  ug : entity work.hsvwqu
    port map (mkxdgr => dlxslrighk);
end yksmzt;



-- Seed after: 4168169522680629770,8089241273282434469
