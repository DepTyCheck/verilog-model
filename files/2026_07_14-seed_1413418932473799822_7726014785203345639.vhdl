-- Seed: 1413418932473799822,7726014785203345639

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity hr is
  port (rkkpwczq : inout subtype_mirror; ndnsa : out std_logic; cfltoltci : linkage bit_vector(0 to 2));
end hr;

architecture ms of hr is
  
begin
  
end ms;

use std.reflection.all;

entity zr is
  port (lfnyiogiha : buffer integer; xbzvinqxl : buffer integer; ltiwbjnql : inout enumeration_subtype_mirror);
end zr;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture pnsfnbevf of zr is
  signal md : bit_vector(0 to 2);
  shared variable avwajfm : subtype_mirror;
  signal pblxu : bit_vector(0 to 2);
  shared variable sk : subtype_mirror;
  signal lyduekvxo : bit_vector(0 to 2);
  signal hjtwjtfpc : std_logic;
  shared variable qftsdm : subtype_mirror;
begin
  vz : entity work.hr
    port map (rkkpwczq => qftsdm, ndnsa => hjtwjtfpc, cfltoltci => lyduekvxo);
  xdj : entity work.hr
    port map (rkkpwczq => sk, ndnsa => hjtwjtfpc, cfltoltci => pblxu);
  kbwqltqr : entity work.hr
    port map (rkkpwczq => avwajfm, ndnsa => hjtwjtfpc, cfltoltci => md);
  
  -- Single-driven assignments
  xbzvinqxl <= xbzvinqxl;
end pnsfnbevf;



-- Seed after: 8009178210193655388,7726014785203345639
