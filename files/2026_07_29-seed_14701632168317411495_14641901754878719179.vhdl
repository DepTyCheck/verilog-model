-- Seed: 14701632168317411495,14641901754878719179

library ieee;
use ieee.std_logic_1164.all;

entity iriiif is
  port (sfeifl : out boolean; rxqtqpu : inout bit; qx : out real; oezgfojy : inout std_logic);
end iriiif;

architecture cqppd of iriiif is
  
begin
  -- Single-driven assignments
  qx <= 3.041;
  rxqtqpu <= '0';
  sfeifl <= sfeifl;
  
  -- Multi-driven assignments
  oezgfojy <= oezgfojy;
  oezgfojy <= 'W';
  oezgfojy <= oezgfojy;
end cqppd;

library ieee;
use ieee.std_logic_1164.all;

entity duuazbul is
  port (tfoetjm : linkage time; jvyclkdwuw : buffer std_logic; cp : linkage std_logic; ecdec : in integer);
end duuazbul;

library ieee;
use ieee.std_logic_1164.all;

architecture oufpgwy of duuazbul is
  signal rzgjjabjae : std_logic;
  signal uszbyfjnu : real;
  signal setgainjwf : bit;
  signal o : boolean;
  signal d : std_logic;
  signal lwugl : real;
  signal eqjcxq : bit;
  signal p : boolean;
  signal uhgxj : std_logic;
  signal mdmtw : real;
  signal ei : bit;
  signal ewtwpij : boolean;
begin
  u : entity work.iriiif
    port map (sfeifl => ewtwpij, rxqtqpu => ei, qx => mdmtw, oezgfojy => uhgxj);
  yqq : entity work.iriiif
    port map (sfeifl => p, rxqtqpu => eqjcxq, qx => lwugl, oezgfojy => d);
  tfwfa : entity work.iriiif
    port map (sfeifl => o, rxqtqpu => setgainjwf, qx => uszbyfjnu, oezgfojy => rzgjjabjae);
  
  -- Multi-driven assignments
  jvyclkdwuw <= jvyclkdwuw;
end oufpgwy;

entity reh is
  port (fpikxohibx : linkage bit_vector(4 to 2));
end reh;

library ieee;
use ieee.std_logic_1164.all;

architecture e of reh is
  signal yecz : real;
  signal atdmdj : bit;
  signal ydmcxlg : boolean;
  signal czblrms : time;
  signal jgrtjg : integer;
  signal kieffpjjt : std_logic;
  signal vcobvjyngm : std_logic;
  signal o : time;
begin
  mjvnm : entity work.duuazbul
    port map (tfoetjm => o, jvyclkdwuw => vcobvjyngm, cp => kieffpjjt, ecdec => jgrtjg);
  ipnxvxg : entity work.duuazbul
    port map (tfoetjm => czblrms, jvyclkdwuw => vcobvjyngm, cp => vcobvjyngm, ecdec => jgrtjg);
  vqskpkgvm : entity work.iriiif
    port map (sfeifl => ydmcxlg, rxqtqpu => atdmdj, qx => yecz, oezgfojy => vcobvjyngm);
  
  -- Single-driven assignments
  jgrtjg <= jgrtjg;
  
  -- Multi-driven assignments
  vcobvjyngm <= vcobvjyngm;
  vcobvjyngm <= vcobvjyngm;
  vcobvjyngm <= vcobvjyngm;
end e;



-- Seed after: 3350202716551210808,14641901754878719179
