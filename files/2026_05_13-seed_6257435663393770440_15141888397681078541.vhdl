-- Seed: 6257435663393770440,15141888397681078541

library ieee;
use ieee.std_logic_1164.all;

entity slbtc is
  port (jkep : out std_logic; ohlqmudef : buffer real; jkskmmy : in time; hcej : inout integer);
end slbtc;



architecture bzfexy of slbtc is
  
begin
  
end bzfexy;

library ieee;
use ieee.std_logic_1164.all;

entity hkaqa is
  port (zhztvgdzez : out std_logic);
end hkaqa;



architecture qeneyirimd of hkaqa is
  
begin
  
end qeneyirimd;



entity gehpybio is
  port (opx : in time; afmgktcy : in time; ttvqybvur : out severity_level; my : out character);
end gehpybio;

library ieee;
use ieee.std_logic_1164.all;

architecture wpbpcw of gehpybio is
  signal f : integer;
  signal ltqftr : time;
  signal hb : real;
  signal shv : integer;
  signal zmyachobr : real;
  signal hw : std_logic;
begin
  bjqyk : entity work.hkaqa
    port map (zhztvgdzez => hw);
  ns : entity work.slbtc
    port map (jkep => hw, ohlqmudef => zmyachobr, jkskmmy => afmgktcy, hcej => shv);
  ofpxjfbuka : entity work.slbtc
    port map (jkep => hw, ohlqmudef => hb, jkskmmy => ltqftr, hcej => f);
end wpbpcw;



entity aqh is
  port (nossijsl : inout character; hvqqtthqsn : in integer);
end aqh;

library ieee;
use ieee.std_logic_1164.all;

architecture dftuc of aqh is
  signal bpbus : integer;
  signal zzamn : real;
  signal tclofdm : integer;
  signal vbhvo : real;
  signal cwzbeyzyvy : integer;
  signal fu : time;
  signal tziybtmwr : real;
  signal aphkdqrx : std_logic;
  signal haovfq : integer;
  signal n : time;
  signal mkox : real;
  signal dlchtf : std_logic;
begin
  fsrmmw : entity work.slbtc
    port map (jkep => dlchtf, ohlqmudef => mkox, jkskmmy => n, hcej => haovfq);
  zkgr : entity work.slbtc
    port map (jkep => aphkdqrx, ohlqmudef => tziybtmwr, jkskmmy => fu, hcej => cwzbeyzyvy);
  q : entity work.slbtc
    port map (jkep => dlchtf, ohlqmudef => vbhvo, jkskmmy => n, hcej => tclofdm);
  cen : entity work.slbtc
    port map (jkep => dlchtf, ohlqmudef => zzamn, jkskmmy => n, hcej => bpbus);
end dftuc;



-- Seed after: 6328163642563577565,15141888397681078541
