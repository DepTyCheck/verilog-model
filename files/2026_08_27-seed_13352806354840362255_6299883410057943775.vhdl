-- Seed: 13352806354840362255,6299883410057943775

entity pryggx is
  port (mjtow : buffer real; cu : inout real; ratq : inout real);
end pryggx;

architecture iept of pryggx is
  
begin
  -- Single-driven assignments
  ratq <= 11321.234;
end iept;

entity hbabnm is
  port (xdrltaum : linkage time; s : in bit_vector(4 downto 1); lqgcip : in real);
end hbabnm;

architecture v of hbabnm is
  signal hd : real;
  signal mcqblayh : real;
  signal qzvuya : real;
  signal c : real;
  signal belncbg : real;
  signal pv : real;
  signal pfx : real;
  signal mf : real;
  signal ta : real;
begin
  zrl : entity work.pryggx
    port map (mjtow => ta, cu => mf, ratq => pfx);
  cht : entity work.pryggx
    port map (mjtow => pv, cu => belncbg, ratq => c);
  fnr : entity work.pryggx
    port map (mjtow => qzvuya, cu => mcqblayh, ratq => hd);
end v;



-- Seed after: 10594613700028823521,6299883410057943775
