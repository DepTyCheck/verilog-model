-- Seed: 3203375656237371581,12143220691580258643

entity imrkyuxni is
  port (kqebgirgai : out time; cl : out time; agtsj : linkage integer);
end imrkyuxni;

architecture tlqscybzvu of imrkyuxni is
  
begin
  -- Single-driven assignments
  cl <= cl;
  kqebgirgai <= cl;
end tlqscybzvu;

entity a is
  port (wowzl : inout integer_vector(2 to 4); vpeipbwnbt : in integer_vector(2 to 0));
end a;

architecture dd of a is
  signal huaaba : integer;
  signal ceec : time;
  signal pbj : time;
  signal nvwze : integer;
  signal vxuwl : time;
  signal aigeh : time;
  signal bvhw : integer;
  signal vsatrdofti : time;
  signal bq : time;
begin
  wu : entity work.imrkyuxni
    port map (kqebgirgai => bq, cl => vsatrdofti, agtsj => bvhw);
  umnif : entity work.imrkyuxni
    port map (kqebgirgai => aigeh, cl => vxuwl, agtsj => nvwze);
  ppdm : entity work.imrkyuxni
    port map (kqebgirgai => pbj, cl => ceec, agtsj => huaaba);
  
  -- Single-driven assignments
  wowzl <= wowzl;
end dd;

library ieee;
use ieee.std_logic_1164.all;

entity nkkonk is
  port (zte : out std_logic_vector(4 to 0); ehbryovp : in std_logic_vector(0 to 1));
end nkkonk;

architecture xxel of nkkonk is
  signal paog : integer_vector(2 to 0);
  signal prbmyfzase : integer_vector(2 to 4);
  signal nko : integer;
  signal f : time;
  signal uehtcava : time;
  signal ziqyls : integer;
  signal jiidth : time;
  signal yxletkvc : time;
  signal ifkwn : integer;
  signal lutiply : time;
  signal aew : time;
begin
  hlpbr : entity work.imrkyuxni
    port map (kqebgirgai => aew, cl => lutiply, agtsj => ifkwn);
  opgdhkqbco : entity work.imrkyuxni
    port map (kqebgirgai => yxletkvc, cl => jiidth, agtsj => ziqyls);
  nvyaamu : entity work.imrkyuxni
    port map (kqebgirgai => uehtcava, cl => f, agtsj => nko);
  zotoltkpm : entity work.a
    port map (wowzl => prbmyfzase, vpeipbwnbt => paog);
  
  -- Single-driven assignments
  paog <= paog;
  
  -- Multi-driven assignments
  zte <= zte;
end xxel;

entity uo is
  port (qwdfpxx : out bit);
end uo;

library ieee;
use ieee.std_logic_1164.all;

architecture wmgl of uo is
  signal tva : integer;
  signal qrrdc : time;
  signal uvk : time;
  signal gbo : std_logic_vector(0 to 1);
  signal bkeewnh : std_logic_vector(4 to 0);
  signal mxutuk : integer;
  signal az : time;
  signal skovjhodga : time;
  signal vmlsyvrgau : integer;
  signal emsunpinb : time;
  signal t : time;
begin
  enl : entity work.imrkyuxni
    port map (kqebgirgai => t, cl => emsunpinb, agtsj => vmlsyvrgau);
  jot : entity work.imrkyuxni
    port map (kqebgirgai => skovjhodga, cl => az, agtsj => mxutuk);
  phvkza : entity work.nkkonk
    port map (zte => bkeewnh, ehbryovp => gbo);
  znewyi : entity work.imrkyuxni
    port map (kqebgirgai => uvk, cl => qrrdc, agtsj => tva);
  
  -- Multi-driven assignments
  bkeewnh <= bkeewnh;
  gbo <= gbo;
  gbo <= gbo;
  gbo <= "HZ";
end wmgl;



-- Seed after: 3553907161780052471,12143220691580258643
