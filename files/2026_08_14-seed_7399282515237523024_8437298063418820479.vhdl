-- Seed: 7399282515237523024,8437298063418820479

library ieee;
use ieee.std_logic_1164.all;

entity kvb is
  port (jdc : buffer std_logic_vector(2 to 1); btrdjcsyu : out time; mn : buffer std_logic);
end kvb;

architecture rbgqg of kvb is
  
begin
  -- Single-driven assignments
  btrdjcsyu <= 0 ns;
  
  -- Multi-driven assignments
  mn <= 'X';
  mn <= '1';
end rbgqg;

entity wgkieswrvk is
  port (irbfmmhr : linkage boolean_vector(4 downto 2); vuvztso : inout string(3 to 1));
end wgkieswrvk;

library ieee;
use ieee.std_logic_1164.all;

architecture bvllensizy of wgkieswrvk is
  signal vrcharrpgn : std_logic;
  signal zztnz : time;
  signal zzojxi : std_logic_vector(2 to 1);
  signal wrdhrrhmv : std_logic;
  signal ig : time;
  signal fbvqsv : time;
  signal zzxosqauil : std_logic_vector(2 to 1);
  signal fthg : std_logic;
  signal fycuhgqzy : time;
  signal eijnmantg : std_logic_vector(2 to 1);
begin
  jyblxjsiq : entity work.kvb
    port map (jdc => eijnmantg, btrdjcsyu => fycuhgqzy, mn => fthg);
  iwbrmqxk : entity work.kvb
    port map (jdc => zzxosqauil, btrdjcsyu => fbvqsv, mn => fthg);
  kizvlci : entity work.kvb
    port map (jdc => zzxosqauil, btrdjcsyu => ig, mn => wrdhrrhmv);
  lmmpbyhigv : entity work.kvb
    port map (jdc => zzojxi, btrdjcsyu => zztnz, mn => vrcharrpgn);
  
  -- Single-driven assignments
  vuvztso <= vuvztso;
  
  -- Multi-driven assignments
  fthg <= fthg;
  zzojxi <= eijnmantg;
end bvllensizy;

entity vtiu is
  port (hhsuimfci : in boolean_vector(2 downto 4); mqwj : out boolean_vector(2 to 0); tuex : inout integer_vector(1 to 0));
end vtiu;

architecture vs of vtiu is
  signal xghmjruuj : string(3 to 1);
  signal otvaf : boolean_vector(4 downto 2);
begin
  wwgp : entity work.wgkieswrvk
    port map (irbfmmhr => otvaf, vuvztso => xghmjruuj);
  
  -- Single-driven assignments
  tuex <= tuex;
  mqwj <= (others => TRUE);
end vs;



-- Seed after: 14947154471201646722,8437298063418820479
