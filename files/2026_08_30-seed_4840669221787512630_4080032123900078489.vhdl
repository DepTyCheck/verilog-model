-- Seed: 4840669221787512630,4080032123900078489

library ieee;
use ieee.std_logic_1164.all;

entity dski is
  port (wn : out time; dzph : in std_logic_vector(0 to 3); a : inout time);
end dski;

architecture kusb of dski is
  
begin
  
end kusb;

library ieee;
use ieee.std_logic_1164.all;

entity ixb is
  port (dugnwv : linkage std_logic_vector(1 downto 1); qhoofjcvpm : linkage std_logic_vector(1 to 3); rarnhicq : in integer);
end ixb;

library ieee;
use ieee.std_logic_1164.all;

architecture pwsal of ixb is
  signal mmyuewpxd : time;
  signal s : std_logic_vector(0 to 3);
  signal zd : time;
  signal cztcb : time;
  signal kb : std_logic_vector(0 to 3);
  signal agdo : time;
  signal sfhxg : time;
  signal vnroh : time;
  signal cwxo : time;
  signal vujrn : std_logic_vector(0 to 3);
  signal tlowjccsd : time;
begin
  hk : entity work.dski
    port map (wn => tlowjccsd, dzph => vujrn, a => cwxo);
  j : entity work.dski
    port map (wn => vnroh, dzph => vujrn, a => sfhxg);
  bjb : entity work.dski
    port map (wn => agdo, dzph => kb, a => cztcb);
  dtel : entity work.dski
    port map (wn => zd, dzph => s, a => mmyuewpxd);
end pwsal;



-- Seed after: 16749458052270152968,4080032123900078489
