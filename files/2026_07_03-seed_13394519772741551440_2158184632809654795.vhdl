-- Seed: 13394519772741551440,2158184632809654795

use std.reflection.all;

entity uvykq is
  port (kpahu : inout enumeration_value_mirror; xk : inout floating_subtype_mirror; mawhbog : buffer real);
end uvykq;

architecture d of uvykq is
  
begin
  -- Single-driven assignments
  mawhbog <= mawhbog;
end d;

entity yv is
  port (ismv : out time; ojp : linkage bit; nrq : buffer bit; furwr : inout real);
end yv;

use std.reflection.all;

architecture gvtpoiuyiu of yv is
  shared variable rrdgakasiz : floating_subtype_mirror;
  shared variable h : enumeration_value_mirror;
begin
  gvebbgcrb : entity work.uvykq
    port map (kpahu => h, xk => rrdgakasiz, mawhbog => furwr);
  
  -- Single-driven assignments
  nrq <= '1';
  ismv <= 0 min;
end gvtpoiuyiu;

use std.reflection.all;

entity w is
  port (lmprsdu : inout access_value_mirror; acba : in bit);
end w;

use std.reflection.all;

architecture e of w is
  signal vyiks : real;
  shared variable mnpi : floating_subtype_mirror;
  shared variable nfakqs : enumeration_value_mirror;
  signal sczf : real;
  shared variable ubpcf : floating_subtype_mirror;
  shared variable d : enumeration_value_mirror;
  signal uxzuacm : real;
  shared variable zlq : floating_subtype_mirror;
  shared variable eooeexrua : enumeration_value_mirror;
begin
  dtntblijg : entity work.uvykq
    port map (kpahu => eooeexrua, xk => zlq, mawhbog => uxzuacm);
  n : entity work.uvykq
    port map (kpahu => d, xk => ubpcf, mawhbog => sczf);
  ummb : entity work.uvykq
    port map (kpahu => nfakqs, xk => mnpi, mawhbog => vyiks);
end e;



-- Seed after: 12593968843515081199,2158184632809654795
