-- Seed: 7971513707179179399,7142793346053417159



entity zt is
  port (qtkv : out bit_vector(2 downto 0));
end zt;



architecture mtdofdglwy of zt is
  
begin
  
end mtdofdglwy;

library ieee;
use ieee.std_logic_1164.all;

entity urdqdfa is
  port (vvrvnuj : inout integer_vector(0 to 2); qzi : inout time; oorpfn : inout real; z : inout std_logic);
end urdqdfa;



architecture n of urdqdfa is
  signal madw : bit_vector(2 downto 0);
  signal xzuczxwah : bit_vector(2 downto 0);
  signal mpk : bit_vector(2 downto 0);
  signal spmm : bit_vector(2 downto 0);
begin
  ovcnjbtav : entity work.zt
    port map (qtkv => spmm);
  rk : entity work.zt
    port map (qtkv => mpk);
  ydu : entity work.zt
    port map (qtkv => xzuczxwah);
  rqkfthlt : entity work.zt
    port map (qtkv => madw);
end n;



entity snwprbgw is
  port (q : buffer integer; ipjsufo : buffer real; jzvqieas : in time_vector(1 to 4));
end snwprbgw;



architecture jesifclu of snwprbgw is
  signal heakycqz : bit_vector(2 downto 0);
begin
  mdvndj : entity work.zt
    port map (qtkv => heakycqz);
end jesifclu;

library ieee;
use ieee.std_logic_1164.all;

entity yywedsde is
  port (lsmtiz : linkage std_logic);
end yywedsde;

library ieee;
use ieee.std_logic_1164.all;

architecture dxe of yywedsde is
  signal hmiknvlcc : std_logic;
  signal dtfcdh : real;
  signal zuxypl : time;
  signal yzf : integer_vector(0 to 2);
  signal ta : bit_vector(2 downto 0);
begin
  ydmkdtfysg : entity work.zt
    port map (qtkv => ta);
  jsmut : entity work.urdqdfa
    port map (vvrvnuj => yzf, qzi => zuxypl, oorpfn => dtfcdh, z => hmiknvlcc);
end dxe;



-- Seed after: 12782167572236252203,7142793346053417159
