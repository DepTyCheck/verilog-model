-- Seed: 13190584993476535326,4404421571376382767

library ieee;
use ieee.std_logic_1164.all;

entity zzrmz is
  port (oaf : in std_logic; flileftev : in std_logic_vector(4 to 0); uarr : inout real);
end zzrmz;

architecture kaamanknru of zzrmz is
  
begin
  -- Single-driven assignments
  uarr <= uarr;
end kaamanknru;

library ieee;
use ieee.std_logic_1164.all;

entity k is
  port (mza : out integer; qpspe : linkage std_logic_vector(3 downto 2));
end k;

library ieee;
use ieee.std_logic_1164.all;

architecture qergligii of k is
  signal xmnarq : real;
  signal xi : std_logic_vector(4 to 0);
  signal rua : std_logic;
begin
  ozekhysgnc : entity work.zzrmz
    port map (oaf => rua, flileftev => xi, uarr => xmnarq);
  
  -- Single-driven assignments
  mza <= mza;
  
  -- Multi-driven assignments
  rua <= rua;
  xi <= xi;
  rua <= rua;
  xi <= xi;
end qergligii;

entity oy is
  port (dw : linkage character);
end oy;

library ieee;
use ieee.std_logic_1164.all;

architecture nmplokv of oy is
  signal taw : real;
  signal mlhlp : real;
  signal xcr : std_logic;
  signal s : std_logic_vector(3 downto 2);
  signal x : integer;
  signal sn : real;
  signal atfpgd : std_logic_vector(4 to 0);
  signal fcvsfaonbv : std_logic;
begin
  o : entity work.zzrmz
    port map (oaf => fcvsfaonbv, flileftev => atfpgd, uarr => sn);
  mrbnraf : entity work.k
    port map (mza => x, qpspe => s);
  kmj : entity work.zzrmz
    port map (oaf => xcr, flileftev => atfpgd, uarr => mlhlp);
  h : entity work.zzrmz
    port map (oaf => fcvsfaonbv, flileftev => atfpgd, uarr => taw);
  
  -- Multi-driven assignments
  s <= s;
end nmplokv;



-- Seed after: 10518433802013571806,4404421571376382767
