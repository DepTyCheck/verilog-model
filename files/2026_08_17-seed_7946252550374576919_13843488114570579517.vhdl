-- Seed: 7946252550374576919,13843488114570579517

library ieee;
use ieee.std_logic_1164.all;

entity ahefr is
  port (vucnn : out std_logic; t : out real; i : linkage bit_vector(2 to 0); wftfmsmas : buffer time);
end ahefr;

architecture zpw of ahefr is
  
begin
  
end zpw;

entity m is
  port (rjli : buffer integer);
end m;

library ieee;
use ieee.std_logic_1164.all;

architecture fsejwo of m is
  signal yyklift : time;
  signal scnob : bit_vector(2 to 0);
  signal mikf : real;
  signal wipek : std_logic;
  signal jehjthr : time;
  signal xtvwmbud : bit_vector(2 to 0);
  signal rj : real;
  signal fq : std_logic;
begin
  wedsto : entity work.ahefr
    port map (vucnn => fq, t => rj, i => xtvwmbud, wftfmsmas => jehjthr);
  vcsrpakdld : entity work.ahefr
    port map (vucnn => wipek, t => mikf, i => scnob, wftfmsmas => yyklift);
  
  -- Single-driven assignments
  rjli <= 2#1#;
  
  -- Multi-driven assignments
  fq <= fq;
  fq <= fq;
  fq <= fq;
  fq <= 'U';
end fsejwo;

library ieee;
use ieee.std_logic_1164.all;

entity yjnpe is
  port (a : buffer std_logic; wmzv : inout integer; cf : linkage string(4 downto 2); nfyjii : inout character);
end yjnpe;

library ieee;
use ieee.std_logic_1164.all;

architecture kdujdbs of yjnpe is
  signal apzzi : time;
  signal rvnbk : bit_vector(2 to 0);
  signal itnkxytdtk : real;
  signal eticyyut : time;
  signal yibjribbph : bit_vector(2 to 0);
  signal hlwf : real;
  signal edoazsumn : std_logic;
begin
  hpphxjqt : entity work.ahefr
    port map (vucnn => edoazsumn, t => hlwf, i => yibjribbph, wftfmsmas => eticyyut);
  rnm : entity work.ahefr
    port map (vucnn => edoazsumn, t => itnkxytdtk, i => rvnbk, wftfmsmas => apzzi);
  
  -- Single-driven assignments
  wmzv <= 8#7605#;
  nfyjii <= nfyjii;
  
  -- Multi-driven assignments
  a <= 'U';
  a <= a;
  a <= a;
  a <= edoazsumn;
end kdujdbs;



-- Seed after: 17433619844827405090,13843488114570579517
