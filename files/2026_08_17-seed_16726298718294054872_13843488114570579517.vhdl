-- Seed: 16726298718294054872,13843488114570579517

library ieee;
use ieee.std_logic_1164.all;

entity xw is
  port (ytckvs : inout integer_vector(3 to 2); opqnae : inout std_logic; pxfh : linkage std_logic);
end xw;

architecture rczgiqhqd of xw is
  
begin
  
end rczgiqhqd;

entity ftutxgei is
  port (tbfkqpsuiw : out character; cob : buffer integer; rrljck : buffer bit_vector(3 downto 3));
end ftutxgei;

library ieee;
use ieee.std_logic_1164.all;

architecture cx of ftutxgei is
  signal vatg : std_logic;
  signal aaqwod : std_logic;
  signal eykv : integer_vector(3 to 2);
  signal jbzfx : std_logic;
  signal j : std_logic;
  signal eprimcw : integer_vector(3 to 2);
  signal wn : std_logic;
  signal sjejlsj : integer_vector(3 to 2);
  signal vneauatw : std_logic;
  signal csqryvzls : std_logic;
  signal tzxza : integer_vector(3 to 2);
begin
  buhmyl : entity work.xw
    port map (ytckvs => tzxza, opqnae => csqryvzls, pxfh => vneauatw);
  aptfma : entity work.xw
    port map (ytckvs => sjejlsj, opqnae => wn, pxfh => csqryvzls);
  nkpvh : entity work.xw
    port map (ytckvs => eprimcw, opqnae => j, pxfh => jbzfx);
  iduljd : entity work.xw
    port map (ytckvs => eykv, opqnae => aaqwod, pxfh => vatg);
  
  -- Multi-driven assignments
  aaqwod <= '-';
  aaqwod <= vatg;
end cx;

entity ucusuqbv is
  port (bmjhs : inout real; dy : out boolean);
end ucusuqbv;

library ieee;
use ieee.std_logic_1164.all;

architecture thshjql of ucusuqbv is
  signal anb : bit_vector(3 downto 3);
  signal wmr : integer;
  signal eoqi : character;
  signal vssa : std_logic;
  signal hechwz : std_logic;
  signal zucgawjqg : integer_vector(3 to 2);
begin
  dejwfnybdt : entity work.xw
    port map (ytckvs => zucgawjqg, opqnae => hechwz, pxfh => vssa);
  xlvoi : entity work.ftutxgei
    port map (tbfkqpsuiw => eoqi, cob => wmr, rrljck => anb);
  
  -- Single-driven assignments
  dy <= FALSE;
  bmjhs <= 4.0212;
  
  -- Multi-driven assignments
  hechwz <= '1';
  hechwz <= hechwz;
end thshjql;

entity nel is
  port (wfaszyf : buffer integer; inkbsov : inout severity_level);
end nel;

library ieee;
use ieee.std_logic_1164.all;

architecture zezvycpiz of nel is
  signal a : std_logic;
  signal ydvsw : integer_vector(3 to 2);
  signal ctmwuhqlhr : integer_vector(3 to 2);
  signal u : std_logic;
  signal xklct : std_logic;
  signal dsmmwwz : integer_vector(3 to 2);
  signal nc : boolean;
  signal j : real;
begin
  dwfl : entity work.ucusuqbv
    port map (bmjhs => j, dy => nc);
  nas : entity work.xw
    port map (ytckvs => dsmmwwz, opqnae => xklct, pxfh => u);
  oewdhjwwl : entity work.xw
    port map (ytckvs => ctmwuhqlhr, opqnae => xklct, pxfh => xklct);
  v : entity work.xw
    port map (ytckvs => ydvsw, opqnae => a, pxfh => a);
  
  -- Single-driven assignments
  inkbsov <= inkbsov;
  wfaszyf <= wfaszyf;
  
  -- Multi-driven assignments
  xklct <= u;
  xklct <= '0';
  xklct <= u;
end zezvycpiz;



-- Seed after: 8185319789810523694,13843488114570579517
