-- Seed: 6198546447605788500,6882842853887419669

entity wklublrl is
  port (hiircvveks : linkage integer_vector(2 to 3); ltyb : buffer time; z : linkage bit_vector(2 to 2); sqdiy : linkage time);
end wklublrl;

architecture ttd of wklublrl is
  
begin
  -- Single-driven assignments
  ltyb <= 2#10100# ps;
end ttd;

entity tuzphip is
  port (xei : buffer real);
end tuzphip;

architecture pwrbyi of tuzphip is
  signal s : time;
  signal iipk : bit_vector(2 to 2);
  signal yf : time;
  signal ttyq : integer_vector(2 to 3);
  signal zzxsw : time;
  signal ggfrms : bit_vector(2 to 2);
  signal oainanj : time;
  signal zwrfpgfg : integer_vector(2 to 3);
  signal ewtzbebopn : time;
  signal yftie : bit_vector(2 to 2);
  signal kpmarewk : time;
  signal rjavcexjq : integer_vector(2 to 3);
  signal lmo : time;
  signal bfpo : bit_vector(2 to 2);
  signal ghx : time;
  signal jkzotakpm : integer_vector(2 to 3);
begin
  usjfmomb : entity work.wklublrl
    port map (hiircvveks => jkzotakpm, ltyb => ghx, z => bfpo, sqdiy => lmo);
  vqpd : entity work.wklublrl
    port map (hiircvveks => rjavcexjq, ltyb => kpmarewk, z => yftie, sqdiy => ewtzbebopn);
  clnuesx : entity work.wklublrl
    port map (hiircvveks => zwrfpgfg, ltyb => oainanj, z => ggfrms, sqdiy => zzxsw);
  suqsge : entity work.wklublrl
    port map (hiircvveks => ttyq, ltyb => yf, z => iipk, sqdiy => s);
end pwrbyi;



-- Seed after: 3993680129914989001,6882842853887419669
