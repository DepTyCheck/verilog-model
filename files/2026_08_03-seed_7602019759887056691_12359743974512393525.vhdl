-- Seed: 7602019759887056691,12359743974512393525

entity wuukbzf is
  port (kpltgxhxxu : buffer real; fbavahnqz : inout integer; rezpfik : linkage real_vector(0 to 2); qis : out severity_level);
end wuukbzf;

architecture wajn of wuukbzf is
  
begin
  -- Single-driven assignments
  qis <= qis;
  kpltgxhxxu <= kpltgxhxxu;
  fbavahnqz <= fbavahnqz;
end wajn;

entity uaqapgfhuc is
  port (pzvwpataee : in bit_vector(3 downto 0); smimiip : buffer character; vdllaoju : out time);
end uaqapgfhuc;

architecture vq of uaqapgfhuc is
  signal wffowizg : severity_level;
  signal nd : real_vector(0 to 2);
  signal fyivkxaqnw : integer;
  signal gfqkfvi : real;
  signal qksadbcpi : severity_level;
  signal gjve : real_vector(0 to 2);
  signal cilhvsil : integer;
  signal ax : real;
  signal ltrknt : severity_level;
  signal yl : real_vector(0 to 2);
  signal yj : integer;
  signal iqnii : real;
begin
  aynk : entity work.wuukbzf
    port map (kpltgxhxxu => iqnii, fbavahnqz => yj, rezpfik => yl, qis => ltrknt);
  i : entity work.wuukbzf
    port map (kpltgxhxxu => ax, fbavahnqz => cilhvsil, rezpfik => gjve, qis => qksadbcpi);
  d : entity work.wuukbzf
    port map (kpltgxhxxu => gfqkfvi, fbavahnqz => fyivkxaqnw, rezpfik => nd, qis => wffowizg);
  
  -- Single-driven assignments
  vdllaoju <= 0 ps;
  smimiip <= smimiip;
end vq;

entity ppoohz is
  port (gtm : in real);
end ppoohz;

architecture fo of ppoohz is
  signal vbd : severity_level;
  signal naganmitt : real_vector(0 to 2);
  signal ddtku : integer;
  signal prpaiorby : real;
  signal ot : severity_level;
  signal wwfp : real_vector(0 to 2);
  signal zhsytowg : integer;
  signal jmp : real;
  signal vptbl : time;
  signal bvkpx : character;
  signal bfgizaoz : bit_vector(3 downto 0);
  signal rjjfrfn : severity_level;
  signal e : real_vector(0 to 2);
  signal uowf : integer;
  signal rityjuhs : real;
begin
  hnuwykjm : entity work.wuukbzf
    port map (kpltgxhxxu => rityjuhs, fbavahnqz => uowf, rezpfik => e, qis => rjjfrfn);
  fzzagvvlct : entity work.uaqapgfhuc
    port map (pzvwpataee => bfgizaoz, smimiip => bvkpx, vdllaoju => vptbl);
  l : entity work.wuukbzf
    port map (kpltgxhxxu => jmp, fbavahnqz => zhsytowg, rezpfik => wwfp, qis => ot);
  lvntnsjnkb : entity work.wuukbzf
    port map (kpltgxhxxu => prpaiorby, fbavahnqz => ddtku, rezpfik => naganmitt, qis => vbd);
  
  -- Single-driven assignments
  bfgizaoz <= ('0', '1', '0', '0');
end fo;



-- Seed after: 9534124316836944538,12359743974512393525
