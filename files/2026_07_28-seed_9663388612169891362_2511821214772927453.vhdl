-- Seed: 9663388612169891362,2511821214772927453

entity bwhqs is
  port (pqerzgesz : buffer bit_vector(0 to 1); rupolsy : in integer);
end bwhqs;

architecture ahp of bwhqs is
  
begin
  -- Single-driven assignments
  pqerzgesz <= ('0', '0');
end ahp;

library ieee;
use ieee.std_logic_1164.all;

entity ohgode is
  port (gluyqqjk : linkage std_logic);
end ohgode;

architecture pvaybabi of ohgode is
  signal pqxp : integer;
  signal ulq : bit_vector(0 to 1);
  signal ztsrojl : integer;
  signal cctqng : bit_vector(0 to 1);
  signal i : bit_vector(0 to 1);
  signal oort : integer;
  signal ycki : bit_vector(0 to 1);
begin
  qeagbd : entity work.bwhqs
    port map (pqerzgesz => ycki, rupolsy => oort);
  eydods : entity work.bwhqs
    port map (pqerzgesz => i, rupolsy => oort);
  fwdbempt : entity work.bwhqs
    port map (pqerzgesz => cctqng, rupolsy => ztsrojl);
  yenyjwnxuc : entity work.bwhqs
    port map (pqerzgesz => ulq, rupolsy => pqxp);
end pvaybabi;

entity xcotnv is
  port (ho : in real; yyiaozkz : linkage time_vector(0 downto 1));
end xcotnv;

architecture anweirb of xcotnv is
  signal shj : bit_vector(0 to 1);
  signal qiuf : integer;
  signal g : bit_vector(0 to 1);
begin
  pfom : entity work.bwhqs
    port map (pqerzgesz => g, rupolsy => qiuf);
  dl : entity work.bwhqs
    port map (pqerzgesz => shj, rupolsy => qiuf);
  
  -- Single-driven assignments
  qiuf <= 16#4#;
end anweirb;



-- Seed after: 2652875718420661369,2511821214772927453
