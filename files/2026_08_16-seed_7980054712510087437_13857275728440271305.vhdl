-- Seed: 7980054712510087437,13857275728440271305

entity tm is
  port (toe : linkage bit; nvibtl : out integer_vector(1 downto 1); lwhqvowh : inout integer_vector(2 to 0));
end tm;

architecture cuy of tm is
  
begin
  
end cuy;

library ieee;
use ieee.std_logic_1164.all;

entity r is
  port (gsjvzdsxlk : inout severity_level; v : buffer time_vector(3 downto 2); hoewylddz : in std_logic_vector(3 to 1); fczax : inout real);
end r;

architecture hpi of r is
  signal oixxm : integer_vector(2 to 0);
  signal wrubwngnke : integer_vector(1 downto 1);
  signal ybipyowzj : bit;
  signal jyjyp : integer_vector(2 to 0);
  signal jcfzhv : integer_vector(1 downto 1);
  signal hqcrqq : bit;
  signal ivdensa : integer_vector(2 to 0);
  signal pfiotkmlzq : integer_vector(1 downto 1);
  signal qn : bit;
  signal yyzgj : integer_vector(2 to 0);
  signal lz : integer_vector(1 downto 1);
  signal ntnw : bit;
begin
  iliuwwjb : entity work.tm
    port map (toe => ntnw, nvibtl => lz, lwhqvowh => yyzgj);
  puijwxg : entity work.tm
    port map (toe => qn, nvibtl => pfiotkmlzq, lwhqvowh => ivdensa);
  xcdest : entity work.tm
    port map (toe => hqcrqq, nvibtl => jcfzhv, lwhqvowh => jyjyp);
  rucoxanfjt : entity work.tm
    port map (toe => ybipyowzj, nvibtl => wrubwngnke, lwhqvowh => oixxm);
  
  -- Single-driven assignments
  fczax <= 16#7D13.9550#;
  gsjvzdsxlk <= ERROR;
  v <= v;
end hpi;



-- Seed after: 1796637536720468042,13857275728440271305
