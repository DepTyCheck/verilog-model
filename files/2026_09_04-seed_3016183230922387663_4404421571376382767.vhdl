-- Seed: 3016183230922387663,4404421571376382767

library ieee;
use ieee.std_logic_1164.all;

entity rodnfanz is
  port (g : buffer bit_vector(0 downto 4); pn : out real; ardwo : inout std_logic; fkrbnig : linkage time);
end rodnfanz;

architecture pqudnk of rodnfanz is
  
begin
  -- Single-driven assignments
  g <= (others => '0');
  
  -- Multi-driven assignments
  ardwo <= ardwo;
  ardwo <= 'W';
  ardwo <= '0';
end pqudnk;

entity yjtlnc is
  port (gwvzqzzs : inout time; brjscnao : inout integer; r : linkage integer_vector(3 to 1); ofwlh : buffer string(4 to 4));
end yjtlnc;

architecture baqr of yjtlnc is
  
begin
  -- Single-driven assignments
  brjscnao <= brjscnao;
end baqr;

library ieee;
use ieee.std_logic_1164.all;

entity ytsifmkxs is
  port (d : out std_logic; fuujdbhgxv : linkage bit_vector(1 to 1); bmsjfvha : in time);
end ytsifmkxs;

architecture m of ytsifmkxs is
  signal xjnisoguew : time;
  signal kjawpomqvj : real;
  signal vj : bit_vector(0 downto 4);
  signal mbs : time;
  signal dihnqeilv : real;
  signal nlxzat : bit_vector(0 downto 4);
  signal rjgv : string(4 to 4);
  signal lsgswq : integer_vector(3 to 1);
  signal lkocsuvvep : integer;
  signal duhekrta : time;
begin
  dy : entity work.yjtlnc
    port map (gwvzqzzs => duhekrta, brjscnao => lkocsuvvep, r => lsgswq, ofwlh => rjgv);
  vsb : entity work.rodnfanz
    port map (g => nlxzat, pn => dihnqeilv, ardwo => d, fkrbnig => mbs);
  k : entity work.rodnfanz
    port map (g => vj, pn => kjawpomqvj, ardwo => d, fkrbnig => xjnisoguew);
  
  -- Multi-driven assignments
  d <= 'H';
  d <= '1';
  d <= 'W';
  d <= 'U';
end m;

entity arxjwnpyvk is
  port (zxrzwqd : out time);
end arxjwnpyvk;

library ieee;
use ieee.std_logic_1164.all;

architecture eerkzeamjx of arxjwnpyvk is
  signal r : time;
  signal skqtbhnoo : real;
  signal onffv : bit_vector(0 downto 4);
  signal uygjid : std_logic;
  signal ziskw : real;
  signal suctxtnnfb : bit_vector(0 downto 4);
begin
  o : entity work.rodnfanz
    port map (g => suctxtnnfb, pn => ziskw, ardwo => uygjid, fkrbnig => zxrzwqd);
  tha : entity work.rodnfanz
    port map (g => onffv, pn => skqtbhnoo, ardwo => uygjid, fkrbnig => r);
  
  -- Multi-driven assignments
  uygjid <= 'X';
  uygjid <= 'Z';
  uygjid <= uygjid;
  uygjid <= uygjid;
end eerkzeamjx;



-- Seed after: 9565321214613255721,4404421571376382767
