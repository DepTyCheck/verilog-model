-- Seed: 4610359683094184872,5306691039457971049

library ieee;
use ieee.std_logic_1164.all;

entity zjqddukrbx is
  port (z : inout time; ujci : buffer boolean; xrqsq : in std_logic; sjomdqdv : out real_vector(2 to 1));
end zjqddukrbx;

architecture iabi of zjqddukrbx is
  
begin
  -- Single-driven assignments
  sjomdqdv <= sjomdqdv;
  z <= z;
  ujci <= FALSE;
end iabi;

library ieee;
use ieee.std_logic_1164.all;

entity spkafcyavq is
  port (ub : buffer real_vector(1 to 1); ulidgvbhti : linkage std_logic_vector(0 downto 0); tlvlzgx : linkage integer);
end spkafcyavq;

library ieee;
use ieee.std_logic_1164.all;

architecture nvzi of spkafcyavq is
  signal epsbxncc : real_vector(2 to 1);
  signal nb : std_logic;
  signal ren : boolean;
  signal isviffi : time;
  signal lpnjv : real_vector(2 to 1);
  signal advx : std_logic;
  signal injqf : boolean;
  signal tkugfvik : time;
  signal opcl : real_vector(2 to 1);
  signal bx : std_logic;
  signal yz : boolean;
  signal ld : time;
begin
  ew : entity work.zjqddukrbx
    port map (z => ld, ujci => yz, xrqsq => bx, sjomdqdv => opcl);
  ouu : entity work.zjqddukrbx
    port map (z => tkugfvik, ujci => injqf, xrqsq => advx, sjomdqdv => lpnjv);
  owvu : entity work.zjqddukrbx
    port map (z => isviffi, ujci => ren, xrqsq => nb, sjomdqdv => epsbxncc);
  
  -- Multi-driven assignments
  bx <= bx;
end nvzi;



-- Seed after: 11428331512484025158,5306691039457971049
