-- Seed: 5968954857333438978,18424117564733761959



entity m is
  port (efhsah : linkage severity_level; fn : out real; x : inout bit; gimrsy : out boolean);
end m;



architecture ttl of m is
  
begin
  
end ttl;

library ieee;
use ieee.std_logic_1164.all;

entity fl is
  port (uzccb : in integer; gbdyf : in std_logic; z : in character; ouglfro : out std_logic);
end fl;



architecture gert of fl is
  signal xuuxuhivf : boolean;
  signal dyuwbrgevh : bit;
  signal evplzpmiwk : real;
  signal izk : severity_level;
  signal ja : boolean;
  signal bi : bit;
  signal lxplw : real;
  signal wykfbu : severity_level;
  signal ujqgixcq : boolean;
  signal t : bit;
  signal yykevstfwx : real;
  signal xvqvegcsoo : severity_level;
begin
  uaaadb : entity work.m
    port map (efhsah => xvqvegcsoo, fn => yykevstfwx, x => t, gimrsy => ujqgixcq);
  fkx : entity work.m
    port map (efhsah => wykfbu, fn => lxplw, x => bi, gimrsy => ja);
  tcnzwg : entity work.m
    port map (efhsah => izk, fn => evplzpmiwk, x => dyuwbrgevh, gimrsy => xuuxuhivf);
end gert;



-- Seed after: 16475377121252379774,18424117564733761959
