-- Seed: 9945003369414173408,6000118208082478503

library ieee;
use ieee.std_logic_1164.all;

entity r is
  port (eff : inout std_logic_vector(0 downto 2); mzsgkq : linkage real);
end r;

architecture xloj of r is
  
begin
  -- Multi-driven assignments
  eff <= "";
  eff <= eff;
  eff <= "";
end xloj;

entity y is
  port (d : out boolean_vector(4 downto 4); gpenhm : inout boolean);
end y;

library ieee;
use ieee.std_logic_1164.all;

architecture bb of y is
  signal twgaqj : real;
  signal uty : real;
  signal i : real;
  signal qqjwtc : std_logic_vector(0 downto 2);
begin
  l : entity work.r
    port map (eff => qqjwtc, mzsgkq => i);
  ro : entity work.r
    port map (eff => qqjwtc, mzsgkq => uty);
  nvevmn : entity work.r
    port map (eff => qqjwtc, mzsgkq => twgaqj);
  
  -- Multi-driven assignments
  qqjwtc <= (others => '0');
end bb;

entity nuausfrjzd is
  port (tkagy : in time; oyaztcmbu : in real; vbs : linkage integer_vector(1 downto 3); thmxo : inout real);
end nuausfrjzd;

library ieee;
use ieee.std_logic_1164.all;

architecture bzg of nuausfrjzd is
  signal a : real;
  signal up : std_logic_vector(0 downto 2);
  signal lyt : boolean;
  signal ziw : boolean_vector(4 downto 4);
begin
  tlfz : entity work.y
    port map (d => ziw, gpenhm => lyt);
  tgpwyvgk : entity work.r
    port map (eff => up, mzsgkq => a);
  
  -- Single-driven assignments
  thmxo <= 3_3.3;
end bzg;

library ieee;
use ieee.std_logic_1164.all;

entity xrwlplwxls is
  port (pexkrsefno : linkage real; citl : linkage time_vector(0 downto 1); ql : linkage std_logic_vector(3 downto 1));
end xrwlplwxls;

library ieee;
use ieee.std_logic_1164.all;

architecture gzkwtwzj of xrwlplwxls is
  signal ynd : real;
  signal lg : std_logic_vector(0 downto 2);
  signal blk : std_logic_vector(0 downto 2);
  signal ve : boolean;
  signal x : boolean_vector(4 downto 4);
  signal ck : real;
  signal mz : std_logic_vector(0 downto 2);
begin
  k : entity work.r
    port map (eff => mz, mzsgkq => ck);
  j : entity work.y
    port map (d => x, gpenhm => ve);
  sclup : entity work.r
    port map (eff => blk, mzsgkq => pexkrsefno);
  zc : entity work.r
    port map (eff => lg, mzsgkq => ynd);
end gzkwtwzj;



-- Seed after: 13089995368412909361,6000118208082478503
