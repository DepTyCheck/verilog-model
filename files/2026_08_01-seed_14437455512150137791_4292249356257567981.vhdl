-- Seed: 14437455512150137791,4292249356257567981

library ieee;
use ieee.std_logic_1164.all;

entity pkkjaob is
  port (ttxwzfryip : linkage time; orrrd : linkage std_logic_vector(2 to 2); lawjwrunfi : linkage time; yhorgtid : in integer);
end pkkjaob;

architecture plzmzm of pkkjaob is
  
begin
  
end plzmzm;

entity csejmr is
  port (x : buffer integer);
end csejmr;

library ieee;
use ieee.std_logic_1164.all;

architecture rktftgpsz of csejmr is
  signal pyyjupop : time;
  signal nk : std_logic_vector(2 to 2);
  signal lnam : time;
  signal ptabn : integer;
  signal lsxswziq : time;
  signal vdcsavbxno : time;
  signal cbumojfqq : time;
  signal eo : std_logic_vector(2 to 2);
  signal eaemvd : time;
  signal izrcy : time;
  signal th : std_logic_vector(2 to 2);
  signal rfk : time;
begin
  rvqdwf : entity work.pkkjaob
    port map (ttxwzfryip => rfk, orrrd => th, lawjwrunfi => izrcy, yhorgtid => x);
  jhxurchhsv : entity work.pkkjaob
    port map (ttxwzfryip => eaemvd, orrrd => eo, lawjwrunfi => cbumojfqq, yhorgtid => x);
  jcdbrffgq : entity work.pkkjaob
    port map (ttxwzfryip => vdcsavbxno, orrrd => th, lawjwrunfi => lsxswziq, yhorgtid => ptabn);
  rvseiu : entity work.pkkjaob
    port map (ttxwzfryip => lnam, orrrd => nk, lawjwrunfi => pyyjupop, yhorgtid => x);
  
  -- Single-driven assignments
  ptabn <= x;
  x <= x;
end rktftgpsz;

entity igelicpd is
  port (ljfj : linkage time; dvilq : in time_vector(1 downto 2));
end igelicpd;

library ieee;
use ieee.std_logic_1164.all;

architecture isl of igelicpd is
  signal hlnrwgema : integer;
  signal d : time;
  signal pghlzciaaj : std_logic_vector(2 to 2);
begin
  bvrnozu : entity work.pkkjaob
    port map (ttxwzfryip => ljfj, orrrd => pghlzciaaj, lawjwrunfi => d, yhorgtid => hlnrwgema);
  
  -- Single-driven assignments
  hlnrwgema <= 1442;
  
  -- Multi-driven assignments
  pghlzciaaj <= "U";
end isl;



-- Seed after: 10354515331842212744,4292249356257567981
