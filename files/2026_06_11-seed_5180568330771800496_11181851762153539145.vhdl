-- Seed: 5180568330771800496,11181851762153539145



entity yp is
  port (upimjsq : inout boolean);
end yp;



architecture shxyu of yp is
  
begin
  
end shxyu;



entity nl is
  port (gigu : in integer; lzjuzxdt : out time);
end nl;



architecture t of nl is
  
begin
  
end t;

library ieee;
use ieee.std_logic_1164.all;

entity ha is
  port (fre : inout integer_vector(3 to 2); ufnei : inout character; jmvs : buffer std_logic);
end ha;



architecture fagixiw of ha is
  signal gbijyvpwh : time;
  signal fr : integer;
  signal idafutbu : boolean;
  signal dkgaak : boolean;
begin
  dvrmppl : entity work.yp
    port map (upimjsq => dkgaak);
  kfhijvfog : entity work.yp
    port map (upimjsq => idafutbu);
  z : entity work.nl
    port map (gigu => fr, lzjuzxdt => gbijyvpwh);
end fagixiw;

library ieee;
use ieee.std_logic_1164.all;

entity kvcvwau is
  port (ica : inout time; pavuswq : linkage std_logic_vector(1 downto 3); w : buffer integer; riyxiauhgj : in time);
end kvcvwau;

library ieee;
use ieee.std_logic_1164.all;

architecture xfxtl of kvcvwau is
  signal zbqor : std_logic;
  signal qfzju : character;
  signal qgubuyussq : integer_vector(3 to 2);
  signal zyxyj : std_logic;
  signal x : character;
  signal fppiz : integer_vector(3 to 2);
begin
  h : entity work.ha
    port map (fre => fppiz, ufnei => x, jmvs => zyxyj);
  pmjskcppy : entity work.ha
    port map (fre => qgubuyussq, ufnei => qfzju, jmvs => zbqor);
end xfxtl;



-- Seed after: 2209081458672074602,11181851762153539145
