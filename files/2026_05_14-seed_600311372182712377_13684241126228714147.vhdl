-- Seed: 600311372182712377,13684241126228714147

library ieee;
use ieee.std_logic_1164.all;

entity im is
  port (dov : buffer std_logic; lm : in real; gtam : inout time; apwhf : in std_logic);
end im;



architecture jtjcvk of im is
  
begin
  
end jtjcvk;



entity dlv is
  port (hpj : buffer integer; lsteaq : buffer real; zymdreq : linkage time);
end dlv;

library ieee;
use ieee.std_logic_1164.all;

architecture ts of dlv is
  signal znuctuc : std_logic;
  signal x : time;
  signal hm : time;
  signal aqiwzzawt : real;
  signal idqqss : std_logic;
begin
  dilfjvs : entity work.im
    port map (dov => idqqss, lm => aqiwzzawt, gtam => hm, apwhf => idqqss);
  kz : entity work.im
    port map (dov => idqqss, lm => aqiwzzawt, gtam => x, apwhf => znuctuc);
end ts;



-- Seed after: 11018163165233830651,13684241126228714147
