-- Seed: 11456331684467461198,10463297573877745897

entity y is
  port (jzokbyndgw : inout boolean_vector(4 downto 0); bcbovp : in time; ds : buffer time; jlhnlhw : in integer);
end y;

architecture ierlupo of y is
  
begin
  -- Single-driven assignments
  jzokbyndgw <= jzokbyndgw;
end ierlupo;

library ieee;
use ieee.std_logic_1164.all;

entity gxw is
  port (h : buffer string(1 to 5); nr : in integer; qtwwpmnzy : linkage std_logic_vector(3 downto 2); cfri : in time);
end gxw;

architecture xm of gxw is
  signal dqwlnz : time;
  signal wldpiclraa : boolean_vector(4 downto 0);
  signal cndmimkyl : time;
  signal wjoiooad : boolean_vector(4 downto 0);
begin
  pilm : entity work.y
    port map (jzokbyndgw => wjoiooad, bcbovp => cfri, ds => cndmimkyl, jlhnlhw => nr);
  pe : entity work.y
    port map (jzokbyndgw => wldpiclraa, bcbovp => dqwlnz, ds => dqwlnz, jlhnlhw => nr);
  
  -- Single-driven assignments
  h <= ('m', 'b', 'l', 'c', 'h');
end xm;

entity pbc is
  port (v : in integer; nimogs : out real);
end pbc;

library ieee;
use ieee.std_logic_1164.all;

architecture kbgkak of pbc is
  signal dferr : time;
  signal rjucionn : integer;
  signal hdsip : string(1 to 5);
  signal ll : time;
  signal lvubr : string(1 to 5);
  signal rquadd : time;
  signal hgtmnoc : std_logic_vector(3 downto 2);
  signal kvwjpm : integer;
  signal ccaslazzy : string(1 to 5);
begin
  cxhptr : entity work.gxw
    port map (h => ccaslazzy, nr => kvwjpm, qtwwpmnzy => hgtmnoc, cfri => rquadd);
  dqubcng : entity work.gxw
    port map (h => lvubr, nr => v, qtwwpmnzy => hgtmnoc, cfri => ll);
  elptintdqm : entity work.gxw
    port map (h => hdsip, nr => rjucionn, qtwwpmnzy => hgtmnoc, cfri => dferr);
  
  -- Single-driven assignments
  nimogs <= nimogs;
  
  -- Multi-driven assignments
  hgtmnoc <= hgtmnoc;
  hgtmnoc <= hgtmnoc;
  hgtmnoc <= "1X";
  hgtmnoc <= hgtmnoc;
end kbgkak;

entity sgyjjqznto is
  port (ef : buffer integer; vrk : linkage character; sfzjaunip : buffer real);
end sgyjjqznto;

architecture t of sgyjjqznto is
  signal wasa : boolean_vector(4 downto 0);
  signal d : integer;
  signal lmhitolad : time;
  signal suzgsjg : time;
  signal kab : boolean_vector(4 downto 0);
begin
  nlfzm : entity work.y
    port map (jzokbyndgw => kab, bcbovp => suzgsjg, ds => lmhitolad, jlhnlhw => d);
  omiqjpxwjq : entity work.y
    port map (jzokbyndgw => wasa, bcbovp => suzgsjg, ds => suzgsjg, jlhnlhw => ef);
  
  -- Single-driven assignments
  d <= 8#2#;
  ef <= d;
  sfzjaunip <= 00.0;
end t;



-- Seed after: 1777115766889895901,10463297573877745897
