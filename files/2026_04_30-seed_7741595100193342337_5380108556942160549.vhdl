-- Seed: 7741595100193342337,5380108556942160549



entity aqlrkaz is
  port (lpy : buffer real; aoxyjxrnua : inout real; ugn : linkage integer; tezfih : out real);
end aqlrkaz;



architecture qchc of aqlrkaz is
  
begin
  
end qchc;

library ieee;
use ieee.std_logic_1164.all;

entity ru is
  port (lgo : out std_logic);
end ru;



architecture aoxpfd of ru is
  signal dimb : real;
  signal jdakpui : real;
  signal kdzys : real;
  signal ejhh : real;
  signal flhgjqdmc : real;
  signal idy : real;
  signal shugtupz : real;
  signal cfkihul : integer;
  signal ufis : real;
  signal fcesjxknwx : real;
  signal q : real;
  signal wazy : integer;
  signal nqfkx : real;
  signal hgnouwiokq : real;
begin
  igrbshkdv : entity work.aqlrkaz
    port map (lpy => hgnouwiokq, aoxyjxrnua => nqfkx, ugn => wazy, tezfih => q);
  mlm : entity work.aqlrkaz
    port map (lpy => fcesjxknwx, aoxyjxrnua => ufis, ugn => cfkihul, tezfih => shugtupz);
  csrcpxkvvn : entity work.aqlrkaz
    port map (lpy => idy, aoxyjxrnua => flhgjqdmc, ugn => wazy, tezfih => ejhh);
  hoc : entity work.aqlrkaz
    port map (lpy => kdzys, aoxyjxrnua => jdakpui, ugn => cfkihul, tezfih => dimb);
end aoxpfd;



-- Seed after: 7556014934882678796,5380108556942160549
