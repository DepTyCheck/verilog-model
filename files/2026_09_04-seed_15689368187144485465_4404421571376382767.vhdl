-- Seed: 15689368187144485465,4404421571376382767

library ieee;
use ieee.std_logic_1164.all;

entity eputcsi is
  port (i : buffer std_logic_vector(3 downto 1); wrhyuuu : buffer std_logic_vector(2 to 1); bosoyh : inout time; qlxklrvj : out time);
end eputcsi;

architecture qlq of eputcsi is
  
begin
  -- Single-driven assignments
  bosoyh <= qlxklrvj;
  
  -- Multi-driven assignments
  wrhyuuu <= "";
  wrhyuuu <= wrhyuuu;
  wrhyuuu <= (others => '0');
  wrhyuuu <= wrhyuuu;
end qlq;

library ieee;
use ieee.std_logic_1164.all;

entity ihqijiopzb is
  port (npik : inout boolean; bqakizl : buffer time; y : buffer std_logic; bz : linkage time);
end ihqijiopzb;

library ieee;
use ieee.std_logic_1164.all;

architecture keldgji of ihqijiopzb is
  signal amhe : time;
  signal irrjuq : std_logic_vector(3 downto 1);
  signal krblqqbjo : time;
  signal o : time;
  signal ikktdrnwr : std_logic_vector(2 to 1);
  signal bebfyp : time;
  signal lcjkuifyjg : time;
  signal zqdm : time;
  signal jb : time;
  signal by : std_logic_vector(2 to 1);
  signal mcpm : std_logic_vector(3 downto 1);
begin
  ipantqn : entity work.eputcsi
    port map (i => mcpm, wrhyuuu => by, bosoyh => jb, qlxklrvj => zqdm);
  su : entity work.eputcsi
    port map (i => mcpm, wrhyuuu => by, bosoyh => lcjkuifyjg, qlxklrvj => bebfyp);
  rsew : entity work.eputcsi
    port map (i => mcpm, wrhyuuu => ikktdrnwr, bosoyh => o, qlxklrvj => krblqqbjo);
  gshlxqzg : entity work.eputcsi
    port map (i => irrjuq, wrhyuuu => ikktdrnwr, bosoyh => amhe, qlxklrvj => bqakizl);
  
  -- Single-driven assignments
  npik <= npik;
end keldgji;

library ieee;
use ieee.std_logic_1164.all;

entity wcqrxsplh is
  port (jl : buffer real; ogc : buffer std_logic_vector(2 to 4));
end wcqrxsplh;

library ieee;
use ieee.std_logic_1164.all;

architecture eacxfaohc of wcqrxsplh is
  signal tisoaoi : time;
  signal kygnzikhn : time;
  signal vbvklkqknz : std_logic_vector(2 to 1);
  signal oqakozzzu : time;
  signal m : time;
  signal q : std_logic_vector(2 to 1);
  signal d : time;
  signal fqaw : time;
  signal mlmxotl : std_logic_vector(2 to 1);
  signal vjimzspy : std_logic_vector(3 downto 1);
  signal ifkdhz : time;
  signal mtpm : time;
  signal jpmpa : std_logic_vector(2 to 1);
begin
  membu : entity work.eputcsi
    port map (i => ogc, wrhyuuu => jpmpa, bosoyh => mtpm, qlxklrvj => ifkdhz);
  kzpnyrnfy : entity work.eputcsi
    port map (i => vjimzspy, wrhyuuu => mlmxotl, bosoyh => fqaw, qlxklrvj => d);
  xxvqhptg : entity work.eputcsi
    port map (i => ogc, wrhyuuu => q, bosoyh => m, qlxklrvj => oqakozzzu);
  dmacowh : entity work.eputcsi
    port map (i => vjimzspy, wrhyuuu => vbvklkqknz, bosoyh => kygnzikhn, qlxklrvj => tisoaoi);
  
  -- Single-driven assignments
  jl <= 04.3003;
  
  -- Multi-driven assignments
  ogc <= ('1', 'X', '1');
  jpmpa <= jpmpa;
  mlmxotl <= mlmxotl;
  vjimzspy <= ogc;
end eacxfaohc;



-- Seed after: 5919815615832733358,4404421571376382767
