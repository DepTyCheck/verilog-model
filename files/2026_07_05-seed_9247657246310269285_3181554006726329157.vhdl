-- Seed: 9247657246310269285,3181554006726329157

use std.reflection.all;

entity crgwrki is
  port (kjv : inout floating_subtype_mirror; ckr : out real; cyn : inout access_subtype_mirror; iz : in real_vector(0 to 3));
end crgwrki;

architecture eix of crgwrki is
  
begin
  -- Single-driven assignments
  ckr <= 2_3_1_4_4.3;
end eix;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity i is
  port (p : out real; ltfmkdb : out std_logic; pscen : inout physical_subtype_mirror; hflv : in real_vector(2 to 1));
end i;

use std.reflection.all;

architecture roxbqdwk of i is
  signal xv : real_vector(0 to 3);
  shared variable vwepgxi : access_subtype_mirror;
  shared variable oggnrn : floating_subtype_mirror;
  signal wmbafn : real_vector(0 to 3);
  shared variable biluw : access_subtype_mirror;
  signal kuthznzq : real;
  shared variable uebhrd : floating_subtype_mirror;
begin
  q : entity work.crgwrki
    port map (kjv => uebhrd, ckr => kuthznzq, cyn => biluw, iz => wmbafn);
  jtdedhx : entity work.crgwrki
    port map (kjv => oggnrn, ckr => p, cyn => vwepgxi, iz => xv);
  
  -- Multi-driven assignments
  ltfmkdb <= ltfmkdb;
end roxbqdwk;



-- Seed after: 18322207684154203625,3181554006726329157
