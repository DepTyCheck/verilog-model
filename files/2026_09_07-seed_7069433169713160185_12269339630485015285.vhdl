-- Seed: 7069433169713160185,12269339630485015285

library ieee;
use ieee.std_logic_1164.all;

entity pm is
  port (cwxp : in std_logic_vector(3 downto 1); qdsk : inout time; nux : out integer_vector(1 downto 0); muvmiceapy : inout bit_vector(3 to 2));
end pm;

architecture gxrmptg of pm is
  
begin
  -- Single-driven assignments
  nux <= (3_0, 1);
  muvmiceapy <= muvmiceapy;
  qdsk <= 0.312 us;
end gxrmptg;

library ieee;
use ieee.std_logic_1164.all;

entity vcszdrqv is
  port (mbcf : out integer; anrixfzzw : in std_logic_vector(4 downto 4); obvwlvuj : in integer; ulbupv : inout time_vector(1 downto 2));
end vcszdrqv;

library ieee;
use ieee.std_logic_1164.all;

architecture jakmjzkhf of vcszdrqv is
  signal ezyrm : bit_vector(3 to 2);
  signal xk : integer_vector(1 downto 0);
  signal q : time;
  signal v : std_logic_vector(3 downto 1);
  signal br : bit_vector(3 to 2);
  signal i : integer_vector(1 downto 0);
  signal bgws : time;
  signal fbc : std_logic_vector(3 downto 1);
begin
  qjdqyhvvm : entity work.pm
    port map (cwxp => fbc, qdsk => bgws, nux => i, muvmiceapy => br);
  tjrhkt : entity work.pm
    port map (cwxp => v, qdsk => q, nux => xk, muvmiceapy => ezyrm);
end jakmjzkhf;



-- Seed after: 1264304532245828401,12269339630485015285
