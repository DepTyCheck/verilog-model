-- Seed: 775400426937689320,17341743235868673497



entity ef is
  port (rmsnbtq : out real; gimoee : out time; w : buffer bit);
end ef;



architecture zkadbhqia of ef is
  
begin
  
end zkadbhqia;

library ieee;
use ieee.std_logic_1164.all;

entity rmlxgf is
  port (z : in real; tp : inout time; hgov : buffer std_logic);
end rmlxgf;



architecture np of rmlxgf is
  signal cbwf : bit;
  signal hlvsvluizf : time;
  signal gopcixq : real;
  signal ictn : bit;
  signal n : real;
  signal wgzzo : bit;
  signal winnfqt : time;
  signal arpbgj : real;
  signal rdjriggsjw : bit;
  signal tvg : time;
  signal kyni : real;
begin
  salix : entity work.ef
    port map (rmsnbtq => kyni, gimoee => tvg, w => rdjriggsjw);
  pmdydxe : entity work.ef
    port map (rmsnbtq => arpbgj, gimoee => winnfqt, w => wgzzo);
  qhlj : entity work.ef
    port map (rmsnbtq => n, gimoee => tp, w => ictn);
  bvoox : entity work.ef
    port map (rmsnbtq => gopcixq, gimoee => hlvsvluizf, w => cbwf);
end np;



-- Seed after: 12474265930147344505,17341743235868673497
