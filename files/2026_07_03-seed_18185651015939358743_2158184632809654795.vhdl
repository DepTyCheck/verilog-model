-- Seed: 18185651015939358743,2158184632809654795

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity ecw is
  port ( jyhqunvkdq : inout integer_value_mirror
  ; sdhnfobaq : buffer integer
  ; jklo : in std_logic_vector(0 downto 0)
  ; uarmv : inout protected_subtype_mirror
  );
end ecw;

architecture ktwgl of ecw is
  
begin
  -- Single-driven assignments
  sdhnfobaq <= sdhnfobaq;
end ktwgl;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity axobcwtyd is
  port (qjwd : buffer severity_level; gdmr : buffer integer; say : inout file_value_mirror; bvkktkrai : inout std_logic);
end axobcwtyd;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture bfxcrrp of axobcwtyd is
  shared variable tyiisrf : protected_subtype_mirror;
  signal gyvsza : std_logic_vector(0 downto 0);
  shared variable guiqul : integer_value_mirror;
  shared variable gfp : protected_subtype_mirror;
  signal tholit : integer;
  shared variable dhjb : integer_value_mirror;
  shared variable tpbcw : protected_subtype_mirror;
  signal vjcxzavq : std_logic_vector(0 downto 0);
  signal lkwlkqx : integer;
  shared variable l : integer_value_mirror;
begin
  ufcwfgll : entity work.ecw
    port map (jyhqunvkdq => l, sdhnfobaq => lkwlkqx, jklo => vjcxzavq, uarmv => tpbcw);
  ixusivdrd : entity work.ecw
    port map (jyhqunvkdq => dhjb, sdhnfobaq => tholit, jklo => vjcxzavq, uarmv => gfp);
  mryzsnmj : entity work.ecw
    port map (jyhqunvkdq => guiqul, sdhnfobaq => gdmr, jklo => gyvsza, uarmv => tyiisrf);
  
  -- Single-driven assignments
  qjwd <= WARNING;
end bfxcrrp;

use std.reflection.all;

entity pbamrhll is
  port (mt : inout access_subtype_mirror; pdwcxkge : in real);
end pbamrhll;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture hhd of pbamrhll is
  signal xpeie : std_logic;
  shared variable jokatb : file_value_mirror;
  signal ovfge : integer;
  signal lgiyxojej : severity_level;
  shared variable dxv : protected_subtype_mirror;
  signal tgypn : std_logic_vector(0 downto 0);
  signal au : integer;
  shared variable tn : integer_value_mirror;
  signal aw : std_logic;
  shared variable ucr : file_value_mirror;
  signal jigjnhne : integer;
  signal uwwo : severity_level;
  shared variable whsjto : protected_subtype_mirror;
  signal clitoqz : std_logic_vector(0 downto 0);
  signal gxp : integer;
  shared variable xkzk : integer_value_mirror;
begin
  oiufjd : entity work.ecw
    port map (jyhqunvkdq => xkzk, sdhnfobaq => gxp, jklo => clitoqz, uarmv => whsjto);
  julvxq : entity work.axobcwtyd
    port map (qjwd => uwwo, gdmr => jigjnhne, say => ucr, bvkktkrai => aw);
  cy : entity work.ecw
    port map (jyhqunvkdq => tn, sdhnfobaq => au, jklo => tgypn, uarmv => dxv);
  fybjp : entity work.axobcwtyd
    port map (qjwd => lgiyxojej, gdmr => ovfge, say => jokatb, bvkktkrai => xpeie);
  
  -- Multi-driven assignments
  clitoqz <= (others => '-');
  tgypn <= clitoqz;
  tgypn <= clitoqz;
end hhd;

use std.reflection.all;

entity gqvr is
  port (hurb : inout integer_value_mirror; rwsrxz : inout file_subtype_mirror; tzjvo : buffer time);
end gqvr;

architecture tfeqnskmr of gqvr is
  
begin
  -- Single-driven assignments
  tzjvo <= 3_0_3_3 us;
end tfeqnskmr;



-- Seed after: 17322757740775851077,2158184632809654795
