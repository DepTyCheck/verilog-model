-- Seed: 659537983896413232,8412319452373742525

entity bhrqvalscz is
  port (a : buffer severity_level; vuzxldjrs : in time; y : linkage time; tuapsyy : linkage time);
end bhrqvalscz;

architecture aagceuxxw of bhrqvalscz is
  
begin
  -- Single-driven assignments
  a <= ERROR;
end aagceuxxw;

entity embyw is
  port (uch : out time);
end embyw;

architecture avj of embyw is
  signal ht : time;
  signal ayyyraqir : time;
  signal zmphoinx : severity_level;
  signal caggb : time;
  signal lemzvczeg : time;
  signal kmc : severity_level;
begin
  oomqesagf : entity work.bhrqvalscz
    port map (a => kmc, vuzxldjrs => uch, y => lemzvczeg, tuapsyy => caggb);
  pygixt : entity work.bhrqvalscz
    port map (a => zmphoinx, vuzxldjrs => ayyyraqir, y => uch, tuapsyy => ht);
  
  -- Single-driven assignments
  ayyyraqir <= uch;
end avj;



-- Seed after: 8319501718837264197,8412319452373742525
