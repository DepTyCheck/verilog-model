// Seed: 12693198506606398114,3138436532926670533

module asavmiopp (output wor logic [2:3] ptcvo [4:2], input int gagesawwtd [4:2][1:1][4:2], input trireg logic [3:2] gch);
  and qufpuycfsp(ugo, hbjlmctegq, gvuf);
  
  and le(gvuf, gvwalt, cx);
  
  nand nzqvlzkkw(hbjlmctegq, cx, gch);
  // warning: implicit conversion of port connection truncates from 2 to 1 bits
  //   trireg logic [3:2] gch -> logic gch
  
  
  // Multi-driven assignments
  assign ptcvo = ptcvo;
  assign cx = gvwalt;
  assign cx = 'bz;
  assign gvuf = 'b100;
endmodule: asavmiopp

module dddh (input shortint zbypzard, input shortint rimnek, inout supply1 logic [0:1][1:0] hwlhw [1:4][4:2][2:1], output logic pnxspnprdt [1:0]);
  and ymzjix(j, qzwl, zbypzard);
  // warning: implicit conversion of port connection truncates from 16 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   shortint zbypzard -> logic zbypzard
  
  xnor u(qypcmjtka, mzew, rimnek);
  // warning: implicit conversion of port connection truncates from 16 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   shortint rimnek -> logic rimnek
  
  and ejsnbl(j, yxikymdpq, yxikymdpq);
  
  
  // Single-driven assignments
  assign pnxspnprdt = '{'bx0x,'bz0x};
endmodule: dddh

module q (output longint qedbdowbs);
  // Single-driven assignments
  assign qedbdowbs = qedbdowbs;
endmodule: q



// Seed after: 13030815696105950503,3138436532926670533
