-- Seed: 12833100159237848063,16461708287571398341

entity hc is
  port (n : in real; c : inout bit);
end hc;

architecture ahuygmwtju of hc is
  
begin
  -- Single-driven assignments
  c <= '0';
end ahuygmwtju;

entity pt is
  port (aqbwynmzc : out bit_vector(0 to 2); lpeihbsy : buffer real; hreql : linkage bit_vector(3 downto 0));
end pt;

architecture vwhpxant of pt is
  signal r : bit;
  signal ulvplllsev : real;
  signal vfiovb : bit;
  signal a : real;
begin
  vzpuvqrd : entity work.hc
    port map (n => a, c => vfiovb);
  juwfrv : entity work.hc
    port map (n => ulvplllsev, c => r);
  
  -- Single-driven assignments
  a <= lpeihbsy;
  ulvplllsev <= lpeihbsy;
  aqbwynmzc <= aqbwynmzc;
  lpeihbsy <= lpeihbsy;
end vwhpxant;

entity ok is
  port (ftotebyv : out real; bmfm : linkage string(4 downto 2));
end ok;

architecture ltkiijrcc of ok is
  signal ilvibi : bit_vector(3 downto 0);
  signal nvd : bit_vector(0 to 2);
begin
  otmnjvd : entity work.pt
    port map (aqbwynmzc => nvd, lpeihbsy => ftotebyv, hreql => ilvibi);
end ltkiijrcc;



-- Seed after: 13664737206077078938,16461708287571398341
