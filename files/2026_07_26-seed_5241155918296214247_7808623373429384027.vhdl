-- Seed: 5241155918296214247,7808623373429384027

entity fokjnisk is
  port (yw : in integer; l : out real; yjqi : buffer real);
end fokjnisk;

architecture w of fokjnisk is
  
begin
  
end w;

entity gmv is
  port (nwaqsnt : buffer integer_vector(2 downto 2); a : inout character);
end gmv;

architecture gdfmusidf of gmv is
  signal wqvmrfiaxx : real;
  signal qtzu : real;
  signal jphonfs : real;
  signal csdiettz : real;
  signal knsbjtrhc : real;
  signal yvhmitnzt : real;
  signal ihlozuvbw : integer;
begin
  jbdpprrzjq : entity work.fokjnisk
    port map (yw => ihlozuvbw, l => yvhmitnzt, yjqi => knsbjtrhc);
  qtlujol : entity work.fokjnisk
    port map (yw => ihlozuvbw, l => csdiettz, yjqi => jphonfs);
  xtn : entity work.fokjnisk
    port map (yw => ihlozuvbw, l => qtzu, yjqi => wqvmrfiaxx);
end gdfmusidf;



-- Seed after: 5295630402503733048,7808623373429384027
