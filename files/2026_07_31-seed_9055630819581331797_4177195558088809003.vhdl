-- Seed: 9055630819581331797,4177195558088809003

library ieee;
use ieee.std_logic_1164.all;

entity ytot is
  port (rrfmyi : linkage integer; f : inout std_logic; grjbbynmlv : inout boolean);
end ytot;

architecture qtpebb of ytot is
  
begin
  -- Single-driven assignments
  grjbbynmlv <= FALSE;
  
  -- Multi-driven assignments
  f <= f;
  f <= f;
  f <= 'W';
end qtpebb;

entity fcgalbjb is
  port (trfbnmq : linkage boolean; qlpek : out real);
end fcgalbjb;

library ieee;
use ieee.std_logic_1164.all;

architecture mdw of fcgalbjb is
  signal ztdqikww : boolean;
  signal vuufotgodb : std_logic;
  signal p : integer;
  signal zeuqcevyop : boolean;
  signal ukqmblgmv : integer;
  signal yuztdrf : boolean;
  signal rq : integer;
  signal n : boolean;
  signal hthp : std_logic;
  signal pxgagw : integer;
begin
  umgldco : entity work.ytot
    port map (rrfmyi => pxgagw, f => hthp, grjbbynmlv => n);
  nztkasi : entity work.ytot
    port map (rrfmyi => rq, f => hthp, grjbbynmlv => yuztdrf);
  x : entity work.ytot
    port map (rrfmyi => ukqmblgmv, f => hthp, grjbbynmlv => zeuqcevyop);
  ymrwrhgpru : entity work.ytot
    port map (rrfmyi => p, f => vuufotgodb, grjbbynmlv => ztdqikww);
  
  -- Single-driven assignments
  qlpek <= 40.0_1_4;
  
  -- Multi-driven assignments
  hthp <= '-';
  hthp <= hthp;
  hthp <= '0';
end mdw;

entity tqtjzszz is
  port (htlra : inout time; umjriddhe : linkage time);
end tqtjzszz;

library ieee;
use ieee.std_logic_1164.all;

architecture g of tqtjzszz is
  signal yfxahc : boolean;
  signal o : std_logic;
  signal wjqyd : integer;
  signal so : boolean;
  signal f : std_logic;
  signal yrs : integer;
  signal xmp : boolean;
  signal rbri : std_logic;
  signal xrf : integer;
  signal elvswub : real;
  signal fbmwcfwfba : boolean;
begin
  zyoybar : entity work.fcgalbjb
    port map (trfbnmq => fbmwcfwfba, qlpek => elvswub);
  p : entity work.ytot
    port map (rrfmyi => xrf, f => rbri, grjbbynmlv => xmp);
  ckrl : entity work.ytot
    port map (rrfmyi => yrs, f => f, grjbbynmlv => so);
  h : entity work.ytot
    port map (rrfmyi => wjqyd, f => o, grjbbynmlv => yfxahc);
  
  -- Single-driven assignments
  htlra <= 3 min;
end g;



-- Seed after: 7277851466847889771,4177195558088809003
