-- Seed: 9913927276511449772,13196211255131729027

entity ljmzuw is
  port (itetm : linkage real; mxeouifx : buffer real);
end ljmzuw;

architecture engdzi of ljmzuw is
  
begin
  
end engdzi;

library ieee;
use ieee.std_logic_1164.all;

entity lhfbmurkpf is
  port (yymjh : linkage std_logic; thuwyd : inout std_logic; id : buffer severity_level);
end lhfbmurkpf;

architecture uvhdmvjhd of lhfbmurkpf is
  signal dhoh : real;
  signal ry : real;
  signal isi : real;
  signal uiejrittai : real;
  signal r : real;
  signal wocrq : real;
begin
  notthvnqn : entity work.ljmzuw
    port map (itetm => wocrq, mxeouifx => r);
  n : entity work.ljmzuw
    port map (itetm => uiejrittai, mxeouifx => isi);
  xcnftkcie : entity work.ljmzuw
    port map (itetm => ry, mxeouifx => dhoh);
  
  -- Single-driven assignments
  id <= NOTE;
  
  -- Multi-driven assignments
  thuwyd <= thuwyd;
  thuwyd <= thuwyd;
  thuwyd <= '0';
  thuwyd <= '-';
end uvhdmvjhd;

entity izx is
  port (nbwkyusb : inout real; vvkqhj : linkage bit_vector(3 to 1));
end izx;

architecture lsip of izx is
  
begin
  -- Single-driven assignments
  nbwkyusb <= 16#1_3_9.811#;
end lsip;

library ieee;
use ieee.std_logic_1164.all;

entity bgtj is
  port (lzykttbc : out time_vector(2 downto 0); hkimvud : in std_logic_vector(0 downto 0); vhhky : buffer time);
end bgtj;

architecture fyggrrinpb of bgtj is
  signal pb : real;
  signal xfvtq : real;
begin
  zckkkkxzo : entity work.ljmzuw
    port map (itetm => xfvtq, mxeouifx => pb);
  
  -- Single-driven assignments
  vhhky <= 1 hr;
end fyggrrinpb;



-- Seed after: 4992216118473863583,13196211255131729027
