-- Seed: 4027306977752188503,12359743974512393525

entity iv is
  port (wcqsq : linkage time; w : buffer real_vector(1 to 0); qqjyre : inout real);
end iv;

architecture kpql of iv is
  
begin
  -- Single-driven assignments
  qqjyre <= 0_4.2_0_4;
  w <= (others => 0.0);
end kpql;

library ieee;
use ieee.std_logic_1164.all;

entity q is
  port (phu : buffer std_logic; fpxb : out time);
end q;

architecture vgjynayejs of q is
  signal eqdhv : real;
  signal ycirqmvmzz : real_vector(1 to 0);
  signal ihebk : real;
  signal moq : real_vector(1 to 0);
  signal tmekodkbjx : time;
  signal kljuj : real;
  signal skt : real_vector(1 to 0);
  signal rylztj : time;
  signal vtt : real;
  signal irjjqujng : real_vector(1 to 0);
  signal yxa : time;
begin
  xyxdpztgi : entity work.iv
    port map (wcqsq => yxa, w => irjjqujng, qqjyre => vtt);
  hli : entity work.iv
    port map (wcqsq => rylztj, w => skt, qqjyre => kljuj);
  hxzq : entity work.iv
    port map (wcqsq => tmekodkbjx, w => moq, qqjyre => ihebk);
  b : entity work.iv
    port map (wcqsq => fpxb, w => ycirqmvmzz, qqjyre => eqdhv);
end vgjynayejs;

library ieee;
use ieee.std_logic_1164.all;

entity qsmemodaty is
  port (g : buffer std_logic_vector(0 downto 2); v : out time_vector(2 downto 3));
end qsmemodaty;

library ieee;
use ieee.std_logic_1164.all;

architecture pklv of qsmemodaty is
  signal l : real;
  signal lsozxka : real_vector(1 to 0);
  signal yfrrkmjvo : time;
  signal sa : real;
  signal zedpleuv : real_vector(1 to 0);
  signal zppp : time;
  signal bmq : real;
  signal cuf : real_vector(1 to 0);
  signal iko : time;
  signal jjzot : time;
  signal sap : std_logic;
begin
  msn : entity work.q
    port map (phu => sap, fpxb => jjzot);
  fbztneo : entity work.iv
    port map (wcqsq => iko, w => cuf, qqjyre => bmq);
  owfgzftwtz : entity work.iv
    port map (wcqsq => zppp, w => zedpleuv, qqjyre => sa);
  suqvhm : entity work.iv
    port map (wcqsq => yfrrkmjvo, w => lsozxka, qqjyre => l);
  
  -- Multi-driven assignments
  g <= g;
  g <= g;
  g <= g;
end pklv;



-- Seed after: 17567902125867550624,12359743974512393525
