-- Seed: 15201020073653007943,9416221572817842275

library ieee;
use ieee.std_logic_1164.all;

entity sjnlmphwo is
  port (ek : out real; zrennlla : inout time; hatwztw : buffer std_logic);
end sjnlmphwo;



architecture yg of sjnlmphwo is
  
begin
  
end yg;

library ieee;
use ieee.std_logic_1164.all;

entity wustqd is
  port (bc : linkage time; bqtkylmzr : linkage std_logic; yqtnni : out severity_level);
end wustqd;

library ieee;
use ieee.std_logic_1164.all;

architecture rfqam of wustqd is
  signal vzpliqs : time;
  signal lvgorzke : real;
  signal oibodtls : std_logic;
  signal fliijhzso : time;
  signal smos : real;
  signal kukg : std_logic;
  signal plvmrskt : time;
  signal pkgnug : real;
  signal e : std_logic;
  signal iiwiinv : time;
  signal mxeaph : real;
begin
  q : entity work.sjnlmphwo
    port map (ek => mxeaph, zrennlla => iiwiinv, hatwztw => e);
  tgl : entity work.sjnlmphwo
    port map (ek => pkgnug, zrennlla => plvmrskt, hatwztw => kukg);
  f : entity work.sjnlmphwo
    port map (ek => smos, zrennlla => fliijhzso, hatwztw => oibodtls);
  ryqwiraxp : entity work.sjnlmphwo
    port map (ek => lvgorzke, zrennlla => vzpliqs, hatwztw => e);
end rfqam;



entity zurekhvu is
  port (jo : buffer real);
end zurekhvu;

library ieee;
use ieee.std_logic_1164.all;

architecture w of zurekhvu is
  signal pripeqc : time;
  signal lgpz : std_logic;
  signal f : time;
  signal q : real;
  signal owizbomlun : time;
  signal bxq : real;
  signal hbhoa : std_logic;
  signal aknp : time;
  signal gupa : real;
begin
  mvlokenneg : entity work.sjnlmphwo
    port map (ek => gupa, zrennlla => aknp, hatwztw => hbhoa);
  m : entity work.sjnlmphwo
    port map (ek => bxq, zrennlla => owizbomlun, hatwztw => hbhoa);
  yykpmdgbov : entity work.sjnlmphwo
    port map (ek => q, zrennlla => f, hatwztw => lgpz);
  kkqjvkb : entity work.sjnlmphwo
    port map (ek => jo, zrennlla => pripeqc, hatwztw => hbhoa);
end w;

library ieee;
use ieee.std_logic_1164.all;

entity yqueqce is
  port (jednznc : buffer time; yuunu : in integer; uceqhhsp : buffer std_logic);
end yqueqce;



architecture txjfp of yqueqce is
  signal wgyv : real;
begin
  pogxg : entity work.sjnlmphwo
    port map (ek => wgyv, zrennlla => jednznc, hatwztw => uceqhhsp);
end txjfp;



-- Seed after: 11772001498768801681,9416221572817842275
