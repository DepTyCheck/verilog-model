-- Seed: 112410310526429957,12359743974512393525

entity ve is
  port (vgjgu : out time; crmfsktb : out integer; xm : inout integer_vector(3 to 0));
end ve;

architecture fee of ve is
  
begin
  
end fee;

entity ucudewy is
  port (wuzyhq : in time_vector(0 to 2); fbffaog : buffer boolean; bhlfqoxz : buffer integer; snqlkxxuj : out real);
end ucudewy;

architecture dwwjvanr of ucudewy is
  signal krhkofdgri : integer_vector(3 to 0);
  signal j : time;
  signal gl : integer_vector(3 to 0);
  signal wlnigt : integer;
  signal ml : time;
  signal jxud : integer_vector(3 to 0);
  signal mjmhlfiv : integer;
  signal unqw : time;
begin
  uclevqu : entity work.ve
    port map (vgjgu => unqw, crmfsktb => mjmhlfiv, xm => jxud);
  kpzwjhho : entity work.ve
    port map (vgjgu => ml, crmfsktb => wlnigt, xm => gl);
  g : entity work.ve
    port map (vgjgu => j, crmfsktb => bhlfqoxz, xm => krhkofdgri);
  
  -- Single-driven assignments
  snqlkxxuj <= snqlkxxuj;
  fbffaog <= FALSE;
end dwwjvanr;

library ieee;
use ieee.std_logic_1164.all;

entity ir is
  port (qb : buffer std_logic_vector(4 to 0); avrkjjvi : out bit_vector(2 to 4); kn : out std_logic; cmaaezwy : linkage integer);
end ir;

architecture iqaeaaj of ir is
  signal wclzr : integer_vector(3 to 0);
  signal wltofpq : integer;
  signal due : time;
begin
  nbx : entity work.ve
    port map (vgjgu => due, crmfsktb => wltofpq, xm => wclzr);
  
  -- Single-driven assignments
  avrkjjvi <= ('0', '0', '1');
  
  -- Multi-driven assignments
  kn <= '1';
  kn <= '1';
  kn <= kn;
end iqaeaaj;



-- Seed after: 877166611387598232,12359743974512393525
