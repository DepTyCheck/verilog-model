-- Seed: 7460146705654035875,5415160250146859793

library ieee;
use ieee.std_logic_1164.all;

entity f is
  port (umrkuurti : out integer; qt : out std_logic; fcssguf : out time; phukowz : out std_logic);
end f;



architecture ly of f is
  
begin
  
end ly;



entity qoxjuop is
  port (fngio : out integer_vector(3 to 1));
end qoxjuop;

library ieee;
use ieee.std_logic_1164.all;

architecture dcjtfqzmk of qoxjuop is
  signal dboxdomb : std_logic;
  signal ibctgw : time;
  signal cbgsmxeqdd : std_logic;
  signal fbkua : integer;
begin
  zgrdnfvzre : entity work.f
    port map (umrkuurti => fbkua, qt => cbgsmxeqdd, fcssguf => ibctgw, phukowz => dboxdomb);
end dcjtfqzmk;



entity hunkyhafca is
  port (uayufh : buffer time; dbkpvqdu : in severity_level; r : buffer time_vector(2 to 3); st : in integer_vector(0 downto 4));
end hunkyhafca;



architecture hclmiwesw of hunkyhafca is
  
begin
  
end hclmiwesw;



entity olptksw is
  port (luuppvsqu : buffer time_vector(0 to 0));
end olptksw;

library ieee;
use ieee.std_logic_1164.all;

architecture fud of olptksw is
  signal ftdsvl : time_vector(2 to 3);
  signal aakfhuop : severity_level;
  signal dxxasm : time;
  signal okhvm : std_logic;
  signal mvdxn : time;
  signal igoeve : std_logic;
  signal gl : integer;
  signal xk : integer_vector(0 downto 4);
  signal j : time_vector(2 to 3);
  signal utwqsnonai : severity_level;
  signal gv : time;
begin
  gzexkaezta : entity work.hunkyhafca
    port map (uayufh => gv, dbkpvqdu => utwqsnonai, r => j, st => xk);
  nweozod : entity work.f
    port map (umrkuurti => gl, qt => igoeve, fcssguf => mvdxn, phukowz => okhvm);
  hixjffzboa : entity work.hunkyhafca
    port map (uayufh => dxxasm, dbkpvqdu => aakfhuop, r => ftdsvl, st => xk);
end fud;



-- Seed after: 1962184592082654239,5415160250146859793
