-- Seed: 5245956426916000744,18238119570016518405

library ieee;
use ieee.std_logic_1164.all;

entity fcuybcm is
  port (tfybevgupb : inout integer_vector(4 to 2); avbkun : buffer boolean; nmj : buffer std_logic_vector(0 to 0));
end fcuybcm;



architecture n of fcuybcm is
  
begin
  
end n;

library ieee;
use ieee.std_logic_1164.all;

entity nvs is
  port (ktbgooexyj : out severity_level; bmnesftiyu : buffer std_logic; hhdcrhhxb : out bit; lvigzerigs : inout time);
end nvs;

library ieee;
use ieee.std_logic_1164.all;

architecture jeoycwcg of nvs is
  signal fgeswg : boolean;
  signal pk : integer_vector(4 to 2);
  signal kxox : std_logic_vector(0 to 0);
  signal f : boolean;
  signal eizanoi : integer_vector(4 to 2);
begin
  k : entity work.fcuybcm
    port map (tfybevgupb => eizanoi, avbkun => f, nmj => kxox);
  lt : entity work.fcuybcm
    port map (tfybevgupb => pk, avbkun => fgeswg, nmj => kxox);
end jeoycwcg;



-- Seed after: 17969993544562399063,18238119570016518405
