-- Seed: 8514858580322703742,6329330932550885447



entity r is
  port (w : buffer real_vector(4 downto 4); dwgr : buffer boolean; p : in time);
end r;



architecture mxtmlkksmc of r is
  
begin
  
end mxtmlkksmc;



entity vyi is
  port (xcw : linkage real);
end vyi;



architecture w of vyi is
  signal mymirx : time;
  signal xm : boolean;
  signal ofxvhorg : real_vector(4 downto 4);
begin
  vqqnvyxu : entity work.r
    port map (w => ofxvhorg, dwgr => xm, p => mymirx);
end w;

library ieee;
use ieee.std_logic_1164.all;

entity ozsuirs is
  port (cbnwati : buffer real; pqki : out std_logic; uhfombah : buffer real; fdggq : buffer integer);
end ozsuirs;



architecture vrigualu of ozsuirs is
  
begin
  cage : entity work.vyi
    port map (xcw => uhfombah);
end vrigualu;



entity hagubx is
  port (q : linkage severity_level; nyqociv : buffer integer; wyl : linkage time_vector(4 downto 0); rdnstht : buffer bit);
end hagubx;

library ieee;
use ieee.std_logic_1164.all;

architecture hguizug of hagubx is
  signal w : boolean;
  signal kh : real_vector(4 downto 4);
  signal qcrt : time;
  signal m : boolean;
  signal lgrwvd : real_vector(4 downto 4);
  signal dwudgncy : real;
  signal ykqq : std_logic;
  signal l : real;
begin
  dwrzbf : entity work.ozsuirs
    port map (cbnwati => l, pqki => ykqq, uhfombah => dwudgncy, fdggq => nyqociv);
  pxnsm : entity work.r
    port map (w => lgrwvd, dwgr => m, p => qcrt);
  kbfao : entity work.r
    port map (w => kh, dwgr => w, p => qcrt);
end hguizug;



-- Seed after: 223686464032243702,6329330932550885447
