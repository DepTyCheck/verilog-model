-- Seed: 11283538158572104567,8412319452373742525

entity cca is
  port (kyzdf : inout real);
end cca;

architecture kvfmoat of cca is
  
begin
  -- Single-driven assignments
  kyzdf <= 3041.2421;
end kvfmoat;

entity kklec is
  port (zbnskxo : out integer);
end kklec;

architecture dypz of kklec is
  signal syb : real;
  signal uwed : real;
  signal sl : real;
  signal ua : real;
begin
  ess : entity work.cca
    port map (kyzdf => ua);
  rxg : entity work.cca
    port map (kyzdf => sl);
  kdsseyyozw : entity work.cca
    port map (kyzdf => uwed);
  ji : entity work.cca
    port map (kyzdf => syb);
  
  -- Single-driven assignments
  zbnskxo <= zbnskxo;
end dypz;

entity s is
  port (mimkd : linkage time);
end s;

architecture r of s is
  signal y : integer;
  signal bbats : real;
  signal yijomlejvi : real;
  signal vphhptuagh : real;
begin
  hvqqb : entity work.cca
    port map (kyzdf => vphhptuagh);
  iq : entity work.cca
    port map (kyzdf => yijomlejvi);
  kzklev : entity work.cca
    port map (kyzdf => bbats);
  xhipkcez : entity work.kklec
    port map (zbnskxo => y);
end r;



-- Seed after: 17246728699867768524,8412319452373742525
