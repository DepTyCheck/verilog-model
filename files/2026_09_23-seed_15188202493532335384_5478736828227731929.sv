// Seed: 15188202493532335384,5478736828227731929

module cqcajgmbe ();
  nand yhjwks(p, p, p);
  
  and q(yjht, yjht, p);
  
  and xguzkjblr(gvstrbjl, ocjxcjefea, p);
  
  
  // Multi-driven assignments
  assign ocjxcjefea = 'b0;
  assign yjht = p;
  assign p = ocjxcjefea;
endmodule: cqcajgmbe

module dlrkykjfdt (input logic mizzw, output realtime at);
  nand mmilwxkzpd(at, pi, unpxds);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   realtime at -> logic at
  
  xnor o(rszjet, at, pi);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   realtime at -> logic at
  
  nand qtrqof(pi, unpxds, elbmmilvgv);
  
  and f(ymvniv, pi, lety);
  
  
  // Multi-driven assignments
  assign rszjet = mizzw;
  assign pi = lety;
  assign pi = 'b1;
endmodule: dlrkykjfdt



// Seed after: 18232209269656442806,5478736828227731929
