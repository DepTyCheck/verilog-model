// Seed: 4687889563777998909,10774379589804174675

module zg (output bit [4:0] oknn, inout tri logic sz [2:1][2:3][3:3][2:1]);
  
  xnor mzo(sdmyho, uvanw, oknn);
  // warning: implicit conversion of port connection truncates from 5 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [4:0] oknn -> logic oknn
  
  xnor edjzffcb(oruzp, sdmyho, oruzp);
  
  xnor t(sdmyho, mq, oruzp);
  
  and ssknssohpr(v, mq, sdmyho);
  
  
  // Single-driven assigns
  assign oknn = '{'b1001,'b11011,'b10,'b00001,'b01001};
  
  // Multi-driven assigns
  assign uvanw = 'bx0110;
  assign sz = sz;
endmodule: zg

module pejismoqu (inout supply0 logic [3:0] abumzuams [1:4][1:3], input integer fk, output reg [1:3] d);
  // Unpacked net declarations
  tri logic glhlpwrh [2:1][2:3][3:3][2:1];
  
  not edxxkb(hfzfpwton, fk);
  // warning: implicit conversion of port connection truncates from 32 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   integer fk -> logic fk
  
  zg bamrn(.oknn(d), .sz(glhlpwrh));
  // warning: implicit conversion of port connection expands from 3 to 5 bits
  // warning: implicit conversion changes possible bit states from 4-state to 2-state
  //   reg [1:3] d -> bit [4:0] oknn
  
  xnor lqljft(nx, nx, nx);
  
  
  // Single-driven assigns
  
  // Multi-driven assigns
  assign hfzfpwton = 'bz;
  assign glhlpwrh = glhlpwrh;
  assign nx = hfzfpwton;
  assign abumzuams = abumzuams;
endmodule: pejismoqu

module yhjtrd (inout supply0 logic [1:2][4:1][2:4] rx [4:0]);
  // Unpacked net declarations
  tri logic ud [2:1][2:3][3:3][2:1];
  
  and xl(vmdvlmgxg, cjrzrcv, vmdvlmgxg);
  
  zg z(.oknn(lfbyphl), .sz(ud));
  // warning: implicit conversion of port connection expands from 1 to 5 bits
  // warning: implicit conversion changes possible bit states from 4-state to 2-state
  //   wire logic lfbyphl -> bit [4:0] oknn
  
  xor a(ywtfh, vmdvlmgxg, vjcyokb);
  
  xnor ir(ywtfh, qlbty, yeqttppch);
  
  
  // Single-driven assigns
  
  // Multi-driven assigns
  assign rx = rx;
endmodule: yhjtrd

module pykajgbqn (input tri1 logic [4:4][2:4][4:0] i [1:2][0:3], output reg [1:1][2:0] vrfazz [0:2]);
  
  xnor gzvcnntsbj(vipc, vipc, vipc);
  
  xnor x(rhelltm, aiqsup, vomedbs);
  
  
  // Single-driven assigns
  assign vrfazz = vrfazz;
  
  // Multi-driven assigns
  assign i = i;
  assign vipc = vomedbs;
  assign vomedbs = 'b01zx1;
endmodule: pykajgbqn



// Seed after: 3475305114461626985,10774379589804174675
