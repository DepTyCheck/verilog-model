-- Seed: 8037130424612488531,1852660963590380551

library ieee;
use ieee.std_logic_1164.all;

entity xfwt is
  port (vdsyi : in std_logic_vector(4 to 0); braxznaml : out integer; i : linkage std_logic_vector(4 to 0); m : linkage std_logic_vector(2 downto 0));
end xfwt;



architecture frf of xfwt is
  
begin
  
end frf;

library ieee;
use ieee.std_logic_1164.all;

entity pm is
  port (kgudh : inout std_logic; vrr : inout std_logic_vector(0 downto 1));
end pm;

library ieee;
use ieee.std_logic_1164.all;

architecture ldrdbz of pm is
  signal b : integer;
  signal dmguhx : std_logic_vector(2 downto 0);
  signal qthuegrj : std_logic_vector(4 to 0);
  signal favfc : integer;
begin
  hts : entity work.xfwt
    port map (vdsyi => vrr, braxznaml => favfc, i => qthuegrj, m => dmguhx);
  dwcasatto : entity work.xfwt
    port map (vdsyi => vrr, braxznaml => b, i => vrr, m => dmguhx);
end ldrdbz;



-- Seed after: 3488094569284663420,1852660963590380551
