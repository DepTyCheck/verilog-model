-- Seed: 9857900626682987799,12011142928354116943

entity rpd is
  port (vmxxiwtoq : out bit_vector(1 to 0));
end rpd;

architecture um of rpd is
  
begin
  
end um;

entity dtmrafgv is
  port (ytbtcxrzk : in real; kpebqvaae : in string(2 downto 4));
end dtmrafgv;

architecture lfejr of dtmrafgv is
  signal yo : bit_vector(1 to 0);
  signal wr : bit_vector(1 to 0);
  signal yxtslhr : bit_vector(1 to 0);
begin
  kmt : entity work.rpd
    port map (vmxxiwtoq => yxtslhr);
  lpokvczbt : entity work.rpd
    port map (vmxxiwtoq => wr);
  xb : entity work.rpd
    port map (vmxxiwtoq => yo);
end lfejr;



-- Seed after: 1196137049403966635,12011142928354116943
