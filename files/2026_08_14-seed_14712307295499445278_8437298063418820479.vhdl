-- Seed: 14712307295499445278,8437298063418820479

entity o is
  port (pzxiosxz : out bit_vector(2 downto 3); ao : inout integer; rbp : out integer; rosbwbczng : in string(3 downto 4));
end o;

architecture zb of o is
  
begin
  -- Single-driven assignments
  ao <= 1;
  rbp <= rbp;
  pzxiosxz <= (others => '0');
end zb;

entity dktvarkssb is
  port (egyhvt : out time; htiqvzaq : linkage real; n : out time; yoxp : inout real);
end dktvarkssb;

architecture fbwea of dktvarkssb is
  signal ahcsw : string(3 downto 4);
  signal k : integer;
  signal muc : integer;
  signal eeylvm : bit_vector(2 downto 3);
  signal kddpwhmv : integer;
  signal de : integer;
  signal bkrxzoytx : bit_vector(2 downto 3);
  signal l : string(3 downto 4);
  signal z : integer;
  signal uxmdmh : integer;
  signal guoco : bit_vector(2 downto 3);
begin
  jsshqw : entity work.o
    port map (pzxiosxz => guoco, ao => uxmdmh, rbp => z, rosbwbczng => l);
  xakyegmo : entity work.o
    port map (pzxiosxz => bkrxzoytx, ao => de, rbp => kddpwhmv, rosbwbczng => l);
  aznjbrgyq : entity work.o
    port map (pzxiosxz => eeylvm, ao => muc, rbp => k, rosbwbczng => ahcsw);
  
  -- Single-driven assignments
  egyhvt <= 2_3.3_4 ps;
  n <= 8#6_4_1# fs;
end fbwea;

entity ukyo is
  port (vc : linkage integer; wulgoc : buffer real; l : out real; hqr : in time);
end ukyo;

architecture xlnd of ukyo is
  signal cagvvdly : time;
  signal idctwuudhz : time;
  signal pf : string(3 downto 4);
  signal fckm : integer;
  signal d : integer;
  signal i : bit_vector(2 downto 3);
begin
  lqcecsb : entity work.o
    port map (pzxiosxz => i, ao => d, rbp => fckm, rosbwbczng => pf);
  eaxzm : entity work.dktvarkssb
    port map (egyhvt => idctwuudhz, htiqvzaq => l, n => cagvvdly, yoxp => wulgoc);
  
  -- Single-driven assignments
  pf <= (others => ' ');
end xlnd;

library ieee;
use ieee.std_logic_1164.all;

entity tdqeiyol is
  port (ngwy : inout real; r : in string(3 downto 5); yvpfudsvq : in std_logic_vector(4 downto 1));
end tdqeiyol;

architecture dtvny of tdqeiyol is
  signal qklcx : time;
  signal lqirujg : real;
  signal nthohjn : time;
begin
  bucelqlg : entity work.dktvarkssb
    port map (egyhvt => nthohjn, htiqvzaq => lqirujg, n => qklcx, yoxp => ngwy);
end dtvny;



-- Seed after: 413879947936707537,8437298063418820479
