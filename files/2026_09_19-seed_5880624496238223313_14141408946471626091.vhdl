-- Seed: 5880624496238223313,14141408946471626091

entity pspuauo is
  port (imn : buffer time; ik : inout integer; bxelt : buffer severity_level; hlzvzfja : linkage real);
end pspuauo;

architecture bka of pspuauo is
  
begin
  -- Single-driven assignments
  imn <= imn;
  ik <= 8#7_4_3_6_2#;
  bxelt <= FAILURE;
end bka;

entity qlman is
  port (twodijcf : out string(1 to 3); zkvbziffx : out integer);
end qlman;

architecture ewaiyfdaq of qlman is
  
begin
  -- Single-driven assignments
  twodijcf <= ('i', 't', 'n');
  zkvbziffx <= 16#E_4_9#;
end ewaiyfdaq;

entity kourdj is
  port (k : buffer real);
end kourdj;

architecture sevmyjxz of kourdj is
  signal q : real;
  signal xt : severity_level;
  signal iwdwnmukaz : integer;
  signal kjz : time;
  signal kxnc : real;
  signal korblb : severity_level;
  signal kxjis : integer;
  signal txmctsfi : time;
  signal fn : real;
  signal nkl : severity_level;
  signal pjktio : integer;
  signal zwqrfh : time;
begin
  jhe : entity work.pspuauo
    port map (imn => zwqrfh, ik => pjktio, bxelt => nkl, hlzvzfja => fn);
  mrnoyqs : entity work.pspuauo
    port map (imn => txmctsfi, ik => kxjis, bxelt => korblb, hlzvzfja => kxnc);
  uim : entity work.pspuauo
    port map (imn => kjz, ik => iwdwnmukaz, bxelt => xt, hlzvzfja => q);
end sevmyjxz;

entity vpaaylqla is
  port (ahttheum : inout string(2 to 5); amfas : in time; fvuv : linkage real; aukajloq : inout boolean_vector(1 downto 3));
end vpaaylqla;

architecture frxf of vpaaylqla is
  
begin
  -- Single-driven assignments
  aukajloq <= aukajloq;
  ahttheum <= ahttheum;
end frxf;



-- Seed after: 3364264401362411044,14141408946471626091
