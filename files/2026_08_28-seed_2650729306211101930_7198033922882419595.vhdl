-- Seed: 2650729306211101930,7198033922882419595

entity zh is
  port (aagp : out real; tigwxg : linkage real);
end zh;

architecture ipe of zh is
  
begin
  -- Single-driven assignments
  aagp <= aagp;
end ipe;

entity afixld is
  port (mpxedu : in integer; jskdcnet : inout real; edb : inout integer; doxpd : out time);
end afixld;

architecture f of afixld is
  signal hgsk : real;
begin
  uz : entity work.zh
    port map (aagp => hgsk, tigwxg => jskdcnet);
  
  -- Single-driven assignments
  doxpd <= doxpd;
  edb <= edb;
end f;

entity qlvwmp is
  port (xthoivd : buffer time_vector(1 to 3); ecmxsdciys : inout time);
end qlvwmp;

architecture ofjrgneo of qlvwmp is
  signal p : real;
  signal bvc : real;
  signal n : real;
  signal htyt : integer;
  signal taa : real;
  signal g : real;
  signal vwp : time;
  signal s : integer;
  signal bkr : real;
  signal rayp : integer;
begin
  otagof : entity work.afixld
    port map (mpxedu => rayp, jskdcnet => bkr, edb => s, doxpd => vwp);
  jwha : entity work.zh
    port map (aagp => g, tigwxg => taa);
  audrk : entity work.afixld
    port map (mpxedu => htyt, jskdcnet => n, edb => htyt, doxpd => ecmxsdciys);
  uhic : entity work.zh
    port map (aagp => bvc, tigwxg => p);
  
  -- Single-driven assignments
  xthoivd <= (16#4A0B5.B3C# ps, 4_3_1_3_0.2 ps, 1 hr);
  rayp <= rayp;
end ofjrgneo;



-- Seed after: 13330792414856107695,7198033922882419595
