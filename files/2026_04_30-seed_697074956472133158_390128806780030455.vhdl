-- Seed: 697074956472133158,390128806780030455



entity vkpilke is
  port (ri : in time; u : out time; ayzu : inout severity_level);
end vkpilke;



architecture tmtcyigw of vkpilke is
  
begin
  
end tmtcyigw;



entity ofveczedj is
  port (okc : linkage integer; yrwwtd : buffer integer);
end ofveczedj;



architecture yb of ofveczedj is
  signal tqtmh : severity_level;
  signal dshc : time;
  signal vpvrqqzt : severity_level;
  signal cuvqyzv : time;
  signal vo : severity_level;
  signal ttws : time;
  signal lqzzecavji : severity_level;
  signal uwtpngu : time;
  signal dglfk : time;
begin
  zeuyevm : entity work.vkpilke
    port map (ri => dglfk, u => uwtpngu, ayzu => lqzzecavji);
  rqlasgwh : entity work.vkpilke
    port map (ri => ttws, u => dglfk, ayzu => vo);
  soojb : entity work.vkpilke
    port map (ri => dglfk, u => cuvqyzv, ayzu => vpvrqqzt);
  h : entity work.vkpilke
    port map (ri => dshc, u => ttws, ayzu => tqtmh);
end yb;

library ieee;
use ieee.std_logic_1164.all;

entity tcavhryh is
  port (ukbv : in bit; u : inout real; nqifin : linkage std_logic);
end tcavhryh;



architecture giufbhb of tcavhryh is
  signal jt : severity_level;
  signal jcczxgwm : time;
  signal bztwhl : severity_level;
  signal tkeynwkk : time;
  signal a : integer;
begin
  yufbnxl : entity work.ofveczedj
    port map (okc => a, yrwwtd => a);
  d : entity work.vkpilke
    port map (ri => tkeynwkk, u => tkeynwkk, ayzu => bztwhl);
  uxlqfzctdw : entity work.vkpilke
    port map (ri => jcczxgwm, u => jcczxgwm, ayzu => jt);
end giufbhb;



-- Seed after: 7601117204657607453,390128806780030455
