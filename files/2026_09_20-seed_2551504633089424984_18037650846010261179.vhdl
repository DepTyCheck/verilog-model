-- Seed: 2551504633089424984,18037650846010261179

entity pts is
  port (d : in string(3 downto 1); uwyzlwtbs : buffer real);
end pts;

architecture ostaem of pts is
  
begin
  -- Single-driven assignments
  uwyzlwtbs <= 16#ACC5.3_7#;
end ostaem;

library ieee;
use ieee.std_logic_1164.all;

entity tkukcr is
  port (qreife : in std_logic; fkhtkhhj : out real; ohzj : out string(4 to 1); binqj : in real);
end tkukcr;

architecture ehtxgl of tkukcr is
  signal brfqsyriof : real;
  signal oafnyc : string(3 downto 1);
  signal zvamx : real;
  signal gpbeivvbu : string(3 downto 1);
begin
  junnz : entity work.pts
    port map (d => gpbeivvbu, uwyzlwtbs => zvamx);
  t : entity work.pts
    port map (d => oafnyc, uwyzlwtbs => brfqsyriof);
  
  -- Single-driven assignments
  ohzj <= ohzj;
  gpbeivvbu <= ('y', 'f', 'r');
end ehtxgl;



-- Seed after: 5017731527856446232,18037650846010261179
