-- Seed: 56417944824590863,2158184632809654795

use std.reflection.all;

entity yr is
  port (qj : inout time; yemjbnnbv : inout protected_subtype_mirror);
end yr;

architecture yc of yr is
  
begin
  -- Single-driven assignments
  qj <= 1 hr;
end yc;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity ayxyyup is
  port (jm : inout value_mirror; osaqut : in real; tzabefa : linkage std_logic);
end ayxyyup;

architecture g of ayxyyup is
  
begin
  
end g;

use std.reflection.all;

entity easfleos is
  port (xm : inout subtype_mirror; gx : inout floating_value_mirror);
end easfleos;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture fn of easfleos is
  shared variable htlgc : protected_subtype_mirror;
  signal o : time;
  signal ajtotsrbks : real;
  shared variable gdusrjq : value_mirror;
  signal ojiie : std_logic;
  signal sjld : real;
  shared variable swstcax : value_mirror;
  shared variable nqpgznd : protected_subtype_mirror;
  signal atsfzkcky : time;
begin
  epqnbehydy : entity work.yr
    port map (qj => atsfzkcky, yemjbnnbv => nqpgznd);
  uukk : entity work.ayxyyup
    port map (jm => swstcax, osaqut => sjld, tzabefa => ojiie);
  tnzffuopn : entity work.ayxyyup
    port map (jm => gdusrjq, osaqut => ajtotsrbks, tzabefa => ojiie);
  xpvvgarl : entity work.yr
    port map (qj => o, yemjbnnbv => htlgc);
  
  -- Multi-driven assignments
  ojiie <= 'U';
  ojiie <= ojiie;
  ojiie <= 'W';
end fn;

use std.reflection.all;

entity vx is
  port (jgps : inout record_value_mirror; nxfxnjvv : out integer; fzha : inout record_subtype_mirror; qnkzlnheu : in time_vector(2 to 3));
end vx;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture h of vx is
  shared variable xrowwp : floating_value_mirror;
  shared variable rsgqbsx : subtype_mirror;
  signal ilgnfuio : std_logic;
  signal wj : real;
  shared variable tvvynozdwq : value_mirror;
  shared variable dzabckvixu : protected_subtype_mirror;
  signal dgbqpuhv : time;
begin
  dostthaio : entity work.yr
    port map (qj => dgbqpuhv, yemjbnnbv => dzabckvixu);
  twgsoexp : entity work.ayxyyup
    port map (jm => tvvynozdwq, osaqut => wj, tzabefa => ilgnfuio);
  dvyonu : entity work.easfleos
    port map (xm => rsgqbsx, gx => xrowwp);
  
  -- Single-driven assignments
  nxfxnjvv <= nxfxnjvv;
  wj <= wj;
end h;



-- Seed after: 5141738438080969637,2158184632809654795
