-- Seed: 11576074860263273749,8068158652091157513

entity esx is
  port (frf : linkage real_vector(1 downto 2); h : linkage boolean_vector(4 downto 4); bfffatrof : in real; eardj : linkage real);
end esx;

architecture ypdgrdbf of esx is
  
begin
  
end ypdgrdbf;

library ieee;
use ieee.std_logic_1164.all;

entity zfcox is
  port (jvscbpf : inout std_logic_vector(0 to 2); iuibh : in real_vector(0 to 2); wcnm : in integer);
end zfcox;

architecture weukhak of zfcox is
  signal tytgkjtv : real;
  signal ytphpowizl : boolean_vector(4 downto 4);
  signal totepmue : real_vector(1 downto 2);
  signal uzgyp : real;
  signal r : real;
  signal rv : boolean_vector(4 downto 4);
  signal wkjxrkjjoh : real_vector(1 downto 2);
begin
  ojpcvve : entity work.esx
    port map (frf => wkjxrkjjoh, h => rv, bfffatrof => r, eardj => uzgyp);
  vu : entity work.esx
    port map (frf => totepmue, h => ytphpowizl, bfffatrof => tytgkjtv, eardj => tytgkjtv);
  
  -- Multi-driven assignments
  jvscbpf <= jvscbpf;
  jvscbpf <= "LHW";
  jvscbpf <= "L-0";
  jvscbpf <= ('U', 'W', 'L');
end weukhak;

library ieee;
use ieee.std_logic_1164.all;

entity exrm is
  port (wnbm : out integer; zoefabzek : out std_logic);
end exrm;

architecture lkmtea of exrm is
  
begin
  -- Single-driven assignments
  wnbm <= 4;
  
  -- Multi-driven assignments
  zoefabzek <= zoefabzek;
  zoefabzek <= zoefabzek;
  zoefabzek <= '-';
end lkmtea;

entity rsakm is
  port (ik : inout integer);
end rsakm;

library ieee;
use ieee.std_logic_1164.all;

architecture kerjqe of rsakm is
  signal ainw : real;
  signal ojux : real;
  signal mmjxwnzlz : boolean_vector(4 downto 4);
  signal rmwur : real_vector(1 downto 2);
  signal zsbmrw : real;
  signal pgxaawk : boolean_vector(4 downto 4);
  signal bbn : real_vector(1 downto 2);
  signal axxxiamo : std_logic;
  signal qzm : real;
  signal orsbed : boolean_vector(4 downto 4);
  signal cmi : real_vector(1 downto 2);
begin
  f : entity work.esx
    port map (frf => cmi, h => orsbed, bfffatrof => qzm, eardj => qzm);
  ivwyao : entity work.exrm
    port map (wnbm => ik, zoefabzek => axxxiamo);
  cpm : entity work.esx
    port map (frf => bbn, h => pgxaawk, bfffatrof => qzm, eardj => zsbmrw);
  idxsglfob : entity work.esx
    port map (frf => rmwur, h => mmjxwnzlz, bfffatrof => ojux, eardj => ainw);
  
  -- Single-driven assignments
  ojux <= qzm;
  
  -- Multi-driven assignments
  axxxiamo <= '-';
  axxxiamo <= 'L';
  axxxiamo <= axxxiamo;
end kerjqe;



-- Seed after: 10294526115987058451,8068158652091157513
