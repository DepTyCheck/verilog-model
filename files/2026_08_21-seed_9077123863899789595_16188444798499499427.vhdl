-- Seed: 9077123863899789595,16188444798499499427

entity lmvrdofkbp is
  port (zunvtzmtj : linkage real_vector(0 to 2); d : inout time_vector(1 downto 3); hf : in time_vector(1 downto 2));
end lmvrdofkbp;

architecture hvdd of lmvrdofkbp is
  
begin
  
end hvdd;

entity ely is
  port (asb : buffer bit_vector(3 downto 3));
end ely;

architecture r of ely is
  signal y : real_vector(0 to 2);
  signal xvnqoer : time_vector(1 downto 2);
  signal zeklk : time_vector(1 downto 3);
  signal qwueasbpp : real_vector(0 to 2);
begin
  rgqwxfl : entity work.lmvrdofkbp
    port map (zunvtzmtj => qwueasbpp, d => zeklk, hf => xvnqoer);
  t : entity work.lmvrdofkbp
    port map (zunvtzmtj => y, d => xvnqoer, hf => xvnqoer);
  
  -- Single-driven assignments
  asb <= asb;
end r;

entity c is
  port (dujsfj : inout real; j : linkage real; p : in real);
end c;

architecture cil of c is
  signal vxrnazijed : time_vector(1 downto 2);
  signal uiocstp : time_vector(1 downto 3);
  signal dqmplzr : real_vector(0 to 2);
begin
  hcs : entity work.lmvrdofkbp
    port map (zunvtzmtj => dqmplzr, d => uiocstp, hf => vxrnazijed);
  
  -- Single-driven assignments
  dujsfj <= 002.3434;
  vxrnazijed <= (others => 0 ns);
end cil;



-- Seed after: 10815419601140527062,16188444798499499427
