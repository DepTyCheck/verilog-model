-- Seed: 2029445178520108314,10871023049702252113

entity hdmozcmqlh is
  port (klaz : inout time_vector(0 to 2); rvmicedf : buffer integer; sr : linkage integer);
end hdmozcmqlh;

architecture kfn of hdmozcmqlh is
  
begin
  -- Single-driven assignments
  klaz <= (16#9.8_1# us, 1 hr, 8#35.2_4_1# us);
  rvmicedf <= rvmicedf;
end kfn;

library ieee;
use ieee.std_logic_1164.all;

entity qtwj is
  port (d : inout real_vector(0 downto 2); fkehggc : inout std_logic);
end qtwj;

architecture ard of qtwj is
  signal whvgpsoyas : integer;
  signal ifeknqnej : integer;
  signal imivkjlkju : time_vector(0 to 2);
  signal qo : integer;
  signal ebgs : integer;
  signal kxwbkph : time_vector(0 to 2);
  signal fstiwb : integer;
  signal rpru : integer;
  signal oztowejzrw : time_vector(0 to 2);
  signal tkt : integer;
  signal dnngcrj : integer;
  signal yeul : time_vector(0 to 2);
begin
  ifyircb : entity work.hdmozcmqlh
    port map (klaz => yeul, rvmicedf => dnngcrj, sr => tkt);
  edyykmo : entity work.hdmozcmqlh
    port map (klaz => oztowejzrw, rvmicedf => rpru, sr => fstiwb);
  kehqam : entity work.hdmozcmqlh
    port map (klaz => kxwbkph, rvmicedf => ebgs, sr => qo);
  mme : entity work.hdmozcmqlh
    port map (klaz => imivkjlkju, rvmicedf => ifeknqnej, sr => whvgpsoyas);
  
  -- Multi-driven assignments
  fkehggc <= 'L';
end ard;

library ieee;
use ieee.std_logic_1164.all;

entity nnuowx is
  port (dwwfe : linkage std_logic_vector(4 to 3));
end nnuowx;

library ieee;
use ieee.std_logic_1164.all;

architecture dezawd of nnuowx is
  signal yx : std_logic;
  signal jk : real_vector(0 downto 2);
  signal aaeczvvp : integer;
  signal u : integer;
  signal sr : time_vector(0 to 2);
  signal jgvgeskv : integer;
  signal kco : integer;
  signal gje : time_vector(0 to 2);
  signal w : integer;
  signal f : integer;
  signal n : time_vector(0 to 2);
begin
  buse : entity work.hdmozcmqlh
    port map (klaz => n, rvmicedf => f, sr => w);
  wmxctlm : entity work.hdmozcmqlh
    port map (klaz => gje, rvmicedf => kco, sr => jgvgeskv);
  xwvknx : entity work.hdmozcmqlh
    port map (klaz => sr, rvmicedf => u, sr => aaeczvvp);
  y : entity work.qtwj
    port map (d => jk, fkehggc => yx);
  
  -- Multi-driven assignments
  yx <= '0';
  yx <= yx;
  yx <= '-';
  yx <= yx;
end dezawd;

library ieee;
use ieee.std_logic_1164.all;

entity fd is
  port (y : inout integer; nyefm : inout std_logic);
end fd;

architecture wiexu of fd is
  
begin
  -- Single-driven assignments
  y <= y;
end wiexu;



-- Seed after: 9433374668390397358,10871023049702252113
