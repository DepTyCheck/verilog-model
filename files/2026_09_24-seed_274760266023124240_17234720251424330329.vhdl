-- Seed: 274760266023124240,17234720251424330329

library ieee;
use ieee.std_logic_1164.all;

entity bmxrezfc is
  port (znpwtylwn : in std_logic; pn : in real; q : out std_logic_vector(3 to 2));
end bmxrezfc;

architecture jxbsd of bmxrezfc is
  
begin
  
end jxbsd;

library ieee;
use ieee.std_logic_1164.all;

entity mux is
  port (tc : out boolean; fhxg : linkage std_logic; wxzvjw : buffer boolean; ud : linkage integer);
end mux;

library ieee;
use ieee.std_logic_1164.all;

architecture balljshtme of mux is
  signal ljrg : real;
  signal bvmpaquaoh : std_logic;
  signal n : std_logic;
  signal od : std_logic_vector(3 to 2);
  signal rwclrytw : std_logic_vector(3 to 2);
  signal ppccpff : real;
  signal ndbdym : std_logic;
begin
  ziu : entity work.bmxrezfc
    port map (znpwtylwn => ndbdym, pn => ppccpff, q => rwclrytw);
  frzezipvci : entity work.bmxrezfc
    port map (znpwtylwn => ndbdym, pn => ppccpff, q => od);
  ytn : entity work.bmxrezfc
    port map (znpwtylwn => n, pn => ppccpff, q => rwclrytw);
  spmmm : entity work.bmxrezfc
    port map (znpwtylwn => bvmpaquaoh, pn => ljrg, q => rwclrytw);
end balljshtme;

entity qluywsjht is
  port (yez : linkage bit_vector(2 downto 3); yiimom : in real; kjd : inout character; ebnudovgs : in severity_level);
end qluywsjht;

library ieee;
use ieee.std_logic_1164.all;

architecture gszo of qluywsjht is
  signal yvetljvf : std_logic_vector(3 to 2);
  signal ldv : real;
  signal cfgtbimks : std_logic;
begin
  hvupuimy : entity work.bmxrezfc
    port map (znpwtylwn => cfgtbimks, pn => ldv, q => yvetljvf);
  
  -- Single-driven assignments
  kjd <= kjd;
  ldv <= yiimom;
  
  -- Multi-driven assignments
  cfgtbimks <= cfgtbimks;
end gszo;

entity tqbkh is
  port (bruxtpp : buffer integer);
end tqbkh;

library ieee;
use ieee.std_logic_1164.all;

architecture llcu of tqbkh is
  signal aqwbs : std_logic_vector(3 to 2);
  signal bwqxcglyux : real;
  signal v : std_logic;
begin
  uwe : entity work.bmxrezfc
    port map (znpwtylwn => v, pn => bwqxcglyux, q => aqwbs);
  
  -- Single-driven assignments
  bwqxcglyux <= 42401.3403;
  
  -- Multi-driven assignments
  v <= '-';
  aqwbs <= aqwbs;
  aqwbs <= aqwbs;
end llcu;



-- Seed after: 8550692159731146415,17234720251424330329
