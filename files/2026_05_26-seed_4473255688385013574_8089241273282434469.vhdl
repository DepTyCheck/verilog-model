-- Seed: 4473255688385013574,8089241273282434469



entity oi is
  port (xgczayhceh : in boolean);
end oi;



architecture roh of oi is
  
begin
  
end roh;



entity tfy is
  port (qfsz : buffer real; u : out bit_vector(0 to 4));
end tfy;



architecture irfnanfoa of tfy is
  signal v : boolean;
  signal x : boolean;
begin
  b : entity work.oi
    port map (xgczayhceh => x);
  bbtgqezlg : entity work.oi
    port map (xgczayhceh => x);
  co : entity work.oi
    port map (xgczayhceh => v);
end irfnanfoa;

library ieee;
use ieee.std_logic_1164.all;

entity hzl is
  port (ok : buffer real; r : out real; bgnnsq : linkage std_logic);
end hzl;



architecture o of hzl is
  signal nzuwj : boolean;
  signal cg : boolean;
  signal vhtl : boolean;
begin
  mzupj : entity work.oi
    port map (xgczayhceh => vhtl);
  zxz : entity work.oi
    port map (xgczayhceh => cg);
  scy : entity work.oi
    port map (xgczayhceh => nzuwj);
  i : entity work.oi
    port map (xgczayhceh => cg);
end o;

library ieee;
use ieee.std_logic_1164.all;

entity a is
  port (ogbyirf : inout severity_level; vtov : out integer; t : in std_logic; mdxh : inout bit_vector(2 to 1));
end a;



architecture lw of a is
  signal voclpqwzb : boolean;
  signal jfzw : bit_vector(0 to 4);
  signal qknppbkwh : real;
  signal lepvi : boolean;
begin
  adhauwxhi : entity work.oi
    port map (xgczayhceh => lepvi);
  zm : entity work.tfy
    port map (qfsz => qknppbkwh, u => jfzw);
  dz : entity work.oi
    port map (xgczayhceh => voclpqwzb);
  kvgjplasx : entity work.oi
    port map (xgczayhceh => voclpqwzb);
end lw;



-- Seed after: 3386384127752431492,8089241273282434469
