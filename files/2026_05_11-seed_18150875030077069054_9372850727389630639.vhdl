-- Seed: 18150875030077069054,9372850727389630639

library ieee;
use ieee.std_logic_1164.all;

entity mpogy is
  port (fwvg : linkage character; euvgabvy : buffer integer; qpkoclmu : buffer std_logic);
end mpogy;



architecture ncj of mpogy is
  
begin
  
end ncj;



entity cufndl is
  port (zy : in character);
end cufndl;

library ieee;
use ieee.std_logic_1164.all;

architecture rux of cufndl is
  signal eh : std_logic;
  signal pmbl : integer;
  signal wq : integer;
  signal rn : std_logic;
  signal actvsf : integer;
  signal qyhnn : std_logic;
  signal hpjro : integer;
begin
  zlyrx : entity work.mpogy
    port map (fwvg => zy, euvgabvy => hpjro, qpkoclmu => qyhnn);
  i : entity work.mpogy
    port map (fwvg => zy, euvgabvy => actvsf, qpkoclmu => rn);
  wqmxvuy : entity work.mpogy
    port map (fwvg => zy, euvgabvy => wq, qpkoclmu => qyhnn);
  m : entity work.mpogy
    port map (fwvg => zy, euvgabvy => pmbl, qpkoclmu => eh);
end rux;



entity km is
  port (pwoqqtqhmr : out integer; dxaybya : buffer bit; fizhascua : buffer time);
end km;

library ieee;
use ieee.std_logic_1164.all;

architecture mvdvwbh of km is
  signal cwnfie : integer;
  signal rtueihuk : character;
  signal cq : std_logic;
  signal fpeovexyj : integer;
  signal vmtn : character;
begin
  qma : entity work.mpogy
    port map (fwvg => vmtn, euvgabvy => fpeovexyj, qpkoclmu => cq);
  yrbikmzp : entity work.mpogy
    port map (fwvg => vmtn, euvgabvy => pwoqqtqhmr, qpkoclmu => cq);
  llmvj : entity work.mpogy
    port map (fwvg => rtueihuk, euvgabvy => cwnfie, qpkoclmu => cq);
end mvdvwbh;



-- Seed after: 608638588064981927,9372850727389630639
