-- Seed: 11121919089228830251,16188444798499499427

entity tabblouv is
  port (fv : in real; m : out time; rfkveoddw : linkage bit; ymwn : buffer time);
end tabblouv;

architecture agx of tabblouv is
  
begin
  -- Single-driven assignments
  ymwn <= 3 min;
  m <= 2 min;
end agx;

entity yrnwjynxg is
  port (cgub : in time; wp : in severity_level);
end yrnwjynxg;

architecture wtn of yrnwjynxg is
  signal qbd : time;
  signal vvdjrrbqk : bit;
  signal zpom : time;
  signal pvl : real;
  signal olacu : time;
  signal roqpgw : bit;
  signal wznr : time;
  signal gs : real;
  signal e : time;
  signal p : bit;
  signal jrak : time;
  signal s : time;
  signal eyv : bit;
  signal as : time;
  signal pjmzxby : real;
begin
  vsgx : entity work.tabblouv
    port map (fv => pjmzxby, m => as, rfkveoddw => eyv, ymwn => s);
  umfu : entity work.tabblouv
    port map (fv => pjmzxby, m => jrak, rfkveoddw => p, ymwn => e);
  fdpgz : entity work.tabblouv
    port map (fv => gs, m => wznr, rfkveoddw => roqpgw, ymwn => olacu);
  vn : entity work.tabblouv
    port map (fv => pvl, m => zpom, rfkveoddw => vvdjrrbqk, ymwn => qbd);
end wtn;



-- Seed after: 5111316522912180740,16188444798499499427
