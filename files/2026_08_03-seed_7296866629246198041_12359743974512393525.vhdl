-- Seed: 7296866629246198041,12359743974512393525

entity acfnuer is
  port (eev : in real; xa : buffer integer; fms : buffer real);
end acfnuer;

architecture bw of acfnuer is
  
begin
  -- Single-driven assignments
  xa <= xa;
end bw;

entity ncrp is
  port (kxy : linkage time);
end ncrp;

architecture ipwrhony of ncrp is
  signal xzabvbawc : real;
  signal sk : integer;
  signal qpewjktbng : integer;
  signal oua : real;
begin
  ufylh : entity work.acfnuer
    port map (eev => oua, xa => qpewjktbng, fms => oua);
  bntms : entity work.acfnuer
    port map (eev => oua, xa => sk, fms => xzabvbawc);
end ipwrhony;

library ieee;
use ieee.std_logic_1164.all;

entity wjfazkjy is
  port (t : linkage std_logic_vector(2 to 0); vabrpqq : inout severity_level);
end wjfazkjy;

architecture lvacpl of wjfazkjy is
  signal ffbrudfm : integer;
  signal dzj : real;
begin
  rz : entity work.acfnuer
    port map (eev => dzj, xa => ffbrudfm, fms => dzj);
  
  -- Single-driven assignments
  vabrpqq <= FAILURE;
end lvacpl;

library ieee;
use ieee.std_logic_1164.all;

entity anbhtf is
  port (lhuuayv : buffer string(2 to 2); ovycphfxym : in std_logic_vector(3 downto 4));
end anbhtf;

architecture otbdtycro of anbhtf is
  signal pu : real;
  signal a : integer;
  signal ayu : real;
  signal bcvjajvz : real;
  signal no : integer;
  signal job : real;
begin
  cwic : entity work.acfnuer
    port map (eev => job, xa => no, fms => bcvjajvz);
  sxijlkj : entity work.acfnuer
    port map (eev => ayu, xa => a, fms => pu);
  
  -- Single-driven assignments
  lhuuayv <= (others => 'm');
end otbdtycro;



-- Seed after: 16691943537016036128,12359743974512393525
