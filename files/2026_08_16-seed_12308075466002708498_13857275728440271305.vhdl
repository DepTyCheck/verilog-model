-- Seed: 12308075466002708498,13857275728440271305

library ieee;
use ieee.std_logic_1164.all;

entity wzldcrko is
  port (qgbvorjg : linkage real; ht : buffer std_logic; mbwvn : in severity_level; lzepz : buffer severity_level);
end wzldcrko;

architecture uajvzeonpc of wzldcrko is
  
begin
  -- Single-driven assignments
  lzepz <= WARNING;
  
  -- Multi-driven assignments
  ht <= ht;
  ht <= 'H';
end uajvzeonpc;

library ieee;
use ieee.std_logic_1164.all;

entity ibvo is
  port (btobqyq : inout std_logic_vector(4 downto 2); rdcdswk : linkage integer_vector(3 to 3));
end ibvo;

library ieee;
use ieee.std_logic_1164.all;

architecture txflivkf of ibvo is
  signal lkts : severity_level;
  signal oqpblidpec : std_logic;
  signal g : real;
  signal fifdz : severity_level;
  signal kcqjmzag : std_logic;
  signal cj : real;
  signal odvzsvwbf : severity_level;
  signal ybhfil : std_logic;
  signal gq : real;
begin
  a : entity work.wzldcrko
    port map (qgbvorjg => gq, ht => ybhfil, mbwvn => odvzsvwbf, lzepz => odvzsvwbf);
  iusjmtmyv : entity work.wzldcrko
    port map (qgbvorjg => cj, ht => kcqjmzag, mbwvn => fifdz, lzepz => fifdz);
  jinnvnji : entity work.wzldcrko
    port map (qgbvorjg => g, ht => oqpblidpec, mbwvn => lkts, lzepz => lkts);
  
  -- Multi-driven assignments
  kcqjmzag <= 'L';
  btobqyq <= ('Z', 'X', 'H');
  kcqjmzag <= ybhfil;
  kcqjmzag <= ybhfil;
end txflivkf;

entity ds is
  port (cme : out boolean);
end ds;

library ieee;
use ieee.std_logic_1164.all;

architecture dmxmi of ds is
  signal ktcfjnmcez : integer_vector(3 to 3);
  signal zcpv : std_logic_vector(4 downto 2);
  signal nzmi : integer_vector(3 to 3);
  signal tulwatb : std_logic_vector(4 downto 2);
  signal ijibd : severity_level;
  signal zta : real;
  signal shywwgcuan : severity_level;
  signal djunoa : std_logic;
  signal qnxdb : real;
begin
  kzynawhcv : entity work.wzldcrko
    port map (qgbvorjg => qnxdb, ht => djunoa, mbwvn => shywwgcuan, lzepz => shywwgcuan);
  yaq : entity work.wzldcrko
    port map (qgbvorjg => zta, ht => djunoa, mbwvn => ijibd, lzepz => ijibd);
  qcvrc : entity work.ibvo
    port map (btobqyq => tulwatb, rdcdswk => nzmi);
  owvbg : entity work.ibvo
    port map (btobqyq => zcpv, rdcdswk => ktcfjnmcez);
  
  -- Single-driven assignments
  cme <= cme;
  
  -- Multi-driven assignments
  djunoa <= djunoa;
  zcpv <= tulwatb;
end dmxmi;

entity laemzycc is
  port (sa : inout real; punrfvbz : out time);
end laemzycc;

library ieee;
use ieee.std_logic_1164.all;

architecture dbmji of laemzycc is
  signal nwbpvaa : severity_level;
  signal iww : real;
  signal wgbcd : severity_level;
  signal qtlver : std_logic;
  signal xvrqand : real;
  signal egkebnnuyr : boolean;
  signal xcftdktv : severity_level;
  signal nrbwdr : severity_level;
  signal y : std_logic;
  signal ifsn : real;
begin
  afcxou : entity work.wzldcrko
    port map (qgbvorjg => ifsn, ht => y, mbwvn => nrbwdr, lzepz => xcftdktv);
  llips : entity work.ds
    port map (cme => egkebnnuyr);
  zyududqkrj : entity work.wzldcrko
    port map (qgbvorjg => xvrqand, ht => qtlver, mbwvn => wgbcd, lzepz => wgbcd);
  x : entity work.wzldcrko
    port map (qgbvorjg => iww, ht => y, mbwvn => wgbcd, lzepz => nwbpvaa);
  
  -- Single-driven assignments
  punrfvbz <= 0 hr;
end dbmji;



-- Seed after: 18298035301901580863,13857275728440271305
