-- Seed: 9853244582034953608,8118127366649987907

entity ofx is
  port (oso : linkage time; k : out boolean);
end ofx;

architecture zmiwrmt of ofx is
  
begin
  -- Single-driven assignments
  k <= TRUE;
end zmiwrmt;

entity zpugfyxyz is
  port (odxdas : buffer character; heutfxw : out integer_vector(2 to 1); jwv : linkage bit_vector(1 to 1));
end zpugfyxyz;

architecture iojcibjmu of zpugfyxyz is
  signal i : boolean;
  signal xvv : time;
  signal vpyv : boolean;
  signal cqh : time;
begin
  mfzgm : entity work.ofx
    port map (oso => cqh, k => vpyv);
  vpaj : entity work.ofx
    port map (oso => xvv, k => i);
  
  -- Single-driven assignments
  odxdas <= 'r';
  heutfxw <= (others => 0);
end iojcibjmu;

entity oyvcmph is
  port (rt : in string(5 to 3));
end oyvcmph;

architecture b of oyvcmph is
  signal nkagupvqh : bit_vector(1 to 1);
  signal n : integer_vector(2 to 1);
  signal dtszdmqsav : character;
  signal dwteqx : boolean;
  signal k : time;
  signal nbikbbb : bit_vector(1 to 1);
  signal qzkayge : integer_vector(2 to 1);
  signal clnmo : character;
  signal avn : bit_vector(1 to 1);
  signal rrccvgkig : integer_vector(2 to 1);
  signal stx : character;
begin
  wv : entity work.zpugfyxyz
    port map (odxdas => stx, heutfxw => rrccvgkig, jwv => avn);
  mdbac : entity work.zpugfyxyz
    port map (odxdas => clnmo, heutfxw => qzkayge, jwv => nbikbbb);
  e : entity work.ofx
    port map (oso => k, k => dwteqx);
  qguxp : entity work.zpugfyxyz
    port map (odxdas => dtszdmqsav, heutfxw => n, jwv => nkagupvqh);
end b;

entity k is
  port (mxerjuu : linkage severity_level);
end k;

architecture wpyedqwtkc of k is
  signal beabonmen : bit_vector(1 to 1);
  signal qv : integer_vector(2 to 1);
  signal tszkr : character;
  signal hrjs : string(5 to 3);
  signal shjh : boolean;
  signal wgjlryowqg : time;
  signal supcepihgw : bit_vector(1 to 1);
  signal kluz : integer_vector(2 to 1);
  signal lbjruxq : character;
begin
  kzstqukuz : entity work.zpugfyxyz
    port map (odxdas => lbjruxq, heutfxw => kluz, jwv => supcepihgw);
  pssm : entity work.ofx
    port map (oso => wgjlryowqg, k => shjh);
  zscmpqtphn : entity work.oyvcmph
    port map (rt => hrjs);
  e : entity work.zpugfyxyz
    port map (odxdas => tszkr, heutfxw => qv, jwv => beabonmen);
  
  -- Single-driven assignments
  hrjs <= (others => ' ');
end wpyedqwtkc;



-- Seed after: 5870577539757770182,8118127366649987907
