-- Seed: 4518666070102001532,12269339630485015285

entity syjhmtn is
  port (b : out time; sdphdzv : inout real);
end syjhmtn;

architecture ybvulbjys of syjhmtn is
  
begin
  -- Single-driven assignments
  b <= 16#1F1# us;
  sdphdzv <= 8#57.42026#;
end ybvulbjys;

library ieee;
use ieee.std_logic_1164.all;

entity waalctream is
  port (im : inout character; wbmukq : out boolean; syq : out std_logic; jqkzjdo : buffer real);
end waalctream;

architecture yloy of waalctream is
  signal ychamfxuim : time;
  signal zmbocm : real;
  signal zxsm : time;
  signal od : real;
  signal mjwxptbnbr : time;
begin
  wzjpjrgce : entity work.syjhmtn
    port map (b => mjwxptbnbr, sdphdzv => od);
  yqntysrin : entity work.syjhmtn
    port map (b => zxsm, sdphdzv => zmbocm);
  nptxkov : entity work.syjhmtn
    port map (b => ychamfxuim, sdphdzv => jqkzjdo);
  
  -- Single-driven assignments
  wbmukq <= FALSE;
  im <= im;
  
  -- Multi-driven assignments
  syq <= 'X';
  syq <= 'U';
end yloy;

entity wglagddxo is
  port (qpxllhm : out boolean; elda : out integer);
end wglagddxo;

library ieee;
use ieee.std_logic_1164.all;

architecture grahwogb of wglagddxo is
  signal trnylw : real;
  signal z : time;
  signal btoaell : real;
  signal gnxun : boolean;
  signal hev : character;
  signal lnpwu : real;
  signal lewmzz : time;
  signal peorcoz : real;
  signal rtlvdkrhfn : std_logic;
  signal gueqwkaq : character;
begin
  njkggwlyz : entity work.waalctream
    port map (im => gueqwkaq, wbmukq => qpxllhm, syq => rtlvdkrhfn, jqkzjdo => peorcoz);
  ohlmiihbtk : entity work.syjhmtn
    port map (b => lewmzz, sdphdzv => lnpwu);
  akkglltnfy : entity work.waalctream
    port map (im => hev, wbmukq => gnxun, syq => rtlvdkrhfn, jqkzjdo => btoaell);
  qavmmqbzps : entity work.syjhmtn
    port map (b => z, sdphdzv => trnylw);
  
  -- Multi-driven assignments
  rtlvdkrhfn <= '0';
  rtlvdkrhfn <= '-';
end grahwogb;

entity nrbzaqo is
  port (xi : buffer time; jwpdtshvc : buffer string(3 downto 1));
end nrbzaqo;

architecture ovz of nrbzaqo is
  
begin
  -- Single-driven assignments
  xi <= xi;
  jwpdtshvc <= jwpdtshvc;
end ovz;



-- Seed after: 8734836363044794525,12269339630485015285
