-- Seed: 4228602699214053102,10871023049702252113

entity te is
  port (wet : inout integer; ut : inout boolean);
end te;

architecture qwuhb of te is
  
begin
  -- Single-driven assignments
  ut <= ut;
  wet <= wet;
end qwuhb;

library ieee;
use ieee.std_logic_1164.all;

entity ycjk is
  port (nlrcobynsq : in bit_vector(1 to 0); y : buffer std_logic);
end ycjk;

architecture qjk of ycjk is
  signal tqrrln : boolean;
  signal ccltevn : integer;
  signal ibefdik : boolean;
  signal kt : integer;
  signal ifyx : boolean;
  signal f : integer;
begin
  cuauid : entity work.te
    port map (wet => f, ut => ifyx);
  fdqb : entity work.te
    port map (wet => kt, ut => ibefdik);
  rp : entity work.te
    port map (wet => ccltevn, ut => tqrrln);
  
  -- Multi-driven assignments
  y <= '1';
  y <= y;
  y <= y;
  y <= 'U';
end qjk;

entity hphfys is
  port (qfj : out time; olatui : buffer character);
end hphfys;

library ieee;
use ieee.std_logic_1164.all;

architecture hvxncul of hphfys is
  signal i : boolean;
  signal dmdlmg : integer;
  signal axdarrhdfv : std_logic;
  signal bf : bit_vector(1 to 0);
  signal onpmvibas : boolean;
  signal ymcodx : integer;
begin
  g : entity work.te
    port map (wet => ymcodx, ut => onpmvibas);
  buiehxtwc : entity work.ycjk
    port map (nlrcobynsq => bf, y => axdarrhdfv);
  a : entity work.te
    port map (wet => dmdlmg, ut => i);
end hvxncul;

entity mt is
  port (jxdplvq : buffer integer; z : in string(5 to 1));
end mt;

library ieee;
use ieee.std_logic_1164.all;

architecture iyb of mt is
  signal inlhi : std_logic;
  signal lrstsfq : bit_vector(1 to 0);
  signal jwyivq : character;
  signal kemly : time;
begin
  aqnormxaep : entity work.hphfys
    port map (qfj => kemly, olatui => jwyivq);
  jd : entity work.ycjk
    port map (nlrcobynsq => lrstsfq, y => inlhi);
  
  -- Single-driven assignments
  lrstsfq <= lrstsfq;
  jxdplvq <= 8#2#;
  
  -- Multi-driven assignments
  inlhi <= inlhi;
  inlhi <= 'X';
end iyb;



-- Seed after: 2786390983905948121,10871023049702252113
