-- Seed: 10958221609713784151,13196211255131729027

entity oxv is
  port (ej : linkage time; hns : in real);
end oxv;

architecture e of oxv is
  
begin
  
end e;

library ieee;
use ieee.std_logic_1164.all;

entity kpkwchnrzj is
  port (f : linkage boolean; b : buffer std_logic; bekjdguvdn : out integer; mupvtqhwd : buffer integer_vector(2 to 1));
end kpkwchnrzj;

architecture zuacixlr of kpkwchnrzj is
  signal o : real;
  signal c : time;
  signal ilcupxyw : real;
  signal qzgj : time;
begin
  yv : entity work.oxv
    port map (ej => qzgj, hns => ilcupxyw);
  pngayzr : entity work.oxv
    port map (ej => c, hns => o);
  
  -- Single-driven assignments
  bekjdguvdn <= 0_1_1_3_1;
  ilcupxyw <= ilcupxyw;
  mupvtqhwd <= (others => 0);
end zuacixlr;

library ieee;
use ieee.std_logic_1164.all;

entity l is
  port (dcdnrfhp : in std_logic_vector(4 to 3); cmleeveejv : linkage std_logic);
end l;

architecture dlmgcf of l is
  signal bamvvbely : time;
  signal rnrfxava : time;
  signal bnbpc : real;
  signal wrgqbwntj : time;
begin
  df : entity work.oxv
    port map (ej => wrgqbwntj, hns => bnbpc);
  t : entity work.oxv
    port map (ej => rnrfxava, hns => bnbpc);
  ufdsjcgz : entity work.oxv
    port map (ej => bamvvbely, hns => bnbpc);
  
  -- Single-driven assignments
  bnbpc <= 8#7_2.304#;
end dlmgcf;

library ieee;
use ieee.std_logic_1164.all;

entity xozrioxdtm is
  port (ygged : out real; hguraog : out boolean; pkw : buffer boolean; fbqbsc : inout std_logic);
end xozrioxdtm;

library ieee;
use ieee.std_logic_1164.all;

architecture po of xozrioxdtm is
  signal qccxsnj : integer_vector(2 to 1);
  signal tvcbreqr : integer;
  signal okfbzhyrpp : std_logic;
  signal yc : boolean;
  signal k : real;
  signal p : time;
  signal cnq : integer_vector(2 to 1);
  signal gpyc : integer;
  signal bdearogw : real;
  signal iixbytkaml : time;
begin
  ys : entity work.oxv
    port map (ej => iixbytkaml, hns => bdearogw);
  en : entity work.kpkwchnrzj
    port map (f => hguraog, b => fbqbsc, bekjdguvdn => gpyc, mupvtqhwd => cnq);
  unobwwpxz : entity work.oxv
    port map (ej => p, hns => k);
  czjmn : entity work.kpkwchnrzj
    port map (f => yc, b => okfbzhyrpp, bekjdguvdn => tvcbreqr, mupvtqhwd => qccxsnj);
  
  -- Single-driven assignments
  pkw <= FALSE;
  ygged <= 3310.2_2;
  bdearogw <= ygged;
  
  -- Multi-driven assignments
  fbqbsc <= fbqbsc;
end po;



-- Seed after: 13712241191409433586,13196211255131729027
