-- Seed: 13713461393161607003,6299883410057943775

entity hoxkhtw is
  port (lorskbbzp : buffer real; pk : linkage string(3 to 3));
end hoxkhtw;

architecture zzna of hoxkhtw is
  
begin
  -- Single-driven assignments
  lorskbbzp <= 2#1_0.00100#;
end zzna;

library ieee;
use ieee.std_logic_1164.all;

entity csw is
  port (nv : inout std_logic; ybrcr : out std_logic_vector(1 to 0); xowicity : out time; hv : inout real);
end csw;

architecture unpjrjuj of csw is
  signal rwsctdmotx : string(3 to 3);
  signal aajizfo : string(3 to 3);
  signal vfi : real;
  signal pkpm : string(3 to 3);
  signal hcse : real;
begin
  ikxt : entity work.hoxkhtw
    port map (lorskbbzp => hcse, pk => pkpm);
  uk : entity work.hoxkhtw
    port map (lorskbbzp => vfi, pk => aajizfo);
  fqnlqow : entity work.hoxkhtw
    port map (lorskbbzp => hv, pk => rwsctdmotx);
  
  -- Single-driven assignments
  xowicity <= xowicity;
  
  -- Multi-driven assignments
  ybrcr <= ybrcr;
  ybrcr <= "";
  ybrcr <= (others => '0');
  ybrcr <= (others => '0');
end unpjrjuj;

library ieee;
use ieee.std_logic_1164.all;

entity ciwhnnhx is
  port (sleib : inout integer; h : out std_logic);
end ciwhnnhx;

library ieee;
use ieee.std_logic_1164.all;

architecture iptyxmhot of ciwhnnhx is
  signal og : real;
  signal yq : time;
  signal mc : std_logic_vector(1 to 0);
  signal hwzzpurj : string(3 to 3);
  signal xspwpzwydf : real;
  signal runulawv : string(3 to 3);
  signal cb : real;
begin
  rgvfszfitb : entity work.hoxkhtw
    port map (lorskbbzp => cb, pk => runulawv);
  o : entity work.hoxkhtw
    port map (lorskbbzp => xspwpzwydf, pk => hwzzpurj);
  uqbyrl : entity work.csw
    port map (nv => h, ybrcr => mc, xowicity => yq, hv => og);
  
  -- Single-driven assignments
  sleib <= 3;
  
  -- Multi-driven assignments
  h <= h;
  h <= 'Z';
  h <= h;
  h <= h;
end iptyxmhot;



-- Seed after: 9123266254480019193,6299883410057943775
