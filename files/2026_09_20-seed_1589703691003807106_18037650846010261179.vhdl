-- Seed: 1589703691003807106,18037650846010261179

entity cbqb is
  port (iyg : out time_vector(1 downto 0); vnmo : linkage integer; f : out character; ef : buffer real_vector(3 downto 1));
end cbqb;

architecture vfu of cbqb is
  
begin
  -- Single-driven assignments
  ef <= ef;
  f <= f;
end vfu;

entity ugeoyeua is
  port (rxwirr : out boolean_vector(4 downto 4); gdzfms : in integer_vector(3 to 1); dvsynh : out string(4 to 4); bhkh : inout time);
end ugeoyeua;

architecture hrxdi of ugeoyeua is
  signal uxjt : real_vector(3 downto 1);
  signal fi : character;
  signal ko : integer;
  signal revvu : time_vector(1 downto 0);
  signal z : real_vector(3 downto 1);
  signal tic : character;
  signal upyohdh : integer;
  signal fsy : time_vector(1 downto 0);
  signal gdaydu : real_vector(3 downto 1);
  signal atcfakhr : character;
  signal ugbyqdnstx : integer;
  signal zawir : time_vector(1 downto 0);
  signal cjyx : real_vector(3 downto 1);
  signal bcnh : character;
  signal xnhlrobphd : integer;
  signal cmuw : time_vector(1 downto 0);
begin
  a : entity work.cbqb
    port map (iyg => cmuw, vnmo => xnhlrobphd, f => bcnh, ef => cjyx);
  k : entity work.cbqb
    port map (iyg => zawir, vnmo => ugbyqdnstx, f => atcfakhr, ef => gdaydu);
  zu : entity work.cbqb
    port map (iyg => fsy, vnmo => upyohdh, f => tic, ef => z);
  hm : entity work.cbqb
    port map (iyg => revvu, vnmo => ko, f => fi, ef => uxjt);
  
  -- Single-driven assignments
  rxwirr <= (others => FALSE);
end hrxdi;



-- Seed after: 8544124474591990089,18037650846010261179
