// Seed: 12354139448520948710,1236351726769057449

module kijkpbxh ();
  xnor ceoaac(npev, npev, euwurt);
  
  xnor pni(uzmono, uzmono, npev);
  
  and ysujrlf(uzmono, npev, uzmono);
  
  and ztwjdlaw(npev, npev, knbfoxg);
  
  
  // Multi-driven assignments
  assign npev = npev;
  assign npev = 'b1;
endmodule: kijkpbxh

module l (output shortint pmioi);
  nand vbghfl(hhcjao, hhcjao, pmioi);
  // warning: implicit conversion of port connection truncates from 16 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   shortint pmioi -> logic pmioi
  
  xnor guurjqeekt(pmioi, hhcjao, brnoovhnx);
  // warning: implicit conversion of port connection truncates from 16 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   shortint pmioi -> logic pmioi
  
  not bpkovu(brnoovhnx, qxiqtu);
  
  and zdzkza(hhcjao, pmioi, pmioi);
  // warning: implicit conversion of port connection truncates from 16 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   shortint pmioi -> logic pmioi
  //
  // warning: implicit conversion of port connection truncates from 16 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   shortint pmioi -> logic pmioi
  
  
  // Multi-driven assignments
  assign brnoovhnx = 'bx;
  assign hhcjao = hhcjao;
endmodule: l



// Seed after: 4363154376874487003,1236351726769057449
