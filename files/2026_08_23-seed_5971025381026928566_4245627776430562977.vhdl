-- Seed: 5971025381026928566,4245627776430562977

entity gf is
  port (wr : out real_vector(1 downto 2); jl : inout string(5 downto 2));
end gf;

architecture kwt of gf is
  
begin
  -- Single-driven assignments
  jl <= jl;
end kwt;

entity wqx is
  port (gyzszu : in string(1 to 2));
end wqx;

architecture efuifh of wqx is
  signal ymhbeb : string(5 downto 2);
  signal a : real_vector(1 downto 2);
  signal upwy : string(5 downto 2);
  signal tnmutbbqqf : real_vector(1 downto 2);
  signal qi : string(5 downto 2);
  signal ejslpnkwk : real_vector(1 downto 2);
begin
  r : entity work.gf
    port map (wr => ejslpnkwk, jl => qi);
  cumhf : entity work.gf
    port map (wr => tnmutbbqqf, jl => upwy);
  g : entity work.gf
    port map (wr => a, jl => ymhbeb);
end efuifh;

entity xwzjzbubm is
  port (omdccyi : out time; lvafju : out real);
end xwzjzbubm;

architecture y of xwzjzbubm is
  signal mwaayr : string(5 downto 2);
  signal ca : real_vector(1 downto 2);
  signal sw : string(5 downto 2);
  signal bvdvyge : real_vector(1 downto 2);
begin
  tg : entity work.gf
    port map (wr => bvdvyge, jl => sw);
  svvnjabqpd : entity work.gf
    port map (wr => ca, jl => mwaayr);
  
  -- Single-driven assignments
  omdccyi <= 1 hr;
  lvafju <= 3.31243;
end y;

library ieee;
use ieee.std_logic_1164.all;

entity tddezcpznq is
  port (vtwbukis : buffer std_logic_vector(2 downto 4); exdbm : in integer; r : linkage std_logic; vfpflhpg : linkage integer);
end tddezcpznq;

architecture xukoicgc of tddezcpznq is
  signal wa : string(5 downto 2);
  signal ysaogl : real_vector(1 downto 2);
begin
  ikvxl : entity work.gf
    port map (wr => ysaogl, jl => wa);
  
  -- Multi-driven assignments
  vtwbukis <= (others => '0');
  vtwbukis <= (others => '0');
  vtwbukis <= (others => '0');
  vtwbukis <= (others => '0');
end xukoicgc;



-- Seed after: 17594881306782270187,4245627776430562977
