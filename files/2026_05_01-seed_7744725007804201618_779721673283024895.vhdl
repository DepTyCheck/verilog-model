-- Seed: 7744725007804201618,779721673283024895



entity td is
  port (ztd : buffer time; rw : inout real; vzimumo : inout real; b : linkage integer);
end td;



architecture h of td is
  
begin
  
end h;



entity tkbtas is
  port (eze : in character);
end tkbtas;



architecture wjdjwrcqw of tkbtas is
  signal sn : integer;
  signal wbeyzfd : real;
  signal o : real;
  signal bmya : time;
  signal mrf : integer;
  signal hyz : real;
  signal zp : real;
  signal mw : time;
begin
  btz : entity work.td
    port map (ztd => mw, rw => zp, vzimumo => hyz, b => mrf);
  niioramsoy : entity work.td
    port map (ztd => bmya, rw => o, vzimumo => wbeyzfd, b => sn);
end wjdjwrcqw;

library ieee;
use ieee.std_logic_1164.all;

entity o is
  port (jmlyc : in time; dyuv : out std_logic; zsdojfq : linkage integer);
end o;



architecture pnw of o is
  signal sl : integer;
  signal fcyf : real;
  signal zdkthh : real;
  signal htjla : time;
  signal mb : integer;
  signal ac : real;
  signal ejbxrtxcqq : real;
  signal h : time;
  signal hkbsqxps : integer;
  signal a : real;
  signal k : real;
  signal e : time;
begin
  b : entity work.td
    port map (ztd => e, rw => k, vzimumo => a, b => hkbsqxps);
  txwy : entity work.td
    port map (ztd => h, rw => ejbxrtxcqq, vzimumo => ac, b => mb);
  iadztirrwp : entity work.td
    port map (ztd => htjla, rw => zdkthh, vzimumo => fcyf, b => sl);
end pnw;



-- Seed after: 5886788001932572237,779721673283024895
