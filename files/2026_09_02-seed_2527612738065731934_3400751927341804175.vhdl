-- Seed: 2527612738065731934,3400751927341804175

entity dcmindb is
  port (elgejtd : out integer; pswk : linkage time; igepqa : out severity_level);
end dcmindb;

architecture ksowxbu of dcmindb is
  
begin
  -- Single-driven assignments
  elgejtd <= 8#6#;
  igepqa <= igepqa;
end ksowxbu;

library ieee;
use ieee.std_logic_1164.all;

entity betgk is
  port (w : linkage real; eunibe : inout integer; zv : buffer std_logic_vector(4 downto 3); fidgxehjr : in time);
end betgk;

architecture ukresjj of betgk is
  signal tebrbavev : severity_level;
  signal awfnedr : time;
  signal dz : severity_level;
  signal ihoz : time;
  signal dpfcsf : integer;
begin
  bbvlu : entity work.dcmindb
    port map (elgejtd => dpfcsf, pswk => ihoz, igepqa => dz);
  gw : entity work.dcmindb
    port map (elgejtd => eunibe, pswk => awfnedr, igepqa => tebrbavev);
  
  -- Multi-driven assignments
  zv <= ('X', 'Z');
end ukresjj;



-- Seed after: 10503588103850797624,3400751927341804175
