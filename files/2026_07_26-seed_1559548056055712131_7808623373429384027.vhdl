-- Seed: 1559548056055712131,7808623373429384027

library ieee;
use ieee.std_logic_1164.all;

entity sy is
  port (aomdaiys : in std_logic_vector(4 to 2); lok : linkage real);
end sy;

architecture insdg of sy is
  
begin
  
end insdg;

entity pfuthctef is
  port (qvdn : inout real_vector(2 to 1); pul : inout integer_vector(2 to 0); lrbfm : inout time; vjd : linkage real);
end pfuthctef;

library ieee;
use ieee.std_logic_1164.all;

architecture ssh of pfuthctef is
  signal vumtrk : std_logic_vector(4 to 2);
  signal mtlzgqsrx : real;
  signal mqqjdd : real;
  signal zavdhlfjb : std_logic_vector(4 to 2);
begin
  owwpfpzeb : entity work.sy
    port map (aomdaiys => zavdhlfjb, lok => mqqjdd);
  baykqp : entity work.sy
    port map (aomdaiys => zavdhlfjb, lok => mtlzgqsrx);
  sk : entity work.sy
    port map (aomdaiys => vumtrk, lok => vjd);
  
  -- Single-driven assignments
  lrbfm <= lrbfm;
  pul <= (others => 0);
  qvdn <= (others => 0.0);
  
  -- Multi-driven assignments
  zavdhlfjb <= zavdhlfjb;
  zavdhlfjb <= zavdhlfjb;
end ssh;

library ieee;
use ieee.std_logic_1164.all;

entity qbm is
  port (yycbin : linkage real; snbklm : linkage std_logic; gwrkdavyy : out real);
end qbm;

library ieee;
use ieee.std_logic_1164.all;

architecture t of qbm is
  signal ryhuf : real;
  signal py : std_logic_vector(4 to 2);
begin
  gsriaugwe : entity work.sy
    port map (aomdaiys => py, lok => ryhuf);
  
  -- Multi-driven assignments
  py <= py;
  py <= (others => '0');
  py <= "";
end t;



-- Seed after: 11236201118236245646,7808623373429384027
