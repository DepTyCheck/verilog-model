-- Seed: 18002651604594193625,3400751927341804175

entity fy is
  port (zcv : linkage time);
end fy;

architecture yforiohe of fy is
  
begin
  
end yforiohe;

library ieee;
use ieee.std_logic_1164.all;

entity htyuga is
  port (hbbxvmkl : linkage std_logic; wbiku : buffer time);
end htyuga;

architecture k of htyuga is
  
begin
  -- Single-driven assignments
  wbiku <= wbiku;
end k;

library ieee;
use ieee.std_logic_1164.all;

entity eb is
  port (hnsglrdovu : out std_logic_vector(2 to 0));
end eb;

architecture pnw of eb is
  signal m : time;
  signal vxa : time;
  signal uxh : time;
begin
  xb : entity work.fy
    port map (zcv => uxh);
  xu : entity work.fy
    port map (zcv => vxa);
  fdpbta : entity work.fy
    port map (zcv => m);
  
  -- Multi-driven assignments
  hnsglrdovu <= hnsglrdovu;
  hnsglrdovu <= hnsglrdovu;
  hnsglrdovu <= hnsglrdovu;
end pnw;

entity rwy is
  port (muxuypsxld : in integer; egh : in real);
end rwy;

library ieee;
use ieee.std_logic_1164.all;

architecture fjcmfwl of rwy is
  signal gguad : std_logic_vector(2 to 0);
begin
  dfmryppl : entity work.eb
    port map (hnsglrdovu => gguad);
  
  -- Multi-driven assignments
  gguad <= "";
  gguad <= gguad;
end fjcmfwl;



-- Seed after: 18115719256085141567,3400751927341804175
