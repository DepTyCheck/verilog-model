-- Seed: 14177327639504847938,7198033922882419595

entity pncle is
  port (urz : buffer real; mf : linkage time; sfdd : linkage time; wc : linkage time);
end pncle;

architecture emcmlx of pncle is
  
begin
  -- Single-driven assignments
  urz <= urz;
end emcmlx;

entity brre is
  port (it : in real; r : inout real; yghlmwrqcc : out severity_level; cjnql : inout integer);
end brre;

architecture hhrulpsnd of brre is
  signal qmktvl : time;
  signal hogjzd : time;
  signal afvzrvg : time;
  signal zqglowdbya : time;
  signal s : time;
  signal q : time;
  signal zmnhxyrv : real;
begin
  wqphya : entity work.pncle
    port map (urz => zmnhxyrv, mf => q, sfdd => s, wc => zqglowdbya);
  vsf : entity work.pncle
    port map (urz => r, mf => afvzrvg, sfdd => hogjzd, wc => qmktvl);
  
  -- Single-driven assignments
  cjnql <= 16#E_4_4#;
  yghlmwrqcc <= FAILURE;
end hhrulpsnd;

entity gtdg is
  port (wbrhbvii : buffer integer_vector(1 downto 1));
end gtdg;

architecture oabqxkpfd of gtdg is
  signal edu : time;
  signal jyfj : time;
  signal vefifvs : time;
  signal tgn : time;
  signal lf : time;
  signal mncspdsdl : time;
  signal srps : real;
  signal trth : integer;
  signal xsyaow : severity_level;
  signal qv : real;
  signal yjnnqrq : real;
  signal cxtoozsmj : time;
  signal ehaekcckg : time;
  signal vwtmfnpqu : time;
  signal f : real;
begin
  do : entity work.pncle
    port map (urz => f, mf => vwtmfnpqu, sfdd => ehaekcckg, wc => cxtoozsmj);
  nksyt : entity work.brre
    port map (it => yjnnqrq, r => qv, yghlmwrqcc => xsyaow, cjnql => trth);
  lobp : entity work.pncle
    port map (urz => srps, mf => mncspdsdl, sfdd => lf, wc => tgn);
  o : entity work.pncle
    port map (urz => yjnnqrq, mf => vefifvs, sfdd => jyfj, wc => edu);
end oabqxkpfd;

entity jcske is
  port (qdzsoo : out integer; flaodjsi : buffer bit_vector(4 to 0); pfcjro : in time; leawyccd : linkage character);
end jcske;

architecture dqyjspde of jcske is
  signal zuhyjhanpd : time;
  signal zqlbf : time;
  signal sowh : time;
  signal keezrdba : real;
  signal nmdbgcbvv : time;
  signal wpfnorxi : time;
  signal swpnjgqxdx : time;
  signal okxxjuyt : real;
begin
  wc : entity work.pncle
    port map (urz => okxxjuyt, mf => swpnjgqxdx, sfdd => wpfnorxi, wc => nmdbgcbvv);
  hfcpr : entity work.pncle
    port map (urz => keezrdba, mf => sowh, sfdd => zqlbf, wc => zuhyjhanpd);
  
  -- Single-driven assignments
  flaodjsi <= (others => '0');
  qdzsoo <= qdzsoo;
end dqyjspde;



-- Seed after: 10188052844014908338,7198033922882419595
