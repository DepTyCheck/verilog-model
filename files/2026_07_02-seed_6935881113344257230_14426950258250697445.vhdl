-- Seed: 6935881113344257230,14426950258250697445

use std.reflection.all;

entity o is
  port (uzp : in severity_level; arjsb : buffer time; yegecbory : inout record_value_mirror);
end o;

architecture lquy of o is
  
begin
  -- Single-driven assignments
  arjsb <= arjsb;
end lquy;

entity s is
  port (noadqikp : in integer);
end s;

use std.reflection.all;

architecture qh of s is
  shared variable ahxc : record_value_mirror;
  signal ihmveh : time;
  shared variable zmmg : record_value_mirror;
  signal u : time;
  shared variable od : record_value_mirror;
  signal ai : time;
  signal zgsq : severity_level;
  shared variable kxt : record_value_mirror;
  signal r : time;
  signal ogydqqibd : severity_level;
begin
  dnkwhl : entity work.o
    port map (uzp => ogydqqibd, arjsb => r, yegecbory => kxt);
  rokyaamw : entity work.o
    port map (uzp => zgsq, arjsb => ai, yegecbory => od);
  lgvoltvy : entity work.o
    port map (uzp => ogydqqibd, arjsb => u, yegecbory => zmmg);
  jhlucjnhk : entity work.o
    port map (uzp => ogydqqibd, arjsb => ihmveh, yegecbory => ahxc);
  
  -- Single-driven assignments
  zgsq <= FAILURE;
  ogydqqibd <= ogydqqibd;
end qh;

use std.reflection.all;

entity putk is
  port (kcc : inout floating_subtype_mirror; vbhv : inout record_value_mirror; wfscj : inout array_subtype_mirror);
end putk;

use std.reflection.all;

architecture mx of putk is
  signal oxfgar : time;
  signal wbybgmiaj : severity_level;
  signal ystaszf : integer;
  shared variable kzmxg : record_value_mirror;
  signal cn : time;
  shared variable hfqh : record_value_mirror;
  signal samqkopju : time;
  signal penxsvezp : severity_level;
begin
  fwgdmin : entity work.o
    port map (uzp => penxsvezp, arjsb => samqkopju, yegecbory => hfqh);
  mt : entity work.o
    port map (uzp => penxsvezp, arjsb => cn, yegecbory => kzmxg);
  mjmq : entity work.s
    port map (noadqikp => ystaszf);
  pfyhqsdu : entity work.o
    port map (uzp => wbybgmiaj, arjsb => oxfgar, yegecbory => vbhv);
  
  -- Single-driven assignments
  wbybgmiaj <= wbybgmiaj;
  penxsvezp <= penxsvezp;
end mx;

use std.reflection.all;

entity je is
  port (sw : linkage character; ivj : inout time; w : inout value_mirror; vjojbzms : inout physical_subtype_mirror);
end je;

use std.reflection.all;

architecture fkyqxa of je is
  shared variable iyqmymnva : record_value_mirror;
  signal zoc : severity_level;
begin
  fuodtoamd : entity work.o
    port map (uzp => zoc, arjsb => ivj, yegecbory => iyqmymnva);
  
  -- Single-driven assignments
  zoc <= zoc;
end fkyqxa;



-- Seed after: 16703377492388653958,14426950258250697445
