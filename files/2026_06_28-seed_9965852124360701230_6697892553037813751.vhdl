-- Seed: 9965852124360701230,6697892553037813751

library ieee;
use ieee.std_logic_1164.all;

entity mletoz is
  port (tx : out bit; o : linkage std_logic_vector(3 downto 0); dakodhfzqo : in std_logic_vector(0 to 2));
end mletoz;

architecture gszmkap of mletoz is
  
begin
  -- Single-driven assignments
  tx <= '1';
end gszmkap;

entity bizoontyjq is
  port (fvssufke : buffer integer);
end bizoontyjq;

library ieee;
use ieee.std_logic_1164.all;

architecture beimlssgm of bizoontyjq is
  signal cmpdu : bit;
  signal apryd : std_logic_vector(0 to 2);
  signal ho : bit;
  signal sc : std_logic_vector(0 to 2);
  signal webagmngsq : std_logic_vector(3 downto 0);
  signal mcghy : bit;
begin
  g : entity work.mletoz
    port map (tx => mcghy, o => webagmngsq, dakodhfzqo => sc);
  vggw : entity work.mletoz
    port map (tx => ho, o => webagmngsq, dakodhfzqo => apryd);
  tkxphdjgdq : entity work.mletoz
    port map (tx => cmpdu, o => webagmngsq, dakodhfzqo => apryd);
  
  -- Single-driven assignments
  fvssufke <= 2_0;
  
  -- Multi-driven assignments
  webagmngsq <= ('1', 'Z', 'X', 'X');
end beimlssgm;



-- Seed after: 17430779064247850037,6697892553037813751
