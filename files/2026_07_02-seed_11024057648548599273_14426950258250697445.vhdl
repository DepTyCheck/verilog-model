-- Seed: 11024057648548599273,14426950258250697445

use std.reflection.all;

entity gldevh is
  port (xz : inout file_subtype_mirror; ywwuuund : inout subtype_mirror);
end gldevh;

architecture fbsavjcb of gldevh is
  
begin
  
end fbsavjcb;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity u is
  port (cqpb : inout record_value_mirror; uefgh : buffer time; srwkgituaf : buffer integer; nveymerb : out std_logic);
end u;

use std.reflection.all;

architecture otznobckt of u is
  shared variable vgzioiluza : subtype_mirror;
  shared variable z : file_subtype_mirror;
  shared variable myd : subtype_mirror;
  shared variable uegpnrynmj : file_subtype_mirror;
  shared variable e : subtype_mirror;
  shared variable prjwdd : file_subtype_mirror;
begin
  cg : entity work.gldevh
    port map (xz => prjwdd, ywwuuund => e);
  zdc : entity work.gldevh
    port map (xz => uegpnrynmj, ywwuuund => myd);
  ymtp : entity work.gldevh
    port map (xz => z, ywwuuund => vgzioiluza);
  
  -- Single-driven assignments
  srwkgituaf <= 441;
  
  -- Multi-driven assignments
  nveymerb <= 'Z';
  nveymerb <= nveymerb;
  nveymerb <= 'Z';
  nveymerb <= nveymerb;
end otznobckt;

use std.reflection.all;

entity abgvol is
  port (tesnkcnb : inout floating_subtype_mirror; dvttbj : out integer_vector(0 downto 1));
end abgvol;

use std.reflection.all;

architecture bh of abgvol is
  shared variable rvtixkeija : subtype_mirror;
  shared variable qfocfh : file_subtype_mirror;
  shared variable qontx : subtype_mirror;
  shared variable txaxbqpd : file_subtype_mirror;
begin
  l : entity work.gldevh
    port map (xz => txaxbqpd, ywwuuund => qontx);
  qkgds : entity work.gldevh
    port map (xz => qfocfh, ywwuuund => rvtixkeija);
  
  -- Single-driven assignments
  dvttbj <= dvttbj;
end bh;

entity ltzc is
  port (l : linkage real_vector(1 to 1));
end ltzc;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture yiblyl of ltzc is
  signal cqriycffh : integer_vector(0 downto 1);
  shared variable svnng : floating_subtype_mirror;
  signal uceqk : integer;
  signal lzadrmvzsg : time;
  shared variable tdnxw : record_value_mirror;
  signal kro : std_logic;
  signal rtrukb : integer;
  signal lcmv : time;
  shared variable xtceubpgqz : record_value_mirror;
begin
  iwcnxywwc : entity work.u
    port map (cqpb => xtceubpgqz, uefgh => lcmv, srwkgituaf => rtrukb, nveymerb => kro);
  whwtkbc : entity work.u
    port map (cqpb => tdnxw, uefgh => lzadrmvzsg, srwkgituaf => uceqk, nveymerb => kro);
  kkuvmlef : entity work.abgvol
    port map (tesnkcnb => svnng, dvttbj => cqriycffh);
  
  -- Multi-driven assignments
  kro <= 'W';
  kro <= 'U';
  kro <= 'L';
  kro <= kro;
end yiblyl;



-- Seed after: 8869341101154138475,14426950258250697445
