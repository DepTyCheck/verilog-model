// Seed: 8136274373842248867,4848891981775420843

module nuahhm (input tri logic [1:4] s, input byte fopa, inout tri logic frqfnl [3:3], inout trireg logic [0:0][2:0][0:0][4:2] m);
  xnor geluxcnpyz(ahyrlffdqu, fopa, fopa);
  // warning: implicit conversion of port connection truncates from 8 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   byte fopa -> logic fopa
  //
  // warning: implicit conversion of port connection truncates from 8 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   byte fopa -> logic fopa
  
  nand ntnxttvrmc(kwzrftyvvi, ahyrlffdqu, boqliqr);
  
  
  // Multi-driven assignments
  assign ahyrlffdqu = 'b1;
  assign ahyrlffdqu = ahyrlffdqu;
endmodule: nuahhm

module xim (input bit [0:1][4:4][1:2] bjmeagv, output tri logic [3:0] fxa [0:2][3:4][0:1], input bit [2:0][3:3] amcj);
  // Unpacked net declarations
  tri logic y [3:3];
  
  not pduoyrzle(isglmp, bjmeagv);
  // warning: implicit conversion of port connection truncates from 4 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [0:1][4:4][1:2] bjmeagv -> logic bjmeagv
  
  nand inhizu(isglmp, jdmaephpgi, vnbgmkhxfj);
  
  and h(zhnzakpedr, psen, jdmaephpgi);
  
  nuahhm st(.s(isglmp), .fopa(jdmaephpgi), .frqfnl(y), .m(sphs));
  // warning: implicit conversion of port connection expands from 1 to 4 bits
  //   wire logic isglmp -> tri logic [1:4] s
  //
  // warning: implicit conversion of port connection expands from 1 to 8 bits
  // warning: implicit conversion changes signedness from unsigned to signed
  // warning: implicit conversion changes possible bit states from 4-state to 2-state
  //   wire logic jdmaephpgi -> byte fopa
  //
  // warning: implicit conversion of port connection expands from 1 to 9 bits
  //   wire logic sphs -> trireg logic [0:0][2:0][0:0][4:2] m
  
endmodule: xim



// Seed after: 7824586768154428695,4848891981775420843
