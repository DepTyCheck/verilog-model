-- Seed: 11251003678349088997,13694093582652240945

library ieee;
use ieee.std_logic_1164.all;

entity tite is
  port (m : linkage std_logic; uyapu : linkage time; trad : inout string(4 to 3); x : buffer std_logic_vector(2 to 0));
end tite;

architecture zcoed of tite is
  
begin
  -- Multi-driven assignments
  x <= "";
end zcoed;

library ieee;
use ieee.std_logic_1164.all;

entity bg is
  port (rwnjghoil : out integer_vector(3 downto 1); wodlyiz : in std_logic_vector(3 downto 1); vz : out integer_vector(3 downto 2));
end bg;

library ieee;
use ieee.std_logic_1164.all;

architecture hcmwi of bg is
  signal gmxzngpde : std_logic_vector(2 to 0);
  signal hb : string(4 to 3);
  signal cbbayf : time;
  signal ctindzeuwt : std_logic;
  signal mutpgucuch : std_logic_vector(2 to 0);
  signal zleqt : string(4 to 3);
  signal bebgnpxxs : time;
  signal uhjxmov : std_logic_vector(2 to 0);
  signal vcyou : string(4 to 3);
  signal pwpiipei : time;
  signal quusrvacg : std_logic;
begin
  eecvwtd : entity work.tite
    port map (m => quusrvacg, uyapu => pwpiipei, trad => vcyou, x => uhjxmov);
  yu : entity work.tite
    port map (m => quusrvacg, uyapu => bebgnpxxs, trad => zleqt, x => mutpgucuch);
  jc : entity work.tite
    port map (m => ctindzeuwt, uyapu => cbbayf, trad => hb, x => gmxzngpde);
  
  -- Single-driven assignments
  rwnjghoil <= (13040, 2_1_2, 2#1#);
  
  -- Multi-driven assignments
  mutpgucuch <= (others => '0');
end hcmwi;

entity rgbz is
  port (jxs : inout string(5 to 1); jmmlnl : linkage boolean; flial : buffer real; jdcoc : out boolean_vector(2 to 0));
end rgbz;

library ieee;
use ieee.std_logic_1164.all;

architecture xztzklh of rgbz is
  signal mkum : std_logic_vector(2 to 0);
  signal yr : time;
  signal hihbqrlj : std_logic;
  signal udw : integer_vector(3 downto 2);
  signal lfzqw : std_logic_vector(3 downto 1);
  signal rpvdjn : integer_vector(3 downto 1);
  signal ukix : string(4 to 3);
  signal cut : time;
  signal pxi : std_logic_vector(2 to 0);
  signal ospniln : string(4 to 3);
  signal zuijk : time;
  signal nsqqrah : std_logic;
begin
  an : entity work.tite
    port map (m => nsqqrah, uyapu => zuijk, trad => ospniln, x => pxi);
  loqgufgf : entity work.tite
    port map (m => nsqqrah, uyapu => cut, trad => ukix, x => pxi);
  kcxtny : entity work.bg
    port map (rwnjghoil => rpvdjn, wodlyiz => lfzqw, vz => udw);
  qvfmtzftex : entity work.tite
    port map (m => hihbqrlj, uyapu => yr, trad => jxs, x => mkum);
  
  -- Single-driven assignments
  flial <= 2_1_4.1_1_1_1;
  
  -- Multi-driven assignments
  pxi <= "";
end xztzklh;

library ieee;
use ieee.std_logic_1164.all;

entity bvllmlcgw is
  port (tffb : linkage std_logic_vector(3 downto 2); foodumreht : inout time; kkww : in real; kkz : buffer real);
end bvllmlcgw;

library ieee;
use ieee.std_logic_1164.all;

architecture chfvl of bvllmlcgw is
  signal agiakh : std_logic_vector(2 to 0);
  signal gizauktgk : string(4 to 3);
  signal v : std_logic_vector(2 to 0);
  signal dadhckjh : string(4 to 3);
  signal jadcgom : time;
  signal qrfo : std_logic;
  signal oy : boolean_vector(2 to 0);
  signal dhmlflgwmt : real;
  signal qbuuau : boolean;
  signal ydp : string(5 to 1);
begin
  wjpaxnvy : entity work.rgbz
    port map (jxs => ydp, jmmlnl => qbuuau, flial => dhmlflgwmt, jdcoc => oy);
  bi : entity work.tite
    port map (m => qrfo, uyapu => jadcgom, trad => dadhckjh, x => v);
  fo : entity work.tite
    port map (m => qrfo, uyapu => foodumreht, trad => gizauktgk, x => agiakh);
  
  -- Single-driven assignments
  kkz <= 16#0_1_9_1.2D735#;
  
  -- Multi-driven assignments
  agiakh <= (others => '0');
  qrfo <= 'Z';
  qrfo <= 'L';
  qrfo <= 'W';
end chfvl;



-- Seed after: 9697862761353047077,13694093582652240945
