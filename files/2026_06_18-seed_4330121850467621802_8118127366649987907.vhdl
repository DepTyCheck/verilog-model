-- Seed: 4330121850467621802,8118127366649987907

library ieee;
use ieee.std_logic_1164.all;

entity gqg is
  port (cri : linkage std_logic_vector(1 to 2); qqlfcbnwen : linkage severity_level; u : linkage bit);
end gqg;

architecture ivlwzchg of gqg is
  
begin
  
end ivlwzchg;

library ieee;
use ieee.std_logic_1164.all;

entity ohdhe is
  port (fvu : linkage std_logic);
end ohdhe;

library ieee;
use ieee.std_logic_1164.all;

architecture mdopqls of ohdhe is
  signal yfmtkpq : bit;
  signal bigaoyryox : severity_level;
  signal uaybffsi : std_logic_vector(1 to 2);
  signal ybs : bit;
  signal ezmw : severity_level;
  signal cos : bit;
  signal fjrckkkg : severity_level;
  signal ptjfhmoh : std_logic_vector(1 to 2);
  signal rvx : bit;
  signal bjtld : severity_level;
  signal trdx : std_logic_vector(1 to 2);
begin
  unlmv : entity work.gqg
    port map (cri => trdx, qqlfcbnwen => bjtld, u => rvx);
  wtcrioyonu : entity work.gqg
    port map (cri => ptjfhmoh, qqlfcbnwen => fjrckkkg, u => cos);
  rub : entity work.gqg
    port map (cri => trdx, qqlfcbnwen => ezmw, u => ybs);
  luwfdwwnha : entity work.gqg
    port map (cri => uaybffsi, qqlfcbnwen => bigaoyryox, u => yfmtkpq);
end mdopqls;



-- Seed after: 11662000674058042814,8118127366649987907
