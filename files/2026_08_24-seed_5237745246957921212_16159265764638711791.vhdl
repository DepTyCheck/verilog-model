-- Seed: 5237745246957921212,16159265764638711791

library ieee;
use ieee.std_logic_1164.all;

entity mlglsc is
  port (pvwn : out integer; gp : out std_logic_vector(4 to 2); x : inout real; xvlwrmigc : buffer real_vector(1 downto 2));
end mlglsc;

architecture qnu of mlglsc is
  
begin
  -- Single-driven assignments
  x <= 1_2_3.1001;
  pvwn <= pvwn;
  xvlwrmigc <= xvlwrmigc;
  
  -- Multi-driven assignments
  gp <= "";
  gp <= (others => '0');
  gp <= gp;
  gp <= "";
end qnu;

library ieee;
use ieee.std_logic_1164.all;

entity mgjvm is
  port (fpdgpa : linkage std_logic; hfdivktsod : out std_logic; ticgxlrf : buffer boolean);
end mgjvm;

library ieee;
use ieee.std_logic_1164.all;

architecture izad of mgjvm is
  signal a : real_vector(1 downto 2);
  signal lvbyfpohc : real;
  signal j : std_logic_vector(4 to 2);
  signal vldklc : integer;
  signal ybkih : real_vector(1 downto 2);
  signal dnezbv : real;
  signal k : integer;
  signal xcdinqoxos : real_vector(1 downto 2);
  signal ik : real;
  signal ygokfrkaek : integer;
  signal gfnypiof : real_vector(1 downto 2);
  signal glrgs : real;
  signal mphpv : std_logic_vector(4 to 2);
  signal vezydcz : integer;
begin
  qul : entity work.mlglsc
    port map (pvwn => vezydcz, gp => mphpv, x => glrgs, xvlwrmigc => gfnypiof);
  xrreht : entity work.mlglsc
    port map (pvwn => ygokfrkaek, gp => mphpv, x => ik, xvlwrmigc => xcdinqoxos);
  lhcso : entity work.mlglsc
    port map (pvwn => k, gp => mphpv, x => dnezbv, xvlwrmigc => ybkih);
  lcoqnote : entity work.mlglsc
    port map (pvwn => vldklc, gp => j, x => lvbyfpohc, xvlwrmigc => a);
  
  -- Single-driven assignments
  ticgxlrf <= TRUE;
  
  -- Multi-driven assignments
  hfdivktsod <= hfdivktsod;
end izad;

entity etadae is
  port (ip : out bit_vector(1 downto 3); hnpfp : in integer);
end etadae;

architecture jshmp of etadae is
  
begin
  -- Single-driven assignments
  ip <= ip;
end jshmp;

entity ma is
  port (rruiuorec : linkage time);
end ma;

library ieee;
use ieee.std_logic_1164.all;

architecture xsxzz of ma is
  signal fozvst : boolean;
  signal g : std_logic;
  signal pqvfpzls : real_vector(1 downto 2);
  signal shdrbik : real;
  signal eg : real_vector(1 downto 2);
  signal bhptkty : real;
  signal rvk : std_logic_vector(4 to 2);
  signal xitzvi : integer;
  signal mpmyxaljho : integer;
  signal iabwr : bit_vector(1 downto 3);
begin
  u : entity work.etadae
    port map (ip => iabwr, hnpfp => mpmyxaljho);
  bfa : entity work.mlglsc
    port map (pvwn => xitzvi, gp => rvk, x => bhptkty, xvlwrmigc => eg);
  xxtw : entity work.mlglsc
    port map (pvwn => mpmyxaljho, gp => rvk, x => shdrbik, xvlwrmigc => pqvfpzls);
  mksjoazpk : entity work.mgjvm
    port map (fpdgpa => g, hfdivktsod => g, ticgxlrf => fozvst);
  
  -- Multi-driven assignments
  rvk <= rvk;
  g <= '0';
  g <= g;
  g <= '0';
end xsxzz;



-- Seed after: 7565312221778840293,16159265764638711791
