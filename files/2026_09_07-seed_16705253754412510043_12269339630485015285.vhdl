-- Seed: 16705253754412510043,12269339630485015285

library ieee;
use ieee.std_logic_1164.all;

entity djevly is
  port (exyv : in real; dkt : out std_logic; eixt : linkage time; v : in real);
end djevly;

architecture cbfxb of djevly is
  
begin
  -- Multi-driven assignments
  dkt <= 'H';
end cbfxb;

entity yxhobrf is
  port (ptwigx : inout integer);
end yxhobrf;

library ieee;
use ieee.std_logic_1164.all;

architecture aa of yxhobrf is
  signal yxly : real;
  signal cnmsh : time;
  signal xsu : std_logic;
  signal egnyqwctud : real;
  signal bgzbfjpnv : time;
  signal yocu : std_logic;
  signal fcx : real;
  signal dspc : real;
  signal qsxnlqrkxs : time;
  signal h : std_logic;
  signal jvkie : time;
  signal jsf : std_logic;
  signal wtxoj : real;
begin
  rdvnbn : entity work.djevly
    port map (exyv => wtxoj, dkt => jsf, eixt => jvkie, v => wtxoj);
  sgpwqfkyb : entity work.djevly
    port map (exyv => wtxoj, dkt => h, eixt => qsxnlqrkxs, v => dspc);
  otg : entity work.djevly
    port map (exyv => fcx, dkt => yocu, eixt => bgzbfjpnv, v => wtxoj);
  vhht : entity work.djevly
    port map (exyv => egnyqwctud, dkt => xsu, eixt => cnmsh, v => yxly);
  
  -- Single-driven assignments
  ptwigx <= ptwigx;
  dspc <= 1_1_0_3_4.3;
  wtxoj <= wtxoj;
end aa;

entity chy is
  port (tn : inout time);
end chy;

library ieee;
use ieee.std_logic_1164.all;

architecture toi of chy is
  signal mhhqa : integer;
  signal blxfzacy : real;
  signal gxwpiruacq : time;
  signal qks : std_logic;
  signal cfuypskql : real;
begin
  vnsrxngtlk : entity work.djevly
    port map (exyv => cfuypskql, dkt => qks, eixt => gxwpiruacq, v => blxfzacy);
  zewqhvojq : entity work.yxhobrf
    port map (ptwigx => mhhqa);
  
  -- Single-driven assignments
  tn <= gxwpiruacq;
  blxfzacy <= cfuypskql;
  cfuypskql <= 130.04;
  
  -- Multi-driven assignments
  qks <= 'H';
  qks <= qks;
  qks <= 'X';
end toi;

entity vwdh is
  port (pvbxsmdcxb : linkage integer; fbsicezgp : out real);
end vwdh;

library ieee;
use ieee.std_logic_1164.all;

architecture wermwjjjym of vwdh is
  signal vniqenz : real;
  signal o : time;
  signal ygsuefpc : std_logic;
  signal zogolvepiw : real;
  signal kdwn : time;
begin
  dbxjm : entity work.chy
    port map (tn => kdwn);
  jfcriym : entity work.djevly
    port map (exyv => zogolvepiw, dkt => ygsuefpc, eixt => o, v => vniqenz);
  
  -- Single-driven assignments
  zogolvepiw <= fbsicezgp;
  vniqenz <= 16#C.43CC3#;
  fbsicezgp <= fbsicezgp;
  
  -- Multi-driven assignments
  ygsuefpc <= 'U';
end wermwjjjym;



-- Seed after: 17449805365926919137,12269339630485015285
