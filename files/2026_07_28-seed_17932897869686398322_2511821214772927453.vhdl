-- Seed: 17932897869686398322,2511821214772927453

entity psfiw is
  port (ssh : in integer);
end psfiw;

architecture xjdraov of psfiw is
  
begin
  
end xjdraov;

library ieee;
use ieee.std_logic_1164.all;

entity wtc is
  port (kpv : buffer std_logic; fh : buffer bit_vector(0 to 1));
end wtc;

architecture ypluwctpcp of wtc is
  signal afavk : integer;
  signal voa : integer;
begin
  kmf : entity work.psfiw
    port map (ssh => voa);
  baekg : entity work.psfiw
    port map (ssh => afavk);
  
  -- Single-driven assignments
  voa <= 1;
  fh <= ('0', '0');
  
  -- Multi-driven assignments
  kpv <= kpv;
  kpv <= 'X';
  kpv <= kpv;
  kpv <= kpv;
end ypluwctpcp;



-- Seed after: 15723820760981973591,2511821214772927453
