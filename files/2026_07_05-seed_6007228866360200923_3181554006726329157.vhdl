-- Seed: 6007228866360200923,3181554006726329157

entity bybn is
  port (yqab : buffer real);
end bybn;

architecture nq of bybn is
  
begin
  -- Single-driven assignments
  yqab <= 31333.4_0_4_1;
end nq;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity s is
  port (fjp : linkage std_logic; rla : linkage real; xbwsca : inout subtype_mirror);
end s;

architecture wt of s is
  signal tvt : real;
  signal wcdaoftrgr : real;
  signal by : real;
  signal pgr : real;
begin
  h : entity work.bybn
    port map (yqab => pgr);
  jgzqtka : entity work.bybn
    port map (yqab => by);
  gyzzmhcd : entity work.bybn
    port map (yqab => wcdaoftrgr);
  qyhwdvwcba : entity work.bybn
    port map (yqab => tvt);
end wt;

use std.reflection.all;

entity abegyi is
  port (pfkwgsmf : inout enumeration_subtype_mirror);
end abegyi;

architecture eptpbicblx of abegyi is
  signal gskrvf : real;
  signal aubxqzyrah : real;
  signal thrgh : real;
begin
  cjxcpa : entity work.bybn
    port map (yqab => thrgh);
  urautyrw : entity work.bybn
    port map (yqab => aubxqzyrah);
  qmwlw : entity work.bybn
    port map (yqab => gskrvf);
end eptpbicblx;

entity okmain is
  port (taum : buffer severity_level);
end okmain;

architecture qtjjf of okmain is
  
begin
  -- Single-driven assignments
  taum <= WARNING;
end qtjjf;



-- Seed after: 17918737711611639368,3181554006726329157
