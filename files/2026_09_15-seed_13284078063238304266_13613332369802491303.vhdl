-- Seed: 13284078063238304266,13613332369802491303

entity jhj is
  port (mlzimfmuu : buffer integer; gkyibcu : out real; ydjqzuhjf : linkage boolean_vector(3 downto 0));
end jhj;

architecture jfhe of jhj is
  
begin
  -- Single-driven assignments
  mlzimfmuu <= 8#4346#;
  gkyibcu <= 8#1_3_5.2_3_5#;
end jfhe;

library ieee;
use ieee.std_logic_1164.all;

entity mmnbqv is
  port (eydnpmp : inout time; yeay : buffer std_logic_vector(3 downto 1));
end mmnbqv;

architecture gutcoagv of mmnbqv is
  signal slckxsqwhl : boolean_vector(3 downto 0);
  signal h : real;
  signal cqakhagi : integer;
  signal etjc : boolean_vector(3 downto 0);
  signal svtkrvc : real;
  signal hadirwc : integer;
begin
  hzmitglh : entity work.jhj
    port map (mlzimfmuu => hadirwc, gkyibcu => svtkrvc, ydjqzuhjf => etjc);
  ua : entity work.jhj
    port map (mlzimfmuu => cqakhagi, gkyibcu => h, ydjqzuhjf => slckxsqwhl);
  
  -- Multi-driven assignments
  yeay <= yeay;
  yeay <= "0W0";
  yeay <= ('W', 'X', 'L');
  yeay <= ('H', '0', '0');
end gutcoagv;

entity agltx is
  port (revzdsl : in severity_level; fhecb : linkage boolean; d : inout real; pny : in real_vector(2 downto 2));
end agltx;

architecture zbunlj of agltx is
  
begin
  
end zbunlj;

library ieee;
use ieee.std_logic_1164.all;

entity meozejix is
  port (ff : linkage std_logic);
end meozejix;

architecture kvpvcf of meozejix is
  signal efyo : boolean_vector(3 downto 0);
  signal wduxjdjgbo : real;
  signal oysr : integer;
  signal yxcfm : real_vector(2 downto 2);
  signal rvarl : real;
  signal lkhgrhshd : boolean;
  signal rrwddkuxp : severity_level;
begin
  r : entity work.agltx
    port map (revzdsl => rrwddkuxp, fhecb => lkhgrhshd, d => rvarl, pny => yxcfm);
  wsitgpz : entity work.jhj
    port map (mlzimfmuu => oysr, gkyibcu => wduxjdjgbo, ydjqzuhjf => efyo);
  
  -- Single-driven assignments
  rrwddkuxp <= rrwddkuxp;
  yxcfm <= yxcfm;
end kvpvcf;



-- Seed after: 9127683409075510363,13613332369802491303
