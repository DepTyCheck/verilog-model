-- Seed: 17963772126896499204,3566912872917928779

use std.reflection.all;

entity psuwn is
  port (i : inout floating_subtype_mirror);
end psuwn;

architecture cdxahyi of psuwn is
  
begin
  
end cdxahyi;

library ieee;
use ieee.std_logic_1164.all;

entity znpnwfql is
  port (lzyrry : in std_logic_vector(3 downto 4); weiglsjcod : linkage std_logic);
end znpnwfql;

use std.reflection.all;

architecture rsaivbsa of znpnwfql is
  shared variable pq : floating_subtype_mirror;
  shared variable etztlxa : floating_subtype_mirror;
  shared variable a : floating_subtype_mirror;
  shared variable kdxlblrct : floating_subtype_mirror;
begin
  niiiawfw : entity work.psuwn
    port map (i => kdxlblrct);
  tleenavp : entity work.psuwn
    port map (i => a);
  kkpvbl : entity work.psuwn
    port map (i => etztlxa);
  pyxmjffdh : entity work.psuwn
    port map (i => pq);
end rsaivbsa;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity exvp is
  port (sf : in std_logic; bcugq : inout file_value_mirror; lvjc : inout integer; fual : linkage string(2 to 3));
end exvp;

architecture tyhxhz of exvp is
  
begin
  
end tyhxhz;

use std.reflection.all;

entity rzepa is
  port (xhm : inout file_subtype_mirror; ojwsxdj : buffer string(5 downto 2); kdlayyxyb : linkage boolean; olvwasqmqy : inout array_value_mirror);
end rzepa;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture bwg of rzepa is
  signal k : std_logic;
  signal nhj : std_logic_vector(3 downto 4);
  shared variable hczhox : floating_subtype_mirror;
  signal plf : string(2 to 3);
  signal tjth : integer;
  shared variable gmihusdhe : file_value_mirror;
  signal z : std_logic;
  shared variable nwqg : floating_subtype_mirror;
begin
  knfletd : entity work.psuwn
    port map (i => nwqg);
  bqbx : entity work.exvp
    port map (sf => z, bcugq => gmihusdhe, lvjc => tjth, fual => plf);
  lh : entity work.psuwn
    port map (i => hczhox);
  nffantot : entity work.znpnwfql
    port map (lzyrry => nhj, weiglsjcod => k);
  
  -- Single-driven assignments
  ojwsxdj <= ('v', 't', 's', 'k');
  
  -- Multi-driven assignments
  nhj <= (others => '0');
end bwg;



-- Seed after: 12586760568732452473,3566912872917928779
