-- Seed: 1174859091749109690,2338584220606314193

entity ieokwihv is
  port (fnlm : buffer real; kpgn : linkage bit_vector(0 downto 2); ntqykjx : out real);
end ieokwihv;

architecture cov of ieokwihv is
  
begin
  -- Single-driven assignments
  ntqykjx <= 2#10.1#;
  fnlm <= ntqykjx;
end cov;

library ieee;
use ieee.std_logic_1164.all;

entity ihcjb is
  port (sfizm : buffer integer; xfcdneok : out std_logic; aj : out time);
end ihcjb;

architecture tktyo of ihcjb is
  signal ribznu : real;
  signal rr : bit_vector(0 downto 2);
  signal tffkl : real;
  signal sde : real;
  signal fmixgwkgti : bit_vector(0 downto 2);
  signal lahe : real;
  signal wjy : real;
  signal voirfdojzf : bit_vector(0 downto 2);
  signal ypkepykpkr : real;
begin
  e : entity work.ieokwihv
    port map (fnlm => ypkepykpkr, kpgn => voirfdojzf, ntqykjx => wjy);
  xmeyso : entity work.ieokwihv
    port map (fnlm => lahe, kpgn => fmixgwkgti, ntqykjx => sde);
  vavueznnsx : entity work.ieokwihv
    port map (fnlm => tffkl, kpgn => rr, ntqykjx => ribznu);
  
  -- Single-driven assignments
  sfizm <= sfizm;
  aj <= 0_0 ms;
  
  -- Multi-driven assignments
  xfcdneok <= xfcdneok;
  xfcdneok <= 'W';
  xfcdneok <= xfcdneok;
  xfcdneok <= xfcdneok;
end tktyo;

entity qsijf is
  port (ekpkkhj : in bit);
end qsijf;

architecture vd of qsijf is
  signal wdhy : real;
  signal kdoqg : bit_vector(0 downto 2);
  signal llt : real;
  signal vnfmzil : real;
  signal irbiwqhif : bit_vector(0 downto 2);
  signal idfkijeyd : real;
  signal igj : real;
  signal kelbxei : bit_vector(0 downto 2);
  signal lux : real;
  signal skdupuc : real;
  signal ot : bit_vector(0 downto 2);
  signal hhrjepx : real;
begin
  pkjheq : entity work.ieokwihv
    port map (fnlm => hhrjepx, kpgn => ot, ntqykjx => skdupuc);
  lbkusvecfs : entity work.ieokwihv
    port map (fnlm => lux, kpgn => kelbxei, ntqykjx => igj);
  lcalhm : entity work.ieokwihv
    port map (fnlm => idfkijeyd, kpgn => irbiwqhif, ntqykjx => vnfmzil);
  mbaabkmnt : entity work.ieokwihv
    port map (fnlm => llt, kpgn => kdoqg, ntqykjx => wdhy);
end vd;

library ieee;
use ieee.std_logic_1164.all;

entity aafhzplgy is
  port (ofe : linkage real; baad : inout std_logic_vector(3 downto 0); qsgphutybh : in character; jdxkdatyh : buffer integer);
end aafhzplgy;

library ieee;
use ieee.std_logic_1164.all;

architecture r of aafhzplgy is
  signal ktnqtqr : time;
  signal dhc : std_logic;
  signal j : bit;
  signal t : real;
  signal kqjlw : bit_vector(0 downto 2);
  signal wzambwxwk : real;
begin
  wysefba : entity work.ieokwihv
    port map (fnlm => wzambwxwk, kpgn => kqjlw, ntqykjx => t);
  l : entity work.qsijf
    port map (ekpkkhj => j);
  wfrsoje : entity work.ihcjb
    port map (sfizm => jdxkdatyh, xfcdneok => dhc, aj => ktnqtqr);
  
  -- Single-driven assignments
  j <= j;
  
  -- Multi-driven assignments
  baad <= baad;
  dhc <= '-';
  dhc <= 'U';
end r;



-- Seed after: 11402504695828732458,2338584220606314193
