-- Seed: 4405640760875476125,8437298063418820479

entity pbvdr is
  port (sepue : buffer severity_level; iofesk : in real);
end pbvdr;

architecture zrtqv of pbvdr is
  
begin
  -- Single-driven assignments
  sepue <= FAILURE;
end zrtqv;

library ieee;
use ieee.std_logic_1164.all;

entity bbhxap is
  port (eoctc : inout real; dtvkdjgwzl : inout std_logic);
end bbhxap;

architecture gc of bbhxap is
  signal r : severity_level;
  signal x : severity_level;
  signal vhivg : severity_level;
begin
  htc : entity work.pbvdr
    port map (sepue => vhivg, iofesk => eoctc);
  xykvkmlw : entity work.pbvdr
    port map (sepue => x, iofesk => eoctc);
  ca : entity work.pbvdr
    port map (sepue => r, iofesk => eoctc);
  
  -- Single-driven assignments
  eoctc <= eoctc;
end gc;

entity eht is
  port (f : buffer boolean; ivbfmioa : out real);
end eht;

architecture pjww of eht is
  signal azqmkxhs : real;
  signal dnydpk : severity_level;
  signal vi : real;
  signal zfgaau : severity_level;
  signal h : severity_level;
  signal dinjo : real;
  signal w : severity_level;
begin
  ymgtltu : entity work.pbvdr
    port map (sepue => w, iofesk => dinjo);
  mgc : entity work.pbvdr
    port map (sepue => h, iofesk => ivbfmioa);
  g : entity work.pbvdr
    port map (sepue => zfgaau, iofesk => vi);
  y : entity work.pbvdr
    port map (sepue => dnydpk, iofesk => azqmkxhs);
end pjww;



-- Seed after: 10117206142318528465,8437298063418820479
