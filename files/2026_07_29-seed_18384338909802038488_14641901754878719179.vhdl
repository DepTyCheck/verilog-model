-- Seed: 18384338909802038488,14641901754878719179

library ieee;
use ieee.std_logic_1164.all;

entity ffcvbv is
  port (ifs : buffer integer_vector(0 to 3); vnorf : out real; tj : inout std_logic);
end ffcvbv;

architecture agfaavfa of ffcvbv is
  
begin
  -- Single-driven assignments
  vnorf <= vnorf;
  ifs <= (24330, 2_2_0, 00201, 2_1_4_4);
  
  -- Multi-driven assignments
  tj <= 'L';
  tj <= tj;
  tj <= '0';
  tj <= 'X';
end agfaavfa;

library ieee;
use ieee.std_logic_1164.all;

entity vy is
  port (xnnzaj : inout integer; wkv : in std_logic; hekobbduz : inout integer; ibxok : linkage integer);
end vy;

library ieee;
use ieee.std_logic_1164.all;

architecture ixg of vy is
  signal s : real;
  signal j : integer_vector(0 to 3);
  signal dr : real;
  signal wdligpvz : integer_vector(0 to 3);
  signal sypdf : std_logic;
  signal lyajgfhee : real;
  signal ji : integer_vector(0 to 3);
  signal gn : std_logic;
  signal yjprzbrnns : real;
  signal zzrzl : integer_vector(0 to 3);
begin
  nzvlth : entity work.ffcvbv
    port map (ifs => zzrzl, vnorf => yjprzbrnns, tj => gn);
  koikaflgyb : entity work.ffcvbv
    port map (ifs => ji, vnorf => lyajgfhee, tj => sypdf);
  tgcuoble : entity work.ffcvbv
    port map (ifs => wdligpvz, vnorf => dr, tj => sypdf);
  ynyk : entity work.ffcvbv
    port map (ifs => j, vnorf => s, tj => gn);
  
  -- Single-driven assignments
  hekobbduz <= hekobbduz;
  xnnzaj <= hekobbduz;
  
  -- Multi-driven assignments
  gn <= 'Z';
end ixg;

entity hzvycumiw is
  port (kjmruv : inout bit; rkmgaogiw : in time);
end hzvycumiw;

library ieee;
use ieee.std_logic_1164.all;

architecture lqolu of hzvycumiw is
  signal rasbkrciyi : std_logic;
  signal lgvchkt : real;
  signal kxjnieumgl : integer_vector(0 to 3);
  signal ink : real;
  signal jqwyansa : integer_vector(0 to 3);
  signal bljn : std_logic;
  signal ys : real;
  signal ane : integer_vector(0 to 3);
  signal twackcx : std_logic;
  signal gqwhccd : real;
  signal ziaocgqkm : integer_vector(0 to 3);
begin
  ejxgltwx : entity work.ffcvbv
    port map (ifs => ziaocgqkm, vnorf => gqwhccd, tj => twackcx);
  pbg : entity work.ffcvbv
    port map (ifs => ane, vnorf => ys, tj => bljn);
  ydmudcngi : entity work.ffcvbv
    port map (ifs => jqwyansa, vnorf => ink, tj => twackcx);
  ah : entity work.ffcvbv
    port map (ifs => kxjnieumgl, vnorf => lgvchkt, tj => rasbkrciyi);
  
  -- Multi-driven assignments
  bljn <= '-';
end lqolu;

library ieee;
use ieee.std_logic_1164.all;

entity vq is
  port (fhvhsv : in integer; jgg : buffer real; gi : out std_logic_vector(0 to 1));
end vq;

architecture qjlgzguhn of vq is
  
begin
  -- Single-driven assignments
  jgg <= jgg;
  
  -- Multi-driven assignments
  gi <= ('W', 'L');
  gi <= gi;
  gi <= ('W', 'L');
end qjlgzguhn;



-- Seed after: 13608137317673312117,14641901754878719179
