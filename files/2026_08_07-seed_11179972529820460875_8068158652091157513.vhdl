-- Seed: 11179972529820460875,8068158652091157513

library ieee;
use ieee.std_logic_1164.all;

entity snsrdc is
  port (eqtdxoj : linkage character; knrxa : inout string(3 to 1); a : linkage std_logic_vector(1 to 1));
end snsrdc;

architecture zmnk of snsrdc is
  
begin
  
end zmnk;

entity pexqstsm is
  port (rghgnrjn : buffer integer; tnmvph : buffer boolean; kmhaeusr : in time);
end pexqstsm;

library ieee;
use ieee.std_logic_1164.all;

architecture uurnome of pexqstsm is
  signal dnswkrz : std_logic_vector(1 to 1);
  signal n : string(3 to 1);
  signal u : character;
  signal xxrddkf : string(3 to 1);
  signal kmdoa : character;
  signal cfdalfwtp : std_logic_vector(1 to 1);
  signal urplz : string(3 to 1);
  signal ykuwugalpj : character;
begin
  qfwszkpgb : entity work.snsrdc
    port map (eqtdxoj => ykuwugalpj, knrxa => urplz, a => cfdalfwtp);
  btgqokn : entity work.snsrdc
    port map (eqtdxoj => kmdoa, knrxa => xxrddkf, a => cfdalfwtp);
  gbf : entity work.snsrdc
    port map (eqtdxoj => u, knrxa => n, a => dnswkrz);
  
  -- Single-driven assignments
  tnmvph <= tnmvph;
  rghgnrjn <= rghgnrjn;
end uurnome;

entity zrdx is
  port (f : buffer string(2 to 2); ytkyhu : buffer time);
end zrdx;

library ieee;
use ieee.std_logic_1164.all;

architecture tr of zrdx is
  signal uhxtth : boolean;
  signal pzpuxn : integer;
  signal dzv : std_logic_vector(1 to 1);
  signal pdqtkkryq : string(3 to 1);
  signal uerbdspvbk : character;
  signal xavwkyw : boolean;
  signal yuvlpmz : integer;
begin
  i : entity work.pexqstsm
    port map (rghgnrjn => yuvlpmz, tnmvph => xavwkyw, kmhaeusr => ytkyhu);
  modppup : entity work.snsrdc
    port map (eqtdxoj => uerbdspvbk, knrxa => pdqtkkryq, a => dzv);
  nntsg : entity work.pexqstsm
    port map (rghgnrjn => pzpuxn, tnmvph => uhxtth, kmhaeusr => ytkyhu);
  
  -- Multi-driven assignments
  dzv <= dzv;
  dzv <= dzv;
end tr;

entity ujf is
  port (kufegib : linkage integer; mat : linkage character; scrial : out integer);
end ujf;

library ieee;
use ieee.std_logic_1164.all;

architecture trpairk of ujf is
  signal afygdb : string(3 to 1);
  signal nul : std_logic_vector(1 to 1);
  signal tnmroii : string(3 to 1);
  signal moj : character;
begin
  ffzjzwpc : entity work.snsrdc
    port map (eqtdxoj => moj, knrxa => tnmroii, a => nul);
  sjrcbjamsb : entity work.snsrdc
    port map (eqtdxoj => mat, knrxa => afygdb, a => nul);
  
  -- Single-driven assignments
  scrial <= scrial;
end trpairk;



-- Seed after: 8431514699459727898,8068158652091157513
