-- Seed: 9097415492959517956,8089241273282434469



entity yc is
  port (bkyzr : buffer time; qoskvo : buffer time);
end yc;



architecture jvwlzo of yc is
  
begin
  
end jvwlzo;

library ieee;
use ieee.std_logic_1164.all;

entity xagukkp is
  port (pyrblas : inout real; vv : buffer time; ouakzaaegu : in time; avyhghw : inout std_logic_vector(2 to 1));
end xagukkp;



architecture iejdqwgqoz of xagukkp is
  
begin
  
end iejdqwgqoz;



entity iungfio is
  port (l : out real_vector(1 downto 4); ohyj : buffer time; xnxo : inout real; uodhli : linkage real);
end iungfio;

library ieee;
use ieee.std_logic_1164.all;

architecture sznf of iungfio is
  signal ouowalj : time;
  signal nanm : time;
  signal q : std_logic_vector(2 to 1);
  signal jf : time;
  signal psc : time;
  signal b : real;
  signal m : time;
  signal spz : time;
begin
  jtuhl : entity work.yc
    port map (bkyzr => spz, qoskvo => m);
  tc : entity work.xagukkp
    port map (pyrblas => b, vv => psc, ouakzaaegu => jf, avyhghw => q);
  vqxt : entity work.yc
    port map (bkyzr => ohyj, qoskvo => jf);
  lmibpmnrbc : entity work.yc
    port map (bkyzr => nanm, qoskvo => ouowalj);
end sznf;



-- Seed after: 5858945114910232534,8089241273282434469
