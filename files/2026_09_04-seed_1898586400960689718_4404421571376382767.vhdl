-- Seed: 1898586400960689718,4404421571376382767

entity hyob is
  port (xxph : linkage integer; swmla : buffer time; mu : linkage real_vector(1 to 0); r : buffer real);
end hyob;

architecture weyjmavrgv of hyob is
  
begin
  -- Single-driven assignments
  r <= r;
  swmla <= swmla;
end weyjmavrgv;

library ieee;
use ieee.std_logic_1164.all;

entity awfpehgznk is
  port (qee : linkage real; zuky : inout std_logic_vector(2 downto 1); ipgzidzey : inout time);
end awfpehgznk;

architecture hawdfwkybo of awfpehgznk is
  
begin
  -- Single-driven assignments
  ipgzidzey <= 8#3_3_6_2_0.7# ps;
  
  -- Multi-driven assignments
  zuky <= zuky;
  zuky <= zuky;
end hawdfwkybo;

library ieee;
use ieee.std_logic_1164.all;

entity jhmvabu is
  port (fh : buffer time; kmnxrt : out std_logic; zkgcnyk : linkage bit; fy : in std_logic);
end jhmvabu;

architecture fiwcldgdyh of jhmvabu is
  signal orpi : real;
  signal noj : real_vector(1 to 0);
  signal bjjhqbzgy : time;
  signal plucokqdor : integer;
  signal pdu : real;
  signal ftmvj : real_vector(1 to 0);
  signal jw : integer;
  signal h : real;
  signal l : real_vector(1 to 0);
  signal mmvbv : time;
  signal u : integer;
begin
  xh : entity work.hyob
    port map (xxph => u, swmla => mmvbv, mu => l, r => h);
  ipwuvrtxx : entity work.hyob
    port map (xxph => jw, swmla => fh, mu => ftmvj, r => pdu);
  x : entity work.hyob
    port map (xxph => plucokqdor, swmla => bjjhqbzgy, mu => noj, r => orpi);
end fiwcldgdyh;

library ieee;
use ieee.std_logic_1164.all;

entity kp is
  port (qsjvwy : linkage real; n : inout real; ausozqqb : linkage std_logic_vector(2 to 3); ohsd : buffer character);
end kp;

library ieee;
use ieee.std_logic_1164.all;

architecture cij of kp is
  signal cnaste : real;
  signal gxmjxduofb : real_vector(1 to 0);
  signal bik : time;
  signal flpp : integer;
  signal rbscuwfwc : real;
  signal zs : real_vector(1 to 0);
  signal gxexrh : time;
  signal vgzrp : integer;
  signal mvrq : time;
  signal zwcrpz : std_logic_vector(2 downto 1);
  signal jhgkermlii : real;
  signal r : real;
  signal asxar : real_vector(1 to 0);
  signal kyirkjin : time;
  signal i : integer;
begin
  hisevrlpw : entity work.hyob
    port map (xxph => i, swmla => kyirkjin, mu => asxar, r => r);
  doceab : entity work.awfpehgznk
    port map (qee => jhgkermlii, zuky => zwcrpz, ipgzidzey => mvrq);
  nwnnqd : entity work.hyob
    port map (xxph => vgzrp, swmla => gxexrh, mu => zs, r => rbscuwfwc);
  zmu : entity work.hyob
    port map (xxph => flpp, swmla => bik, mu => gxmjxduofb, r => cnaste);
  
  -- Single-driven assignments
  ohsd <= 't';
  n <= 8#7_4_5_1_0.5#;
  
  -- Multi-driven assignments
  zwcrpz <= "XU";
  zwcrpz <= zwcrpz;
end cij;



-- Seed after: 15917575925291519628,4404421571376382767
