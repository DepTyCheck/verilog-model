-- Seed: 5024247691942371250,7808623373429384027

entity fk is
  port (shbkamf : in integer; lg : buffer time);
end fk;

architecture xavg of fk is
  
begin
  -- Single-driven assignments
  lg <= lg;
end xavg;

library ieee;
use ieee.std_logic_1164.all;

entity xcufjrj is
  port (ntiyjgi : in real; yx : in real_vector(2 to 1); notp : buffer std_logic_vector(4 to 1));
end xcufjrj;

architecture omznuudocp of xcufjrj is
  signal g : time;
  signal si : integer;
  signal wyfbclixur : time;
  signal ggw : integer;
  signal klqkkaijm : time;
  signal iexhzq : integer;
begin
  z : entity work.fk
    port map (shbkamf => iexhzq, lg => klqkkaijm);
  nwcz : entity work.fk
    port map (shbkamf => ggw, lg => wyfbclixur);
  hlpfitt : entity work.fk
    port map (shbkamf => si, lg => g);
  
  -- Multi-driven assignments
  notp <= notp;
  notp <= notp;
  notp <= notp;
  notp <= notp;
end omznuudocp;



-- Seed after: 10449470152064074857,7808623373429384027
