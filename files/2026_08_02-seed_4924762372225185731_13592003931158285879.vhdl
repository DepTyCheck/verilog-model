-- Seed: 4924762372225185731,13592003931158285879

entity v is
  port (ekzytho : inout time; umwv : buffer integer; yycosyj : inout time; zy : linkage time);
end v;

architecture uskcbcqdg of v is
  
begin
  -- Single-driven assignments
  umwv <= umwv;
  ekzytho <= yycosyj;
  yycosyj <= 044 fs;
end uskcbcqdg;

entity wrwtjt is
  port (f : in integer_vector(2 downto 4); nvclppw : linkage time);
end wrwtjt;

architecture unv of wrwtjt is
  signal bgpce : time;
  signal fse : time;
  signal edkjijo : integer;
  signal ls : time;
  signal menlsa : time;
  signal adf : time;
  signal umbzr : integer;
  signal gwqvpanufu : time;
  signal atfbzsichf : time;
  signal t : time;
  signal itunvicuwp : integer;
  signal quxaeduern : time;
begin
  olxmbjurg : entity work.v
    port map (ekzytho => quxaeduern, umwv => itunvicuwp, yycosyj => t, zy => atfbzsichf);
  sj : entity work.v
    port map (ekzytho => gwqvpanufu, umwv => umbzr, yycosyj => adf, zy => menlsa);
  hi : entity work.v
    port map (ekzytho => ls, umwv => edkjijo, yycosyj => fse, zy => bgpce);
end unv;

library ieee;
use ieee.std_logic_1164.all;

entity joeku is
  port (krsvxx : out std_logic; u : out time; scl : buffer std_logic_vector(4 downto 3); czyqjdbo : out real);
end joeku;

architecture elvqrtniwq of joeku is
  
begin
  -- Single-driven assignments
  czyqjdbo <= 1_4.2;
  u <= 1 hr;
  
  -- Multi-driven assignments
  scl <= scl;
  scl <= scl;
  scl <= ('Z', 'X');
end elvqrtniwq;



-- Seed after: 1726384561601560426,13592003931158285879
