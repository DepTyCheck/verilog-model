-- Seed: 15505607193563305734,9125939553767483053

library ieee;
use ieee.std_logic_1164.all;

entity kor is
  port (wcccavthsz : buffer integer; nqssznj : inout std_logic; sbutpv : in integer_vector(3 to 0); nzj : out integer_vector(2 downto 0));
end kor;



architecture izczj of kor is
  
begin
  
end izczj;



entity awowb is
  port (cpi : in bit; cqxgbdjt : out integer_vector(3 to 3); lgja : in real);
end awowb;

library ieee;
use ieee.std_logic_1164.all;

architecture eelmuos of awowb is
  signal imgij : integer_vector(2 downto 0);
  signal lbssvb : integer_vector(3 to 0);
  signal tcuuczm : std_logic;
  signal qqhlafafd : integer;
begin
  h : entity work.kor
    port map (wcccavthsz => qqhlafafd, nqssznj => tcuuczm, sbutpv => lbssvb, nzj => imgij);
end eelmuos;

library ieee;
use ieee.std_logic_1164.all;

entity vddbmnv is
  port (n : in time_vector(0 to 2); mabroxfmok : inout std_logic_vector(0 to 3); eltowf : linkage bit_vector(0 to 3); nnha : in integer);
end vddbmnv;

library ieee;
use ieee.std_logic_1164.all;

architecture axfj of vddbmnv is
  signal gswhsq : integer_vector(2 downto 0);
  signal logspam : integer_vector(3 to 0);
  signal nfph : std_logic;
  signal s : integer;
  signal ieckp : integer_vector(2 downto 0);
  signal tirhtpodn : integer_vector(3 to 0);
  signal tmw : std_logic;
  signal projalpi : integer;
  signal oycii : integer_vector(2 downto 0);
  signal enevxh : integer_vector(3 to 0);
  signal jvpi : std_logic;
  signal gqhjuipgb : integer;
begin
  mcjd : entity work.kor
    port map (wcccavthsz => gqhjuipgb, nqssznj => jvpi, sbutpv => enevxh, nzj => oycii);
  zypysai : entity work.kor
    port map (wcccavthsz => projalpi, nqssznj => tmw, sbutpv => tirhtpodn, nzj => ieckp);
  ebhenttnbi : entity work.kor
    port map (wcccavthsz => s, nqssznj => nfph, sbutpv => logspam, nzj => gswhsq);
end axfj;

library ieee;
use ieee.std_logic_1164.all;

entity nfsaneigok is
  port (uymjgp : in real; csf : inout std_logic_vector(4 downto 0); jy : out boolean);
end nfsaneigok;

library ieee;
use ieee.std_logic_1164.all;

architecture igkhfwqokk of nfsaneigok is
  signal jba : integer_vector(2 downto 0);
  signal xhncda : integer_vector(3 to 0);
  signal phrq : std_logic;
  signal zqm : integer;
begin
  dy : entity work.kor
    port map (wcccavthsz => zqm, nqssznj => phrq, sbutpv => xhncda, nzj => jba);
end igkhfwqokk;



-- Seed after: 641883321052321177,9125939553767483053
