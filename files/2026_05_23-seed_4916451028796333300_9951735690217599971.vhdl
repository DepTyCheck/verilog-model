-- Seed: 4916451028796333300,9951735690217599971

library ieee;
use ieee.std_logic_1164.all;

entity vxyulismr is
  port (ap : out real_vector(2 to 4); qdzx : in time; ibpny : in std_logic_vector(3 downto 0));
end vxyulismr;



architecture rwwbfxvwf of vxyulismr is
  
begin
  
end rwwbfxvwf;

library ieee;
use ieee.std_logic_1164.all;

entity gss is
  port (lox : buffer time; jidnxdbs : out std_logic_vector(2 downto 2); clvgaalni : out std_logic; u : linkage std_logic_vector(3 to 3));
end gss;

library ieee;
use ieee.std_logic_1164.all;

architecture cjeqsqeu of gss is
  signal n : time;
  signal kvexbkyiza : real_vector(2 to 4);
  signal onhbtt : std_logic_vector(3 downto 0);
  signal na : time;
  signal cr : real_vector(2 to 4);
begin
  fjnuzkgan : entity work.vxyulismr
    port map (ap => cr, qdzx => na, ibpny => onhbtt);
  zjkq : entity work.vxyulismr
    port map (ap => kvexbkyiza, qdzx => n, ibpny => onhbtt);
end cjeqsqeu;



entity fhan is
  port (nsr : buffer real_vector(1 downto 0); wnij : linkage time);
end fhan;



architecture tk of fhan is
  
begin
  
end tk;



entity dnsssxtar is
  port (gdnwgw : inout integer; czdets : linkage boolean_vector(4 to 1); g : in time);
end dnsssxtar;

library ieee;
use ieee.std_logic_1164.all;

architecture xhnpbgd of dnsssxtar is
  signal plxsicxgwh : time;
  signal hlsxqkpxjd : real_vector(1 downto 0);
  signal bmyawfrmxl : real_vector(1 downto 0);
  signal rk : std_logic_vector(3 to 3);
  signal dlpb : std_logic;
  signal epde : std_logic_vector(2 downto 2);
  signal qtqnxacsw : std_logic_vector(3 downto 0);
  signal wfjl : time;
  signal ymkv : real_vector(2 to 4);
begin
  xvx : entity work.vxyulismr
    port map (ap => ymkv, qdzx => wfjl, ibpny => qtqnxacsw);
  kxf : entity work.gss
    port map (lox => wfjl, jidnxdbs => epde, clvgaalni => dlpb, u => rk);
  ylsqlznsb : entity work.fhan
    port map (nsr => bmyawfrmxl, wnij => g);
  rrtnzp : entity work.fhan
    port map (nsr => hlsxqkpxjd, wnij => plxsicxgwh);
end xhnpbgd;



-- Seed after: 16623274751582062502,9951735690217599971
