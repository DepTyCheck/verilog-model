-- Seed: 8878537222189060417,4080032123900078489

library ieee;
use ieee.std_logic_1164.all;

entity hmjekufru is
  port (qfdfyj : buffer real; pohephw : buffer std_logic_vector(1 to 0); toddbxh : inout bit_vector(2 to 2); bvdbehq : buffer std_logic);
end hmjekufru;

architecture muwdg of hmjekufru is
  
begin
  -- Single-driven assignments
  toddbxh <= toddbxh;
  qfdfyj <= 23301.1;
end muwdg;

entity aiqzutm is
  port (an : inout real; ncciqbr : buffer time_vector(2 to 1); pk : out integer; ugvvfvqrm : inout time_vector(3 to 3));
end aiqzutm;

library ieee;
use ieee.std_logic_1164.all;

architecture xjzitas of aiqzutm is
  signal yqbtb : std_logic;
  signal juzt : bit_vector(2 to 2);
  signal fdlly : std_logic_vector(1 to 0);
  signal fwyphapafx : real;
begin
  q : entity work.hmjekufru
    port map (qfdfyj => fwyphapafx, pohephw => fdlly, toddbxh => juzt, bvdbehq => yqbtb);
  
  -- Single-driven assignments
  ugvvfvqrm <= ugvvfvqrm;
  an <= 8#5_2_7_4_5.1#;
  ncciqbr <= ncciqbr;
  pk <= pk;
  
  -- Multi-driven assignments
  yqbtb <= yqbtb;
end xjzitas;

library ieee;
use ieee.std_logic_1164.all;

entity ozbnzqeok is
  port (ekiwyxv : in severity_level; nycvt : inout bit; pwxyj : in std_logic);
end ozbnzqeok;

library ieee;
use ieee.std_logic_1164.all;

architecture ikrgvgcl of ozbnzqeok is
  signal gtq : std_logic;
  signal q : bit_vector(2 to 2);
  signal xhwlbi : real;
  signal syoeqc : std_logic;
  signal l : bit_vector(2 to 2);
  signal kvoadlofc : std_logic_vector(1 to 0);
  signal rofyjrmze : real;
  signal uxinnas : time_vector(3 to 3);
  signal f : integer;
  signal ncxx : time_vector(2 to 1);
  signal ymjwra : real;
begin
  intxttgobp : entity work.aiqzutm
    port map (an => ymjwra, ncciqbr => ncxx, pk => f, ugvvfvqrm => uxinnas);
  zdrakrja : entity work.hmjekufru
    port map (qfdfyj => rofyjrmze, pohephw => kvoadlofc, toddbxh => l, bvdbehq => syoeqc);
  eiwv : entity work.hmjekufru
    port map (qfdfyj => xhwlbi, pohephw => kvoadlofc, toddbxh => q, bvdbehq => gtq);
  
  -- Single-driven assignments
  nycvt <= nycvt;
  
  -- Multi-driven assignments
  kvoadlofc <= kvoadlofc;
  kvoadlofc <= (others => '0');
  syoeqc <= 'L';
  gtq <= 'U';
end ikrgvgcl;



-- Seed after: 4114086023417707520,4080032123900078489
