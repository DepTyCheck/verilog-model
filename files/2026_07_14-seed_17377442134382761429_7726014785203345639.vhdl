-- Seed: 17377442134382761429,7726014785203345639

use std.reflection.all;

entity tkarkcfmac is
  port (ltmywwzzox : inout file_subtype_mirror; gpsj : inout floating_value_mirror; bdxvztzakp : inout floating_value_mirror);
end tkarkcfmac;

architecture pfjistzzao of tkarkcfmac is
  
begin
  
end pfjistzzao;

use std.reflection.all;

entity nxcymoyamo is
  port (omuqv : inout floating_subtype_mirror);
end nxcymoyamo;

use std.reflection.all;

architecture naieiinv of nxcymoyamo is
  shared variable hiskdqyxpd : floating_value_mirror;
  shared variable pcktfh : floating_value_mirror;
  shared variable a : file_subtype_mirror;
  shared variable vqblgmdos : floating_value_mirror;
  shared variable ko : floating_value_mirror;
  shared variable vak : file_subtype_mirror;
  shared variable uwkllhd : floating_value_mirror;
  shared variable vwovmba : floating_value_mirror;
  shared variable ercpfmflw : file_subtype_mirror;
begin
  nqn : entity work.tkarkcfmac
    port map (ltmywwzzox => ercpfmflw, gpsj => vwovmba, bdxvztzakp => uwkllhd);
  otbqppsa : entity work.tkarkcfmac
    port map (ltmywwzzox => vak, gpsj => ko, bdxvztzakp => vqblgmdos);
  felwx : entity work.tkarkcfmac
    port map (ltmywwzzox => a, gpsj => pcktfh, bdxvztzakp => hiskdqyxpd);
end naieiinv;

use std.reflection.all;

entity vlgcuqpdnb is
  port (zlowazukzb : inout access_subtype_mirror; ddwkenngqt : inout value_mirror; dk : out real; vogesanxxg : out real);
end vlgcuqpdnb;

use std.reflection.all;

architecture xow of vlgcuqpdnb is
  shared variable tgrfqopw : floating_value_mirror;
  shared variable klqmzo : floating_value_mirror;
  shared variable phadivbrty : file_subtype_mirror;
  shared variable ctphax : floating_value_mirror;
  shared variable rlbhid : floating_value_mirror;
  shared variable q : file_subtype_mirror;
  shared variable zr : floating_subtype_mirror;
begin
  tnqoqjt : entity work.nxcymoyamo
    port map (omuqv => zr);
  twayide : entity work.tkarkcfmac
    port map (ltmywwzzox => q, gpsj => rlbhid, bdxvztzakp => ctphax);
  srlygkv : entity work.tkarkcfmac
    port map (ltmywwzzox => phadivbrty, gpsj => klqmzo, bdxvztzakp => tgrfqopw);
end xow;



-- Seed after: 14744039320251907230,7726014785203345639
