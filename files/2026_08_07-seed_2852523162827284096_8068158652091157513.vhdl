-- Seed: 2852523162827284096,8068158652091157513

library ieee;
use ieee.std_logic_1164.all;

entity ptkymdoca is
  port (zvflrtymnv : linkage integer; jguqkhe : buffer real; lbaxcqtt : in std_logic_vector(4 downto 1); bnpcudkbs : buffer real_vector(0 downto 1));
end ptkymdoca;

architecture k of ptkymdoca is
  
begin
  -- Single-driven assignments
  bnpcudkbs <= bnpcudkbs;
  jguqkhe <= jguqkhe;
end k;

library ieee;
use ieee.std_logic_1164.all;

entity iiqzl is
  port (sl : inout real; lufxhauygn : linkage std_logic_vector(1 downto 0); szabyotyo : inout std_logic_vector(3 to 4); dmg : out time);
end iiqzl;

library ieee;
use ieee.std_logic_1164.all;

architecture qysjpj of iiqzl is
  signal zbyfp : real_vector(0 downto 1);
  signal po : std_logic_vector(4 downto 1);
  signal jqscprvw : real;
  signal bsam : integer;
  signal zqtcbhjvct : real_vector(0 downto 1);
  signal bdug : integer;
  signal udancjzbmv : real_vector(0 downto 1);
  signal aepy : real;
  signal wrm : integer;
  signal dhry : real_vector(0 downto 1);
  signal vqpewnvu : std_logic_vector(4 downto 1);
  signal hklma : real;
  signal ahzkvrv : integer;
begin
  sqanutdigs : entity work.ptkymdoca
    port map (zvflrtymnv => ahzkvrv, jguqkhe => hklma, lbaxcqtt => vqpewnvu, bnpcudkbs => dhry);
  rvnev : entity work.ptkymdoca
    port map (zvflrtymnv => wrm, jguqkhe => aepy, lbaxcqtt => vqpewnvu, bnpcudkbs => udancjzbmv);
  gkcvy : entity work.ptkymdoca
    port map (zvflrtymnv => bdug, jguqkhe => sl, lbaxcqtt => vqpewnvu, bnpcudkbs => zqtcbhjvct);
  vnouocd : entity work.ptkymdoca
    port map (zvflrtymnv => bsam, jguqkhe => jqscprvw, lbaxcqtt => po, bnpcudkbs => zbyfp);
  
  -- Single-driven assignments
  dmg <= dmg;
  
  -- Multi-driven assignments
  szabyotyo <= ('U', 'H');
  vqpewnvu <= ('0', 'Z', 'L', 'W');
  vqpewnvu <= vqpewnvu;
end qysjpj;

entity idqrbkslm is
  port (sjbige : out bit);
end idqrbkslm;

library ieee;
use ieee.std_logic_1164.all;

architecture ekh of idqrbkslm is
  signal xnoeufiu : time;
  signal laqqjfqtt : real;
  signal vrljdvb : real_vector(0 downto 1);
  signal ichyoqy : real;
  signal etibujfvld : integer;
  signal qidyu : time;
  signal uiufsnihs : std_logic_vector(3 to 4);
  signal uijwr : real;
  signal gbuaszq : real_vector(0 downto 1);
  signal dzz : std_logic_vector(4 downto 1);
  signal zqfa : real;
  signal ldxlac : integer;
begin
  nulxlyqa : entity work.ptkymdoca
    port map (zvflrtymnv => ldxlac, jguqkhe => zqfa, lbaxcqtt => dzz, bnpcudkbs => gbuaszq);
  pgu : entity work.iiqzl
    port map (sl => uijwr, lufxhauygn => uiufsnihs, szabyotyo => uiufsnihs, dmg => qidyu);
  znjsbvamj : entity work.ptkymdoca
    port map (zvflrtymnv => etibujfvld, jguqkhe => ichyoqy, lbaxcqtt => dzz, bnpcudkbs => vrljdvb);
  qzyvkp : entity work.iiqzl
    port map (sl => laqqjfqtt, lufxhauygn => uiufsnihs, szabyotyo => uiufsnihs, dmg => xnoeufiu);
  
  -- Single-driven assignments
  sjbige <= sjbige;
end ekh;

library ieee;
use ieee.std_logic_1164.all;

entity vbg is
  port (htghqe : inout time; buo : out std_logic_vector(0 to 1); amvggsd : in severity_level);
end vbg;

library ieee;
use ieee.std_logic_1164.all;

architecture gl of vbg is
  signal vqg : real_vector(0 downto 1);
  signal ehvlokh : std_logic_vector(4 downto 1);
  signal az : real;
  signal fwylo : integer;
  signal at : bit;
  signal jsxdgh : time;
  signal yqswfytq : std_logic_vector(1 downto 0);
  signal fccnmymka : real;
  signal retqzoml : real_vector(0 downto 1);
  signal kinwik : std_logic_vector(4 downto 1);
  signal ohgemxz : real;
  signal xkk : integer;
begin
  jxhpiaeh : entity work.ptkymdoca
    port map (zvflrtymnv => xkk, jguqkhe => ohgemxz, lbaxcqtt => kinwik, bnpcudkbs => retqzoml);
  sh : entity work.iiqzl
    port map (sl => fccnmymka, lufxhauygn => yqswfytq, szabyotyo => buo, dmg => jsxdgh);
  ytyecpk : entity work.idqrbkslm
    port map (sjbige => at);
  m : entity work.ptkymdoca
    port map (zvflrtymnv => fwylo, jguqkhe => az, lbaxcqtt => ehvlokh, bnpcudkbs => vqg);
  
  -- Single-driven assignments
  htghqe <= 12301.2_4 us;
  
  -- Multi-driven assignments
  buo <= ('-', 'L');
  kinwik <= "ZXZH";
  kinwik <= kinwik;
end gl;



-- Seed after: 5129770779625005709,8068158652091157513
