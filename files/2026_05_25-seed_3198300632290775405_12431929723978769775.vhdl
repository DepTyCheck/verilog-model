-- Seed: 3198300632290775405,12431929723978769775

library ieee;
use ieee.std_logic_1164.all;

entity y is
  port (vrrjd : linkage std_logic_vector(1 downto 4); jmcrvyf : out real_vector(2 to 2); tyopivhb : inout boolean; lav : in time);
end y;



architecture lzdvsukc of y is
  
begin
  
end lzdvsukc;



entity uahbeslsin is
  port (kqjphxurfr : linkage character; oqfbgutmwb : in severity_level);
end uahbeslsin;

library ieee;
use ieee.std_logic_1164.all;

architecture iw of uahbeslsin is
  signal i : time;
  signal tou : boolean;
  signal tvkfqwcug : real_vector(2 to 2);
  signal otm : std_logic_vector(1 downto 4);
  signal bvcwxkvzgw : time;
  signal sz : boolean;
  signal kccm : real_vector(2 to 2);
  signal xbvyl : std_logic_vector(1 downto 4);
  signal yhsr : time;
  signal lbqcnjmtl : boolean;
  signal zgtvzkwc : real_vector(2 to 2);
  signal unpwnso : time;
  signal fqrqns : boolean;
  signal zlzazqmkdf : real_vector(2 to 2);
  signal qp : std_logic_vector(1 downto 4);
begin
  rvke : entity work.y
    port map (vrrjd => qp, jmcrvyf => zlzazqmkdf, tyopivhb => fqrqns, lav => unpwnso);
  yspiruo : entity work.y
    port map (vrrjd => qp, jmcrvyf => zgtvzkwc, tyopivhb => lbqcnjmtl, lav => yhsr);
  ldfgpfymi : entity work.y
    port map (vrrjd => xbvyl, jmcrvyf => kccm, tyopivhb => sz, lav => bvcwxkvzgw);
  aqksgijea : entity work.y
    port map (vrrjd => otm, jmcrvyf => tvkfqwcug, tyopivhb => tou, lav => i);
end iw;



-- Seed after: 6710222815037611502,12431929723978769775
