-- Seed: 4923228089844934756,6290177331721581829

use std.reflection.all;

entity mxrwmoqp is
  port (r : inout protected_subtype_mirror);
end mxrwmoqp;

architecture kz of mxrwmoqp is
  
begin
  
end kz;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity uofvc is
  port (ln : in severity_level; opypgvi : inout access_subtype_mirror; lr : linkage std_logic; jdnduhumw : inout integer_value_mirror);
end uofvc;

use std.reflection.all;

architecture n of uofvc is
  shared variable fv : protected_subtype_mirror;
  shared variable hphg : protected_subtype_mirror;
  shared variable byzlukh : protected_subtype_mirror;
begin
  kd : entity work.mxrwmoqp
    port map (r => byzlukh);
  la : entity work.mxrwmoqp
    port map (r => hphg);
  sralkp : entity work.mxrwmoqp
    port map (r => fv);
end n;

entity xg is
  port (fzeixxeiv : in severity_level);
end xg;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture vf of xg is
  shared variable kuls : integer_value_mirror;
  shared variable fwafqcgmlr : access_subtype_mirror;
  signal fwsp : severity_level;
  shared variable qw : integer_value_mirror;
  signal bzfd : std_logic;
  shared variable rba : access_subtype_mirror;
  signal g : severity_level;
  shared variable kr : protected_subtype_mirror;
begin
  kzlgaiaxxs : entity work.mxrwmoqp
    port map (r => kr);
  ldkjkrg : entity work.uofvc
    port map (ln => g, opypgvi => rba, lr => bzfd, jdnduhumw => qw);
  jxbysgbt : entity work.uofvc
    port map (ln => fwsp, opypgvi => fwafqcgmlr, lr => bzfd, jdnduhumw => kuls);
  
  -- Single-driven assignments
  g <= ERROR;
  fwsp <= fzeixxeiv;
  
  -- Multi-driven assignments
  bzfd <= bzfd;
end vf;

use std.reflection.all;

entity oal is
  port (vjluqbjs : inout integer_subtype_mirror);
end oal;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture l of oal is
  shared variable pm : protected_subtype_mirror;
  shared variable pevpvipsa : integer_value_mirror;
  signal qyinegzvf : std_logic;
  shared variable tpzu : access_subtype_mirror;
  signal r : severity_level;
begin
  llxchfqqw : entity work.uofvc
    port map (ln => r, opypgvi => tpzu, lr => qyinegzvf, jdnduhumw => pevpvipsa);
  myxgvfghk : entity work.mxrwmoqp
    port map (r => pm);
  
  -- Single-driven assignments
  r <= r;
  
  -- Multi-driven assignments
  qyinegzvf <= '1';
  qyinegzvf <= 'H';
end l;



-- Seed after: 12880741564750697234,6290177331721581829
