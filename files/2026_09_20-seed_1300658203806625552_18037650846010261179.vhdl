-- Seed: 1300658203806625552,18037650846010261179

entity birxxplhat is
  port (ylpuemynv : out bit_vector(2 to 0); amg : buffer time);
end birxxplhat;

architecture kh of birxxplhat is
  
begin
  -- Single-driven assignments
  ylpuemynv <= ylpuemynv;
  amg <= 2#01101# fs;
end kh;

library ieee;
use ieee.std_logic_1164.all;

entity thzhek is
  port (l : inout time; y : buffer time_vector(0 downto 2); zzmnk : linkage std_logic_vector(4 downto 3); oddodlk : inout severity_level);
end thzhek;

architecture coxyygv of thzhek is
  signal vy : bit_vector(2 to 0);
begin
  gqm : entity work.birxxplhat
    port map (ylpuemynv => vy, amg => l);
  
  -- Single-driven assignments
  oddodlk <= oddodlk;
end coxyygv;

entity b is
  port (gshsvfk : out time);
end b;

library ieee;
use ieee.std_logic_1164.all;

architecture srny of b is
  signal zqyk : severity_level;
  signal wahtgkiqga : std_logic_vector(4 downto 3);
  signal g : time_vector(0 downto 2);
  signal exoushgyi : time;
  signal icxo : bit_vector(2 to 0);
  signal ihjosexj : time;
  signal witdk : bit_vector(2 to 0);
  signal dbvf : time;
  signal ggtoslcn : bit_vector(2 to 0);
begin
  ozi : entity work.birxxplhat
    port map (ylpuemynv => ggtoslcn, amg => dbvf);
  cyvebmjkv : entity work.birxxplhat
    port map (ylpuemynv => witdk, amg => ihjosexj);
  wwo : entity work.birxxplhat
    port map (ylpuemynv => icxo, amg => gshsvfk);
  fchu : entity work.thzhek
    port map (l => exoushgyi, y => g, zzmnk => wahtgkiqga, oddodlk => zqyk);
  
  -- Multi-driven assignments
  wahtgkiqga <= wahtgkiqga;
end srny;

library ieee;
use ieee.std_logic_1164.all;

entity oiwhxkg is
  port (l : linkage time; xmld : out time; rzgwyie : out std_logic_vector(0 to 1); irjkno : inout std_logic_vector(1 to 1));
end oiwhxkg;

architecture pkfxs of oiwhxkg is
  signal zcxnreshwo : bit_vector(2 to 0);
begin
  kgv : entity work.birxxplhat
    port map (ylpuemynv => zcxnreshwo, amg => xmld);
end pkfxs;



-- Seed after: 7867759099108955953,18037650846010261179
