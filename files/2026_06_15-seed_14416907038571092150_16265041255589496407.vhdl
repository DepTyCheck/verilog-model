-- Seed: 14416907038571092150,16265041255589496407

library ieee;
use ieee.std_logic_1164.all;

entity buig is
  port (azo : in std_logic_vector(0 to 0));
end buig;



architecture gjpdr of buig is
  
begin
  
end gjpdr;



entity evh is
  port (c : inout real_vector(1 downto 0));
end evh;

library ieee;
use ieee.std_logic_1164.all;

architecture vym of evh is
  signal tqryucsg : std_logic_vector(0 to 0);
begin
  uu : entity work.buig
    port map (azo => tqryucsg);
  dw : entity work.buig
    port map (azo => tqryucsg);
end vym;

library ieee;
use ieee.std_logic_1164.all;

entity r is
  port (sjlizz : buffer bit_vector(3 downto 2); llwhed : inout std_logic_vector(3 downto 4); ojk : buffer time; pslra : in std_logic_vector(2 to 0));
end r;

library ieee;
use ieee.std_logic_1164.all;

architecture ame of r is
  signal wtq : real_vector(1 downto 0);
  signal ccypc : std_logic_vector(0 to 0);
begin
  ri : entity work.buig
    port map (azo => ccypc);
  kjeympacik : entity work.buig
    port map (azo => ccypc);
  nztzpxiw : entity work.evh
    port map (c => wtq);
  wfuilkxua : entity work.buig
    port map (azo => ccypc);
end ame;

library ieee;
use ieee.std_logic_1164.all;

entity kifwi is
  port (avwcduuk : inout integer; aznj : out std_logic; ayacsv : linkage time; yojwscgc : buffer time);
end kifwi;

library ieee;
use ieee.std_logic_1164.all;

architecture mlv of kifwi is
  signal dol : time;
  signal xdsvzhhw : std_logic_vector(2 to 0);
  signal sli : bit_vector(3 downto 2);
  signal niz : real_vector(1 downto 0);
  signal w : real_vector(1 downto 0);
  signal ukbtupyosb : std_logic_vector(0 to 0);
begin
  xlqn : entity work.buig
    port map (azo => ukbtupyosb);
  nf : entity work.evh
    port map (c => w);
  owx : entity work.evh
    port map (c => niz);
  i : entity work.r
    port map (sjlizz => sli, llwhed => xdsvzhhw, ojk => dol, pslra => xdsvzhhw);
end mlv;



-- Seed after: 11615302620929719460,16265041255589496407
