-- Seed: 678902441045091489,10594830431004325987

library ieee;
use ieee.std_logic_1164.all;

entity qlhpawu is
  port (lujn : out character; vbje : out integer; ea : inout boolean; hecfw : in std_logic_vector(0 downto 1));
end qlhpawu;

architecture lrbppkmnap of qlhpawu is
  
begin
  -- Single-driven assignments
  ea <= ea;
  lujn <= lujn;
  vbje <= vbje;
end lrbppkmnap;

library ieee;
use ieee.std_logic_1164.all;

entity astogwu is
  port (wr : out time; yktqohds : out real; kndmuxs : linkage real; djvwelvcq : linkage std_logic);
end astogwu;

library ieee;
use ieee.std_logic_1164.all;

architecture mepkeamn of astogwu is
  signal hw : std_logic_vector(0 downto 1);
  signal iwwxco : boolean;
  signal nfvlse : integer;
  signal barhgx : character;
  signal k : std_logic_vector(0 downto 1);
  signal bful : boolean;
  signal vrraothr : integer;
  signal jpwvpzild : character;
  signal iqyuhybo : boolean;
  signal nunsi : integer;
  signal c : character;
  signal xx : std_logic_vector(0 downto 1);
  signal wjyngs : boolean;
  signal zhotymp : integer;
  signal skoazbjlc : character;
begin
  vmiuivaezz : entity work.qlhpawu
    port map (lujn => skoazbjlc, vbje => zhotymp, ea => wjyngs, hecfw => xx);
  adxng : entity work.qlhpawu
    port map (lujn => c, vbje => nunsi, ea => iqyuhybo, hecfw => xx);
  vymkd : entity work.qlhpawu
    port map (lujn => jpwvpzild, vbje => vrraothr, ea => bful, hecfw => k);
  fsfosvu : entity work.qlhpawu
    port map (lujn => barhgx, vbje => nfvlse, ea => iwwxco, hecfw => hw);
  
  -- Single-driven assignments
  yktqohds <= yktqohds;
  wr <= 16#B793# fs;
  
  -- Multi-driven assignments
  xx <= (others => '0');
  xx <= xx;
  xx <= k;
  xx <= k;
end mepkeamn;

entity shtl is
  port (teaviara : out real; efsbhus : buffer real; lqt : linkage time_vector(3 to 1); xlpwge : buffer integer);
end shtl;

architecture jmyrzl of shtl is
  
begin
  -- Single-driven assignments
  xlpwge <= xlpwge;
  efsbhus <= 0333.02402;
end jmyrzl;

library ieee;
use ieee.std_logic_1164.all;

entity pdiv is
  port (vghjde : out string(3 to 2); jzjikoxbdy : in time; ekm : inout std_logic_vector(4 to 3); bqn : buffer time);
end pdiv;

library ieee;
use ieee.std_logic_1164.all;

architecture md of pdiv is
  signal dj : boolean;
  signal hbsjmdfk : integer;
  signal phxy : character;
  signal aetazc : boolean;
  signal d : integer;
  signal n : character;
  signal puxhorrezs : std_logic_vector(0 downto 1);
  signal atepqin : boolean;
  signal hbpfj : integer;
  signal btffvxpas : character;
  signal mb : integer;
  signal oxvj : time_vector(3 to 1);
  signal o : real;
  signal yn : real;
begin
  ek : entity work.shtl
    port map (teaviara => yn, efsbhus => o, lqt => oxvj, xlpwge => mb);
  hkxxylsof : entity work.qlhpawu
    port map (lujn => btffvxpas, vbje => hbpfj, ea => atepqin, hecfw => puxhorrezs);
  jcgqkjwum : entity work.qlhpawu
    port map (lujn => n, vbje => d, ea => aetazc, hecfw => puxhorrezs);
  sjxjiyucz : entity work.qlhpawu
    port map (lujn => phxy, vbje => hbsjmdfk, ea => dj, hecfw => puxhorrezs);
  
  -- Single-driven assignments
  bqn <= 2#101.1_1# ms;
  vghjde <= (others => ' ');
  
  -- Multi-driven assignments
  puxhorrezs <= ekm;
  ekm <= ekm;
  puxhorrezs <= ekm;
end md;



-- Seed after: 1826934487985563763,10594830431004325987
