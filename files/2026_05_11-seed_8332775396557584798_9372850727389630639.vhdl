-- Seed: 8332775396557584798,9372850727389630639



entity zqykpha is
  port (wghenanilp : out boolean; prbilwuntg : linkage time; ihctysuuq : out real);
end zqykpha;



architecture vyl of zqykpha is
  
begin
  
end vyl;

library ieee;
use ieee.std_logic_1164.all;

entity enrfpr is
  port (sywubs : buffer std_logic);
end enrfpr;



architecture tuxbq of enrfpr is
  signal fbsl : real;
  signal qpqcodh : time;
  signal z : boolean;
  signal ez : real;
  signal ydxtm : boolean;
  signal m : real;
  signal uxnf : time;
  signal iqpknsuqzw : boolean;
  signal ele : real;
  signal j : time;
  signal mfi : boolean;
begin
  kpejvbofe : entity work.zqykpha
    port map (wghenanilp => mfi, prbilwuntg => j, ihctysuuq => ele);
  f : entity work.zqykpha
    port map (wghenanilp => iqpknsuqzw, prbilwuntg => uxnf, ihctysuuq => m);
  uzzul : entity work.zqykpha
    port map (wghenanilp => ydxtm, prbilwuntg => j, ihctysuuq => ez);
  mpgrv : entity work.zqykpha
    port map (wghenanilp => z, prbilwuntg => qpqcodh, ihctysuuq => fbsl);
end tuxbq;



entity hyoifl is
  port (y : in boolean; lstegrrvx : buffer integer; gofrqr : out time; u : in integer);
end hyoifl;

library ieee;
use ieee.std_logic_1164.all;

architecture kanyorwwfa of hyoifl is
  signal hovm : real;
  signal nrcatmdxf : time;
  signal rcbg : boolean;
  signal ucfnuyyrq : std_logic;
  signal w : real;
  signal us : time;
  signal tnftu : boolean;
  signal ohcsl : real;
  signal i : time;
  signal lhnhpaotzb : boolean;
begin
  eaerlwog : entity work.zqykpha
    port map (wghenanilp => lhnhpaotzb, prbilwuntg => i, ihctysuuq => ohcsl);
  uglubdqno : entity work.zqykpha
    port map (wghenanilp => tnftu, prbilwuntg => us, ihctysuuq => w);
  obloirwhh : entity work.enrfpr
    port map (sywubs => ucfnuyyrq);
  tfu : entity work.zqykpha
    port map (wghenanilp => rcbg, prbilwuntg => nrcatmdxf, ihctysuuq => hovm);
end kanyorwwfa;



-- Seed after: 5568816391294421418,9372850727389630639
