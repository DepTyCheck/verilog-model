-- Seed: 11317163957720498274,16461708287571398341

library ieee;
use ieee.std_logic_1164.all;

entity kh is
  port (e : in integer; vxfocdocbv : linkage std_logic);
end kh;

architecture g of kh is
  
begin
  
end g;

library ieee;
use ieee.std_logic_1164.all;

entity qfrunkmwtf is
  port ( uxmx : out std_logic_vector(3 to 1)
  ; jmhkupiu : linkage std_logic_vector(3 downto 3)
  ; zluo : out std_logic_vector(0 downto 4)
  ; wyzi : buffer std_logic_vector(1 to 4)
  );
end qfrunkmwtf;

library ieee;
use ieee.std_logic_1164.all;

architecture xbohd of qfrunkmwtf is
  signal bzpfop : std_logic;
  signal e : std_logic;
  signal au : integer;
begin
  uzq : entity work.kh
    port map (e => au, vxfocdocbv => e);
  hrvp : entity work.kh
    port map (e => au, vxfocdocbv => bzpfop);
  
  -- Single-driven assignments
  au <= 02012;
  
  -- Multi-driven assignments
  bzpfop <= 'Z';
end xbohd;

entity wsth is
  port (feozd : out integer_vector(4 to 2); fhojyqy : inout bit; fyu : in real_vector(1 to 4));
end wsth;

library ieee;
use ieee.std_logic_1164.all;

architecture fgwu of wsth is
  signal r : std_logic;
  signal ydgv : integer;
  signal gklhkvhhuf : std_logic;
  signal lwjo : std_logic;
  signal hhukinx : integer;
begin
  pyb : entity work.kh
    port map (e => hhukinx, vxfocdocbv => lwjo);
  bewzvcirs : entity work.kh
    port map (e => hhukinx, vxfocdocbv => gklhkvhhuf);
  eau : entity work.kh
    port map (e => ydgv, vxfocdocbv => r);
  
  -- Single-driven assignments
  fhojyqy <= fhojyqy;
  feozd <= feozd;
  ydgv <= hhukinx;
  
  -- Multi-driven assignments
  lwjo <= 'L';
  lwjo <= 'L';
  lwjo <= 'L';
  gklhkvhhuf <= lwjo;
end fgwu;

library ieee;
use ieee.std_logic_1164.all;

entity osurtnq is
  port (aowtwi : linkage std_logic_vector(3 downto 2); rov : out time);
end osurtnq;

library ieee;
use ieee.std_logic_1164.all;

architecture thsxe of osurtnq is
  signal topdlyqgpk : std_logic;
  signal jpbnhc : integer;
  signal kosme : real_vector(1 to 4);
  signal zqeq : bit;
  signal xm : integer_vector(4 to 2);
begin
  mxtzpegzca : entity work.wsth
    port map (feozd => xm, fhojyqy => zqeq, fyu => kosme);
  mwdhdb : entity work.kh
    port map (e => jpbnhc, vxfocdocbv => topdlyqgpk);
  
  -- Single-driven assignments
  rov <= 300 fs;
  kosme <= (1213.4_1_4_4, 103.1, 2#1.1_1_1_0_0#, 8#3.3_0#);
  
  -- Multi-driven assignments
  topdlyqgpk <= '0';
  topdlyqgpk <= topdlyqgpk;
  topdlyqgpk <= topdlyqgpk;
end thsxe;



-- Seed after: 12322266089256510943,16461708287571398341
