-- Seed: 268123006682126348,11460338385561364835



entity bg is
  port (cgcptnt : out bit; rmpytzjgo : inout real; qp : in severity_level; hyosvwru : inout time);
end bg;



architecture rctx of bg is
  
begin
  
end rctx;

library ieee;
use ieee.std_logic_1164.all;

entity hoyefc is
  port (vrlwpqyf : inout std_logic; pkonbz : out real; iztfipvdsl : out std_logic);
end hoyefc;



architecture gbxvyfjuj of hoyefc is
  signal glkw : time;
  signal gprhboto : real;
  signal xkjzfa : bit;
  signal sagzfv : time;
  signal jmofmu : severity_level;
  signal c : bit;
begin
  mexkthpw : entity work.bg
    port map (cgcptnt => c, rmpytzjgo => pkonbz, qp => jmofmu, hyosvwru => sagzfv);
  yo : entity work.bg
    port map (cgcptnt => xkjzfa, rmpytzjgo => gprhboto, qp => jmofmu, hyosvwru => glkw);
end gbxvyfjuj;



entity e is
  port (yjyyfrakez : inout real);
end e;

library ieee;
use ieee.std_logic_1164.all;

architecture iildpll of e is
  signal drdjgmu : std_logic;
  signal npqlauppr : std_logic;
  signal jwqgwd : std_logic;
  signal gpn : real;
  signal yvncvqg : time;
  signal dxtr : severity_level;
  signal mo : real;
  signal arvysjvja : bit;
  signal ugmsdcvwxw : real;
  signal h : std_logic;
begin
  uz : entity work.hoyefc
    port map (vrlwpqyf => h, pkonbz => ugmsdcvwxw, iztfipvdsl => h);
  statiudx : entity work.bg
    port map (cgcptnt => arvysjvja, rmpytzjgo => mo, qp => dxtr, hyosvwru => yvncvqg);
  spplferizz : entity work.hoyefc
    port map (vrlwpqyf => h, pkonbz => gpn, iztfipvdsl => jwqgwd);
  mlppdg : entity work.hoyefc
    port map (vrlwpqyf => npqlauppr, pkonbz => yjyyfrakez, iztfipvdsl => drdjgmu);
end iildpll;



entity dplgp is
  port (wgaiidgyo : in time; mbvok : in real; hdgxnrqqz : out time);
end dplgp;



architecture yp of dplgp is
  signal z : real;
  signal uguei : bit;
  signal hzqbnukg : time;
  signal xmxxc : real;
  signal dzjj : bit;
  signal kg : time;
  signal tr : severity_level;
  signal x : real;
  signal zfiz : bit;
begin
  pfiskn : entity work.bg
    port map (cgcptnt => zfiz, rmpytzjgo => x, qp => tr, hyosvwru => kg);
  eczlntquk : entity work.bg
    port map (cgcptnt => dzjj, rmpytzjgo => xmxxc, qp => tr, hyosvwru => hzqbnukg);
  vceq : entity work.bg
    port map (cgcptnt => uguei, rmpytzjgo => z, qp => tr, hyosvwru => hdgxnrqqz);
end yp;



-- Seed after: 17777952045633245512,11460338385561364835
