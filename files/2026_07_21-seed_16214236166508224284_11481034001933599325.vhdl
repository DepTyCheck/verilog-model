-- Seed: 16214236166508224284,11481034001933599325

entity ufzbmp is
  port (zijkq : in real);
end ufzbmp;

architecture bwdrlesppu of ufzbmp is
  
begin
  
end bwdrlesppu;

library ieee;
use ieee.std_logic_1164.all;

entity e is
  port (kbj : inout std_logic_vector(0 downto 0); ubqmjslso : linkage real; cdf : inout severity_level);
end e;

architecture se of e is
  signal miegjs : real;
begin
  d : entity work.ufzbmp
    port map (zijkq => miegjs);
  qx : entity work.ufzbmp
    port map (zijkq => miegjs);
  
  -- Single-driven assignments
  cdf <= cdf;
  miegjs <= miegjs;
  
  -- Multi-driven assignments
  kbj <= kbj;
  kbj <= kbj;
  kbj <= kbj;
end se;

entity lbk is
  port (pwjhxaeaeb : out real);
end lbk;

architecture mqdpef of lbk is
  signal wymnsh : real;
  signal bfh : real;
  signal d : real;
begin
  bpaxie : entity work.ufzbmp
    port map (zijkq => d);
  zggssfwpsa : entity work.ufzbmp
    port map (zijkq => bfh);
  gbnxwul : entity work.ufzbmp
    port map (zijkq => wymnsh);
  bmvnpxtry : entity work.ufzbmp
    port map (zijkq => d);
  
  -- Single-driven assignments
  bfh <= 001.10;
  pwjhxaeaeb <= pwjhxaeaeb;
end mqdpef;



-- Seed after: 18310331581801842858,11481034001933599325
