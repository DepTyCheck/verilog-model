-- Seed: 8532650859740715189,4860866131898729603

entity nxdxy is
  port (tsunehcuu : out severity_level; pcixf : inout severity_level);
end nxdxy;

architecture ebzmhr of nxdxy is
  
begin
  -- Single-driven assignments
  pcixf <= WARNING;
  tsunehcuu <= WARNING;
end ebzmhr;

library ieee;
use ieee.std_logic_1164.all;

entity buc is
  port (gkdg : in std_logic);
end buc;

architecture gv of buc is
  signal vrozst : severity_level;
  signal jaakarcuzo : severity_level;
  signal crc : severity_level;
  signal hj : severity_level;
  signal fb : severity_level;
  signal iyknakpxo : severity_level;
  signal m : severity_level;
  signal pi : severity_level;
begin
  ci : entity work.nxdxy
    port map (tsunehcuu => pi, pcixf => m);
  aahaksj : entity work.nxdxy
    port map (tsunehcuu => iyknakpxo, pcixf => fb);
  pezis : entity work.nxdxy
    port map (tsunehcuu => hj, pcixf => crc);
  glzu : entity work.nxdxy
    port map (tsunehcuu => jaakarcuzo, pcixf => vrozst);
end gv;



-- Seed after: 2870891630173269466,4860866131898729603
