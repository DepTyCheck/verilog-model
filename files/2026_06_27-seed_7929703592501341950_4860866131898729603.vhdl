-- Seed: 7929703592501341950,4860866131898729603

library ieee;
use ieee.std_logic_1164.all;

entity ip is
  port (kxjhnbye : inout bit; brae : buffer std_logic_vector(2 to 2); svukwxu : linkage real; ws : in real_vector(2 to 1));
end ip;

architecture cop of ip is
  
begin
  -- Single-driven assignments
  kxjhnbye <= '0';
end cop;

entity gsqipwlz is
  port (xa : buffer boolean; rxasuistp : in bit; yayipa : out real);
end gsqipwlz;

library ieee;
use ieee.std_logic_1164.all;

architecture su of gsqipwlz is
  signal w : real_vector(2 to 1);
  signal aspb : std_logic_vector(2 to 2);
  signal eemf : bit;
begin
  zrwwezq : entity work.ip
    port map (kxjhnbye => eemf, brae => aspb, svukwxu => yayipa, ws => w);
  
  -- Single-driven assignments
  xa <= TRUE;
  w <= (others => 0.0);
end su;

library ieee;
use ieee.std_logic_1164.all;

entity cucxnecdi is
  port (jjlmgb : buffer std_logic_vector(2 downto 4); gq : inout integer; tapczsva : in boolean_vector(3 to 4); pnrfafnn : inout time_vector(3 to 0));
end cucxnecdi;

library ieee;
use ieee.std_logic_1164.all;

architecture shb of cucxnecdi is
  signal akvbyht : real_vector(2 to 1);
  signal libjnzrzx : real;
  signal qbniv : bit;
  signal zssz : real_vector(2 to 1);
  signal izs : real;
  signal bbswf : std_logic_vector(2 to 2);
  signal oqibzzy : bit;
begin
  j : entity work.ip
    port map (kxjhnbye => oqibzzy, brae => bbswf, svukwxu => izs, ws => zssz);
  tukhjcimac : entity work.ip
    port map (kxjhnbye => qbniv, brae => bbswf, svukwxu => libjnzrzx, ws => akvbyht);
  
  -- Single-driven assignments
  zssz <= (others => 0.0);
  pnrfafnn <= (others => 0 ns);
  
  -- Multi-driven assignments
  jjlmgb <= (others => '0');
  jjlmgb <= (others => '0');
  bbswf <= (others => 'W');
end shb;



-- Seed after: 8411411976511946583,4860866131898729603
