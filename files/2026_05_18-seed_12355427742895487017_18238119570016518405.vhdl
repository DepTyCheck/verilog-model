-- Seed: 12355427742895487017,18238119570016518405



entity gbptcn is
  port (tuhozwifw : out real_vector(2 downto 0); np : buffer boolean);
end gbptcn;



architecture ppsyr of gbptcn is
  
begin
  
end ppsyr;

library ieee;
use ieee.std_logic_1164.all;

entity ki is
  port (dhjwabsyac : buffer integer; dpy : linkage std_logic_vector(3 to 4); uspg : buffer real; zkvsnwqoot : linkage bit_vector(0 downto 1));
end ki;



architecture kmirnsax of ki is
  signal wmht : boolean;
  signal vwclhl : real_vector(2 downto 0);
  signal pgdu : boolean;
  signal ubyuvxr : real_vector(2 downto 0);
  signal scstf : boolean;
  signal rtocsirfc : real_vector(2 downto 0);
  signal soqyzw : boolean;
  signal emw : real_vector(2 downto 0);
begin
  u : entity work.gbptcn
    port map (tuhozwifw => emw, np => soqyzw);
  jiwjq : entity work.gbptcn
    port map (tuhozwifw => rtocsirfc, np => scstf);
  wz : entity work.gbptcn
    port map (tuhozwifw => ubyuvxr, np => pgdu);
  gfdifwu : entity work.gbptcn
    port map (tuhozwifw => vwclhl, np => wmht);
end kmirnsax;

library ieee;
use ieee.std_logic_1164.all;

entity yh is
  port (oxace : linkage std_logic_vector(0 to 2));
end yh;

library ieee;
use ieee.std_logic_1164.all;

architecture qsz of yh is
  signal bx : boolean;
  signal hnha : real_vector(2 downto 0);
  signal nsumpxmgk : bit_vector(0 downto 1);
  signal ogrnhydgm : real;
  signal qkmt : std_logic_vector(3 to 4);
  signal vccds : integer;
  signal l : boolean;
  signal e : real_vector(2 downto 0);
begin
  vf : entity work.gbptcn
    port map (tuhozwifw => e, np => l);
  otcwmuecl : entity work.ki
    port map (dhjwabsyac => vccds, dpy => qkmt, uspg => ogrnhydgm, zkvsnwqoot => nsumpxmgk);
  f : entity work.gbptcn
    port map (tuhozwifw => hnha, np => bx);
end qsz;



-- Seed after: 18345655588447027323,18238119570016518405
