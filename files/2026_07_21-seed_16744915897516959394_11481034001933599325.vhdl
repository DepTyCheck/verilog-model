-- Seed: 16744915897516959394,11481034001933599325

entity t is
  port (yhfc : out time; nncxxcuuj : linkage real_vector(0 downto 2));
end t;

architecture qqxgyjxcx of t is
  
begin
  -- Single-driven assignments
  yhfc <= yhfc;
end qqxgyjxcx;

entity x is
  port (ejulyhi : in real_vector(1 downto 2); u : buffer severity_level; tw : buffer real);
end x;

architecture txpo of x is
  signal duh : real_vector(0 downto 2);
  signal ad : time;
begin
  eldinbqc : entity work.t
    port map (yhfc => ad, nncxxcuuj => duh);
  
  -- Single-driven assignments
  u <= u;
  tw <= 241.331;
end txpo;

library ieee;
use ieee.std_logic_1164.all;

entity sna is
  port (wtx : linkage std_logic_vector(4 to 0); dxcpukmwvi : linkage boolean_vector(0 downto 1));
end sna;

architecture mfan of sna is
  signal am : real;
  signal b : severity_level;
  signal tbefcwcr : real_vector(1 downto 2);
  signal agse : real_vector(0 downto 2);
  signal pko : time;
  signal pp : real_vector(0 downto 2);
  signal pdkd : time;
begin
  bggt : entity work.t
    port map (yhfc => pdkd, nncxxcuuj => pp);
  pgoqfeue : entity work.t
    port map (yhfc => pko, nncxxcuuj => agse);
  nfehpfb : entity work.x
    port map (ejulyhi => tbefcwcr, u => b, tw => am);
  
  -- Single-driven assignments
  tbefcwcr <= (others => 0.0);
end mfan;



-- Seed after: 10198324598716866770,11481034001933599325
