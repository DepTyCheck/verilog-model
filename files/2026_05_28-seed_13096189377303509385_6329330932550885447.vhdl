-- Seed: 13096189377303509385,6329330932550885447

library ieee;
use ieee.std_logic_1164.all;

entity rm is
  port (nvp : buffer boolean; ouzukmcyxr : out bit; vcsnbyoxiv : inout std_logic; yliegcpwfw : inout bit);
end rm;



architecture lsrq of rm is
  
begin
  
end lsrq;

library ieee;
use ieee.std_logic_1164.all;

entity blnnhul is
  port (kjrznptrmi : in std_logic_vector(1 to 2));
end blnnhul;

library ieee;
use ieee.std_logic_1164.all;

architecture ocma of blnnhul is
  signal fut : bit;
  signal fxbhkpthg : std_logic;
  signal jms : bit;
  signal dsdrmdbsg : boolean;
  signal hatrlrafur : bit;
  signal omopfejwt : std_logic;
  signal jp : bit;
  signal aqc : boolean;
  signal euiwhl : bit;
  signal eqelwti : bit;
  signal ujrv : boolean;
  signal e : bit;
  signal twzk : std_logic;
  signal csxxndsmn : bit;
  signal pkygpn : boolean;
begin
  k : entity work.rm
    port map (nvp => pkygpn, ouzukmcyxr => csxxndsmn, vcsnbyoxiv => twzk, yliegcpwfw => e);
  dskuq : entity work.rm
    port map (nvp => ujrv, ouzukmcyxr => eqelwti, vcsnbyoxiv => twzk, yliegcpwfw => euiwhl);
  llgjguko : entity work.rm
    port map (nvp => aqc, ouzukmcyxr => jp, vcsnbyoxiv => omopfejwt, yliegcpwfw => hatrlrafur);
  kkfzotceul : entity work.rm
    port map (nvp => dsdrmdbsg, ouzukmcyxr => jms, vcsnbyoxiv => fxbhkpthg, yliegcpwfw => fut);
end ocma;

library ieee;
use ieee.std_logic_1164.all;

entity fyiibdcn is
  port (u : buffer std_logic_vector(4 to 1); m : inout time);
end fyiibdcn;

library ieee;
use ieee.std_logic_1164.all;

architecture ve of fyiibdcn is
  signal rrl : bit;
  signal fcfswul : bit;
  signal tgewdmsvge : boolean;
  signal yue : bit;
  signal yxstdifkls : std_logic;
  signal ycsgvsa : bit;
  signal vuut : boolean;
  signal mfoifamtpi : std_logic_vector(1 to 2);
  signal zixghevx : std_logic_vector(1 to 2);
begin
  loxwguzxnj : entity work.blnnhul
    port map (kjrznptrmi => zixghevx);
  k : entity work.blnnhul
    port map (kjrznptrmi => mfoifamtpi);
  tkolbqs : entity work.rm
    port map (nvp => vuut, ouzukmcyxr => ycsgvsa, vcsnbyoxiv => yxstdifkls, yliegcpwfw => yue);
  us : entity work.rm
    port map (nvp => tgewdmsvge, ouzukmcyxr => fcfswul, vcsnbyoxiv => yxstdifkls, yliegcpwfw => rrl);
end ve;



-- Seed after: 9089022717319516701,6329330932550885447
