-- Seed: 12020979435668144404,10463297573877745897

library ieee;
use ieee.std_logic_1164.all;

entity nckqf is
  port (ihbgr : out real; bfcreydxl : linkage time; jjaox : inout integer; vygx : buffer std_logic);
end nckqf;

architecture fpoqmirav of nckqf is
  
begin
  -- Single-driven assignments
  jjaox <= 2;
  ihbgr <= ihbgr;
end fpoqmirav;

entity lcotuuau is
  port (fte : buffer bit_vector(0 to 0); abg : in integer_vector(1 to 0));
end lcotuuau;

library ieee;
use ieee.std_logic_1164.all;

architecture qydghzzy of lcotuuau is
  signal yp : std_logic;
  signal eqebqry : integer;
  signal ihk : time;
  signal xcqvhckbxs : real;
  signal w : integer;
  signal aiy : time;
  signal vwyrox : real;
  signal wj : integer;
  signal xzhwbcwj : time;
  signal iupgiaoxaf : real;
  signal pjt : std_logic;
  signal myshpqs : integer;
  signal fwuwthex : time;
  signal zemjwt : real;
begin
  swk : entity work.nckqf
    port map (ihbgr => zemjwt, bfcreydxl => fwuwthex, jjaox => myshpqs, vygx => pjt);
  gzko : entity work.nckqf
    port map (ihbgr => iupgiaoxaf, bfcreydxl => xzhwbcwj, jjaox => wj, vygx => pjt);
  yxm : entity work.nckqf
    port map (ihbgr => vwyrox, bfcreydxl => aiy, jjaox => w, vygx => pjt);
  ibpxxitus : entity work.nckqf
    port map (ihbgr => xcqvhckbxs, bfcreydxl => ihk, jjaox => eqebqry, vygx => yp);
  
  -- Single-driven assignments
  fte <= fte;
  
  -- Multi-driven assignments
  pjt <= pjt;
  pjt <= pjt;
  yp <= 'W';
end qydghzzy;

entity z is
  port (zj : out time; vsoidc : linkage time; gi : in severity_level; rmysgvmej : linkage bit_vector(2 to 1));
end z;

library ieee;
use ieee.std_logic_1164.all;

architecture xms of z is
  signal drapauy : std_logic;
  signal maptpljp : integer;
  signal jrnedulsu : time;
  signal gouogbdp : real;
  signal fe : std_logic;
  signal jwnxuhs : integer;
  signal npykvickmb : time;
  signal tkqip : real;
  signal eiggswnft : integer_vector(1 to 0);
  signal lznr : bit_vector(0 to 0);
begin
  tr : entity work.lcotuuau
    port map (fte => lznr, abg => eiggswnft);
  rqg : entity work.nckqf
    port map (ihbgr => tkqip, bfcreydxl => npykvickmb, jjaox => jwnxuhs, vygx => fe);
  wzv : entity work.nckqf
    port map (ihbgr => gouogbdp, bfcreydxl => jrnedulsu, jjaox => maptpljp, vygx => drapauy);
  
  -- Single-driven assignments
  zj <= zj;
  eiggswnft <= eiggswnft;
  
  -- Multi-driven assignments
  drapauy <= '0';
  fe <= fe;
  fe <= fe;
end xms;



-- Seed after: 14760054169280349014,10463297573877745897
