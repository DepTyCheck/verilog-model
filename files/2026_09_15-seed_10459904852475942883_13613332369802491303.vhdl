-- Seed: 10459904852475942883,13613332369802491303

library ieee;
use ieee.std_logic_1164.all;

entity wrotubvc is
  port (y : inout std_logic_vector(4 to 0); kvdbp : linkage std_logic_vector(3 to 3); tonfdfhf : inout time_vector(1 to 0); epsok : out integer);
end wrotubvc;

architecture jq of wrotubvc is
  
begin
  -- Single-driven assignments
  epsok <= epsok;
  
  -- Multi-driven assignments
  y <= (others => '0');
  y <= (others => '0');
end jq;

library ieee;
use ieee.std_logic_1164.all;

entity fkvkaj is
  port (b : buffer std_logic; jegz : inout std_logic_vector(0 to 1); crhneaccw : inout integer; rtpz : linkage real);
end fkvkaj;

library ieee;
use ieee.std_logic_1164.all;

architecture ghixaauox of fkvkaj is
  signal wohn : time_vector(1 to 0);
  signal zbfbdvo : std_logic_vector(4 to 0);
  signal yord : integer;
  signal kfqtbxdq : time_vector(1 to 0);
  signal eifceicqlg : std_logic_vector(3 to 3);
  signal ghtwy : std_logic_vector(4 to 0);
begin
  paqmlmrt : entity work.wrotubvc
    port map (y => ghtwy, kvdbp => eifceicqlg, tonfdfhf => kfqtbxdq, epsok => yord);
  m : entity work.wrotubvc
    port map (y => zbfbdvo, kvdbp => eifceicqlg, tonfdfhf => wohn, epsok => crhneaccw);
  
  -- Multi-driven assignments
  jegz <= jegz;
  zbfbdvo <= ghtwy;
end ghixaauox;



-- Seed after: 15336610608718863862,13613332369802491303
