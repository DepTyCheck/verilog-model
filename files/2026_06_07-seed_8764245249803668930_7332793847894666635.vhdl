-- Seed: 8764245249803668930,7332793847894666635



entity wufgi is
  port (na : in real; q : linkage integer; rozsb : in real);
end wufgi;



architecture ush of wufgi is
  
begin
  
end ush;

library ieee;
use ieee.std_logic_1164.all;

entity fua is
  port (vfelc : buffer std_logic; wm : inout real);
end fua;



architecture ckt of fua is
  signal tajfcts : real;
  signal hvlnvsiu : real;
  signal fcvkjrbcv : real;
  signal c : integer;
  signal drde : real;
begin
  qichxpl : entity work.wufgi
    port map (na => drde, q => c, rozsb => fcvkjrbcv);
  zhlqr : entity work.wufgi
    port map (na => hvlnvsiu, q => c, rozsb => tajfcts);
end ckt;

library ieee;
use ieee.std_logic_1164.all;

entity qqayqiyfo is
  port (wlnnwro : out character; yrhk : linkage real; oj : buffer integer; knzdhh : linkage std_logic);
end qqayqiyfo;

library ieee;
use ieee.std_logic_1164.all;

architecture ldtq of qqayqiyfo is
  signal gn : real;
  signal xzkvgvrby : integer;
  signal yiazu : real;
  signal uecasc : std_logic;
  signal ywffjfq : integer;
  signal ob : real;
  signal rt : real;
begin
  mtcilvone : entity work.wufgi
    port map (na => rt, q => oj, rozsb => ob);
  nittwzre : entity work.wufgi
    port map (na => ob, q => ywffjfq, rozsb => rt);
  fqarxrgtcf : entity work.fua
    port map (vfelc => uecasc, wm => yiazu);
  oehqcwvy : entity work.wufgi
    port map (na => rt, q => xzkvgvrby, rozsb => gn);
end ldtq;

library ieee;
use ieee.std_logic_1164.all;

entity wz is
  port (ndw : in real; pvmopy : linkage std_logic_vector(3 downto 2); hxby : in real);
end wz;

library ieee;
use ieee.std_logic_1164.all;

architecture dyuw of wz is
  signal v : real;
  signal gtsuyzs : std_logic;
begin
  lxpfydh : entity work.fua
    port map (vfelc => gtsuyzs, wm => v);
end dyuw;



-- Seed after: 17607582569821432195,7332793847894666635
