-- Seed: 1138580726255765206,1641934135882347475

library ieee;
use ieee.std_logic_1164.all;

entity ualmhmz is
  port (spstiqesdy : buffer integer; exwlaklz : linkage std_logic; rqif : inout severity_level; e : out time);
end ualmhmz;



architecture z of ualmhmz is
  
begin
  
end z;



entity pntd is
  port (lnluv : buffer real; f : out integer; fizacq : inout integer);
end pntd;

library ieee;
use ieee.std_logic_1164.all;

architecture zwji of pntd is
  signal ukxazfv : time;
  signal ja : severity_level;
  signal wabwgudu : time;
  signal lk : severity_level;
  signal whdh : std_logic;
begin
  ipeqzo : entity work.ualmhmz
    port map (spstiqesdy => f, exwlaklz => whdh, rqif => lk, e => wabwgudu);
  yzi : entity work.ualmhmz
    port map (spstiqesdy => fizacq, exwlaklz => whdh, rqif => ja, e => ukxazfv);
end zwji;



entity gqwqfs is
  port (dhpciujmk : inout character; fofpfas : buffer bit_vector(4 to 0));
end gqwqfs;

library ieee;
use ieee.std_logic_1164.all;

architecture veqqpigfvz of gqwqfs is
  signal zhwigjac : time;
  signal c : severity_level;
  signal yxg : integer;
  signal tooubcecba : time;
  signal tjhq : severity_level;
  signal fsds : integer;
  signal kwkbc : time;
  signal wg : severity_level;
  signal hjbofe : std_logic;
  signal ryqqaekpm : integer;
begin
  ebyjmc : entity work.ualmhmz
    port map (spstiqesdy => ryqqaekpm, exwlaklz => hjbofe, rqif => wg, e => kwkbc);
  ngvkb : entity work.ualmhmz
    port map (spstiqesdy => fsds, exwlaklz => hjbofe, rqif => tjhq, e => tooubcecba);
  jbx : entity work.ualmhmz
    port map (spstiqesdy => yxg, exwlaklz => hjbofe, rqif => c, e => zhwigjac);
end veqqpigfvz;



-- Seed after: 15706337408897063313,1641934135882347475
