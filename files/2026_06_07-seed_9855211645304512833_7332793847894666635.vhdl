-- Seed: 9855211645304512833,7332793847894666635



entity xjebs is
  port (ohhs : in time_vector(2 to 0); fe : linkage integer; kv : out real);
end xjebs;



architecture vedofgoq of xjebs is
  
begin
  
end vedofgoq;



entity cwqclanmz is
  port (yxqhpeyl : in boolean);
end cwqclanmz;



architecture ktfzni of cwqclanmz is
  signal qbfp : real;
  signal lwkiu : real;
  signal quc : real;
  signal p : integer;
  signal rctyag : time_vector(2 to 0);
begin
  amojrns : entity work.xjebs
    port map (ohhs => rctyag, fe => p, kv => quc);
  mavwngao : entity work.xjebs
    port map (ohhs => rctyag, fe => p, kv => lwkiu);
  zredns : entity work.xjebs
    port map (ohhs => rctyag, fe => p, kv => qbfp);
end ktfzni;



entity kmnt is
  port (zkhyft : buffer real_vector(1 to 2); gnlevmu : linkage integer_vector(4 downto 3); obvwp : linkage real_vector(0 downto 4); oszr : in real);
end kmnt;



architecture icrsx of kmnt is
  signal xp : real;
  signal ky : time_vector(2 to 0);
  signal fp : real;
  signal kgrmbltgyc : integer;
  signal qilmlf : time_vector(2 to 0);
begin
  kaiyxdxntz : entity work.xjebs
    port map (ohhs => qilmlf, fe => kgrmbltgyc, kv => fp);
  ajuvskp : entity work.xjebs
    port map (ohhs => ky, fe => kgrmbltgyc, kv => xp);
end icrsx;



-- Seed after: 14732565511478067718,7332793847894666635
