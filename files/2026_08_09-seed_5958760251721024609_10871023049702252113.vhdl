-- Seed: 5958760251721024609,10871023049702252113

entity mv is
  port (bbkdwtml : inout time; vkaza : in real; hpxl : linkage string(3 to 1));
end mv;

architecture s of mv is
  
begin
  -- Single-driven assignments
  bbkdwtml <= 8#4_0_3_7# ps;
end s;

library ieee;
use ieee.std_logic_1164.all;

entity je is
  port (dnetl : inout std_logic_vector(2 downto 1); ejcaecn : inout bit);
end je;

architecture gwvw of je is
  signal xqna : string(3 to 1);
  signal vdhwd : real;
  signal ojnesbqq : time;
  signal m : string(3 to 1);
  signal b : time;
  signal d : string(3 to 1);
  signal urs : real;
  signal lot : time;
  signal fthe : string(3 to 1);
  signal cbsky : real;
  signal irrja : time;
begin
  zrlekit : entity work.mv
    port map (bbkdwtml => irrja, vkaza => cbsky, hpxl => fthe);
  gglxwyeqa : entity work.mv
    port map (bbkdwtml => lot, vkaza => urs, hpxl => d);
  hukenz : entity work.mv
    port map (bbkdwtml => b, vkaza => urs, hpxl => m);
  mwojvz : entity work.mv
    port map (bbkdwtml => ojnesbqq, vkaza => vdhwd, hpxl => xqna);
  
  -- Single-driven assignments
  cbsky <= 2444.1;
  ejcaecn <= '1';
  urs <= cbsky;
end gwvw;



-- Seed after: 4506810479725553194,10871023049702252113
