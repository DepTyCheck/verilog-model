-- Seed: 4980987900541193526,17611625116949931313



entity dn is
  port (ohdjhu : inout time; rvqbxk : inout real; ihogyv : buffer character; sisbb : linkage real);
end dn;



architecture argrehdgi of dn is
  
begin
  
end argrehdgi;

library ieee;
use ieee.std_logic_1164.all;

entity hvnzvex is
  port (srblmpgd : in integer; kgdlfpla : out real; ypihbyfid : in std_logic; qtofdsqtv : in std_logic);
end hvnzvex;



architecture kvvj of hvnzvex is
  signal ritmre : real;
  signal fjlfyahqoi : character;
  signal u : real;
  signal webn : time;
  signal lyl : real;
  signal hzusf : character;
  signal vbpiieg : real;
  signal evu : time;
  signal qfarwqnbz : character;
  signal lntivc : time;
begin
  bvjpqsb : entity work.dn
    port map (ohdjhu => lntivc, rvqbxk => kgdlfpla, ihogyv => qfarwqnbz, sisbb => kgdlfpla);
  ft : entity work.dn
    port map (ohdjhu => evu, rvqbxk => vbpiieg, ihogyv => hzusf, sisbb => lyl);
  tgc : entity work.dn
    port map (ohdjhu => webn, rvqbxk => u, ihogyv => fjlfyahqoi, sisbb => ritmre);
end kvvj;



entity lknwderyx is
  port (lqakzjn : out severity_level; cdnax : buffer time; jhdmdcnkm : buffer real; nfflqpg : linkage integer);
end lknwderyx;



architecture gy of lknwderyx is
  signal jcgrfciekx : character;
  signal ocfnpqcmd : time;
  signal pb : character;
  signal uwqsgxn : real;
  signal pip : character;
  signal dfdsffsg : real;
  signal ovbcus : time;
begin
  gb : entity work.dn
    port map (ohdjhu => ovbcus, rvqbxk => dfdsffsg, ihogyv => pip, sisbb => uwqsgxn);
  nbvcrgijix : entity work.dn
    port map (ohdjhu => cdnax, rvqbxk => jhdmdcnkm, ihogyv => pb, sisbb => dfdsffsg);
  nvivm : entity work.dn
    port map (ohdjhu => ocfnpqcmd, rvqbxk => uwqsgxn, ihogyv => jcgrfciekx, sisbb => jhdmdcnkm);
end gy;



-- Seed after: 3317558308909607397,17611625116949931313
