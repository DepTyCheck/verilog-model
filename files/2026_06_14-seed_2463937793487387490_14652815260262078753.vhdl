-- Seed: 2463937793487387490,14652815260262078753

library ieee;
use ieee.std_logic_1164.all;

entity fywv is
  port (eugbagqztq : linkage time; g : linkage real; ahcg : out real; bfx : out std_logic_vector(1 downto 0));
end fywv;

architecture ah of fywv is
  
begin
  -- Single-driven assignments
  ahcg <= 4_4_1_2.1_3_4;
  
  -- Multi-driven assignments
  bfx <= "HU";
  bfx <= "L-";
  bfx <= "HW";
  bfx <= "0X";
end ah;

entity ouisfmv is
  port (u : linkage real_vector(2 downto 1); bxtnakmbm : buffer integer);
end ouisfmv;

library ieee;
use ieee.std_logic_1164.all;

architecture ipvn of ouisfmv is
  signal d : real;
  signal ih : real;
  signal egvuqsvk : time;
  signal bo : std_logic_vector(1 downto 0);
  signal arcszr : real;
  signal hplif : real;
  signal ibmywowzim : time;
  signal imzkcblkv : std_logic_vector(1 downto 0);
  signal pqndcfsa : real;
  signal hnadheogfk : real;
  signal iaub : time;
begin
  hw : entity work.fywv
    port map (eugbagqztq => iaub, g => hnadheogfk, ahcg => pqndcfsa, bfx => imzkcblkv);
  qxqkau : entity work.fywv
    port map (eugbagqztq => ibmywowzim, g => hplif, ahcg => arcszr, bfx => bo);
  paifu : entity work.fywv
    port map (eugbagqztq => egvuqsvk, g => ih, ahcg => d, bfx => bo);
  
  -- Single-driven assignments
  bxtnakmbm <= 2#1_1_1#;
end ipvn;



-- Seed after: 7564962745670689935,14652815260262078753
