-- Seed: 9444782367127515787,4177195558088809003

entity gxxzksg is
  port (x : linkage time; pjkz : linkage time; dwlcvvxa : out time_vector(2 to 2));
end gxxzksg;

architecture y of gxxzksg is
  
begin
  -- Single-driven assignments
  dwlcvvxa <= (others => 8#10# fs);
end y;

library ieee;
use ieee.std_logic_1164.all;

entity szbq is
  port (wyiorqjwjn : inout std_logic; rury : buffer time);
end szbq;

architecture yhw of szbq is
  signal eebfxekfs : time_vector(2 to 2);
  signal kuzpcui : time;
  signal sjulwhlyn : time_vector(2 to 2);
  signal hzm : time;
  signal m : time;
  signal okokoxa : time_vector(2 to 2);
  signal xxuhouv : time;
  signal jcit : time;
begin
  z : entity work.gxxzksg
    port map (x => jcit, pjkz => xxuhouv, dwlcvvxa => okokoxa);
  zkcqdpl : entity work.gxxzksg
    port map (x => m, pjkz => hzm, dwlcvvxa => sjulwhlyn);
  quclfkyz : entity work.gxxzksg
    port map (x => rury, pjkz => kuzpcui, dwlcvvxa => eebfxekfs);
  
  -- Multi-driven assignments
  wyiorqjwjn <= wyiorqjwjn;
  wyiorqjwjn <= 'H';
  wyiorqjwjn <= wyiorqjwjn;
  wyiorqjwjn <= 'X';
end yhw;

library ieee;
use ieee.std_logic_1164.all;

entity owjgdg is
  port (jf : in time_vector(0 to 3); bjvsg : in std_logic; z : linkage time_vector(3 downto 3));
end owjgdg;

architecture bcyiwqah of owjgdg is
  signal j : time_vector(2 to 2);
  signal adgkq : time;
  signal uvrhzobkly : time;
  signal qwrnwur : time_vector(2 to 2);
  signal ksj : time;
  signal mqlderejge : time;
begin
  lpqz : entity work.gxxzksg
    port map (x => mqlderejge, pjkz => ksj, dwlcvvxa => qwrnwur);
  oempiuf : entity work.gxxzksg
    port map (x => uvrhzobkly, pjkz => adgkq, dwlcvvxa => j);
end bcyiwqah;

entity ahyyg is
  port (sxo : buffer time; wddcvbv : inout time; wpjjzrj : linkage boolean);
end ahyyg;

architecture fftflkqct of ahyyg is
  signal udtvz : time_vector(2 to 2);
  signal eosxpchc : time;
  signal axgyhdesnw : time_vector(2 to 2);
  signal kmm : time;
  signal wxucgazsm : time_vector(2 to 2);
  signal nnjixcdq : time;
  signal mxcfgu : time;
  signal njn : time_vector(2 to 2);
  signal re : time;
  signal tnc : time;
begin
  fvyobtkm : entity work.gxxzksg
    port map (x => tnc, pjkz => re, dwlcvvxa => njn);
  hvrc : entity work.gxxzksg
    port map (x => mxcfgu, pjkz => nnjixcdq, dwlcvvxa => wxucgazsm);
  f : entity work.gxxzksg
    port map (x => kmm, pjkz => wddcvbv, dwlcvvxa => axgyhdesnw);
  pfbqus : entity work.gxxzksg
    port map (x => eosxpchc, pjkz => sxo, dwlcvvxa => udtvz);
end fftflkqct;



-- Seed after: 11201921722049846772,4177195558088809003
