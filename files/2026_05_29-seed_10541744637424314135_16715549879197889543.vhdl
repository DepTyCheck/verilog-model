-- Seed: 10541744637424314135,16715549879197889543



entity hgaikxrt is
  port (k : inout severity_level);
end hgaikxrt;



architecture ul of hgaikxrt is
  
begin
  
end ul;



entity vcseh is
  port (k : linkage bit_vector(1 to 4); so : buffer real; tnnnegqfql : inout bit);
end vcseh;



architecture jlgeuqx of vcseh is
  signal zw : severity_level;
begin
  xasrdm : entity work.hgaikxrt
    port map (k => zw);
end jlgeuqx;



entity ka is
  port (pjadfjary : linkage time);
end ka;



architecture kmacb of ka is
  signal a : severity_level;
  signal qztkslqu : bit;
  signal jex : real;
  signal tuoymbbc : severity_level;
  signal kfdxiudld : bit;
  signal tqs : real;
  signal tipt : bit_vector(1 to 4);
begin
  tdek : entity work.vcseh
    port map (k => tipt, so => tqs, tnnnegqfql => kfdxiudld);
  zackbkkod : entity work.hgaikxrt
    port map (k => tuoymbbc);
  ebeehqdkl : entity work.vcseh
    port map (k => tipt, so => jex, tnnnegqfql => qztkslqu);
  d : entity work.hgaikxrt
    port map (k => a);
end kmacb;

library ieee;
use ieee.std_logic_1164.all;

entity thmasiwo is
  port (mdemrwvhff : buffer std_logic_vector(2 downto 3));
end thmasiwo;



architecture s of thmasiwo is
  signal b : severity_level;
  signal ojnlayfnu : time;
  signal zkzou : bit;
  signal dpbinn : real;
  signal myiocql : bit_vector(1 to 4);
begin
  coo : entity work.vcseh
    port map (k => myiocql, so => dpbinn, tnnnegqfql => zkzou);
  nobg : entity work.ka
    port map (pjadfjary => ojnlayfnu);
  slqk : entity work.hgaikxrt
    port map (k => b);
end s;



-- Seed after: 6723399465233774359,16715549879197889543
