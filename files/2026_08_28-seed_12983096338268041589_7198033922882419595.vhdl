-- Seed: 12983096338268041589,7198033922882419595

entity xbo is
  port (mflrmlx : in string(5 downto 4));
end xbo;

architecture hggtmawdt of xbo is
  
begin
  
end hggtmawdt;

entity eigmnhc is
  port (yphajvxcg : in real);
end eigmnhc;

architecture r of eigmnhc is
  signal tivkd : string(5 downto 4);
  signal mvupovf : string(5 downto 4);
  signal cff : string(5 downto 4);
begin
  hruz : entity work.xbo
    port map (mflrmlx => cff);
  tbrk : entity work.xbo
    port map (mflrmlx => mvupovf);
  ahsd : entity work.xbo
    port map (mflrmlx => tivkd);
  rgvkt : entity work.xbo
    port map (mflrmlx => cff);
  
  -- Single-driven assignments
  cff <= mvupovf;
  tivkd <= ('f', 'y');
  mvupovf <= ('x', 'n');
end r;

entity nx is
  port (krhdqchvq : out character);
end nx;

architecture chlphsrzg of nx is
  signal xrh : string(5 downto 4);
  signal x : real;
begin
  ypypf : entity work.eigmnhc
    port map (yphajvxcg => x);
  brfowowml : entity work.xbo
    port map (mflrmlx => xrh);
  xggbclggo : entity work.xbo
    port map (mflrmlx => xrh);
  
  -- Single-driven assignments
  krhdqchvq <= krhdqchvq;
  x <= x;
  xrh <= xrh;
end chlphsrzg;



-- Seed after: 9960576173864060723,7198033922882419595
