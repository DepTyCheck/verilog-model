-- Seed: 17632942443840243409,1420300365443511127

library ieee;
use ieee.std_logic_1164.all;

entity dqgxnzs is
  port (u : in integer; dhrkfr : in time; maljug : out time; qav : in std_logic);
end dqgxnzs;



architecture uwipq of dqgxnzs is
  
begin
  
end uwipq;



entity mh is
  port (inupykrh : out real; bfoa : inout time; j : out boolean);
end mh;

library ieee;
use ieee.std_logic_1164.all;

architecture eoxxdqdmcy of mh is
  signal nfcufjrcj : std_logic;
  signal gqoaa : time;
  signal bxtlgnavcn : integer;
begin
  ix : entity work.dqgxnzs
    port map (u => bxtlgnavcn, dhrkfr => bfoa, maljug => gqoaa, qav => nfcufjrcj);
end eoxxdqdmcy;



entity cekfbs is
  port (p : linkage bit; qzhca : linkage integer; lvwzwtkphv : out character);
end cekfbs;

library ieee;
use ieee.std_logic_1164.all;

architecture pddv of cekfbs is
  signal cytwyk : time;
  signal fhmcz : time;
  signal jjz : time;
  signal lw : std_logic;
  signal ge : time;
  signal u : integer;
  signal dgbctbcye : boolean;
  signal hid : time;
  signal iox : real;
begin
  tl : entity work.mh
    port map (inupykrh => iox, bfoa => hid, j => dgbctbcye);
  qqmsriiw : entity work.dqgxnzs
    port map (u => u, dhrkfr => ge, maljug => ge, qav => lw);
  gbco : entity work.dqgxnzs
    port map (u => u, dhrkfr => jjz, maljug => fhmcz, qav => lw);
  fyzeiwkg : entity work.dqgxnzs
    port map (u => u, dhrkfr => hid, maljug => cytwyk, qav => lw);
end pddv;

library ieee;
use ieee.std_logic_1164.all;

entity ryrjtyd is
  port (xkm : in integer; mau : in std_logic; ffjhjuhpg : linkage time; banpxrczb : linkage time);
end ryrjtyd;

library ieee;
use ieee.std_logic_1164.all;

architecture jfrzwmn of ryrjtyd is
  signal qzsaisyfl : time;
  signal cdocqhp : std_logic;
  signal aeftqts : time;
  signal otwvrl : time;
  signal fc : time;
  signal kvc : time;
  signal arlm : character;
  signal ejfo : integer;
  signal z : bit;
begin
  xdtrbttfw : entity work.cekfbs
    port map (p => z, qzhca => ejfo, lvwzwtkphv => arlm);
  owqat : entity work.dqgxnzs
    port map (u => xkm, dhrkfr => kvc, maljug => fc, qav => mau);
  dpgp : entity work.dqgxnzs
    port map (u => xkm, dhrkfr => otwvrl, maljug => aeftqts, qav => cdocqhp);
  qn : entity work.dqgxnzs
    port map (u => xkm, dhrkfr => kvc, maljug => qzsaisyfl, qav => mau);
end jfrzwmn;



-- Seed after: 9913622647986549600,1420300365443511127
