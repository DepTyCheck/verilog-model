-- Seed: 1201476585823813322,1834764876137802293

library ieee;
use ieee.std_logic_1164.all;

entity vm is
  port (z : buffer time; nenza : buffer real; eblvkl : linkage std_logic_vector(2 to 3); qli : in integer);
end vm;

architecture kgnc of vm is
  
begin
  -- Single-driven assignments
  z <= 1 min;
  nenza <= 8#4.617#;
end kgnc;

entity aquu is
  port (qketgs : in time);
end aquu;

library ieee;
use ieee.std_logic_1164.all;

architecture dzia of aquu is
  signal webatxamx : integer;
  signal ysxo : real;
  signal turewgp : time;
  signal gbtmwdb : integer;
  signal cjrqfrz : std_logic_vector(2 to 3);
  signal bucjnqwu : real;
  signal qlnurh : time;
  signal krbxzhigv : integer;
  signal wzqtnokhhk : real;
  signal hkr : time;
  signal wzauvk : integer;
  signal yvi : std_logic_vector(2 to 3);
  signal qvskzhnb : real;
  signal cnguh : time;
begin
  frhwrmys : entity work.vm
    port map (z => cnguh, nenza => qvskzhnb, eblvkl => yvi, qli => wzauvk);
  jyjndheaz : entity work.vm
    port map (z => hkr, nenza => wzqtnokhhk, eblvkl => yvi, qli => krbxzhigv);
  inrlmqrek : entity work.vm
    port map (z => qlnurh, nenza => bucjnqwu, eblvkl => cjrqfrz, qli => gbtmwdb);
  moajuslz : entity work.vm
    port map (z => turewgp, nenza => ysxo, eblvkl => yvi, qli => webatxamx);
  
  -- Single-driven assignments
  wzauvk <= 2#1_0_0#;
  krbxzhigv <= 8#7144#;
end dzia;



-- Seed after: 16891576168288269477,1834764876137802293
