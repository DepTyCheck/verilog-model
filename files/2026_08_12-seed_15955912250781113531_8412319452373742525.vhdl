-- Seed: 15955912250781113531,8412319452373742525

library ieee;
use ieee.std_logic_1164.all;

entity pxi is
  port (hfhwpxi : linkage integer; bwp : in integer_vector(2 to 3); bw : inout std_logic; qiire : linkage string(3 downto 3));
end pxi;

architecture bppg of pxi is
  
begin
  -- Multi-driven assignments
  bw <= 'Z';
end bppg;

library ieee;
use ieee.std_logic_1164.all;

entity fpikzqp is
  port (emh : buffer std_logic; p : buffer integer; hybof : out character);
end fpikzqp;

library ieee;
use ieee.std_logic_1164.all;

architecture jyuun of fpikzqp is
  signal sdfpbvgr : string(3 downto 3);
  signal zyrzbzyxy : integer_vector(2 to 3);
  signal xs : integer;
  signal qej : string(3 downto 3);
  signal lznnucojf : std_logic;
  signal waotd : integer_vector(2 to 3);
  signal uhwmb : integer;
begin
  jlmyjemahl : entity work.pxi
    port map (hfhwpxi => uhwmb, bwp => waotd, bw => lznnucojf, qiire => qej);
  vfxkgwi : entity work.pxi
    port map (hfhwpxi => xs, bwp => zyrzbzyxy, bw => lznnucojf, qiire => sdfpbvgr);
  
  -- Single-driven assignments
  p <= p;
  zyrzbzyxy <= waotd;
  waotd <= (14113, 8#3_6_3#);
  hybof <= hybof;
  
  -- Multi-driven assignments
  lznnucojf <= '0';
end jyuun;

library ieee;
use ieee.std_logic_1164.all;

entity aibbkxpw is
  port (suku : buffer integer; rvewwpd : buffer std_logic);
end aibbkxpw;

library ieee;
use ieee.std_logic_1164.all;

architecture z of aibbkxpw is
  signal i : string(3 downto 3);
  signal muyrmfuo : string(3 downto 3);
  signal v : std_logic;
  signal ru : integer_vector(2 to 3);
  signal jyv : integer;
begin
  pmrdxzomu : entity work.pxi
    port map (hfhwpxi => jyv, bwp => ru, bw => v, qiire => muyrmfuo);
  jfy : entity work.pxi
    port map (hfhwpxi => suku, bwp => ru, bw => rvewwpd, qiire => i);
  
  -- Single-driven assignments
  ru <= ru;
  
  -- Multi-driven assignments
  v <= rvewwpd;
  rvewwpd <= 'U';
  v <= 'X';
  rvewwpd <= rvewwpd;
end z;

entity ir is
  port (tzpkjq : out time; vmjoaqoi : linkage real; yy : inout real; hkltcqqal : inout time);
end ir;

library ieee;
use ieee.std_logic_1164.all;

architecture kgmc of ir is
  signal avv : character;
  signal khfbsnis : integer;
  signal uhcvg : std_logic;
begin
  j : entity work.fpikzqp
    port map (emh => uhcvg, p => khfbsnis, hybof => avv);
  
  -- Multi-driven assignments
  uhcvg <= 'U';
  uhcvg <= 'W';
  uhcvg <= uhcvg;
end kgmc;



-- Seed after: 15395050745067457145,8412319452373742525
