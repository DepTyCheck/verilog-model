-- Seed: 7257926889235781718,16188444798499499427

entity m is
  port (k : linkage character; ymayot : out time);
end m;

architecture tiknqf of m is
  
begin
  
end tiknqf;

library ieee;
use ieee.std_logic_1164.all;

entity ufbouou is
  port (wszav : inout real; lrsbyuurw : in severity_level; aqrqzd : in std_logic);
end ufbouou;

architecture cbvs of ufbouou is
  signal xyufft : time;
  signal zzexsvnorr : character;
  signal sveekd : time;
  signal h : character;
  signal lffzh : time;
  signal kephfbg : character;
begin
  rxljril : entity work.m
    port map (k => kephfbg, ymayot => lffzh);
  p : entity work.m
    port map (k => h, ymayot => sveekd);
  rlbai : entity work.m
    port map (k => zzexsvnorr, ymayot => xyufft);
  
  -- Single-driven assignments
  wszav <= wszav;
end cbvs;

entity hdzyfuss is
  port (eponnwwcn : inout time; jhdq : linkage character);
end hdzyfuss;

architecture jousy of hdzyfuss is
  signal yuye : time;
  signal awbc : character;
  signal lyhxmb : time;
  signal ilhrz : character;
begin
  dscj : entity work.m
    port map (k => ilhrz, ymayot => eponnwwcn);
  fexy : entity work.m
    port map (k => jhdq, ymayot => lyhxmb);
  lzudlhz : entity work.m
    port map (k => awbc, ymayot => yuye);
end jousy;

library ieee;
use ieee.std_logic_1164.all;

entity ldhcx is
  port (p : inout integer; pypriz : inout std_logic; dgnxjt : buffer string(4 to 2));
end ldhcx;

architecture oymupc of ldhcx is
  signal szujzvdvh : severity_level;
  signal mwrdsfgnm : real;
  signal bfmin : time;
  signal gmstk : character;
  signal efawtip : time;
  signal gpy : character;
begin
  fwp : entity work.m
    port map (k => gpy, ymayot => efawtip);
  lw : entity work.m
    port map (k => gmstk, ymayot => bfmin);
  dsrqz : entity work.ufbouou
    port map (wszav => mwrdsfgnm, lrsbyuurw => szujzvdvh, aqrqzd => pypriz);
  
  -- Single-driven assignments
  szujzvdvh <= szujzvdvh;
  dgnxjt <= (others => ' ');
  p <= p;
  
  -- Multi-driven assignments
  pypriz <= '-';
end oymupc;



-- Seed after: 5836671062454208458,16188444798499499427
