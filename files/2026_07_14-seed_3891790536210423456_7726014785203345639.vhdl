-- Seed: 3891790536210423456,7726014785203345639

use std.reflection.all;

entity mule is
  port (ybihubrsr : inout array_value_mirror; zja : inout integer);
end mule;

architecture xrjor of mule is
  
begin
  
end xrjor;

entity fku is
  port (s : in integer);
end fku;

use std.reflection.all;

architecture ulwd of fku is
  signal pbdjpd : integer;
  shared variable vgmcpswdwa : array_value_mirror;
  signal okp : integer;
  shared variable x : array_value_mirror;
  signal qrc : integer;
  shared variable agfreklcmy : array_value_mirror;
begin
  nl : entity work.mule
    port map (ybihubrsr => agfreklcmy, zja => qrc);
  b : entity work.mule
    port map (ybihubrsr => x, zja => okp);
  tkdbgblbnd : entity work.mule
    port map (ybihubrsr => vgmcpswdwa, zja => pbdjpd);
end ulwd;

use std.reflection.all;

entity ekxry is
  port (wubjujumf : inout protected_subtype_mirror; mbnabqgf : inout subtype_mirror; ygthlz : buffer time; wmwyuhm : out real);
end ekxry;

use std.reflection.all;

architecture xxanjc of ekxry is
  signal wdmoo : integer;
  shared variable suatu : array_value_mirror;
begin
  g : entity work.mule
    port map (ybihubrsr => suatu, zja => wdmoo);
  
  -- Single-driven assignments
  ygthlz <= ygthlz;
  wmwyuhm <= 1.3;
end xxanjc;

use std.reflection.all;

entity axao is
  port (uprdjucj : inout array_subtype_mirror; czxpeq : inout floating_value_mirror);
end axao;

use std.reflection.all;

architecture qsvgjaw of axao is
  signal y : integer;
  shared variable ommtbbrx : array_value_mirror;
begin
  fkm : entity work.mule
    port map (ybihubrsr => ommtbbrx, zja => y);
end qsvgjaw;



-- Seed after: 3586797883844544327,7726014785203345639
