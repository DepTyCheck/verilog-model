-- Seed: 1108963469845681186,4860866131898729603

entity g is
  port (hcnwwx : buffer real; wefwipdijn : in time; ncp : buffer boolean);
end g;

architecture jl of g is
  
begin
  
end jl;

entity iprwmxdurv is
  port (qwb : inout integer; yugpbvzggb : in bit_vector(0 downto 4); hlg : out real);
end iprwmxdurv;

architecture obfz of iprwmxdurv is
  signal arenw : boolean;
  signal c : boolean;
  signal scjf : time;
  signal fqsqt : real;
begin
  lg : entity work.g
    port map (hcnwwx => fqsqt, wefwipdijn => scjf, ncp => c);
  mccf : entity work.g
    port map (hcnwwx => hlg, wefwipdijn => scjf, ncp => arenw);
end obfz;

library ieee;
use ieee.std_logic_1164.all;

entity zyjd is
  port (rhdax : inout std_logic; xe : inout std_logic_vector(4 downto 3); c : inout time);
end zyjd;

architecture bdks of zyjd is
  signal chbekkmyae : boolean;
  signal sabwchm : real;
begin
  avsnslkb : entity work.g
    port map (hcnwwx => sabwchm, wefwipdijn => c, ncp => chbekkmyae);
  
  -- Single-driven assignments
  c <= 8#3_7_6_6_3.20271# ps;
end bdks;

entity qncdrzh is
  port (uuvr : out integer_vector(0 to 1); ht : inout real; aqhd : in bit_vector(3 downto 3));
end qncdrzh;

architecture p of qncdrzh is
  signal ienbhtmoa : boolean;
  signal hywllnrqi : time;
  signal j : real;
  signal i : boolean;
  signal cayggo : boolean;
  signal rmevy : time;
  signal nnqbndexu : real;
begin
  dqctfr : entity work.g
    port map (hcnwwx => nnqbndexu, wefwipdijn => rmevy, ncp => cayggo);
  srcrvfyof : entity work.g
    port map (hcnwwx => ht, wefwipdijn => rmevy, ncp => i);
  vslmlwa : entity work.g
    port map (hcnwwx => j, wefwipdijn => hywllnrqi, ncp => ienbhtmoa);
end p;



-- Seed after: 18227546451280204108,4860866131898729603
