-- Seed: 10971730912179855371,4292249356257567981

entity ccymznliei is
  port (eap : in bit_vector(3 to 1); sk : out character; gw : in real);
end ccymznliei;

architecture lirykn of ccymznliei is
  
begin
  
end lirykn;

entity cfjrzl is
  port (ey : buffer bit_vector(2 to 3));
end cfjrzl;

architecture ohle of cfjrzl is
  signal sjgq : character;
  signal cv : real;
  signal ssivkgcjil : character;
  signal vfi : bit_vector(3 to 1);
  signal vn : character;
  signal vu : real;
  signal kms : character;
  signal glw : bit_vector(3 to 1);
begin
  ub : entity work.ccymznliei
    port map (eap => glw, sk => kms, gw => vu);
  sibni : entity work.ccymznliei
    port map (eap => glw, sk => vn, gw => vu);
  vzkvkhgrqr : entity work.ccymznliei
    port map (eap => vfi, sk => ssivkgcjil, gw => cv);
  hwzkr : entity work.ccymznliei
    port map (eap => glw, sk => sjgq, gw => vu);
  
  -- Single-driven assignments
  vu <= 16#A.6A085#;
end ohle;



-- Seed after: 12737001308195344404,4292249356257567981
