-- Seed: 3432904438337859714,11127274767545411571

entity jnj is
  port (tt : inout real_vector(4 to 1); yazbpqkcp : out severity_level; uyzhuh : inout real; ibwnp : out severity_level);
end jnj;

architecture qmq of jnj is
  
begin
  -- Single-driven assignments
  ibwnp <= ibwnp;
  uyzhuh <= uyzhuh;
end qmq;

entity zzdb is
  port (kopibj : out integer; sh : inout real; yflgagrr : buffer real);
end zzdb;

architecture fwj of zzdb is
  signal rmgf : severity_level;
  signal jhjloobch : severity_level;
  signal oblz : real_vector(4 to 1);
  signal qm : severity_level;
  signal p : severity_level;
  signal vf : real_vector(4 to 1);
begin
  whd : entity work.jnj
    port map (tt => vf, yazbpqkcp => p, uyzhuh => yflgagrr, ibwnp => qm);
  ceimrr : entity work.jnj
    port map (tt => oblz, yazbpqkcp => jhjloobch, uyzhuh => sh, ibwnp => rmgf);
end fwj;

entity crrc is
  port (t : inout integer; dmy : buffer time);
end crrc;

architecture qxocxa of crrc is
  signal agocnoayk : severity_level;
  signal w : real;
  signal gcwep : severity_level;
  signal mndsicd : real_vector(4 to 1);
  signal baetmj : real;
  signal qt : real;
  signal rqhtiq : integer;
  signal sgtw : real;
  signal ccvqxu : real;
  signal hr : integer;
  signal soeaeztfe : severity_level;
  signal gc : real;
  signal oy : severity_level;
  signal tfxrb : real_vector(4 to 1);
begin
  eqk : entity work.jnj
    port map (tt => tfxrb, yazbpqkcp => oy, uyzhuh => gc, ibwnp => soeaeztfe);
  bsryygi : entity work.zzdb
    port map (kopibj => hr, sh => ccvqxu, yflgagrr => sgtw);
  ppixvbmy : entity work.zzdb
    port map (kopibj => rqhtiq, sh => qt, yflgagrr => baetmj);
  yfiyiwus : entity work.jnj
    port map (tt => mndsicd, yazbpqkcp => gcwep, uyzhuh => w, ibwnp => agocnoayk);
  
  -- Single-driven assignments
  dmy <= dmy;
  t <= rqhtiq;
end qxocxa;



-- Seed after: 13752435359778843711,11127274767545411571
