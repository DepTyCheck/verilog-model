-- Seed: 18356844301300875642,14426950258250697445

use std.reflection.all;

entity reejmygl is
  port (xdz : linkage integer; zwrwohdytn : inout subtype_mirror; vhusons : buffer integer_vector(0 downto 0); qltvywnkg : linkage time);
end reejmygl;

architecture wqgeydeb of reejmygl is
  
begin
  -- Single-driven assignments
  vhusons <= (others => 16#32A3#);
end wqgeydeb;

use std.reflection.all;

entity dqnvzpt is
  port (ctclnd : out string(4 to 2); aespojqmyy : inout file_subtype_mirror; rtlcwn : linkage real);
end dqnvzpt;

use std.reflection.all;

architecture klwcfqr of dqnvzpt is
  signal jtac : time;
  signal mtdku : integer_vector(0 downto 0);
  shared variable rhpwhsui : subtype_mirror;
  signal hnskpqbkov : integer;
  signal fjljlzau : time;
  signal astahj : integer_vector(0 downto 0);
  shared variable evqvic : subtype_mirror;
  signal opymftzkph : integer;
begin
  dalq : entity work.reejmygl
    port map (xdz => opymftzkph, zwrwohdytn => evqvic, vhusons => astahj, qltvywnkg => fjljlzau);
  qxjndchcn : entity work.reejmygl
    port map (xdz => hnskpqbkov, zwrwohdytn => rhpwhsui, vhusons => mtdku, qltvywnkg => jtac);
  
  -- Single-driven assignments
  ctclnd <= "";
end klwcfqr;

entity auxztjly is
  port (gpxwwxhz : linkage bit);
end auxztjly;

use std.reflection.all;

architecture qlahlsnke of auxztjly is
  signal pqr : real;
  shared variable numqn : file_subtype_mirror;
  signal vt : string(4 to 2);
  signal qeew : time;
  signal zhpezfoxm : integer_vector(0 downto 0);
  shared variable o : subtype_mirror;
  signal qicr : integer;
begin
  dhypoxkt : entity work.reejmygl
    port map (xdz => qicr, zwrwohdytn => o, vhusons => zhpezfoxm, qltvywnkg => qeew);
  tas : entity work.dqnvzpt
    port map (ctclnd => vt, aespojqmyy => numqn, rtlcwn => pqr);
end qlahlsnke;



-- Seed after: 9995772359277333094,14426950258250697445
