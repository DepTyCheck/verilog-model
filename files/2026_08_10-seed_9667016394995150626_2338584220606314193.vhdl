-- Seed: 9667016394995150626,2338584220606314193

library ieee;
use ieee.std_logic_1164.all;

entity k is
  port (uckmeyykz : linkage real; fbkpcv : in time_vector(3 to 2); muraaixe : in string(1 downto 4); vqrupw : linkage std_logic);
end k;

architecture mj of k is
  
begin
  
end mj;

entity onhfxksdi is
  port (gsnqvu : in time_vector(3 to 3); iovu : inout real; qenmqwvgyy : in real; l : inout severity_level);
end onhfxksdi;

library ieee;
use ieee.std_logic_1164.all;

architecture hkiq of onhfxksdi is
  signal txrssrd : std_logic;
  signal wp : string(1 downto 4);
  signal fzlxpa : time_vector(3 to 2);
  signal nvcyvxojmg : real;
  signal rpjbrgc : time_vector(3 to 2);
  signal v : real;
  signal j : std_logic;
  signal xtq : string(1 downto 4);
  signal jiffosiv : time_vector(3 to 2);
begin
  an : entity work.k
    port map (uckmeyykz => iovu, fbkpcv => jiffosiv, muraaixe => xtq, vqrupw => j);
  cubrqz : entity work.k
    port map (uckmeyykz => v, fbkpcv => rpjbrgc, muraaixe => xtq, vqrupw => j);
  imunnieql : entity work.k
    port map (uckmeyykz => nvcyvxojmg, fbkpcv => fzlxpa, muraaixe => wp, vqrupw => txrssrd);
  
  -- Single-driven assignments
  wp <= xtq;
  jiffosiv <= jiffosiv;
  l <= FAILURE;
  
  -- Multi-driven assignments
  j <= j;
  j <= '1';
  txrssrd <= '1';
  j <= 'X';
end hkiq;

library ieee;
use ieee.std_logic_1164.all;

entity rnx is
  port (m : linkage std_logic_vector(3 to 4));
end rnx;

architecture npiylqi of rnx is
  
begin
  
end npiylqi;

entity lw is
  port (a : in integer; vrsxotlvjh : in integer; bjduerd : linkage time_vector(2 to 4));
end lw;

library ieee;
use ieee.std_logic_1164.all;

architecture pqjmoxfj of lw is
  signal rmragik : std_logic;
  signal hssdlers : string(1 downto 4);
  signal zvxf : time_vector(3 to 2);
  signal n : real;
begin
  hsibgbqsl : entity work.k
    port map (uckmeyykz => n, fbkpcv => zvxf, muraaixe => hssdlers, vqrupw => rmragik);
  
  -- Single-driven assignments
  zvxf <= zvxf;
  hssdlers <= hssdlers;
  
  -- Multi-driven assignments
  rmragik <= 'H';
end pqjmoxfj;



-- Seed after: 8851919755302316908,2338584220606314193
