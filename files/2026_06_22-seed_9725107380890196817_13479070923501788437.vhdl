-- Seed: 9725107380890196817,13479070923501788437

entity dyniluq is
  port (xrsfjy : inout real; ftvn : out real; o : inout bit; rmgpcjp : linkage time);
end dyniluq;

architecture unvljhmesi of dyniluq is
  
begin
  
end unvljhmesi;

entity wyndnguv is
  port (dnfppwqs : in bit);
end wyndnguv;

architecture t of wyndnguv is
  signal vqqoruv : time;
  signal lukvwqv : bit;
  signal hdbfc : real;
  signal dtqy : real;
  signal snkl : time;
  signal rhs : bit;
  signal azhs : real;
  signal ezexbka : real;
  signal gpqperqsn : time;
  signal dhjylooane : bit;
  signal dtmmpyeeuq : real;
  signal umhwct : real;
begin
  sfnfms : entity work.dyniluq
    port map (xrsfjy => umhwct, ftvn => dtmmpyeeuq, o => dhjylooane, rmgpcjp => gpqperqsn);
  ekosqj : entity work.dyniluq
    port map (xrsfjy => ezexbka, ftvn => azhs, o => rhs, rmgpcjp => snkl);
  vsvtme : entity work.dyniluq
    port map (xrsfjy => dtqy, ftvn => hdbfc, o => lukvwqv, rmgpcjp => vqqoruv);
end t;

entity zfx is
  port (oxjaoceu : inout integer; hxuhdzd : linkage real; uuhdpt : buffer time);
end zfx;

architecture ster of zfx is
  signal rp : bit;
  signal bc : real;
  signal ms : real;
  signal nwshxm : time;
  signal uuknuf : bit;
  signal kuzh : real;
  signal lk : real;
  signal h : time;
  signal ix : bit;
  signal qsekqaysiv : real;
  signal iaxg : real;
begin
  lnsjl : entity work.dyniluq
    port map (xrsfjy => iaxg, ftvn => qsekqaysiv, o => ix, rmgpcjp => h);
  kpmrzk : entity work.dyniluq
    port map (xrsfjy => lk, ftvn => kuzh, o => uuknuf, rmgpcjp => nwshxm);
  heqeoifl : entity work.dyniluq
    port map (xrsfjy => ms, ftvn => bc, o => rp, rmgpcjp => uuhdpt);
  
  -- Single-driven assignments
  oxjaoceu <= 30232;
end ster;

entity bmyabuvm is
  port (k : inout integer_vector(0 downto 4));
end bmyabuvm;

architecture sfud of bmyabuvm is
  
begin
  -- Single-driven assignments
  k <= (others => 0);
end sfud;



-- Seed after: 7979364881789443527,13479070923501788437
