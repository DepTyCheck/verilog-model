-- Seed: 16375355563198578807,4122021602305298647

library ieee;
use ieee.std_logic_1164.all;

entity cy is
  port (hkzpwom : linkage real; ak : buffer integer_vector(2 to 2); cfhemldw : buffer time; sgy : linkage std_logic_vector(0 to 4));
end cy;

architecture sfz of cy is
  
begin
  -- Single-driven assignments
  cfhemldw <= 2 sec;
  ak <= ak;
end sfz;

entity zja is
  port (njqlafkyf : inout integer);
end zja;

library ieee;
use ieee.std_logic_1164.all;

architecture dfihqfo of zja is
  signal uwvn : time;
  signal dt : integer_vector(2 to 2);
  signal xbphppk : real;
  signal cijndv : std_logic_vector(0 to 4);
  signal hlkpluu : time;
  signal nsc : integer_vector(2 to 2);
  signal c : real;
begin
  iojf : entity work.cy
    port map (hkzpwom => c, ak => nsc, cfhemldw => hlkpluu, sgy => cijndv);
  kkids : entity work.cy
    port map (hkzpwom => xbphppk, ak => dt, cfhemldw => uwvn, sgy => cijndv);
  
  -- Single-driven assignments
  njqlafkyf <= 2#11#;
end dfihqfo;



-- Seed after: 7103871998041197315,4122021602305298647
