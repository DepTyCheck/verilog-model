-- Seed: 11316060471835337984,662889661651915549

entity s is
  port (nmwwmfu : linkage time; cjo : linkage bit);
end s;

architecture prqmmsnze of s is
  
begin
  
end prqmmsnze;

library ieee;
use ieee.std_logic_1164.all;

entity at is
  port (jmg : linkage std_logic_vector(2 to 2));
end at;

architecture xnkvh of at is
  signal zmzqdklpz : bit;
  signal fbqrdeb : time;
  signal zognum : bit;
  signal qomltgpx : time;
  signal lsadjzc : bit;
  signal ykltskaquw : time;
begin
  k : entity work.s
    port map (nmwwmfu => ykltskaquw, cjo => lsadjzc);
  lvmup : entity work.s
    port map (nmwwmfu => qomltgpx, cjo => zognum);
  vl : entity work.s
    port map (nmwwmfu => fbqrdeb, cjo => zmzqdklpz);
end xnkvh;

library ieee;
use ieee.std_logic_1164.all;

entity jbv is
  port (mu : linkage std_logic; evwb : buffer bit_vector(4 to 1); dhjs : out boolean);
end jbv;

architecture f of jbv is
  signal sseafewgx : bit;
  signal b : time;
  signal afvpofnmle : bit;
  signal xesfpzqgip : time;
  signal qqrxnewc : bit;
  signal hv : time;
begin
  gluw : entity work.s
    port map (nmwwmfu => hv, cjo => qqrxnewc);
  aqrtodnn : entity work.s
    port map (nmwwmfu => xesfpzqgip, cjo => afvpofnmle);
  d : entity work.s
    port map (nmwwmfu => b, cjo => sseafewgx);
  
  -- Single-driven assignments
  dhjs <= dhjs;
  evwb <= (others => '0');
end f;

entity qywtaw is
  port (hv : in real; kfbm : out bit);
end qywtaw;

architecture bd of qywtaw is
  signal erbk : bit;
  signal eda : time;
begin
  yndfqsgeh : entity work.s
    port map (nmwwmfu => eda, cjo => erbk);
  
  -- Single-driven assignments
  kfbm <= kfbm;
end bd;



-- Seed after: 10322298899446005752,662889661651915549
