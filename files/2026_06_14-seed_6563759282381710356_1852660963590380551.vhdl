-- Seed: 6563759282381710356,1852660963590380551

library ieee;
use ieee.std_logic_1164.all;

entity nzhxepoer is
  port (nmeszu : buffer time; cig : out real; oz : in std_logic_vector(2 to 0));
end nzhxepoer;



architecture nya of nzhxepoer is
  
begin
  
end nya;

library ieee;
use ieee.std_logic_1164.all;

entity rddpp is
  port (zdoqqfsorn : out time_vector(0 downto 2); rhttj : inout real_vector(1 downto 3); adubk : in std_logic_vector(2 to 2));
end rddpp;

library ieee;
use ieee.std_logic_1164.all;

architecture oow of rddpp is
  signal rq : std_logic_vector(2 to 0);
  signal alqrx : real;
  signal oiftyle : time;
  signal erditnejz : std_logic_vector(2 to 0);
  signal cyfaewklba : real;
  signal hl : time;
begin
  nlxdp : entity work.nzhxepoer
    port map (nmeszu => hl, cig => cyfaewklba, oz => erditnejz);
  okgeqjuk : entity work.nzhxepoer
    port map (nmeszu => oiftyle, cig => alqrx, oz => rq);
end oow;



-- Seed after: 5417436535161720709,1852660963590380551
