-- Seed: 5244073670804574144,10557070023141912087

entity yett is
  port (iz : linkage real; glrdcipmu : out time_vector(1 to 2); sov : inout real);
end yett;

architecture ibqlgj of yett is
  
begin
  -- Single-driven assignments
  sov <= 1.4312;
end ibqlgj;

library ieee;
use ieee.std_logic_1164.all;

entity r is
  port (zx : inout integer; xfsno : linkage std_logic_vector(2 downto 0); ouuv : in integer; q : inout real);
end r;

architecture mxemqtouq of r is
  signal hdrddtnd : time_vector(1 to 2);
  signal impuxtkvck : real;
  signal vhyr : real;
  signal nckv : time_vector(1 to 2);
  signal pxki : real;
begin
  wonrhu : entity work.yett
    port map (iz => pxki, glrdcipmu => nckv, sov => vhyr);
  dyj : entity work.yett
    port map (iz => impuxtkvck, glrdcipmu => hdrddtnd, sov => q);
  
  -- Single-driven assignments
  zx <= 2_4_3_3_2;
end mxemqtouq;

library ieee;
use ieee.std_logic_1164.all;

entity cwq is
  port (j : in string(2 downto 1); ab : inout std_logic_vector(1 downto 3));
end cwq;

library ieee;
use ieee.std_logic_1164.all;

architecture uu of cwq is
  signal ku : real;
  signal ggieny : time_vector(1 to 2);
  signal iclv : real;
  signal hw : real;
  signal skkl : time_vector(1 to 2);
  signal iwvftebu : real;
  signal ijogtluasc : real;
  signal ztwbl : time_vector(1 to 2);
  signal wf : real;
  signal eto : real;
  signal phhhklxphq : std_logic_vector(2 downto 0);
  signal fcv : integer;
begin
  fsvtoeudxn : entity work.r
    port map (zx => fcv, xfsno => phhhklxphq, ouuv => fcv, q => eto);
  hcm : entity work.yett
    port map (iz => wf, glrdcipmu => ztwbl, sov => ijogtluasc);
  brmg : entity work.yett
    port map (iz => iwvftebu, glrdcipmu => skkl, sov => hw);
  wpc : entity work.yett
    port map (iz => iclv, glrdcipmu => ggieny, sov => ku);
  
  -- Multi-driven assignments
  ab <= "";
  ab <= "";
  ab <= "";
end uu;



-- Seed after: 3145021173033838537,10557070023141912087
