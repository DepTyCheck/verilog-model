-- Seed: 12439746010131478917,14141408946471626091

entity kqnphz is
  port (ysbmpv : inout real; sidgr : out time; lwtjfm : inout severity_level; us : linkage real);
end kqnphz;

architecture b of kqnphz is
  
begin
  -- Single-driven assignments
  lwtjfm <= WARNING;
end b;

entity ktdoxsv is
  port (figraiv : buffer integer; rhlpymi : inout string(2 to 4); nxnlcluj : out time);
end ktdoxsv;

architecture akmm of ktdoxsv is
  signal rnd : real;
  signal g : severity_level;
  signal gqpibyrwbw : real;
  signal uxzpw : real;
  signal ur : severity_level;
  signal wcfhnphbv : time;
  signal q : real;
begin
  rknoeokn : entity work.kqnphz
    port map (ysbmpv => q, sidgr => wcfhnphbv, lwtjfm => ur, us => uxzpw);
  zlvbss : entity work.kqnphz
    port map (ysbmpv => gqpibyrwbw, sidgr => nxnlcluj, lwtjfm => g, us => rnd);
  
  -- Single-driven assignments
  rhlpymi <= rhlpymi;
  figraiv <= 16#12FB#;
end akmm;

entity r is
  port (efvm : out integer; qe : buffer bit);
end r;

architecture ephffllp of r is
  signal xpocnam : real;
  signal pnbhviiqvo : severity_level;
  signal mmjrjgkyvz : time;
  signal bexhxsrp : real;
  signal ltdulldfh : real;
  signal vc : severity_level;
  signal u : time;
  signal kfeys : real;
  signal yvu : time;
  signal nxtkuoifrz : string(2 to 4);
  signal dkvcgdex : integer;
  signal aabyvkp : real;
  signal dbbseimm : severity_level;
  signal wqoquk : time;
  signal lbis : real;
begin
  z : entity work.kqnphz
    port map (ysbmpv => lbis, sidgr => wqoquk, lwtjfm => dbbseimm, us => aabyvkp);
  vmooicknt : entity work.ktdoxsv
    port map (figraiv => dkvcgdex, rhlpymi => nxtkuoifrz, nxnlcluj => yvu);
  zlrgtd : entity work.kqnphz
    port map (ysbmpv => kfeys, sidgr => u, lwtjfm => vc, us => ltdulldfh);
  j : entity work.kqnphz
    port map (ysbmpv => bexhxsrp, sidgr => mmjrjgkyvz, lwtjfm => pnbhviiqvo, us => xpocnam);
  
  -- Single-driven assignments
  qe <= qe;
end ephffllp;



-- Seed after: 7780914591561937015,14141408946471626091
