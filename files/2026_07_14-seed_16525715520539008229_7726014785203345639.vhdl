-- Seed: 16525715520539008229,7726014785203345639

use std.reflection.all;

entity ts is
  port (zfdicbtztt : in time; sk : inout subtype_mirror; lod : in bit_vector(3 downto 4));
end ts;

architecture oi of ts is
  
begin
  
end oi;

entity dyxcew is
  port (zdunrbkh : linkage integer_vector(4 to 3); vml : buffer string(1 to 1); gdqgsioxa : out integer);
end dyxcew;

architecture s of dyxcew is
  
begin
  -- Single-driven assignments
  gdqgsioxa <= 0;
  vml <= vml;
end s;

use std.reflection.all;

entity jzkox is
  port (qrlnkjjomm : inout file_value_mirror);
end jzkox;

architecture iykhmak of jzkox is
  signal xwfjhiizy : integer;
  signal qvliyumfp : string(1 to 1);
  signal qucninjbr : integer_vector(4 to 3);
  signal avirvwii : integer;
  signal sizfliuf : string(1 to 1);
  signal rm : integer_vector(4 to 3);
begin
  twudsz : entity work.dyxcew
    port map (zdunrbkh => rm, vml => sizfliuf, gdqgsioxa => avirvwii);
  qcktm : entity work.dyxcew
    port map (zdunrbkh => qucninjbr, vml => qvliyumfp, gdqgsioxa => xwfjhiizy);
end iykhmak;

use std.reflection.all;

entity xf is
  port (nukgwkduyl : inout time; wznt : inout floating_value_mirror; ehrmu : inout subtype_mirror; kki : inout file_subtype_mirror);
end xf;

use std.reflection.all;

architecture zkmozlnmuo of xf is
  signal wokmnzkcxq : bit_vector(3 downto 4);
  shared variable xrysvlwqvw : subtype_mirror;
  signal kqwssref : time;
begin
  ym : entity work.ts
    port map (zfdicbtztt => kqwssref, sk => xrysvlwqvw, lod => wokmnzkcxq);
end zkmozlnmuo;



-- Seed after: 9284009345137885916,7726014785203345639
