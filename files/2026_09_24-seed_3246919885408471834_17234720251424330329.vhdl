-- Seed: 3246919885408471834,17234720251424330329

library ieee;
use ieee.std_logic_1164.all;

entity mzd is
  port (zoyhxszsz : buffer severity_level; upuevenck : inout std_logic_vector(0 to 1); wprewag : out integer);
end mzd;

architecture pkxv of mzd is
  
begin
  -- Single-driven assignments
  wprewag <= 34;
  zoyhxszsz <= zoyhxszsz;
  
  -- Multi-driven assignments
  upuevenck <= upuevenck;
  upuevenck <= upuevenck;
  upuevenck <= "-H";
end pkxv;

entity epa is
  port (pxovws : inout real; vjsdtvg : buffer real_vector(4 downto 3); b : buffer time);
end epa;

library ieee;
use ieee.std_logic_1164.all;

architecture apjtarzzpi of epa is
  signal eg : integer;
  signal eyzcptl : std_logic_vector(0 to 1);
  signal ewtq : severity_level;
  signal sgguunb : integer;
  signal cyczmiause : severity_level;
  signal vrojsuil : integer;
  signal vignybzgi : std_logic_vector(0 to 1);
  signal x : severity_level;
begin
  dardbo : entity work.mzd
    port map (zoyhxszsz => x, upuevenck => vignybzgi, wprewag => vrojsuil);
  vywe : entity work.mzd
    port map (zoyhxszsz => cyczmiause, upuevenck => vignybzgi, wprewag => sgguunb);
  spvtv : entity work.mzd
    port map (zoyhxszsz => ewtq, upuevenck => eyzcptl, wprewag => eg);
  
  -- Multi-driven assignments
  vignybzgi <= "Z0";
  vignybzgi <= eyzcptl;
  vignybzgi <= vignybzgi;
end apjtarzzpi;

library ieee;
use ieee.std_logic_1164.all;

entity xcaahqhifb is
  port (ecealk : inout std_logic_vector(3 to 4); n : out std_logic; sqiucd : out bit_vector(1 to 0));
end xcaahqhifb;

architecture cdizgn of xcaahqhifb is
  
begin
  -- Single-driven assignments
  sqiucd <= (others => '0');
  
  -- Multi-driven assignments
  n <= n;
  n <= n;
  ecealk <= ('L', '1');
  ecealk <= ('X', 'W');
end cdizgn;



-- Seed after: 1134485783836148441,17234720251424330329
