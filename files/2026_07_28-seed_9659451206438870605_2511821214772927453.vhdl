-- Seed: 9659451206438870605,2511821214772927453

library ieee;
use ieee.std_logic_1164.all;

entity xpbndwtar is
  port (wn : out real_vector(1 downto 0); o : linkage std_logic_vector(4 to 2));
end xpbndwtar;

architecture azcd of xpbndwtar is
  
begin
  -- Single-driven assignments
  wn <= wn;
end azcd;

entity vcok is
  port (lqjpo : inout character; pyvbs : out time; ulfrhpanze : inout real; fekqzi : inout boolean_vector(3 to 1));
end vcok;

library ieee;
use ieee.std_logic_1164.all;

architecture enupo of vcok is
  signal yfox : real_vector(1 downto 0);
  signal urvallgukt : std_logic_vector(4 to 2);
  signal m : real_vector(1 downto 0);
  signal htzug : std_logic_vector(4 to 2);
  signal ljonquxqva : real_vector(1 downto 0);
begin
  psypbhnj : entity work.xpbndwtar
    port map (wn => ljonquxqva, o => htzug);
  komgrkcsad : entity work.xpbndwtar
    port map (wn => m, o => urvallgukt);
  z : entity work.xpbndwtar
    port map (wn => yfox, o => htzug);
end enupo;

entity xqocpebmk is
  port (qludn : in boolean_vector(2 downto 2));
end xqocpebmk;

library ieee;
use ieee.std_logic_1164.all;

architecture wuzvxr of xqocpebmk is
  signal ok : std_logic_vector(4 to 2);
  signal mjbvs : real_vector(1 downto 0);
begin
  ei : entity work.xpbndwtar
    port map (wn => mjbvs, o => ok);
  
  -- Multi-driven assignments
  ok <= ok;
  ok <= ok;
end wuzvxr;

entity uwp is
  port (zemle : in integer; ygmrrqjzkm : in time; yaqt : linkage boolean; c : inout severity_level);
end uwp;

library ieee;
use ieee.std_logic_1164.all;

architecture vh of uwp is
  signal lfpsbc : real_vector(1 downto 0);
  signal adgxzwi : real_vector(1 downto 0);
  signal ubaqw : std_logic_vector(4 to 2);
  signal dzinourszw : real_vector(1 downto 0);
begin
  xm : entity work.xpbndwtar
    port map (wn => dzinourszw, o => ubaqw);
  sowuyltsfb : entity work.xpbndwtar
    port map (wn => adgxzwi, o => ubaqw);
  ulejhzdc : entity work.xpbndwtar
    port map (wn => lfpsbc, o => ubaqw);
end vh;



-- Seed after: 1313094687580927376,2511821214772927453
