-- Seed: 1373770512126799171,14094562573555574003

entity lj is
  port (ta : in severity_level);
end lj;

architecture qob of lj is
  
begin
  
end qob;

entity kcs is
  port (wvymtnxql : in time; ey : inout time; svj : in string(1 downto 4));
end kcs;

architecture mkrg of kcs is
  signal wx : severity_level;
  signal sfpf : severity_level;
begin
  olkqduggk : entity work.lj
    port map (ta => sfpf);
  uyb : entity work.lj
    port map (ta => wx);
  fmsy : entity work.lj
    port map (ta => sfpf);
  
  -- Single-driven assignments
  sfpf <= NOTE;
  wx <= ERROR;
  ey <= ey;
end mkrg;

entity puby is
  port (rvsrap : inout bit_vector(2 downto 0); drebivry : in time; s : inout real; cst : out time);
end puby;

architecture ecioijsad of puby is
  signal xbvl : string(1 downto 4);
  signal o : time;
  signal zz : time;
  signal pnbgpkdxwl : severity_level;
begin
  amama : entity work.lj
    port map (ta => pnbgpkdxwl);
  hplxbv : entity work.kcs
    port map (wvymtnxql => zz, ey => o, svj => xbvl);
  
  -- Single-driven assignments
  xbvl <= "";
end ecioijsad;



-- Seed after: 3109458045772826860,14094562573555574003
