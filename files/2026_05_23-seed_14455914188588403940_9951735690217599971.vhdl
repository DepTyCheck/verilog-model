-- Seed: 14455914188588403940,9951735690217599971



entity wbzor is
  port (ism : inout time; ioey : in integer; bb : in severity_level; fpbumi : buffer real);
end wbzor;



architecture bmvlz of wbzor is
  
begin
  
end bmvlz;

library ieee;
use ieee.std_logic_1164.all;

entity g is
  port (froech : linkage time; p : buffer time; iuxsoburh : in integer; zymux : in std_logic_vector(2 downto 2));
end g;



architecture bp of g is
  signal wqnpixry : real;
  signal yueli : severity_level;
  signal yn : integer;
  signal fnsnqxyrkj : time;
begin
  jljaiyqn : entity work.wbzor
    port map (ism => fnsnqxyrkj, ioey => yn, bb => yueli, fpbumi => wqnpixry);
end bp;

library ieee;
use ieee.std_logic_1164.all;

entity svz is
  port (kcgt : inout std_logic; czinzzgms : linkage time; ziejj : in bit);
end svz;

library ieee;
use ieee.std_logic_1164.all;

architecture mjsogo of svz is
  signal zpfisovl : integer;
  signal ubjbldcdv : time;
  signal wlxtvkm : time;
  signal hbp : std_logic_vector(2 downto 2);
  signal lxab : time;
  signal nmo : real;
  signal gzpevvhgjk : severity_level;
  signal vne : time;
  signal kkdyblwd : real;
  signal jilmnk : severity_level;
  signal wwuumleig : integer;
  signal fexphs : time;
begin
  fe : entity work.wbzor
    port map (ism => fexphs, ioey => wwuumleig, bb => jilmnk, fpbumi => kkdyblwd);
  xtdbgcsbo : entity work.wbzor
    port map (ism => vne, ioey => wwuumleig, bb => gzpevvhgjk, fpbumi => nmo);
  v : entity work.g
    port map (froech => lxab, p => lxab, iuxsoburh => wwuumleig, zymux => hbp);
  ptybffqhok : entity work.g
    port map (froech => wlxtvkm, p => ubjbldcdv, iuxsoburh => zpfisovl, zymux => hbp);
end mjsogo;



-- Seed after: 5936883901847087189,9951735690217599971
