-- Seed: 14610447119571490908,3181554006726329157

use std.reflection.all;

entity ytzm is
  port (vvl : inout time_vector(0 to 2); wrfe : linkage character; vjwkbrly : inout protected_value_mirror; rddvfp : linkage real_vector(2 to 1));
end ytzm;

architecture xfum of ytzm is
  
begin
  -- Single-driven assignments
  vvl <= vvl;
end xfum;

use std.reflection.all;

entity epfsnim is
  port (tccp : inout physical_subtype_mirror);
end epfsnim;

use std.reflection.all;

architecture oggko of epfsnim is
  signal pobanp : real_vector(2 to 1);
  shared variable ckhdyuwvm : protected_value_mirror;
  signal xk : character;
  signal m : time_vector(0 to 2);
begin
  jtqsqqbzj : entity work.ytzm
    port map (vvl => m, wrfe => xk, vjwkbrly => ckhdyuwvm, rddvfp => pobanp);
end oggko;

library ieee;
use ieee.std_logic_1164.all;

entity j is
  port (efmr : buffer integer; unbywnn : in std_logic; tptybvjsp : linkage integer; u : out integer);
end j;

use std.reflection.all;

architecture lslzf of j is
  signal drhrjku : real_vector(2 to 1);
  shared variable mtk : protected_value_mirror;
  signal ulg : character;
  signal g : time_vector(0 to 2);
  signal r : real_vector(2 to 1);
  shared variable lxwo : protected_value_mirror;
  signal npabr : character;
  signal oiwwpe : time_vector(0 to 2);
  signal zlkh : real_vector(2 to 1);
  shared variable rnt : protected_value_mirror;
  signal jyld : character;
  signal vwhxkkuy : time_vector(0 to 2);
begin
  hkih : entity work.ytzm
    port map (vvl => vwhxkkuy, wrfe => jyld, vjwkbrly => rnt, rddvfp => zlkh);
  lnech : entity work.ytzm
    port map (vvl => oiwwpe, wrfe => npabr, vjwkbrly => lxwo, rddvfp => r);
  tzap : entity work.ytzm
    port map (vvl => g, wrfe => ulg, vjwkbrly => mtk, rddvfp => drhrjku);
  
  -- Single-driven assignments
  u <= 16#2B4#;
end lslzf;



-- Seed after: 16695029875198336697,3181554006726329157
