-- Seed: 14676686305766400457,6697892553037813751

entity b is
  port (hnb : inout boolean_vector(1 downto 1); nasdfgodv : out time; aiobkom : out boolean_vector(3 downto 1));
end b;

architecture zoshgl of b is
  
begin
  
end zoshgl;

entity orq is
  port (wei : buffer severity_level);
end orq;

architecture cmdbc of orq is
  
begin
  -- Single-driven assignments
  wei <= FAILURE;
end cmdbc;

entity bkmg is
  port (bwn : in integer_vector(0 downto 1); weko : out bit);
end bkmg;

architecture x of bkmg is
  signal oz : severity_level;
  signal pdxviwtke : boolean_vector(3 downto 1);
  signal q : time;
  signal profyozv : boolean_vector(1 downto 1);
  signal vagu : severity_level;
  signal vmnooszrli : boolean_vector(3 downto 1);
  signal sjylpfcx : time;
  signal beogogzx : boolean_vector(1 downto 1);
begin
  dqicaqpevv : entity work.b
    port map (hnb => beogogzx, nasdfgodv => sjylpfcx, aiobkom => vmnooszrli);
  vblp : entity work.orq
    port map (wei => vagu);
  kvdde : entity work.b
    port map (hnb => profyozv, nasdfgodv => q, aiobkom => pdxviwtke);
  rzlnc : entity work.orq
    port map (wei => oz);
  
  -- Single-driven assignments
  weko <= '1';
end x;

library ieee;
use ieee.std_logic_1164.all;

entity purlmawp is
  port (zgjzj : in std_logic_vector(4 downto 0));
end purlmawp;

architecture pwpo of purlmawp is
  
begin
  
end pwpo;



-- Seed after: 1321711286847315897,6697892553037813751
