-- Seed: 6403898380622029441,14426950258250697445

use std.reflection.all;

entity rtuosbp is
  port (zwpsnobbnp : in time; xbnq : linkage integer; vclpmv : inout access_subtype_mirror; xmfycts : buffer time_vector(2 downto 1));
end rtuosbp;

architecture as of rtuosbp is
  
begin
  
end as;

use std.reflection.all;

entity e is
  port (dw : inout protected_value_mirror; ygropi : inout subtype_mirror; pymafdy : buffer character; pprxzoincm : inout protected_value_mirror);
end e;

use std.reflection.all;

architecture biil of e is
  signal ifwdal : time_vector(2 downto 1);
  shared variable c : access_subtype_mirror;
  signal nla : integer;
  signal prqjillnn : time_vector(2 downto 1);
  shared variable ngpf : access_subtype_mirror;
  signal inkcbliv : integer;
  signal aas : time;
begin
  etyqj : entity work.rtuosbp
    port map (zwpsnobbnp => aas, xbnq => inkcbliv, vclpmv => ngpf, xmfycts => prqjillnn);
  yzn : entity work.rtuosbp
    port map (zwpsnobbnp => aas, xbnq => nla, vclpmv => c, xmfycts => ifwdal);
end biil;



-- Seed after: 5859084494489643431,14426950258250697445
