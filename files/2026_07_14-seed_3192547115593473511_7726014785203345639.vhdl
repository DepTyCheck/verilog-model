-- Seed: 3192547115593473511,7726014785203345639

library ieee;
use ieee.std_logic_1164.all;

entity mqyrb is
  port (ehna : inout std_logic);
end mqyrb;

architecture lman of mqyrb is
  
begin
  -- Multi-driven assignments
  ehna <= ehna;
  ehna <= 'X';
end lman;

use std.reflection.all;

entity h is
  port (ehmngfe : inout file_value_mirror);
end h;

library ieee;
use ieee.std_logic_1164.all;

architecture vng of h is
  signal jxvrxmfr : std_logic;
  signal hw : std_logic;
begin
  ksleoru : entity work.mqyrb
    port map (ehna => hw);
  qbt : entity work.mqyrb
    port map (ehna => hw);
  c : entity work.mqyrb
    port map (ehna => hw);
  hxwy : entity work.mqyrb
    port map (ehna => jxvrxmfr);
  
  -- Multi-driven assignments
  hw <= hw;
  hw <= 'U';
end vng;

entity halbmipur is
  port (lz : inout boolean);
end halbmipur;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture rjbtilgknb of halbmipur is
  signal rv : std_logic;
  shared variable plsdhfhtw : file_value_mirror;
  shared variable poikkx : file_value_mirror;
begin
  ndavuowzf : entity work.h
    port map (ehmngfe => poikkx);
  do : entity work.h
    port map (ehmngfe => plsdhfhtw);
  r : entity work.mqyrb
    port map (ehna => rv);
  
  -- Single-driven assignments
  lz <= lz;
  
  -- Multi-driven assignments
  rv <= rv;
end rjbtilgknb;



-- Seed after: 6244432913192966920,7726014785203345639
