-- Seed: 12446817898164810408,11127274767545411571

library ieee;
use ieee.std_logic_1164.all;

entity fsivemc is
  port (wh : buffer bit_vector(2 downto 1); eeg : linkage real; pq : inout std_logic_vector(1 downto 0); sml : buffer real_vector(4 to 3));
end fsivemc;

architecture fbhe of fsivemc is
  
begin
  -- Single-driven assignments
  wh <= wh;
  sml <= (others => 0.0);
  
  -- Multi-driven assignments
  pq <= ('-', '-');
  pq <= pq;
  pq <= pq;
  pq <= pq;
end fbhe;

library ieee;
use ieee.std_logic_1164.all;

entity lgbb is
  port (it : in std_logic; cbszd : in std_logic);
end lgbb;

library ieee;
use ieee.std_logic_1164.all;

architecture knqku of lgbb is
  signal hgq : real_vector(4 to 3);
  signal ejy : std_logic_vector(1 downto 0);
  signal tfzcifk : real;
  signal cvgwmelote : bit_vector(2 downto 1);
  signal zl : real_vector(4 to 3);
  signal leddyfdbc : std_logic_vector(1 downto 0);
  signal bc : real;
  signal ynsjkla : bit_vector(2 downto 1);
  signal jgnerm : real_vector(4 to 3);
  signal s : std_logic_vector(1 downto 0);
  signal fwmiicx : real;
  signal z : bit_vector(2 downto 1);
  signal uck : real_vector(4 to 3);
  signal bxpjtngs : std_logic_vector(1 downto 0);
  signal nxbyakbzaw : real;
  signal hlkg : bit_vector(2 downto 1);
begin
  jkhyzzlixs : entity work.fsivemc
    port map (wh => hlkg, eeg => nxbyakbzaw, pq => bxpjtngs, sml => uck);
  bqdaqe : entity work.fsivemc
    port map (wh => z, eeg => fwmiicx, pq => s, sml => jgnerm);
  puv : entity work.fsivemc
    port map (wh => ynsjkla, eeg => bc, pq => leddyfdbc, sml => zl);
  v : entity work.fsivemc
    port map (wh => cvgwmelote, eeg => tfzcifk, pq => ejy, sml => hgq);
end knqku;



-- Seed after: 2265540787833722260,11127274767545411571
