-- Seed: 137536568265380251,7142793346053417159



entity jsmhqncp is
  port (rzbczr : linkage real; o : buffer bit; ltiz : linkage real; mzqdpe : out real);
end jsmhqncp;



architecture ejaboes of jsmhqncp is
  
begin
  
end ejaboes;



entity yxnr is
  port (nawapk : linkage boolean);
end yxnr;



architecture nguj of yxnr is
  
begin
  
end nguj;

library ieee;
use ieee.std_logic_1164.all;

entity iauvmlwceh is
  port (ucrvp : inout std_logic_vector(0 to 4));
end iauvmlwceh;



architecture owdyf of iauvmlwceh is
  signal zblk : boolean;
  signal rjk : real;
  signal whwogd : bit;
  signal zfifem : real;
  signal qdn : boolean;
  signal vapbdp : real;
  signal mvvnkhpx : bit;
  signal rv : real;
begin
  ifcfsr : entity work.jsmhqncp
    port map (rzbczr => rv, o => mvvnkhpx, ltiz => rv, mzqdpe => vapbdp);
  ooiokmih : entity work.yxnr
    port map (nawapk => qdn);
  ztob : entity work.jsmhqncp
    port map (rzbczr => zfifem, o => whwogd, ltiz => rjk, mzqdpe => rv);
  ifaia : entity work.yxnr
    port map (nawapk => zblk);
end owdyf;



entity qrddh is
  port (fzgpd : inout boolean; ll : out time);
end qrddh;



architecture rvzwxpecf of qrddh is
  signal j : bit;
  signal oxuxywizv : real;
  signal bokqkrpok : real;
  signal nixxi : real;
  signal wcui : bit;
  signal xic : real;
  signal ed : real;
  signal jlkzs : bit;
  signal r : real;
begin
  plvq : entity work.jsmhqncp
    port map (rzbczr => r, o => jlkzs, ltiz => ed, mzqdpe => r);
  a : entity work.jsmhqncp
    port map (rzbczr => xic, o => wcui, ltiz => nixxi, mzqdpe => bokqkrpok);
  biuqtboufv : entity work.yxnr
    port map (nawapk => fzgpd);
  aqojhx : entity work.jsmhqncp
    port map (rzbczr => oxuxywizv, o => j, ltiz => r, mzqdpe => ed);
end rvzwxpecf;



-- Seed after: 10393480895268177168,7142793346053417159
