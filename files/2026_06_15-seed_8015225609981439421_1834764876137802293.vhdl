-- Seed: 8015225609981439421,1834764876137802293

library ieee;
use ieee.std_logic_1164.all;

entity ujsxrzqgy is
  port (g : out real; dyauoomn : linkage std_logic_vector(4 to 4); xibqda : inout std_logic);
end ujsxrzqgy;

architecture fqpxdk of ujsxrzqgy is
  
begin
  -- Single-driven assignments
  g <= 2#1_1.00111#;
  
  -- Multi-driven assignments
  xibqda <= 'H';
  xibqda <= '1';
  xibqda <= '1';
  xibqda <= 'W';
end fqpxdk;

library ieee;
use ieee.std_logic_1164.all;

entity rgylokr is
  port (fw : in std_logic_vector(4 downto 2); wkrcdp : out time; svbqcwotcs : linkage std_logic; rrvz : buffer real);
end rgylokr;

library ieee;
use ieee.std_logic_1164.all;

architecture ls of rgylokr is
  signal psg : std_logic;
  signal m : std_logic_vector(4 to 4);
  signal pz : real;
  signal jpasqkd : std_logic;
  signal wpdpueb : real;
  signal juzhnf : std_logic;
  signal rgglshysre : std_logic_vector(4 to 4);
  signal jwqqbrwv : real;
begin
  b : entity work.ujsxrzqgy
    port map (g => jwqqbrwv, dyauoomn => rgglshysre, xibqda => juzhnf);
  clivzifkbl : entity work.ujsxrzqgy
    port map (g => wpdpueb, dyauoomn => rgglshysre, xibqda => juzhnf);
  ckfkuqrafu : entity work.ujsxrzqgy
    port map (g => rrvz, dyauoomn => rgglshysre, xibqda => jpasqkd);
  uxduvxsj : entity work.ujsxrzqgy
    port map (g => pz, dyauoomn => m, xibqda => psg);
  
  -- Single-driven assignments
  wkrcdp <= 0_3_4_0 ps;
  
  -- Multi-driven assignments
  rgglshysre <= "-";
  psg <= 'U';
  jpasqkd <= '0';
  rgglshysre <= (others => '0');
end ls;

entity fdeuhup is
  port (dcklg : in time; vwkyh : linkage severity_level; bnbpxk : in boolean);
end fdeuhup;

library ieee;
use ieee.std_logic_1164.all;

architecture lzbojnsn of fdeuhup is
  signal msf : real;
  signal q : std_logic;
  signal b : time;
  signal lxyztguls : std_logic;
  signal npk : real;
  signal i : real;
  signal esnoimnkxb : time;
  signal nqntve : std_logic_vector(4 downto 2);
  signal jog : std_logic;
  signal rnokmyb : std_logic_vector(4 to 4);
  signal felud : real;
begin
  jsdp : entity work.ujsxrzqgy
    port map (g => felud, dyauoomn => rnokmyb, xibqda => jog);
  p : entity work.rgylokr
    port map (fw => nqntve, wkrcdp => esnoimnkxb, svbqcwotcs => jog, rrvz => i);
  hmtydkvuh : entity work.ujsxrzqgy
    port map (g => npk, dyauoomn => rnokmyb, xibqda => lxyztguls);
  dfand : entity work.rgylokr
    port map (fw => nqntve, wkrcdp => b, svbqcwotcs => q, rrvz => msf);
  
  -- Multi-driven assignments
  rnokmyb <= "H";
  jog <= '0';
end lzbojnsn;

library ieee;
use ieee.std_logic_1164.all;

entity zspzkrdvl is
  port (qhifpcucz : linkage std_logic_vector(4 downto 4));
end zspzkrdvl;

library ieee;
use ieee.std_logic_1164.all;

architecture qd of zspzkrdvl is
  signal nrjd : std_logic;
  signal mdtzi : real;
  signal oe : std_logic;
  signal enfjp : real;
  signal qnekla : real;
  signal fffvbf : std_logic;
  signal wkhvunwzab : std_logic_vector(4 to 4);
  signal dunklna : real;
begin
  eciwcr : entity work.ujsxrzqgy
    port map (g => dunklna, dyauoomn => wkhvunwzab, xibqda => fffvbf);
  wffgryd : entity work.ujsxrzqgy
    port map (g => qnekla, dyauoomn => wkhvunwzab, xibqda => fffvbf);
  frwzcv : entity work.ujsxrzqgy
    port map (g => enfjp, dyauoomn => wkhvunwzab, xibqda => oe);
  fb : entity work.ujsxrzqgy
    port map (g => mdtzi, dyauoomn => qhifpcucz, xibqda => nrjd);
  
  -- Multi-driven assignments
  wkhvunwzab <= (others => 'L');
  fffvbf <= 'W';
end qd;



-- Seed after: 5240567020665533259,1834764876137802293
