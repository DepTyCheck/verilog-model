-- Seed: 4316786945416690154,11127274767545411571

entity lmw is
  port (bulmxyoxo : in real; vdhgaher : out integer_vector(0 downto 1); o : linkage bit_vector(0 downto 3); vtvqacsm : buffer real);
end lmw;

architecture gz of lmw is
  
begin
  
end gz;

library ieee;
use ieee.std_logic_1164.all;

entity k is
  port (mlskxqfknh : in real; xz : out integer; v : inout std_logic_vector(1 downto 3));
end k;

architecture hagoz of k is
  
begin
  -- Single-driven assignments
  xz <= xz;
  
  -- Multi-driven assignments
  v <= v;
end hagoz;

entity lcveb is
  port (dmqc : out severity_level; pbnlr : linkage character; ayrybm : linkage real);
end lcveb;

architecture abdbwdf of lcveb is
  signal yl : real;
  signal wbemizqsqk : bit_vector(0 downto 3);
  signal xbupzmzwkk : integer_vector(0 downto 1);
  signal ydbcmgw : real;
  signal jaumzrliwn : bit_vector(0 downto 3);
  signal plzshedpd : integer_vector(0 downto 1);
  signal t : real;
begin
  jlcbiphzdp : entity work.lmw
    port map (bulmxyoxo => t, vdhgaher => plzshedpd, o => jaumzrliwn, vtvqacsm => t);
  u : entity work.lmw
    port map (bulmxyoxo => ydbcmgw, vdhgaher => xbupzmzwkk, o => wbemizqsqk, vtvqacsm => yl);
  
  -- Single-driven assignments
  ydbcmgw <= 2#1.1_1#;
  dmqc <= WARNING;
end abdbwdf;

entity ptfxhrdu is
  port (z : out integer);
end ptfxhrdu;

architecture plkxj of ptfxhrdu is
  signal ebysnkvtbe : character;
  signal gtci : severity_level;
  signal uq : real;
  signal snbsdg : bit_vector(0 downto 3);
  signal i : integer_vector(0 downto 1);
  signal jhokwf : real;
  signal wti : real;
  signal y : bit_vector(0 downto 3);
  signal nctlbcmlr : integer_vector(0 downto 1);
  signal xsvc : real;
begin
  gunvr : entity work.lmw
    port map (bulmxyoxo => xsvc, vdhgaher => nctlbcmlr, o => y, vtvqacsm => wti);
  yrmz : entity work.lmw
    port map (bulmxyoxo => jhokwf, vdhgaher => i, o => snbsdg, vtvqacsm => uq);
  ayeesbm : entity work.lcveb
    port map (dmqc => gtci, pbnlr => ebysnkvtbe, ayrybm => jhokwf);
end plkxj;



-- Seed after: 853232228989785777,11127274767545411571
