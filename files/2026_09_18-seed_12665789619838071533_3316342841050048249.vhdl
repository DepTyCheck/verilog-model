-- Seed: 12665789619838071533,3316342841050048249

library ieee;
use ieee.std_logic_1164.all;

entity gggzehuht is
  port (tyshbhqrld : inout character; ow : in std_logic_vector(4 downto 1));
end gggzehuht;

architecture prr of gggzehuht is
  
begin
  -- Single-driven assignments
  tyshbhqrld <= 'a';
end prr;

entity beloo is
  port (jzaja : buffer time; fisrbvrilr : linkage time; mpsq : inout bit; azfgcay : inout real);
end beloo;

library ieee;
use ieee.std_logic_1164.all;

architecture buvwe of beloo is
  signal mnxeiscs : character;
  signal untmstmnlc : std_logic_vector(4 downto 1);
  signal qdzz : character;
  signal klwuwudqs : std_logic_vector(4 downto 1);
  signal vwfw : character;
begin
  d : entity work.gggzehuht
    port map (tyshbhqrld => vwfw, ow => klwuwudqs);
  sj : entity work.gggzehuht
    port map (tyshbhqrld => qdzz, ow => untmstmnlc);
  ugtejg : entity work.gggzehuht
    port map (tyshbhqrld => mnxeiscs, ow => untmstmnlc);
  
  -- Single-driven assignments
  azfgcay <= 41101.1_4_1;
  mpsq <= '1';
  
  -- Multi-driven assignments
  klwuwudqs <= "X0UZ";
  klwuwudqs <= klwuwudqs;
  untmstmnlc <= ('H', 'H', 'L', 'L');
  klwuwudqs <= "0HLW";
end buvwe;

library ieee;
use ieee.std_logic_1164.all;

entity jspxydcwv is
  port (wvn : buffer std_logic; vlmim : buffer std_logic_vector(0 to 0); xxy : out time; lbogrhl : linkage std_logic);
end jspxydcwv;

library ieee;
use ieee.std_logic_1164.all;

architecture tkqgwhy of jspxydcwv is
  signal k : character;
  signal sjk : character;
  signal bsrnnf : real;
  signal zw : bit;
  signal hyrzoxnqz : time;
  signal o : std_logic_vector(4 downto 1);
  signal kqto : character;
begin
  lxlbm : entity work.gggzehuht
    port map (tyshbhqrld => kqto, ow => o);
  huxiunlk : entity work.beloo
    port map (jzaja => hyrzoxnqz, fisrbvrilr => xxy, mpsq => zw, azfgcay => bsrnnf);
  mwjup : entity work.gggzehuht
    port map (tyshbhqrld => sjk, ow => o);
  zqtaakq : entity work.gggzehuht
    port map (tyshbhqrld => k, ow => o);
  
  -- Multi-driven assignments
  vlmim <= vlmim;
  vlmim <= vlmim;
end tkqgwhy;



-- Seed after: 17358003203047134083,3316342841050048249
