-- Seed: 16276701581984662290,13613332369802491303

library ieee;
use ieee.std_logic_1164.all;

entity m is
  port (spzoizxq : in real; osomaqik : buffer bit_vector(3 downto 2); lsfq : out std_logic_vector(3 to 1));
end m;

architecture qavh of m is
  
begin
  -- Single-driven assignments
  osomaqik <= osomaqik;
  
  -- Multi-driven assignments
  lsfq <= lsfq;
  lsfq <= lsfq;
end qavh;

entity jxu is
  port (r : inout string(4 downto 2));
end jxu;

library ieee;
use ieee.std_logic_1164.all;

architecture bpr of jxu is
  signal any : std_logic_vector(3 to 1);
  signal giqvp : bit_vector(3 downto 2);
  signal cnwnhfedp : bit_vector(3 downto 2);
  signal u : real;
  signal dpmjmdppdq : std_logic_vector(3 to 1);
  signal gy : bit_vector(3 downto 2);
  signal jmphthhqli : real;
  signal wd : std_logic_vector(3 to 1);
  signal aqrqid : bit_vector(3 downto 2);
  signal szzn : real;
begin
  rwaye : entity work.m
    port map (spzoizxq => szzn, osomaqik => aqrqid, lsfq => wd);
  ikbc : entity work.m
    port map (spzoizxq => jmphthhqli, osomaqik => gy, lsfq => dpmjmdppdq);
  sasaapues : entity work.m
    port map (spzoizxq => u, osomaqik => cnwnhfedp, lsfq => dpmjmdppdq);
  bfz : entity work.m
    port map (spzoizxq => u, osomaqik => giqvp, lsfq => any);
  
  -- Multi-driven assignments
  wd <= wd;
end bpr;

library ieee;
use ieee.std_logic_1164.all;

entity c is
  port (dhnroxlnaj : in std_logic_vector(1 to 3));
end c;

architecture pw of c is
  
begin
  
end pw;

library ieee;
use ieee.std_logic_1164.all;

entity xx is
  port (mokkqrz : linkage std_logic; gjabfpm : buffer time; vvoag : linkage bit);
end xx;

library ieee;
use ieee.std_logic_1164.all;

architecture cw of xx is
  signal gjk : std_logic_vector(3 to 1);
  signal pvxxhl : bit_vector(3 downto 2);
  signal th : real;
  signal ebjkzu : bit_vector(3 downto 2);
  signal shajbao : std_logic_vector(3 to 1);
  signal xjwyat : bit_vector(3 downto 2);
  signal o : real;
  signal glaxjgrfy : string(4 downto 2);
begin
  eqhkg : entity work.jxu
    port map (r => glaxjgrfy);
  dmqvjlxpt : entity work.m
    port map (spzoizxq => o, osomaqik => xjwyat, lsfq => shajbao);
  n : entity work.m
    port map (spzoizxq => o, osomaqik => ebjkzu, lsfq => shajbao);
  brhyfbfl : entity work.m
    port map (spzoizxq => th, osomaqik => pvxxhl, lsfq => gjk);
  
  -- Single-driven assignments
  gjabfpm <= gjabfpm;
  th <= 16#987D.4_2#;
  o <= o;
end cw;



-- Seed after: 6700631069838227060,13613332369802491303
