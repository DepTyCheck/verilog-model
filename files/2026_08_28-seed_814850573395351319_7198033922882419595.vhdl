-- Seed: 814850573395351319,7198033922882419595

entity lerm is
  port (qoth : in time; awswo : out real; tlq : linkage integer_vector(4 downto 4); ybxb : buffer time);
end lerm;

architecture fbymto of lerm is
  
begin
  -- Single-driven assignments
  ybxb <= ybxb;
  awswo <= 4.0_4_1;
end fbymto;

library ieee;
use ieee.std_logic_1164.all;

entity nntb is
  port (ff : inout std_logic_vector(4 to 1); iqzwvoqw : linkage boolean);
end nntb;

architecture ojlyikohs of nntb is
  signal sdbnbyb : integer_vector(4 downto 4);
  signal sqdp : real;
  signal dhdj : integer_vector(4 downto 4);
  signal za : real;
  signal zrevnzslnq : time;
  signal gi : time;
  signal hc : integer_vector(4 downto 4);
  signal w : real;
  signal bsxd : time;
begin
  bxto : entity work.lerm
    port map (qoth => bsxd, awswo => w, tlq => hc, ybxb => gi);
  a : entity work.lerm
    port map (qoth => zrevnzslnq, awswo => za, tlq => dhdj, ybxb => bsxd);
  ncnutpvwv : entity work.lerm
    port map (qoth => bsxd, awswo => sqdp, tlq => sdbnbyb, ybxb => zrevnzslnq);
  
  -- Multi-driven assignments
  ff <= (others => '0');
  ff <= ff;
end ojlyikohs;



-- Seed after: 6684037220720192694,7198033922882419595
