-- Seed: 5317232507325447942,17047277710231705797

library ieee;
use ieee.std_logic_1164.all;

entity mbvlgsli is
  port (p : linkage time; ockosfikzk : inout integer; lzncpcrftp : inout std_logic);
end mbvlgsli;

architecture whne of mbvlgsli is
  
begin
  -- Single-driven assignments
  ockosfikzk <= 2#0_0#;
  
  -- Multi-driven assignments
  lzncpcrftp <= '0';
  lzncpcrftp <= 'Z';
  lzncpcrftp <= 'U';
end whne;

library ieee;
use ieee.std_logic_1164.all;

entity glekadgx is
  port ( r : out bit_vector(3 to 1)
  ; k : linkage std_logic_vector(4 downto 2)
  ; hnjzart : in std_logic_vector(0 to 2)
  ; kh : buffer boolean_vector(2 to 4)
  );
end glekadgx;

library ieee;
use ieee.std_logic_1164.all;

architecture gjnachd of glekadgx is
  signal ohihsmrbfm : integer;
  signal hiz : time;
  signal sy : std_logic;
  signal nda : integer;
  signal sozift : time;
  signal h : std_logic;
  signal pgqx : integer;
  signal fc : time;
  signal xgehypv : std_logic;
  signal bnjffbjkjb : integer;
  signal jklhhvnjov : time;
begin
  xuunz : entity work.mbvlgsli
    port map (p => jklhhvnjov, ockosfikzk => bnjffbjkjb, lzncpcrftp => xgehypv);
  zobeuzjiiu : entity work.mbvlgsli
    port map (p => fc, ockosfikzk => pgqx, lzncpcrftp => h);
  rwqi : entity work.mbvlgsli
    port map (p => sozift, ockosfikzk => nda, lzncpcrftp => sy);
  j : entity work.mbvlgsli
    port map (p => hiz, ockosfikzk => ohihsmrbfm, lzncpcrftp => xgehypv);
  
  -- Single-driven assignments
  kh <= (TRUE, FALSE, FALSE);
  r <= (others => '0');
  
  -- Multi-driven assignments
  xgehypv <= 'W';
end gjnachd;

library ieee;
use ieee.std_logic_1164.all;

entity bwf is
  port (odwavl : linkage integer; dfr : in std_logic; ujtm : buffer integer; zkgaddqj : buffer time_vector(3 downto 1));
end bwf;

library ieee;
use ieee.std_logic_1164.all;

architecture vooq of bwf is
  signal j : std_logic;
  signal edeisftwyu : time;
  signal csgfis : std_logic;
  signal te : integer;
  signal abpzdneuy : time;
  signal qw : integer;
  signal hyjx : time;
  signal sdqphsc : std_logic;
  signal cobttsbngq : integer;
  signal wkkffw : time;
begin
  tyw : entity work.mbvlgsli
    port map (p => wkkffw, ockosfikzk => cobttsbngq, lzncpcrftp => sdqphsc);
  fugulgy : entity work.mbvlgsli
    port map (p => hyjx, ockosfikzk => qw, lzncpcrftp => sdqphsc);
  ksgmds : entity work.mbvlgsli
    port map (p => abpzdneuy, ockosfikzk => te, lzncpcrftp => csgfis);
  ygnmmhdafs : entity work.mbvlgsli
    port map (p => edeisftwyu, ockosfikzk => ujtm, lzncpcrftp => j);
  
  -- Single-driven assignments
  zkgaddqj <= (0 hr, 2 sec, 16#C_8_E_4_E.1F# fs);
  
  -- Multi-driven assignments
  sdqphsc <= 'W';
end vooq;

entity nlr is
  port (lfxpeys : buffer time; nwebiwv : buffer bit);
end nlr;

library ieee;
use ieee.std_logic_1164.all;

architecture wnjm of nlr is
  signal oo : std_logic;
  signal fogcryae : integer;
  signal jkojc : time;
  signal zfzq : time_vector(3 downto 1);
  signal dfur : integer;
  signal ycrm : std_logic;
  signal qx : integer;
  signal rkpobw : std_logic;
  signal ig : integer;
  signal qm : time;
begin
  elamnbfxg : entity work.mbvlgsli
    port map (p => qm, ockosfikzk => ig, lzncpcrftp => rkpobw);
  ix : entity work.bwf
    port map (odwavl => qx, dfr => ycrm, ujtm => dfur, zkgaddqj => zfzq);
  j : entity work.mbvlgsli
    port map (p => jkojc, ockosfikzk => fogcryae, lzncpcrftp => oo);
  
  -- Single-driven assignments
  lfxpeys <= 2#000.1_0# fs;
  nwebiwv <= '0';
  
  -- Multi-driven assignments
  rkpobw <= 'W';
  ycrm <= '0';
  rkpobw <= 'X';
end wnjm;



-- Seed after: 7162683270750053953,17047277710231705797
