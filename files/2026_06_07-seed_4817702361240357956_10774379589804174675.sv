// Seed: 4817702361240357956,10774379589804174675

module kn (output tri1 logic [0:0][1:4] eaurivd);
  
  xor cwqddzjnu(eaurivd, eaurivd, xmr);
  // warning: implicit conversion of port connection truncates from 4 to 1 bits
  //   tri1 logic [0:0][1:4] eaurivd -> logic eaurivd
  //
  // warning: implicit conversion of port connection truncates from 4 to 1 bits
  //   tri1 logic [0:0][1:4] eaurivd -> logic eaurivd
  
  xnor aqdiuqwzix(h, joqluwcmj, eaurivd);
  // warning: implicit conversion of port connection truncates from 4 to 1 bits
  //   tri1 logic [0:0][1:4] eaurivd -> logic eaurivd
  
  not mmurroilcg(nynuezx, cgtuhegd);
  
  nand merb(c, joqluwcmj, eaurivd);
  // warning: implicit conversion of port connection truncates from 4 to 1 bits
  //   tri1 logic [0:0][1:4] eaurivd -> logic eaurivd
  
  
  // Single-driven assigns
  
  // Multi-driven assigns
  assign h = h;
  assign eaurivd = '{'{'b011,'b1xz10,'b1x,'bzz1}};
endmodule: kn

module x (output wor logic [1:2][2:4][2:4][1:1] c [4:3]);
  
  nand migh(bxcpcyow, bxcpcyow, nclg);
  
  xnor ev(va, drcx, ece);
  
  
  // Single-driven assigns
  
  // Multi-driven assigns
endmodule: x

module p (input shortint jyi [2:1][3:2], input wand logic [2:3] wqaik [1:1][3:1][1:3][1:2]);
  // Unpacked net declarations
  wor logic [1:2][2:4][2:4][1:1] rsugthto [4:3];
  wor logic [1:2][2:4][2:4][1:1] xirua [4:3];
  
  x ngkgzxhyn(.c(xirua));
  
  x qqsza(.c(rsugthto));
  
  xnor xlgx(gh, gh, gh);
  
  
  // Single-driven assigns
  
  // Multi-driven assigns
  assign xirua = xirua;
  assign gh = 'bxz;
  assign rsugthto = xirua;
  assign wqaik = wqaik;
endmodule: p



// Seed after: 16618050451160949041,10774379589804174675
