-- Seed: 57272416605169094,5415160250146859793

library ieee;
use ieee.std_logic_1164.all;

entity ipsu is
  port (hpzurbgor : linkage std_logic_vector(2 to 1); johy : buffer time; deqfbnadl : linkage real; nll : linkage time);
end ipsu;



architecture gtrhmvww of ipsu is
  
begin
  
end gtrhmvww;



entity dmqdjawifc is
  port (dklyo : buffer time_vector(1 to 4); vtdyokwe : in bit_vector(4 downto 3); glmy : in severity_level; lphbl : out severity_level);
end dmqdjawifc;



architecture jsbez of dmqdjawifc is
  
begin
  
end jsbez;

library ieee;
use ieee.std_logic_1164.all;

entity antel is
  port (zkjnif : inout time; eqfozab : buffer severity_level; aekxijt : buffer std_logic);
end antel;

library ieee;
use ieee.std_logic_1164.all;

architecture zkqx of antel is
  signal eosyjuuyl : severity_level;
  signal h : severity_level;
  signal ji : bit_vector(4 downto 3);
  signal zlt : time_vector(1 to 4);
  signal phf : bit_vector(4 downto 3);
  signal osfrxfszfe : time_vector(1 to 4);
  signal sljabuhzhw : real;
  signal oig : time;
  signal af : std_logic_vector(2 to 1);
  signal gtl : time;
  signal dfody : real;
  signal tzgpcac : time;
  signal tlirwtjwdw : std_logic_vector(2 to 1);
begin
  pbodvxa : entity work.ipsu
    port map (hpzurbgor => tlirwtjwdw, johy => tzgpcac, deqfbnadl => dfody, nll => gtl);
  fcplq : entity work.ipsu
    port map (hpzurbgor => af, johy => oig, deqfbnadl => sljabuhzhw, nll => zkjnif);
  rg : entity work.dmqdjawifc
    port map (dklyo => osfrxfszfe, vtdyokwe => phf, glmy => eqfozab, lphbl => eqfozab);
  qsq : entity work.dmqdjawifc
    port map (dklyo => zlt, vtdyokwe => ji, glmy => h, lphbl => eosyjuuyl);
end zkqx;



-- Seed after: 18024516133542519103,5415160250146859793
