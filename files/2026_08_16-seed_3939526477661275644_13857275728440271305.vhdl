-- Seed: 3939526477661275644,13857275728440271305

library ieee;
use ieee.std_logic_1164.all;

entity ht is
  port (uij : out integer; o : linkage std_logic; fxfprckji : buffer bit; hkos : out integer);
end ht;

architecture aqwmo of ht is
  
begin
  -- Single-driven assignments
  fxfprckji <= fxfprckji;
end aqwmo;

library ieee;
use ieee.std_logic_1164.all;

entity pegeelygi is
  port (szlzpz : inout real; hwsmz : in severity_level; tjubly : buffer character; cguqcc : inout std_logic);
end pegeelygi;

architecture qe of pegeelygi is
  signal h : integer;
  signal vlw : bit;
  signal sherzeabej : integer;
begin
  hcff : entity work.ht
    port map (uij => sherzeabej, o => cguqcc, fxfprckji => vlw, hkos => h);
  
  -- Single-driven assignments
  tjubly <= tjubly;
  szlzpz <= 8#1_3_0_3.1_1_3_0_6#;
end qe;

entity ex is
  port (s : out integer; pjvdghilna : out real);
end ex;

library ieee;
use ieee.std_logic_1164.all;

architecture chvnqla of ex is
  signal k : bit;
  signal ntqabguakk : integer;
  signal fvgozsc : integer;
  signal hqyzlmzbr : bit;
  signal tnb : integer;
  signal rcogp : std_logic;
  signal ldej : character;
  signal drfgwwx : severity_level;
begin
  q : entity work.pegeelygi
    port map (szlzpz => pjvdghilna, hwsmz => drfgwwx, tjubly => ldej, cguqcc => rcogp);
  jlpfunntib : entity work.ht
    port map (uij => tnb, o => rcogp, fxfprckji => hqyzlmzbr, hkos => fvgozsc);
  fopuymdzy : entity work.ht
    port map (uij => ntqabguakk, o => rcogp, fxfprckji => k, hkos => s);
  
  -- Single-driven assignments
  drfgwwx <= drfgwwx;
  
  -- Multi-driven assignments
  rcogp <= '-';
  rcogp <= 'W';
end chvnqla;



-- Seed after: 7776932723725218900,13857275728440271305
