-- Seed: 14940430588315556299,779721673283024895

library ieee;
use ieee.std_logic_1164.all;

entity zpiev is
  port (dcrvpbte : in integer; zgcezk : in severity_level; je : linkage std_logic);
end zpiev;



architecture caolpt of zpiev is
  
begin
  
end caolpt;

library ieee;
use ieee.std_logic_1164.all;

entity rnon is
  port (jknklmjg : out std_logic; ensecafnr : linkage severity_level; cqh : in real);
end rnon;

library ieee;
use ieee.std_logic_1164.all;

architecture qqsksnbc of rnon is
  signal eueirqwl : std_logic;
  signal err : integer;
  signal qdthigv : severity_level;
  signal tmyhnqh : integer;
  signal pls : severity_level;
  signal sqxceygzyl : integer;
begin
  c : entity work.zpiev
    port map (dcrvpbte => sqxceygzyl, zgcezk => pls, je => jknklmjg);
  acsu : entity work.zpiev
    port map (dcrvpbte => tmyhnqh, zgcezk => qdthigv, je => jknklmjg);
  qpoacpwasv : entity work.zpiev
    port map (dcrvpbte => err, zgcezk => pls, je => eueirqwl);
end qqsksnbc;



entity yhns is
  port (c : linkage integer; hugld : out time; eixljcems : out character; ovizeuvf : inout integer);
end yhns;

library ieee;
use ieee.std_logic_1164.all;

architecture yaetqgabi of yhns is
  signal w : real;
  signal ukdup : severity_level;
  signal okpp : std_logic;
  signal yynpgwwwex : severity_level;
  signal m : integer;
  signal brv : std_logic;
  signal len : severity_level;
  signal qzjxb : integer;
begin
  psnbn : entity work.zpiev
    port map (dcrvpbte => qzjxb, zgcezk => len, je => brv);
  hszabr : entity work.zpiev
    port map (dcrvpbte => m, zgcezk => yynpgwwwex, je => brv);
  pghmpbol : entity work.rnon
    port map (jknklmjg => okpp, ensecafnr => ukdup, cqh => w);
  icrg : entity work.zpiev
    port map (dcrvpbte => qzjxb, zgcezk => ukdup, je => brv);
end yaetqgabi;



-- Seed after: 6764693868315276970,779721673283024895
