-- Seed: 5134184200265928356,9125939553767483053



entity vwdsa is
  port (ge : in real_vector(4 to 2); eohruorxd : in integer; bjrf : out character; gak : in integer);
end vwdsa;



architecture hnr of vwdsa is
  
begin
  
end hnr;



entity xdiyan is
  port (jnbyh : inout bit);
end xdiyan;



architecture dauht of xdiyan is
  signal wihng : integer;
  signal uq : character;
  signal el : integer;
  signal tueupobyoh : integer;
  signal a : character;
  signal bdpo : real_vector(4 to 2);
  signal uqcxp : character;
  signal d : real_vector(4 to 2);
  signal yevefd : character;
  signal izdqsu : integer;
  signal wmkbuyghs : real_vector(4 to 2);
begin
  lkhdsxrcej : entity work.vwdsa
    port map (ge => wmkbuyghs, eohruorxd => izdqsu, bjrf => yevefd, gak => izdqsu);
  ldaixl : entity work.vwdsa
    port map (ge => d, eohruorxd => izdqsu, bjrf => uqcxp, gak => izdqsu);
  yuyx : entity work.vwdsa
    port map (ge => bdpo, eohruorxd => izdqsu, bjrf => a, gak => tueupobyoh);
  kwvo : entity work.vwdsa
    port map (ge => wmkbuyghs, eohruorxd => el, bjrf => uq, gak => wihng);
end dauht;

library ieee;
use ieee.std_logic_1164.all;

entity djxpg is
  port (msxgonxaaf : out std_logic; jgbwfp : inout real; qnkvit : out time);
end djxpg;



architecture a of djxpg is
  signal uxtafaxlid : integer;
  signal sc : character;
  signal lz : integer;
  signal zwmjr : character;
  signal uvvisl : integer;
  signal iuwzmkippq : real_vector(4 to 2);
  signal ikss : character;
  signal yvbcuso : integer;
  signal v : real_vector(4 to 2);
  signal ct : integer;
  signal vznm : character;
  signal s : integer;
  signal eubgoigii : real_vector(4 to 2);
begin
  atcd : entity work.vwdsa
    port map (ge => eubgoigii, eohruorxd => s, bjrf => vznm, gak => ct);
  d : entity work.vwdsa
    port map (ge => v, eohruorxd => yvbcuso, bjrf => ikss, gak => s);
  r : entity work.vwdsa
    port map (ge => iuwzmkippq, eohruorxd => uvvisl, bjrf => zwmjr, gak => s);
  kgxesrxmnm : entity work.vwdsa
    port map (ge => iuwzmkippq, eohruorxd => lz, bjrf => sc, gak => uxtafaxlid);
end a;



-- Seed after: 13065406852820558493,9125939553767483053
