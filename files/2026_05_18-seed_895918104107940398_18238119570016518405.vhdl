-- Seed: 895918104107940398,18238119570016518405



entity tk is
  port (fgh : linkage real; xxprf : inout time; lokb : inout boolean);
end tk;



architecture zroklvqk of tk is
  
begin
  
end zroklvqk;

library ieee;
use ieee.std_logic_1164.all;

entity kri is
  port (ymp : buffer std_logic_vector(0 downto 3); owigfd : in integer);
end kri;



architecture eukapa of kri is
  signal n : boolean;
  signal wsmvmsqprx : time;
  signal sgizae : boolean;
  signal urpgaq : time;
  signal huc : boolean;
  signal uibfd : time;
  signal sppfy : boolean;
  signal agfdi : time;
  signal bf : real;
begin
  augwrxt : entity work.tk
    port map (fgh => bf, xxprf => agfdi, lokb => sppfy);
  c : entity work.tk
    port map (fgh => bf, xxprf => uibfd, lokb => huc);
  yshh : entity work.tk
    port map (fgh => bf, xxprf => urpgaq, lokb => sgizae);
  f : entity work.tk
    port map (fgh => bf, xxprf => wsmvmsqprx, lokb => n);
end eukapa;



-- Seed after: 12530366819392073698,18238119570016518405
