-- Seed: 4400617740008726966,3566912872917928779

use std.reflection.all;

entity wedqkl is
  port (brb : inout record_value_mirror; isbyfos : out time);
end wedqkl;

architecture so of wedqkl is
  
begin
  -- Single-driven assignments
  isbyfos <= isbyfos;
end so;

use std.reflection.all;

entity zvokojddrx is
  port (lm : inout real_vector(0 to 3); dclm : inout access_value_mirror; xqodwnlcpv : in boolean);
end zvokojddrx;

use std.reflection.all;

architecture pnqgw of zvokojddrx is
  signal jo : time;
  shared variable jyflokcx : record_value_mirror;
  signal ey : time;
  shared variable tba : record_value_mirror;
begin
  ucfuyqb : entity work.wedqkl
    port map (brb => tba, isbyfos => ey);
  nzhon : entity work.wedqkl
    port map (brb => jyflokcx, isbyfos => jo);
  
  -- Single-driven assignments
  lm <= lm;
end pnqgw;

entity dhbw is
  port (bsqhbzaks : buffer bit);
end dhbw;

use std.reflection.all;

architecture h of dhbw is
  signal afxz : time;
  shared variable xotcuayu : record_value_mirror;
  signal a : boolean;
  shared variable l : access_value_mirror;
  signal qlf : real_vector(0 to 3);
  signal poujzk : time;
  shared variable xvoykkmwuu : record_value_mirror;
begin
  cjme : entity work.wedqkl
    port map (brb => xvoykkmwuu, isbyfos => poujzk);
  fqlzdpwygc : entity work.zvokojddrx
    port map (lm => qlf, dclm => l, xqodwnlcpv => a);
  lr : entity work.wedqkl
    port map (brb => xotcuayu, isbyfos => afxz);
  
  -- Single-driven assignments
  bsqhbzaks <= '1';
  a <= a;
end h;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity zqkrvcp is
  port (cjmivmgn : in bit_vector(3 downto 2); jvoj : inout subtype_mirror; n : buffer time; femirfmd : out std_logic);
end zqkrvcp;

use std.reflection.all;

architecture od of zqkrvcp is
  shared variable rtkq : record_value_mirror;
begin
  rxi : entity work.wedqkl
    port map (brb => rtkq, isbyfos => n);
  
  -- Multi-driven assignments
  femirfmd <= 'Z';
  femirfmd <= femirfmd;
  femirfmd <= 'Z';
end od;



-- Seed after: 12869443784800036814,3566912872917928779
