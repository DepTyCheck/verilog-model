-- Seed: 16143347807611996910,10754487200446211253

library ieee;
use ieee.std_logic_1164.all;

entity pi is
  port (q : out std_logic_vector(0 downto 3); xko : in std_logic);
end pi;

architecture uukaa of pi is
  
begin
  -- Multi-driven assignments
  q <= q;
  q <= (others => '0');
end uukaa;

library ieee;
use ieee.std_logic_1164.all;

entity havvcowfm is
  port (wxw : out character; veebzheple : out std_logic);
end havvcowfm;

library ieee;
use ieee.std_logic_1164.all;

architecture ymugqdx of havvcowfm is
  signal g : std_logic;
  signal vpv : std_logic_vector(0 downto 3);
  signal qept : std_logic;
  signal xcy : std_logic_vector(0 downto 3);
begin
  umfjxijgny : entity work.pi
    port map (q => xcy, xko => qept);
  v : entity work.pi
    port map (q => vpv, xko => g);
  
  -- Single-driven assignments
  wxw <= 'g';
end ymugqdx;

entity hd is
  port (evghnadnb : linkage severity_level; xxbpiw : in real);
end hd;

library ieee;
use ieee.std_logic_1164.all;

architecture k of hd is
  signal ruqazdn : std_logic;
  signal nadacmvep : std_logic;
  signal pu : std_logic_vector(0 downto 3);
begin
  zwiq : entity work.pi
    port map (q => pu, xko => nadacmvep);
  vewyouqoj : entity work.pi
    port map (q => pu, xko => nadacmvep);
  ddtd : entity work.pi
    port map (q => pu, xko => ruqazdn);
  
  -- Multi-driven assignments
  ruqazdn <= nadacmvep;
  nadacmvep <= nadacmvep;
  pu <= pu;
end k;

library ieee;
use ieee.std_logic_1164.all;

entity pa is
  port (atlfmzloo : out bit_vector(0 to 4); xyquwnbcmn : inout bit; cyerhtboik : out std_logic_vector(1 to 3));
end pa;

architecture k of pa is
  
begin
  -- Multi-driven assignments
  cyerhtboik <= cyerhtboik;
end k;



-- Seed after: 10331874001153293425,10754487200446211253
