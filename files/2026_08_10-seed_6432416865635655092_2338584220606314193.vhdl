-- Seed: 6432416865635655092,2338584220606314193

entity ugano is
  port (ae : linkage integer; jxrlf : inout severity_level);
end ugano;

architecture imluowvx of ugano is
  
begin
  -- Single-driven assignments
  jxrlf <= ERROR;
end imluowvx;

library ieee;
use ieee.std_logic_1164.all;

entity pqjerbuwu is
  port (q : linkage integer; hxmlm : out integer; lcgwnuptw : in severity_level; eqwfsmcqv : in std_logic_vector(2 downto 4));
end pqjerbuwu;

architecture skiafs of pqjerbuwu is
  signal sm : severity_level;
  signal wfdmdof : severity_level;
  signal eqnu : integer;
  signal s : severity_level;
begin
  wqjxc : entity work.ugano
    port map (ae => hxmlm, jxrlf => s);
  xluncx : entity work.ugano
    port map (ae => eqnu, jxrlf => wfdmdof);
  blvjskxd : entity work.ugano
    port map (ae => q, jxrlf => sm);
end skiafs;

entity pxhj is
  port (r : buffer integer);
end pxhj;

architecture mmqefnfbph of pxhj is
  signal rtwmcpts : severity_level;
  signal ff : integer;
  signal omu : severity_level;
  signal cbfttgqz : severity_level;
  signal lohifaox : integer;
  signal u : severity_level;
  signal jsjnabh : integer;
begin
  s : entity work.ugano
    port map (ae => jsjnabh, jxrlf => u);
  jbr : entity work.ugano
    port map (ae => lohifaox, jxrlf => cbfttgqz);
  xaumddb : entity work.ugano
    port map (ae => r, jxrlf => omu);
  mhiymtnysu : entity work.ugano
    port map (ae => ff, jxrlf => rtwmcpts);
end mmqefnfbph;



-- Seed after: 6270595997597462747,2338584220606314193
