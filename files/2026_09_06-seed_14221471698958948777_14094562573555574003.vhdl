-- Seed: 14221471698958948777,14094562573555574003

entity orl is
  port (ld : linkage integer; yayvq : out severity_level);
end orl;

architecture brexpd of orl is
  
begin
  -- Single-driven assignments
  yayvq <= yayvq;
end brexpd;

library ieee;
use ieee.std_logic_1164.all;

entity zerlquevz is
  port (pcrvrors : linkage std_logic_vector(3 downto 4); jqcwf : in integer; jesyjc : linkage std_logic_vector(0 downto 1));
end zerlquevz;

architecture vwo of zerlquevz is
  signal hevpx : severity_level;
  signal ijouvlt : integer;
  signal su : severity_level;
  signal am : integer;
  signal l : severity_level;
  signal scwrlu : integer;
begin
  uq : entity work.orl
    port map (ld => scwrlu, yayvq => l);
  jxc : entity work.orl
    port map (ld => am, yayvq => su);
  pypyxqm : entity work.orl
    port map (ld => ijouvlt, yayvq => hevpx);
end vwo;

library ieee;
use ieee.std_logic_1164.all;

entity cee is
  port (rr : in std_logic; pn : buffer integer; bxoiuwsry : in std_logic);
end cee;

architecture jzqt of cee is
  signal erdajco : severity_level;
  signal wzjmt : integer;
begin
  sgmgkksuep : entity work.orl
    port map (ld => wzjmt, yayvq => erdajco);
  
  -- Single-driven assignments
  pn <= pn;
end jzqt;

entity ebm is
  port (mnqo : in integer; hfohnuk : buffer integer_vector(2 to 4); bfpqma : in integer; czsrxvsiq : in bit);
end ebm;

architecture asddzfx of ebm is
  signal cqzjsoijv : severity_level;
  signal si : integer;
  signal ebotw : severity_level;
  signal ibwjzdcnfy : integer;
  signal vylkdtv : severity_level;
  signal zt : integer;
begin
  ygeucur : entity work.orl
    port map (ld => zt, yayvq => vylkdtv);
  kvuxfjz : entity work.orl
    port map (ld => ibwjzdcnfy, yayvq => ebotw);
  gzxdouj : entity work.orl
    port map (ld => si, yayvq => cqzjsoijv);
  
  -- Single-driven assignments
  hfohnuk <= (16#58A3#, 2#0_1_0_0#, 16#DB584#);
end asddzfx;



-- Seed after: 17643400771450913521,14094562573555574003
