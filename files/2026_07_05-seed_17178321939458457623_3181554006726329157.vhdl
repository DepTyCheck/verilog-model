-- Seed: 17178321939458457623,3181554006726329157

use std.reflection.all;

entity ul is
  port (abnohru : out integer; grfljudxu : inout record_value_mirror);
end ul;

architecture nuemtkixs of ul is
  
begin
  -- Single-driven assignments
  abnohru <= abnohru;
end nuemtkixs;

entity idwsuj is
  port (voeltmvc : linkage character);
end idwsuj;

architecture isatmpt of idwsuj is
  
begin
  
end isatmpt;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity ifjywxuwj is
  port (pot : in std_logic_vector(3 downto 1); ms : inout subtype_mirror; arcfbm : linkage real);
end ifjywxuwj;

use std.reflection.all;

architecture hysptco of ifjywxuwj is
  shared variable zcpwjxcq : record_value_mirror;
  signal s : integer;
  shared variable jullrtrxmv : record_value_mirror;
  signal rhrfr : integer;
  shared variable guesrp : record_value_mirror;
  signal ewaw : integer;
  shared variable mqhu : record_value_mirror;
  signal sofxfrd : integer;
begin
  epvkiaz : entity work.ul
    port map (abnohru => sofxfrd, grfljudxu => mqhu);
  gcaplzokxn : entity work.ul
    port map (abnohru => ewaw, grfljudxu => guesrp);
  nuhbokgrmp : entity work.ul
    port map (abnohru => rhrfr, grfljudxu => jullrtrxmv);
  xndzxrn : entity work.ul
    port map (abnohru => s, grfljudxu => zcpwjxcq);
end hysptco;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity sbjm is
  port (gzokuxj : inout file_subtype_mirror; zpq : linkage std_logic; e : out std_logic_vector(2 to 3));
end sbjm;

architecture nctl of sbjm is
  
begin
  -- Multi-driven assignments
  e <= "UU";
  e <= "11";
  e <= ('1', 'U');
  e <= e;
end nctl;



-- Seed after: 11457888661906421340,3181554006726329157
