-- Seed: 10113165100254081938,7198033922882419595

entity xkhtthlj is
  port (wlphb : buffer boolean; tmpnchmv : out string(2 to 3); whatn : linkage bit);
end xkhtthlj;

architecture hpcavzo of xkhtthlj is
  
begin
  -- Single-driven assignments
  wlphb <= TRUE;
  tmpnchmv <= ('j', 'k');
end hpcavzo;

entity mjkuhy is
  port (obiyarwjvc : in character);
end mjkuhy;

architecture de of mjkuhy is
  
begin
  
end de;

entity zbapfa is
  port (mhsk : in real);
end zbapfa;

architecture vxaa of zbapfa is
  signal vcdunnfp : bit;
  signal op : string(2 to 3);
  signal tf : boolean;
  signal hmwcispd : bit;
  signal uxspqs : string(2 to 3);
  signal ufbdto : boolean;
begin
  evtax : entity work.xkhtthlj
    port map (wlphb => ufbdto, tmpnchmv => uxspqs, whatn => hmwcispd);
  poanhlfjx : entity work.xkhtthlj
    port map (wlphb => tf, tmpnchmv => op, whatn => vcdunnfp);
end vxaa;

library ieee;
use ieee.std_logic_1164.all;

entity bq is
  port (sh : buffer std_logic_vector(4 to 4); w : in real; t : in severity_level; fqu : buffer severity_level);
end bq;

architecture thskkdjqk of bq is
  signal j : bit;
  signal fj : string(2 to 3);
  signal nhmlmf : boolean;
begin
  utig : entity work.xkhtthlj
    port map (wlphb => nhmlmf, tmpnchmv => fj, whatn => j);
  
  -- Single-driven assignments
  fqu <= ERROR;
  
  -- Multi-driven assignments
  sh <= sh;
end thskkdjqk;



-- Seed after: 2251254133356314221,7198033922882419595
