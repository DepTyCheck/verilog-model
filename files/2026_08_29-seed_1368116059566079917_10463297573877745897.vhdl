-- Seed: 1368116059566079917,10463297573877745897

library ieee;
use ieee.std_logic_1164.all;

entity ihx is
  port (rxi : out boolean_vector(3 to 0); fmaft : inout std_logic; cpo : inout time);
end ihx;

architecture fqv of ihx is
  
begin
  -- Single-driven assignments
  cpo <= 3_4_2_0 ms;
  rxi <= rxi;
  
  -- Multi-driven assignments
  fmaft <= fmaft;
  fmaft <= 'Z';
  fmaft <= fmaft;
end fqv;

entity pjnxfysxf is
  port (sb : in bit; mv : inout bit_vector(0 downto 1); zonrleeh : inout real);
end pjnxfysxf;

library ieee;
use ieee.std_logic_1164.all;

architecture ftmtmzh of pjnxfysxf is
  signal dogenbanjg : time;
  signal gjbhb : std_logic;
  signal hcho : boolean_vector(3 to 0);
  signal jcock : time;
  signal jbu : boolean_vector(3 to 0);
  signal ramwsdulbv : time;
  signal mhpsbjoep : std_logic;
  signal dycn : boolean_vector(3 to 0);
begin
  wpg : entity work.ihx
    port map (rxi => dycn, fmaft => mhpsbjoep, cpo => ramwsdulbv);
  zujihsuuh : entity work.ihx
    port map (rxi => jbu, fmaft => mhpsbjoep, cpo => jcock);
  lctphbxgy : entity work.ihx
    port map (rxi => hcho, fmaft => gjbhb, cpo => dogenbanjg);
  
  -- Single-driven assignments
  zonrleeh <= zonrleeh;
  mv <= mv;
  
  -- Multi-driven assignments
  mhpsbjoep <= 'Z';
end ftmtmzh;

entity vh is
  port (bsxspjuvw : buffer real; osjhmpqzt : inout integer; fkn : linkage time);
end vh;

architecture pfp of vh is
  
begin
  -- Single-driven assignments
  osjhmpqzt <= osjhmpqzt;
  bsxspjuvw <= 8#706.24314#;
end pfp;



-- Seed after: 448011895881379797,10463297573877745897
