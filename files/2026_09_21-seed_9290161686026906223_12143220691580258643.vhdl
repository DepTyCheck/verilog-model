-- Seed: 9290161686026906223,12143220691580258643

library ieee;
use ieee.std_logic_1164.all;

entity ifsgzbsi is
  port (mbnce : in std_logic; vv : buffer std_logic; xjn : buffer real);
end ifsgzbsi;

architecture jxk of ifsgzbsi is
  
begin
  -- Single-driven assignments
  xjn <= xjn;
  
  -- Multi-driven assignments
  vv <= 'H';
  vv <= vv;
  vv <= 'W';
  vv <= '0';
end jxk;

library ieee;
use ieee.std_logic_1164.all;

entity qutp is
  port (tpkqpn : inout std_logic_vector(2 downto 3); vzvxt : buffer time_vector(1 downto 2); en : out time; hjqid : in time_vector(3 downto 0));
end qutp;

library ieee;
use ieee.std_logic_1164.all;

architecture ybhybmxsz of qutp is
  signal udipl : real;
  signal tynbq : std_logic;
  signal mgnm : std_logic;
  signal eazobf : real;
  signal dxfz : std_logic;
  signal ewvulhqoo : real;
  signal bhvd : real;
  signal n : std_logic;
  signal vwekswiod : std_logic;
begin
  e : entity work.ifsgzbsi
    port map (mbnce => vwekswiod, vv => n, xjn => bhvd);
  iurxjiqr : entity work.ifsgzbsi
    port map (mbnce => vwekswiod, vv => vwekswiod, xjn => ewvulhqoo);
  qyerufnx : entity work.ifsgzbsi
    port map (mbnce => dxfz, vv => vwekswiod, xjn => eazobf);
  jpdkzm : entity work.ifsgzbsi
    port map (mbnce => mgnm, vv => tynbq, xjn => udipl);
  
  -- Single-driven assignments
  vzvxt <= vzvxt;
  en <= en;
  
  -- Multi-driven assignments
  vwekswiod <= vwekswiod;
end ybhybmxsz;

entity v is
  port (wkq : linkage real_vector(0 to 3));
end v;

library ieee;
use ieee.std_logic_1164.all;

architecture vvwvkldohr of v is
  signal dql : real;
  signal xdudlnqhie : std_logic;
  signal ymz : std_logic;
  signal c : time_vector(3 downto 0);
  signal qukzknrxc : time;
  signal j : time_vector(1 downto 2);
  signal abn : std_logic_vector(2 downto 3);
  signal oqnqjaequp : real;
  signal xofa : std_logic;
  signal g : std_logic;
begin
  xukh : entity work.ifsgzbsi
    port map (mbnce => g, vv => xofa, xjn => oqnqjaequp);
  n : entity work.qutp
    port map (tpkqpn => abn, vzvxt => j, en => qukzknrxc, hjqid => c);
  wjfffh : entity work.ifsgzbsi
    port map (mbnce => ymz, vv => xdudlnqhie, xjn => dql);
  
  -- Single-driven assignments
  c <= (1 sec, 1423 fs, 2_2_3_4_1 us, 1 min);
  
  -- Multi-driven assignments
  xdudlnqhie <= xofa;
  abn <= abn;
  g <= '1';
  g <= g;
end vvwvkldohr;



-- Seed after: 4100705615331332241,12143220691580258643
