-- Seed: 16127053197727197169,6290177331721581829

library ieee;
use ieee.std_logic_1164.all;

entity l is
  port (telvzrp : buffer std_logic_vector(4 to 2));
end l;

architecture rsmcddj of l is
  
begin
  -- Multi-driven assignments
  telvzrp <= telvzrp;
  telvzrp <= telvzrp;
  telvzrp <= telvzrp;
end rsmcddj;

use std.reflection.all;

entity jzldewy is
  port (inyihy : inout record_value_mirror; cqkumsrgy : inout physical_subtype_mirror);
end jzldewy;

library ieee;
use ieee.std_logic_1164.all;

architecture k of jzldewy is
  signal ba : std_logic_vector(4 to 2);
  signal ik : std_logic_vector(4 to 2);
begin
  kasaaiptpk : entity work.l
    port map (telvzrp => ik);
  umxfrpj : entity work.l
    port map (telvzrp => ba);
  
  -- Multi-driven assignments
  ik <= "";
end k;

use std.reflection.all;

entity eyfch is
  port (yxejz : inout access_subtype_mirror; npcmond : inout floating_subtype_mirror; lcbavzkjo : inout record_value_mirror);
end eyfch;

library ieee;
use ieee.std_logic_1164.all;

architecture ocmnjirong of eyfch is
  signal f : std_logic_vector(4 to 2);
  signal lrul : std_logic_vector(4 to 2);
  signal kotui : std_logic_vector(4 to 2);
begin
  k : entity work.l
    port map (telvzrp => kotui);
  idefrh : entity work.l
    port map (telvzrp => lrul);
  ihlulhstb : entity work.l
    port map (telvzrp => f);
  xyb : entity work.l
    port map (telvzrp => kotui);
end ocmnjirong;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity kddos is
  port (npqalgxqua : inout value_mirror; yrgxhkhp : inout protected_value_mirror; ruwgmbbrxo : linkage std_logic_vector(1 to 3));
end kddos;

library ieee;
use ieee.std_logic_1164.all;

architecture dcmg of kddos is
  signal coc : std_logic_vector(4 to 2);
begin
  nemzvlfg : entity work.l
    port map (telvzrp => coc);
end dcmg;



-- Seed after: 709589502490970506,6290177331721581829
