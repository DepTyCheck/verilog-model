-- Seed: 2323546858892181713,7726014785203345639

use std.reflection.all;

entity fox is
  port (okqhhx : in real; vlk : inout physical_subtype_mirror; tmef : out time);
end fox;

architecture gohm of fox is
  
begin
  -- Single-driven assignments
  tmef <= tmef;
end gohm;

library ieee;
use ieee.std_logic_1164.all;

entity mpkfoazk is
  port (iibku : in std_logic_vector(4 to 0); e : buffer integer_vector(0 to 3));
end mpkfoazk;

use std.reflection.all;

architecture znei of mpkfoazk is
  signal irj : time;
  shared variable xfssprq : physical_subtype_mirror;
  signal loadohqg : real;
  signal yzluufscam : time;
  shared variable molhgg : physical_subtype_mirror;
  signal ydicowgz : time;
  shared variable mjxwfg : physical_subtype_mirror;
  signal kfm : real;
begin
  fvmxpc : entity work.fox
    port map (okqhhx => kfm, vlk => mjxwfg, tmef => ydicowgz);
  c : entity work.fox
    port map (okqhhx => kfm, vlk => molhgg, tmef => yzluufscam);
  eybakknche : entity work.fox
    port map (okqhhx => loadohqg, vlk => xfssprq, tmef => irj);
  
  -- Single-driven assignments
  loadohqg <= 2#00.0#;
end znei;

use std.reflection.all;

entity eife is
  port (iigid : buffer real; vq : inout record_subtype_mirror; jlkwvnh : out integer);
end eife;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture r of eife is
  signal dbqqaqn : time;
  shared variable qkvn : physical_subtype_mirror;
  signal yei : integer_vector(0 to 3);
  signal xlvjnqn : std_logic_vector(4 to 0);
  signal siue : time;
  shared variable iuscznsjyn : physical_subtype_mirror;
  signal utcyd : real;
begin
  ntfo : entity work.fox
    port map (okqhhx => utcyd, vlk => iuscznsjyn, tmef => siue);
  ohgrkpem : entity work.mpkfoazk
    port map (iibku => xlvjnqn, e => yei);
  ark : entity work.fox
    port map (okqhhx => utcyd, vlk => qkvn, tmef => dbqqaqn);
  
  -- Single-driven assignments
  jlkwvnh <= jlkwvnh;
  
  -- Multi-driven assignments
  xlvjnqn <= xlvjnqn;
  xlvjnqn <= xlvjnqn;
  xlvjnqn <= xlvjnqn;
end r;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity gvpnrv is
  port ( jmurd : in boolean_vector(2 downto 3)
  ; dogpgqyv : inout record_value_mirror
  ; oq : inout enumeration_subtype_mirror
  ; dckndoae : buffer std_logic_vector(1 to 0)
  );
end gvpnrv;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture c of gvpnrv is
  signal qfewzvhsx : integer_vector(0 to 3);
  signal thhpsya : std_logic_vector(4 to 0);
  signal f : time;
  shared variable gjgavokll : physical_subtype_mirror;
  signal unxlcrbx : time;
  shared variable xoxqdxhpv : physical_subtype_mirror;
  signal tfcfqrp : time;
  shared variable iwxeieaf : physical_subtype_mirror;
  signal ritrrhznx : real;
begin
  rhpcbc : entity work.fox
    port map (okqhhx => ritrrhznx, vlk => iwxeieaf, tmef => tfcfqrp);
  jjxmuoljyi : entity work.fox
    port map (okqhhx => ritrrhznx, vlk => xoxqdxhpv, tmef => unxlcrbx);
  qlosgbi : entity work.fox
    port map (okqhhx => ritrrhznx, vlk => gjgavokll, tmef => f);
  gmshfri : entity work.mpkfoazk
    port map (iibku => thhpsya, e => qfewzvhsx);
  
  -- Single-driven assignments
  ritrrhznx <= ritrrhznx;
  
  -- Multi-driven assignments
  dckndoae <= dckndoae;
end c;



-- Seed after: 1377360603608764390,7726014785203345639
