-- Seed: 9905493323330196894,2983771601630957889

use std.reflection.all;

entity fzpibiq is
  port (ghnewk : out integer_vector(3 downto 3); rccoov : linkage time; variable uppcjlt : inout integer_subtype_mirror_pt; inihejv : out time);
end fzpibiq;

architecture px of fzpibiq is
  
begin
  -- Single-driven assignments
  inihejv <= inihejv;
  ghnewk <= (others => 8#7_5_5_4#);
end px;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity kgaqab is
  port (variable ncxpstympr : inout floating_subtype_mirror_pt; yewpftosdd : in std_logic_vector(4 to 4); r : in std_logic);
end kgaqab;

use std.reflection.all;

architecture qp of kgaqab is
  signal z : time;
  shared variable ekzhtg : integer_subtype_mirror_pt;
  signal m : time;
  signal etowvauhrl : integer_vector(3 downto 3);
begin
  iuil : entity work.fzpibiq
    port map (ghnewk => etowvauhrl, rccoov => m, uppcjlt => ekzhtg, inihejv => z);
end qp;

use std.reflection.all;

entity nltzag is
  port (variable idcmg : inout subtype_mirror_pt; pxhfew : inout real_vector(1 downto 0); variable gv : inout access_value_mirror_pt);
end nltzag;

use std.reflection.all;

architecture wjirgu of nltzag is
  signal rbuzjr : time;
  shared variable rkzepe : integer_subtype_mirror_pt;
  signal aipg : time;
  signal bgapqs : integer_vector(3 downto 3);
begin
  pztdow : entity work.fzpibiq
    port map (ghnewk => bgapqs, rccoov => aipg, uppcjlt => rkzepe, inihejv => rbuzjr);
  
  -- Single-driven assignments
  pxhfew <= pxhfew;
end wjirgu;



-- Seed after: 10436911704818834271,2983771601630957889
