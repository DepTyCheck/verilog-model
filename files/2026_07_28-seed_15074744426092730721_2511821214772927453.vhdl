-- Seed: 15074744426092730721,2511821214772927453

library ieee;
use ieee.std_logic_1164.all;

entity lzcia is
  port (cidv : linkage std_logic; aanpqdhut : linkage integer_vector(3 to 1));
end lzcia;

architecture wtnpqwdgun of lzcia is
  
begin
  
end wtnpqwdgun;

library ieee;
use ieee.std_logic_1164.all;

entity xoqntz is
  port (ojrvp : linkage bit_vector(3 downto 1); azpjznqq : in std_logic_vector(1 downto 4));
end xoqntz;

library ieee;
use ieee.std_logic_1164.all;

architecture vz of xoqntz is
  signal o : integer_vector(3 to 1);
  signal hvsjnbmstj : integer_vector(3 to 1);
  signal dc : std_logic;
  signal gho : integer_vector(3 to 1);
  signal paca : std_logic;
begin
  zonrs : entity work.lzcia
    port map (cidv => paca, aanpqdhut => gho);
  vtrzeyg : entity work.lzcia
    port map (cidv => dc, aanpqdhut => hvsjnbmstj);
  wjx : entity work.lzcia
    port map (cidv => paca, aanpqdhut => o);
  
  -- Multi-driven assignments
  paca <= paca;
  dc <= paca;
  paca <= paca;
  paca <= paca;
end vz;



-- Seed after: 4777911148053217311,2511821214772927453
