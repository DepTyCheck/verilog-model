-- Seed: 6449977411290714365,10871023049702252113

entity wkw is
  port (ctkungkiai : linkage real; pygs : buffer time; kml : buffer time);
end wkw;

architecture daumukfd of wkw is
  
begin
  -- Single-driven assignments
  pygs <= pygs;
  kml <= kml;
end daumukfd;

entity rbc is
  port (msqgpniu : buffer integer; hibjt : inout real);
end rbc;

architecture nfuudmavg of rbc is
  signal ztsglzbe : time;
  signal ejj : time;
  signal ebwex : real;
  signal bnogbl : time;
  signal kardvd : time;
  signal syohlo : real;
  signal witk : time;
  signal avc : time;
begin
  qrfrpnz : entity work.wkw
    port map (ctkungkiai => hibjt, pygs => avc, kml => witk);
  gl : entity work.wkw
    port map (ctkungkiai => syohlo, pygs => kardvd, kml => bnogbl);
  evvccs : entity work.wkw
    port map (ctkungkiai => ebwex, pygs => ejj, kml => ztsglzbe);
  
  -- Single-driven assignments
  msqgpniu <= msqgpniu;
end nfuudmavg;

entity kdrfyixl is
  port (xbyamawgd : buffer boolean);
end kdrfyixl;

architecture tzdhxa of kdrfyixl is
  signal lmwiaa : time;
  signal xpsrhhk : time;
  signal vczlek : real;
  signal bs : time;
  signal q : time;
  signal oixphqk : real;
begin
  t : entity work.wkw
    port map (ctkungkiai => oixphqk, pygs => q, kml => bs);
  etsgql : entity work.wkw
    port map (ctkungkiai => vczlek, pygs => xpsrhhk, kml => lmwiaa);
  
  -- Single-driven assignments
  xbyamawgd <= TRUE;
end tzdhxa;



-- Seed after: 6399980822843617233,10871023049702252113
