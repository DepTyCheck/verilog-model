-- Seed: 16660844538617298782,13592003931158285879

entity c is
  port (pd : buffer real);
end c;

architecture ydtbh of c is
  
begin
  -- Single-driven assignments
  pd <= pd;
end ydtbh;

entity unqddab is
  port (ewkik : inout bit_vector(4 to 1); f : out time; kuzgzgn : inout string(5 downto 3); rpm : in time);
end unqddab;

architecture rjptp of unqddab is
  signal ktw : real;
  signal qq : real;
  signal gukuw : real;
  signal vtapfptg : real;
begin
  dxsxlx : entity work.c
    port map (pd => vtapfptg);
  pqfbnpq : entity work.c
    port map (pd => gukuw);
  cb : entity work.c
    port map (pd => qq);
  ijpotm : entity work.c
    port map (pd => ktw);
  
  -- Single-driven assignments
  ewkik <= (others => '0');
  kuzgzgn <= kuzgzgn;
  f <= 2#1_0_1_1# ms;
end rjptp;

library ieee;
use ieee.std_logic_1164.all;

entity xp is
  port (zmeyqcsx : linkage std_logic_vector(0 downto 3); zqrvhmmzh : in std_logic_vector(4 to 1));
end xp;

architecture zrjhd of xp is
  
begin
  
end zrjhd;



-- Seed after: 9976845132913956524,13592003931158285879
