-- Seed: 3169013888862612575,12431929723978769775

library ieee;
use ieee.std_logic_1164.all;

entity uwvbodves is
  port (u : inout std_logic_vector(4 to 1); lmaoyohuvw : buffer std_logic_vector(1 to 1); duy : linkage time_vector(1 to 2));
end uwvbodves;



architecture ykc of uwvbodves is
  
begin
  
end ykc;



entity z is
  port (diqj : linkage bit; y : linkage character);
end z;

library ieee;
use ieee.std_logic_1164.all;

architecture jyublt of z is
  signal eiwnkhhnb : std_logic_vector(4 to 1);
  signal myraphdj : time_vector(1 to 2);
  signal dxsv : time_vector(1 to 2);
  signal zrkotgbx : std_logic_vector(1 to 1);
  signal jfimpl : std_logic_vector(4 to 1);
begin
  tltmwrz : entity work.uwvbodves
    port map (u => jfimpl, lmaoyohuvw => zrkotgbx, duy => dxsv);
  u : entity work.uwvbodves
    port map (u => jfimpl, lmaoyohuvw => zrkotgbx, duy => myraphdj);
  s : entity work.uwvbodves
    port map (u => eiwnkhhnb, lmaoyohuvw => zrkotgbx, duy => dxsv);
end jyublt;

library ieee;
use ieee.std_logic_1164.all;

entity hltms is
  port (q : out std_logic_vector(1 to 1); pkgrok : buffer real; vhxqezitl : inout bit);
end hltms;

library ieee;
use ieee.std_logic_1164.all;

architecture i of hltms is
  signal veotfrdtue : time_vector(1 to 2);
  signal awlka : time_vector(1 to 2);
  signal craamto : std_logic_vector(4 to 1);
begin
  ygilo : entity work.uwvbodves
    port map (u => craamto, lmaoyohuvw => q, duy => awlka);
  nzfkvfvmqj : entity work.uwvbodves
    port map (u => craamto, lmaoyohuvw => q, duy => awlka);
  atp : entity work.uwvbodves
    port map (u => craamto, lmaoyohuvw => q, duy => veotfrdtue);
end i;



-- Seed after: 2157119711783633879,12431929723978769775
