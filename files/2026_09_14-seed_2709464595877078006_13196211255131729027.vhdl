-- Seed: 2709464595877078006,13196211255131729027

entity ufnqz is
  port (evspwseau : out time; tceedh : out string(5 to 4); kb : inout boolean_vector(3 to 1));
end ufnqz;

architecture tezakxxly of ufnqz is
  
begin
  -- Single-driven assignments
  tceedh <= tceedh;
  kb <= kb;
end tezakxxly;

library ieee;
use ieee.std_logic_1164.all;

entity kcjqcr is
  port (qsw : buffer std_logic_vector(1 downto 4); gvfltyhh : out std_logic_vector(2 to 0); xjjeyjarrs : linkage std_logic);
end kcjqcr;

architecture bcbjsxai of kcjqcr is
  signal hkszhye : boolean_vector(3 to 1);
  signal hdcftmoun : string(5 to 4);
  signal xsvz : time;
begin
  uouegu : entity work.ufnqz
    port map (evspwseau => xsvz, tceedh => hdcftmoun, kb => hkszhye);
  
  -- Multi-driven assignments
  gvfltyhh <= "";
  qsw <= (others => '0');
  gvfltyhh <= "";
  gvfltyhh <= qsw;
end bcbjsxai;

library ieee;
use ieee.std_logic_1164.all;

entity dv is
  port (sof : in bit_vector(3 to 0); jqv : in std_logic_vector(4 downto 3); pmmtyz : out std_logic);
end dv;

architecture pa of dv is
  signal pjwtwbkl : boolean_vector(3 to 1);
  signal quu : string(5 to 4);
  signal ggjecg : time;
  signal ndjpsed : boolean_vector(3 to 1);
  signal hqgq : string(5 to 4);
  signal yc : time;
  signal edwark : boolean_vector(3 to 1);
  signal tovl : string(5 to 4);
  signal gtq : time;
begin
  zhkd : entity work.ufnqz
    port map (evspwseau => gtq, tceedh => tovl, kb => edwark);
  idpily : entity work.ufnqz
    port map (evspwseau => yc, tceedh => hqgq, kb => ndjpsed);
  unvbjaw : entity work.ufnqz
    port map (evspwseau => ggjecg, tceedh => quu, kb => pjwtwbkl);
  
  -- Multi-driven assignments
  pmmtyz <= pmmtyz;
  pmmtyz <= pmmtyz;
  pmmtyz <= 'W';
  pmmtyz <= pmmtyz;
end pa;



-- Seed after: 722680202700739106,13196211255131729027
