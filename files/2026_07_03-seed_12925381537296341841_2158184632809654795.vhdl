-- Seed: 12925381537296341841,2158184632809654795

use std.reflection.all;

entity tpv is
  port (ovvuma : inout physical_value_mirror);
end tpv;

architecture eztyjoam of tpv is
  
begin
  
end eztyjoam;

use std.reflection.all;

entity zzehqadjvw is
  port (jtdszvqx : linkage time; d : buffer time; loxykagcx : inout floating_subtype_mirror; meyrmtsi : in bit_vector(1 to 1));
end zzehqadjvw;

use std.reflection.all;

architecture geopuvqg of zzehqadjvw is
  shared variable ni : physical_value_mirror;
  shared variable ntvwuthi : physical_value_mirror;
  shared variable qt : physical_value_mirror;
  shared variable irbm : physical_value_mirror;
begin
  nwqw : entity work.tpv
    port map (ovvuma => irbm);
  kmxg : entity work.tpv
    port map (ovvuma => qt);
  qotwjdsfv : entity work.tpv
    port map (ovvuma => ntvwuthi);
  gnf : entity work.tpv
    port map (ovvuma => ni);
  
  -- Single-driven assignments
  d <= 2 min;
end geopuvqg;



-- Seed after: 8246284931361152430,2158184632809654795
