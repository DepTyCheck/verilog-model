-- Seed: 8646103522336534652,12359743974512393525

library ieee;
use ieee.std_logic_1164.all;

entity xjsdo is
  port (auqf : linkage real; t : in time; vevbj : linkage std_logic);
end xjsdo;

architecture yhs of xjsdo is
  
begin
  
end yhs;

entity qx is
  port (ubqfd : out bit_vector(2 downto 4); brrrbolo : out integer; dkagptrq : in real);
end qx;

library ieee;
use ieee.std_logic_1164.all;

architecture whzekmazzy of qx is
  signal lfc : std_logic;
  signal accjlugl : real;
  signal vtvv : real;
  signal qtrp : std_logic;
  signal u : real;
  signal tb : std_logic;
  signal sdk : time;
  signal r : real;
begin
  pj : entity work.xjsdo
    port map (auqf => r, t => sdk, vevbj => tb);
  jcmr : entity work.xjsdo
    port map (auqf => u, t => sdk, vevbj => qtrp);
  pztrilek : entity work.xjsdo
    port map (auqf => vtvv, t => sdk, vevbj => tb);
  oibpg : entity work.xjsdo
    port map (auqf => accjlugl, t => sdk, vevbj => lfc);
  
  -- Single-driven assignments
  brrrbolo <= brrrbolo;
  ubqfd <= ubqfd;
  
  -- Multi-driven assignments
  tb <= 'H';
  qtrp <= '-';
  lfc <= tb;
end whzekmazzy;



-- Seed after: 11346126210980978512,12359743974512393525
