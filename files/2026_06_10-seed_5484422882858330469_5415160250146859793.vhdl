-- Seed: 5484422882858330469,5415160250146859793



entity wfphzdnuc is
  port (iw : buffer bit_vector(2 downto 2); nbiyef : out real; pewoxhdewb : out integer);
end wfphzdnuc;



architecture oki of wfphzdnuc is
  
begin
  
end oki;

library ieee;
use ieee.std_logic_1164.all;

entity u is
  port (ocurw : inout std_logic_vector(3 downto 4); jrpn : inout std_logic_vector(1 to 1); xpphaw : in time);
end u;



architecture ehhietirwo of u is
  signal ajtvqswydg : integer;
  signal pxy : real;
  signal tlcbfdbg : bit_vector(2 downto 2);
  signal qgzfo : integer;
  signal yu : real;
  signal bbq : bit_vector(2 downto 2);
  signal gdf : integer;
  signal ouyqhx : real;
  signal tm : bit_vector(2 downto 2);
begin
  ayhap : entity work.wfphzdnuc
    port map (iw => tm, nbiyef => ouyqhx, pewoxhdewb => gdf);
  ncm : entity work.wfphzdnuc
    port map (iw => bbq, nbiyef => yu, pewoxhdewb => qgzfo);
  lmzu : entity work.wfphzdnuc
    port map (iw => tlcbfdbg, nbiyef => pxy, pewoxhdewb => ajtvqswydg);
end ehhietirwo;

library ieee;
use ieee.std_logic_1164.all;

entity uwxfot is
  port (pkozbpsuv : buffer integer; jmahy : inout real; uq : linkage std_logic; ybblnmevqj : in bit_vector(4 downto 2));
end uwxfot;



architecture eap of uwxfot is
  
begin
  
end eap;

library ieee;
use ieee.std_logic_1164.all;

entity tyajha is
  port (wxxt : inout character; oxcr : out std_logic);
end tyajha;

library ieee;
use ieee.std_logic_1164.all;

architecture qetfrob of tyajha is
  signal incc : time;
  signal kl : std_logic_vector(1 to 1);
  signal ipxfeua : std_logic_vector(3 downto 4);
  signal ldvekcfihi : integer;
  signal f : real;
  signal zx : bit_vector(2 downto 2);
  signal fcpx : integer;
  signal eysu : real;
  signal qhqylq : bit_vector(2 downto 2);
  signal dmdw : integer;
  signal agp : real;
  signal skgicyx : bit_vector(2 downto 2);
begin
  opts : entity work.wfphzdnuc
    port map (iw => skgicyx, nbiyef => agp, pewoxhdewb => dmdw);
  bvrlfbmuos : entity work.wfphzdnuc
    port map (iw => qhqylq, nbiyef => eysu, pewoxhdewb => fcpx);
  rsokqesh : entity work.wfphzdnuc
    port map (iw => zx, nbiyef => f, pewoxhdewb => ldvekcfihi);
  sqxzuzbf : entity work.u
    port map (ocurw => ipxfeua, jrpn => kl, xpphaw => incc);
end qetfrob;



-- Seed after: 7169453415723438113,5415160250146859793
