-- Seed: 7608889349042553365,4080032123900078489

entity iof is
  port (loori : inout integer);
end iof;

architecture nuxaacjpuw of iof is
  
begin
  -- Single-driven assignments
  loori <= loori;
end nuxaacjpuw;

entity oogptnwg is
  port (ufgwdcy : linkage character; hdw : linkage integer; drycb : linkage integer);
end oogptnwg;

architecture sk of oogptnwg is
  signal oac : integer;
  signal vbaiyn : integer;
begin
  hmpvkq : entity work.iof
    port map (loori => vbaiyn);
  gzssk : entity work.iof
    port map (loori => oac);
end sk;

library ieee;
use ieee.std_logic_1164.all;

entity ypnem is
  port (srv : out boolean_vector(2 downto 3); hgdg : in std_logic_vector(2 downto 3));
end ypnem;

architecture rprvbayfe of ypnem is
  signal dkedq : integer;
  signal klnwczqhxc : integer;
  signal m : integer;
  signal jw : integer;
  signal oqc : character;
  signal sd : integer;
  signal p : integer;
  signal hhja : character;
begin
  muxc : entity work.oogptnwg
    port map (ufgwdcy => hhja, hdw => p, drycb => sd);
  wmbmu : entity work.oogptnwg
    port map (ufgwdcy => oqc, hdw => jw, drycb => m);
  tzfdc : entity work.iof
    port map (loori => klnwczqhxc);
  hrb : entity work.iof
    port map (loori => dkedq);
  
  -- Single-driven assignments
  srv <= (others => TRUE);
end rprvbayfe;



-- Seed after: 10851013094588466536,4080032123900078489
