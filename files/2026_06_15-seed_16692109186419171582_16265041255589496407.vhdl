-- Seed: 16692109186419171582,16265041255589496407

library ieee;
use ieee.std_logic_1164.all;

entity eu is
  port (yp : buffer std_logic; ffwzgr : buffer character; xabgcyl : linkage std_logic_vector(4 to 4); stphk : out integer_vector(3 downto 1));
end eu;



architecture secbcqi of eu is
  
begin
  
end secbcqi;



entity chrcwit is
  port (bjeyw : linkage integer; xhoklubh : linkage integer; nixgavsy : buffer real; nhbaapwlhe : buffer time);
end chrcwit;



architecture qatcq of chrcwit is
  
begin
  
end qatcq;

library ieee;
use ieee.std_logic_1164.all;

entity i is
  port (hmkkgqxyvr : in time; klsf : buffer character; ceu : out std_logic);
end i;

library ieee;
use ieee.std_logic_1164.all;

architecture hnagjlzs of i is
  signal blakcxolb : integer_vector(3 downto 1);
  signal fg : character;
  signal epcqcea : integer_vector(3 downto 1);
  signal pvxgf : std_logic_vector(4 to 4);
  signal qndqosi : character;
  signal quqqdmwmo : std_logic;
  signal kvabky : integer_vector(3 downto 1);
  signal volwndoro : std_logic_vector(4 to 4);
  signal petnxogugt : character;
  signal fom : std_logic;
begin
  nooct : entity work.eu
    port map (yp => fom, ffwzgr => petnxogugt, xabgcyl => volwndoro, stphk => kvabky);
  jmqmz : entity work.eu
    port map (yp => quqqdmwmo, ffwzgr => qndqosi, xabgcyl => pvxgf, stphk => epcqcea);
  csed : entity work.eu
    port map (yp => fom, ffwzgr => fg, xabgcyl => volwndoro, stphk => blakcxolb);
end hnagjlzs;



entity a is
  port (uiycco : in real; lvy : buffer boolean);
end a;

library ieee;
use ieee.std_logic_1164.all;

architecture olualnptqw of a is
  signal snzeszn : integer_vector(3 downto 1);
  signal tdusahdy : std_logic_vector(4 to 4);
  signal hgssow : character;
  signal dhi : std_logic;
  signal xo : time;
  signal ahw : real;
  signal wzucsp : integer;
  signal ubvupf : integer_vector(3 downto 1);
  signal cfqs : character;
  signal w : std_logic;
  signal yhekestl : integer_vector(3 downto 1);
  signal crwrwc : std_logic_vector(4 to 4);
  signal z : character;
  signal s : std_logic;
begin
  yojrljemqu : entity work.eu
    port map (yp => s, ffwzgr => z, xabgcyl => crwrwc, stphk => yhekestl);
  bstokll : entity work.eu
    port map (yp => w, ffwzgr => cfqs, xabgcyl => crwrwc, stphk => ubvupf);
  tlmts : entity work.chrcwit
    port map (bjeyw => wzucsp, xhoklubh => wzucsp, nixgavsy => ahw, nhbaapwlhe => xo);
  fepisj : entity work.eu
    port map (yp => dhi, ffwzgr => hgssow, xabgcyl => tdusahdy, stphk => snzeszn);
end olualnptqw;



-- Seed after: 10349880865814222102,16265041255589496407
