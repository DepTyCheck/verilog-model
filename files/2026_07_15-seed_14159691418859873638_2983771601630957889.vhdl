-- Seed: 14159691418859873638,2983771601630957889

use std.reflection.all;

entity dbirxxohhq is
  port (variable jp : inout access_value_mirror_pt);
end dbirxxohhq;

architecture pdbwjhbf of dbirxxohhq is
  
begin
  
end pdbwjhbf;

use std.reflection.all;

entity wau is
  port (variable rp : inout enumeration_value_mirror_pt);
end wau;

use std.reflection.all;

architecture rypspssdm of wau is
  shared variable tkr : access_value_mirror_pt;
  shared variable zbos : access_value_mirror_pt;
  shared variable q : access_value_mirror_pt;
  shared variable capvuth : access_value_mirror_pt;
begin
  umq : entity work.dbirxxohhq
    port map (jp => capvuth);
  yhqyjhyxap : entity work.dbirxxohhq
    port map (jp => q);
  frgjwjul : entity work.dbirxxohhq
    port map (jp => zbos);
  cryg : entity work.dbirxxohhq
    port map (jp => tkr);
end rypspssdm;

use std.reflection.all;

entity ewgmo is
  port (pobayhexut : inout severity_level; oamymid : out time; variable l : inout enumeration_value_mirror_pt);
end ewgmo;

use std.reflection.all;

architecture grloj of ewgmo is
  shared variable stmiuq : access_value_mirror_pt;
  shared variable sh : access_value_mirror_pt;
begin
  svuylqvy : entity work.dbirxxohhq
    port map (jp => sh);
  yh : entity work.dbirxxohhq
    port map (jp => stmiuq);
  
  -- Single-driven assignments
  oamymid <= 1_3 ns;
  pobayhexut <= pobayhexut;
end grloj;



-- Seed after: 18097523783090787299,2983771601630957889
