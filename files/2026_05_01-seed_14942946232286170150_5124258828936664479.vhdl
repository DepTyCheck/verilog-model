-- Seed: 14942946232286170150,5124258828936664479

library ieee;
use ieee.std_logic_1164.all;

entity tbwzivdila is
  port (dnk : inout real; utg : inout time; sj : buffer bit; nfllgcdzx : buffer std_logic);
end tbwzivdila;



architecture peadlqoybo of tbwzivdila is
  
begin
  
end peadlqoybo;



entity vtiqrfosyz is
  port (b : out bit; jpqtynewp : linkage integer; vxsphx : in integer; zzjbwner : inout severity_level);
end vtiqrfosyz;

library ieee;
use ieee.std_logic_1164.all;

architecture i of vtiqrfosyz is
  signal smuyyc : std_logic;
  signal djzpobsd : bit;
  signal fzfco : time;
  signal dxem : real;
  signal ocfwnlwrwr : std_logic;
  signal vhlekvz : time;
  signal tebkle : real;
  signal azsezngqn : bit;
  signal sv : time;
  signal gj : real;
  signal ofe : std_logic;
  signal e : bit;
  signal cpp : time;
  signal bxo : real;
begin
  obroggtbh : entity work.tbwzivdila
    port map (dnk => bxo, utg => cpp, sj => e, nfllgcdzx => ofe);
  hwq : entity work.tbwzivdila
    port map (dnk => gj, utg => sv, sj => azsezngqn, nfllgcdzx => ofe);
  bb : entity work.tbwzivdila
    port map (dnk => tebkle, utg => vhlekvz, sj => b, nfllgcdzx => ocfwnlwrwr);
  fllu : entity work.tbwzivdila
    port map (dnk => dxem, utg => fzfco, sj => djzpobsd, nfllgcdzx => smuyyc);
end i;

library ieee;
use ieee.std_logic_1164.all;

entity ao is
  port (pirkassnc : in std_logic);
end ao;

library ieee;
use ieee.std_logic_1164.all;

architecture r of ao is
  signal srv : std_logic;
  signal e : bit;
  signal xtvkc : time;
  signal bedumuveg : real;
  signal stbglzcvc : bit;
  signal z : time;
  signal bkxl : real;
  signal fspa : std_logic;
  signal brwxfcsf : bit;
  signal otp : time;
  signal pdeyggp : real;
begin
  btzavfhe : entity work.tbwzivdila
    port map (dnk => pdeyggp, utg => otp, sj => brwxfcsf, nfllgcdzx => fspa);
  utibenkyvs : entity work.tbwzivdila
    port map (dnk => bkxl, utg => z, sj => stbglzcvc, nfllgcdzx => fspa);
  kuywug : entity work.tbwzivdila
    port map (dnk => bedumuveg, utg => xtvkc, sj => e, nfllgcdzx => srv);
end r;



-- Seed after: 13529260701781048201,5124258828936664479
