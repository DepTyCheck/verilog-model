-- Seed: 8556537254729753061,12143220691580258643

entity vcttixtaoc is
  port (gnfco : in integer_vector(0 to 3); d : buffer string(1 downto 2));
end vcttixtaoc;

architecture o of vcttixtaoc is
  
begin
  -- Single-driven assignments
  d <= d;
end o;

entity bjpnty is
  port (zmoh : inout severity_level);
end bjpnty;

architecture jm of bjpnty is
  signal otwtpzpt : string(1 downto 2);
  signal qe : integer_vector(0 to 3);
  signal l : string(1 downto 2);
  signal sqnegoqc : integer_vector(0 to 3);
begin
  woxehjc : entity work.vcttixtaoc
    port map (gnfco => sqnegoqc, d => l);
  nxhbaj : entity work.vcttixtaoc
    port map (gnfco => qe, d => otwtpzpt);
  
  -- Single-driven assignments
  zmoh <= FAILURE;
  sqnegoqc <= sqnegoqc;
end jm;



-- Seed after: 15304442737611118764,12143220691580258643
