-- Seed: 9334586608190111147,14652815260262078753

library ieee;
use ieee.std_logic_1164.all;

entity saj is
  port (mru : inout severity_level; qhx : inout std_logic_vector(0 downto 1); gh : out std_logic);
end saj;

architecture rrmqmrlu of saj is
  
begin
  -- Single-driven assignments
  mru <= ERROR;
end rrmqmrlu;

entity vssbw is
  port (ea : out real; nsov : inout integer);
end vssbw;

library ieee;
use ieee.std_logic_1164.all;

architecture oz of vssbw is
  signal k : std_logic;
  signal xopusmfqux : std_logic_vector(0 downto 1);
  signal si : severity_level;
  signal yav : std_logic;
  signal utwqoer : std_logic_vector(0 downto 1);
  signal ibwpt : severity_level;
  signal hshh : std_logic;
  signal ftquuzgpve : std_logic_vector(0 downto 1);
  signal drt : severity_level;
begin
  jvji : entity work.saj
    port map (mru => drt, qhx => ftquuzgpve, gh => hshh);
  vgsycpafp : entity work.saj
    port map (mru => ibwpt, qhx => utwqoer, gh => yav);
  fqj : entity work.saj
    port map (mru => si, qhx => xopusmfqux, gh => k);
  
  -- Multi-driven assignments
  ftquuzgpve <= (others => '0');
  k <= 'X';
  ftquuzgpve <= "";
  xopusmfqux <= "";
end oz;

library ieee;
use ieee.std_logic_1164.all;

entity sylvwxpffm is
  port (jtbkj : inout integer; x : inout real; algewnshx : linkage bit; bvjbz : linkage std_logic_vector(1 to 0));
end sylvwxpffm;

library ieee;
use ieee.std_logic_1164.all;

architecture jgw of sylvwxpffm is
  signal xbobhi : std_logic;
  signal yyqtzhxzm : std_logic_vector(0 downto 1);
  signal jkupxbiv : severity_level;
begin
  alx : entity work.saj
    port map (mru => jkupxbiv, qhx => yyqtzhxzm, gh => xbobhi);
  
  -- Single-driven assignments
  x <= 2#0_1_1_0_1.1#;
  jtbkj <= 2#00#;
  
  -- Multi-driven assignments
  yyqtzhxzm <= "";
  xbobhi <= 'H';
  xbobhi <= '0';
end jgw;

entity xlwgjo is
  port (b : out real; vsfe : in character; szucmm : in real);
end xlwgjo;

library ieee;
use ieee.std_logic_1164.all;

architecture suuk of xlwgjo is
  signal dzn : std_logic_vector(0 downto 1);
  signal p : severity_level;
  signal srltl : std_logic;
  signal g : std_logic_vector(0 downto 1);
  signal nkngiydtc : severity_level;
  signal rghywah : bit;
  signal iz : integer;
  signal evbyrqj : std_logic;
  signal tqcapz : std_logic_vector(1 to 0);
  signal pdaw : severity_level;
begin
  ug : entity work.saj
    port map (mru => pdaw, qhx => tqcapz, gh => evbyrqj);
  kyepmxwvoi : entity work.sylvwxpffm
    port map (jtbkj => iz, x => b, algewnshx => rghywah, bvjbz => tqcapz);
  tczt : entity work.saj
    port map (mru => nkngiydtc, qhx => g, gh => srltl);
  qypjllhcol : entity work.saj
    port map (mru => p, qhx => dzn, gh => evbyrqj);
  
  -- Multi-driven assignments
  tqcapz <= (others => '0');
  srltl <= '1';
  srltl <= 'X';
end suuk;



-- Seed after: 5156835408847299426,14652815260262078753
