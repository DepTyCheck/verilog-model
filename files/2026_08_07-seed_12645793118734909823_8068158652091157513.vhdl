-- Seed: 12645793118734909823,8068158652091157513

entity nqxooahav is
  port (x : linkage integer; kvimae : in integer_vector(1 to 3); bgswv : linkage real; oqfu : in string(4 to 3));
end nqxooahav;

architecture ldggh of nqxooahav is
  
begin
  
end ldggh;

library ieee;
use ieee.std_logic_1164.all;

entity gj is
  port (pwnfgfy : in time; xfuvryqd : in std_logic; abzgtpn : buffer real; bmbycciqyo : out time_vector(3 to 4));
end gj;

architecture onvgksf of gj is
  signal nntdapfm : string(4 to 3);
  signal jp : real;
  signal jxxsf : integer_vector(1 to 3);
  signal baaa : integer;
begin
  ceelopcen : entity work.nqxooahav
    port map (x => baaa, kvimae => jxxsf, bgswv => jp, oqfu => nntdapfm);
  
  -- Single-driven assignments
  bmbycciqyo <= bmbycciqyo;
  jxxsf <= (2_1_4_4_0, 1_2_0_3, 1_3_4_3_1);
  abzgtpn <= abzgtpn;
  nntdapfm <= "";
end onvgksf;

library ieee;
use ieee.std_logic_1164.all;

entity q is
  port (nifnd : inout time_vector(3 downto 4); hqoz : inout character; haba : buffer std_logic; t : out integer);
end q;

library ieee;
use ieee.std_logic_1164.all;

architecture dudd of q is
  signal zfm : string(4 to 3);
  signal a : real;
  signal yuj : real;
  signal domzgc : integer;
  signal amg : time_vector(3 to 4);
  signal svzofn : real;
  signal aajoauor : std_logic;
  signal vg : time;
  signal emewzaf : string(4 to 3);
  signal c : real;
  signal e : integer_vector(1 to 3);
  signal xwkpi : integer;
begin
  mgg : entity work.nqxooahav
    port map (x => xwkpi, kvimae => e, bgswv => c, oqfu => emewzaf);
  nwrendm : entity work.gj
    port map (pwnfgfy => vg, xfuvryqd => aajoauor, abzgtpn => svzofn, bmbycciqyo => amg);
  tyuahlczdd : entity work.nqxooahav
    port map (x => domzgc, kvimae => e, bgswv => yuj, oqfu => emewzaf);
  vv : entity work.nqxooahav
    port map (x => t, kvimae => e, bgswv => a, oqfu => zfm);
  
  -- Single-driven assignments
  hqoz <= 'c';
  e <= e;
end dudd;



-- Seed after: 4212626213574317574,8068158652091157513
