-- Seed: 13869741229651143393,7726014785203345639

use std.reflection.all;

entity mftyovkfaf is
  port (qrguzzy : inout file_subtype_mirror; qjagnwico : inout real; dhirknhs : in time);
end mftyovkfaf;

architecture ygplbj of mftyovkfaf is
  
begin
  -- Single-driven assignments
  qjagnwico <= 16#2_5_7_6_0.99C#;
end ygplbj;

use std.reflection.all;

entity cdsrjcsfft is
  port (xkbacs : in time; nsttsd : inout array_value_mirror);
end cdsrjcsfft;

use std.reflection.all;

architecture ptadf of cdsrjcsfft is
  signal oqtpvaxv : real;
  shared variable xjtjzhk : file_subtype_mirror;
  signal ijmemmpynk : time;
  signal upqq : real;
  shared variable dphphnqu : file_subtype_mirror;
  signal xsp : real;
  shared variable nuwpuzuon : file_subtype_mirror;
begin
  ujkzxycdwh : entity work.mftyovkfaf
    port map (qrguzzy => nuwpuzuon, qjagnwico => xsp, dhirknhs => xkbacs);
  uwdqatqnyt : entity work.mftyovkfaf
    port map (qrguzzy => dphphnqu, qjagnwico => upqq, dhirknhs => ijmemmpynk);
  ynyblrtads : entity work.mftyovkfaf
    port map (qrguzzy => xjtjzhk, qjagnwico => oqtpvaxv, dhirknhs => ijmemmpynk);
  
  -- Single-driven assignments
  ijmemmpynk <= xkbacs;
end ptadf;

use std.reflection.all;

entity zvmhllr is
  port (mvll : inout array_subtype_mirror);
end zvmhllr;

use std.reflection.all;

architecture ks of zvmhllr is
  signal pin : time;
  signal wmqeb : real;
  shared variable xeez : file_subtype_mirror;
  signal czop : real;
  shared variable abaaefngj : file_subtype_mirror;
  signal tokea : time;
  signal kozhzooa : real;
  shared variable der : file_subtype_mirror;
  signal keltr : time;
  signal djvmxwommo : real;
  shared variable emwiy : file_subtype_mirror;
begin
  ag : entity work.mftyovkfaf
    port map (qrguzzy => emwiy, qjagnwico => djvmxwommo, dhirknhs => keltr);
  uawf : entity work.mftyovkfaf
    port map (qrguzzy => der, qjagnwico => kozhzooa, dhirknhs => tokea);
  deozwzgztc : entity work.mftyovkfaf
    port map (qrguzzy => abaaefngj, qjagnwico => czop, dhirknhs => keltr);
  jlmxj : entity work.mftyovkfaf
    port map (qrguzzy => xeez, qjagnwico => wmqeb, dhirknhs => pin);
  
  -- Single-driven assignments
  keltr <= 3_0 ps;
end ks;



-- Seed after: 11201475083716885890,7726014785203345639
