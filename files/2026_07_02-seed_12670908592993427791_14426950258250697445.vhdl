-- Seed: 12670908592993427791,14426950258250697445

entity nf is
  port (l : linkage integer; mtpxxmpa : inout severity_level);
end nf;

architecture u of nf is
  
begin
  -- Single-driven assignments
  mtpxxmpa <= FAILURE;
end u;

use std.reflection.all;

entity ezjwvpdcyu is
  port (iiwhtb : inout array_value_mirror);
end ezjwvpdcyu;

architecture irpeubm of ezjwvpdcyu is
  signal h : severity_level;
  signal bk : integer;
  signal jrjsef : severity_level;
  signal gyqxcp : integer;
begin
  qbmjz : entity work.nf
    port map (l => gyqxcp, mtpxxmpa => jrjsef);
  hlxr : entity work.nf
    port map (l => bk, mtpxxmpa => h);
end irpeubm;

use std.reflection.all;

entity pawq is
  port (nnsfv : inout real; sxype : inout array_subtype_mirror; hsvb : inout file_value_mirror);
end pawq;

architecture avntgkfm of pawq is
  signal uhvbbxrlvn : severity_level;
  signal lcixdxk : integer;
  signal daumqhi : severity_level;
  signal zcjej : integer;
  signal xzi : severity_level;
  signal kcn : integer;
begin
  u : entity work.nf
    port map (l => kcn, mtpxxmpa => xzi);
  dcbcjuaw : entity work.nf
    port map (l => zcjej, mtpxxmpa => daumqhi);
  xkji : entity work.nf
    port map (l => lcixdxk, mtpxxmpa => uhvbbxrlvn);
end avntgkfm;

entity vxrgjhhla is
  port (dhkdcuml : in boolean);
end vxrgjhhla;

architecture ysjcb of vxrgjhhla is
  signal uq : severity_level;
  signal ldkwixbhbe : integer;
begin
  ygugbssihl : entity work.nf
    port map (l => ldkwixbhbe, mtpxxmpa => uq);
end ysjcb;



-- Seed after: 10807071877795486297,14426950258250697445
