-- Seed: 17401612634901166372,7726014785203345639

use std.reflection.all;

entity nsewndlfr is
  port ( wcl : inout access_subtype_mirror
  ; vvadjxzh : inout value_mirror
  ; wrwntcnqcw : out bit_vector(2 downto 3)
  ; mxioe : inout floating_subtype_mirror
  );
end nsewndlfr;

architecture lrcmmxvmhs of nsewndlfr is
  
begin
  -- Single-driven assignments
  wrwntcnqcw <= wrwntcnqcw;
end lrcmmxvmhs;

library ieee;
use ieee.std_logic_1164.all;

entity caqm is
  port (istiiwoc : buffer integer; qa : in real; f : buffer std_logic_vector(4 to 1));
end caqm;

use std.reflection.all;

architecture iileap of caqm is
  shared variable aqankxc : floating_subtype_mirror;
  signal gjqqq : bit_vector(2 downto 3);
  shared variable tdtavgqab : value_mirror;
  shared variable dgy : access_subtype_mirror;
  shared variable r : floating_subtype_mirror;
  signal ura : bit_vector(2 downto 3);
  shared variable gtzlm : value_mirror;
  shared variable y : access_subtype_mirror;
  shared variable vcwkmj : floating_subtype_mirror;
  signal kux : bit_vector(2 downto 3);
  shared variable qs : value_mirror;
  shared variable zprfwknkco : access_subtype_mirror;
begin
  pgtvmqqv : entity work.nsewndlfr
    port map (wcl => zprfwknkco, vvadjxzh => qs, wrwntcnqcw => kux, mxioe => vcwkmj);
  vbskis : entity work.nsewndlfr
    port map (wcl => y, vvadjxzh => gtzlm, wrwntcnqcw => ura, mxioe => r);
  blrubzvy : entity work.nsewndlfr
    port map (wcl => dgy, vvadjxzh => tdtavgqab, wrwntcnqcw => gjqqq, mxioe => aqankxc);
  
  -- Single-driven assignments
  istiiwoc <= 4;
end iileap;



-- Seed after: 10766236730614178949,7726014785203345639
