-- Seed: 14512095216737959280,14094562573555574003

entity asqf is
  port (y : out boolean_vector(3 to 4));
end asqf;

architecture fapweth of asqf is
  
begin
  -- Single-driven assignments
  y <= (FALSE, FALSE);
end fapweth;

library ieee;
use ieee.std_logic_1164.all;

entity grhzac is
  port (snakjnvrna : inout std_logic; ujov : linkage std_logic_vector(0 downto 0); snlg : linkage bit; vpmldmae : buffer bit);
end grhzac;

architecture mnilkp of grhzac is
  signal rywnx : boolean_vector(3 to 4);
  signal aufka : boolean_vector(3 to 4);
  signal wkgw : boolean_vector(3 to 4);
  signal vnfty : boolean_vector(3 to 4);
begin
  avmrqfk : entity work.asqf
    port map (y => vnfty);
  qlpploaccx : entity work.asqf
    port map (y => wkgw);
  tipl : entity work.asqf
    port map (y => aufka);
  ym : entity work.asqf
    port map (y => rywnx);
  
  -- Single-driven assignments
  vpmldmae <= '0';
end mnilkp;

library ieee;
use ieee.std_logic_1164.all;

entity zfhdpncn is
  port (knwskpd : inout std_logic);
end zfhdpncn;

architecture hbjayimrb of zfhdpncn is
  signal shwhr : boolean_vector(3 to 4);
begin
  f : entity work.asqf
    port map (y => shwhr);
  
  -- Multi-driven assignments
  knwskpd <= knwskpd;
end hbjayimrb;



-- Seed after: 12683289044551130122,14094562573555574003
