-- Seed: 15535306501743202270,14629254427735353553

entity x is
  port (qerdrkph : in integer; ggd : inout integer; yelfq : linkage bit; speqxbx : linkage real);
end x;

architecture nertg of x is
  
begin
  
end nertg;

library ieee;
use ieee.std_logic_1164.all;

entity vcjh is
  port (xcngez : out string(5 to 1); wtfbb : inout std_logic_vector(2 to 1); mxsnwoz : buffer severity_level; vqsuybhxo : in std_logic);
end vcjh;

architecture sqhlbo of vcjh is
  signal yyxbkpau : real;
  signal kkfemhgq : bit;
  signal ehpcnhmr : integer;
  signal nhf : real;
  signal v : bit;
  signal idasiswa : integer;
  signal ergkfaoms : integer;
  signal abvpjs : real;
  signal cal : bit;
  signal i : integer;
  signal kpakcje : real;
  signal gsinybaol : bit;
  signal g : integer;
begin
  qundu : entity work.x
    port map (qerdrkph => g, ggd => g, yelfq => gsinybaol, speqxbx => kpakcje);
  qfb : entity work.x
    port map (qerdrkph => i, ggd => i, yelfq => cal, speqxbx => abvpjs);
  nq : entity work.x
    port map (qerdrkph => ergkfaoms, ggd => idasiswa, yelfq => v, speqxbx => nhf);
  vhejpkcrtd : entity work.x
    port map (qerdrkph => g, ggd => ehpcnhmr, yelfq => kkfemhgq, speqxbx => yyxbkpau);
  
  -- Multi-driven assignments
  wtfbb <= (others => '0');
  wtfbb <= "";
end sqhlbo;

library ieee;
use ieee.std_logic_1164.all;

entity myceuksgr is
  port (lbnocy : in string(4 downto 5); nba : in std_logic; e : in std_logic_vector(2 downto 2); nnfsvdeub : in integer_vector(1 downto 0));
end myceuksgr;

library ieee;
use ieee.std_logic_1164.all;

architecture rcsod of myceuksgr is
  signal nljdu : std_logic;
  signal gafjec : severity_level;
  signal kwqme : std_logic_vector(2 to 1);
  signal rfhegnnpnk : string(5 to 1);
begin
  ytdjmm : entity work.vcjh
    port map (xcngez => rfhegnnpnk, wtfbb => kwqme, mxsnwoz => gafjec, vqsuybhxo => nljdu);
  
  -- Multi-driven assignments
  kwqme <= (others => '0');
  kwqme <= "";
  kwqme <= (others => '0');
  kwqme <= "";
end rcsod;



-- Seed after: 14883288413121998231,14629254427735353553
