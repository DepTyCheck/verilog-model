-- Seed: 3177205084185806612,4245627776430562977

entity nfy is
  port (ytfyks : inout real);
end nfy;

architecture hcrsy of nfy is
  
begin
  -- Single-driven assignments
  ytfyks <= ytfyks;
end hcrsy;

library ieee;
use ieee.std_logic_1164.all;

entity vuaof is
  port (shi : in std_logic_vector(4 to 0));
end vuaof;

architecture br of vuaof is
  signal ossctlsghr : real;
begin
  qv : entity work.nfy
    port map (ytfyks => ossctlsghr);
end br;

entity gznud is
  port (i : in time; eaxxiz : out time);
end gznud;

library ieee;
use ieee.std_logic_1164.all;

architecture ueegmzsabr of gznud is
  signal wjwycirtzj : std_logic_vector(4 to 0);
  signal alhijyw : real;
  signal t : real;
begin
  wmxhpkxs : entity work.nfy
    port map (ytfyks => t);
  rqrabwa : entity work.nfy
    port map (ytfyks => alhijyw);
  xlimrqy : entity work.vuaof
    port map (shi => wjwycirtzj);
  
  -- Single-driven assignments
  eaxxiz <= 11211.20 ns;
end ueegmzsabr;

entity vh is
  port (n : linkage real);
end vh;

library ieee;
use ieee.std_logic_1164.all;

architecture v of vh is
  signal iroau : real;
  signal jigxrhkw : real;
  signal gir : real;
  signal sgfn : std_logic_vector(4 to 0);
begin
  gnu : entity work.vuaof
    port map (shi => sgfn);
  fuzqmmji : entity work.nfy
    port map (ytfyks => gir);
  rdjph : entity work.nfy
    port map (ytfyks => jigxrhkw);
  dveye : entity work.nfy
    port map (ytfyks => iroau);
  
  -- Multi-driven assignments
  sgfn <= (others => '0');
  sgfn <= sgfn;
  sgfn <= sgfn;
end v;



-- Seed after: 8134893694358696280,4245627776430562977
