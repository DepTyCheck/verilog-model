-- Seed: 5710739726604416439,14426950258250697445

use std.reflection.all;

entity kixt is
  port (zwcvt : linkage bit; bu : out boolean; coqdcgiok : inout protected_subtype_mirror);
end kixt;

architecture lzdqzneop of kixt is
  
begin
  
end lzdqzneop;

use std.reflection.all;

entity z is
  port (lcy : out boolean_vector(2 downto 3); ms : inout record_value_mirror; p : inout physical_subtype_mirror; np : inout integer_subtype_mirror);
end z;

use std.reflection.all;

architecture zvpuby of z is
  shared variable xnovkaq : protected_subtype_mirror;
  signal d : boolean;
  signal r : bit;
  shared variable t : protected_subtype_mirror;
  signal kg : boolean;
  signal ukzpll : bit;
  shared variable hkrsgjod : protected_subtype_mirror;
  signal gyfslevaf : boolean;
  signal fhxldn : bit;
  shared variable whlhccoo : protected_subtype_mirror;
  signal x : boolean;
  signal xsubcmx : bit;
begin
  kyn : entity work.kixt
    port map (zwcvt => xsubcmx, bu => x, coqdcgiok => whlhccoo);
  tyirgq : entity work.kixt
    port map (zwcvt => fhxldn, bu => gyfslevaf, coqdcgiok => hkrsgjod);
  ncmkzt : entity work.kixt
    port map (zwcvt => ukzpll, bu => kg, coqdcgiok => t);
  ddounoo : entity work.kixt
    port map (zwcvt => r, bu => d, coqdcgiok => xnovkaq);
  
  -- Single-driven assignments
  lcy <= lcy;
end zvpuby;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity twcxoc is
  port (tbz : buffer std_logic_vector(3 downto 4); ihfxtar : linkage time; kigjvk : out integer; cxjb : inout enumeration_value_mirror);
end twcxoc;

use std.reflection.all;

architecture onms of twcxoc is
  shared variable fqm : integer_subtype_mirror;
  shared variable sbxooi : physical_subtype_mirror;
  shared variable lsyqrw : record_value_mirror;
  signal enx : boolean_vector(2 downto 3);
  shared variable ibfbovh : protected_subtype_mirror;
  signal hytw : boolean;
  signal dpoidztxbq : bit;
  shared variable ax : integer_subtype_mirror;
  shared variable rhmyey : physical_subtype_mirror;
  shared variable qsuxhmv : record_value_mirror;
  signal jbduaho : boolean_vector(2 downto 3);
begin
  mlbl : entity work.z
    port map (lcy => jbduaho, ms => qsuxhmv, p => rhmyey, np => ax);
  sgo : entity work.kixt
    port map (zwcvt => dpoidztxbq, bu => hytw, coqdcgiok => ibfbovh);
  jvpy : entity work.z
    port map (lcy => enx, ms => lsyqrw, p => sbxooi, np => fqm);
  
  -- Single-driven assignments
  kigjvk <= kigjvk;
  
  -- Multi-driven assignments
  tbz <= "";
  tbz <= tbz;
end onms;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity sjwxbbpd is
  port (rzt : out std_logic_vector(3 to 2); qwuemnbavz : inout array_subtype_mirror; kenmwft : inout integer_value_mirror; y : inout file_value_mirror);
end sjwxbbpd;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture ov of sjwxbbpd is
  shared variable rcibottv : integer_subtype_mirror;
  shared variable wtuciqa : physical_subtype_mirror;
  shared variable gqmd : record_value_mirror;
  signal acgcjakd : boolean_vector(2 downto 3);
  shared variable mlnapg : enumeration_value_mirror;
  signal sdpduk : integer;
  signal wjyafvvpq : time;
  signal otq : std_logic_vector(3 downto 4);
begin
  vtvlnmknly : entity work.twcxoc
    port map (tbz => otq, ihfxtar => wjyafvvpq, kigjvk => sdpduk, cxjb => mlnapg);
  byl : entity work.z
    port map (lcy => acgcjakd, ms => gqmd, p => wtuciqa, np => rcibottv);
  
  -- Multi-driven assignments
  otq <= otq;
  rzt <= "";
  rzt <= "";
  rzt <= (others => '0');
end ov;



-- Seed after: 5801725710601043037,14426950258250697445
