-- Seed: 5959302718444281960,13592003931158285879

library ieee;
use ieee.std_logic_1164.all;

entity svw is
  port (i : out std_logic; ahd : out time; hgekt : buffer std_logic);
end svw;

architecture bscprd of svw is
  
begin
  -- Single-driven assignments
  ahd <= ahd;
  
  -- Multi-driven assignments
  hgekt <= 'H';
  hgekt <= hgekt;
end bscprd;

library ieee;
use ieee.std_logic_1164.all;

entity xmsdllaacw is
  port (jddsujgul : linkage std_logic);
end xmsdllaacw;

library ieee;
use ieee.std_logic_1164.all;

architecture qidqnbqc of xmsdllaacw is
  signal hfrlmo : time;
  signal gltm : std_logic;
  signal fj : time;
  signal rtqzjkm : std_logic;
  signal edswzeq : std_logic;
  signal zfieoke : time;
  signal xpdx : std_logic;
  signal skyud : time;
  signal ryhrhy : std_logic;
begin
  reg : entity work.svw
    port map (i => ryhrhy, ahd => skyud, hgekt => ryhrhy);
  hycdil : entity work.svw
    port map (i => xpdx, ahd => zfieoke, hgekt => edswzeq);
  xpx : entity work.svw
    port map (i => rtqzjkm, ahd => fj, hgekt => rtqzjkm);
  vai : entity work.svw
    port map (i => gltm, ahd => hfrlmo, hgekt => rtqzjkm);
  
  -- Multi-driven assignments
  ryhrhy <= ryhrhy;
  edswzeq <= '0';
end qidqnbqc;

entity axtmzf is
  port (qurlx : linkage real; azsuzbk : buffer character);
end axtmzf;

library ieee;
use ieee.std_logic_1164.all;

architecture gjrmfabjbn of axtmzf is
  signal mvubmqh : std_logic;
  signal jwzjbv : time;
  signal wwbncxqqng : std_logic;
  signal guaqyrrnb : time;
  signal noopxuie : std_logic;
begin
  qzjcj : entity work.svw
    port map (i => noopxuie, ahd => guaqyrrnb, hgekt => wwbncxqqng);
  nsoftfip : entity work.svw
    port map (i => noopxuie, ahd => jwzjbv, hgekt => mvubmqh);
  
  -- Single-driven assignments
  azsuzbk <= 'b';
  
  -- Multi-driven assignments
  noopxuie <= 'U';
  noopxuie <= noopxuie;
  wwbncxqqng <= noopxuie;
  noopxuie <= '0';
end gjrmfabjbn;



-- Seed after: 5664889717454608805,13592003931158285879
