-- Seed: 7698992787577394696,7726014785203345639

use std.reflection.all;

entity zhs is
  port (ywvacoyd : inout subtype_mirror; cptwqkilz : inout time_vector(3 downto 4));
end zhs;

architecture z of zhs is
  
begin
  -- Single-driven assignments
  cptwqkilz <= (others => 0 ns);
end z;

library ieee;
use ieee.std_logic_1164.all;

entity ion is
  port (pdi : in time; rdwqdr : linkage std_logic_vector(3 downto 3));
end ion;

use std.reflection.all;

architecture vkb of ion is
  signal jwsh : time_vector(3 downto 4);
  shared variable geofyw : subtype_mirror;
  signal hxnyngvf : time_vector(3 downto 4);
  shared variable u : subtype_mirror;
  signal sfxhmnd : time_vector(3 downto 4);
  shared variable avtvrl : subtype_mirror;
  signal apzzsc : time_vector(3 downto 4);
  shared variable hvmndidxrw : subtype_mirror;
begin
  hpqbvim : entity work.zhs
    port map (ywvacoyd => hvmndidxrw, cptwqkilz => apzzsc);
  oeusye : entity work.zhs
    port map (ywvacoyd => avtvrl, cptwqkilz => sfxhmnd);
  tipylmq : entity work.zhs
    port map (ywvacoyd => u, cptwqkilz => hxnyngvf);
  ikl : entity work.zhs
    port map (ywvacoyd => geofyw, cptwqkilz => jwsh);
end vkb;

use std.reflection.all;

entity jfgsimimrh is
  port (vf : inout value_mirror; xtgsdpn : inout integer_value_mirror; bouhwrxxpy : inout integer_value_mirror);
end jfgsimimrh;

use std.reflection.all;

architecture zvhsfrw of jfgsimimrh is
  signal xfrjfnpxvt : time_vector(3 downto 4);
  shared variable w : subtype_mirror;
begin
  oyefk : entity work.zhs
    port map (ywvacoyd => w, cptwqkilz => xfrjfnpxvt);
end zvhsfrw;



-- Seed after: 16174692208381710933,7726014785203345639
