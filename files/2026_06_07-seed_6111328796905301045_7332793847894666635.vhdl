-- Seed: 6111328796905301045,7332793847894666635

library ieee;
use ieee.std_logic_1164.all;

entity sllbasz is
  port (sgjmqg : inout character; ex : out boolean_vector(3 to 4); urstng : inout severity_level; ocvryecziz : buffer std_logic);
end sllbasz;



architecture irgtmzz of sllbasz is
  
begin
  
end irgtmzz;

library ieee;
use ieee.std_logic_1164.all;

entity zdex is
  port (y : buffer integer_vector(3 downto 4); tvqyrlbx : linkage std_logic);
end zdex;

library ieee;
use ieee.std_logic_1164.all;

architecture x of zdex is
  signal uv : std_logic;
  signal oztlyn : severity_level;
  signal ugk : boolean_vector(3 to 4);
  signal w : character;
  signal fb : severity_level;
  signal nwffar : boolean_vector(3 to 4);
  signal allewd : character;
  signal cdqhfwo : std_logic;
  signal ixgks : severity_level;
  signal toedtyeajj : boolean_vector(3 to 4);
  signal gnxzyfghb : character;
begin
  riijmyco : entity work.sllbasz
    port map (sgjmqg => gnxzyfghb, ex => toedtyeajj, urstng => ixgks, ocvryecziz => cdqhfwo);
  vp : entity work.sllbasz
    port map (sgjmqg => allewd, ex => nwffar, urstng => fb, ocvryecziz => cdqhfwo);
  vnsddkyht : entity work.sllbasz
    port map (sgjmqg => w, ex => ugk, urstng => oztlyn, ocvryecziz => uv);
end x;

library ieee;
use ieee.std_logic_1164.all;

entity iue is
  port (ta : linkage std_logic_vector(4 to 1));
end iue;

library ieee;
use ieee.std_logic_1164.all;

architecture wqrfwve of iue is
  signal hig : std_logic;
  signal aazrce : severity_level;
  signal acoiwvcord : boolean_vector(3 to 4);
  signal vvfisl : character;
begin
  jzqouhowo : entity work.sllbasz
    port map (sgjmqg => vvfisl, ex => acoiwvcord, urstng => aazrce, ocvryecziz => hig);
end wqrfwve;



-- Seed after: 11521140710648367911,7332793847894666635
