-- Seed: 2877928943628438072,12359743974512393525

library ieee;
use ieee.std_logic_1164.all;

entity zamc is
  port (byqiums : in time; fcagkfe : in std_logic; gibo : in real);
end zamc;

architecture xkmvox of zamc is
  
begin
  
end xkmvox;

library ieee;
use ieee.std_logic_1164.all;

entity nbdigq is
  port (lamq : inout std_logic);
end nbdigq;

library ieee;
use ieee.std_logic_1164.all;

architecture ejymq of nbdigq is
  signal pt : real;
  signal atcvwyqk : time;
  signal r : std_logic;
  signal n : time;
  signal qzanfgw : real;
  signal fdfsf : std_logic;
  signal mwcxzuhhc : time;
begin
  trr : entity work.zamc
    port map (byqiums => mwcxzuhhc, fcagkfe => fdfsf, gibo => qzanfgw);
  y : entity work.zamc
    port map (byqiums => n, fcagkfe => r, gibo => qzanfgw);
  uc : entity work.zamc
    port map (byqiums => atcvwyqk, fcagkfe => fdfsf, gibo => pt);
  
  -- Multi-driven assignments
  lamq <= '-';
  r <= fdfsf;
end ejymq;

library ieee;
use ieee.std_logic_1164.all;

entity nxqdtlinu is
  port (l : buffer string(5 downto 1); edwok : linkage real; embl : out time; d : out std_logic_vector(3 downto 4));
end nxqdtlinu;

library ieee;
use ieee.std_logic_1164.all;

architecture rnbjrnxj of nxqdtlinu is
  signal dhldzyfbqb : real;
  signal pxflnbtkh : std_logic;
  signal zclvfpx : time;
begin
  dghgfoks : entity work.zamc
    port map (byqiums => zclvfpx, fcagkfe => pxflnbtkh, gibo => dhldzyfbqb);
  
  -- Single-driven assignments
  dhldzyfbqb <= 00144.1344;
  l <= "qhezk";
  embl <= embl;
end rnbjrnxj;

library ieee;
use ieee.std_logic_1164.all;

entity cvzt is
  port (insafex : linkage std_logic; kie : inout real; xf : inout std_logic_vector(2 to 1); h : out real);
end cvzt;

library ieee;
use ieee.std_logic_1164.all;

architecture ohenojcx of cvzt is
  signal yuphclsob : std_logic;
  signal ojoi : time;
  signal ncikwp : std_logic_vector(3 downto 4);
  signal b : time;
  signal jfprfkcij : string(5 downto 1);
  signal stcf : std_logic;
  signal jajrblosd : std_logic_vector(3 downto 4);
  signal wuftvs : time;
  signal iztooma : string(5 downto 1);
begin
  bucml : entity work.nxqdtlinu
    port map (l => iztooma, edwok => h, embl => wuftvs, d => jajrblosd);
  wrcfdfqp : entity work.nbdigq
    port map (lamq => stcf);
  csl : entity work.nxqdtlinu
    port map (l => jfprfkcij, edwok => kie, embl => b, d => ncikwp);
  dfeaiwxsih : entity work.zamc
    port map (byqiums => ojoi, fcagkfe => yuphclsob, gibo => h);
end ohenojcx;



-- Seed after: 12874871547907758074,12359743974512393525
