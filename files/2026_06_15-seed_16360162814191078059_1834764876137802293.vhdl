-- Seed: 16360162814191078059,1834764876137802293

entity ay is
  port (jso : inout time; zrp : linkage boolean);
end ay;

architecture oipf of ay is
  
begin
  -- Single-driven assignments
  jso <= 8#1_2_0# fs;
end oipf;

entity vw is
  port (jjtuprpbml : buffer integer);
end vw;

architecture azj of vw is
  signal owoymncwfp : boolean;
  signal nvaaxwd : time;
  signal ygbwgyygap : boolean;
  signal asupof : time;
  signal f : boolean;
  signal kbbdpiut : time;
  signal gcwwmozepu : boolean;
  signal qmufm : time;
begin
  vzp : entity work.ay
    port map (jso => qmufm, zrp => gcwwmozepu);
  gcrpbgvqq : entity work.ay
    port map (jso => kbbdpiut, zrp => f);
  wth : entity work.ay
    port map (jso => asupof, zrp => ygbwgyygap);
  lqnjg : entity work.ay
    port map (jso => nvaaxwd, zrp => owoymncwfp);
  
  -- Single-driven assignments
  jjtuprpbml <= 16#5#;
end azj;

library ieee;
use ieee.std_logic_1164.all;

entity liubwhj is
  port (quxsdfsa : linkage real; vnjzn : buffer boolean_vector(4 downto 3); jeeebqegub : linkage std_logic_vector(1 to 3); br : out real);
end liubwhj;

architecture sfmdr of liubwhj is
  signal mv : boolean;
  signal wvnbmsrwg : time;
  signal fsflooqxk : boolean;
  signal voogwz : time;
  signal ld : boolean;
  signal a : time;
  signal vj : boolean;
  signal en : time;
begin
  i : entity work.ay
    port map (jso => en, zrp => vj);
  n : entity work.ay
    port map (jso => a, zrp => ld);
  aeyjt : entity work.ay
    port map (jso => voogwz, zrp => fsflooqxk);
  oruvzj : entity work.ay
    port map (jso => wvnbmsrwg, zrp => mv);
  
  -- Single-driven assignments
  br <= 1010.202;
  vnjzn <= (TRUE, TRUE);
end sfmdr;



-- Seed after: 14576457348190458757,1834764876137802293
