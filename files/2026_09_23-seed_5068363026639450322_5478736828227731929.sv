// Seed: 5068363026639450322,5478736828227731929

module yclafy (output reg [4:3] gz);
  not bfmjpq(gz, gz);
  // warning: implicit conversion of port connection truncates from 2 to 1 bits
  //   reg [4:3] gz -> logic gz
  //
  // warning: implicit conversion of port connection truncates from 2 to 1 bits
  //   reg [4:3] gz -> logic gz
  
  xnor p(jlhpuy, yrozf, gz);
  // warning: implicit conversion of port connection truncates from 2 to 1 bits
  //   reg [4:3] gz -> logic gz
  
  
  // Multi-driven assignments
  assign jlhpuy = 'b1xz;
  assign jlhpuy = jlhpuy;
  assign yrozf = 'bzz;
endmodule: yclafy

module vezboj (inout tri logic [2:0][4:0][2:0] flrk [2:3]);
  yclafy xkznyqppe(.gz(a));
  // warning: implicit conversion of port connection expands from 1 to 2 bits
  //   wire logic a -> reg [4:3] gz
  
  xnor anbzceogq(a, bjksgfvvl, bjksgfvvl);
  
  
  // Multi-driven assignments
  assign flrk = flrk;
  assign bjksgfvvl = 'b0;
  assign bjksgfvvl = 'bz;
  assign a = a;
endmodule: vezboj

module o (output reg [2:3][0:4] vmcixmxxqz [1:3]);
  xnor rht(jk, njt, gn);
  
  xnor fu(jk, jk, gn);
  
  not jqc(gn, jk);
  
  
  // Single-driven assignments
  assign vmcixmxxqz = vmcixmxxqz;
  
  // Multi-driven assignments
  assign njt = 'b0;
endmodule: o



// Seed after: 8814941422471764255,5478736828227731929
