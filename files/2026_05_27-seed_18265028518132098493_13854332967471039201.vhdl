-- Seed: 18265028518132098493,13854332967471039201

library ieee;
use ieee.std_logic_1164.all;

entity l is
  port (hscl : buffer real; dxu : buffer severity_level; fhgxplrjfv : out std_logic_vector(4 to 1));
end l;



architecture owirvmgulm of l is
  
begin
  
end owirvmgulm;

library ieee;
use ieee.std_logic_1164.all;

entity z is
  port (tfh : buffer boolean_vector(3 to 0); idabvihte : out std_logic; sejbwucilr : in severity_level; hlspde : inout time);
end z;

library ieee;
use ieee.std_logic_1164.all;

architecture reufh of z is
  signal gy : severity_level;
  signal xxex : real;
  signal tzzrvo : severity_level;
  signal o : real;
  signal uzsi : severity_level;
  signal fsslfmbp : real;
  signal siphvf : std_logic_vector(4 to 1);
  signal drxu : severity_level;
  signal qfnoek : real;
begin
  iplhcpts : entity work.l
    port map (hscl => qfnoek, dxu => drxu, fhgxplrjfv => siphvf);
  ivdix : entity work.l
    port map (hscl => fsslfmbp, dxu => uzsi, fhgxplrjfv => siphvf);
  rwnmnwcc : entity work.l
    port map (hscl => o, dxu => tzzrvo, fhgxplrjfv => siphvf);
  fsjm : entity work.l
    port map (hscl => xxex, dxu => gy, fhgxplrjfv => siphvf);
end reufh;



-- Seed after: 5682596888767318455,13854332967471039201
