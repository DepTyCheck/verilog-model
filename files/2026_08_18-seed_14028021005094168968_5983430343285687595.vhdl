-- Seed: 14028021005094168968,5983430343285687595

entity odbxescbu is
  port (txoqtjdw : inout time; dlezd : inout real; tr : out bit; tqe : in real);
end odbxescbu;

architecture zxmxzmlnpt of odbxescbu is
  
begin
  -- Single-driven assignments
  txoqtjdw <= 14132 ns;
end zxmxzmlnpt;

library ieee;
use ieee.std_logic_1164.all;

entity mp is
  port (mvuhlltjqn : inout real; p : inout std_logic_vector(3 to 3); bszrunbv : out integer);
end mp;

architecture rltkrapx of mp is
  signal sxo : bit;
  signal apow : time;
  signal gqxt : bit;
  signal xgjyeirbui : time;
  signal oqczkhdad : bit;
  signal dklocvell : real;
  signal qvudk : time;
  signal ceeklhlqk : real;
  signal qghuv : bit;
  signal cmdmcwkl : real;
  signal cztkldotjn : time;
begin
  fhq : entity work.odbxescbu
    port map (txoqtjdw => cztkldotjn, dlezd => cmdmcwkl, tr => qghuv, tqe => ceeklhlqk);
  knixcqaodh : entity work.odbxescbu
    port map (txoqtjdw => qvudk, dlezd => dklocvell, tr => oqczkhdad, tqe => dklocvell);
  um : entity work.odbxescbu
    port map (txoqtjdw => xgjyeirbui, dlezd => mvuhlltjqn, tr => gqxt, tqe => ceeklhlqk);
  ijufoa : entity work.odbxescbu
    port map (txoqtjdw => apow, dlezd => ceeklhlqk, tr => sxo, tqe => dklocvell);
  
  -- Single-driven assignments
  bszrunbv <= bszrunbv;
  
  -- Multi-driven assignments
  p <= p;
  p <= (others => 'X');
  p <= "0";
end rltkrapx;

library ieee;
use ieee.std_logic_1164.all;

entity j is
  port (biyrk : buffer time; gmqv : out bit_vector(4 downto 4); upgactybr : inout std_logic; vlnxtbujql : inout std_logic_vector(3 downto 0));
end j;

architecture qzr of j is
  signal ii : real;
  signal nblxyklmf : bit;
  signal todduurqk : real;
  signal gdte : real;
  signal nta : bit;
  signal d : real;
  signal kx : time;
  signal fiivunoxvy : bit;
  signal tmn : real;
  signal mwclhkwiay : time;
begin
  trk : entity work.odbxescbu
    port map (txoqtjdw => mwclhkwiay, dlezd => tmn, tr => fiivunoxvy, tqe => tmn);
  v : entity work.odbxescbu
    port map (txoqtjdw => kx, dlezd => d, tr => nta, tqe => gdte);
  qnzs : entity work.odbxescbu
    port map (txoqtjdw => biyrk, dlezd => todduurqk, tr => nblxyklmf, tqe => ii);
end qzr;



-- Seed after: 17729713725325393093,5983430343285687595
