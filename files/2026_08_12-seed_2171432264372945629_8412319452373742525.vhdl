-- Seed: 2171432264372945629,8412319452373742525

entity l is
  port (izolcqjnrs : in integer_vector(4 downto 1); whytm : linkage integer; tmrvkspdg : linkage real);
end l;

architecture x of l is
  
begin
  
end x;

library ieee;
use ieee.std_logic_1164.all;

entity ga is
  port (wxieout : out std_logic_vector(2 to 1); oryq : in bit; qrkxrobrxr : buffer std_logic_vector(2 to 3));
end ga;

architecture eqqph of ga is
  signal y : real;
  signal xzuo : integer;
  signal eosfat : integer_vector(4 downto 1);
  signal ri : real;
  signal d : integer;
  signal owyrlzsibt : real;
  signal hnzqrlr : integer;
  signal qlb : integer_vector(4 downto 1);
begin
  hcyefpndrq : entity work.l
    port map (izolcqjnrs => qlb, whytm => hnzqrlr, tmrvkspdg => owyrlzsibt);
  kb : entity work.l
    port map (izolcqjnrs => qlb, whytm => d, tmrvkspdg => ri);
  oww : entity work.l
    port map (izolcqjnrs => eosfat, whytm => xzuo, tmrvkspdg => y);
  
  -- Single-driven assignments
  qlb <= (3_0, 8#411#, 2_2_0_0_1, 8#6773#);
  
  -- Multi-driven assignments
  qrkxrobrxr <= qrkxrobrxr;
end eqqph;

library ieee;
use ieee.std_logic_1164.all;

entity uw is
  port ( eiqhtlv : inout std_logic
  ; wasosipsa : in bit_vector(2 to 2)
  ; zixmt : linkage std_logic_vector(1 downto 0)
  ; djyhw : inout real_vector(2 downto 2)
  );
end uw;

architecture inerrr of uw is
  signal datsvsixlg : real;
  signal vggq : integer;
  signal twdrlmgnbo : integer_vector(4 downto 1);
  signal egxsrkl : real;
  signal hsdbz : integer;
  signal ccohm : integer_vector(4 downto 1);
begin
  ltt : entity work.l
    port map (izolcqjnrs => ccohm, whytm => hsdbz, tmrvkspdg => egxsrkl);
  voj : entity work.l
    port map (izolcqjnrs => twdrlmgnbo, whytm => vggq, tmrvkspdg => datsvsixlg);
  
  -- Single-driven assignments
  djyhw <= (others => 2#0_1.0_0_0_1_1#);
  twdrlmgnbo <= (1, 16#2E#, 2#1_1_0#, 8#10371#);
  ccohm <= ccohm;
  
  -- Multi-driven assignments
  eiqhtlv <= '0';
  eiqhtlv <= 'Z';
  eiqhtlv <= eiqhtlv;
end inerrr;

entity nkklkieiqb is
  port (zytvjpcxn : out integer_vector(3 downto 4); jrfyiwf : inout severity_level);
end nkklkieiqb;

library ieee;
use ieee.std_logic_1164.all;

architecture jqnfak of nkklkieiqb is
  signal aizeohgw : real;
  signal abn : integer;
  signal xmqvy : integer_vector(4 downto 1);
  signal xnx : real_vector(2 downto 2);
  signal nfolx : std_logic_vector(1 downto 0);
  signal imjaoanbw : bit_vector(2 to 2);
  signal zngtnhgpvx : real_vector(2 downto 2);
  signal wekkrhkdqg : std_logic_vector(1 downto 0);
  signal dy : bit_vector(2 to 2);
  signal uzs : std_logic;
begin
  rkpvopbbi : entity work.uw
    port map (eiqhtlv => uzs, wasosipsa => dy, zixmt => wekkrhkdqg, djyhw => zngtnhgpvx);
  kblj : entity work.uw
    port map (eiqhtlv => uzs, wasosipsa => imjaoanbw, zixmt => nfolx, djyhw => xnx);
  zorqfh : entity work.l
    port map (izolcqjnrs => xmqvy, whytm => abn, tmrvkspdg => aizeohgw);
  
  -- Multi-driven assignments
  uzs <= 'L';
  uzs <= '-';
  uzs <= 'W';
end jqnfak;



-- Seed after: 14214071986940219561,8412319452373742525
