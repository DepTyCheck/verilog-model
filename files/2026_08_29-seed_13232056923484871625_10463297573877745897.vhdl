-- Seed: 13232056923484871625,10463297573877745897

entity ln is
  port (o : inout real; rtouey : inout bit; otvyazm : linkage integer_vector(1 to 0); rlpwynz : linkage character);
end ln;

architecture ljpc of ln is
  
begin
  -- Single-driven assignments
  o <= 2#0_1_0_1.1#;
  rtouey <= rtouey;
end ljpc;

library ieee;
use ieee.std_logic_1164.all;

entity dnxkdtxh is
  port (uign : out integer; qbwnvpab : in std_logic; upfnlziu : in std_logic_vector(3 downto 0); piw : buffer integer);
end dnxkdtxh;

architecture imakyngfbc of dnxkdtxh is
  signal izrhy : character;
  signal bbknqsxzn : integer_vector(1 to 0);
  signal extgwkjsu : bit;
  signal sefoo : real;
  signal rbravpbw : character;
  signal ltys : integer_vector(1 to 0);
  signal hizva : bit;
  signal rc : real;
  signal yag : character;
  signal dq : integer_vector(1 to 0);
  signal b : bit;
  signal cvij : real;
  signal wlchzyll : character;
  signal bfflq : integer_vector(1 to 0);
  signal mrrghdiymb : bit;
  signal kihyuenz : real;
begin
  zvzdmh : entity work.ln
    port map (o => kihyuenz, rtouey => mrrghdiymb, otvyazm => bfflq, rlpwynz => wlchzyll);
  zpoyvdlup : entity work.ln
    port map (o => cvij, rtouey => b, otvyazm => dq, rlpwynz => yag);
  yjbgqneydb : entity work.ln
    port map (o => rc, rtouey => hizva, otvyazm => ltys, rlpwynz => rbravpbw);
  ygjwfkl : entity work.ln
    port map (o => sefoo, rtouey => extgwkjsu, otvyazm => bbknqsxzn, rlpwynz => izrhy);
  
  -- Single-driven assignments
  uign <= 23021;
  piw <= 1;
end imakyngfbc;

entity dcodvtxoah is
  port (ucn : buffer time);
end dcodvtxoah;

architecture aeoobzii of dcodvtxoah is
  
begin
  -- Single-driven assignments
  ucn <= 8#63# ns;
end aeoobzii;

entity ylgkirg is
  port (omjxzla : out time);
end ylgkirg;

architecture gmpabvxakh of ylgkirg is
  signal uijzo : character;
  signal topv : integer_vector(1 to 0);
  signal f : bit;
  signal thokmmzsf : real;
begin
  xnx : entity work.ln
    port map (o => thokmmzsf, rtouey => f, otvyazm => topv, rlpwynz => uijzo);
  
  -- Single-driven assignments
  omjxzla <= omjxzla;
end gmpabvxakh;



-- Seed after: 5420501016475896727,10463297573877745897
