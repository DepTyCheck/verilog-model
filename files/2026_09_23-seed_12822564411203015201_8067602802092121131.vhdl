-- Seed: 12822564411203015201,8067602802092121131

entity p is
  port (w : in real);
end p;

architecture o of p is
  
begin
  
end o;

library ieee;
use ieee.std_logic_1164.all;

entity g is
  port (zmsankrm : buffer std_logic_vector(4 downto 3); ipvxs : out string(3 to 2));
end g;

architecture hzh of g is
  signal w : real;
  signal ehksolqrrf : real;
  signal dtpms : real;
begin
  wg : entity work.p
    port map (w => dtpms);
  ki : entity work.p
    port map (w => ehksolqrrf);
  zaynas : entity work.p
    port map (w => ehksolqrrf);
  u : entity work.p
    port map (w => w);
  
  -- Multi-driven assignments
  zmsankrm <= zmsankrm;
end hzh;



-- Seed after: 4848919974941452401,8067602802092121131
