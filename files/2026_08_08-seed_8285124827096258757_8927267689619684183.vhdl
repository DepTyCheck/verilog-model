-- Seed: 8285124827096258757,8927267689619684183

library ieee;
use ieee.std_logic_1164.all;

entity hnikzz is
  port (dg : linkage real_vector(3 to 0); rfcggw : out std_logic_vector(1 downto 0); o : in integer);
end hnikzz;

architecture nvredhce of hnikzz is
  
begin
  -- Multi-driven assignments
  rfcggw <= rfcggw;
end nvredhce;

library ieee;
use ieee.std_logic_1164.all;

entity gemswbs is
  port (a : out bit_vector(0 to 1); y : inout integer; g : linkage character; hgpgfj : in std_logic);
end gemswbs;

library ieee;
use ieee.std_logic_1164.all;

architecture tqyp of gemswbs is
  signal ukjuhtdp : std_logic_vector(1 downto 0);
  signal rxjcsfjpa : real_vector(3 to 0);
  signal lvlvuvdo : std_logic_vector(1 downto 0);
  signal yhj : real_vector(3 to 0);
begin
  ys : entity work.hnikzz
    port map (dg => yhj, rfcggw => lvlvuvdo, o => y);
  jrnob : entity work.hnikzz
    port map (dg => rxjcsfjpa, rfcggw => ukjuhtdp, o => y);
  
  -- Single-driven assignments
  y <= y;
end tqyp;

library ieee;
use ieee.std_logic_1164.all;

entity vj is
  port (rj : buffer std_logic);
end vj;

library ieee;
use ieee.std_logic_1164.all;

architecture insusmyh of vj is
  signal nocozqj : integer;
  signal lprkcbp : std_logic_vector(1 downto 0);
  signal zrjxhwk : real_vector(3 to 0);
  signal txju : integer;
  signal dmdq : std_logic_vector(1 downto 0);
  signal oslkjgqrky : real_vector(3 to 0);
  signal ksccl : std_logic;
  signal ziys : character;
  signal yioynkucip : integer;
  signal sqg : bit_vector(0 to 1);
begin
  ysftho : entity work.gemswbs
    port map (a => sqg, y => yioynkucip, g => ziys, hgpgfj => ksccl);
  lgh : entity work.hnikzz
    port map (dg => oslkjgqrky, rfcggw => dmdq, o => txju);
  vs : entity work.hnikzz
    port map (dg => zrjxhwk, rfcggw => lprkcbp, o => nocozqj);
  
  -- Multi-driven assignments
  ksccl <= rj;
end insusmyh;



-- Seed after: 11418467728983080213,8927267689619684183
