-- Seed: 4512429073649988375,6290177331721581829

entity xz is
  port (mymalaprm : in integer);
end xz;

architecture vnkb of xz is
  
begin
  
end vnkb;

use std.reflection.all;

entity apr is
  port (c : linkage real; rhcfef : in time; daovcai : inout physical_subtype_mirror; syusp : inout access_value_mirror);
end apr;

architecture uhy of apr is
  signal rrajqpvyzt : integer;
  signal rmyrdw : integer;
begin
  govtaoxwsx : entity work.xz
    port map (mymalaprm => rmyrdw);
  kkazkt : entity work.xz
    port map (mymalaprm => rrajqpvyzt);
  a : entity work.xz
    port map (mymalaprm => rmyrdw);
  
  -- Single-driven assignments
  rmyrdw <= rmyrdw;
  rrajqpvyzt <= 2212;
end uhy;

entity r is
  port (euyabojyz : out integer);
end r;

use std.reflection.all;

architecture tnp of r is
  signal l : integer;
  signal lofj : integer;
  shared variable n : access_value_mirror;
  shared variable hosfkwfbl : physical_subtype_mirror;
  signal np : time;
  signal y : real;
begin
  caz : entity work.apr
    port map (c => y, rhcfef => np, daovcai => hosfkwfbl, syusp => n);
  g : entity work.xz
    port map (mymalaprm => lofj);
  vbdmq : entity work.xz
    port map (mymalaprm => l);
  
  -- Single-driven assignments
  lofj <= euyabojyz;
end tnp;



-- Seed after: 18375797296675524653,6290177331721581829
