-- Seed: 15857409632813341183,8118127366649987907

entity oepzpyolt is
  port (uuulho : buffer string(1 downto 3); g : in integer; pxmcbs : linkage time);
end oepzpyolt;

architecture uj of oepzpyolt is
  
begin
  
end uj;

entity iw is
  port (vz : out bit_vector(0 to 2); ftv : in real; wnq : in integer; fgy : buffer real);
end iw;

architecture w of iw is
  signal rdqvtx : time;
  signal ocruop : integer;
  signal bwglffsz : string(1 downto 3);
  signal qudbcxowhx : time;
  signal mmcpygue : integer;
  signal sxt : string(1 downto 3);
  signal vhwjeke : time;
  signal yyrc : integer;
  signal ffx : string(1 downto 3);
  signal vkwepavx : time;
  signal l : string(1 downto 3);
begin
  kxjrmrby : entity work.oepzpyolt
    port map (uuulho => l, g => wnq, pxmcbs => vkwepavx);
  dphx : entity work.oepzpyolt
    port map (uuulho => ffx, g => yyrc, pxmcbs => vhwjeke);
  yfzccduv : entity work.oepzpyolt
    port map (uuulho => sxt, g => mmcpygue, pxmcbs => qudbcxowhx);
  uttt : entity work.oepzpyolt
    port map (uuulho => bwglffsz, g => ocruop, pxmcbs => rdqvtx);
  
  -- Single-driven assignments
  fgy <= 16#CA7EC.FFF5A#;
end w;



-- Seed after: 6020155435316348568,8118127366649987907
