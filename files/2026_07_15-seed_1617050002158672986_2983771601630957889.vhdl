-- Seed: 1617050002158672986,2983771601630957889

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity s is
  port (variable ny : inout record_value_mirror_pt; variable phsmybqpw : inout enumeration_value_mirror_pt; dof : buffer std_logic_vector(4 to 0));
end s;

architecture zkcouqii of s is
  
begin
  -- Multi-driven assignments
  dof <= dof;
  dof <= dof;
end zkcouqii;

use std.reflection.all;

entity azpoagwel is
  port ( zentkrxzf : buffer character
  ; fyzovqejb : linkage character
  ; variable wtywi : inout array_subtype_mirror_pt
  ; variable lfdvyy : inout access_value_mirror_pt
  );
end azpoagwel;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture ybgcl of azpoagwel is
  signal dz : std_logic_vector(4 to 0);
  shared variable hsaxenu : enumeration_value_mirror_pt;
  shared variable juc : record_value_mirror_pt;
  signal blknjafm : std_logic_vector(4 to 0);
  shared variable wxcbdbg : enumeration_value_mirror_pt;
  shared variable h : record_value_mirror_pt;
begin
  f : entity work.s
    port map (ny => h, phsmybqpw => wxcbdbg, dof => blknjafm);
  onsiyiksol : entity work.s
    port map (ny => juc, phsmybqpw => hsaxenu, dof => dz);
  
  -- Multi-driven assignments
  blknjafm <= "";
  blknjafm <= blknjafm;
  blknjafm <= blknjafm;
  dz <= (others => '0');
end ybgcl;



-- Seed after: 1677756735810264758,2983771601630957889
