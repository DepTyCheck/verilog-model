-- Seed: 8235315472700638557,14141408946471626091

library ieee;
use ieee.std_logic_1164.all;

entity gty is
  port (txveatbqb : buffer time; hipvnrql : in real_vector(3 to 0); l : linkage bit; vqdyzn : buffer std_logic);
end gty;

architecture nbxjebsxz of gty is
  
begin
  -- Single-driven assignments
  txveatbqb <= txveatbqb;
  
  -- Multi-driven assignments
  vqdyzn <= vqdyzn;
  vqdyzn <= '-';
  vqdyzn <= 'U';
  vqdyzn <= vqdyzn;
end nbxjebsxz;

entity necqt is
  port (alugdtaqvs : in time; t : buffer integer);
end necqt;

library ieee;
use ieee.std_logic_1164.all;

architecture a of necqt is
  signal sxnaffyd : std_logic;
  signal ql : bit;
  signal irgtao : real_vector(3 to 0);
  signal kxxyqwc : time;
begin
  sdsmgcun : entity work.gty
    port map (txveatbqb => kxxyqwc, hipvnrql => irgtao, l => ql, vqdyzn => sxnaffyd);
  
  -- Single-driven assignments
  t <= 16#2_3_D_2#;
  irgtao <= (others => 0.0);
  
  -- Multi-driven assignments
  sxnaffyd <= '0';
  sxnaffyd <= sxnaffyd;
  sxnaffyd <= 'W';
  sxnaffyd <= 'Z';
end a;



-- Seed after: 10309770794166506445,14141408946471626091
