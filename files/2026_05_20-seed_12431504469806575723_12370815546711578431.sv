// Seed: 12431504469806575723,12370815546711578431

module mtfwwvhpf (output bit [2:0] doxni, output bit tnqd);
  
  
  
  // Single-driven assigns
  assign doxni = doxni;
  assign tnqd = 'b11;
  
  // Multi-driven assigns
endmodule: mtfwwvhpf

module spvncjum ();
  
  or zdb(xmhpssspgn, xmhpssspgn, hjy);
  
  not urfsnjeo(oiw, k);
  
  and xlzuyflfey(camrpwx, camrpwx, xmhpssspgn);
  
  nand lydhunzn(uiokbjr, uiokbjr, ofcv);
  
  
  // Single-driven assigns
  
  // Multi-driven assigns
  assign k = xmhpssspgn;
  assign uiokbjr = xmhpssspgn;
  assign xmhpssspgn = camrpwx;
  assign oiw = 'bz;
endmodule: spvncjum

module p (output reg [4:4][4:1] hzpssl [3:3][4:0], output wire logic pw [1:3][2:1][0:4], input logic [2:0] ggh);
  
  and kkovbwmndp(lbj, lbj, ggh);
  // warning: implicit conversion of port connection truncates from 3 to 1 bits
  //   logic [2:0] ggh -> logic ggh
  
  not vrainbhbmb(lbj, lyzoo);
  
  
  // Single-driven assigns
  assign hzpssl = hzpssl;
  
  // Multi-driven assigns
endmodule: p

module crsmd (inout tri1 logic y [2:0][4:1][3:4][0:2], output bit [3:3][0:0][1:0][3:3] uatu);
  
  xnor vnaz(uatu, kzpojmj, uatu);
  // warning: implicit conversion of port connection truncates from 2 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [3:3][0:0][1:0][3:3] uatu -> logic uatu
  //
  // warning: implicit conversion of port connection truncates from 2 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [3:3][0:0][1:0][3:3] uatu -> logic uatu
  
  xnor oujg(zhifxv, zhifxv, hmob);
  
  nand xd(b, rsu, uatu);
  // warning: implicit conversion of port connection truncates from 2 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [3:3][0:0][1:0][3:3] uatu -> logic uatu
  
  xnor irlv(clrpsezgos, vqjairg, vxsylittex);
  
  
  // Single-driven assigns
  
  // Multi-driven assigns
  assign rsu = vxsylittex;
  assign y = y;
  assign kzpojmj = 'bzz10z;
  assign hmob = 'b0x01x;
  assign vxsylittex = 'bz0;
endmodule: crsmd



// Seed after: 10551752226898711340,12370815546711578431
