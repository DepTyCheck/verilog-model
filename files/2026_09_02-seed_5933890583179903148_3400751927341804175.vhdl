-- Seed: 5933890583179903148,3400751927341804175

library ieee;
use ieee.std_logic_1164.all;

entity f is
  port (cg : out std_logic_vector(0 downto 4); ipqflqux : inout time; xss : linkage character);
end f;

architecture feok of f is
  
begin
  -- Single-driven assignments
  ipqflqux <= ipqflqux;
  
  -- Multi-driven assignments
  cg <= "";
  cg <= cg;
  cg <= (others => '0');
end feok;

library ieee;
use ieee.std_logic_1164.all;

entity fnrasvyz is
  port (iwpnirip : in boolean; jkciltpb : inout std_logic_vector(0 downto 0); ddwcjol : inout bit);
end fnrasvyz;

library ieee;
use ieee.std_logic_1164.all;

architecture czm of fnrasvyz is
  signal erhop : character;
  signal kwaawfeoh : time;
  signal j : character;
  signal gtmocvd : time;
  signal jykxcg : std_logic_vector(0 downto 4);
  signal rupkb : character;
  signal dravgfd : time;
  signal hnybxccknz : character;
  signal fmgwfhzrtl : time;
  signal xo : std_logic_vector(0 downto 4);
begin
  kkdasix : entity work.f
    port map (cg => xo, ipqflqux => fmgwfhzrtl, xss => hnybxccknz);
  mcnjrx : entity work.f
    port map (cg => xo, ipqflqux => dravgfd, xss => rupkb);
  td : entity work.f
    port map (cg => jykxcg, ipqflqux => gtmocvd, xss => j);
  ii : entity work.f
    port map (cg => jykxcg, ipqflqux => kwaawfeoh, xss => erhop);
  
  -- Multi-driven assignments
  jkciltpb <= jkciltpb;
  jykxcg <= "";
  jykxcg <= xo;
  jkciltpb <= (others => 'H');
end czm;



-- Seed after: 12925948497679388769,3400751927341804175
