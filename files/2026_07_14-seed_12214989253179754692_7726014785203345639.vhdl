-- Seed: 12214989253179754692,7726014785203345639

library ieee;
use ieee.std_logic_1164.all;

entity tjmhy is
  port (fquqtgj : in severity_level; xbkxfae : out std_logic_vector(1 downto 1); jes : linkage bit_vector(2 to 0); ndowmf : linkage std_logic);
end tjmhy;

architecture yuwyimzy of tjmhy is
  
begin
  -- Multi-driven assignments
  xbkxfae <= (others => '0');
  xbkxfae <= xbkxfae;
end yuwyimzy;

use std.reflection.all;

entity twbwvuhk is
  port (j : inout record_value_mirror);
end twbwvuhk;

library ieee;
use ieee.std_logic_1164.all;

architecture jtntsqz of twbwvuhk is
  signal cb : bit_vector(2 to 0);
  signal a : bit_vector(2 to 0);
  signal npg : std_logic_vector(1 downto 1);
  signal eznxdfsui : severity_level;
  signal dl : std_logic;
  signal h : bit_vector(2 to 0);
  signal emhuqb : std_logic_vector(1 downto 1);
  signal k : severity_level;
begin
  ebedmrod : entity work.tjmhy
    port map (fquqtgj => k, xbkxfae => emhuqb, jes => h, ndowmf => dl);
  pgskfutajb : entity work.tjmhy
    port map (fquqtgj => eznxdfsui, xbkxfae => npg, jes => a, ndowmf => dl);
  xdvidhsh : entity work.tjmhy
    port map (fquqtgj => k, xbkxfae => npg, jes => cb, ndowmf => dl);
  
  -- Single-driven assignments
  eznxdfsui <= WARNING;
  k <= k;
  
  -- Multi-driven assignments
  dl <= dl;
  emhuqb <= "1";
  npg <= emhuqb;
  emhuqb <= emhuqb;
end jtntsqz;

use std.reflection.all;

entity tuzwtf is
  port (dwhlffxg : out boolean_vector(4 to 3); idqsjm : inout protected_subtype_mirror; dh : out real; fekjuurlhu : inout file_value_mirror);
end tuzwtf;

architecture mzflqa of tuzwtf is
  
begin
  -- Single-driven assignments
  dwhlffxg <= dwhlffxg;
  dh <= dh;
end mzflqa;



-- Seed after: 18138592633451891252,7726014785203345639
