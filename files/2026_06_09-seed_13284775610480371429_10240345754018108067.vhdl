-- Seed: 13284775610480371429,10240345754018108067



entity iji is
  port (nywkqk : buffer real; gkt : linkage real; xyqghwoil : out time; i : linkage time_vector(2 downto 0));
end iji;



architecture luwfjh of iji is
  
begin
  
end luwfjh;

library ieee;
use ieee.std_logic_1164.all;

entity vse is
  port (ahrr : linkage std_logic);
end vse;



architecture e of vse is
  signal qkhsh : time_vector(2 downto 0);
  signal flu : time;
  signal jaqh : real;
  signal gfpfwbbb : real;
  signal nvzu : time_vector(2 downto 0);
  signal xahfxyy : time;
  signal d : real;
  signal a : time_vector(2 downto 0);
  signal cda : time;
  signal i : real;
  signal iuw : real;
begin
  nhsr : entity work.iji
    port map (nywkqk => iuw, gkt => i, xyqghwoil => cda, i => a);
  mhirkl : entity work.iji
    port map (nywkqk => i, gkt => d, xyqghwoil => xahfxyy, i => nvzu);
  qmmxy : entity work.iji
    port map (nywkqk => gfpfwbbb, gkt => jaqh, xyqghwoil => flu, i => qkhsh);
end e;

library ieee;
use ieee.std_logic_1164.all;

entity nja is
  port (aw : buffer std_logic_vector(2 to 4); ziprck : out bit_vector(4 downto 2); dcaoxc : in bit);
end nja;



architecture svfmky of nja is
  signal z : time_vector(2 downto 0);
  signal gzs : time;
  signal dsihyxjc : real;
  signal zce : real;
begin
  mgqzzigmqe : entity work.iji
    port map (nywkqk => zce, gkt => dsihyxjc, xyqghwoil => gzs, i => z);
end svfmky;



-- Seed after: 17037537017995071220,10240345754018108067
