-- Seed: 10524409049894288708,662889661651915549

library ieee;
use ieee.std_logic_1164.all;

entity ehr is
  port (dp : inout integer; saki : inout time; obxgrwndu : linkage std_logic);
end ehr;

architecture aflpriti of ehr is
  
begin
  -- Single-driven assignments
  dp <= dp;
  saki <= saki;
end aflpriti;

entity kjsnkltsw is
  port (z : inout boolean_vector(4 downto 1); iewvble : linkage real);
end kjsnkltsw;

library ieee;
use ieee.std_logic_1164.all;

architecture jblosiudi of kjsnkltsw is
  signal iepmil : time;
  signal lslrckn : integer;
  signal bgcddjuf : time;
  signal kjc : integer;
  signal xihv : std_logic;
  signal bbrhbu : time;
  signal zknqujgbn : integer;
begin
  lqbpvcn : entity work.ehr
    port map (dp => zknqujgbn, saki => bbrhbu, obxgrwndu => xihv);
  yzu : entity work.ehr
    port map (dp => kjc, saki => bgcddjuf, obxgrwndu => xihv);
  gcexs : entity work.ehr
    port map (dp => lslrckn, saki => iepmil, obxgrwndu => xihv);
  
  -- Single-driven assignments
  z <= (TRUE, FALSE, FALSE, FALSE);
  
  -- Multi-driven assignments
  xihv <= xihv;
  xihv <= xihv;
end jblosiudi;

library ieee;
use ieee.std_logic_1164.all;

entity opjitbyc is
  port (a : linkage integer; yknx : inout time; mro : inout std_logic; f : out boolean);
end opjitbyc;

library ieee;
use ieee.std_logic_1164.all;

architecture lljy of opjitbyc is
  signal plfifuvd : std_logic;
  signal dlhsrlwj : integer;
  signal be : real;
  signal mlzim : boolean_vector(4 downto 1);
  signal kdbpdzh : real;
  signal azttdih : boolean_vector(4 downto 1);
begin
  zntdhth : entity work.kjsnkltsw
    port map (z => azttdih, iewvble => kdbpdzh);
  dv : entity work.kjsnkltsw
    port map (z => mlzim, iewvble => be);
  ovv : entity work.ehr
    port map (dp => dlhsrlwj, saki => yknx, obxgrwndu => plfifuvd);
  
  -- Single-driven assignments
  f <= TRUE;
end lljy;

entity l is
  port (jlxxm : out integer);
end l;

architecture nn of l is
  
begin
  -- Single-driven assignments
  jlxxm <= 2#110#;
end nn;



-- Seed after: 10009422341293878487,662889661651915549
