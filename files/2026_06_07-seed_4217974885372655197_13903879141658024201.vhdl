-- Seed: 4217974885372655197,13903879141658024201



entity n is
  port (kld : linkage integer; slp : in time);
end n;



architecture sdvcq of n is
  
begin
  
end sdvcq;

library ieee;
use ieee.std_logic_1164.all;

entity r is
  port (acl : in integer_vector(3 to 1); qoxui : inout time; qebfcgseym : inout std_logic_vector(0 downto 3));
end r;



architecture yfsk of r is
  signal amba : integer;
begin
  befqf : entity work.n
    port map (kld => amba, slp => qoxui);
  lnsh : entity work.n
    port map (kld => amba, slp => qoxui);
end yfsk;



entity fysxg is
  port (zeot : inout real; nngm : buffer real; otwdfgrii : linkage bit; rbbklauo : linkage character);
end fysxg;



architecture mqjuet of fysxg is
  signal tlrksi : time;
  signal ev : time;
  signal okwighh : integer;
begin
  rs : entity work.n
    port map (kld => okwighh, slp => ev);
  iabtjqifu : entity work.n
    port map (kld => okwighh, slp => ev);
  t : entity work.n
    port map (kld => okwighh, slp => tlrksi);
end mqjuet;



-- Seed after: 7721269477507064833,13903879141658024201
