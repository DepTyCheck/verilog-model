-- Seed: 12100014144839308994,7726014785203345639

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity ew is
  port (jwaaong : buffer std_logic; pwditezlow : inout physical_value_mirror);
end ew;

architecture cykhcvwghz of ew is
  
begin
  -- Multi-driven assignments
  jwaaong <= 'X';
end cykhcvwghz;

entity qvzqlclixh is
  port (htf : buffer character);
end qvzqlclixh;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture covxxnesp of qvzqlclixh is
  shared variable yx : physical_value_mirror;
  signal zdnhtit : std_logic;
  shared variable knoktimb : physical_value_mirror;
  shared variable kmggsyncc : physical_value_mirror;
  signal icrxtu : std_logic;
begin
  bfp : entity work.ew
    port map (jwaaong => icrxtu, pwditezlow => kmggsyncc);
  afnwlwa : entity work.ew
    port map (jwaaong => icrxtu, pwditezlow => knoktimb);
  vosw : entity work.ew
    port map (jwaaong => zdnhtit, pwditezlow => yx);
  
  -- Single-driven assignments
  htf <= 'p';
end covxxnesp;

use std.reflection.all;

entity zpbdnspi is
  port (voaoxrf : inout enumeration_value_mirror);
end zpbdnspi;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture u of zpbdnspi is
  shared variable wdn : physical_value_mirror;
  signal jmmyoau : std_logic;
  signal g : character;
  shared variable xuxtneovk : physical_value_mirror;
  signal cnhfgng : std_logic;
  signal qhurpok : character;
begin
  ymqmjoz : entity work.qvzqlclixh
    port map (htf => qhurpok);
  wjjfldzmaj : entity work.ew
    port map (jwaaong => cnhfgng, pwditezlow => xuxtneovk);
  co : entity work.qvzqlclixh
    port map (htf => g);
  v : entity work.ew
    port map (jwaaong => jmmyoau, pwditezlow => wdn);
  
  -- Multi-driven assignments
  jmmyoau <= cnhfgng;
  cnhfgng <= cnhfgng;
  cnhfgng <= cnhfgng;
  cnhfgng <= 'H';
end u;

use std.reflection.all;

entity lu is
  port (dz : inout physical_value_mirror; pdxl : inout integer_value_mirror; qazu : inout integer_value_mirror);
end lu;

architecture pigyoi of lu is
  signal imockxnua : character;
begin
  rncqsjo : entity work.qvzqlclixh
    port map (htf => imockxnua);
end pigyoi;



-- Seed after: 15615851108384407208,7726014785203345639
