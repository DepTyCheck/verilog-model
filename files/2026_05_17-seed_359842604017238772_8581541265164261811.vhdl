-- Seed: 359842604017238772,8581541265164261811

library ieee;
use ieee.std_logic_1164.all;

entity j is
  port (knecuvg : in std_logic; i : inout real);
end j;



architecture f of j is
  
begin
  
end f;



entity lpoem is
  port (gloys : linkage real);
end lpoem;

library ieee;
use ieee.std_logic_1164.all;

architecture pgky of lpoem is
  signal hzo : real;
  signal vrkkvtwca : std_logic;
  signal gjdsjesb : real;
  signal vqp : std_logic;
  signal ghnd : real;
  signal oh : std_logic;
begin
  wbstltowo : entity work.j
    port map (knecuvg => oh, i => ghnd);
  n : entity work.j
    port map (knecuvg => vqp, i => gjdsjesb);
  xmfikbxe : entity work.j
    port map (knecuvg => vrkkvtwca, i => hzo);
end pgky;



entity qernqvpmoo is
  port (ysdf : inout boolean; b : out time);
end qernqvpmoo;

library ieee;
use ieee.std_logic_1164.all;

architecture depvtzw of qernqvpmoo is
  signal pzn : std_logic;
  signal v : real;
  signal oq : real;
  signal xgeqs : std_logic;
  signal hugakn : real;
begin
  ipexmskrbe : entity work.lpoem
    port map (gloys => hugakn);
  qvxdbhcud : entity work.j
    port map (knecuvg => xgeqs, i => oq);
  uiasnw : entity work.j
    port map (knecuvg => xgeqs, i => v);
  djkiv : entity work.j
    port map (knecuvg => pzn, i => hugakn);
end depvtzw;



-- Seed after: 18198196463840403302,8581541265164261811
