-- Seed: 1980895608077296028,14094562573555574003

entity qsif is
  port (rkqyypa : inout real; ogvhl : inout time; wuss : out string(4 downto 2));
end qsif;

architecture azyizy of qsif is
  
begin
  -- Single-driven assignments
  wuss <= ('u', 'f', 'c');
end azyizy;

entity ogkmoc is
  port (rdmesgls : out integer; i : inout integer; lispsv : linkage time);
end ogkmoc;

architecture ykbwseoufe of ogkmoc is
  signal ssn : string(4 downto 2);
  signal uylpsveinu : time;
  signal l : real;
  signal ohqwleheg : string(4 downto 2);
  signal fajcaiw : time;
  signal vizld : real;
begin
  cgxcd : entity work.qsif
    port map (rkqyypa => vizld, ogvhl => fajcaiw, wuss => ohqwleheg);
  yy : entity work.qsif
    port map (rkqyypa => l, ogvhl => uylpsveinu, wuss => ssn);
  
  -- Single-driven assignments
  i <= i;
  rdmesgls <= 8#0_1_1_5_5#;
end ykbwseoufe;



-- Seed after: 4867792393599710825,14094562573555574003
