-- Seed: 1004983137235398482,4245627776430562977

entity lfs is
  port (fixgcmiic : out time);
end lfs;

architecture qkuefun of lfs is
  
begin
  
end qkuefun;

entity zbbp is
  port (pdaoijhbll : in real; czkizsfnr : buffer real_vector(0 downto 1); rxkrxxr : inout boolean);
end zbbp;

architecture vgxtghq of zbbp is
  
begin
  -- Single-driven assignments
  rxkrxxr <= FALSE;
  czkizsfnr <= czkizsfnr;
end vgxtghq;

library ieee;
use ieee.std_logic_1164.all;

entity vcmhf is
  port (eeyku : buffer std_logic_vector(4 to 3));
end vcmhf;

architecture tryaepe of vcmhf is
  signal dzo : time;
begin
  xppatxeugf : entity work.lfs
    port map (fixgcmiic => dzo);
  
  -- Multi-driven assignments
  eeyku <= eeyku;
  eeyku <= eeyku;
  eeyku <= (others => '0');
end tryaepe;

library ieee;
use ieee.std_logic_1164.all;

entity f is
  port (ofrpxtdm : out std_logic_vector(4 to 2));
end f;

library ieee;
use ieee.std_logic_1164.all;

architecture i of f is
  signal bwjrrzutn : time;
  signal xv : time;
  signal idehcfu : std_logic_vector(4 to 3);
  signal px : time;
begin
  doeosme : entity work.lfs
    port map (fixgcmiic => px);
  oydak : entity work.vcmhf
    port map (eeyku => idehcfu);
  abopq : entity work.lfs
    port map (fixgcmiic => xv);
  xifyfnhnyp : entity work.lfs
    port map (fixgcmiic => bwjrrzutn);
  
  -- Multi-driven assignments
  ofrpxtdm <= idehcfu;
  ofrpxtdm <= (others => '0');
end i;



-- Seed after: 4263289774714680631,4245627776430562977
