-- Seed: 3988467182857136003,10463297573877745897

library ieee;
use ieee.std_logic_1164.all;

entity ekh is
  port (s : linkage real; mprpw : inout bit; sf : buffer std_logic);
end ekh;

architecture diw of ekh is
  
begin
  -- Single-driven assignments
  mprpw <= '0';
  
  -- Multi-driven assignments
  sf <= 'H';
  sf <= sf;
  sf <= 'L';
end diw;

library ieee;
use ieee.std_logic_1164.all;

entity qmqfsnm is
  port (ifppscmcv : in std_logic);
end qmqfsnm;

library ieee;
use ieee.std_logic_1164.all;

architecture ctdl of qmqfsnm is
  signal ekczszrz : std_logic;
  signal ezwfti : bit;
  signal gwe : real;
  signal hva : std_logic;
  signal d : bit;
  signal dmhyoqxcj : real;
  signal qgiw : std_logic;
  signal h : bit;
  signal y : real;
begin
  tccbuxu : entity work.ekh
    port map (s => y, mprpw => h, sf => qgiw);
  ljhabgdff : entity work.ekh
    port map (s => dmhyoqxcj, mprpw => d, sf => hva);
  x : entity work.ekh
    port map (s => gwe, mprpw => ezwfti, sf => ekczszrz);
  
  -- Multi-driven assignments
  qgiw <= '0';
  qgiw <= ifppscmcv;
end ctdl;

entity brcw is
  port (pcsuqpr : in bit_vector(1 to 1); wpypkkn : inout real; os : buffer integer; mk : buffer time);
end brcw;

library ieee;
use ieee.std_logic_1164.all;

architecture igcd of brcw is
  signal a : std_logic;
  signal q : bit;
begin
  inhfxfwduf : entity work.ekh
    port map (s => wpypkkn, mprpw => q, sf => a);
  
  -- Single-driven assignments
  os <= 8#4#;
  mk <= mk;
  
  -- Multi-driven assignments
  a <= a;
  a <= a;
  a <= a;
end igcd;



-- Seed after: 13790311188434236544,10463297573877745897
