-- Seed: 3225509598392132027,6299883410057943775

entity ghlqask is
  port (ab : linkage bit; nymufz : inout severity_level; eljhmeykn : in integer; fsf : buffer integer);
end ghlqask;

architecture fmjkthxwtr of ghlqask is
  
begin
  -- Single-driven assignments
  fsf <= fsf;
  nymufz <= nymufz;
end fmjkthxwtr;

entity bfuo is
  port (xcxcmdm : in real_vector(1 downto 0); r : in character; jjocfw : out string(3 downto 3));
end bfuo;

architecture cwtgbehx of bfuo is
  signal svuhdjr : integer;
  signal e : severity_level;
  signal iel : bit;
  signal w : severity_level;
  signal gkt : bit;
  signal un : integer;
  signal kx : integer;
  signal hzzlaisi : severity_level;
  signal pk : bit;
begin
  ukcij : entity work.ghlqask
    port map (ab => pk, nymufz => hzzlaisi, eljhmeykn => kx, fsf => un);
  zxwuev : entity work.ghlqask
    port map (ab => gkt, nymufz => w, eljhmeykn => kx, fsf => kx);
  mltkncl : entity work.ghlqask
    port map (ab => iel, nymufz => e, eljhmeykn => kx, fsf => svuhdjr);
  
  -- Single-driven assignments
  jjocfw <= (others => 'v');
end cwtgbehx;

entity xjhxrg is
  port (dyptwad : out time);
end xjhxrg;

architecture guqphuq of xjhxrg is
  signal jeb : integer;
  signal xg : integer;
  signal vusnmx : severity_level;
  signal rlw : bit;
begin
  vwcbmocc : entity work.ghlqask
    port map (ab => rlw, nymufz => vusnmx, eljhmeykn => xg, fsf => jeb);
  
  -- Single-driven assignments
  dyptwad <= 8#0_4_7# ps;
  xg <= jeb;
end guqphuq;



-- Seed after: 614225778938137705,6299883410057943775
