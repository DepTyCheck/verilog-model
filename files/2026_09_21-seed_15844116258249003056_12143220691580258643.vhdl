-- Seed: 15844116258249003056,12143220691580258643

library ieee;
use ieee.std_logic_1164.all;

entity wqkg is
  port (m : out std_logic; voydzzinp : in std_logic; fltagrsi : buffer string(1 downto 2); ujmrlwsz : buffer integer_vector(2 downto 4));
end wqkg;

architecture nzfkrjo of wqkg is
  
begin
  -- Single-driven assignments
  ujmrlwsz <= ujmrlwsz;
  fltagrsi <= fltagrsi;
end nzfkrjo;

entity yqksu is
  port (m : buffer bit; bkjmncuw : out real; o : out character);
end yqksu;

library ieee;
use ieee.std_logic_1164.all;

architecture wmlmghjyj of yqksu is
  signal wmpv : integer_vector(2 downto 4);
  signal ua : string(1 downto 2);
  signal zvqduoiyu : std_logic;
  signal ceft : integer_vector(2 downto 4);
  signal pulsckvdnp : string(1 downto 2);
  signal fvdgdxdjgs : integer_vector(2 downto 4);
  signal irl : string(1 downto 2);
  signal cbfnvipem : std_logic;
  signal d : integer_vector(2 downto 4);
  signal my : string(1 downto 2);
  signal vpdq : std_logic;
  signal eja : std_logic;
begin
  mgutahphk : entity work.wqkg
    port map (m => eja, voydzzinp => vpdq, fltagrsi => my, ujmrlwsz => d);
  ynl : entity work.wqkg
    port map (m => cbfnvipem, voydzzinp => eja, fltagrsi => irl, ujmrlwsz => fvdgdxdjgs);
  stwfgkpfx : entity work.wqkg
    port map (m => eja, voydzzinp => eja, fltagrsi => pulsckvdnp, ujmrlwsz => ceft);
  jsyvflfj : entity work.wqkg
    port map (m => zvqduoiyu, voydzzinp => cbfnvipem, fltagrsi => ua, ujmrlwsz => wmpv);
  
  -- Multi-driven assignments
  eja <= eja;
  eja <= cbfnvipem;
  eja <= 'Z';
  vpdq <= eja;
end wmlmghjyj;



-- Seed after: 14426455539444004524,12143220691580258643
