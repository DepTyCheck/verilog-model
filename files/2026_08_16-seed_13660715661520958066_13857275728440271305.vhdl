-- Seed: 13660715661520958066,13857275728440271305

entity vifw is
  port (tgopofsyfr : linkage real);
end vifw;

architecture noy of vifw is
  
begin
  
end noy;

entity vhxerzrn is
  port (cff : linkage time);
end vhxerzrn;

architecture hcjkbkwcd of vhxerzrn is
  signal rru : real;
  signal jvqd : real;
  signal icufdwdzw : real;
  signal udsnacznoz : real;
begin
  topklmo : entity work.vifw
    port map (tgopofsyfr => udsnacznoz);
  wlejepm : entity work.vifw
    port map (tgopofsyfr => icufdwdzw);
  m : entity work.vifw
    port map (tgopofsyfr => jvqd);
  iyywsy : entity work.vifw
    port map (tgopofsyfr => rru);
end hcjkbkwcd;

library ieee;
use ieee.std_logic_1164.all;

entity ojdunezfr is
  port (eycjxsg : in severity_level; xhbcpo : linkage time; phvtx : linkage std_logic_vector(2 downto 4));
end ojdunezfr;

architecture u of ojdunezfr is
  signal ynp : real;
  signal iop : real;
begin
  htz : entity work.vifw
    port map (tgopofsyfr => iop);
  rov : entity work.vifw
    port map (tgopofsyfr => ynp);
end u;

library ieee;
use ieee.std_logic_1164.all;

entity hs is
  port (l : out std_logic; qknupgslyp : out real);
end hs;

architecture plezsgdt of hs is
  signal vdl : real;
begin
  hlxd : entity work.vifw
    port map (tgopofsyfr => vdl);
  ovnqr : entity work.vifw
    port map (tgopofsyfr => qknupgslyp);
end plezsgdt;



-- Seed after: 4959847984716117541,13857275728440271305
