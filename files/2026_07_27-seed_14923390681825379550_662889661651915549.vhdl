-- Seed: 14923390681825379550,662889661651915549

entity y is
  port (ptlpd : buffer time; kaenzyouz : inout time_vector(2 to 2); ws : out integer; j : out integer);
end y;

architecture qnnzzm of y is
  
begin
  
end qnnzzm;

library ieee;
use ieee.std_logic_1164.all;

entity fasniuauvb is
  port (pngzpguzfm : in std_logic);
end fasniuauvb;

architecture wz of fasniuauvb is
  signal uw : integer;
  signal mbproymzbv : integer;
  signal aqpnxy : time_vector(2 to 2);
  signal xvm : time;
  signal dwnpz : integer;
  signal fsrmeemqx : integer;
  signal lyq : time_vector(2 to 2);
  signal rdcxjnpm : time;
  signal x : integer;
  signal ralaonghqg : integer;
  signal aqdemyxyzk : time_vector(2 to 2);
  signal mepwft : time;
  signal ra : integer;
  signal sgqkacgx : integer;
  signal rhnap : time_vector(2 to 2);
  signal zi : time;
begin
  j : entity work.y
    port map (ptlpd => zi, kaenzyouz => rhnap, ws => sgqkacgx, j => ra);
  qbi : entity work.y
    port map (ptlpd => mepwft, kaenzyouz => aqdemyxyzk, ws => ralaonghqg, j => x);
  pkrtdzn : entity work.y
    port map (ptlpd => rdcxjnpm, kaenzyouz => lyq, ws => fsrmeemqx, j => dwnpz);
  io : entity work.y
    port map (ptlpd => xvm, kaenzyouz => aqpnxy, ws => mbproymzbv, j => uw);
end wz;



-- Seed after: 13019918673564526091,662889661651915549
