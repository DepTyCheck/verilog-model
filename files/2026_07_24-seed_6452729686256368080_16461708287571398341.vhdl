-- Seed: 6452729686256368080,16461708287571398341

entity pjspmyvndf is
  port (vz : inout time; dma : buffer bit_vector(3 to 0));
end pjspmyvndf;

architecture hhezfjrqo of pjspmyvndf is
  
begin
  -- Single-driven assignments
  vz <= 4_3_0_2_3 us;
  dma <= dma;
end hhezfjrqo;

entity zfoypdcr is
  port (xypr : out string(4 to 1); why : linkage integer; nrxo : inout time);
end zfoypdcr;

architecture xuotblyr of zfoypdcr is
  signal w : bit_vector(3 to 0);
  signal quopqte : time;
  signal g : bit_vector(3 to 0);
  signal jwydvs : time;
  signal ifs : bit_vector(3 to 0);
begin
  l : entity work.pjspmyvndf
    port map (vz => nrxo, dma => ifs);
  wzz : entity work.pjspmyvndf
    port map (vz => jwydvs, dma => g);
  bxisrws : entity work.pjspmyvndf
    port map (vz => quopqte, dma => w);
  
  -- Single-driven assignments
  xypr <= "";
end xuotblyr;

entity lqs is
  port (mmlhwf : buffer severity_level);
end lqs;

architecture loecectwxa of lqs is
  signal nmrqlqhu : bit_vector(3 to 0);
  signal x : time;
  signal mgd : bit_vector(3 to 0);
  signal hrz : time;
  signal afkufnju : bit_vector(3 to 0);
  signal wjwjk : time;
  signal xqppgh : bit_vector(3 to 0);
  signal gjr : time;
begin
  ornpsjkvph : entity work.pjspmyvndf
    port map (vz => gjr, dma => xqppgh);
  kf : entity work.pjspmyvndf
    port map (vz => wjwjk, dma => afkufnju);
  dhusqoq : entity work.pjspmyvndf
    port map (vz => hrz, dma => mgd);
  y : entity work.pjspmyvndf
    port map (vz => x, dma => nmrqlqhu);
  
  -- Single-driven assignments
  mmlhwf <= mmlhwf;
end loecectwxa;



-- Seed after: 11458388447123721245,16461708287571398341
