-- Seed: 6746366614430340675,10875537289884587119

entity bbcsn is
  port (nyp : out real; ewdhyv : inout time; obcrfan : out real; zcreon : inout integer_vector(1 downto 1));
end bbcsn;

architecture nzyv of bbcsn is
  
begin
  -- Single-driven assignments
  obcrfan <= obcrfan;
  nyp <= nyp;
  ewdhyv <= ewdhyv;
  zcreon <= (others => 2212);
end nzyv;

library ieee;
use ieee.std_logic_1164.all;

entity ahn is
  port (ic : in time; ceflqh : linkage boolean; ztozmocle : in std_logic_vector(1 downto 0));
end ahn;

architecture l of ahn is
  signal jenvsak : integer_vector(1 downto 1);
  signal xotkfeqe : real;
  signal bcxmqzeet : time;
  signal ooyvfvkm : real;
  signal o : integer_vector(1 downto 1);
  signal gfpvcv : real;
  signal swzcw : time;
  signal dqzhmgpn : real;
  signal ytgy : integer_vector(1 downto 1);
  signal stmuaf : real;
  signal hlsexhrw : time;
  signal xvhtdgdn : real;
  signal yaz : integer_vector(1 downto 1);
  signal nvk : real;
  signal kplmyk : time;
  signal rk : real;
begin
  fcxuciou : entity work.bbcsn
    port map (nyp => rk, ewdhyv => kplmyk, obcrfan => nvk, zcreon => yaz);
  qqvg : entity work.bbcsn
    port map (nyp => xvhtdgdn, ewdhyv => hlsexhrw, obcrfan => stmuaf, zcreon => ytgy);
  hiftepuq : entity work.bbcsn
    port map (nyp => dqzhmgpn, ewdhyv => swzcw, obcrfan => gfpvcv, zcreon => o);
  ffblfppt : entity work.bbcsn
    port map (nyp => ooyvfvkm, ewdhyv => bcxmqzeet, obcrfan => xotkfeqe, zcreon => jenvsak);
end l;

entity lwrav is
  port (mwplpkajam : in time; cuqmrhp : buffer time_vector(4 to 3); cesedxqupt : out time);
end lwrav;

architecture z of lwrav is
  signal fcx : integer_vector(1 downto 1);
  signal nxvhgza : real;
  signal eoos : time;
  signal hvqy : real;
  signal hxwsyf : integer_vector(1 downto 1);
  signal ychqfkiq : real;
  signal bvevxfhq : time;
  signal zhluixnya : real;
begin
  nm : entity work.bbcsn
    port map (nyp => zhluixnya, ewdhyv => bvevxfhq, obcrfan => ychqfkiq, zcreon => hxwsyf);
  zhio : entity work.bbcsn
    port map (nyp => hvqy, ewdhyv => eoos, obcrfan => nxvhgza, zcreon => fcx);
  
  -- Single-driven assignments
  cesedxqupt <= 16#1_2_1.DC0# ps;
end z;



-- Seed after: 14266859643249316198,10875537289884587119
