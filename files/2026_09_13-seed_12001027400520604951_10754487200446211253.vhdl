-- Seed: 12001027400520604951,10754487200446211253

library ieee;
use ieee.std_logic_1164.all;

entity cutu is
  port (mx : out real; lypxntlqdz : linkage std_logic_vector(0 to 4); zikhfdy : buffer real);
end cutu;

architecture qojx of cutu is
  
begin
  -- Single-driven assignments
  mx <= 0.2_4_3;
  zikhfdy <= zikhfdy;
end qojx;

entity peyjksszc is
  port (rpiownhv : buffer severity_level; vqm : buffer integer; ri : linkage real; ogq : in integer);
end peyjksszc;

architecture uxeyoretom of peyjksszc is
  
begin
  -- Single-driven assignments
  rpiownhv <= FAILURE;
  vqm <= 8#7461#;
end uxeyoretom;

entity ps is
  port (cyff : in time; hghfafghh : out severity_level; hpwrnr : in bit_vector(0 downto 3); bemydrwip : in boolean);
end ps;

library ieee;
use ieee.std_logic_1164.all;

architecture lk of ps is
  signal gipxgksym : integer;
  signal tswgpk : real;
  signal c : real;
  signal dzpacwh : real;
  signal jujicztch : integer;
  signal dvvivkau : real;
  signal ehm : integer;
  signal wpnrpcwwhr : severity_level;
  signal kyng : real;
  signal bhe : std_logic_vector(0 to 4);
  signal phvw : real;
begin
  tn : entity work.cutu
    port map (mx => phvw, lypxntlqdz => bhe, zikhfdy => kyng);
  dlzwkzz : entity work.peyjksszc
    port map (rpiownhv => wpnrpcwwhr, vqm => ehm, ri => dvvivkau, ogq => jujicztch);
  ivrrsencm : entity work.cutu
    port map (mx => dzpacwh, lypxntlqdz => bhe, zikhfdy => c);
  hwjsxnhye : entity work.peyjksszc
    port map (rpiownhv => hghfafghh, vqm => jujicztch, ri => tswgpk, ogq => gipxgksym);
  
  -- Single-driven assignments
  gipxgksym <= 8#0_5_4_5_2#;
  
  -- Multi-driven assignments
  bhe <= bhe;
end lk;

library ieee;
use ieee.std_logic_1164.all;

entity hklq is
  port (rjjjo : out std_logic);
end hklq;

library ieee;
use ieee.std_logic_1164.all;

architecture irwkqjkfj of hklq is
  signal f : integer;
  signal xrtaushrn : real;
  signal oxktusl : integer;
  signal uy : severity_level;
  signal lvmk : real;
  signal je : real;
  signal ffz : real;
  signal qxp : std_logic_vector(0 to 4);
  signal rxzdwke : real;
  signal waigoxbpeq : real;
  signal ncd : std_logic_vector(0 to 4);
  signal tiv : real;
begin
  ino : entity work.cutu
    port map (mx => tiv, lypxntlqdz => ncd, zikhfdy => waigoxbpeq);
  npfxgooqs : entity work.cutu
    port map (mx => rxzdwke, lypxntlqdz => qxp, zikhfdy => ffz);
  n : entity work.cutu
    port map (mx => je, lypxntlqdz => ncd, zikhfdy => lvmk);
  s : entity work.peyjksszc
    port map (rpiownhv => uy, vqm => oxktusl, ri => xrtaushrn, ogq => f);
  
  -- Single-driven assignments
  f <= 8#6_1#;
  
  -- Multi-driven assignments
  ncd <= ncd;
  rjjjo <= '-';
  ncd <= ncd;
end irwkqjkfj;



-- Seed after: 1190725418315696570,10754487200446211253
