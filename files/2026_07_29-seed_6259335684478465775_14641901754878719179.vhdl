-- Seed: 6259335684478465775,14641901754878719179

entity wkryyi is
  port (aiyt : in time; yg : linkage real; shgiambk : inout boolean_vector(4 to 0));
end wkryyi;

architecture xgfhwvmf of wkryyi is
  
begin
  -- Single-driven assignments
  shgiambk <= (others => TRUE);
end xgfhwvmf;

library ieee;
use ieee.std_logic_1164.all;

entity kljvendjzv is
  port (ud : out severity_level; salefphtqq : in time_vector(3 downto 0); vsdcmuao : out boolean_vector(2 downto 2); dbectwjrua : inout std_logic);
end kljvendjzv;

architecture c of kljvendjzv is
  signal fnwxrcadrl : boolean_vector(4 to 0);
  signal ysxdwsbxxx : real;
  signal nmqc : time;
  signal cnfbxdno : boolean_vector(4 to 0);
  signal tbzyiiwir : real;
  signal corki : boolean_vector(4 to 0);
  signal sbe : real;
  signal ogsexwncj : time;
begin
  up : entity work.wkryyi
    port map (aiyt => ogsexwncj, yg => sbe, shgiambk => corki);
  kxkgk : entity work.wkryyi
    port map (aiyt => ogsexwncj, yg => tbzyiiwir, shgiambk => cnfbxdno);
  op : entity work.wkryyi
    port map (aiyt => nmqc, yg => ysxdwsbxxx, shgiambk => fnwxrcadrl);
  
  -- Single-driven assignments
  vsdcmuao <= (others => FALSE);
end c;



-- Seed after: 15142002697015234280,14641901754878719179
