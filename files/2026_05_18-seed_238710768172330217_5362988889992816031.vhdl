-- Seed: 238710768172330217,5362988889992816031

library ieee;
use ieee.std_logic_1164.all;

entity lym is
  port (lpml : out std_logic; tg : out std_logic; q : inout real);
end lym;



architecture tfuozcy of lym is
  
begin
  
end tfuozcy;



entity fj is
  port (u : buffer integer);
end fj;

library ieee;
use ieee.std_logic_1164.all;

architecture iaclbeae of fj is
  signal kvi : real;
  signal dmh : std_logic;
  signal fsssck : real;
  signal nfuzafvo : real;
  signal lphg : std_logic;
  signal s : real;
  signal r : std_logic;
begin
  judrtcklm : entity work.lym
    port map (lpml => r, tg => r, q => s);
  kr : entity work.lym
    port map (lpml => lphg, tg => lphg, q => nfuzafvo);
  hmugz : entity work.lym
    port map (lpml => r, tg => r, q => fsssck);
  k : entity work.lym
    port map (lpml => dmh, tg => lphg, q => kvi);
end iaclbeae;



-- Seed after: 6819493042332181779,5362988889992816031
