-- Seed: 4461115972834751618,12269339630485015285

entity rorkwot is
  port (hju : out severity_level; q : linkage real);
end rorkwot;

architecture jkqaij of rorkwot is
  
begin
  -- Single-driven assignments
  hju <= FAILURE;
end jkqaij;

entity xipwvdefx is
  port (bqiblmcdyr : buffer bit; pa : in character);
end xipwvdefx;

architecture jqsgsxr of xipwvdefx is
  signal danlexmwho : real;
  signal t : severity_level;
  signal s : real;
  signal kokhmpxc : severity_level;
begin
  hubm : entity work.rorkwot
    port map (hju => kokhmpxc, q => s);
  c : entity work.rorkwot
    port map (hju => t, q => danlexmwho);
  
  -- Single-driven assignments
  bqiblmcdyr <= bqiblmcdyr;
end jqsgsxr;

entity clif is
  port (vnpokzwkd : linkage bit_vector(0 to 1); pkqboksb : out integer);
end clif;

architecture bcggrm of clif is
  signal slhddbd : real;
  signal yypmgnsrq : severity_level;
  signal ukt : real;
  signal qemfuxbfx : severity_level;
  signal ys : character;
  signal gf : bit;
begin
  ltirv : entity work.xipwvdefx
    port map (bqiblmcdyr => gf, pa => ys);
  k : entity work.rorkwot
    port map (hju => qemfuxbfx, q => ukt);
  tedebw : entity work.rorkwot
    port map (hju => yypmgnsrq, q => slhddbd);
end bcggrm;

library ieee;
use ieee.std_logic_1164.all;

entity xnnbyxu is
  port (qf : buffer std_logic; hqezdqtv : linkage time; xemwoqwv : in integer; anbz : out boolean_vector(3 to 2));
end xnnbyxu;

architecture ogpwryf of xnnbyxu is
  signal onqsr : real;
  signal myomhsbbg : severity_level;
  signal wycmqv : real;
  signal trjgm : severity_level;
  signal aodzkuzm : character;
  signal xohlovqif : bit;
  signal iyiemwtvpp : integer;
  signal el : bit_vector(0 to 1);
begin
  pcwvcwaor : entity work.clif
    port map (vnpokzwkd => el, pkqboksb => iyiemwtvpp);
  r : entity work.xipwvdefx
    port map (bqiblmcdyr => xohlovqif, pa => aodzkuzm);
  xezmdw : entity work.rorkwot
    port map (hju => trjgm, q => wycmqv);
  givx : entity work.rorkwot
    port map (hju => myomhsbbg, q => onqsr);
  
  -- Multi-driven assignments
  qf <= qf;
end ogpwryf;



-- Seed after: 1203227768565305809,12269339630485015285
