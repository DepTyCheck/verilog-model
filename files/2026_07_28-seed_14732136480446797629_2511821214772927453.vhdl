-- Seed: 14732136480446797629,2511821214772927453

entity gbls is
  port (rn : linkage character; rkc : linkage real; dwankmqalh : in integer);
end gbls;

architecture ueh of gbls is
  
begin
  
end ueh;

entity bbfqezqa is
  port (dtytv : out time; mpxyyqt : in bit; kns : buffer time);
end bbfqezqa;

architecture ctgcrtamq of bbfqezqa is
  signal h : real;
  signal ebq : character;
  signal kwcvay : integer;
  signal yuwssjy : real;
  signal k : character;
  signal vnewp : real;
  signal waa : character;
  signal gwholt : integer;
  signal dlqsgtqcsv : real;
  signal tkjctarf : character;
begin
  kjwfc : entity work.gbls
    port map (rn => tkjctarf, rkc => dlqsgtqcsv, dwankmqalh => gwholt);
  koc : entity work.gbls
    port map (rn => waa, rkc => vnewp, dwankmqalh => gwholt);
  mjwtaie : entity work.gbls
    port map (rn => k, rkc => yuwssjy, dwankmqalh => kwcvay);
  dondyl : entity work.gbls
    port map (rn => ebq, rkc => h, dwankmqalh => kwcvay);
  
  -- Single-driven assignments
  kns <= 2#01000.0_0_1_0# fs;
  kwcvay <= gwholt;
  gwholt <= gwholt;
  dtytv <= 4.2_0_1_1 ps;
end ctgcrtamq;

library ieee;
use ieee.std_logic_1164.all;

entity nwhapkrrh is
  port (pwdjyjfdkb : in std_logic; ldzh : linkage real);
end nwhapkrrh;

architecture a of nwhapkrrh is
  signal kylao : integer;
  signal yy : real;
  signal zljqsxmwan : character;
begin
  pclyd : entity work.gbls
    port map (rn => zljqsxmwan, rkc => yy, dwankmqalh => kylao);
  
  -- Single-driven assignments
  kylao <= kylao;
end a;



-- Seed after: 10268722610162693407,2511821214772927453
