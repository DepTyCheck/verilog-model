-- Seed: 16699226924634268382,2511821214772927453

library ieee;
use ieee.std_logic_1164.all;

entity gj is
  port (q : in std_logic_vector(1 downto 3); evcrol : linkage real; thm : inout std_logic_vector(3 to 1));
end gj;

architecture xszbxsngnv of gj is
  
begin
  -- Multi-driven assignments
  thm <= "";
  thm <= thm;
  thm <= "";
  thm <= (others => '0');
end xszbxsngnv;

library ieee;
use ieee.std_logic_1164.all;

entity mlpeuilt is
  port (tnohnrg : out time; dszs : out std_logic_vector(4 to 3));
end mlpeuilt;

library ieee;
use ieee.std_logic_1164.all;

architecture bbmtkb of mlpeuilt is
  signal akj : real;
  signal egviukmmi : std_logic_vector(1 downto 3);
  signal ehhcslsb : real;
  signal vkcd : std_logic_vector(1 downto 3);
begin
  ynac : entity work.gj
    port map (q => vkcd, evcrol => ehhcslsb, thm => dszs);
  vnjrdhyb : entity work.gj
    port map (q => egviukmmi, evcrol => akj, thm => dszs);
  
  -- Multi-driven assignments
  egviukmmi <= "";
  vkcd <= dszs;
  dszs <= (others => '0');
  dszs <= (others => '0');
end bbmtkb;

entity eaoxydra is
  port (urkvhuppkx : inout integer; muqeulabmw : in time; sx : linkage character);
end eaoxydra;

library ieee;
use ieee.std_logic_1164.all;

architecture nd of eaoxydra is
  signal txuxxt : real;
  signal owjfrqi : std_logic_vector(3 to 1);
  signal epujaev : real;
  signal nbn : std_logic_vector(3 to 1);
begin
  kw : entity work.gj
    port map (q => nbn, evcrol => epujaev, thm => owjfrqi);
  ekp : entity work.gj
    port map (q => nbn, evcrol => txuxxt, thm => nbn);
  
  -- Single-driven assignments
  urkvhuppkx <= 4_3;
  
  -- Multi-driven assignments
  nbn <= nbn;
  nbn <= owjfrqi;
end nd;

library ieee;
use ieee.std_logic_1164.all;

entity zw is
  port (hjwdjosa : buffer integer; d : linkage std_logic_vector(0 to 3));
end zw;

library ieee;
use ieee.std_logic_1164.all;

architecture oqnkxj of zw is
  signal nh : std_logic_vector(4 to 3);
  signal cw : time;
  signal m : real;
  signal delxzoh : std_logic_vector(3 to 1);
begin
  le : entity work.gj
    port map (q => delxzoh, evcrol => m, thm => delxzoh);
  wwdbvnqp : entity work.mlpeuilt
    port map (tnohnrg => cw, dszs => nh);
  
  -- Single-driven assignments
  hjwdjosa <= hjwdjosa;
  
  -- Multi-driven assignments
  delxzoh <= (others => '0');
  nh <= "";
end oqnkxj;



-- Seed after: 8914028878646081327,2511821214772927453
