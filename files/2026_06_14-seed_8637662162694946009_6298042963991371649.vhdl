-- Seed: 8637662162694946009,6298042963991371649

library ieee;
use ieee.std_logic_1164.all;

entity zuhew is
  port (bzqeitlr : out severity_level; dgmrn : buffer std_logic_vector(0 downto 1); tplppl : out integer; zydrcake : inout real);
end zuhew;



architecture rrdmfgywwl of zuhew is
  
begin
  
end rrdmfgywwl;

library ieee;
use ieee.std_logic_1164.all;

entity c is
  port (wjhpl : inout time; tujwrdeq : buffer bit; fcyt : linkage std_logic_vector(1 downto 0));
end c;

library ieee;
use ieee.std_logic_1164.all;

architecture qqmhrod of c is
  signal kasys : real;
  signal jllqi : integer;
  signal xxxtxaxzmk : std_logic_vector(0 downto 1);
  signal qngtpb : severity_level;
  signal fdehze : real;
  signal imotj : integer;
  signal pct : severity_level;
  signal surjebjkii : real;
  signal ixjgjdpb : integer;
  signal hlic : std_logic_vector(0 downto 1);
  signal it : severity_level;
  signal fexhidm : real;
  signal m : integer;
  signal arcshh : std_logic_vector(0 downto 1);
  signal zxmy : severity_level;
begin
  bnfbhh : entity work.zuhew
    port map (bzqeitlr => zxmy, dgmrn => arcshh, tplppl => m, zydrcake => fexhidm);
  j : entity work.zuhew
    port map (bzqeitlr => it, dgmrn => hlic, tplppl => ixjgjdpb, zydrcake => surjebjkii);
  zriw : entity work.zuhew
    port map (bzqeitlr => pct, dgmrn => arcshh, tplppl => imotj, zydrcake => fdehze);
  foyyddqy : entity work.zuhew
    port map (bzqeitlr => qngtpb, dgmrn => xxxtxaxzmk, tplppl => jllqi, zydrcake => kasys);
end qqmhrod;



-- Seed after: 6287662826266097739,6298042963991371649
