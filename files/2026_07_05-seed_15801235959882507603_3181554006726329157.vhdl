-- Seed: 15801235959882507603,3181554006726329157

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity glpp is
  port (mjbfgbre : inout floating_subtype_mirror; np : linkage std_logic; ajhs : inout access_value_mirror);
end glpp;

architecture nmdjk of glpp is
  
begin
  
end nmdjk;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity y is
  port (bmqwadduim : inout physical_value_mirror; fcfgwxrdyx : in time; ghz : in std_logic);
end y;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture iovy of y is
  shared variable zcja : access_value_mirror;
  signal gm : std_logic;
  shared variable jw : floating_subtype_mirror;
  shared variable bdomt : access_value_mirror;
  shared variable jsvz : floating_subtype_mirror;
  shared variable l : access_value_mirror;
  shared variable wbridz : floating_subtype_mirror;
begin
  xv : entity work.glpp
    port map (mjbfgbre => wbridz, np => ghz, ajhs => l);
  j : entity work.glpp
    port map (mjbfgbre => jsvz, np => ghz, ajhs => bdomt);
  evwnogj : entity work.glpp
    port map (mjbfgbre => jw, np => gm, ajhs => zcja);
  
  -- Multi-driven assignments
  gm <= 'X';
  gm <= ghz;
  gm <= '-';
  gm <= 'Z';
end iovy;



-- Seed after: 3670647498328472185,3181554006726329157
