-- Seed: 11457888661906421340,3181554006726329157

entity orlorvax is
  port (al : in time);
end orlorvax;

architecture cqbwuufe of orlorvax is
  
begin
  
end cqbwuufe;

library ieee;
use ieee.std_logic_1164.all;

entity rkqaqckis is
  port (zfuax : inout std_logic_vector(0 downto 2); kvyfim : inout bit_vector(0 to 4));
end rkqaqckis;

architecture kn of rkqaqckis is
  signal oaaql : time;
  signal cifsfdns : time;
  signal otg : time;
begin
  ep : entity work.orlorvax
    port map (al => otg);
  ythk : entity work.orlorvax
    port map (al => cifsfdns);
  lajwllblyy : entity work.orlorvax
    port map (al => oaaql);
  nqycum : entity work.orlorvax
    port map (al => otg);
  
  -- Single-driven assignments
  kvyfim <= kvyfim;
  cifsfdns <= 2 sec;
  
  -- Multi-driven assignments
  zfuax <= zfuax;
  zfuax <= zfuax;
end kn;

use std.reflection.all;

entity ppliz is
  port (bpegodnl : inout enumeration_value_mirror; cxc : inout bit);
end ppliz;

library ieee;
use ieee.std_logic_1164.all;

architecture onevdrzsn of ppliz is
  signal xwwb : time;
  signal lykakuqy : bit_vector(0 to 4);
  signal rh : std_logic_vector(0 downto 2);
begin
  hqusjkxdx : entity work.rkqaqckis
    port map (zfuax => rh, kvyfim => lykakuqy);
  ozoo : entity work.orlorvax
    port map (al => xwwb);
  erkxwzc : entity work.orlorvax
    port map (al => xwwb);
  oqwlvhq : entity work.orlorvax
    port map (al => xwwb);
  
  -- Single-driven assignments
  xwwb <= xwwb;
  cxc <= '1';
  
  -- Multi-driven assignments
  rh <= rh;
  rh <= rh;
end onevdrzsn;



-- Seed after: 17868100707459739698,3181554006726329157
