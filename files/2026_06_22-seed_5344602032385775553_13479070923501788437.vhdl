-- Seed: 5344602032385775553,13479070923501788437

library ieee;
use ieee.std_logic_1164.all;

entity tca is
  port (pngcfx : out bit; vrchs : buffer time; ggs : out std_logic_vector(2 to 2); v : out std_logic);
end tca;

architecture xuy of tca is
  
begin
  -- Multi-driven assignments
  v <= 'Z';
  v <= 'U';
end xuy;

library ieee;
use ieee.std_logic_1164.all;

entity ufns is
  port (rloojresz : inout std_logic; mbjbwtfbpt : inout time; rba : out real; kiadtepmn : inout real);
end ufns;

library ieee;
use ieee.std_logic_1164.all;

architecture znjymhox of ufns is
  signal mbvvajtz : time;
  signal hxvzc : bit;
  signal a : std_logic;
  signal qqqykfiaz : bit;
  signal lg : std_logic_vector(2 to 2);
  signal lzkfoas : time;
  signal b : bit;
  signal yvpvydi : std_logic_vector(2 to 2);
  signal ygegp : time;
  signal fvnhvo : bit;
begin
  u : entity work.tca
    port map (pngcfx => fvnhvo, vrchs => ygegp, ggs => yvpvydi, v => rloojresz);
  vjbuuwjb : entity work.tca
    port map (pngcfx => b, vrchs => lzkfoas, ggs => lg, v => rloojresz);
  omifl : entity work.tca
    port map (pngcfx => qqqykfiaz, vrchs => mbjbwtfbpt, ggs => yvpvydi, v => a);
  evtqvt : entity work.tca
    port map (pngcfx => hxvzc, vrchs => mbvvajtz, ggs => yvpvydi, v => rloojresz);
  
  -- Multi-driven assignments
  rloojresz <= 'W';
  yvpvydi <= (others => 'U');
end znjymhox;

library ieee;
use ieee.std_logic_1164.all;

entity uqhbzelnej is
  port (gn : linkage integer; r : out std_logic);
end uqhbzelnej;

library ieee;
use ieee.std_logic_1164.all;

architecture mtyxqypd of uqhbzelnej is
  signal vvzzw : std_logic_vector(2 to 2);
  signal g : time;
  signal qqnzjnlu : bit;
  signal gefkzhzm : std_logic_vector(2 to 2);
  signal ihdzy : time;
  signal wxnkqhcbc : bit;
begin
  hhvqgdmus : entity work.tca
    port map (pngcfx => wxnkqhcbc, vrchs => ihdzy, ggs => gefkzhzm, v => r);
  fqbavpmauw : entity work.tca
    port map (pngcfx => qqnzjnlu, vrchs => g, ggs => vvzzw, v => r);
  
  -- Multi-driven assignments
  vvzzw <= (others => 'L');
  gefkzhzm <= (others => 'L');
end mtyxqypd;

entity cabi is
  port (dutmsveor : linkage real; fabxa : linkage integer; qgx : linkage bit; xr : linkage character);
end cabi;

architecture hl of cabi is
  
begin
  
end hl;



-- Seed after: 8345987780227279537,13479070923501788437
