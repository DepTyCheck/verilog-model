-- Seed: 18186644337509004139,16188444798499499427

entity gh is
  port (y : inout real);
end gh;

architecture voeyyccek of gh is
  
begin
  -- Single-driven assignments
  y <= y;
end voeyyccek;

entity dfg is
  port (wyoysqem : buffer integer);
end dfg;

architecture ngtemj of dfg is
  signal bzyekg : real;
  signal vn : real;
  signal zhzqzfjike : real;
  signal fofyvqmyjk : real;
begin
  xlct : entity work.gh
    port map (y => fofyvqmyjk);
  eqgswjdtf : entity work.gh
    port map (y => zhzqzfjike);
  pvcvwjzkjr : entity work.gh
    port map (y => vn);
  vzzl : entity work.gh
    port map (y => bzyekg);
end ngtemj;

entity uv is
  port (qqiwnkkk : buffer real_vector(4 to 2); wq : buffer integer);
end uv;

architecture w of uv is
  signal ckgl : real;
  signal upiozkq : real;
  signal eyilbid : real;
  signal cvrsuc : real;
begin
  wtn : entity work.gh
    port map (y => cvrsuc);
  tlcefy : entity work.gh
    port map (y => eyilbid);
  e : entity work.gh
    port map (y => upiozkq);
  zfrqdsyl : entity work.gh
    port map (y => ckgl);
  
  -- Single-driven assignments
  qqiwnkkk <= qqiwnkkk;
  wq <= wq;
end w;



-- Seed after: 16885287174365002766,16188444798499499427
