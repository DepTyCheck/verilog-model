-- Seed: 1494219366123121064,12359743974512393525

library ieee;
use ieee.std_logic_1164.all;

entity rhicg is
  port (aj : linkage string(2 downto 1); cjxourng : buffer real_vector(2 to 3); mzeudtminm : linkage time; zgucknml : buffer std_logic);
end rhicg;

architecture uivhtc of rhicg is
  
begin
  -- Single-driven assignments
  cjxourng <= cjxourng;
  
  -- Multi-driven assignments
  zgucknml <= zgucknml;
  zgucknml <= 'Z';
  zgucknml <= 'X';
end uivhtc;

entity xlawpruige is
  port (mqiooorc : linkage severity_level; yhtfhpng : buffer severity_level; ubvwa : inout boolean_vector(1 downto 3));
end xlawpruige;

architecture nkkg of xlawpruige is
  
begin
  -- Single-driven assignments
  yhtfhpng <= WARNING;
  ubvwa <= ubvwa;
end nkkg;

entity js is
  port (ckqr : inout time);
end js;

library ieee;
use ieee.std_logic_1164.all;

architecture hatpzz of js is
  signal ebrhg : time;
  signal fiounw : real_vector(2 to 3);
  signal iks : string(2 downto 1);
  signal ff : std_logic;
  signal wsoliczqc : real_vector(2 to 3);
  signal c : string(2 downto 1);
  signal iji : std_logic;
  signal hfjbxmct : time;
  signal iderjf : real_vector(2 to 3);
  signal mbp : string(2 downto 1);
begin
  dzslpzdpg : entity work.rhicg
    port map (aj => mbp, cjxourng => iderjf, mzeudtminm => hfjbxmct, zgucknml => iji);
  zgjjqw : entity work.rhicg
    port map (aj => c, cjxourng => wsoliczqc, mzeudtminm => ckqr, zgucknml => ff);
  ywybulpmhz : entity work.rhicg
    port map (aj => iks, cjxourng => fiounw, mzeudtminm => ebrhg, zgucknml => iji);
  
  -- Multi-driven assignments
  iji <= iji;
  iji <= iji;
  iji <= iji;
  iji <= iji;
end hatpzz;



-- Seed after: 7122553032229480355,12359743974512393525
