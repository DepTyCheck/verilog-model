-- Seed: 7504486600069134656,1112937151005418631

entity pxboi is
  port (daxzbiik : linkage integer_vector(3 downto 0); o : linkage character; qdikr : out bit_vector(3 to 2));
end pxboi;

architecture f of pxboi is
  
begin
  -- Single-driven assignments
  qdikr <= (others => '0');
end f;

entity avjqd is
  port (de : inout character);
end avjqd;

architecture dev of avjqd is
  signal qlyn : bit_vector(3 to 2);
  signal vqzogsxd : character;
  signal puzhznuul : integer_vector(3 downto 0);
  signal q : bit_vector(3 to 2);
  signal emw : character;
  signal ugidndowsg : integer_vector(3 downto 0);
  signal agxnm : bit_vector(3 to 2);
  signal psordsiw : character;
  signal fczbgrlyq : integer_vector(3 downto 0);
begin
  memkrpo : entity work.pxboi
    port map (daxzbiik => fczbgrlyq, o => psordsiw, qdikr => agxnm);
  sggpo : entity work.pxboi
    port map (daxzbiik => ugidndowsg, o => emw, qdikr => q);
  viofghe : entity work.pxboi
    port map (daxzbiik => puzhznuul, o => vqzogsxd, qdikr => qlyn);
  
  -- Single-driven assignments
  de <= de;
end dev;



-- Seed after: 6997211117934595475,1112937151005418631
