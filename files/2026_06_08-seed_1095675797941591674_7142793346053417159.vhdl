-- Seed: 1095675797941591674,7142793346053417159

library ieee;
use ieee.std_logic_1164.all;

entity cvwfsefzzi is
  port (mxbpmcacg : buffer std_logic; imz : out bit_vector(4 to 0); lsrcffpzd : out time; m : linkage boolean_vector(0 downto 3));
end cvwfsefzzi;



architecture rd of cvwfsefzzi is
  
begin
  
end rd;



entity abwxzltg is
  port (nnoarpgyd : inout bit; podm : buffer boolean; jvo : in integer; apsbkygffg : linkage bit_vector(1 to 2));
end abwxzltg;

library ieee;
use ieee.std_logic_1164.all;

architecture oqbe of abwxzltg is
  signal ndw : time;
  signal dbfu : bit_vector(4 to 0);
  signal vhsxxdfobo : time;
  signal jyzmclxlx : bit_vector(4 to 0);
  signal hvotca : std_logic;
  signal iezymqzu : boolean_vector(0 downto 3);
  signal y : time;
  signal zrawo : bit_vector(4 to 0);
  signal bwboojvo : std_logic;
begin
  zssjf : entity work.cvwfsefzzi
    port map (mxbpmcacg => bwboojvo, imz => zrawo, lsrcffpzd => y, m => iezymqzu);
  nacxk : entity work.cvwfsefzzi
    port map (mxbpmcacg => hvotca, imz => jyzmclxlx, lsrcffpzd => vhsxxdfobo, m => iezymqzu);
  gisruyx : entity work.cvwfsefzzi
    port map (mxbpmcacg => bwboojvo, imz => dbfu, lsrcffpzd => ndw, m => iezymqzu);
end oqbe;

library ieee;
use ieee.std_logic_1164.all;

entity elagqa is
  port (xybiozvzyu : in std_logic_vector(3 to 2));
end elagqa;

library ieee;
use ieee.std_logic_1164.all;

architecture y of elagqa is
  signal blrmcj : time;
  signal ueqctp : bit_vector(4 to 0);
  signal kgq : boolean_vector(0 downto 3);
  signal tydexq : time;
  signal euh : bit_vector(4 to 0);
  signal zuvhxtv : std_logic;
begin
  hqcwmdimie : entity work.cvwfsefzzi
    port map (mxbpmcacg => zuvhxtv, imz => euh, lsrcffpzd => tydexq, m => kgq);
  v : entity work.cvwfsefzzi
    port map (mxbpmcacg => zuvhxtv, imz => ueqctp, lsrcffpzd => blrmcj, m => kgq);
end y;



entity jisv is
  port (gvyjr : out severity_level; rkdtt : in boolean_vector(0 to 1));
end jisv;



architecture qo of jisv is
  
begin
  
end qo;



-- Seed after: 864811297467248503,7142793346053417159
