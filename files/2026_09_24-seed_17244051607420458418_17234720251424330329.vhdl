-- Seed: 17244051607420458418,17234720251424330329

library ieee;
use ieee.std_logic_1164.all;

entity ehmvd is
  port (ctmhppw : inout time; bp : buffer character; cpfzuv : inout std_logic);
end ehmvd;

architecture qwmrltc of ehmvd is
  
begin
  -- Single-driven assignments
  bp <= 'o';
  ctmhppw <= ctmhppw;
  
  -- Multi-driven assignments
  cpfzuv <= cpfzuv;
  cpfzuv <= '1';
  cpfzuv <= cpfzuv;
end qwmrltc;

entity snk is
  port (d : linkage string(5 downto 3));
end snk;

library ieee;
use ieee.std_logic_1164.all;

architecture xvmbe of snk is
  signal olckejtyvi : std_logic;
  signal ocimnpgguz : character;
  signal ehhhjzyyl : time;
  signal ixtpx : character;
  signal mvjlas : time;
  signal ueacyi : character;
  signal qbswiqp : time;
  signal g : std_logic;
  signal yavighzd : character;
  signal jzjfxwv : time;
begin
  gidicuor : entity work.ehmvd
    port map (ctmhppw => jzjfxwv, bp => yavighzd, cpfzuv => g);
  kykw : entity work.ehmvd
    port map (ctmhppw => qbswiqp, bp => ueacyi, cpfzuv => g);
  s : entity work.ehmvd
    port map (ctmhppw => mvjlas, bp => ixtpx, cpfzuv => g);
  xxprb : entity work.ehmvd
    port map (ctmhppw => ehhhjzyyl, bp => ocimnpgguz, cpfzuv => olckejtyvi);
end xvmbe;

entity av is
  port (riehy : out real; dkumrsep : linkage integer; f : buffer string(5 to 3));
end av;

library ieee;
use ieee.std_logic_1164.all;

architecture fbaadwvmku of av is
  signal ody : std_logic;
  signal cf : character;
  signal otzslxicn : time;
begin
  vupbtvqioo : entity work.ehmvd
    port map (ctmhppw => otzslxicn, bp => cf, cpfzuv => ody);
  
  -- Multi-driven assignments
  ody <= ody;
  ody <= ody;
  ody <= '1';
end fbaadwvmku;

library ieee;
use ieee.std_logic_1164.all;

entity jekc is
  port (dt : in std_logic_vector(3 downto 4); owbnunjb : linkage std_logic_vector(2 downto 4); spou : inout boolean; sjnzqpdrsn : in integer);
end jekc;

library ieee;
use ieee.std_logic_1164.all;

architecture spujj of jekc is
  signal salsfzq : character;
  signal gegy : time;
  signal xizliivvl : string(5 to 3);
  signal xwokiemgpg : integer;
  signal ujejizg : real;
  signal fl : character;
  signal hnymoqgon : time;
  signal grpzk : std_logic;
  signal romb : character;
  signal jnzspvo : time;
begin
  blmuad : entity work.ehmvd
    port map (ctmhppw => jnzspvo, bp => romb, cpfzuv => grpzk);
  ovz : entity work.ehmvd
    port map (ctmhppw => hnymoqgon, bp => fl, cpfzuv => grpzk);
  psovkdn : entity work.av
    port map (riehy => ujejizg, dkumrsep => xwokiemgpg, f => xizliivvl);
  lcqqqlnsc : entity work.ehmvd
    port map (ctmhppw => gegy, bp => salsfzq, cpfzuv => grpzk);
  
  -- Single-driven assignments
  spou <= FALSE;
  
  -- Multi-driven assignments
  grpzk <= grpzk;
  grpzk <= grpzk;
  grpzk <= grpzk;
end spujj;



-- Seed after: 6008409312231714294,17234720251424330329
