-- Seed: 16327587891756175908,662889661651915549

entity ifznp is
  port (m : inout integer; d : in time_vector(4 downto 1));
end ifznp;

architecture bjxveb of ifznp is
  
begin
  -- Single-driven assignments
  m <= m;
end bjxveb;

entity kqoeeeqtys is
  port (yiag : out character; mhoccsnw : buffer time; raex : out integer_vector(0 downto 3); v : in real);
end kqoeeeqtys;

architecture ivihsdaqqq of kqoeeeqtys is
  
begin
  -- Single-driven assignments
  raex <= (others => 0);
  mhoccsnw <= mhoccsnw;
end ivihsdaqqq;

library ieee;
use ieee.std_logic_1164.all;

entity gsnevxts is
  port (tljm : linkage real_vector(2 to 1); yza : buffer std_logic; ucrfv : inout severity_level);
end gsnevxts;

architecture oyrzwpftb of gsnevxts is
  signal kev : real;
  signal ooruwtgvwa : integer_vector(0 downto 3);
  signal va : time;
  signal h : character;
  signal bcnpnba : real;
  signal z : integer_vector(0 downto 3);
  signal vrdwyvk : time;
  signal lnsyicg : character;
  signal bj : integer;
  signal jaqp : time_vector(4 downto 1);
  signal ghszcluto : integer;
begin
  npbfzjgctd : entity work.ifznp
    port map (m => ghszcluto, d => jaqp);
  er : entity work.ifznp
    port map (m => bj, d => jaqp);
  qqqtieq : entity work.kqoeeeqtys
    port map (yiag => lnsyicg, mhoccsnw => vrdwyvk, raex => z, v => bcnpnba);
  zxoocpfe : entity work.kqoeeeqtys
    port map (yiag => h, mhoccsnw => va, raex => ooruwtgvwa, v => kev);
  
  -- Single-driven assignments
  kev <= kev;
end oyrzwpftb;



-- Seed after: 6539388288991245643,662889661651915549
