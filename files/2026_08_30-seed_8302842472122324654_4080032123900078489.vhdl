-- Seed: 8302842472122324654,4080032123900078489

entity mzaguxw is
  port (cyk : out integer_vector(2 to 2); wyo : out time);
end mzaguxw;

architecture qnt of mzaguxw is
  
begin
  -- Single-driven assignments
  cyk <= cyk;
  wyo <= wyo;
end qnt;

library ieee;
use ieee.std_logic_1164.all;

entity thge is
  port (jb : linkage std_logic_vector(2 to 1); wa : linkage integer; ql : inout character);
end thge;

architecture sougvvy of thge is
  signal fn : time;
  signal cxnt : integer_vector(2 to 2);
  signal aleswjxtay : time;
  signal rnsahsbz : integer_vector(2 to 2);
  signal sodgg : time;
  signal xtcfbr : integer_vector(2 to 2);
begin
  uicsb : entity work.mzaguxw
    port map (cyk => xtcfbr, wyo => sodgg);
  xydmoocxm : entity work.mzaguxw
    port map (cyk => rnsahsbz, wyo => aleswjxtay);
  maz : entity work.mzaguxw
    port map (cyk => cxnt, wyo => fn);
  
  -- Single-driven assignments
  ql <= ql;
end sougvvy;



-- Seed after: 10782138905670090663,4080032123900078489
