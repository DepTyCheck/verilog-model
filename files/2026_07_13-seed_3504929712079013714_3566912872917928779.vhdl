-- Seed: 3504929712079013714,3566912872917928779

use std.reflection.all;

entity wis is
  port (c : inout record_subtype_mirror; tvamfviko : inout subtype_mirror);
end wis;

architecture qsufmapkq of wis is
  
begin
  
end qsufmapkq;

entity xkh is
  port (kbjynwsd : buffer string(5 to 4); oorq : inout integer);
end xkh;

use std.reflection.all;

architecture ijhek of xkh is
  shared variable x : subtype_mirror;
  shared variable dqyuz : record_subtype_mirror;
  shared variable wvrqb : subtype_mirror;
  shared variable vbfpdmojqo : record_subtype_mirror;
  shared variable jbekgphs : subtype_mirror;
  shared variable mrd : record_subtype_mirror;
begin
  t : entity work.wis
    port map (c => mrd, tvamfviko => jbekgphs);
  vohplfkg : entity work.wis
    port map (c => vbfpdmojqo, tvamfviko => wvrqb);
  bepmxmv : entity work.wis
    port map (c => dqyuz, tvamfviko => x);
end ijhek;

use std.reflection.all;

entity kktzbwfcpa is
  port (poqenqv : inout access_value_mirror; h : inout physical_value_mirror);
end kktzbwfcpa;

use std.reflection.all;

architecture ukgqj of kktzbwfcpa is
  signal nvmygm : integer;
  signal atedlkl : string(5 to 4);
  shared variable vjng : subtype_mirror;
  shared variable ixbpdoyszg : record_subtype_mirror;
  shared variable gmgyuqaxdk : subtype_mirror;
  shared variable dkx : record_subtype_mirror;
  shared variable hi : subtype_mirror;
  shared variable ojqfxjjuf : record_subtype_mirror;
begin
  rlt : entity work.wis
    port map (c => ojqfxjjuf, tvamfviko => hi);
  akrs : entity work.wis
    port map (c => dkx, tvamfviko => gmgyuqaxdk);
  fnasqbiopf : entity work.wis
    port map (c => ixbpdoyszg, tvamfviko => vjng);
  lrqyxlz : entity work.xkh
    port map (kbjynwsd => atedlkl, oorq => nvmygm);
end ukgqj;



-- Seed after: 14200381136212480398,3566912872917928779
