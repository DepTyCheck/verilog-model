-- Seed: 3110779741917172,14629254427735353553

entity qkqcr is
  port (xvwtlmu : buffer time; l : linkage real; d : out integer; fcwciimp : in integer_vector(3 downto 1));
end qkqcr;

architecture hgtthmxm of qkqcr is
  
begin
  -- Single-driven assignments
  d <= 024;
  xvwtlmu <= 33142.4_4_2_3_3 ps;
end hgtthmxm;

entity nexkgd is
  port (ysdddibyci : linkage time; tegcv : linkage character; qa : in boolean_vector(4 to 1));
end nexkgd;

architecture zcy of nexkgd is
  signal wv : integer_vector(3 downto 1);
  signal vjum : integer;
  signal obzanqhgz : real;
  signal lxupzub : time;
  signal ivsc : integer;
  signal ss : real;
  signal xipox : time;
  signal eiytnl : integer;
  signal kfutsn : real;
  signal mipgnosxco : time;
  signal jsg : integer_vector(3 downto 1);
  signal xu : integer;
  signal pajjhgzmr : real;
  signal vifw : time;
begin
  jarhbyxzq : entity work.qkqcr
    port map (xvwtlmu => vifw, l => pajjhgzmr, d => xu, fcwciimp => jsg);
  ufw : entity work.qkqcr
    port map (xvwtlmu => mipgnosxco, l => kfutsn, d => eiytnl, fcwciimp => jsg);
  a : entity work.qkqcr
    port map (xvwtlmu => xipox, l => ss, d => ivsc, fcwciimp => jsg);
  yryoaqz : entity work.qkqcr
    port map (xvwtlmu => lxupzub, l => obzanqhgz, d => vjum, fcwciimp => wv);
  
  -- Single-driven assignments
  jsg <= (16#7_B#, 2#1#, 3_1_2);
end zcy;

entity nogr is
  port (yxoehfstq : linkage time; nxioo : linkage real; zbz : out real);
end nogr;

architecture tcuvwdz of nogr is
  
begin
  -- Single-driven assignments
  zbz <= 16#8.E_5_A_D_D#;
end tcuvwdz;



-- Seed after: 14088231110538783655,14629254427735353553
