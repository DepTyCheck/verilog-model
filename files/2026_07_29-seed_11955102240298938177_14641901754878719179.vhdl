-- Seed: 11955102240298938177,14641901754878719179

library ieee;
use ieee.std_logic_1164.all;

entity cwsjdtfp is
  port (e : inout std_logic; rgrjjqa : buffer time; nc : out real; gnmm : out real);
end cwsjdtfp;

architecture wfju of cwsjdtfp is
  
begin
  -- Single-driven assignments
  gnmm <= 2_4_4_4.3;
  nc <= nc;
  
  -- Multi-driven assignments
  e <= '1';
  e <= e;
  e <= 'L';
  e <= e;
end wfju;

library ieee;
use ieee.std_logic_1164.all;

entity hjbdmnbyec is
  port (qiyrfhyepz : out real; scngotsa : inout std_logic);
end hjbdmnbyec;

architecture aykbi of hjbdmnbyec is
  
begin
  -- Single-driven assignments
  qiyrfhyepz <= qiyrfhyepz;
  
  -- Multi-driven assignments
  scngotsa <= 'U';
  scngotsa <= scngotsa;
  scngotsa <= 'U';
  scngotsa <= scngotsa;
end aykbi;

library ieee;
use ieee.std_logic_1164.all;

entity hbroht is
  port (exel : out std_logic_vector(3 to 1); pkgqdkoa : inout std_logic_vector(0 downto 3); ipefek : out bit; oa : in severity_level);
end hbroht;

library ieee;
use ieee.std_logic_1164.all;

architecture mgo of hbroht is
  signal dxwyfryf : real;
  signal ytn : real;
  signal u : time;
  signal qmrxpldh : std_logic;
  signal eny : real;
  signal buti : real;
  signal lkqtgrons : time;
  signal tdz : std_logic;
  signal gxurspv : real;
  signal mm : real;
  signal ljsilbgjt : real;
  signal x : time;
  signal tvl : std_logic;
begin
  dbw : entity work.cwsjdtfp
    port map (e => tvl, rgrjjqa => x, nc => ljsilbgjt, gnmm => mm);
  nxozjno : entity work.hjbdmnbyec
    port map (qiyrfhyepz => gxurspv, scngotsa => tdz);
  iinespp : entity work.cwsjdtfp
    port map (e => tvl, rgrjjqa => lkqtgrons, nc => buti, gnmm => eny);
  yapldodbtp : entity work.cwsjdtfp
    port map (e => qmrxpldh, rgrjjqa => u, nc => ytn, gnmm => dxwyfryf);
  
  -- Single-driven assignments
  ipefek <= ipefek;
end mgo;



-- Seed after: 3096023365333035443,14641901754878719179
