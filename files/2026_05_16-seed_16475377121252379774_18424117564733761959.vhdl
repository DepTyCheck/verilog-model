-- Seed: 16475377121252379774,18424117564733761959

library ieee;
use ieee.std_logic_1164.all;

entity m is
  port (cwaroec : buffer real; gclqwp : out std_logic; v : buffer severity_level);
end m;



architecture dllobg of m is
  
begin
  
end dllobg;

library ieee;
use ieee.std_logic_1164.all;

entity cqkhrfkr is
  port (z : in std_logic);
end cqkhrfkr;

library ieee;
use ieee.std_logic_1164.all;

architecture atuu of cqkhrfkr is
  signal w : severity_level;
  signal rr : real;
  signal l : severity_level;
  signal kr : real;
  signal qrdmo : severity_level;
  signal n : real;
  signal a : severity_level;
  signal wpzjmd : std_logic;
  signal x : real;
begin
  fnqx : entity work.m
    port map (cwaroec => x, gclqwp => wpzjmd, v => a);
  nk : entity work.m
    port map (cwaroec => n, gclqwp => wpzjmd, v => qrdmo);
  aaaxsnd : entity work.m
    port map (cwaroec => kr, gclqwp => wpzjmd, v => l);
  xsjm : entity work.m
    port map (cwaroec => rr, gclqwp => wpzjmd, v => w);
end atuu;



-- Seed after: 16575883465172789537,18424117564733761959
