-- Seed: 13108242447526081611,2983771601630957889

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity dwpvgt is
  port ( dwnl : inout std_logic_vector(4 downto 0)
  ; ynaeeafb : linkage real
  ; variable j : inout file_value_mirror_pt
  ; variable nva : inout integer_subtype_mirror_pt
  );
end dwpvgt;

architecture yudln of dwpvgt is
  
begin
  -- Multi-driven assignments
  dwnl <= "L-XUH";
end yudln;

use std.reflection.all;

entity bf is
  port (variable i : inout access_value_mirror_pt; trn : in bit_vector(1 to 0));
end bf;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture tfwno of bf is
  shared variable uoiymwk : integer_subtype_mirror_pt;
  shared variable yomtkfgvb : file_value_mirror_pt;
  signal tgojppo : real;
  shared variable m : integer_subtype_mirror_pt;
  shared variable fiajt : file_value_mirror_pt;
  signal nveacxdnt : real;
  shared variable ztupmv : integer_subtype_mirror_pt;
  shared variable r : file_value_mirror_pt;
  signal tubv : real;
  signal zyyfh : std_logic_vector(4 downto 0);
begin
  gg : entity work.dwpvgt
    port map (dwnl => zyyfh, ynaeeafb => tubv, j => r, nva => ztupmv);
  krbkymlud : entity work.dwpvgt
    port map (dwnl => zyyfh, ynaeeafb => nveacxdnt, j => fiajt, nva => m);
  bclb : entity work.dwpvgt
    port map (dwnl => zyyfh, ynaeeafb => tgojppo, j => yomtkfgvb, nva => uoiymwk);
  
  -- Multi-driven assignments
  zyyfh <= zyyfh;
  zyyfh <= ('X', '1', 'Z', 'Z', '0');
  zyyfh <= "0LLLU";
end tfwno;



-- Seed after: 1547670539878538897,2983771601630957889
