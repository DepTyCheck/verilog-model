-- Seed: 9489734581962541430,10754487200446211253

entity dherrrd is
  port (mfhvuejk : in integer; gm : out severity_level; sx : out integer; wgnwnv : linkage bit_vector(3 to 1));
end dherrrd;

architecture baj of dherrrd is
  
begin
  -- Single-driven assignments
  gm <= WARNING;
  sx <= sx;
end baj;

entity baspd is
  port (g : linkage string(4 downto 1));
end baspd;

architecture ouzcar of baspd is
  signal uf : bit_vector(3 to 1);
  signal grjiutlio : integer;
  signal vlit : severity_level;
  signal qq : bit_vector(3 to 1);
  signal ckqtf : severity_level;
  signal hauobgpvzl : bit_vector(3 to 1);
  signal toeanjp : integer;
  signal dit : severity_level;
  signal da : integer;
begin
  wcolwl : entity work.dherrrd
    port map (mfhvuejk => da, gm => dit, sx => toeanjp, wgnwnv => hauobgpvzl);
  v : entity work.dherrrd
    port map (mfhvuejk => da, gm => ckqtf, sx => da, wgnwnv => qq);
  vhckz : entity work.dherrrd
    port map (mfhvuejk => da, gm => vlit, sx => grjiutlio, wgnwnv => uf);
end ouzcar;

library ieee;
use ieee.std_logic_1164.all;

entity s is
  port (wmrfhchz : in real; tttdhur : in real; t : in time; zmmetor : buffer std_logic_vector(0 to 0));
end s;

architecture bxvqehe of s is
  signal enyowjki : string(4 downto 1);
begin
  p : entity work.baspd
    port map (g => enyowjki);
  
  -- Multi-driven assignments
  zmmetor <= "W";
  zmmetor <= zmmetor;
  zmmetor <= "1";
  zmmetor <= zmmetor;
end bxvqehe;



-- Seed after: 5585675651424321121,10754487200446211253
