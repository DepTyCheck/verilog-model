-- Seed: 4943737177311402315,4292249356257567981

entity jalfo is
  port (aohisp : inout integer; nqnak : in real; y : inout boolean_vector(2 downto 1); rbnltsj : linkage time);
end jalfo;

architecture mvt of jalfo is
  
begin
  -- Single-driven assignments
  aohisp <= 16#3_2_8#;
  y <= (FALSE, FALSE);
end mvt;

library ieee;
use ieee.std_logic_1164.all;

entity ekt is
  port (lqcjea : inout time; gecci : inout integer; pscavxh : linkage std_logic_vector(1 to 2); gcbzlnlb : linkage real);
end ekt;

architecture ms of ekt is
  signal penogsm : boolean_vector(2 downto 1);
  signal lseviivltj : integer;
  signal jdriaopywb : time;
  signal krxysi : boolean_vector(2 downto 1);
  signal gpezmry : integer;
  signal qltctux : time;
  signal wqhctevtr : boolean_vector(2 downto 1);
  signal hatdgoe : real;
  signal xuofth : integer;
begin
  pcugom : entity work.jalfo
    port map (aohisp => xuofth, nqnak => hatdgoe, y => wqhctevtr, rbnltsj => qltctux);
  t : entity work.jalfo
    port map (aohisp => gpezmry, nqnak => hatdgoe, y => krxysi, rbnltsj => jdriaopywb);
  mmvmuytmo : entity work.jalfo
    port map (aohisp => lseviivltj, nqnak => hatdgoe, y => penogsm, rbnltsj => lqcjea);
end ms;

library ieee;
use ieee.std_logic_1164.all;

entity alsfvc is
  port (oiji : linkage character; tglh : in std_logic_vector(4 to 3));
end alsfvc;

architecture mg of alsfvc is
  signal dhcotcnvdn : time;
  signal fqutkotuh : boolean_vector(2 downto 1);
  signal tdmug : integer;
  signal irx : time;
  signal gibhidmopk : boolean_vector(2 downto 1);
  signal vhsy : real;
  signal hivce : integer;
  signal nbayb : time;
  signal vkm : boolean_vector(2 downto 1);
  signal kqbuexg : integer;
  signal dwhiouik : time;
  signal kiqnlymm : boolean_vector(2 downto 1);
  signal fjn : real;
  signal osycvnrjq : integer;
begin
  qf : entity work.jalfo
    port map (aohisp => osycvnrjq, nqnak => fjn, y => kiqnlymm, rbnltsj => dwhiouik);
  dsp : entity work.jalfo
    port map (aohisp => kqbuexg, nqnak => fjn, y => vkm, rbnltsj => nbayb);
  vusmaohtw : entity work.jalfo
    port map (aohisp => hivce, nqnak => vhsy, y => gibhidmopk, rbnltsj => irx);
  tdyqdte : entity work.jalfo
    port map (aohisp => tdmug, nqnak => vhsy, y => fqutkotuh, rbnltsj => dhcotcnvdn);
  
  -- Single-driven assignments
  fjn <= fjn;
  vhsy <= fjn;
end mg;

library ieee;
use ieee.std_logic_1164.all;

entity ifqfzlrnid is
  port (t : buffer std_logic_vector(2 downto 0); etz : linkage time);
end ifqfzlrnid;

architecture fpgcmxm of ifqfzlrnid is
  signal pnf : time;
  signal sehfo : boolean_vector(2 downto 1);
  signal rkf : real;
  signal p : integer;
  signal kdgmgter : boolean_vector(2 downto 1);
  signal kw : real;
  signal d : integer;
begin
  kambw : entity work.jalfo
    port map (aohisp => d, nqnak => kw, y => kdgmgter, rbnltsj => etz);
  thdbktk : entity work.jalfo
    port map (aohisp => p, nqnak => rkf, y => sehfo, rbnltsj => pnf);
  
  -- Single-driven assignments
  kw <= kw;
  rkf <= rkf;
end fpgcmxm;



-- Seed after: 1299870177026871906,4292249356257567981
