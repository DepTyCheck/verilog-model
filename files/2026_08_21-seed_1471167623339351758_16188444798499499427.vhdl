-- Seed: 1471167623339351758,16188444798499499427

entity ifpazexdro is
  port (eucva : out string(2 downto 5); dzpgebk : buffer time);
end ifpazexdro;

architecture axlwvjk of ifpazexdro is
  
begin
  -- Single-driven assignments
  dzpgebk <= 4_2_3_4.42 ps;
  eucva <= (others => ' ');
end axlwvjk;

library ieee;
use ieee.std_logic_1164.all;

entity ftokdjdwad is
  port (ebpe : out integer; ijwhccp : out std_logic_vector(2 to 4));
end ftokdjdwad;

architecture sfqgu of ftokdjdwad is
  signal ju : time;
  signal nq : string(2 downto 5);
  signal mxt : time;
  signal adbbrmfj : string(2 downto 5);
  signal lsi : time;
  signal eifvpcsic : string(2 downto 5);
begin
  lsv : entity work.ifpazexdro
    port map (eucva => eifvpcsic, dzpgebk => lsi);
  nghwrf : entity work.ifpazexdro
    port map (eucva => adbbrmfj, dzpgebk => mxt);
  xwssm : entity work.ifpazexdro
    port map (eucva => nq, dzpgebk => ju);
  
  -- Single-driven assignments
  ebpe <= ebpe;
  
  -- Multi-driven assignments
  ijwhccp <= ijwhccp;
  ijwhccp <= ijwhccp;
  ijwhccp <= ijwhccp;
  ijwhccp <= ijwhccp;
end sfqgu;

entity etxsts is
  port (yuqw : out integer; xjuzyzp : linkage integer; ftzu : in integer);
end etxsts;

architecture fpcvyeuto of etxsts is
  signal ksvp : time;
  signal gqszcp : string(2 downto 5);
begin
  wyvuf : entity work.ifpazexdro
    port map (eucva => gqszcp, dzpgebk => ksvp);
  
  -- Single-driven assignments
  yuqw <= yuqw;
end fpcvyeuto;

library ieee;
use ieee.std_logic_1164.all;

entity gdopuao is
  port (uc : linkage std_logic);
end gdopuao;

library ieee;
use ieee.std_logic_1164.all;

architecture dfn of gdopuao is
  signal cbcfrm : time;
  signal wepk : string(2 downto 5);
  signal uyxjdp : std_logic_vector(2 to 4);
  signal eemadz : integer;
  signal kzciptfny : time;
  signal f : string(2 downto 5);
begin
  fvdqa : entity work.ifpazexdro
    port map (eucva => f, dzpgebk => kzciptfny);
  wmgchtely : entity work.ftokdjdwad
    port map (ebpe => eemadz, ijwhccp => uyxjdp);
  mghvlt : entity work.ifpazexdro
    port map (eucva => wepk, dzpgebk => cbcfrm);
  
  -- Multi-driven assignments
  uyxjdp <= ('0', '1', 'W');
  uyxjdp <= "0H0";
end dfn;



-- Seed after: 9867159159014103041,16188444798499499427
