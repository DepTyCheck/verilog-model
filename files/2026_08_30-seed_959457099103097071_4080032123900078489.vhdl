-- Seed: 959457099103097071,4080032123900078489

library ieee;
use ieee.std_logic_1164.all;

entity jcxyypji is
  port (o : buffer integer; uucgl : out std_logic; us : out real; imz : buffer integer_vector(3 downto 0));
end jcxyypji;

architecture ir of jcxyypji is
  
begin
  -- Single-driven assignments
  us <= us;
  o <= o;
  imz <= imz;
  
  -- Multi-driven assignments
  uucgl <= '1';
end ir;

library ieee;
use ieee.std_logic_1164.all;

entity kx is
  port (knaymdql : in boolean_vector(2 to 2); iji : in std_logic_vector(0 downto 3));
end kx;

library ieee;
use ieee.std_logic_1164.all;

architecture nfc of kx is
  signal i : integer_vector(3 downto 0);
  signal d : real;
  signal u : std_logic;
  signal xv : integer;
  signal shjko : integer_vector(3 downto 0);
  signal xiybaafk : real;
  signal rs : std_logic;
  signal dqxeui : integer;
  signal tdku : integer_vector(3 downto 0);
  signal vkpmp : real;
  signal ynchwvde : std_logic;
  signal xjteeavbt : integer;
begin
  hmiwfep : entity work.jcxyypji
    port map (o => xjteeavbt, uucgl => ynchwvde, us => vkpmp, imz => tdku);
  bzvq : entity work.jcxyypji
    port map (o => dqxeui, uucgl => rs, us => xiybaafk, imz => shjko);
  v : entity work.jcxyypji
    port map (o => xv, uucgl => u, us => d, imz => i);
  
  -- Multi-driven assignments
  rs <= 'X';
end nfc;



-- Seed after: 6017029288591309624,4080032123900078489
