-- Seed: 17922404963998741029,10875537289884587119

entity no is
  port (ehllfaj : out boolean; utci : out time; ndae : linkage severity_level);
end no;

architecture bagzmjs of no is
  
begin
  -- Single-driven assignments
  utci <= 0 min;
  ehllfaj <= FALSE;
end bagzmjs;

library ieee;
use ieee.std_logic_1164.all;

entity iylqr is
  port (amzgp : linkage std_logic);
end iylqr;

architecture uivhkr of iylqr is
  signal uovgtthzkx : severity_level;
  signal okvjegok : time;
  signal vreo : boolean;
  signal idfel : severity_level;
  signal x : time;
  signal xsiakumm : boolean;
begin
  bdjy : entity work.no
    port map (ehllfaj => xsiakumm, utci => x, ndae => idfel);
  yhmrxuwmd : entity work.no
    port map (ehllfaj => vreo, utci => okvjegok, ndae => uovgtthzkx);
end uivhkr;

library ieee;
use ieee.std_logic_1164.all;

entity x is
  port (nzm : out time; fixrhvuv : linkage character; buml : inout std_logic);
end x;

library ieee;
use ieee.std_logic_1164.all;

architecture csj of x is
  signal geib : severity_level;
  signal xadhqomyb : time;
  signal bvdqyw : boolean;
  signal lbskjbbii : std_logic;
  signal vl : severity_level;
  signal wgpeyd : boolean;
begin
  n : entity work.iylqr
    port map (amzgp => buml);
  uojajg : entity work.no
    port map (ehllfaj => wgpeyd, utci => nzm, ndae => vl);
  a : entity work.iylqr
    port map (amzgp => lbskjbbii);
  msjfj : entity work.no
    port map (ehllfaj => bvdqyw, utci => xadhqomyb, ndae => geib);
end csj;



-- Seed after: 15187009107098810217,10875537289884587119
