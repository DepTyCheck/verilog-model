-- Seed: 7725011532692017209,7142793346053417159

library ieee;
use ieee.std_logic_1164.all;

entity yeodbru is
  port (rpolgq : linkage real; pupmsja : linkage std_logic_vector(3 to 0); yfysnduw : buffer real; svdn : inout severity_level);
end yeodbru;



architecture w of yeodbru is
  
begin
  
end w;

library ieee;
use ieee.std_logic_1164.all;

entity z is
  port (efhn : in std_logic_vector(4 downto 0));
end z;

library ieee;
use ieee.std_logic_1164.all;

architecture zhtmy of z is
  signal wzyjkptpwo : severity_level;
  signal rfu : real;
  signal kzagbn : severity_level;
  signal abjglbkurg : real;
  signal dza : real;
  signal hiwcs : severity_level;
  signal ogirq : real;
  signal gkrqgxbxa : severity_level;
  signal chkufem : std_logic_vector(3 to 0);
  signal dftnvxxe : real;
begin
  soqwzgip : entity work.yeodbru
    port map (rpolgq => dftnvxxe, pupmsja => chkufem, yfysnduw => dftnvxxe, svdn => gkrqgxbxa);
  wolcsmkdu : entity work.yeodbru
    port map (rpolgq => ogirq, pupmsja => chkufem, yfysnduw => ogirq, svdn => hiwcs);
  tbr : entity work.yeodbru
    port map (rpolgq => dza, pupmsja => chkufem, yfysnduw => abjglbkurg, svdn => kzagbn);
  dnxzj : entity work.yeodbru
    port map (rpolgq => rfu, pupmsja => chkufem, yfysnduw => dza, svdn => wzyjkptpwo);
end zhtmy;



entity upbsaapvm is
  port (brprkvq : linkage real; lmx : linkage time; xbvhqbjhl : linkage integer_vector(1 downto 4); ptfvey : inout bit_vector(3 to 2));
end upbsaapvm;

library ieee;
use ieee.std_logic_1164.all;

architecture nqub of upbsaapvm is
  signal dndaqxer : std_logic_vector(4 downto 0);
  signal zrli : severity_level;
  signal avujtrhnm : real;
  signal qee : std_logic_vector(3 to 0);
  signal sxrouoaept : severity_level;
  signal bzlihxec : real;
  signal dqmfmxg : real;
  signal r : severity_level;
  signal zhe : real;
  signal zenwenwj : std_logic_vector(3 to 0);
  signal dtgrk : real;
begin
  ttgjeogaf : entity work.yeodbru
    port map (rpolgq => dtgrk, pupmsja => zenwenwj, yfysnduw => zhe, svdn => r);
  rbx : entity work.yeodbru
    port map (rpolgq => dqmfmxg, pupmsja => zenwenwj, yfysnduw => bzlihxec, svdn => sxrouoaept);
  fklzjyerzh : entity work.yeodbru
    port map (rpolgq => dtgrk, pupmsja => qee, yfysnduw => avujtrhnm, svdn => zrli);
  ct : entity work.z
    port map (efhn => dndaqxer);
end nqub;

library ieee;
use ieee.std_logic_1164.all;

entity c is
  port ( endot : inout std_logic_vector(2 downto 0)
  ; vdzmbdea : out severity_level
  ; enefl : buffer integer
  ; cuakdaku : linkage boolean_vector(4 downto 1)
  );
end c;

library ieee;
use ieee.std_logic_1164.all;

architecture lltpqjjcd of c is
  signal egvpgjn : bit_vector(3 to 2);
  signal rxa : integer_vector(1 downto 4);
  signal wmbq : time;
  signal ifnaudp : real;
  signal ibylkttdu : severity_level;
  signal eagr : real;
  signal g : std_logic_vector(3 to 0);
  signal lbykc : real;
begin
  h : entity work.yeodbru
    port map (rpolgq => lbykc, pupmsja => g, yfysnduw => eagr, svdn => ibylkttdu);
  neixdi : entity work.upbsaapvm
    port map (brprkvq => ifnaudp, lmx => wmbq, xbvhqbjhl => rxa, ptfvey => egvpgjn);
end lltpqjjcd;



-- Seed after: 12263408317833461772,7142793346053417159
