-- Seed: 12776753746803106275,8067602802092121131

library ieee;
use ieee.std_logic_1164.all;

entity roamlpdfq is
  port (wq : out severity_level; homrcq : linkage time_vector(3 to 4); tq : linkage std_logic);
end roamlpdfq;

architecture ptvduf of roamlpdfq is
  
begin
  
end ptvduf;

library ieee;
use ieee.std_logic_1164.all;

entity psu is
  port (xaiiuws : buffer real; tkylaw : buffer string(3 downto 1); ptdzgnofz : in std_logic; sbtagk : linkage real);
end psu;

library ieee;
use ieee.std_logic_1164.all;

architecture jxnkqjq of psu is
  signal jzjoc : std_logic;
  signal b : time_vector(3 to 4);
  signal oq : severity_level;
  signal bmec : time_vector(3 to 4);
  signal hde : severity_level;
  signal gobhveszo : std_logic;
  signal secnebgpmz : time_vector(3 to 4);
  signal iujvtkhd : severity_level;
begin
  pymfhli : entity work.roamlpdfq
    port map (wq => iujvtkhd, homrcq => secnebgpmz, tq => gobhveszo);
  q : entity work.roamlpdfq
    port map (wq => hde, homrcq => bmec, tq => ptdzgnofz);
  rwhq : entity work.roamlpdfq
    port map (wq => oq, homrcq => b, tq => jzjoc);
  
  -- Single-driven assignments
  xaiiuws <= xaiiuws;
  
  -- Multi-driven assignments
  gobhveszo <= 'Z';
  gobhveszo <= ptdzgnofz;
  jzjoc <= ptdzgnofz;
end jxnkqjq;

library ieee;
use ieee.std_logic_1164.all;

entity xvp is
  port (vjy : buffer real_vector(4 downto 4); cjsoyrv : buffer std_logic);
end xvp;

library ieee;
use ieee.std_logic_1164.all;

architecture qgurtxgy of xvp is
  signal blytsb : std_logic;
  signal gdtxsb : time_vector(3 to 4);
  signal yamjkbfovu : severity_level;
  signal fdungth : time_vector(3 to 4);
  signal bk : severity_level;
begin
  dxbodbist : entity work.roamlpdfq
    port map (wq => bk, homrcq => fdungth, tq => cjsoyrv);
  v : entity work.roamlpdfq
    port map (wq => yamjkbfovu, homrcq => gdtxsb, tq => blytsb);
  
  -- Multi-driven assignments
  cjsoyrv <= cjsoyrv;
end qgurtxgy;

library ieee;
use ieee.std_logic_1164.all;

entity qmm is
  port (rsagey : buffer std_logic; tws : inout std_logic; axfdrrnkg : in real);
end qmm;

library ieee;
use ieee.std_logic_1164.all;

architecture mstoyj of qmm is
  signal pkoysl : std_logic;
  signal qogjkfi : time_vector(3 to 4);
  signal bijltnnc : severity_level;
begin
  ozpbjhpx : entity work.roamlpdfq
    port map (wq => bijltnnc, homrcq => qogjkfi, tq => pkoysl);
end mstoyj;



-- Seed after: 726353894288539826,8067602802092121131
