-- Seed: 13752435359778843711,11127274767545411571

library ieee;
use ieee.std_logic_1164.all;

entity v is
  port (dbldm : in std_logic; gmltfqf : out time; ndzuqfdsch : inout integer; t : buffer boolean_vector(2 to 1));
end v;

architecture ddpifrn of v is
  
begin
  
end ddpifrn;

entity vu is
  port (rhbyzisiyp : inout real; eefzqvma : in time; tyro : linkage integer);
end vu;

library ieee;
use ieee.std_logic_1164.all;

architecture hm of vu is
  signal d : boolean_vector(2 to 1);
  signal drt : integer;
  signal gmr : time;
  signal zg : std_logic;
begin
  yar : entity work.v
    port map (dbldm => zg, gmltfqf => gmr, ndzuqfdsch => drt, t => d);
  
  -- Multi-driven assignments
  zg <= '0';
  zg <= '0';
end hm;

library ieee;
use ieee.std_logic_1164.all;

entity egxchxxno is
  port (q : buffer std_logic_vector(3 to 1));
end egxchxxno;

library ieee;
use ieee.std_logic_1164.all;

architecture oajw of egxchxxno is
  signal talzmfsmtp : boolean_vector(2 to 1);
  signal mvipmhz : integer;
  signal zlrkgxim : time;
  signal zl : std_logic;
  signal cfjtgodsj : boolean_vector(2 to 1);
  signal lacsyekpma : integer;
  signal rugpoo : time;
  signal dfu : std_logic;
begin
  ijvtk : entity work.v
    port map (dbldm => dfu, gmltfqf => rugpoo, ndzuqfdsch => lacsyekpma, t => cfjtgodsj);
  aawhhzphl : entity work.v
    port map (dbldm => zl, gmltfqf => zlrkgxim, ndzuqfdsch => mvipmhz, t => talzmfsmtp);
  
  -- Multi-driven assignments
  q <= q;
  q <= q;
end oajw;



-- Seed after: 18254025151986633270,11127274767545411571
