-- Seed: 12132912139632967218,662889661651915549

entity frof is
  port (ylrkc : buffer integer; inulxmin : inout time; ihp : out time; zqqh : buffer severity_level);
end frof;

architecture slawv of frof is
  
begin
  -- Single-driven assignments
  ihp <= 23341.1_3_0 ps;
  ylrkc <= 8#6312#;
  inulxmin <= 16#9.F3# fs;
  zqqh <= zqqh;
end slawv;

entity vovsy is
  port (rv : linkage real);
end vovsy;

architecture ebjmroal of vovsy is
  signal osx : severity_level;
  signal tuvfoc : time;
  signal vwaxlyf : time;
  signal ppl : integer;
  signal wnbzatkczj : severity_level;
  signal zgs : time;
  signal xxwxdk : time;
  signal ydnevex : integer;
  signal p : severity_level;
  signal clbfrquomk : time;
  signal ellebmvmkw : time;
  signal knq : integer;
begin
  qqsbazlltw : entity work.frof
    port map (ylrkc => knq, inulxmin => ellebmvmkw, ihp => clbfrquomk, zqqh => p);
  vqpubdehdv : entity work.frof
    port map (ylrkc => ydnevex, inulxmin => xxwxdk, ihp => zgs, zqqh => wnbzatkczj);
  opjwpv : entity work.frof
    port map (ylrkc => ppl, inulxmin => vwaxlyf, ihp => tuvfoc, zqqh => osx);
end ebjmroal;

library ieee;
use ieee.std_logic_1164.all;

entity niydb is
  port (vhmoe : out time; xikqvhu : out std_logic_vector(4 downto 1); ivmavtbkd : out std_logic; nhn : buffer boolean_vector(0 to 3));
end niydb;

architecture nckprblo of niydb is
  signal h : severity_level;
  signal mqprdcqqv : time;
  signal wsstfc : time;
  signal gdw : integer;
begin
  uqcfds : entity work.frof
    port map (ylrkc => gdw, inulxmin => wsstfc, ihp => mqprdcqqv, zqqh => h);
  
  -- Single-driven assignments
  nhn <= (TRUE, TRUE, TRUE, FALSE);
  
  -- Multi-driven assignments
  ivmavtbkd <= 'Z';
end nckprblo;

entity osdinpfyp is
  port (qkbdvctt : buffer integer_vector(2 to 3));
end osdinpfyp;

architecture ngoqkssv of osdinpfyp is
  signal akf : severity_level;
  signal nrueu : time;
  signal qogeuyokb : time;
  signal alisjf : integer;
begin
  pfwpux : entity work.frof
    port map (ylrkc => alisjf, inulxmin => qogeuyokb, ihp => nrueu, zqqh => akf);
  
  -- Single-driven assignments
  qkbdvctt <= (2#0_0#, 100);
end ngoqkssv;



-- Seed after: 12321412015428497699,662889661651915549
