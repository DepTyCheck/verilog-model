-- Seed: 17467421258795570336,13592003931158285879

entity tjrjdioje is
  port (znnpw : in boolean_vector(1 downto 2));
end tjrjdioje;

architecture lgr of tjrjdioje is
  
begin
  
end lgr;

library ieee;
use ieee.std_logic_1164.all;

entity pgxze is
  port (lcizwfwid : linkage bit; i : inout real; su : out std_logic);
end pgxze;

architecture pry of pgxze is
  signal znpv : boolean_vector(1 downto 2);
begin
  waqs : entity work.tjrjdioje
    port map (znnpw => znpv);
  
  -- Multi-driven assignments
  su <= su;
  su <= 'H';
  su <= su;
  su <= 'W';
end pry;

entity m is
  port (weo : buffer real);
end m;

library ieee;
use ieee.std_logic_1164.all;

architecture zqnnvok of m is
  signal pgk : boolean_vector(1 downto 2);
  signal pictkfwgi : std_logic;
  signal amd : real;
  signal eq : bit;
begin
  hmpvn : entity work.pgxze
    port map (lcizwfwid => eq, i => amd, su => pictkfwgi);
  qnsscq : entity work.tjrjdioje
    port map (znnpw => pgk);
  
  -- Multi-driven assignments
  pictkfwgi <= pictkfwgi;
  pictkfwgi <= pictkfwgi;
  pictkfwgi <= pictkfwgi;
end zqnnvok;



-- Seed after: 6343599170130163033,13592003931158285879
