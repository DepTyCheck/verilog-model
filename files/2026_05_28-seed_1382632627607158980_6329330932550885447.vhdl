-- Seed: 1382632627607158980,6329330932550885447

library ieee;
use ieee.std_logic_1164.all;

entity xzcoynp is
  port (so : buffer bit_vector(4 to 1); fg : in integer; xepr : in real; heur : in std_logic);
end xzcoynp;



architecture jwqliakoav of xzcoynp is
  
begin
  
end jwqliakoav;

library ieee;
use ieee.std_logic_1164.all;

entity gtnx is
  port (cfpmzxni : in integer; jbjhen : inout std_logic);
end gtnx;

library ieee;
use ieee.std_logic_1164.all;

architecture utmjwet of gtnx is
  signal kcleiqu : integer;
  signal fhtlhmf : bit_vector(4 to 1);
  signal burmkgzip : std_logic;
  signal tvb : real;
  signal bbggfr : integer;
  signal avel : bit_vector(4 to 1);
begin
  lanemnrjr : entity work.xzcoynp
    port map (so => avel, fg => bbggfr, xepr => tvb, heur => burmkgzip);
  ikqmlsmt : entity work.xzcoynp
    port map (so => fhtlhmf, fg => kcleiqu, xepr => tvb, heur => jbjhen);
end utmjwet;

library ieee;
use ieee.std_logic_1164.all;

entity sbxxrflzjs is
  port (sibdw : buffer std_logic; xk : in real);
end sbxxrflzjs;

library ieee;
use ieee.std_logic_1164.all;

architecture ge of sbxxrflzjs is
  signal cnoynk : std_logic;
  signal btd : integer;
  signal tbjrp : bit_vector(4 to 1);
  signal ovlkcqafg : real;
  signal ktod : integer;
  signal ppipqxltl : bit_vector(4 to 1);
begin
  ytan : entity work.xzcoynp
    port map (so => ppipqxltl, fg => ktod, xepr => ovlkcqafg, heur => sibdw);
  jjst : entity work.xzcoynp
    port map (so => tbjrp, fg => btd, xepr => xk, heur => cnoynk);
end ge;



-- Seed after: 16896605156598093707,6329330932550885447
