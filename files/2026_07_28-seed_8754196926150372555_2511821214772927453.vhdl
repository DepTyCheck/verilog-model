-- Seed: 8754196926150372555,2511821214772927453

entity oyhmv is
  port (cdqkegy : in string(3 to 2); wowwyymqf : linkage real; b : buffer real; mlakbuhmi : buffer real);
end oyhmv;

architecture p of oyhmv is
  
begin
  -- Single-driven assignments
  mlakbuhmi <= 2#001.1_0_1_0_0#;
  b <= 0334.0033;
end p;

library ieee;
use ieee.std_logic_1164.all;

entity bkfq is
  port (jcaku : linkage std_logic_vector(2 to 2); o : linkage std_logic_vector(3 downto 2); as : linkage real_vector(3 to 3));
end bkfq;

architecture iiy of bkfq is
  
begin
  
end iiy;

entity bsdntlx is
  port (gn : out character; eldnapi : out integer_vector(4 downto 2));
end bsdntlx;

architecture u of bsdntlx is
  signal qtnuigpl : real;
  signal a : real;
  signal lbwqzn : real;
  signal oyz : string(3 to 2);
begin
  sopmsj : entity work.oyhmv
    port map (cdqkegy => oyz, wowwyymqf => lbwqzn, b => a, mlakbuhmi => qtnuigpl);
end u;

library ieee;
use ieee.std_logic_1164.all;

entity bq is
  port (hmrpqrzt : buffer severity_level; ptpnbz : in time; e : buffer std_logic_vector(2 to 1));
end bq;

architecture rij of bq is
  signal omqurd : real;
  signal ywrarshpl : real;
  signal ubv : real;
  signal ardn : string(3 to 2);
begin
  zshjsav : entity work.oyhmv
    port map (cdqkegy => ardn, wowwyymqf => ubv, b => ywrarshpl, mlakbuhmi => omqurd);
  
  -- Single-driven assignments
  hmrpqrzt <= hmrpqrzt;
  ardn <= "";
  
  -- Multi-driven assignments
  e <= (others => '0');
  e <= "";
end rij;



-- Seed after: 2100088782394347153,2511821214772927453
