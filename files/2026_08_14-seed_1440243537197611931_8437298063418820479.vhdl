-- Seed: 1440243537197611931,8437298063418820479

entity k is
  port (xmkgivksc : in time; crkaanfjb : buffer string(1 downto 1); yyycj : linkage real);
end k;

architecture kdlz of k is
  
begin
  
end kdlz;

library ieee;
use ieee.std_logic_1164.all;

entity xezko is
  port (y : inout std_logic_vector(3 downto 4));
end xezko;

architecture wjremdhdb of xezko is
  signal oogudi : real;
  signal e : string(1 downto 1);
  signal dmi : time;
begin
  ibtxsqp : entity work.k
    port map (xmkgivksc => dmi, crkaanfjb => e, yyycj => oogudi);
  
  -- Single-driven assignments
  dmi <= dmi;
  
  -- Multi-driven assignments
  y <= (others => '0');
  y <= (others => '0');
end wjremdhdb;

entity oldxit is
  port (ukr : buffer time; jrwc : in time);
end oldxit;

architecture tuqs of oldxit is
  signal fqnztnp : real;
  signal tq : string(1 downto 1);
begin
  nkvjucrkc : entity work.k
    port map (xmkgivksc => ukr, crkaanfjb => tq, yyycj => fqnztnp);
  
  -- Single-driven assignments
  ukr <= 8#201.0_2_1# us;
end tuqs;

entity xi is
  port (kmpvpun : buffer real);
end xi;

architecture s of xi is
  signal shezuv : string(1 downto 1);
  signal bwdkuehw : real;
  signal ha : string(1 downto 1);
  signal eeo : time;
  signal y : real;
  signal mzwhd : string(1 downto 1);
  signal m : time;
begin
  nssx : entity work.k
    port map (xmkgivksc => m, crkaanfjb => mzwhd, yyycj => y);
  xcqngsnjc : entity work.k
    port map (xmkgivksc => eeo, crkaanfjb => ha, yyycj => bwdkuehw);
  fbwbiqfrqn : entity work.k
    port map (xmkgivksc => m, crkaanfjb => shezuv, yyycj => kmpvpun);
  
  -- Single-driven assignments
  m <= 8#0_3_2_1# ns;
end s;



-- Seed after: 12819659913465738563,8437298063418820479
