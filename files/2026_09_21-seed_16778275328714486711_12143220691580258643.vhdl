-- Seed: 16778275328714486711,12143220691580258643

entity mvnuw is
  port (tetw : out real; zhcyjkgjp : buffer real_vector(4 downto 4); wbtlonyq : linkage bit_vector(0 to 2); p : out severity_level);
end mvnuw;

architecture dxsziyg of mvnuw is
  
begin
  -- Single-driven assignments
  p <= WARNING;
  tetw <= tetw;
  zhcyjkgjp <= (others => 16#D.6#);
end dxsziyg;

library ieee;
use ieee.std_logic_1164.all;

entity sbwwowbf is
  port (fkhy : out std_logic; vsw : in integer);
end sbwwowbf;

architecture tzvin of sbwwowbf is
  signal jzubkzzt : severity_level;
  signal plmcdbl : bit_vector(0 to 2);
  signal nc : real_vector(4 downto 4);
  signal zbv : real;
begin
  dleu : entity work.mvnuw
    port map (tetw => zbv, zhcyjkgjp => nc, wbtlonyq => plmcdbl, p => jzubkzzt);
end tzvin;

library ieee;
use ieee.std_logic_1164.all;

entity xspc is
  port (yfeohxfr : inout std_logic_vector(4 to 2); wtmo : inout string(4 downto 4); llrryxwqy : linkage time);
end xspc;

architecture snaq of xspc is
  signal cdshjdfyc : severity_level;
  signal jwtvzwlfn : bit_vector(0 to 2);
  signal opuoj : real_vector(4 downto 4);
  signal scbfqhn : real;
  signal vfvlmeo : severity_level;
  signal zhnskayvh : bit_vector(0 to 2);
  signal km : real_vector(4 downto 4);
  signal rygltqf : real;
  signal d : severity_level;
  signal uya : bit_vector(0 to 2);
  signal aiums : real_vector(4 downto 4);
  signal rie : real;
begin
  zfg : entity work.mvnuw
    port map (tetw => rie, zhcyjkgjp => aiums, wbtlonyq => uya, p => d);
  bqyxnhzjhw : entity work.mvnuw
    port map (tetw => rygltqf, zhcyjkgjp => km, wbtlonyq => zhnskayvh, p => vfvlmeo);
  zloxxpv : entity work.mvnuw
    port map (tetw => scbfqhn, zhcyjkgjp => opuoj, wbtlonyq => jwtvzwlfn, p => cdshjdfyc);
  
  -- Multi-driven assignments
  yfeohxfr <= yfeohxfr;
  yfeohxfr <= yfeohxfr;
end snaq;



-- Seed after: 13604870623775957421,12143220691580258643
