-- Seed: 4543145733029374020,3316342841050048249

entity tcmqcywyq is
  port (knxo : inout bit);
end tcmqcywyq;

architecture xrbgwes of tcmqcywyq is
  
begin
  -- Single-driven assignments
  knxo <= '0';
end xrbgwes;

library ieee;
use ieee.std_logic_1164.all;

entity ygtcybvmv is
  port (huiqfqqc : linkage severity_level; r : out boolean; bu : out std_logic_vector(2 downto 3); x : linkage std_logic);
end ygtcybvmv;

architecture celi of ygtcybvmv is
  signal wj : bit;
  signal xgjchgpgq : bit;
begin
  cwyn : entity work.tcmqcywyq
    port map (knxo => xgjchgpgq);
  wvcf : entity work.tcmqcywyq
    port map (knxo => wj);
  
  -- Single-driven assignments
  r <= r;
  
  -- Multi-driven assignments
  bu <= bu;
  bu <= (others => '0');
  bu <= bu;
end celi;

entity pyckdr is
  port (jwnx : linkage boolean; dizt : linkage character; l : in real);
end pyckdr;

library ieee;
use ieee.std_logic_1164.all;

architecture jhrdzm of pyckdr is
  signal hhsza : bit;
  signal lkenlyo : std_logic;
  signal wi : std_logic_vector(2 downto 3);
  signal qc : boolean;
  signal en : severity_level;
  signal kuervenef : std_logic;
  signal fkdbhl : std_logic_vector(2 downto 3);
  signal gpbybsr : boolean;
  signal me : severity_level;
begin
  bhcnnn : entity work.ygtcybvmv
    port map (huiqfqqc => me, r => gpbybsr, bu => fkdbhl, x => kuervenef);
  ncnw : entity work.ygtcybvmv
    port map (huiqfqqc => en, r => qc, bu => wi, x => lkenlyo);
  equ : entity work.tcmqcywyq
    port map (knxo => hhsza);
  
  -- Multi-driven assignments
  fkdbhl <= fkdbhl;
  fkdbhl <= fkdbhl;
  fkdbhl <= fkdbhl;
end jhrdzm;



-- Seed after: 15782145235019445103,3316342841050048249
