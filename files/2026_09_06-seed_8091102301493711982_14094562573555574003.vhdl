-- Seed: 8091102301493711982,14094562573555574003

entity dnsrxcv is
  port (okpcfbajm : out string(2 to 1); moukyji : buffer time; wfzzytvja : linkage severity_level; fykeym : linkage severity_level);
end dnsrxcv;

architecture jna of dnsrxcv is
  
begin
  -- Single-driven assignments
  moukyji <= 8#0_3_1# us;
  okpcfbajm <= "";
end jna;

entity vniduxycxh is
  port (b : inout boolean; swcjmhr : linkage real);
end vniduxycxh;

architecture hcwonwqv of vniduxycxh is
  signal i : severity_level;
  signal ueqvvddkt : severity_level;
  signal xziemsswd : time;
  signal ivojzfh : string(2 to 1);
begin
  wqvuatwvst : entity work.dnsrxcv
    port map (okpcfbajm => ivojzfh, moukyji => xziemsswd, wfzzytvja => ueqvvddkt, fykeym => i);
  
  -- Single-driven assignments
  b <= FALSE;
end hcwonwqv;

library ieee;
use ieee.std_logic_1164.all;

entity lymvsmlis is
  port (oozfygllh : out std_logic; n : linkage boolean_vector(2 to 3));
end lymvsmlis;

architecture frprgnour of lymvsmlis is
  signal bxcpkeak : severity_level;
  signal mcqhou : severity_level;
  signal xgzysgddbn : time;
  signal j : string(2 to 1);
begin
  geizo : entity work.dnsrxcv
    port map (okpcfbajm => j, moukyji => xgzysgddbn, wfzzytvja => mcqhou, fykeym => bxcpkeak);
end frprgnour;

library ieee;
use ieee.std_logic_1164.all;

entity efuzzcf is
  port (frog : in time; n : linkage std_logic; wmbicur : out std_logic; cvje : buffer std_logic_vector(3 downto 1));
end efuzzcf;

architecture pifgvqria of efuzzcf is
  signal ppedtibhfu : severity_level;
  signal wrhywga : severity_level;
  signal xhcx : time;
  signal phwdwxg : string(2 to 1);
  signal uldltdjyj : severity_level;
  signal rhh : severity_level;
  signal pyiflktas : time;
  signal gkz : string(2 to 1);
  signal rfzx : severity_level;
  signal jyz : severity_level;
  signal uuxxzkfml : time;
  signal sn : string(2 to 1);
begin
  oxqtrjnyb : entity work.dnsrxcv
    port map (okpcfbajm => sn, moukyji => uuxxzkfml, wfzzytvja => jyz, fykeym => rfzx);
  eq : entity work.dnsrxcv
    port map (okpcfbajm => gkz, moukyji => pyiflktas, wfzzytvja => rhh, fykeym => uldltdjyj);
  ptrx : entity work.dnsrxcv
    port map (okpcfbajm => phwdwxg, moukyji => xhcx, wfzzytvja => wrhywga, fykeym => ppedtibhfu);
  
  -- Multi-driven assignments
  wmbicur <= 'Z';
end pifgvqria;



-- Seed after: 10424792229898928121,14094562573555574003
