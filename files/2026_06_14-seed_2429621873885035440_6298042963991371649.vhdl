-- Seed: 2429621873885035440,6298042963991371649



entity oetnklbr is
  port (ocewpfs : out bit_vector(1 downto 1));
end oetnklbr;



architecture vuatz of oetnklbr is
  
begin
  
end vuatz;

library ieee;
use ieee.std_logic_1164.all;

entity xz is
  port (cunwi : in bit_vector(4 downto 0); gv : linkage boolean; byzvauak : inout boolean; o : linkage std_logic);
end xz;



architecture sxcvzv of xz is
  signal bfabtljfti : bit_vector(1 downto 1);
  signal fil : bit_vector(1 downto 1);
  signal r : bit_vector(1 downto 1);
  signal zeem : bit_vector(1 downto 1);
begin
  osrjbvub : entity work.oetnklbr
    port map (ocewpfs => zeem);
  puwspskix : entity work.oetnklbr
    port map (ocewpfs => r);
  jqqqdkjq : entity work.oetnklbr
    port map (ocewpfs => fil);
  jaidmgv : entity work.oetnklbr
    port map (ocewpfs => bfabtljfti);
end sxcvzv;

library ieee;
use ieee.std_logic_1164.all;

entity ta is
  port (nbzqx : out std_logic; ospoimuibd : linkage integer);
end ta;

library ieee;
use ieee.std_logic_1164.all;

architecture mwa of ta is
  signal yitff : std_logic;
  signal xdsyobkml : boolean;
  signal nckx : boolean;
  signal hv : bit_vector(4 downto 0);
  signal rzcpny : bit_vector(1 downto 1);
begin
  iv : entity work.oetnklbr
    port map (ocewpfs => rzcpny);
  fcrxh : entity work.xz
    port map (cunwi => hv, gv => nckx, byzvauak => xdsyobkml, o => yitff);
end mwa;



entity zyaeycv is
  port (zswbx : inout severity_level);
end zyaeycv;

library ieee;
use ieee.std_logic_1164.all;

architecture dwza of zyaeycv is
  signal bmdbff : integer;
  signal gcef : std_logic;
  signal kibrpxxu : bit_vector(1 downto 1);
begin
  tmhaucipnk : entity work.oetnklbr
    port map (ocewpfs => kibrpxxu);
  jl : entity work.ta
    port map (nbzqx => gcef, ospoimuibd => bmdbff);
end dwza;



-- Seed after: 4833700751787117953,6298042963991371649
