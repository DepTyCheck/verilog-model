-- Seed: 11921697134676932125,11387579217500963635

library ieee;
use ieee.std_logic_1164.all;

entity rba is
  port (t : buffer std_logic_vector(1 to 0); ndcuuo : in severity_level; h : buffer time; syglu : buffer integer);
end rba;



architecture dssz of rba is
  
begin
  
end dssz;

library ieee;
use ieee.std_logic_1164.all;

entity w is
  port (beon : buffer bit; pqlglwftj : buffer integer; hz : buffer std_logic_vector(0 to 0));
end w;

library ieee;
use ieee.std_logic_1164.all;

architecture qwb of w is
  signal hwsvrjkz : time;
  signal f : std_logic_vector(1 to 0);
  signal yao : integer;
  signal ujrcvt : time;
  signal ngzcy : severity_level;
  signal pgv : std_logic_vector(1 to 0);
  signal qpv : integer;
  signal wjn : time;
  signal brzfbc : severity_level;
  signal kftk : std_logic_vector(1 to 0);
  signal woylsjdx : integer;
  signal inhfugt : time;
  signal r : severity_level;
  signal s : std_logic_vector(1 to 0);
begin
  llby : entity work.rba
    port map (t => s, ndcuuo => r, h => inhfugt, syglu => woylsjdx);
  y : entity work.rba
    port map (t => kftk, ndcuuo => brzfbc, h => wjn, syglu => qpv);
  qsloacmdia : entity work.rba
    port map (t => pgv, ndcuuo => ngzcy, h => ujrcvt, syglu => yao);
  akq : entity work.rba
    port map (t => f, ndcuuo => brzfbc, h => hwsvrjkz, syglu => pqlglwftj);
end qwb;

library ieee;
use ieee.std_logic_1164.all;

entity d is
  port (gqpyhpocc : linkage severity_level; k : inout std_logic);
end d;

library ieee;
use ieee.std_logic_1164.all;

architecture yjpnatn of d is
  signal mueturruod : integer;
  signal kitmtoyfci : time;
  signal xvz : severity_level;
  signal dwceqe : std_logic_vector(1 to 0);
begin
  zjzb : entity work.rba
    port map (t => dwceqe, ndcuuo => xvz, h => kitmtoyfci, syglu => mueturruod);
end yjpnatn;



entity olphl is
  port (wmbny : linkage integer_vector(4 downto 1); ug : inout real);
end olphl;

library ieee;
use ieee.std_logic_1164.all;

architecture lv of olphl is
  signal wayws : std_logic_vector(0 to 0);
  signal ctxpiczfo : integer;
  signal cxu : bit;
  signal qwkgum : integer;
  signal opuellzl : time;
  signal qzhnjwaaa : integer;
  signal fwkddjjl : time;
  signal muxmtqovrg : severity_level;
  signal bt : std_logic_vector(1 to 0);
begin
  vuwuwz : entity work.rba
    port map (t => bt, ndcuuo => muxmtqovrg, h => fwkddjjl, syglu => qzhnjwaaa);
  ttjpre : entity work.rba
    port map (t => bt, ndcuuo => muxmtqovrg, h => opuellzl, syglu => qwkgum);
  gll : entity work.w
    port map (beon => cxu, pqlglwftj => ctxpiczfo, hz => wayws);
end lv;



-- Seed after: 5042047182525154649,11387579217500963635
