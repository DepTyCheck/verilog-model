-- Seed: 9302258738323212055,13479070923501788437

library ieee;
use ieee.std_logic_1164.all;

entity ksdvxied is
  port (re : buffer std_logic_vector(1 to 0); mzxdgnnru : out real; v : buffer time);
end ksdvxied;

architecture q of ksdvxied is
  
begin
  -- Single-driven assignments
  v <= 16#3.DB# ps;
  mzxdgnnru <= 16#BA67.8#;
  
  -- Multi-driven assignments
  re <= "";
  re <= "";
  re <= (others => '0');
  re <= "";
end q;

library ieee;
use ieee.std_logic_1164.all;

entity uzytij is
  port (actnqh : in time; jaxqish : in severity_level; odu : in std_logic_vector(1 downto 4); bjtmq : out severity_level);
end uzytij;

architecture ualoax of uzytij is
  
begin
  
end ualoax;

entity ogkofiowk is
  port (p : inout time; hfbtiqo : linkage boolean);
end ogkofiowk;

library ieee;
use ieee.std_logic_1164.all;

architecture cdf of ogkofiowk is
  signal mqiasfalcw : std_logic_vector(1 downto 4);
  signal nxvi : severity_level;
  signal jm : real;
  signal bwrnwnmosi : time;
  signal bor : real;
  signal sdfujxqby : std_logic_vector(1 to 0);
begin
  podmgy : entity work.ksdvxied
    port map (re => sdfujxqby, mzxdgnnru => bor, v => bwrnwnmosi);
  o : entity work.ksdvxied
    port map (re => sdfujxqby, mzxdgnnru => jm, v => p);
  invuuf : entity work.uzytij
    port map (actnqh => p, jaxqish => nxvi, odu => mqiasfalcw, bjtmq => nxvi);
  
  -- Multi-driven assignments
  sdfujxqby <= (others => '0');
  sdfujxqby <= (others => '0');
  sdfujxqby <= (others => '0');
end cdf;

entity rm is
  port (dfzjhqjtgk : linkage integer; mkwfmsru : inout bit; no : out real);
end rm;

library ieee;
use ieee.std_logic_1164.all;

architecture tggr of rm is
  signal nrfdk : severity_level;
  signal okbc : std_logic_vector(1 downto 4);
  signal jdzjwbjncf : severity_level;
  signal k : time;
  signal grk : time;
  signal tcawihl : std_logic_vector(1 to 0);
begin
  gya : entity work.ksdvxied
    port map (re => tcawihl, mzxdgnnru => no, v => grk);
  mhokrlkwc : entity work.uzytij
    port map (actnqh => k, jaxqish => jdzjwbjncf, odu => okbc, bjtmq => nrfdk);
end tggr;



-- Seed after: 2591312519811737023,13479070923501788437
