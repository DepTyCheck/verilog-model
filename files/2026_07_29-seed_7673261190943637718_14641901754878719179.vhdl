-- Seed: 7673261190943637718,14641901754878719179

library ieee;
use ieee.std_logic_1164.all;

entity ckb is
  port (ahi : inout std_logic_vector(3 downto 1); vnxh : in real; yyijgwhf : buffer time; tfqxti : inout time);
end ckb;

architecture z of ckb is
  
begin
  -- Single-driven assignments
  yyijgwhf <= 0 min;
  tfqxti <= 2#00# ms;
  
  -- Multi-driven assignments
  ahi <= ahi;
  ahi <= ahi;
  ahi <= ahi;
  ahi <= ('X', 'X', 'W');
end z;

library ieee;
use ieee.std_logic_1164.all;

entity agsif is
  port (iu : in std_logic; kuqm : inout std_logic_vector(0 downto 3); o : in std_logic; sgbqsgwc : out real);
end agsif;

library ieee;
use ieee.std_logic_1164.all;

architecture nnub of agsif is
  signal bi : time;
  signal nrp : time;
  signal p : real;
  signal knmfatak : time;
  signal jcu : time;
  signal at : std_logic_vector(3 downto 1);
  signal corljqj : time;
  signal eaeuq : time;
  signal pghl : real;
  signal c : std_logic_vector(3 downto 1);
  signal gcpp : time;
  signal wooiz : time;
  signal ojnb : real;
  signal ncnnb : std_logic_vector(3 downto 1);
begin
  nkrgifkdw : entity work.ckb
    port map (ahi => ncnnb, vnxh => ojnb, yyijgwhf => wooiz, tfqxti => gcpp);
  uxoj : entity work.ckb
    port map (ahi => c, vnxh => pghl, yyijgwhf => eaeuq, tfqxti => corljqj);
  vowiqv : entity work.ckb
    port map (ahi => at, vnxh => sgbqsgwc, yyijgwhf => jcu, tfqxti => knmfatak);
  jepdzlrkgf : entity work.ckb
    port map (ahi => c, vnxh => p, yyijgwhf => nrp, tfqxti => bi);
  
  -- Single-driven assignments
  sgbqsgwc <= sgbqsgwc;
  ojnb <= pghl;
  
  -- Multi-driven assignments
  kuqm <= (others => '0');
end nnub;

entity jabvq is
  port (yqwlltul : buffer integer; vxvelqza : in time; yawt : out boolean);
end jabvq;

library ieee;
use ieee.std_logic_1164.all;

architecture gnumm of jabvq is
  signal yobhse : time;
  signal vjtayiea : time;
  signal i : std_logic_vector(3 downto 1);
  signal eq : time;
  signal fyvl : time;
  signal ajzzdstzu : real;
  signal x : std_logic_vector(3 downto 1);
  signal ivvmaxy : real;
  signal igbili : std_logic;
  signal ynwrwkgt : std_logic_vector(0 downto 3);
  signal cegblvat : std_logic;
begin
  ra : entity work.agsif
    port map (iu => cegblvat, kuqm => ynwrwkgt, o => igbili, sgbqsgwc => ivvmaxy);
  czyctpcp : entity work.ckb
    port map (ahi => x, vnxh => ajzzdstzu, yyijgwhf => fyvl, tfqxti => eq);
  izh : entity work.ckb
    port map (ahi => i, vnxh => ivvmaxy, yyijgwhf => vjtayiea, tfqxti => yobhse);
  
  -- Single-driven assignments
  yqwlltul <= 0_4;
  yawt <= TRUE;
  ajzzdstzu <= ivvmaxy;
  
  -- Multi-driven assignments
  i <= x;
  igbili <= igbili;
  x <= ('X', '-', 'U');
end gnumm;

entity kxulz is
  port (rbj : inout real);
end kxulz;

library ieee;
use ieee.std_logic_1164.all;

architecture sgtcybcc of kxulz is
  signal rnfirb : time;
  signal xqjeaocjd : time;
  signal bz : real;
  signal lg : std_logic_vector(3 downto 1);
  signal eqztwnp : time;
  signal srrie : time;
  signal qkcxadvo : std_logic_vector(3 downto 1);
begin
  kd : entity work.ckb
    port map (ahi => qkcxadvo, vnxh => rbj, yyijgwhf => srrie, tfqxti => eqztwnp);
  hk : entity work.ckb
    port map (ahi => lg, vnxh => bz, yyijgwhf => xqjeaocjd, tfqxti => rnfirb);
  
  -- Single-driven assignments
  rbj <= 2#1_1_1.1#;
  
  -- Multi-driven assignments
  qkcxadvo <= "W1H";
  qkcxadvo <= ('U', 'X', 'X');
  lg <= ('U', 'H', 'L');
  qkcxadvo <= lg;
end sgtcybcc;



-- Seed after: 9201304897343280965,14641901754878719179
