-- Seed: 1998055637472750376,18037650846010261179

library ieee;
use ieee.std_logic_1164.all;

entity arcgsbk is
  port (z : buffer time; udeson : linkage std_logic; cvhf : buffer time);
end arcgsbk;

architecture xkkpczekyb of arcgsbk is
  
begin
  -- Single-driven assignments
  cvhf <= cvhf;
  z <= cvhf;
end xkkpczekyb;

library ieee;
use ieee.std_logic_1164.all;

entity re is
  port (rtbgdbbnpj : out real; f : out real; eqgxea : linkage std_logic_vector(3 downto 3); wpul : inout time_vector(3 to 4));
end re;

library ieee;
use ieee.std_logic_1164.all;

architecture zn of re is
  signal awrhzr : time;
  signal hyv : std_logic;
  signal s : time;
  signal wsnjv : time;
  signal dlumspy : std_logic;
  signal n : time;
begin
  tyzqawiyhe : entity work.arcgsbk
    port map (z => n, udeson => dlumspy, cvhf => wsnjv);
  qmoso : entity work.arcgsbk
    port map (z => s, udeson => hyv, cvhf => awrhzr);
  
  -- Single-driven assignments
  wpul <= wpul;
  f <= 8#2.7_1_4#;
  rtbgdbbnpj <= f;
  
  -- Multi-driven assignments
  dlumspy <= 'X';
  hyv <= dlumspy;
  dlumspy <= dlumspy;
  dlumspy <= '0';
end zn;

entity nyfigdmyi is
  port (omkgqlewy : out integer; hzsktlq : in boolean_vector(1 to 2));
end nyfigdmyi;

library ieee;
use ieee.std_logic_1164.all;

architecture bivs of nyfigdmyi is
  signal lbosz : time_vector(3 to 4);
  signal vgdlvwlco : std_logic_vector(3 downto 3);
  signal lqnciczbg : real;
  signal hfyddut : real;
  signal opgp : time;
  signal cvyoepfzh : time;
  signal ijgqfxyocz : time;
  signal o : time;
  signal ffqbkzwhj : time;
  signal conum : std_logic;
  signal efkk : time;
begin
  lkz : entity work.arcgsbk
    port map (z => efkk, udeson => conum, cvhf => ffqbkzwhj);
  e : entity work.arcgsbk
    port map (z => o, udeson => conum, cvhf => ijgqfxyocz);
  wbnact : entity work.arcgsbk
    port map (z => cvyoepfzh, udeson => conum, cvhf => opgp);
  edtiewt : entity work.re
    port map (rtbgdbbnpj => hfyddut, f => lqnciczbg, eqgxea => vgdlvwlco, wpul => lbosz);
  
  -- Single-driven assignments
  omkgqlewy <= 8#60244#;
  
  -- Multi-driven assignments
  conum <= conum;
end bivs;



-- Seed after: 8719295713698019605,18037650846010261179
