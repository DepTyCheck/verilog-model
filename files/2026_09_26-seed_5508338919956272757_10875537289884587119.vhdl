-- Seed: 5508338919956272757,10875537289884587119

entity rht is
  port (judbtrx : out time; y : linkage time_vector(0 to 0));
end rht;

architecture pmlsw of rht is
  
begin
  -- Single-driven assignments
  judbtrx <= judbtrx;
end pmlsw;

library ieee;
use ieee.std_logic_1164.all;

entity cuqwclbj is
  port (kmd : inout real; oqqbkq : out integer; so : buffer std_logic_vector(4 downto 4));
end cuqwclbj;

architecture zx of cuqwclbj is
  signal hcgnxeem : time_vector(0 to 0);
  signal mdbgndmehd : time;
  signal wo : time_vector(0 to 0);
  signal qp : time;
  signal uxity : time_vector(0 to 0);
  signal mr : time;
begin
  aw : entity work.rht
    port map (judbtrx => mr, y => uxity);
  ntjcq : entity work.rht
    port map (judbtrx => qp, y => wo);
  ssx : entity work.rht
    port map (judbtrx => mdbgndmehd, y => hcgnxeem);
  
  -- Single-driven assignments
  oqqbkq <= oqqbkq;
  kmd <= kmd;
end zx;

library ieee;
use ieee.std_logic_1164.all;

entity xbjbvjlfa is
  port ( szywytqu : inout bit_vector(3 downto 3)
  ; asvqsup : out severity_level
  ; gqi : out std_logic_vector(2 downto 3)
  ; srmumiszev : out time_vector(1 downto 4)
  );
end xbjbvjlfa;

architecture r of xbjbvjlfa is
  signal vp : time_vector(0 to 0);
  signal kjo : time;
  signal zradkyyvb : time_vector(0 to 0);
  signal nljhipp : time;
begin
  nlhjxpks : entity work.rht
    port map (judbtrx => nljhipp, y => zradkyyvb);
  qkcxg : entity work.rht
    port map (judbtrx => kjo, y => vp);
  
  -- Single-driven assignments
  asvqsup <= ERROR;
  
  -- Multi-driven assignments
  gqi <= "";
  gqi <= gqi;
end r;



-- Seed after: 5760524746514192949,10875537289884587119
