-- Seed: 10991140733774997802,11181851762153539145



entity qe is
  port (avgb : in severity_level);
end qe;



architecture qur of qe is
  
begin
  
end qur;

library ieee;
use ieee.std_logic_1164.all;

entity cukszz is
  port (zvzhgkouka : buffer std_logic_vector(4 downto 1); dl : out real; efxjp : inout real; gyjpfuhpn : in std_logic);
end cukszz;



architecture v of cukszz is
  signal sut : severity_level;
  signal w : severity_level;
begin
  b : entity work.qe
    port map (avgb => w);
  nazg : entity work.qe
    port map (avgb => sut);
  mmwn : entity work.qe
    port map (avgb => w);
end v;

library ieee;
use ieee.std_logic_1164.all;

entity mczieblox is
  port (zsrfmibwf : inout std_logic; xrlkftkcq : linkage time; kwxgafx : linkage bit_vector(2 to 2));
end mczieblox;

library ieee;
use ieee.std_logic_1164.all;

architecture asv of mczieblox is
  signal v : std_logic;
  signal xnlio : real;
  signal goc : real;
  signal ds : std_logic_vector(4 downto 1);
begin
  a : entity work.cukszz
    port map (zvzhgkouka => ds, dl => goc, efxjp => xnlio, gyjpfuhpn => v);
end asv;



-- Seed after: 4345815931262894006,11181851762153539145
