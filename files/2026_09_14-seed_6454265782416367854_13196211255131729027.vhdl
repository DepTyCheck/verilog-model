-- Seed: 6454265782416367854,13196211255131729027

library ieee;
use ieee.std_logic_1164.all;

entity ynr is
  port (dti : out integer; al : linkage std_logic_vector(1 downto 3); ujeo : in boolean; vvealoyy : inout time);
end ynr;

architecture vujldsklur of ynr is
  
begin
  -- Single-driven assignments
  vvealoyy <= 0342.1 us;
  dti <= dti;
end vujldsklur;

entity ntoqop is
  port (lvpu : linkage boolean_vector(0 downto 4); zbyos : out string(5 downto 4));
end ntoqop;

library ieee;
use ieee.std_logic_1164.all;

architecture nlplndeax of ntoqop is
  signal ebrjamqqr : time;
  signal owdp : boolean;
  signal eeak : integer;
  signal ikpvytk : time;
  signal yp : boolean;
  signal fqgpiiiwr : integer;
  signal jofclqhxg : time;
  signal cnnm : boolean;
  signal sxwjbuiaem : std_logic_vector(1 downto 3);
  signal dkbpxgoaoa : integer;
  signal p : time;
  signal jtufzny : boolean;
  signal igqd : std_logic_vector(1 downto 3);
  signal hz : integer;
begin
  k : entity work.ynr
    port map (dti => hz, al => igqd, ujeo => jtufzny, vvealoyy => p);
  oltgfn : entity work.ynr
    port map (dti => dkbpxgoaoa, al => sxwjbuiaem, ujeo => cnnm, vvealoyy => jofclqhxg);
  srhdgm : entity work.ynr
    port map (dti => fqgpiiiwr, al => igqd, ujeo => yp, vvealoyy => ikpvytk);
  yfbicdayzn : entity work.ynr
    port map (dti => eeak, al => igqd, ujeo => owdp, vvealoyy => ebrjamqqr);
  
  -- Single-driven assignments
  zbyos <= zbyos;
  yp <= jtufzny;
  jtufzny <= jtufzny;
end nlplndeax;



-- Seed after: 9597440124523268437,13196211255131729027
