-- Seed: 3528111943013529270,11481034001933599325

library ieee;
use ieee.std_logic_1164.all;

entity mrihjxn is
  port (p : linkage time_vector(4 to 0); cl : buffer boolean; ckwab : out std_logic_vector(1 to 0));
end mrihjxn;

architecture xjfn of mrihjxn is
  
begin
  -- Single-driven assignments
  cl <= cl;
end xjfn;

library ieee;
use ieee.std_logic_1164.all;

entity og is
  port (djpo : buffer std_logic; rsqiybu : linkage character; dijfyvqe : linkage real; uk : inout bit);
end og;

library ieee;
use ieee.std_logic_1164.all;

architecture wn of og is
  signal uetwonqwbx : boolean;
  signal aemr : time_vector(4 to 0);
  signal hik : boolean;
  signal qfu : time_vector(4 to 0);
  signal da : std_logic_vector(1 to 0);
  signal mdfeokd : boolean;
  signal mbzmgpc : time_vector(4 to 0);
begin
  csidjir : entity work.mrihjxn
    port map (p => mbzmgpc, cl => mdfeokd, ckwab => da);
  u : entity work.mrihjxn
    port map (p => qfu, cl => hik, ckwab => da);
  rjeirtopzr : entity work.mrihjxn
    port map (p => aemr, cl => uetwonqwbx, ckwab => da);
  
  -- Single-driven assignments
  uk <= '0';
  
  -- Multi-driven assignments
  da <= "";
end wn;

entity vrlhogp is
  port (hbz : inout time);
end vrlhogp;

library ieee;
use ieee.std_logic_1164.all;

architecture sg of vrlhogp is
  signal lw : std_logic_vector(1 to 0);
  signal hxd : boolean;
  signal jinbuzjzrt : time_vector(4 to 0);
  signal otyyunkn : std_logic_vector(1 to 0);
  signal vxcvnibfe : boolean;
  signal enj : time_vector(4 to 0);
begin
  h : entity work.mrihjxn
    port map (p => enj, cl => vxcvnibfe, ckwab => otyyunkn);
  fxurwv : entity work.mrihjxn
    port map (p => jinbuzjzrt, cl => hxd, ckwab => lw);
  
  -- Single-driven assignments
  hbz <= hbz;
  
  -- Multi-driven assignments
  otyyunkn <= "";
  otyyunkn <= "";
  otyyunkn <= (others => '0');
end sg;



-- Seed after: 13417098686207294153,11481034001933599325
