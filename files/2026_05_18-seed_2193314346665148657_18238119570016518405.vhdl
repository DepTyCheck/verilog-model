-- Seed: 2193314346665148657,18238119570016518405



entity l is
  port (phyqy : inout time);
end l;



architecture lyz of l is
  
begin
  
end lyz;

library ieee;
use ieee.std_logic_1164.all;

entity isvqtirdnz is
  port (sm : linkage time_vector(1 downto 4); ltcr : inout time; vcdjrqbqi : linkage real; rruil : buffer std_logic);
end isvqtirdnz;



architecture xawwwyh of isvqtirdnz is
  signal rmx : time;
begin
  nmz : entity work.l
    port map (phyqy => rmx);
end xawwwyh;



entity kempy is
  port (mozcmafy : linkage boolean_vector(0 downto 1));
end kempy;

library ieee;
use ieee.std_logic_1164.all;

architecture e of kempy is
  signal jg : real;
  signal bpzvsnz : time;
  signal fmjqvcc : time;
  signal qftgixpnj : std_logic;
  signal qegjoqi : real;
  signal xyiwuge : time;
  signal ldymfgs : time_vector(1 downto 4);
begin
  jgc : entity work.isvqtirdnz
    port map (sm => ldymfgs, ltcr => xyiwuge, vcdjrqbqi => qegjoqi, rruil => qftgixpnj);
  qos : entity work.l
    port map (phyqy => fmjqvcc);
  rpdklb : entity work.isvqtirdnz
    port map (sm => ldymfgs, ltcr => bpzvsnz, vcdjrqbqi => jg, rruil => qftgixpnj);
end e;



-- Seed after: 1891758075177917518,18238119570016518405
