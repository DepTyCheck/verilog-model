-- Seed: 17960260571894583191,13694093582652240945

entity qsxwzcbtf is
  port (vpmrgpfr : out boolean; ebhbfs : inout time_vector(2 to 1); k : in string(5 to 1); b : out real);
end qsxwzcbtf;

architecture fbhem of qsxwzcbtf is
  
begin
  -- Single-driven assignments
  b <= 24330.2_3_3;
  ebhbfs <= (others => 0 ns);
  vpmrgpfr <= FALSE;
end fbhem;

library ieee;
use ieee.std_logic_1164.all;

entity ts is
  port (nsmmf : in std_logic);
end ts;

architecture qujp of ts is
  
begin
  
end qujp;

library ieee;
use ieee.std_logic_1164.all;

entity mlifuhrwn is
  port (zkazff : in std_logic);
end mlifuhrwn;

architecture nczjdwauia of mlifuhrwn is
  signal tmanke : real;
  signal jowno : time_vector(2 to 1);
  signal uvi : boolean;
  signal cssoxg : real;
  signal u : string(5 to 1);
  signal hcim : time_vector(2 to 1);
  signal xwvqppoz : boolean;
begin
  hrl : entity work.qsxwzcbtf
    port map (vpmrgpfr => xwvqppoz, ebhbfs => hcim, k => u, b => cssoxg);
  fvfnfg : entity work.qsxwzcbtf
    port map (vpmrgpfr => uvi, ebhbfs => jowno, k => u, b => tmanke);
end nczjdwauia;



-- Seed after: 16542362756374949404,13694093582652240945
