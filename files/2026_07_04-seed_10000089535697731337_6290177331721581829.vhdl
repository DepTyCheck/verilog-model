-- Seed: 10000089535697731337,6290177331721581829

entity zuukwqzp is
  port (g : linkage integer);
end zuukwqzp;

architecture sxrwdfdfd of zuukwqzp is
  
begin
  
end sxrwdfdfd;

use std.reflection.all;

entity iqwj is
  port (rupaeq : inout record_value_mirror; qiwg : inout file_value_mirror; obtlgrcv : inout access_subtype_mirror);
end iqwj;

architecture beltkfrg of iqwj is
  signal pqgcf : integer;
  signal zzhk : integer;
  signal w : integer;
begin
  smeuadowze : entity work.zuukwqzp
    port map (g => w);
  pjwe : entity work.zuukwqzp
    port map (g => zzhk);
  fxy : entity work.zuukwqzp
    port map (g => pqgcf);
end beltkfrg;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity caktbpmhrp is
  port (fifmdc : in std_logic; z : inout enumeration_subtype_mirror; hyzidhrc : inout protected_value_mirror; lvdq : inout enumeration_subtype_mirror);
end caktbpmhrp;

architecture myvtvttyk of caktbpmhrp is
  signal gkhw : integer;
begin
  pngf : entity work.zuukwqzp
    port map (g => gkhw);
end myvtvttyk;

use std.reflection.all;

entity xxpogd is
  port (wndct : inout file_subtype_mirror);
end xxpogd;

use std.reflection.all;

architecture nyatr of xxpogd is
  shared variable wyzdvvye : access_subtype_mirror;
  shared variable ckwwqbssly : file_value_mirror;
  shared variable tputdvux : record_value_mirror;
  shared variable mmxtninyh : access_subtype_mirror;
  shared variable gqe : file_value_mirror;
  shared variable loaxeip : record_value_mirror;
  shared variable viaodebc : access_subtype_mirror;
  shared variable tyefael : file_value_mirror;
  shared variable g : record_value_mirror;
begin
  pogcb : entity work.iqwj
    port map (rupaeq => g, qiwg => tyefael, obtlgrcv => viaodebc);
  urhw : entity work.iqwj
    port map (rupaeq => loaxeip, qiwg => gqe, obtlgrcv => mmxtninyh);
  yprprwvldf : entity work.iqwj
    port map (rupaeq => tputdvux, qiwg => ckwwqbssly, obtlgrcv => wyzdvvye);
end nyatr;



-- Seed after: 11156294457938859363,6290177331721581829
