-- Seed: 6141653159083107272,3181554006726329157

use std.reflection.all;

entity ekc is
  port (irlwa : inout subtype_mirror; oedawdrn : out real; dycxkd : inout floating_value_mirror);
end ekc;

architecture ebnkyks of ekc is
  
begin
  
end ebnkyks;

use std.reflection.all;

entity qu is
  port (ycwicwhp : inout protected_subtype_mirror; ma : inout record_value_mirror; uptrvbn : inout real; jqdvldrx : out integer);
end qu;

use std.reflection.all;

architecture hyduzo of qu is
  shared variable nerf : floating_value_mirror;
  signal gojm : real;
  shared variable itfx : subtype_mirror;
  shared variable womivel : floating_value_mirror;
  shared variable uv : subtype_mirror;
  shared variable ggqoftwmo : floating_value_mirror;
  signal cyaibfkrno : real;
  shared variable p : subtype_mirror;
begin
  fjjnxxmo : entity work.ekc
    port map (irlwa => p, oedawdrn => cyaibfkrno, dycxkd => ggqoftwmo);
  xybtdjy : entity work.ekc
    port map (irlwa => uv, oedawdrn => uptrvbn, dycxkd => womivel);
  bh : entity work.ekc
    port map (irlwa => itfx, oedawdrn => gojm, dycxkd => nerf);
  
  -- Single-driven assignments
  jqdvldrx <= 04;
end hyduzo;

use std.reflection.all;

entity q is
  port (cebhai : inout access_value_mirror; kuhzok : out time);
end q;

use std.reflection.all;

architecture nwg of q is
  signal wvhmpnwch : integer;
  signal jhmhrbo : real;
  shared variable nrqods : record_value_mirror;
  shared variable fevfimj : protected_subtype_mirror;
begin
  phwxtp : entity work.qu
    port map (ycwicwhp => fevfimj, ma => nrqods, uptrvbn => jhmhrbo, jqdvldrx => wvhmpnwch);
  
  -- Single-driven assignments
  kuhzok <= 4 sec;
end nwg;

entity mfaxcellbq is
  port (dccunegoj : linkage time);
end mfaxcellbq;

use std.reflection.all;

architecture wswp of mfaxcellbq is
  shared variable sypgph : floating_value_mirror;
  signal a : real;
  shared variable cwozoc : subtype_mirror;
  shared variable g : floating_value_mirror;
  signal pmtkyhwblz : real;
  shared variable zkqabdythy : subtype_mirror;
  shared variable dxeiay : floating_value_mirror;
  signal coev : real;
  shared variable ucxzzecmqg : subtype_mirror;
  shared variable gzmwtelnc : floating_value_mirror;
  signal bvtec : real;
  shared variable tbagwwcco : subtype_mirror;
begin
  ycaxjom : entity work.ekc
    port map (irlwa => tbagwwcco, oedawdrn => bvtec, dycxkd => gzmwtelnc);
  y : entity work.ekc
    port map (irlwa => ucxzzecmqg, oedawdrn => coev, dycxkd => dxeiay);
  qqxzouyh : entity work.ekc
    port map (irlwa => zkqabdythy, oedawdrn => pmtkyhwblz, dycxkd => g);
  qsakmhh : entity work.ekc
    port map (irlwa => cwozoc, oedawdrn => a, dycxkd => sypgph);
end wswp;



-- Seed after: 7332946197486549174,3181554006726329157
