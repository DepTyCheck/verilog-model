-- Seed: 10536682192639094904,8437298063418820479

entity llksdzca is
  port (oa : out string(4 downto 4); o : in boolean);
end llksdzca;

architecture otehtovz of llksdzca is
  
begin
  -- Single-driven assignments
  oa <= "h";
end otehtovz;

library ieee;
use ieee.std_logic_1164.all;

entity dzartivf is
  port (vuaqki : out time; mwxbaqar : buffer std_logic_vector(3 to 0); io : buffer std_logic);
end dzartivf;

architecture h of dzartivf is
  signal qvlmerjy : boolean;
  signal cmru : string(4 downto 4);
  signal fjpzkvxa : boolean;
  signal mtmhulc : string(4 downto 4);
  signal k : boolean;
  signal kx : string(4 downto 4);
begin
  kge : entity work.llksdzca
    port map (oa => kx, o => k);
  yqbn : entity work.llksdzca
    port map (oa => mtmhulc, o => fjpzkvxa);
  cjmoigddh : entity work.llksdzca
    port map (oa => cmru, o => qvlmerjy);
  
  -- Single-driven assignments
  k <= k;
  
  -- Multi-driven assignments
  mwxbaqar <= mwxbaqar;
  io <= io;
  io <= io;
end h;

library ieee;
use ieee.std_logic_1164.all;

entity wymy is
  port (tzml : inout std_logic_vector(2 to 3); xa : in time);
end wymy;

architecture gsszsuqov of wymy is
  signal shot : boolean;
  signal wuztrhv : string(4 downto 4);
  signal xjx : boolean;
  signal j : string(4 downto 4);
begin
  e : entity work.llksdzca
    port map (oa => j, o => xjx);
  rmwfrbuxz : entity work.llksdzca
    port map (oa => wuztrhv, o => shot);
  
  -- Single-driven assignments
  shot <= TRUE;
  xjx <= xjx;
  
  -- Multi-driven assignments
  tzml <= ('1', 'H');
end gsszsuqov;

entity wq is
  port (s : inout character; rjard : in time);
end wq;

library ieee;
use ieee.std_logic_1164.all;

architecture rinzbtwkwi of wq is
  signal q : boolean;
  signal lx : string(4 downto 4);
  signal o : std_logic_vector(2 to 3);
begin
  stckvwexu : entity work.wymy
    port map (tzml => o, xa => rjard);
  pthq : entity work.llksdzca
    port map (oa => lx, o => q);
  
  -- Single-driven assignments
  q <= q;
  s <= 'g';
  
  -- Multi-driven assignments
  o <= ('H', 'W');
  o <= ('-', 'X');
  o <= "H0";
end rinzbtwkwi;



-- Seed after: 1957001292864052573,8437298063418820479
