-- Seed: 6191544753370227017,1112937151005418631

library ieee;
use ieee.std_logic_1164.all;

entity ytc is
  port (kikllgwhsq : in std_logic_vector(1 downto 2));
end ytc;

architecture eqdazwpdm of ytc is
  
begin
  
end eqdazwpdm;

library ieee;
use ieee.std_logic_1164.all;

entity gn is
  port (uie : linkage std_logic_vector(1 downto 2); ebn : in boolean; r : out real; vlkolprk : in time);
end gn;

library ieee;
use ieee.std_logic_1164.all;

architecture kv of gn is
  signal eavk : std_logic_vector(1 downto 2);
  signal bhepzcq : std_logic_vector(1 downto 2);
begin
  cngbw : entity work.ytc
    port map (kikllgwhsq => bhepzcq);
  ekvnzx : entity work.ytc
    port map (kikllgwhsq => bhepzcq);
  enpn : entity work.ytc
    port map (kikllgwhsq => bhepzcq);
  qcadyaowsx : entity work.ytc
    port map (kikllgwhsq => eavk);
  
  -- Single-driven assignments
  r <= r;
  
  -- Multi-driven assignments
  bhepzcq <= "";
end kv;

entity qj is
  port (ye : out integer; xjecfoma : in integer);
end qj;

library ieee;
use ieee.std_logic_1164.all;

architecture b of qj is
  signal lhdb : real;
  signal i : boolean;
  signal yu : time;
  signal m : real;
  signal nmz : boolean;
  signal hx : std_logic_vector(1 downto 2);
  signal sgjjph : std_logic_vector(1 downto 2);
  signal odzuj : std_logic_vector(1 downto 2);
begin
  lfvmpgp : entity work.ytc
    port map (kikllgwhsq => odzuj);
  ihynttbw : entity work.ytc
    port map (kikllgwhsq => sgjjph);
  wqzjqmrd : entity work.gn
    port map (uie => hx, ebn => nmz, r => m, vlkolprk => yu);
  om : entity work.gn
    port map (uie => odzuj, ebn => i, r => lhdb, vlkolprk => yu);
  
  -- Multi-driven assignments
  odzuj <= sgjjph;
  odzuj <= sgjjph;
  odzuj <= hx;
  odzuj <= (others => '0');
end b;

entity uuqg is
  port (db : buffer time; ofge : linkage boolean_vector(4 to 3); baqm : inout boolean; bobxtlhcq : inout integer);
end uuqg;

architecture rwkiysvw of uuqg is
  signal vuxqshramv : integer;
  signal xq : integer;
begin
  ppwu : entity work.qj
    port map (ye => xq, xjecfoma => vuxqshramv);
  evfnivc : entity work.qj
    port map (ye => bobxtlhcq, xjecfoma => bobxtlhcq);
  
  -- Single-driven assignments
  baqm <= baqm;
  vuxqshramv <= 2#011#;
  db <= 3 ps;
end rwkiysvw;



-- Seed after: 10881742709030230103,1112937151005418631
