-- Seed: 8494668579971904822,16188444798499499427

entity lkghe is
  port (tykcs : buffer boolean; nxnzh : out bit_vector(3 to 2));
end lkghe;

architecture vcbp of lkghe is
  
begin
  -- Single-driven assignments
  nxnzh <= nxnzh;
  tykcs <= tykcs;
end vcbp;

entity uymhwikj is
  port (brnztrh : out time);
end uymhwikj;

architecture yqad of uymhwikj is
  signal t : bit_vector(3 to 2);
  signal fpahaw : boolean;
  signal sw : bit_vector(3 to 2);
  signal bhuae : boolean;
begin
  zrutcsg : entity work.lkghe
    port map (tykcs => bhuae, nxnzh => sw);
  purhcmgfsd : entity work.lkghe
    port map (tykcs => fpahaw, nxnzh => t);
  
  -- Single-driven assignments
  brnztrh <= brnztrh;
end yqad;

library ieee;
use ieee.std_logic_1164.all;

entity mp is
  port (tmq : out real; dhrspiw : inout std_logic_vector(4 to 2));
end mp;

architecture vny of mp is
  signal owcppkjd : bit_vector(3 to 2);
  signal hqih : boolean;
begin
  sjfyp : entity work.lkghe
    port map (tykcs => hqih, nxnzh => owcppkjd);
  
  -- Single-driven assignments
  tmq <= tmq;
end vny;

library ieee;
use ieee.std_logic_1164.all;

entity irsyzlic is
  port (ebucsxm : inout std_logic_vector(2 to 1); biyzrmyh : out real);
end irsyzlic;

library ieee;
use ieee.std_logic_1164.all;

architecture rkw of irsyzlic is
  signal qt : bit_vector(3 to 2);
  signal cmkc : boolean;
  signal op : std_logic_vector(4 to 2);
  signal jmyz : real;
  signal wsdbontk : time;
begin
  rv : entity work.uymhwikj
    port map (brnztrh => wsdbontk);
  rsx : entity work.mp
    port map (tmq => jmyz, dhrspiw => op);
  cjm : entity work.lkghe
    port map (tykcs => cmkc, nxnzh => qt);
  
  -- Single-driven assignments
  biyzrmyh <= biyzrmyh;
  
  -- Multi-driven assignments
  ebucsxm <= "";
  ebucsxm <= ebucsxm;
end rkw;



-- Seed after: 17460510764579999977,16188444798499499427
