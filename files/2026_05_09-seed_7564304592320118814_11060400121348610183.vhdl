-- Seed: 7564304592320118814,11060400121348610183



entity mlt is
  port (clbm : inout integer; sliqbgpwn : linkage real; n : buffer time);
end mlt;



architecture vgirjvs of mlt is
  
begin
  
end vgirjvs;



entity ifh is
  port (hqsxdhc : out severity_level; o : in real);
end ifh;



architecture faxyfety of ifh is
  signal ik : time;
  signal yryo : real;
  signal spq : integer;
  signal xobfpnslv : time;
  signal su : integer;
  signal drcfmt : time;
  signal nac : real;
  signal mikijosff : integer;
begin
  qnzemstl : entity work.mlt
    port map (clbm => mikijosff, sliqbgpwn => nac, n => drcfmt);
  wn : entity work.mlt
    port map (clbm => su, sliqbgpwn => o, n => xobfpnslv);
  vglabdp : entity work.mlt
    port map (clbm => spq, sliqbgpwn => yryo, n => ik);
end faxyfety;



entity ryyu is
  port (hqxpduioa : out integer; bxwswlnzaz : in real; db : inout real);
end ryyu;



architecture qa of ryyu is
  signal m : real;
  signal n : severity_level;
  signal fxao : time;
  signal xiw : integer;
  signal odwhtsb : real;
  signal cipc : severity_level;
  signal jchprbuhj : time;
  signal rwwgdogkw : integer;
begin
  kmb : entity work.mlt
    port map (clbm => rwwgdogkw, sliqbgpwn => db, n => jchprbuhj);
  fgczmgicgv : entity work.ifh
    port map (hqsxdhc => cipc, o => odwhtsb);
  dwvlgvhfqu : entity work.mlt
    port map (clbm => xiw, sliqbgpwn => bxwswlnzaz, n => fxao);
  cqu : entity work.ifh
    port map (hqsxdhc => n, o => m);
end qa;



-- Seed after: 14812987104328717130,11060400121348610183
