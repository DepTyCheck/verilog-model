-- Seed: 3538473637350997063,8089241273282434469

library ieee;
use ieee.std_logic_1164.all;

entity txapus is
  port (qlpk : inout time; pk : inout real; mcbhhzyry : out character; rahpakfmwe : linkage std_logic_vector(3 to 3));
end txapus;



architecture hs of txapus is
  
begin
  
end hs;



entity nbealkg is
  port (kgnnmcwsj : out integer; skvx : in bit_vector(1 to 3));
end nbealkg;

library ieee;
use ieee.std_logic_1164.all;

architecture dj of nbealkg is
  signal dl : character;
  signal vzbdzdehg : real;
  signal gdelin : time;
  signal jeqixgryu : character;
  signal iovntp : real;
  signal bf : time;
  signal k : std_logic_vector(3 to 3);
  signal nkkhewmak : character;
  signal qezoh : real;
  signal ftjyoqo : time;
begin
  bythg : entity work.txapus
    port map (qlpk => ftjyoqo, pk => qezoh, mcbhhzyry => nkkhewmak, rahpakfmwe => k);
  tncqfz : entity work.txapus
    port map (qlpk => bf, pk => iovntp, mcbhhzyry => jeqixgryu, rahpakfmwe => k);
  ag : entity work.txapus
    port map (qlpk => gdelin, pk => vzbdzdehg, mcbhhzyry => dl, rahpakfmwe => k);
end dj;



-- Seed after: 14143824685777967037,8089241273282434469
