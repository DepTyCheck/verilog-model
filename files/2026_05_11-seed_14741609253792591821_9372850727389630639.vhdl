-- Seed: 14741609253792591821,9372850727389630639



entity mfqkaveq is
  port (wtvcpg : buffer severity_level; a : buffer real; l : buffer real; kgx : buffer bit);
end mfqkaveq;



architecture ngueskmlvv of mfqkaveq is
  
begin
  
end ngueskmlvv;



entity xk is
  port (lgh : in integer; jijgnctiez : linkage severity_level);
end xk;



architecture dfpl of xk is
  signal qr : bit;
  signal pdkbnpsiz : real;
  signal rlzq : real;
  signal aejl : severity_level;
  signal igj : bit;
  signal xqpprxtg : real;
  signal erlb : real;
  signal atamcnelkw : severity_level;
  signal gbqm : bit;
  signal unp : real;
  signal up : real;
  signal etnjednomb : severity_level;
  signal byktqiv : bit;
  signal yg : real;
  signal mfe : real;
  signal qjuxh : severity_level;
begin
  hdwn : entity work.mfqkaveq
    port map (wtvcpg => qjuxh, a => mfe, l => yg, kgx => byktqiv);
  ymds : entity work.mfqkaveq
    port map (wtvcpg => etnjednomb, a => up, l => unp, kgx => gbqm);
  fyrryji : entity work.mfqkaveq
    port map (wtvcpg => atamcnelkw, a => erlb, l => xqpprxtg, kgx => igj);
  bybjcel : entity work.mfqkaveq
    port map (wtvcpg => aejl, a => rlzq, l => pdkbnpsiz, kgx => qr);
end dfpl;

library ieee;
use ieee.std_logic_1164.all;

entity onjjuf is
  port (odnugbamgp : in std_logic; z : out integer);
end onjjuf;



architecture krrvynhie of onjjuf is
  signal s : bit;
  signal xf : real;
  signal awvspwylxq : real;
  signal lgj : bit;
  signal rotwsxkyxk : real;
  signal bhsqjex : real;
  signal yjkzgj : severity_level;
  signal subec : severity_level;
  signal ze : bit;
  signal jmy : real;
  signal rdsserqy : real;
  signal hcn : severity_level;
begin
  iiyyxeco : entity work.mfqkaveq
    port map (wtvcpg => hcn, a => rdsserqy, l => jmy, kgx => ze);
  q : entity work.xk
    port map (lgh => z, jijgnctiez => subec);
  lbxwaziny : entity work.mfqkaveq
    port map (wtvcpg => yjkzgj, a => bhsqjex, l => rotwsxkyxk, kgx => lgj);
  kmzj : entity work.mfqkaveq
    port map (wtvcpg => subec, a => awvspwylxq, l => xf, kgx => s);
end krrvynhie;



-- Seed after: 13509561877685066592,9372850727389630639
