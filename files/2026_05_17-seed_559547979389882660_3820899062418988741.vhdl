-- Seed: 559547979389882660,3820899062418988741



entity yshcngibyi is
  port (x : out real; nogdn : inout real; i : in severity_level; obnopnrbu : in time);
end yshcngibyi;



architecture tshzauhh of yshcngibyi is
  
begin
  
end tshzauhh;

library ieee;
use ieee.std_logic_1164.all;

entity dt is
  port (sszpj : buffer time; ciwwdig : in std_logic; jyywqre : linkage std_logic);
end dt;



architecture d of dt is
  signal humay : severity_level;
  signal jxrow : real;
  signal jixxt : real;
  signal dgxdsfjgu : severity_level;
  signal igzhn : real;
  signal oporlv : real;
  signal ldolime : time;
  signal gpewhf : severity_level;
  signal qofzkadza : real;
  signal vqxtw : real;
begin
  c : entity work.yshcngibyi
    port map (x => vqxtw, nogdn => qofzkadza, i => gpewhf, obnopnrbu => ldolime);
  dntf : entity work.yshcngibyi
    port map (x => oporlv, nogdn => igzhn, i => dgxdsfjgu, obnopnrbu => sszpj);
  brjpbyomt : entity work.yshcngibyi
    port map (x => jixxt, nogdn => jxrow, i => humay, obnopnrbu => ldolime);
end d;



entity nrqzpgrsug is
  port (qcb : linkage character);
end nrqzpgrsug;



architecture gpmrdmqk of nrqzpgrsug is
  signal krmjdsxk : time;
  signal xrcqzrkx : real;
  signal hcmimun : real;
  signal iwnpig : time;
  signal pistohs : severity_level;
  signal ngonnwacxw : real;
  signal kbcyrvh : real;
begin
  upnbvvjus : entity work.yshcngibyi
    port map (x => kbcyrvh, nogdn => ngonnwacxw, i => pistohs, obnopnrbu => iwnpig);
  sr : entity work.yshcngibyi
    port map (x => hcmimun, nogdn => xrcqzrkx, i => pistohs, obnopnrbu => krmjdsxk);
end gpmrdmqk;



-- Seed after: 11819941988528115801,3820899062418988741
