-- Seed: 14107585039345679640,2511821214772927453

library ieee;
use ieee.std_logic_1164.all;

entity qaqdxug is
  port (h : buffer std_logic_vector(2 to 2); apm : buffer real);
end qaqdxug;

architecture b of qaqdxug is
  
begin
  -- Single-driven assignments
  apm <= apm;
end b;

library ieee;
use ieee.std_logic_1164.all;

entity cphjtiuxta is
  port (fnlt : in std_logic);
end cphjtiuxta;

library ieee;
use ieee.std_logic_1164.all;

architecture pvs of cphjtiuxta is
  signal goer : real;
  signal hjbcsvswj : real;
  signal ffniymjyxm : real;
  signal axwzg : real;
  signal ruxsiz : std_logic_vector(2 to 2);
begin
  wefu : entity work.qaqdxug
    port map (h => ruxsiz, apm => axwzg);
  dxjqzdjnf : entity work.qaqdxug
    port map (h => ruxsiz, apm => ffniymjyxm);
  zpfwuc : entity work.qaqdxug
    port map (h => ruxsiz, apm => hjbcsvswj);
  fbe : entity work.qaqdxug
    port map (h => ruxsiz, apm => goer);
end pvs;

library ieee;
use ieee.std_logic_1164.all;

entity xrablongy is
  port (suh : linkage std_logic);
end xrablongy;

library ieee;
use ieee.std_logic_1164.all;

architecture ojgbdvm of xrablongy is
  signal bharkf : real;
  signal atfj : std_logic_vector(2 to 2);
begin
  u : entity work.qaqdxug
    port map (h => atfj, apm => bharkf);
  
  -- Multi-driven assignments
  atfj <= atfj;
  atfj <= "W";
  atfj <= atfj;
  atfj <= (others => '-');
end ojgbdvm;



-- Seed after: 5369452970441532953,2511821214772927453
