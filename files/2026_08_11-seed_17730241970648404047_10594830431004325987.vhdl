-- Seed: 17730241970648404047,10594830431004325987

entity cpw is
  port (vnoxhaxcb : out time; e : buffer real_vector(0 to 4));
end cpw;

architecture rfld of cpw is
  
begin
  -- Single-driven assignments
  e <= e;
  vnoxhaxcb <= vnoxhaxcb;
end rfld;

entity lrwb is
  port (kscb : linkage integer_vector(3 downto 2));
end lrwb;

architecture sn of lrwb is
  signal khvwc : real_vector(0 to 4);
  signal pla : time;
  signal qfmikgkp : real_vector(0 to 4);
  signal vlzrem : time;
  signal zecbndt : real_vector(0 to 4);
  signal kagoy : time;
  signal evsxx : real_vector(0 to 4);
  signal lms : time;
begin
  gvhgml : entity work.cpw
    port map (vnoxhaxcb => lms, e => evsxx);
  fvdrqctznh : entity work.cpw
    port map (vnoxhaxcb => kagoy, e => zecbndt);
  cqjh : entity work.cpw
    port map (vnoxhaxcb => vlzrem, e => qfmikgkp);
  jsccja : entity work.cpw
    port map (vnoxhaxcb => pla, e => khvwc);
end sn;

entity unyfijfozw is
  port (hf : in boolean; evxtwue : in integer);
end unyfijfozw;

architecture wouul of unyfijfozw is
  
begin
  
end wouul;



-- Seed after: 17197685006984670058,10594830431004325987
