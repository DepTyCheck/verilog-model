-- Seed: 7692819122419389905,16265041255589496407

library ieee;
use ieee.std_logic_1164.all;

entity kbgx is
  port (agpvmeynt : buffer boolean; dgcod : linkage std_logic_vector(1 to 3); miwjpeie : linkage character);
end kbgx;



architecture mtrcgvd of kbgx is
  
begin
  
end mtrcgvd;



entity nko is
  port (sofkwkrp : buffer integer; tcjezly : linkage time; dcmij : in integer);
end nko;

library ieee;
use ieee.std_logic_1164.all;

architecture x of nko is
  signal zio : std_logic_vector(1 to 3);
  signal e : boolean;
  signal fnpto : character;
  signal pclfdipq : std_logic_vector(1 to 3);
  signal zza : boolean;
  signal vplxpyo : character;
  signal irizdoc : boolean;
  signal eedpix : character;
  signal bdpc : std_logic_vector(1 to 3);
  signal pbfwjo : boolean;
begin
  bh : entity work.kbgx
    port map (agpvmeynt => pbfwjo, dgcod => bdpc, miwjpeie => eedpix);
  bqsxx : entity work.kbgx
    port map (agpvmeynt => irizdoc, dgcod => bdpc, miwjpeie => vplxpyo);
  sujcij : entity work.kbgx
    port map (agpvmeynt => zza, dgcod => pclfdipq, miwjpeie => fnpto);
  amiqs : entity work.kbgx
    port map (agpvmeynt => e, dgcod => zio, miwjpeie => vplxpyo);
end x;



-- Seed after: 8241027466874421517,16265041255589496407
