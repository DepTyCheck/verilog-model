-- Seed: 3395943447731156735,2983771601630957889

use std.reflection.all;

entity wrogogjqub is
  port (variable dxz : inout subtype_mirror_pt; jimwprwgee : in bit_vector(2 to 0); fomslzd : buffer real; ad : in real);
end wrogogjqub;

architecture h of wrogogjqub is
  
begin
  
end h;

library ieee;
use ieee.std_logic_1164.all;

entity ucbj is
  port (cqwtpltva : linkage time; jqwvc : out real_vector(4 to 0); vfhncq : out std_logic_vector(3 downto 0));
end ucbj;

use std.reflection.all;

architecture iqq of ucbj is
  signal bphvw : real;
  shared variable mzlchcivpy : subtype_mirror_pt;
  shared variable hnrsfjsd : subtype_mirror_pt;
  signal qc : real;
  signal onysto : real;
  signal dziljqr : bit_vector(2 to 0);
  shared variable pmashi : subtype_mirror_pt;
  signal hgtyszibnh : real;
  signal w : real;
  signal wbzhthv : bit_vector(2 to 0);
  shared variable xxiof : subtype_mirror_pt;
begin
  mwfngoblg : entity work.wrogogjqub
    port map (dxz => xxiof, jimwprwgee => wbzhthv, fomslzd => w, ad => hgtyszibnh);
  cyqbv : entity work.wrogogjqub
    port map (dxz => pmashi, jimwprwgee => dziljqr, fomslzd => onysto, ad => qc);
  rlhy : entity work.wrogogjqub
    port map (dxz => hnrsfjsd, jimwprwgee => dziljqr, fomslzd => qc, ad => w);
  kwneftlhuw : entity work.wrogogjqub
    port map (dxz => mzlchcivpy, jimwprwgee => wbzhthv, fomslzd => hgtyszibnh, ad => bphvw);
  
  -- Single-driven assignments
  jqwvc <= (others => 0.0);
  dziljqr <= (others => '0');
  wbzhthv <= wbzhthv;
  
  -- Multi-driven assignments
  vfhncq <= vfhncq;
  vfhncq <= vfhncq;
end iqq;

use std.reflection.all;

entity dfgcct is
  port (variable dhmqmqnx : inout enumeration_subtype_mirror_pt);
end dfgcct;

use std.reflection.all;

architecture jibj of dfgcct is
  signal a : real;
  signal aa : bit_vector(2 to 0);
  shared variable tptucr : subtype_mirror_pt;
  signal aedsic : real;
  signal wagcta : bit_vector(2 to 0);
  shared variable bdemsszsxh : subtype_mirror_pt;
begin
  pzzc : entity work.wrogogjqub
    port map (dxz => bdemsszsxh, jimwprwgee => wagcta, fomslzd => aedsic, ad => aedsic);
  wydrf : entity work.wrogogjqub
    port map (dxz => tptucr, jimwprwgee => aa, fomslzd => a, ad => aedsic);
end jibj;



-- Seed after: 16277472967393843920,2983771601630957889
