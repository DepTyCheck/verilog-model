-- Seed: 9881839223103014723,11181851762153539145

library ieee;
use ieee.std_logic_1164.all;

entity vatke is
  port (u : out std_logic_vector(0 downto 3); n : buffer time; erblzt : inout std_logic);
end vatke;



architecture wwsor of vatke is
  
begin
  
end wwsor;

library ieee;
use ieee.std_logic_1164.all;

entity ir is
  port (vw : linkage std_logic; knggavtod : buffer severity_level; nvbh : inout std_logic_vector(1 downto 0));
end ir;

library ieee;
use ieee.std_logic_1164.all;

architecture uusr of ir is
  signal hp : std_logic;
  signal mjl : time;
  signal ya : std_logic_vector(0 downto 3);
  signal xdbktsi : std_logic;
  signal pshopogt : time;
  signal wj : std_logic_vector(0 downto 3);
begin
  o : entity work.vatke
    port map (u => wj, n => pshopogt, erblzt => xdbktsi);
  vm : entity work.vatke
    port map (u => ya, n => mjl, erblzt => hp);
end uusr;

library ieee;
use ieee.std_logic_1164.all;

entity pjbhyo is
  port (xofew : inout std_logic; mlmoqzax : out integer_vector(0 downto 0));
end pjbhyo;

library ieee;
use ieee.std_logic_1164.all;

architecture corvfsoj of pjbhyo is
  signal ezyysjotf : std_logic;
  signal j : time;
  signal vl : std_logic_vector(1 downto 0);
  signal fg : severity_level;
  signal bspztrq : std_logic;
  signal jr : time;
  signal gxgweudk : std_logic_vector(0 downto 3);
begin
  n : entity work.vatke
    port map (u => gxgweudk, n => jr, erblzt => bspztrq);
  whayocu : entity work.ir
    port map (vw => xofew, knggavtod => fg, nvbh => vl);
  tvliydvj : entity work.vatke
    port map (u => gxgweudk, n => j, erblzt => ezyysjotf);
end corvfsoj;



-- Seed after: 8913709529669056647,11181851762153539145
