-- Seed: 8414763847956381050,17047277710231705797

library ieee;
use ieee.std_logic_1164.all;

entity grqz is
  port (wldjy : out time; afbriqapb : in boolean_vector(4 to 1); hbksrjh : linkage time; kr : inout std_logic_vector(2 to 1));
end grqz;

architecture gexnwvovsz of grqz is
  
begin
  -- Single-driven assignments
  wldjy <= 16#E7# ns;
  
  -- Multi-driven assignments
  kr <= "";
  kr <= "";
  kr <= (others => '0');
  kr <= (others => '0');
end gexnwvovsz;

entity msrr is
  port (k : in real_vector(1 downto 4));
end msrr;

library ieee;
use ieee.std_logic_1164.all;

architecture ekcl of msrr is
  signal u : time;
  signal swkihpvk : boolean_vector(4 to 1);
  signal lkgfkg : time;
  signal dtn : std_logic_vector(2 to 1);
  signal fnbxytgzhr : time;
  signal xtrzb : boolean_vector(4 to 1);
  signal jg : time;
  signal uyzxvrui : std_logic_vector(2 to 1);
  signal e : time;
  signal pvnxtdaz : boolean_vector(4 to 1);
  signal x : time;
begin
  pxomrdmaj : entity work.grqz
    port map (wldjy => x, afbriqapb => pvnxtdaz, hbksrjh => e, kr => uyzxvrui);
  njiqo : entity work.grqz
    port map (wldjy => jg, afbriqapb => xtrzb, hbksrjh => fnbxytgzhr, kr => dtn);
  zedal : entity work.grqz
    port map (wldjy => lkgfkg, afbriqapb => swkihpvk, hbksrjh => u, kr => uyzxvrui);
  
  -- Single-driven assignments
  pvnxtdaz <= (others => TRUE);
  swkihpvk <= (others => TRUE);
  xtrzb <= (others => TRUE);
  
  -- Multi-driven assignments
  uyzxvrui <= "";
end ekcl;

library ieee;
use ieee.std_logic_1164.all;

entity bedgmehv is
  port (wwuqcyd : buffer std_logic_vector(3 to 3));
end bedgmehv;

library ieee;
use ieee.std_logic_1164.all;

architecture bodrrufl of bedgmehv is
  signal zhl : std_logic_vector(2 to 1);
  signal vftlld : time;
  signal iq : boolean_vector(4 to 1);
  signal vhzcb : time;
begin
  zxt : entity work.grqz
    port map (wldjy => vhzcb, afbriqapb => iq, hbksrjh => vftlld, kr => zhl);
  
  -- Single-driven assignments
  iq <= (others => TRUE);
  
  -- Multi-driven assignments
  wwuqcyd <= "-";
  wwuqcyd <= "W";
end bodrrufl;



-- Seed after: 7789815186225482005,17047277710231705797
