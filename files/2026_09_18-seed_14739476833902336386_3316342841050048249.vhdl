-- Seed: 14739476833902336386,3316342841050048249

entity okxecfkoka is
  port (pnji : inout integer_vector(1 downto 1); qb : linkage time; xoa : in time);
end okxecfkoka;

architecture oftwufz of okxecfkoka is
  
begin
  -- Single-driven assignments
  pnji <= (others => 2#1010#);
end oftwufz;

library ieee;
use ieee.std_logic_1164.all;

entity pwjqn is
  port (qlzrroy : in std_logic_vector(2 to 2); keadlr : linkage integer);
end pwjqn;

architecture ohfeyigm of pwjqn is
  signal zd : integer_vector(1 downto 1);
  signal umyr : time;
  signal xod : time;
  signal ot : integer_vector(1 downto 1);
  signal mbbbkjhcs : time;
  signal cxt : integer_vector(1 downto 1);
  signal sako : time;
  signal mbo : time;
  signal rtvmwkzz : integer_vector(1 downto 1);
begin
  vjkjhpb : entity work.okxecfkoka
    port map (pnji => rtvmwkzz, qb => mbo, xoa => sako);
  hu : entity work.okxecfkoka
    port map (pnji => cxt, qb => sako, xoa => mbbbkjhcs);
  ohhu : entity work.okxecfkoka
    port map (pnji => ot, qb => xod, xoa => umyr);
  tnziybbdmo : entity work.okxecfkoka
    port map (pnji => zd, qb => umyr, xoa => mbo);
  
  -- Single-driven assignments
  mbbbkjhcs <= 0_3_4_3_0.3_3 ns;
end ohfeyigm;



-- Seed after: 8069711679717360780,3316342841050048249
