-- Seed: 792191479276612679,10557070023141912087

library ieee;
use ieee.std_logic_1164.all;

entity lox is
  port ( lqask : buffer integer
  ; cr : linkage std_logic_vector(3 downto 0)
  ; pylznqxyor : inout severity_level
  ; pjmkwdoq : linkage std_logic_vector(3 downto 1)
  );
end lox;

architecture dz of lox is
  
begin
  
end dz;

library ieee;
use ieee.std_logic_1164.all;

entity ixifq is
  port (jxrmfee : buffer string(5 to 1); zswifnpvjw : buffer integer; qb : out std_logic_vector(2 to 3));
end ixifq;

library ieee;
use ieee.std_logic_1164.all;

architecture cvafjlzw of ixifq is
  signal sarcynym : std_logic_vector(3 downto 1);
  signal yfjwtk : severity_level;
  signal zs : std_logic_vector(3 downto 0);
begin
  jvvewxsnw : entity work.lox
    port map (lqask => zswifnpvjw, cr => zs, pylznqxyor => yfjwtk, pjmkwdoq => sarcynym);
  
  -- Single-driven assignments
  jxrmfee <= "";
end cvafjlzw;

entity uffrk is
  port (qdm : inout time);
end uffrk;

library ieee;
use ieee.std_logic_1164.all;

architecture tauye of uffrk is
  signal rbizs : severity_level;
  signal ulpjaf : std_logic_vector(3 downto 0);
  signal t : integer;
  signal wsizg : std_logic_vector(3 downto 1);
  signal oskhyamjyw : severity_level;
  signal waeg : std_logic_vector(3 downto 0);
  signal gmsdlxd : integer;
  signal ewk : severity_level;
  signal vxj : std_logic_vector(3 downto 0);
  signal kbhrfs : integer;
  signal id : std_logic_vector(3 downto 1);
  signal s : severity_level;
  signal prixl : std_logic_vector(3 downto 0);
  signal dmuvqpesgz : integer;
begin
  luzicuvld : entity work.lox
    port map (lqask => dmuvqpesgz, cr => prixl, pylznqxyor => s, pjmkwdoq => id);
  ejgyd : entity work.lox
    port map (lqask => kbhrfs, cr => vxj, pylznqxyor => ewk, pjmkwdoq => id);
  fwywrudq : entity work.lox
    port map (lqask => gmsdlxd, cr => waeg, pylznqxyor => oskhyamjyw, pjmkwdoq => wsizg);
  zjzscgn : entity work.lox
    port map (lqask => t, cr => ulpjaf, pylznqxyor => rbizs, pjmkwdoq => id);
  
  -- Single-driven assignments
  qdm <= 8#2_1_2# ms;
  
  -- Multi-driven assignments
  id <= "ZUU";
  prixl <= ('0', '-', 'U', '0');
  vxj <= ('U', 'L', 'W', '0');
  prixl <= ('X', 'W', '1', '1');
end tauye;



-- Seed after: 18105867670756859692,10557070023141912087
