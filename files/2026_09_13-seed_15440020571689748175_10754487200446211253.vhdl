-- Seed: 15440020571689748175,10754487200446211253

entity yakuttt is
  port (tnrsvy : linkage character; sregzo : inout bit; xmrz : linkage character);
end yakuttt;

architecture qrjyurip of yakuttt is
  
begin
  -- Single-driven assignments
  sregzo <= sregzo;
end qrjyurip;

library ieee;
use ieee.std_logic_1164.all;

entity sibfpgtlg is
  port (ky : inout boolean; prfhzegaah : out integer_vector(4 downto 0); gbauqmtw : inout real; suyozrvy : out std_logic);
end sibfpgtlg;

architecture corinsowfn of sibfpgtlg is
  
begin
  -- Single-driven assignments
  prfhzegaah <= prfhzegaah;
  ky <= ky;
  gbauqmtw <= 242.0_1_3_1;
  
  -- Multi-driven assignments
  suyozrvy <= 'U';
  suyozrvy <= 'L';
  suyozrvy <= '0';
end corinsowfn;

library ieee;
use ieee.std_logic_1164.all;

entity gzqiqkok is
  port (rmvlzycxq : out std_logic; otjabqiz : in time; u : out std_logic_vector(1 to 3); kqcsaql : out std_logic);
end gzqiqkok;

architecture qrvpt of gzqiqkok is
  signal ayhalgeq : character;
  signal x : bit;
  signal afyqdttea : character;
  signal mvlitu : real;
  signal ptg : integer_vector(4 downto 0);
  signal qrgn : boolean;
  signal ooojxwgq : real;
  signal uacvbneypz : integer_vector(4 downto 0);
  signal lta : boolean;
  signal qr : real;
  signal phr : integer_vector(4 downto 0);
  signal olqvnvc : boolean;
begin
  hmbgptnv : entity work.sibfpgtlg
    port map (ky => olqvnvc, prfhzegaah => phr, gbauqmtw => qr, suyozrvy => kqcsaql);
  tpfwwrhho : entity work.sibfpgtlg
    port map (ky => lta, prfhzegaah => uacvbneypz, gbauqmtw => ooojxwgq, suyozrvy => kqcsaql);
  tokogl : entity work.sibfpgtlg
    port map (ky => qrgn, prfhzegaah => ptg, gbauqmtw => mvlitu, suyozrvy => kqcsaql);
  cifha : entity work.yakuttt
    port map (tnrsvy => afyqdttea, sregzo => x, xmrz => ayhalgeq);
end qrvpt;



-- Seed after: 8289050455674339020,10754487200446211253
