-- Seed: 6690811828465763820,17234720251424330329

library ieee;
use ieee.std_logic_1164.all;

entity m is
  port (hbrshvexo : out real; jg : out severity_level; jlo : out boolean; rrhqapwy : in std_logic_vector(2 to 2));
end m;

architecture dqxoxb of m is
  
begin
  -- Single-driven assignments
  jlo <= jlo;
  hbrshvexo <= 3.22;
  jg <= jg;
end dqxoxb;

entity sojbdvec is
  port (ibggojlm : linkage time; hulc : buffer real_vector(4 to 1); vuovexv : out real);
end sojbdvec;

library ieee;
use ieee.std_logic_1164.all;

architecture yy of sojbdvec is
  signal yfhdxdcmm : boolean;
  signal eyaujv : severity_level;
  signal waobaer : real;
  signal y : std_logic_vector(2 to 2);
  signal frxjazfz : boolean;
  signal lqtno : severity_level;
  signal wwxluatgnw : real;
begin
  dnmwdkr : entity work.m
    port map (hbrshvexo => wwxluatgnw, jg => lqtno, jlo => frxjazfz, rrhqapwy => y);
  zbnnqd : entity work.m
    port map (hbrshvexo => waobaer, jg => eyaujv, jlo => yfhdxdcmm, rrhqapwy => y);
  
  -- Single-driven assignments
  vuovexv <= vuovexv;
end yy;

library ieee;
use ieee.std_logic_1164.all;

entity dnakk is
  port (xw : buffer std_logic_vector(4 downto 4); xuwqh : linkage integer; ptnkz : inout std_logic; oykemw : in time);
end dnakk;

architecture sdo of dnakk is
  signal tzw : boolean;
  signal bges : severity_level;
  signal z : real;
begin
  eqmwfybbl : entity work.m
    port map (hbrshvexo => z, jg => bges, jlo => tzw, rrhqapwy => xw);
  
  -- Multi-driven assignments
  ptnkz <= 'U';
  ptnkz <= ptnkz;
end sdo;

entity libn is
  port (cecsg : linkage time; v : linkage integer; cr : linkage time_vector(1 to 3));
end libn;

library ieee;
use ieee.std_logic_1164.all;

architecture kiqiahwrry of libn is
  signal kj : std_logic_vector(2 to 2);
  signal xwtwvak : boolean;
  signal lxakbxgka : severity_level;
  signal hpmztxy : real;
  signal pwsvenayl : std_logic_vector(2 to 2);
  signal jxhzoprre : boolean;
  signal pzq : severity_level;
  signal cphdqetqu : real;
begin
  wlpszkt : entity work.m
    port map (hbrshvexo => cphdqetqu, jg => pzq, jlo => jxhzoprre, rrhqapwy => pwsvenayl);
  nac : entity work.m
    port map (hbrshvexo => hpmztxy, jg => lxakbxgka, jlo => xwtwvak, rrhqapwy => kj);
  
  -- Multi-driven assignments
  kj <= pwsvenayl;
  pwsvenayl <= (others => 'L');
  pwsvenayl <= (others => '0');
end kiqiahwrry;



-- Seed after: 7988271360120756920,17234720251424330329
