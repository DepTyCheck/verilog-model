-- Seed: 247898136164478261,13592003931158285879

entity ckpnvdjku is
  port (xiu : out time_vector(3 downto 0); iprr : out bit_vector(1 to 1); ipcgn : buffer real_vector(3 downto 2); vtfft : out integer);
end ckpnvdjku;

architecture aidrv of ckpnvdjku is
  
begin
  -- Single-driven assignments
  iprr <= iprr;
  vtfft <= 3242;
end aidrv;

library ieee;
use ieee.std_logic_1164.all;

entity azu is
  port (umptukfvvb : out std_logic; mwvqi : inout std_logic_vector(4 downto 3); dsg : linkage time_vector(4 downto 4); qqqa : buffer bit);
end azu;

architecture sqssecyot of azu is
  signal lz : integer;
  signal y : real_vector(3 downto 2);
  signal kjxbwez : bit_vector(1 to 1);
  signal eozeefkql : time_vector(3 downto 0);
  signal mnt : integer;
  signal tmjztbofm : real_vector(3 downto 2);
  signal lt : bit_vector(1 to 1);
  signal yc : time_vector(3 downto 0);
  signal yyktfrbi : integer;
  signal as : real_vector(3 downto 2);
  signal ayhjr : bit_vector(1 to 1);
  signal ubtug : time_vector(3 downto 0);
  signal edmpjb : integer;
  signal gjqxocou : real_vector(3 downto 2);
  signal d : bit_vector(1 to 1);
  signal lavn : time_vector(3 downto 0);
begin
  oj : entity work.ckpnvdjku
    port map (xiu => lavn, iprr => d, ipcgn => gjqxocou, vtfft => edmpjb);
  egdlti : entity work.ckpnvdjku
    port map (xiu => ubtug, iprr => ayhjr, ipcgn => as, vtfft => yyktfrbi);
  lbnfhpd : entity work.ckpnvdjku
    port map (xiu => yc, iprr => lt, ipcgn => tmjztbofm, vtfft => mnt);
  cdqhbdeat : entity work.ckpnvdjku
    port map (xiu => eozeefkql, iprr => kjxbwez, ipcgn => y, vtfft => lz);
  
  -- Single-driven assignments
  qqqa <= qqqa;
  
  -- Multi-driven assignments
  mwvqi <= ('H', '-');
  umptukfvvb <= 'X';
  umptukfvvb <= umptukfvvb;
  mwvqi <= mwvqi;
end sqssecyot;

library ieee;
use ieee.std_logic_1164.all;

entity esn is
  port (x : inout real; igvibsxfpp : inout std_logic_vector(1 to 4); omt : buffer std_logic; al : buffer severity_level);
end esn;

architecture mywnddjsrw of esn is
  
begin
  -- Single-driven assignments
  x <= x;
  
  -- Multi-driven assignments
  igvibsxfpp <= "UZ-U";
  omt <= omt;
  omt <= 'X';
end mywnddjsrw;



-- Seed after: 1709486418150100193,13592003931158285879
