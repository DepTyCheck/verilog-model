-- Seed: 8900331790474766326,3566912872917928779

use std.reflection.all;

entity suhmulmot is
  port (txals : linkage real; uzzpuu : inout real_vector(2 downto 2); wknqrkfxiq : inout array_subtype_mirror);
end suhmulmot;

architecture ruyfcpw of suhmulmot is
  
begin
  -- Single-driven assignments
  uzzpuu <= (others => 0_3_3_0.22200);
end ruyfcpw;

entity czggqh is
  port (wbghwhk : inout time);
end czggqh;

architecture fcwl of czggqh is
  
begin
  -- Single-driven assignments
  wbghwhk <= wbghwhk;
end fcwl;

use std.reflection.all;

entity sb is
  port (w : inout integer_value_mirror);
end sb;

use std.reflection.all;

architecture jzefdlitmp of sb is
  shared variable awo : array_subtype_mirror;
  signal ossiurrxr : real_vector(2 downto 2);
  signal uuljikbyo : real;
  shared variable qlsyei : array_subtype_mirror;
  signal tgp : real_vector(2 downto 2);
  signal i : real;
begin
  elnm : entity work.suhmulmot
    port map (txals => i, uzzpuu => tgp, wknqrkfxiq => qlsyei);
  qe : entity work.suhmulmot
    port map (txals => uuljikbyo, uzzpuu => ossiurrxr, wknqrkfxiq => awo);
end jzefdlitmp;



-- Seed after: 17243467066395680719,3566912872917928779
