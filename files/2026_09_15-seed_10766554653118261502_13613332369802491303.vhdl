-- Seed: 10766554653118261502,13613332369802491303

library ieee;
use ieee.std_logic_1164.all;

entity bfsay is
  port (r : buffer std_logic; wjlqg : buffer real; dlupgku : linkage time);
end bfsay;

architecture yy of bfsay is
  
begin
  -- Single-driven assignments
  wjlqg <= 8#1.2311#;
  
  -- Multi-driven assignments
  r <= r;
end yy;

entity qat is
  port (beaxqoic : inout integer; ig : out integer; myrjgoox : in time);
end qat;

library ieee;
use ieee.std_logic_1164.all;

architecture a of qat is
  signal mzozxn : time;
  signal lg : real;
  signal ow : time;
  signal qbvtb : real;
  signal vwncjrbr : std_logic;
  signal v : time;
  signal pc : real;
  signal hewajgz : std_logic;
  signal edxyw : time;
  signal ocrbc : real;
  signal bbufjagr : std_logic;
begin
  jetwa : entity work.bfsay
    port map (r => bbufjagr, wjlqg => ocrbc, dlupgku => edxyw);
  pfaxiendnt : entity work.bfsay
    port map (r => hewajgz, wjlqg => pc, dlupgku => v);
  gvgrgexol : entity work.bfsay
    port map (r => vwncjrbr, wjlqg => qbvtb, dlupgku => ow);
  dejrze : entity work.bfsay
    port map (r => bbufjagr, wjlqg => lg, dlupgku => mzozxn);
  
  -- Single-driven assignments
  ig <= 2#01100#;
  beaxqoic <= 13342;
  
  -- Multi-driven assignments
  bbufjagr <= '-';
  hewajgz <= bbufjagr;
end a;

library ieee;
use ieee.std_logic_1164.all;

entity hn is
  port (kpibmp : linkage std_logic; iqae : buffer std_logic_vector(0 to 0));
end hn;

library ieee;
use ieee.std_logic_1164.all;

architecture mutqcp of hn is
  signal vcvjonk : time;
  signal bgpbjr : real;
  signal kxlyx : std_logic;
begin
  n : entity work.bfsay
    port map (r => kxlyx, wjlqg => bgpbjr, dlupgku => vcvjonk);
  
  -- Multi-driven assignments
  kxlyx <= kxlyx;
  iqae <= iqae;
  iqae <= "X";
end mutqcp;



-- Seed after: 3558060289689604673,13613332369802491303
