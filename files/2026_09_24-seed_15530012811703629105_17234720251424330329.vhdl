-- Seed: 15530012811703629105,17234720251424330329

library ieee;
use ieee.std_logic_1164.all;

entity mdu is
  port (ctnmnfflt : inout integer; xic : inout std_logic; dziqxea : out severity_level; brbz : inout bit_vector(3 to 0));
end mdu;

architecture hmm of mdu is
  
begin
  -- Single-driven assignments
  brbz <= brbz;
  dziqxea <= WARNING;
  
  -- Multi-driven assignments
  xic <= '0';
  xic <= xic;
  xic <= 'L';
  xic <= 'L';
end hmm;

entity uzrbue is
  port (jnbpkqxedi : buffer character; jwbkx : out real; prgfqd : linkage real; e : inout real);
end uzrbue;

library ieee;
use ieee.std_logic_1164.all;

architecture nkjczwdtsz of uzrbue is
  signal ujepwcn : bit_vector(3 to 0);
  signal wvyb : severity_level;
  signal rizkz : integer;
  signal ngw : bit_vector(3 to 0);
  signal gftlskkd : severity_level;
  signal vo : integer;
  signal i : bit_vector(3 to 0);
  signal m : severity_level;
  signal bxnp : std_logic;
  signal grott : integer;
begin
  mfsjt : entity work.mdu
    port map (ctnmnfflt => grott, xic => bxnp, dziqxea => m, brbz => i);
  ludeep : entity work.mdu
    port map (ctnmnfflt => vo, xic => bxnp, dziqxea => gftlskkd, brbz => ngw);
  nbaqhqcrs : entity work.mdu
    port map (ctnmnfflt => rizkz, xic => bxnp, dziqxea => wvyb, brbz => ujepwcn);
  
  -- Single-driven assignments
  jwbkx <= e;
  jnbpkqxedi <= 'm';
  e <= e;
end nkjczwdtsz;



-- Seed after: 15732491025793482942,17234720251424330329
