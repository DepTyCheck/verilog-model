-- Seed: 3829903147456762553,14094562573555574003

library ieee;
use ieee.std_logic_1164.all;

entity bwjfzdug is
  port (rk : out time; cffssn : inout std_logic_vector(2 downto 3); eklcrksge : in real; wfkltsvdk : inout time);
end bwjfzdug;

architecture sd of bwjfzdug is
  
begin
  -- Single-driven assignments
  rk <= 4 sec;
  wfkltsvdk <= wfkltsvdk;
end sd;

entity qgjx is
  port (yib : in real; acejpdw : out real; auhmv : linkage bit_vector(4 downto 0); tremtmda : out integer);
end qgjx;

library ieee;
use ieee.std_logic_1164.all;

architecture z of qgjx is
  signal loeptqpn : time;
  signal ms : real;
  signal bpupwxllif : std_logic_vector(2 downto 3);
  signal mtwjfirhn : time;
  signal rbisftfh : time;
  signal pjavwg : real;
  signal a : time;
  signal kapxjlvrlc : time;
  signal gecbttko : std_logic_vector(2 downto 3);
  signal xg : time;
  signal tezxoxtd : time;
  signal ljexshtxmd : std_logic_vector(2 downto 3);
  signal ioc : time;
begin
  xnadz : entity work.bwjfzdug
    port map (rk => ioc, cffssn => ljexshtxmd, eklcrksge => yib, wfkltsvdk => tezxoxtd);
  sguoenlb : entity work.bwjfzdug
    port map (rk => xg, cffssn => gecbttko, eklcrksge => acejpdw, wfkltsvdk => kapxjlvrlc);
  zupeh : entity work.bwjfzdug
    port map (rk => a, cffssn => ljexshtxmd, eklcrksge => pjavwg, wfkltsvdk => rbisftfh);
  f : entity work.bwjfzdug
    port map (rk => mtwjfirhn, cffssn => bpupwxllif, eklcrksge => ms, wfkltsvdk => loeptqpn);
  
  -- Single-driven assignments
  tremtmda <= tremtmda;
  
  -- Multi-driven assignments
  bpupwxllif <= ljexshtxmd;
  ljexshtxmd <= ljexshtxmd;
end z;

entity gf is
  port (sgjbxj : buffer real; avajt : out real);
end gf;

library ieee;
use ieee.std_logic_1164.all;

architecture ssr of gf is
  signal cjxlcetg : integer;
  signal ncoqzj : bit_vector(4 downto 0);
  signal xhadcqj : real;
  signal w : real;
  signal y : time;
  signal k : std_logic_vector(2 downto 3);
  signal yx : time;
  signal ehw : time;
  signal lkpz : std_logic_vector(2 downto 3);
  signal suhma : time;
  signal dm : time;
  signal socanzcs : real;
  signal cztrnkaudw : std_logic_vector(2 downto 3);
  signal rwq : time;
begin
  ebpx : entity work.bwjfzdug
    port map (rk => rwq, cffssn => cztrnkaudw, eklcrksge => socanzcs, wfkltsvdk => dm);
  xoek : entity work.bwjfzdug
    port map (rk => suhma, cffssn => lkpz, eklcrksge => sgjbxj, wfkltsvdk => ehw);
  jfhubpe : entity work.bwjfzdug
    port map (rk => yx, cffssn => k, eklcrksge => socanzcs, wfkltsvdk => y);
  wqa : entity work.qgjx
    port map (yib => w, acejpdw => xhadcqj, auhmv => ncoqzj, tremtmda => cjxlcetg);
  
  -- Single-driven assignments
  avajt <= 2#0.10#;
  sgjbxj <= avajt;
  socanzcs <= 16#F.C_3_4#;
  
  -- Multi-driven assignments
  cztrnkaudw <= "";
end ssr;



-- Seed after: 12153345196254819533,14094562573555574003
