-- Seed: 18298035301901580863,13857275728440271305

entity u is
  port (lus : linkage time; o : linkage real);
end u;

architecture zcrvzkox of u is
  
begin
  
end zcrvzkox;

library ieee;
use ieee.std_logic_1164.all;

entity betnlo is
  port (zy : in bit_vector(2 to 1); eyfuju : out std_logic_vector(3 downto 3));
end betnlo;

architecture rppvk of betnlo is
  signal hbskqtfx : real;
  signal otddke : time;
  signal ewbd : real;
  signal fdpmmixs : time;
  signal s : real;
  signal yrqelv : time;
  signal dzfwmigix : real;
  signal ceclkzkvpz : time;
begin
  sqyvqrkaj : entity work.u
    port map (lus => ceclkzkvpz, o => dzfwmigix);
  swzxarjfbh : entity work.u
    port map (lus => yrqelv, o => s);
  dngiudj : entity work.u
    port map (lus => fdpmmixs, o => ewbd);
  hofjq : entity work.u
    port map (lus => otddke, o => hbskqtfx);
  
  -- Multi-driven assignments
  eyfuju <= eyfuju;
end rppvk;

entity q is
  port (suba : inout character; wgoyucj : linkage integer);
end q;

architecture yjf of q is
  signal hbv : real;
  signal zbghpfnoyf : time;
  signal irvhp : real;
  signal rwywhpdulf : time;
begin
  mas : entity work.u
    port map (lus => rwywhpdulf, o => irvhp);
  dahozncfd : entity work.u
    port map (lus => zbghpfnoyf, o => hbv);
  
  -- Single-driven assignments
  suba <= suba;
end yjf;



-- Seed after: 2284515479839217528,13857275728440271305
