-- Seed: 12907913573113030402,3108530264173481209

library ieee;
use ieee.std_logic_1164.all;

entity iuvys is
  port (gsxwoy : out bit_vector(0 downto 1); jjhxz : inout real; opidoy : linkage std_logic);
end iuvys;

architecture zmwwi of iuvys is
  
begin
  -- Single-driven assignments
  gsxwoy <= (others => '0');
  jjhxz <= 122.43210;
end zmwwi;

library ieee;
use ieee.std_logic_1164.all;

entity c is
  port (gsssn : buffer real; adihv : out std_logic; ciesnb : buffer character);
end c;

library ieee;
use ieee.std_logic_1164.all;

architecture n of c is
  signal cfuhcgx : std_logic;
  signal okmyfqjy : real;
  signal pzsfxmdyl : bit_vector(0 downto 1);
begin
  gpzkksf : entity work.iuvys
    port map (gsxwoy => pzsfxmdyl, jjhxz => okmyfqjy, opidoy => cfuhcgx);
  
  -- Single-driven assignments
  ciesnb <= 'n';
  gsssn <= 2#1.0_0#;
  
  -- Multi-driven assignments
  cfuhcgx <= '-';
  adihv <= '0';
  adihv <= 'X';
  cfuhcgx <= '1';
end n;

library ieee;
use ieee.std_logic_1164.all;

entity prcykoc is
  port (rbrrgtpqlh : in time; bbwlyfpzpb : linkage time; hxuhr : buffer string(2 downto 4); xixrwapb : buffer std_logic);
end prcykoc;

architecture rw of prcykoc is
  
begin
  -- Multi-driven assignments
  xixrwapb <= 'Z';
  xixrwapb <= '0';
  xixrwapb <= 'W';
end rw;

entity zyf is
  port (ehjbdbv : out real);
end zyf;

library ieee;
use ieee.std_logic_1164.all;

architecture xkcogdb of zyf is
  signal p : std_logic;
  signal sbytmjezn : real;
  signal ntw : bit_vector(0 downto 1);
  signal s : real;
  signal wrajdt : bit_vector(0 downto 1);
  signal prgymyotw : std_logic;
  signal yzkyos : real;
  signal uejdpvclwi : bit_vector(0 downto 1);
  signal oju : std_logic;
  signal raivv : real;
  signal iiaruihg : bit_vector(0 downto 1);
begin
  wfhi : entity work.iuvys
    port map (gsxwoy => iiaruihg, jjhxz => raivv, opidoy => oju);
  vfbz : entity work.iuvys
    port map (gsxwoy => uejdpvclwi, jjhxz => yzkyos, opidoy => prgymyotw);
  jsbtp : entity work.iuvys
    port map (gsxwoy => wrajdt, jjhxz => s, opidoy => oju);
  qmw : entity work.iuvys
    port map (gsxwoy => ntw, jjhxz => sbytmjezn, opidoy => p);
  
  -- Multi-driven assignments
  oju <= 'W';
end xkcogdb;



-- Seed after: 9581672711668950807,3108530264173481209
