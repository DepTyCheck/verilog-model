-- Seed: 3961543953737274925,2983771601630957889

use std.reflection.all;

entity k is
  port (pgl : inout integer; variable rlobmc : inout protected_subtype_mirror_pt; cjuntqxn : linkage time);
end k;

architecture becrx of k is
  
begin
  -- Single-driven assignments
  pgl <= 8#7_1_4_6_3#;
end becrx;

use std.reflection.all;

entity eb is
  port (variable s : inout floating_value_mirror_pt; la : out string(5 downto 3); zswluyma : buffer time);
end eb;

use std.reflection.all;

architecture gr of eb is
  signal ny : time;
  shared variable qdl : protected_subtype_mirror_pt;
  signal g : integer;
begin
  ot : entity work.k
    port map (pgl => g, rlobmc => qdl, cjuntqxn => ny);
  
  -- Single-driven assignments
  zswluyma <= zswluyma;
  la <= ('a', 'i', 's');
end gr;



-- Seed after: 17609935602071075804,2983771601630957889
