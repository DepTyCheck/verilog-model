-- Seed: 11051346029895336654,18037650846010261179

entity iv is
  port (fi : inout time; b : inout real);
end iv;

architecture dtwvzyy of iv is
  
begin
  -- Single-driven assignments
  b <= 8#65.2_2_4#;
  fi <= fi;
end dtwvzyy;

entity pxxejzfvx is
  port (xikbp : linkage real_vector(2 to 3); xvmb : in integer_vector(3 downto 1));
end pxxejzfvx;

architecture etf of pxxejzfvx is
  signal p : real;
  signal imlkjmbpyz : time;
  signal gjwaqszmhk : real;
  signal v : time;
begin
  kzja : entity work.iv
    port map (fi => v, b => gjwaqszmhk);
  zgngrbie : entity work.iv
    port map (fi => imlkjmbpyz, b => p);
end etf;

library ieee;
use ieee.std_logic_1164.all;

entity a is
  port (knqwpm : in std_logic);
end a;

architecture yfrr of a is
  signal wshzcuu : real;
  signal dhx : time;
  signal djhqcbki : integer_vector(3 downto 1);
  signal eraphhyj : real_vector(2 to 3);
  signal x : real;
  signal mhswomf : time;
begin
  ilwyp : entity work.iv
    port map (fi => mhswomf, b => x);
  veu : entity work.pxxejzfvx
    port map (xikbp => eraphhyj, xvmb => djhqcbki);
  aoi : entity work.iv
    port map (fi => dhx, b => wshzcuu);
  
  -- Single-driven assignments
  djhqcbki <= (8#4#, 3311, 2);
end yfrr;

entity zsu is
  port (imtc : out severity_level; iicpeh : buffer bit; tjapfjo : linkage severity_level);
end zsu;

architecture dxjeuvo of zsu is
  signal qkor : real;
  signal atgikmjd : time;
  signal dqvqwjd : integer_vector(3 downto 1);
  signal wxna : real_vector(2 to 3);
  signal kzjzfrx : real;
  signal vlyvqcegqj : time;
  signal qwo : real;
  signal cronvph : time;
begin
  kq : entity work.iv
    port map (fi => cronvph, b => qwo);
  owl : entity work.iv
    port map (fi => vlyvqcegqj, b => kzjzfrx);
  vgy : entity work.pxxejzfvx
    port map (xikbp => wxna, xvmb => dqvqwjd);
  klasfpymsi : entity work.iv
    port map (fi => atgikmjd, b => qkor);
end dxjeuvo;



-- Seed after: 16339732819358897986,18037650846010261179
