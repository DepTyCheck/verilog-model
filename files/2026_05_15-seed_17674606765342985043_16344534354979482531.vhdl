-- Seed: 17674606765342985043,16344534354979482531

library ieee;
use ieee.std_logic_1164.all;

entity hga is
  port (h : inout boolean; zjcr : in bit; f : inout std_logic; ieyaxymv : linkage real);
end hga;



architecture tqzpffyh of hga is
  
begin
  
end tqzpffyh;

library ieee;
use ieee.std_logic_1164.all;

entity bhcl is
  port (n : buffer std_logic; nnw : linkage std_logic; bhjyv : in integer);
end bhcl;

library ieee;
use ieee.std_logic_1164.all;

architecture dkhisrff of bhcl is
  signal golx : bit;
  signal bfhuvoo : boolean;
  signal zyyinydj : real;
  signal yj : std_logic;
  signal j : bit;
  signal hhamsnzijo : boolean;
  signal fnub : real;
  signal ayturcbjl : std_logic;
  signal qfadqyjf : bit;
  signal cslipeqfa : boolean;
begin
  bzhdyedwl : entity work.hga
    port map (h => cslipeqfa, zjcr => qfadqyjf, f => ayturcbjl, ieyaxymv => fnub);
  mflfhj : entity work.hga
    port map (h => hhamsnzijo, zjcr => j, f => yj, ieyaxymv => zyyinydj);
  ddczuj : entity work.hga
    port map (h => bfhuvoo, zjcr => golx, f => yj, ieyaxymv => zyyinydj);
end dkhisrff;



entity lfufto is
  port (uovasmrhiy : out time; alrakypsnu : inout time; gfcqltau : linkage severity_level);
end lfufto;

library ieee;
use ieee.std_logic_1164.all;

architecture iteutytga of lfufto is
  signal cqztrsbo : real;
  signal nll : boolean;
  signal tss : bit;
  signal uclou : boolean;
  signal oziek : real;
  signal mt : std_logic;
  signal uhotqo : bit;
  signal rjo : boolean;
  signal zwsepk : real;
  signal pszbbofh : std_logic;
  signal xqesrhbl : bit;
  signal dl : boolean;
begin
  iry : entity work.hga
    port map (h => dl, zjcr => xqesrhbl, f => pszbbofh, ieyaxymv => zwsepk);
  da : entity work.hga
    port map (h => rjo, zjcr => uhotqo, f => mt, ieyaxymv => oziek);
  zen : entity work.hga
    port map (h => uclou, zjcr => tss, f => pszbbofh, ieyaxymv => zwsepk);
  xf : entity work.hga
    port map (h => nll, zjcr => xqesrhbl, f => pszbbofh, ieyaxymv => cqztrsbo);
end iteutytga;



-- Seed after: 6915456118181232596,16344534354979482531
