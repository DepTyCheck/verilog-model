-- Seed: 7575122953468797949,17234720251424330329

entity u is
  port (cjbdhnoyce : linkage integer; lifimoyqhx : out boolean_vector(2 to 1));
end u;

architecture axbzrg of u is
  
begin
  -- Single-driven assignments
  lifimoyqhx <= lifimoyqhx;
end axbzrg;

library ieee;
use ieee.std_logic_1164.all;

entity atjdcq is
  port (ghjqcnl : buffer integer; whivcmt : buffer std_logic_vector(3 downto 1));
end atjdcq;

architecture biuknau of atjdcq is
  signal mb : boolean_vector(2 to 1);
  signal jo : integer;
  signal f : boolean_vector(2 to 1);
  signal cfuzcc : integer;
  signal ft : boolean_vector(2 to 1);
  signal gtdphcf : boolean_vector(2 to 1);
  signal evbeqhu : integer;
begin
  tsoeg : entity work.u
    port map (cjbdhnoyce => evbeqhu, lifimoyqhx => gtdphcf);
  uwimad : entity work.u
    port map (cjbdhnoyce => ghjqcnl, lifimoyqhx => ft);
  sws : entity work.u
    port map (cjbdhnoyce => cfuzcc, lifimoyqhx => f);
  k : entity work.u
    port map (cjbdhnoyce => jo, lifimoyqhx => mb);
  
  -- Multi-driven assignments
  whivcmt <= whivcmt;
  whivcmt <= "X0H";
  whivcmt <= whivcmt;
end biuknau;

entity hsszna is
  port (dlmhxi : buffer boolean);
end hsszna;

architecture emab of hsszna is
  signal amgszwv : boolean_vector(2 to 1);
  signal ljececmtm : integer;
  signal j : boolean_vector(2 to 1);
  signal jwgsozrhl : integer;
begin
  tvxyzvvke : entity work.u
    port map (cjbdhnoyce => jwgsozrhl, lifimoyqhx => j);
  t : entity work.u
    port map (cjbdhnoyce => ljececmtm, lifimoyqhx => amgszwv);
  
  -- Single-driven assignments
  dlmhxi <= FALSE;
end emab;



-- Seed after: 10242574560469693864,17234720251424330329
