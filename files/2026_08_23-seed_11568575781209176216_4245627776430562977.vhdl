-- Seed: 11568575781209176216,4245627776430562977

entity vmd is
  port (w : inout real; kl : buffer bit_vector(0 downto 0); swegyqeh : buffer integer; v : inout real_vector(3 downto 2));
end vmd;

architecture t of vmd is
  
begin
  -- Single-driven assignments
  v <= v;
  swegyqeh <= swegyqeh;
end t;

library ieee;
use ieee.std_logic_1164.all;

entity oymrbjp is
  port (clrgoihv : linkage std_logic);
end oymrbjp;

architecture oxhikwpun of oymrbjp is
  signal sh : real_vector(3 downto 2);
  signal gebk : integer;
  signal wqfvjhp : bit_vector(0 downto 0);
  signal bwh : real;
  signal azi : real_vector(3 downto 2);
  signal dqj : integer;
  signal p : bit_vector(0 downto 0);
  signal gaxo : real;
begin
  rukhgarlfd : entity work.vmd
    port map (w => gaxo, kl => p, swegyqeh => dqj, v => azi);
  qn : entity work.vmd
    port map (w => bwh, kl => wqfvjhp, swegyqeh => gebk, v => sh);
end oxhikwpun;



-- Seed after: 4883662028070347602,4245627776430562977
