-- Seed: 5754983450889294878,10871023049702252113

entity rgih is
  port (fqr : out severity_level; cvtyff : in string(2 downto 3));
end rgih;

architecture usxbmfc of rgih is
  
begin
  -- Single-driven assignments
  fqr <= fqr;
end usxbmfc;

entity yl is
  port (mfcfnmxgua : buffer integer; bmcuqwrx : in integer);
end yl;

architecture b of yl is
  signal wwkkp : severity_level;
  signal uhtwyfvboc : string(2 downto 3);
  signal mzhjjmp : severity_level;
  signal jr : string(2 downto 3);
  signal s : severity_level;
  signal lrx : string(2 downto 3);
  signal d : severity_level;
begin
  aikgcex : entity work.rgih
    port map (fqr => d, cvtyff => lrx);
  fcqmsfstn : entity work.rgih
    port map (fqr => s, cvtyff => jr);
  fpnqrntyq : entity work.rgih
    port map (fqr => mzhjjmp, cvtyff => uhtwyfvboc);
  oobikaid : entity work.rgih
    port map (fqr => wwkkp, cvtyff => lrx);
end b;

library ieee;
use ieee.std_logic_1164.all;

entity tmaqny is
  port (axbi : buffer integer; djoqa : buffer std_logic; apagl : linkage character; ckkuefeiv : inout time);
end tmaqny;

architecture mb of tmaqny is
  signal nsw : severity_level;
  signal ndbydb : string(2 downto 3);
  signal px : severity_level;
begin
  miciq : entity work.rgih
    port map (fqr => px, cvtyff => ndbydb);
  joalsnqbhn : entity work.rgih
    port map (fqr => nsw, cvtyff => ndbydb);
  
  -- Single-driven assignments
  ndbydb <= ndbydb;
  ckkuefeiv <= 4030.1232 ns;
  axbi <= 2#01001#;
  
  -- Multi-driven assignments
  djoqa <= 'L';
  djoqa <= 'U';
end mb;



-- Seed after: 11039995535193768922,10871023049702252113
