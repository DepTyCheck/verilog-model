-- Seed: 8137358974720253787,14141408946471626091

library ieee;
use ieee.std_logic_1164.all;

entity blzqsaj is
  port (ocd : inout real; w : in severity_level; floaudvsaf : inout integer; ydqigyz : inout std_logic_vector(4 to 2));
end blzqsaj;

architecture y of blzqsaj is
  
begin
  -- Single-driven assignments
  floaudvsaf <= floaudvsaf;
  ocd <= ocd;
  
  -- Multi-driven assignments
  ydqigyz <= ydqigyz;
end y;

entity xynpoxgtzu is
  port (lx : linkage real_vector(4 downto 1));
end xynpoxgtzu;

library ieee;
use ieee.std_logic_1164.all;

architecture yfanqlfuo of xynpoxgtzu is
  signal eez : std_logic_vector(4 to 2);
  signal vszrg : integer;
  signal lathex : real;
  signal cds : std_logic_vector(4 to 2);
  signal femv : integer;
  signal poqvl : severity_level;
  signal ikp : real;
  signal zlnfh : std_logic_vector(4 to 2);
  signal qufkxogxjm : integer;
  signal ftfcpn : severity_level;
  signal gzcl : real;
begin
  pqyrggfqvn : entity work.blzqsaj
    port map (ocd => gzcl, w => ftfcpn, floaudvsaf => qufkxogxjm, ydqigyz => zlnfh);
  ssaeiqbl : entity work.blzqsaj
    port map (ocd => ikp, w => poqvl, floaudvsaf => femv, ydqigyz => cds);
  jbckrf : entity work.blzqsaj
    port map (ocd => lathex, w => ftfcpn, floaudvsaf => vszrg, ydqigyz => eez);
  
  -- Single-driven assignments
  ftfcpn <= ftfcpn;
  poqvl <= ftfcpn;
  
  -- Multi-driven assignments
  cds <= zlnfh;
  zlnfh <= zlnfh;
  cds <= zlnfh;
end yfanqlfuo;



-- Seed after: 15830961470260224720,14141408946471626091
