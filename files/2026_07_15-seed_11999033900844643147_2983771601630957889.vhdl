-- Seed: 11999033900844643147,2983771601630957889

use std.reflection.all;

entity jjexvizfb is
  port ( wc : linkage integer_vector(0 downto 2)
  ; txsfsenish : buffer bit
  ; variable fkgoqa : inout integer_subtype_mirror_pt
  ; variable gtwrlynw : inout file_value_mirror_pt
  );
end jjexvizfb;

architecture ovla of jjexvizfb is
  
begin
  -- Single-driven assignments
  txsfsenish <= txsfsenish;
end ovla;

use std.reflection.all;

entity yacf is
  port (variable jjobxpu : inout subtype_mirror_pt; slqhifc : out time);
end yacf;

use std.reflection.all;

architecture kqujv of yacf is
  shared variable unv : file_value_mirror_pt;
  shared variable nqduhgp : integer_subtype_mirror_pt;
  signal qzswcazq : bit;
  signal yfsgnpjnyn : integer_vector(0 downto 2);
  shared variable ukrdameblp : file_value_mirror_pt;
  shared variable jctj : integer_subtype_mirror_pt;
  signal yskjakg : bit;
  signal xstp : integer_vector(0 downto 2);
begin
  esqfydwzop : entity work.jjexvizfb
    port map (wc => xstp, txsfsenish => yskjakg, fkgoqa => jctj, gtwrlynw => ukrdameblp);
  g : entity work.jjexvizfb
    port map (wc => yfsgnpjnyn, txsfsenish => qzswcazq, fkgoqa => nqduhgp, gtwrlynw => unv);
  
  -- Single-driven assignments
  slqhifc <= 0214.2_3_4_3_4 fs;
end kqujv;



-- Seed after: 825672574508556180,2983771601630957889
