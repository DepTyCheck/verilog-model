-- Seed: 10036620291198835016,16188444798499499427

entity ntlxlhml is
  port (xevzcmug : out severity_level; qdbu : linkage real);
end ntlxlhml;

architecture cde of ntlxlhml is
  
begin
  -- Single-driven assignments
  xevzcmug <= xevzcmug;
end cde;

library ieee;
use ieee.std_logic_1164.all;

entity zplmlftp is
  port (whuvbelk : buffer std_logic_vector(1 to 3); jrdihai : buffer time; flvgkes : in severity_level; tayni : buffer integer_vector(0 to 3));
end zplmlftp;

architecture ysiktub of zplmlftp is
  signal up : real;
  signal xgam : severity_level;
  signal dhkqiu : real;
  signal bpxmezrr : severity_level;
  signal rtgmqihcz : real;
  signal doqz : severity_level;
begin
  cxj : entity work.ntlxlhml
    port map (xevzcmug => doqz, qdbu => rtgmqihcz);
  cheyaoh : entity work.ntlxlhml
    port map (xevzcmug => bpxmezrr, qdbu => dhkqiu);
  zgmgw : entity work.ntlxlhml
    port map (xevzcmug => xgam, qdbu => up);
  
  -- Single-driven assignments
  jrdihai <= jrdihai;
  tayni <= tayni;
  
  -- Multi-driven assignments
  whuvbelk <= ('-', '1', '-');
  whuvbelk <= whuvbelk;
  whuvbelk <= ('1', 'H', 'L');
  whuvbelk <= whuvbelk;
end ysiktub;

entity lyotirtm is
  port (l : inout integer; kcu : linkage time; gag : inout real_vector(4 downto 2));
end lyotirtm;

library ieee;
use ieee.std_logic_1164.all;

architecture hjj of lyotirtm is
  signal iydljmot : real;
  signal mshunt : severity_level;
  signal ygmhbxgut : integer_vector(0 to 3);
  signal jx : time;
  signal bb : std_logic_vector(1 to 3);
  signal zmxgabmff : real;
  signal ijimx : severity_level;
  signal ktpggbnsqz : real;
  signal ctafagujfc : severity_level;
begin
  vwjhykslc : entity work.ntlxlhml
    port map (xevzcmug => ctafagujfc, qdbu => ktpggbnsqz);
  kpauyvmf : entity work.ntlxlhml
    port map (xevzcmug => ijimx, qdbu => zmxgabmff);
  rrtgob : entity work.zplmlftp
    port map (whuvbelk => bb, jrdihai => jx, flvgkes => ctafagujfc, tayni => ygmhbxgut);
  okvwfwqdvf : entity work.ntlxlhml
    port map (xevzcmug => mshunt, qdbu => iydljmot);
  
  -- Single-driven assignments
  gag <= gag;
  l <= 20;
  
  -- Multi-driven assignments
  bb <= bb;
  bb <= "-XX";
  bb <= bb;
  bb <= bb;
end hjj;



-- Seed after: 8995264897039491004,16188444798499499427
