-- Seed: 3565733940756063819,14141408946471626091

entity ypldlvwbmy is
  port (frvyb : linkage integer_vector(2 downto 3); hwtgs : inout time; gggqyhlba : in integer);
end ypldlvwbmy;

architecture yarnh of ypldlvwbmy is
  
begin
  -- Single-driven assignments
  hwtgs <= hwtgs;
end yarnh;

entity nkxdera is
  port (n : linkage integer_vector(1 to 2));
end nkxdera;

architecture zjpprj of nkxdera is
  signal qmuczd : time;
  signal twi : integer_vector(2 downto 3);
  signal kzfhdz : integer;
  signal aj : time;
  signal nlhwutliw : integer_vector(2 downto 3);
begin
  yxpcjmk : entity work.ypldlvwbmy
    port map (frvyb => nlhwutliw, hwtgs => aj, gggqyhlba => kzfhdz);
  ci : entity work.ypldlvwbmy
    port map (frvyb => twi, hwtgs => qmuczd, gggqyhlba => kzfhdz);
end zjpprj;

library ieee;
use ieee.std_logic_1164.all;

entity iu is
  port (jx : in std_logic_vector(2 to 2));
end iu;

architecture snbohve of iu is
  signal ckcoujwxt : integer;
  signal idlxxxl : time;
  signal o : integer_vector(2 downto 3);
  signal g : integer_vector(1 to 2);
begin
  nplqb : entity work.nkxdera
    port map (n => g);
  eiwkzcyqq : entity work.ypldlvwbmy
    port map (frvyb => o, hwtgs => idlxxxl, gggqyhlba => ckcoujwxt);
  
  -- Single-driven assignments
  ckcoujwxt <= 2#10101#;
end snbohve;

library ieee;
use ieee.std_logic_1164.all;

entity ruvzh is
  port (fikwmeezid : inout time_vector(3 to 2); kvytqgx : buffer boolean; qe : out severity_level; zgzewuve : in std_logic);
end ruvzh;

library ieee;
use ieee.std_logic_1164.all;

architecture swixwb of ruvzh is
  signal rkdy : integer_vector(1 to 2);
  signal purmnfpc : std_logic_vector(2 to 2);
  signal aldpia : integer;
  signal jnrsbnlsz : time;
  signal ynttohm : integer_vector(2 downto 3);
begin
  hsyrm : entity work.ypldlvwbmy
    port map (frvyb => ynttohm, hwtgs => jnrsbnlsz, gggqyhlba => aldpia);
  lvzvq : entity work.iu
    port map (jx => purmnfpc);
  pwbuqu : entity work.nkxdera
    port map (n => rkdy);
  
  -- Multi-driven assignments
  purmnfpc <= "X";
end swixwb;



-- Seed after: 10359922846379481108,14141408946471626091
