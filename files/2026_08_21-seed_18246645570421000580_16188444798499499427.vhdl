-- Seed: 18246645570421000580,16188444798499499427

library ieee;
use ieee.std_logic_1164.all;

entity zccfxtw is
  port (ocufuedpdz : buffer boolean; gjbtdwdzv : buffer std_logic_vector(3 to 2); y : inout real; yfcp : inout string(1 to 2));
end zccfxtw;

architecture ahjhgnoyft of zccfxtw is
  
begin
  -- Single-driven assignments
  y <= y;
  ocufuedpdz <= ocufuedpdz;
  
  -- Multi-driven assignments
  gjbtdwdzv <= (others => '0');
end ahjhgnoyft;

library ieee;
use ieee.std_logic_1164.all;

entity ajto is
  port (fsfwu : inout string(4 to 5); k : inout time; pkogpdogvf : buffer std_logic);
end ajto;

library ieee;
use ieee.std_logic_1164.all;

architecture pltdlwvz of ajto is
  signal wubf : real;
  signal ld : boolean;
  signal qxbgxjbusb : string(1 to 2);
  signal l : real;
  signal xbqsoekao : std_logic_vector(3 to 2);
  signal veozt : boolean;
  signal slccs : string(1 to 2);
  signal wmmlzqyafu : real;
  signal a : boolean;
  signal gfki : string(1 to 2);
  signal fbjoye : real;
  signal gbljz : std_logic_vector(3 to 2);
  signal od : boolean;
begin
  yjgcooekqm : entity work.zccfxtw
    port map (ocufuedpdz => od, gjbtdwdzv => gbljz, y => fbjoye, yfcp => gfki);
  borl : entity work.zccfxtw
    port map (ocufuedpdz => a, gjbtdwdzv => gbljz, y => wmmlzqyafu, yfcp => slccs);
  maunhvgrlf : entity work.zccfxtw
    port map (ocufuedpdz => veozt, gjbtdwdzv => xbqsoekao, y => l, yfcp => qxbgxjbusb);
  xqzestcqin : entity work.zccfxtw
    port map (ocufuedpdz => ld, gjbtdwdzv => gbljz, y => wubf, yfcp => fsfwu);
  
  -- Multi-driven assignments
  xbqsoekao <= (others => '0');
  pkogpdogvf <= 'W';
  pkogpdogvf <= pkogpdogvf;
end pltdlwvz;

entity yvqhcprj is
  port (johpxt : inout integer; etfezqi : out real);
end yvqhcprj;

library ieee;
use ieee.std_logic_1164.all;

architecture ro of yvqhcprj is
  signal wxovv : std_logic;
  signal ehtrhc : time;
  signal o : string(4 to 5);
  signal oopukjynm : string(1 to 2);
  signal oznfkype : std_logic_vector(3 to 2);
  signal unkb : boolean;
begin
  bju : entity work.zccfxtw
    port map (ocufuedpdz => unkb, gjbtdwdzv => oznfkype, y => etfezqi, yfcp => oopukjynm);
  bhnvoxsmhf : entity work.ajto
    port map (fsfwu => o, k => ehtrhc, pkogpdogvf => wxovv);
  
  -- Single-driven assignments
  johpxt <= johpxt;
end ro;

entity ipeknep is
  port (gupsuycfxu : inout time);
end ipeknep;

library ieee;
use ieee.std_logic_1164.all;

architecture espq of ipeknep is
  signal mbbafnzh : string(1 to 2);
  signal plnqh : real;
  signal sxvzx : std_logic_vector(3 to 2);
  signal lg : boolean;
begin
  gggkmzdfdb : entity work.zccfxtw
    port map (ocufuedpdz => lg, gjbtdwdzv => sxvzx, y => plnqh, yfcp => mbbafnzh);
  
  -- Single-driven assignments
  gupsuycfxu <= gupsuycfxu;
  
  -- Multi-driven assignments
  sxvzx <= (others => '0');
  sxvzx <= sxvzx;
  sxvzx <= sxvzx;
end espq;



-- Seed after: 18228688325746011273,16188444798499499427
