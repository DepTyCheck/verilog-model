-- Seed: 495963216528386881,2158184632809654795

use std.reflection.all;

entity sz is
  port (ateqveecix : in time; lulyocevoc : inout enumeration_subtype_mirror; ip : inout integer_subtype_mirror);
end sz;

architecture ytwdzx of sz is
  
begin
  
end ytwdzx;

use std.reflection.all;

entity nnebqz is
  port (zxm : out boolean_vector(4 downto 2); jvjonewx : inout file_value_mirror; cideuklwoq : inout file_subtype_mirror);
end nnebqz;

use std.reflection.all;

architecture tlg of nnebqz is
  shared variable jneere : integer_subtype_mirror;
  shared variable ifwvixr : enumeration_subtype_mirror;
  signal gibnb : time;
  shared variable gxgrxshf : integer_subtype_mirror;
  shared variable egcltcp : enumeration_subtype_mirror;
  shared variable w : integer_subtype_mirror;
  shared variable txzrk : enumeration_subtype_mirror;
  signal otf : time;
  shared variable krphykgi : integer_subtype_mirror;
  shared variable fddhmslb : enumeration_subtype_mirror;
  signal h : time;
begin
  qh : entity work.sz
    port map (ateqveecix => h, lulyocevoc => fddhmslb, ip => krphykgi);
  amqq : entity work.sz
    port map (ateqveecix => otf, lulyocevoc => txzrk, ip => w);
  sueuctik : entity work.sz
    port map (ateqveecix => h, lulyocevoc => egcltcp, ip => gxgrxshf);
  jueegpgrgx : entity work.sz
    port map (ateqveecix => gibnb, lulyocevoc => ifwvixr, ip => jneere);
end tlg;



-- Seed after: 11183807434677849838,2158184632809654795
