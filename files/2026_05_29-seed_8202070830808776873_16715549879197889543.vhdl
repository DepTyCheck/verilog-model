-- Seed: 8202070830808776873,16715549879197889543



entity abvvfzfi is
  port (bwbfdja : out time; vquerrzino : in bit_vector(0 downto 2); hivky : inout severity_level; spdjnawlgx : inout real);
end abvvfzfi;



architecture wkpazx of abvvfzfi is
  
begin
  
end wkpazx;

library ieee;
use ieee.std_logic_1164.all;

entity vkmgwsj is
  port (bbpk : out std_logic_vector(4 to 2); nuxfy : out integer);
end vkmgwsj;



architecture bgbkdpn of vkmgwsj is
  signal nmqjqcb : real;
  signal aqnxspguil : severity_level;
  signal kl : time;
  signal bej : real;
  signal mhwhthg : severity_level;
  signal rgp : bit_vector(0 downto 2);
  signal pdoyojslu : time;
  signal t : real;
  signal yrbzdten : severity_level;
  signal aqotoqq : bit_vector(0 downto 2);
  signal anijo : time;
  signal fgvkwthve : real;
  signal clpzbffk : severity_level;
  signal ohslfwzc : bit_vector(0 downto 2);
  signal ffjzrhy : time;
begin
  caoxa : entity work.abvvfzfi
    port map (bwbfdja => ffjzrhy, vquerrzino => ohslfwzc, hivky => clpzbffk, spdjnawlgx => fgvkwthve);
  mtvfdfcja : entity work.abvvfzfi
    port map (bwbfdja => anijo, vquerrzino => aqotoqq, hivky => yrbzdten, spdjnawlgx => t);
  czjzi : entity work.abvvfzfi
    port map (bwbfdja => pdoyojslu, vquerrzino => rgp, hivky => mhwhthg, spdjnawlgx => bej);
  w : entity work.abvvfzfi
    port map (bwbfdja => kl, vquerrzino => ohslfwzc, hivky => aqnxspguil, spdjnawlgx => nmqjqcb);
end bgbkdpn;



entity myihe is
  port (gkzvlye : linkage time; gr : inout integer; gztyoirz : buffer character);
end myihe;

library ieee;
use ieee.std_logic_1164.all;

architecture ctceywdqhw of myihe is
  signal rg : real;
  signal ll : severity_level;
  signal bvkzvwrxa : time;
  signal cevpncy : real;
  signal xlwvjpdq : severity_level;
  signal ewd : bit_vector(0 downto 2);
  signal qiegebm : time;
  signal gcpumro : std_logic_vector(4 to 2);
begin
  mwinkew : entity work.vkmgwsj
    port map (bbpk => gcpumro, nuxfy => gr);
  qkfrhrnjmw : entity work.abvvfzfi
    port map (bwbfdja => qiegebm, vquerrzino => ewd, hivky => xlwvjpdq, spdjnawlgx => cevpncy);
  sc : entity work.abvvfzfi
    port map (bwbfdja => bvkzvwrxa, vquerrzino => ewd, hivky => ll, spdjnawlgx => rg);
end ctceywdqhw;

library ieee;
use ieee.std_logic_1164.all;

entity dtvqtzv is
  port (xeacqjfotv : in boolean; dybzqzxdz : buffer std_logic);
end dtvqtzv;



architecture xgi of dtvqtzv is
  signal mmc : character;
  signal gvxbjtbt : integer;
  signal nqzdnotknb : character;
  signal y : integer;
  signal beou : real;
  signal moy : severity_level;
  signal egltgmr : bit_vector(0 downto 2);
  signal qope : time;
begin
  f : entity work.abvvfzfi
    port map (bwbfdja => qope, vquerrzino => egltgmr, hivky => moy, spdjnawlgx => beou);
  trfqfldysr : entity work.myihe
    port map (gkzvlye => qope, gr => y, gztyoirz => nqzdnotknb);
  slgbutyzr : entity work.myihe
    port map (gkzvlye => qope, gr => gvxbjtbt, gztyoirz => mmc);
end xgi;



-- Seed after: 1633773797652409460,16715549879197889543
