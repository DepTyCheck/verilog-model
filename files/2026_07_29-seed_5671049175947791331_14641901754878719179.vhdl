-- Seed: 5671049175947791331,14641901754878719179

library ieee;
use ieee.std_logic_1164.all;

entity bbewzw is
  port (rzsxwc : in severity_level; g : out real; ubnhfer : inout std_logic_vector(4 downto 0); veapdnguch : buffer boolean);
end bbewzw;

architecture xmcne of bbewzw is
  
begin
  -- Single-driven assignments
  veapdnguch <= TRUE;
  g <= 8#1_3_4_5_1.5_6_4_4_5#;
  
  -- Multi-driven assignments
  ubnhfer <= "1W1UZ";
  ubnhfer <= ('X', 'X', 'W', '-', 'U');
  ubnhfer <= ubnhfer;
  ubnhfer <= ('U', 'H', '-', 'L', 'U');
end xmcne;

library ieee;
use ieee.std_logic_1164.all;

entity yascsopwk is
  port (wvvzsyi : linkage severity_level; wmaof : inout character; l : in std_logic_vector(0 downto 0); srbxltwii : linkage time);
end yascsopwk;

library ieee;
use ieee.std_logic_1164.all;

architecture zufustgdho of yascsopwk is
  signal s : boolean;
  signal raeyg : std_logic_vector(4 downto 0);
  signal ip : real;
  signal wogoisygod : severity_level;
begin
  talz : entity work.bbewzw
    port map (rzsxwc => wogoisygod, g => ip, ubnhfer => raeyg, veapdnguch => s);
  
  -- Single-driven assignments
  wogoisygod <= WARNING;
  wmaof <= wmaof;
  
  -- Multi-driven assignments
  raeyg <= "LL0W0";
end zufustgdho;

library ieee;
use ieee.std_logic_1164.all;

entity ykcpaje is
  port (qfthc : in integer; dlkdzepodr : in std_logic);
end ykcpaje;

library ieee;
use ieee.std_logic_1164.all;

architecture ktmdobifo of ykcpaje is
  signal uwe : boolean;
  signal ytxho : std_logic_vector(4 downto 0);
  signal nx : real;
  signal ziryhs : severity_level;
  signal j : time;
  signal kwmmkwsuvy : std_logic_vector(0 downto 0);
  signal sl : character;
  signal qehhskbhmd : severity_level;
  signal aq : time;
  signal pau : std_logic_vector(0 downto 0);
  signal esx : character;
  signal ixnezvenvd : severity_level;
begin
  xyzp : entity work.yascsopwk
    port map (wvvzsyi => ixnezvenvd, wmaof => esx, l => pau, srbxltwii => aq);
  o : entity work.yascsopwk
    port map (wvvzsyi => qehhskbhmd, wmaof => sl, l => kwmmkwsuvy, srbxltwii => j);
  aou : entity work.bbewzw
    port map (rzsxwc => ziryhs, g => nx, ubnhfer => ytxho, veapdnguch => uwe);
  
  -- Single-driven assignments
  ziryhs <= FAILURE;
  
  -- Multi-driven assignments
  pau <= pau;
  pau <= (others => 'L');
end ktmdobifo;

entity grva is
  port (njcoxfkabr : linkage bit; ggvahqorq : inout real);
end grva;

library ieee;
use ieee.std_logic_1164.all;

architecture neqcf of grva is
  signal bsuw : boolean;
  signal txbqnm : boolean;
  signal sbivpjvki : real;
  signal ox : boolean;
  signal u : std_logic_vector(4 downto 0);
  signal unlvs : real;
  signal bfydji : boolean;
  signal gxveucqe : std_logic_vector(4 downto 0);
  signal vre : real;
  signal tthits : severity_level;
begin
  bnmtxacdjk : entity work.bbewzw
    port map (rzsxwc => tthits, g => vre, ubnhfer => gxveucqe, veapdnguch => bfydji);
  dl : entity work.bbewzw
    port map (rzsxwc => tthits, g => unlvs, ubnhfer => u, veapdnguch => ox);
  gmxabrtq : entity work.bbewzw
    port map (rzsxwc => tthits, g => sbivpjvki, ubnhfer => gxveucqe, veapdnguch => txbqnm);
  unbb : entity work.bbewzw
    port map (rzsxwc => tthits, g => ggvahqorq, ubnhfer => u, veapdnguch => bsuw);
  
  -- Single-driven assignments
  tthits <= tthits;
end neqcf;



-- Seed after: 7532077675613364373,14641901754878719179
