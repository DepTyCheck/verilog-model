-- Seed: 14701785254543792081,13613332369802491303

entity ipzcdx is
  port (phpqf : buffer boolean_vector(1 downto 1); ichb : in integer; ixsnscxy : in time);
end ipzcdx;

architecture sf of ipzcdx is
  
begin
  -- Single-driven assignments
  phpqf <= (others => FALSE);
end sf;

entity mz is
  port (vajmok : inout real; uborae : linkage real; irzkmlgafp : buffer integer_vector(0 downto 3));
end mz;

architecture ujm of mz is
  signal banl : time;
  signal wcpah : integer;
  signal euibfz : boolean_vector(1 downto 1);
  signal kv : time;
  signal tjasu : integer;
  signal ksurtvr : boolean_vector(1 downto 1);
  signal ysnqvrbhvv : time;
  signal steknzo : integer;
  signal d : boolean_vector(1 downto 1);
begin
  regrqpbid : entity work.ipzcdx
    port map (phpqf => d, ichb => steknzo, ixsnscxy => ysnqvrbhvv);
  xaaii : entity work.ipzcdx
    port map (phpqf => ksurtvr, ichb => tjasu, ixsnscxy => kv);
  vjohk : entity work.ipzcdx
    port map (phpqf => euibfz, ichb => wcpah, ixsnscxy => banl);
  
  -- Single-driven assignments
  irzkmlgafp <= irzkmlgafp;
  wcpah <= 03421;
  ysnqvrbhvv <= ysnqvrbhvv;
end ujm;

library ieee;
use ieee.std_logic_1164.all;

entity uhhchr is
  port (gdpydsg : in std_logic);
end uhhchr;

architecture gtxkczdsu of uhhchr is
  signal vdibtls : time;
  signal j : boolean_vector(1 downto 1);
  signal dfpk : time;
  signal fjw : integer;
  signal opo : boolean_vector(1 downto 1);
begin
  y : entity work.ipzcdx
    port map (phpqf => opo, ichb => fjw, ixsnscxy => dfpk);
  tvy : entity work.ipzcdx
    port map (phpqf => j, ichb => fjw, ixsnscxy => vdibtls);
  
  -- Single-driven assignments
  fjw <= fjw;
  vdibtls <= dfpk;
  dfpk <= dfpk;
end gtxkczdsu;



-- Seed after: 1997825724016030760,13613332369802491303
