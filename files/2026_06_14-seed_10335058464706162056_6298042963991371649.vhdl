-- Seed: 10335058464706162056,6298042963991371649

library ieee;
use ieee.std_logic_1164.all;

entity ifaprk is
  port (fl : inout std_logic; ra : in time; ndwuadzxt : buffer real_vector(4 downto 1); wxs : buffer time_vector(4 downto 4));
end ifaprk;



architecture ajlepjkpp of ifaprk is
  
begin
  
end ajlepjkpp;

library ieee;
use ieee.std_logic_1164.all;

entity tz is
  port (e : out real; qfuayd : linkage std_logic);
end tz;

library ieee;
use ieee.std_logic_1164.all;

architecture ksxm of tz is
  signal wujesztphq : time_vector(4 downto 4);
  signal lswxtngsf : real_vector(4 downto 1);
  signal lkga : time;
  signal vmjmq : std_logic;
begin
  kyawawufa : entity work.ifaprk
    port map (fl => vmjmq, ra => lkga, ndwuadzxt => lswxtngsf, wxs => wujesztphq);
end ksxm;



entity msq is
  port (chhummg : inout bit);
end msq;

library ieee;
use ieee.std_logic_1164.all;

architecture hzesgsvd of msq is
  signal bby : time_vector(4 downto 4);
  signal bcl : real_vector(4 downto 1);
  signal cjksh : time;
  signal f : std_logic;
  signal urjx : time_vector(4 downto 4);
  signal jieddrc : real_vector(4 downto 1);
  signal ziwoo : time;
  signal fchi : real;
  signal vnkdgvl : std_logic;
  signal cphjdz : real;
begin
  okqovhltt : entity work.tz
    port map (e => cphjdz, qfuayd => vnkdgvl);
  iqfvhvl : entity work.tz
    port map (e => fchi, qfuayd => vnkdgvl);
  g : entity work.ifaprk
    port map (fl => vnkdgvl, ra => ziwoo, ndwuadzxt => jieddrc, wxs => urjx);
  c : entity work.ifaprk
    port map (fl => f, ra => cjksh, ndwuadzxt => bcl, wxs => bby);
end hzesgsvd;

library ieee;
use ieee.std_logic_1164.all;

entity svfa is
  port (seghtkfjh : in std_logic; ankstjc : buffer boolean_vector(3 to 0); abvbf : in integer);
end svfa;

library ieee;
use ieee.std_logic_1164.all;

architecture zlightdaf of svfa is
  signal sjfkxqzhob : time_vector(4 downto 4);
  signal uveumb : real_vector(4 downto 1);
  signal rxvvw : time;
  signal jr : std_logic;
  signal xmagb : real;
begin
  wkbno : entity work.tz
    port map (e => xmagb, qfuayd => jr);
  cmjefkidup : entity work.ifaprk
    port map (fl => jr, ra => rxvvw, ndwuadzxt => uveumb, wxs => sjfkxqzhob);
end zlightdaf;



-- Seed after: 5779502931681915771,6298042963991371649
