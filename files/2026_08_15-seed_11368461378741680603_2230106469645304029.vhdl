-- Seed: 11368461378741680603,2230106469645304029

library ieee;
use ieee.std_logic_1164.all;

entity gid is
  port (l : out time; kwhflq : buffer std_logic_vector(1 downto 1); zmmpno : inout character; vjmzpd : buffer std_logic);
end gid;

architecture kugf of gid is
  
begin
  -- Multi-driven assignments
  vjmzpd <= vjmzpd;
end kugf;

library ieee;
use ieee.std_logic_1164.all;

entity jcygxbuol is
  port (nd : out character; quo : inout time; vz : buffer std_logic; azrvycrq : out real);
end jcygxbuol;

library ieee;
use ieee.std_logic_1164.all;

architecture pgcc of jcygxbuol is
  signal bls : std_logic;
  signal kwxgjr : time;
  signal k : std_logic;
  signal o : character;
  signal roxkmpo : std_logic_vector(1 downto 1);
begin
  grpcrynil : entity work.gid
    port map (l => quo, kwhflq => roxkmpo, zmmpno => o, vjmzpd => k);
  spdhexgp : entity work.gid
    port map (l => kwxgjr, kwhflq => roxkmpo, zmmpno => nd, vjmzpd => bls);
  
  -- Single-driven assignments
  azrvycrq <= azrvycrq;
  
  -- Multi-driven assignments
  vz <= vz;
  vz <= vz;
  vz <= vz;
end pgcc;



-- Seed after: 102813254877672799,2230106469645304029
