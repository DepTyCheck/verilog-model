-- Seed: 1826508528100385200,13684241126228714147

library ieee;
use ieee.std_logic_1164.all;

entity jnmkcx is
  port (tqxiysdns : linkage real; xziebtxc : in std_logic; kqkk : in boolean; pq : out bit);
end jnmkcx;



architecture trha of jnmkcx is
  
begin
  
end trha;

library ieee;
use ieee.std_logic_1164.all;

entity uxpvlu is
  port (r : buffer std_logic; emeciw : in boolean; e : linkage integer);
end uxpvlu;

library ieee;
use ieee.std_logic_1164.all;

architecture dzyoibt of uxpvlu is
  signal dshz : bit;
  signal boevzlfair : real;
  signal xunpl : bit;
  signal oeuxwba : bit;
  signal gczsnxt : boolean;
  signal rr : bit;
  signal cu : std_logic;
  signal vaxe : real;
begin
  qicsgqk : entity work.jnmkcx
    port map (tqxiysdns => vaxe, xziebtxc => cu, kqkk => emeciw, pq => rr);
  lia : entity work.jnmkcx
    port map (tqxiysdns => vaxe, xziebtxc => r, kqkk => gczsnxt, pq => oeuxwba);
  k : entity work.jnmkcx
    port map (tqxiysdns => vaxe, xziebtxc => r, kqkk => emeciw, pq => xunpl);
  vs : entity work.jnmkcx
    port map (tqxiysdns => boevzlfair, xziebtxc => r, kqkk => emeciw, pq => dshz);
end dzyoibt;



-- Seed after: 11618378218591325720,13684241126228714147
