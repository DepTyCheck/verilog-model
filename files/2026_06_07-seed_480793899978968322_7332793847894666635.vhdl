-- Seed: 480793899978968322,7332793847894666635

library ieee;
use ieee.std_logic_1164.all;

entity h is
  port (yy : in std_logic; uhpxsclx : buffer real; urfrhmjv : inout real; avmde : inout integer);
end h;



architecture pgmuhzp of h is
  
begin
  
end pgmuhzp;

library ieee;
use ieee.std_logic_1164.all;

entity uehzjfeuep is
  port (ctxenm : buffer std_logic_vector(0 downto 3); y : inout std_logic_vector(1 to 4));
end uehzjfeuep;

library ieee;
use ieee.std_logic_1164.all;

architecture pjsviv of uehzjfeuep is
  signal kb : integer;
  signal iokzmn : real;
  signal wtpidpj : real;
  signal hdza : std_logic;
  signal cpujmeofpp : integer;
  signal kmyv : real;
  signal mfizn : real;
  signal dhdpg : integer;
  signal xvhpbol : real;
  signal xootdjwgwc : real;
  signal rqsxegky : std_logic;
  signal yxohns : integer;
  signal mokis : real;
  signal hclk : real;
  signal krlg : std_logic;
begin
  sr : entity work.h
    port map (yy => krlg, uhpxsclx => hclk, urfrhmjv => mokis, avmde => yxohns);
  ktrlahaxq : entity work.h
    port map (yy => rqsxegky, uhpxsclx => xootdjwgwc, urfrhmjv => xvhpbol, avmde => dhdpg);
  ep : entity work.h
    port map (yy => rqsxegky, uhpxsclx => mfizn, urfrhmjv => kmyv, avmde => cpujmeofpp);
  yu : entity work.h
    port map (yy => hdza, uhpxsclx => wtpidpj, urfrhmjv => iokzmn, avmde => kb);
end pjsviv;



entity qaulyuvob is
  port (yrcltta : out real; fggcectvdd : in time);
end qaulyuvob;



architecture sz of qaulyuvob is
  
begin
  
end sz;

library ieee;
use ieee.std_logic_1164.all;

entity itsnxr is
  port (zwclp : inout std_logic_vector(2 to 0); tc : buffer integer);
end itsnxr;



architecture bektn of itsnxr is
  signal vzcnntie : time;
  signal ddg : real;
begin
  rzkwymmrww : entity work.qaulyuvob
    port map (yrcltta => ddg, fggcectvdd => vzcnntie);
end bektn;



-- Seed after: 17804519607620248507,7332793847894666635
