-- Seed: 17841299780066741899,4177195558088809003

entity m is
  port (bycksfvy : out time);
end m;

architecture f of m is
  
begin
  -- Single-driven assignments
  bycksfvy <= bycksfvy;
end f;

library ieee;
use ieee.std_logic_1164.all;

entity lowb is
  port (sfpp : inout std_logic_vector(2 downto 3); sv : in std_logic);
end lowb;

architecture ommrlnzbq of lowb is
  signal sduieoyc : time;
  signal ctuarpszmm : time;
begin
  tcx : entity work.m
    port map (bycksfvy => ctuarpszmm);
  nknncxc : entity work.m
    port map (bycksfvy => sduieoyc);
  
  -- Multi-driven assignments
  sfpp <= "";
  sfpp <= sfpp;
end ommrlnzbq;

entity hzx is
  port (g : buffer string(1 to 5));
end hzx;

architecture jmqdtrv of hzx is
  signal iuqjdem : time;
  signal gzcikq : time;
begin
  jnzr : entity work.m
    port map (bycksfvy => gzcikq);
  ww : entity work.m
    port map (bycksfvy => iuqjdem);
  
  -- Single-driven assignments
  g <= "egime";
end jmqdtrv;



-- Seed after: 16733731154042095324,4177195558088809003
