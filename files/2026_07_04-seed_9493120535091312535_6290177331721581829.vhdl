-- Seed: 9493120535091312535,6290177331721581829

use std.reflection.all;

entity s is
  port (mmogolsg : buffer integer; q : inout floating_subtype_mirror; nstydsc : out string(1 to 2); ac : inout protected_value_mirror);
end s;

architecture jn of s is
  
begin
  -- Single-driven assignments
  nstydsc <= nstydsc;
  mmogolsg <= 8#11#;
end jn;

use std.reflection.all;

entity zw is
  port ( elkkvx : inout protected_subtype_mirror
  ; hoipddueyk : inout protected_subtype_mirror
  ; llegqt : linkage integer
  ; j : inout enumeration_value_mirror
  );
end zw;

use std.reflection.all;

architecture jwqtfuot of zw is
  shared variable r : protected_value_mirror;
  signal fkvqqufgu : string(1 to 2);
  shared variable aoihstxmv : floating_subtype_mirror;
  signal orykfi : integer;
  shared variable ojklfuzcy : protected_value_mirror;
  signal bopyzw : string(1 to 2);
  shared variable wdseoae : floating_subtype_mirror;
  signal jkx : integer;
  shared variable fza : protected_value_mirror;
  signal yib : string(1 to 2);
  shared variable qlayynimm : floating_subtype_mirror;
  signal dcj : integer;
begin
  oynqf : entity work.s
    port map (mmogolsg => dcj, q => qlayynimm, nstydsc => yib, ac => fza);
  txbhb : entity work.s
    port map (mmogolsg => jkx, q => wdseoae, nstydsc => bopyzw, ac => ojklfuzcy);
  dsiv : entity work.s
    port map (mmogolsg => orykfi, q => aoihstxmv, nstydsc => fkvqqufgu, ac => r);
end jwqtfuot;

library ieee;
use ieee.std_logic_1164.all;

entity nhk is
  port (izfsoxjh : out real; atromq : linkage std_logic_vector(1 to 1));
end nhk;

use std.reflection.all;

architecture yda of nhk is
  shared variable efsgxtp : enumeration_value_mirror;
  signal mxihrwek : integer;
  shared variable gdgrhdhsi : protected_subtype_mirror;
  shared variable slkpr : protected_subtype_mirror;
  shared variable wjihcup : enumeration_value_mirror;
  signal ugcvn : integer;
  shared variable gurjweh : protected_subtype_mirror;
  shared variable recnmh : protected_subtype_mirror;
  shared variable w : protected_value_mirror;
  signal tabvym : string(1 to 2);
  shared variable qwmg : floating_subtype_mirror;
  signal nqmvjsro : integer;
  shared variable izebwn : enumeration_value_mirror;
  signal jvq : integer;
  shared variable wfpqo : protected_subtype_mirror;
  shared variable jmakmeo : protected_subtype_mirror;
begin
  chzqnb : entity work.zw
    port map (elkkvx => jmakmeo, hoipddueyk => wfpqo, llegqt => jvq, j => izebwn);
  thzxjmfrj : entity work.s
    port map (mmogolsg => nqmvjsro, q => qwmg, nstydsc => tabvym, ac => w);
  ef : entity work.zw
    port map (elkkvx => recnmh, hoipddueyk => gurjweh, llegqt => ugcvn, j => wjihcup);
  exqorf : entity work.zw
    port map (elkkvx => slkpr, hoipddueyk => gdgrhdhsi, llegqt => mxihrwek, j => efsgxtp);
  
  -- Single-driven assignments
  izfsoxjh <= izfsoxjh;
end yda;

use std.reflection.all;

entity agae is
  port (asmzr : inout access_subtype_mirror; i : in time; abxsvi : buffer character);
end agae;

use std.reflection.all;

architecture yopx of agae is
  shared variable ifbsxdaips : protected_value_mirror;
  signal hlot : string(1 to 2);
  shared variable qoafzikz : floating_subtype_mirror;
  signal lmhrxfidj : integer;
begin
  fvj : entity work.s
    port map (mmogolsg => lmhrxfidj, q => qoafzikz, nstydsc => hlot, ac => ifbsxdaips);
  
  -- Single-driven assignments
  abxsvi <= abxsvi;
end yopx;



-- Seed after: 16595944433831624952,6290177331721581829
