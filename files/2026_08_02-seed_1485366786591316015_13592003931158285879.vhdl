-- Seed: 1485366786591316015,13592003931158285879

entity jox is
  port (nbat : inout time; ulez : buffer real; qkydhyb : out integer; sainu : buffer time);
end jox;

architecture hyxdll of jox is
  
begin
  -- Single-driven assignments
  sainu <= sainu;
  nbat <= 2 ns;
  ulez <= 2_2.3;
  qkydhyb <= 2_3;
end hyxdll;

entity oz is
  port (rcl : inout integer; fvsnbcd : out bit; lsxsptb : linkage real; do : buffer string(2 downto 5));
end oz;

architecture jbrsnbu of oz is
  signal nsxmlo : time;
  signal zqongm : integer;
  signal v : real;
  signal vde : time;
  signal uogtxplwrh : time;
  signal dbqyannz : integer;
  signal tost : real;
  signal mkdkvbcjdq : time;
  signal incwfygh : time;
  signal swoynwermk : integer;
  signal rqigf : real;
  signal guqgr : time;
  signal rzjrk : time;
  signal ehpr : integer;
  signal zo : real;
  signal gg : time;
begin
  fyzed : entity work.jox
    port map (nbat => gg, ulez => zo, qkydhyb => ehpr, sainu => rzjrk);
  dxvagkrx : entity work.jox
    port map (nbat => guqgr, ulez => rqigf, qkydhyb => swoynwermk, sainu => incwfygh);
  h : entity work.jox
    port map (nbat => mkdkvbcjdq, ulez => tost, qkydhyb => dbqyannz, sainu => uogtxplwrh);
  dwwxwifaw : entity work.jox
    port map (nbat => vde, ulez => v, qkydhyb => zqongm, sainu => nsxmlo);
  
  -- Single-driven assignments
  do <= "";
  fvsnbcd <= fvsnbcd;
  rcl <= swoynwermk;
end jbrsnbu;

library ieee;
use ieee.std_logic_1164.all;

entity anhlnunw is
  port (kqoe : linkage real; zlqyllqhpz : out std_logic_vector(1 downto 1));
end anhlnunw;

architecture r of anhlnunw is
  signal ryfgaaosp : time;
  signal zv : integer;
  signal a : real;
  signal xjjrvamcfm : time;
  signal kl : time;
  signal oti : integer;
  signal mjniisyw : real;
  signal jkzntmvu : time;
begin
  dcc : entity work.jox
    port map (nbat => jkzntmvu, ulez => mjniisyw, qkydhyb => oti, sainu => kl);
  rieekbcrb : entity work.jox
    port map (nbat => xjjrvamcfm, ulez => a, qkydhyb => zv, sainu => ryfgaaosp);
  
  -- Multi-driven assignments
  zlqyllqhpz <= "-";
end r;

entity igniifodm is
  port (hdh : in character);
end igniifodm;

architecture oacjbfno of igniifodm is
  
begin
  
end oacjbfno;



-- Seed after: 1873261133260771847,13592003931158285879
