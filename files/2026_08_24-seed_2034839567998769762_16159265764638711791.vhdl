-- Seed: 2034839567998769762,16159265764638711791

entity aiuvao is
  port (clrlpd : inout time; fbaiecdt : in integer; am : linkage bit_vector(0 downto 2));
end aiuvao;

architecture pdjswlf of aiuvao is
  
begin
  -- Single-driven assignments
  clrlpd <= clrlpd;
end pdjswlf;

entity ir is
  port (gchlrtpnvl : in real_vector(0 to 0); gptw : inout real);
end ir;

architecture seqan of ir is
  signal kyhmz : bit_vector(0 downto 2);
  signal upxsnbkwp : time;
  signal epx : bit_vector(0 downto 2);
  signal q : integer;
  signal uwofjjekta : time;
  signal mag : bit_vector(0 downto 2);
  signal zpavniwyv : integer;
  signal muidwsdd : time;
begin
  wwxiyi : entity work.aiuvao
    port map (clrlpd => muidwsdd, fbaiecdt => zpavniwyv, am => mag);
  y : entity work.aiuvao
    port map (clrlpd => uwofjjekta, fbaiecdt => q, am => epx);
  s : entity work.aiuvao
    port map (clrlpd => upxsnbkwp, fbaiecdt => zpavniwyv, am => kyhmz);
  
  -- Single-driven assignments
  q <= zpavniwyv;
  zpavniwyv <= 3;
  gptw <= 8#660.5721#;
end seqan;



-- Seed after: 18291182513149185890,16159265764638711791
