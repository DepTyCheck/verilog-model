-- Seed: 3074222363124434128,8068158652091157513

entity fq is
  port (fr : inout boolean; i : in integer; evypzgijqk : linkage severity_level; lfdezo : linkage string(4 downto 4));
end fq;

architecture nrplvr of fq is
  
begin
  -- Single-driven assignments
  fr <= TRUE;
end nrplvr;

library ieee;
use ieee.std_logic_1164.all;

entity l is
  port (bdnwfuh : buffer integer; drx : inout std_logic; s : inout time; yqexmz : linkage time);
end l;

architecture imungxium of l is
  signal ydua : string(4 downto 4);
  signal ncbmrmrjko : severity_level;
  signal apdm : boolean;
  signal nhzjafzx : string(4 downto 4);
  signal fpomekkcoj : severity_level;
  signal cboh : boolean;
begin
  pdht : entity work.fq
    port map (fr => cboh, i => bdnwfuh, evypzgijqk => fpomekkcoj, lfdezo => nhzjafzx);
  re : entity work.fq
    port map (fr => apdm, i => bdnwfuh, evypzgijqk => ncbmrmrjko, lfdezo => ydua);
end imungxium;



-- Seed after: 5275485171133635558,8068158652091157513
