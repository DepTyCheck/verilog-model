-- Seed: 2023863852148465263,3181554006726329157

use std.reflection.all;

entity dlhvsgexvy is
  port (wutp : inout physical_subtype_mirror; d : out time);
end dlhvsgexvy;

architecture euz of dlhvsgexvy is
  
begin
  -- Single-driven assignments
  d <= 24.4441 ns;
end euz;

use std.reflection.all;

entity pxauankvsm is
  port (ivhykxx : inout value_mirror; x : inout array_value_mirror; dasoyrqhon : inout protected_subtype_mirror);
end pxauankvsm;

use std.reflection.all;

architecture jolhfiuj of pxauankvsm is
  signal mdalswyp : time;
  shared variable dusazo : physical_subtype_mirror;
  signal gu : time;
  shared variable wkwoae : physical_subtype_mirror;
begin
  cbmzebt : entity work.dlhvsgexvy
    port map (wutp => wkwoae, d => gu);
  pcyldiekel : entity work.dlhvsgexvy
    port map (wutp => dusazo, d => mdalswyp);
end jolhfiuj;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity stf is
  port (p : linkage real; enh : buffer time; zkiqkv : inout physical_subtype_mirror; cmcaykznwe : out std_logic_vector(0 downto 1));
end stf;

use std.reflection.all;

architecture gt of stf is
  shared variable upvl : physical_subtype_mirror;
  signal fcjksfq : time;
  signal d : time;
  shared variable auriav : physical_subtype_mirror;
begin
  cekc : entity work.dlhvsgexvy
    port map (wutp => auriav, d => d);
  gpjqoj : entity work.dlhvsgexvy
    port map (wutp => zkiqkv, d => fcjksfq);
  jl : entity work.dlhvsgexvy
    port map (wutp => upvl, d => enh);
  
  -- Multi-driven assignments
  cmcaykznwe <= cmcaykznwe;
  cmcaykznwe <= cmcaykznwe;
end gt;



-- Seed after: 7681795015173324010,3181554006726329157
