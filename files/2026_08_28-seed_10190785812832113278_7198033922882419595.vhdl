-- Seed: 10190785812832113278,7198033922882419595

library ieee;
use ieee.std_logic_1164.all;

entity gddwc is
  port (il : inout boolean; yb : linkage real_vector(1 downto 0); fwcq : inout std_logic; miuznzekpm : linkage std_logic);
end gddwc;

architecture bmowxsfrk of gddwc is
  
begin
  -- Single-driven assignments
  il <= TRUE;
  
  -- Multi-driven assignments
  fwcq <= '-';
  fwcq <= '-';
end bmowxsfrk;

entity oxfdejze is
  port (tjm : buffer time; zpscepwfud : linkage bit_vector(0 downto 0));
end oxfdejze;

library ieee;
use ieee.std_logic_1164.all;

architecture ofl of oxfdejze is
  signal cwtfqdkpwv : std_logic;
  signal m : std_logic;
  signal spetza : real_vector(1 downto 0);
  signal ejuw : boolean;
  signal eytdbxhg : std_logic;
  signal dzyxyuxr : std_logic;
  signal oqpwehi : real_vector(1 downto 0);
  signal wwcuelfk : boolean;
begin
  ronhpyrdrj : entity work.gddwc
    port map (il => wwcuelfk, yb => oqpwehi, fwcq => dzyxyuxr, miuznzekpm => eytdbxhg);
  svj : entity work.gddwc
    port map (il => ejuw, yb => spetza, fwcq => m, miuznzekpm => cwtfqdkpwv);
  
  -- Single-driven assignments
  tjm <= tjm;
  
  -- Multi-driven assignments
  dzyxyuxr <= 'W';
end ofl;



-- Seed after: 11363699957294721095,7198033922882419595
