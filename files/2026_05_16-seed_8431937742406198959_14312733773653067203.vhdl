-- Seed: 8431937742406198959,14312733773653067203



entity deapgo is
  port (ivpq : out real; cayildy : inout boolean; a : buffer severity_level);
end deapgo;



architecture kqtwuok of deapgo is
  
begin
  
end kqtwuok;



entity n is
  port (qcxf : in severity_level; ygcnn : out bit; nkqq : linkage time);
end n;



architecture hjnlggalml of n is
  signal akb : severity_level;
  signal tkf : boolean;
  signal mo : real;
  signal jsph : severity_level;
  signal byx : boolean;
  signal cav : real;
  signal dmfeandlvq : severity_level;
  signal jwhaoei : boolean;
  signal droiuylm : real;
  signal ltsbhi : severity_level;
  signal ezcduokev : boolean;
  signal tauggvhhdd : real;
begin
  wejfexlmts : entity work.deapgo
    port map (ivpq => tauggvhhdd, cayildy => ezcduokev, a => ltsbhi);
  g : entity work.deapgo
    port map (ivpq => droiuylm, cayildy => jwhaoei, a => dmfeandlvq);
  ky : entity work.deapgo
    port map (ivpq => cav, cayildy => byx, a => jsph);
  zcet : entity work.deapgo
    port map (ivpq => mo, cayildy => tkf, a => akb);
end hjnlggalml;



entity zeabigntg is
  port (k : out real; ipkrduwi : linkage time);
end zeabigntg;



architecture zhhfn of zeabigntg is
  signal rjakkd : bit;
  signal ephove : severity_level;
  signal sccze : boolean;
begin
  uoeuncy : entity work.deapgo
    port map (ivpq => k, cayildy => sccze, a => ephove);
  hxe : entity work.n
    port map (qcxf => ephove, ygcnn => rjakkd, nkqq => ipkrduwi);
end zhhfn;



-- Seed after: 18370037515182628330,14312733773653067203
