-- Seed: 401411823598331786,390128806780030455



entity xfp is
  port (aeqyadm : buffer character; zdgdbd : buffer time; gbayrays : in integer);
end xfp;



architecture lwcf of xfp is
  
begin
  
end lwcf;



entity dfatf is
  port (jrjoif : buffer time);
end dfatf;



architecture trau of dfatf is
  signal g : character;
  signal ig : integer;
  signal qszidkoew : time;
  signal xjkwove : character;
  signal vwdrs : integer;
  signal izfr : time;
  signal firyysg : character;
begin
  tsjrtlqj : entity work.xfp
    port map (aeqyadm => firyysg, zdgdbd => izfr, gbayrays => vwdrs);
  tjnnfack : entity work.xfp
    port map (aeqyadm => xjkwove, zdgdbd => qszidkoew, gbayrays => ig);
  qsqctftbj : entity work.xfp
    port map (aeqyadm => g, zdgdbd => jrjoif, gbayrays => ig);
end trau;



-- Seed after: 4141011391757808097,390128806780030455
