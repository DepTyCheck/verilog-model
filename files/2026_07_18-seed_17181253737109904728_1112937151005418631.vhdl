-- Seed: 17181253737109904728,1112937151005418631

library ieee;
use ieee.std_logic_1164.all;

entity prgj is
  port (dwhcvaa : in time; aim : in std_logic_vector(1 to 0); gcqfxy : inout std_logic_vector(0 downto 1));
end prgj;

architecture mxnva of prgj is
  
begin
  
end mxnva;

library ieee;
use ieee.std_logic_1164.all;

entity vqzbtqeo is
  port (gtftpxeiyr : buffer std_logic_vector(2 downto 4); pmtweiqvm : in boolean; qfipfaewx : inout std_logic);
end vqzbtqeo;

library ieee;
use ieee.std_logic_1164.all;

architecture wuevameasz of vqzbtqeo is
  signal ql : std_logic_vector(1 to 0);
  signal qyzfoe : std_logic_vector(1 to 0);
  signal pjec : std_logic_vector(0 downto 1);
  signal un : time;
begin
  yffmecmdy : entity work.prgj
    port map (dwhcvaa => un, aim => gtftpxeiyr, gcqfxy => pjec);
  moihfnmnbb : entity work.prgj
    port map (dwhcvaa => un, aim => gtftpxeiyr, gcqfxy => gtftpxeiyr);
  muz : entity work.prgj
    port map (dwhcvaa => un, aim => qyzfoe, gcqfxy => gtftpxeiyr);
  zdosf : entity work.prgj
    port map (dwhcvaa => un, aim => ql, gcqfxy => pjec);
  
  -- Single-driven assignments
  un <= un;
  
  -- Multi-driven assignments
  qfipfaewx <= 'X';
  qfipfaewx <= '-';
  qfipfaewx <= 'U';
end wuevameasz;

library ieee;
use ieee.std_logic_1164.all;

entity glrw is
  port (jtvh : out boolean_vector(1 downto 0); erprexv : buffer real; puuv : linkage severity_level; wnp : out std_logic);
end glrw;

library ieee;
use ieee.std_logic_1164.all;

architecture juqphqtagl of glrw is
  signal dgigsiso : std_logic_vector(0 downto 1);
  signal ie : std_logic_vector(1 to 0);
  signal hsxtj : std_logic_vector(1 to 0);
  signal lmdrsxprtb : time;
begin
  x : entity work.prgj
    port map (dwhcvaa => lmdrsxprtb, aim => hsxtj, gcqfxy => ie);
  csqi : entity work.prgj
    port map (dwhcvaa => lmdrsxprtb, aim => ie, gcqfxy => dgigsiso);
  
  -- Single-driven assignments
  erprexv <= 2#011.110#;
  lmdrsxprtb <= lmdrsxprtb;
  
  -- Multi-driven assignments
  wnp <= '0';
end juqphqtagl;

library ieee;
use ieee.std_logic_1164.all;

entity lrwgoza is
  port (vluvhww : out time_vector(0 downto 1); c : buffer std_logic_vector(2 to 4); yx : linkage std_logic_vector(4 to 1); ujiypyhr : in time);
end lrwgoza;

architecture eybxeu of lrwgoza is
  
begin
  -- Single-driven assignments
  vluvhww <= vluvhww;
  
  -- Multi-driven assignments
  c <= "H1L";
  c <= ('-', 'X', 'L');
  c <= c;
end eybxeu;



-- Seed after: 11147133341000710957,1112937151005418631
