-- Seed: 3524437671893355530,2511821214772927453

library ieee;
use ieee.std_logic_1164.all;

entity ktpewu is
  port (idgm : inout std_logic_vector(4 downto 1); oajnd : inout character; qunductfk : linkage real);
end ktpewu;

architecture fpid of ktpewu is
  
begin
  -- Single-driven assignments
  oajnd <= 'e';
  
  -- Multi-driven assignments
  idgm <= idgm;
  idgm <= ('-', '0', 'W', 'L');
  idgm <= idgm;
  idgm <= idgm;
end fpid;

library ieee;
use ieee.std_logic_1164.all;

entity hxjlio is
  port (mwva : linkage real; j : inout std_logic; hzgvbror : buffer integer_vector(1 downto 4); nnkn : buffer std_logic);
end hxjlio;

library ieee;
use ieee.std_logic_1164.all;

architecture h of hxjlio is
  signal fhcehdowz : character;
  signal puzoxad : real;
  signal hfsz : character;
  signal tpxzictnpm : real;
  signal bhtb : character;
  signal md : std_logic_vector(4 downto 1);
  signal xyfzoxkhdt : real;
  signal uzua : character;
  signal qyyipwlvg : std_logic_vector(4 downto 1);
begin
  rcebv : entity work.ktpewu
    port map (idgm => qyyipwlvg, oajnd => uzua, qunductfk => xyfzoxkhdt);
  iotjgztnmb : entity work.ktpewu
    port map (idgm => md, oajnd => bhtb, qunductfk => tpxzictnpm);
  lvzmdfdm : entity work.ktpewu
    port map (idgm => qyyipwlvg, oajnd => hfsz, qunductfk => puzoxad);
  ihyqjfvz : entity work.ktpewu
    port map (idgm => qyyipwlvg, oajnd => fhcehdowz, qunductfk => mwva);
  
  -- Single-driven assignments
  hzgvbror <= (others => 0);
  
  -- Multi-driven assignments
  j <= '0';
end h;

library ieee;
use ieee.std_logic_1164.all;

entity uwf is
  port (vtkdk : in std_logic);
end uwf;

library ieee;
use ieee.std_logic_1164.all;

architecture ylill of uwf is
  signal eycsx : real;
  signal dn : character;
  signal cdeoqatsye : real;
  signal ftokgrodu : character;
  signal pcg : std_logic_vector(4 downto 1);
  signal wdirdgxf : real;
  signal grtpfgcg : character;
  signal sfma : std_logic_vector(4 downto 1);
  signal yhscohwpb : real;
  signal b : character;
  signal tdxpeefy : std_logic_vector(4 downto 1);
begin
  snslmfps : entity work.ktpewu
    port map (idgm => tdxpeefy, oajnd => b, qunductfk => yhscohwpb);
  wumfzns : entity work.ktpewu
    port map (idgm => sfma, oajnd => grtpfgcg, qunductfk => wdirdgxf);
  pjn : entity work.ktpewu
    port map (idgm => pcg, oajnd => ftokgrodu, qunductfk => cdeoqatsye);
  r : entity work.ktpewu
    port map (idgm => tdxpeefy, oajnd => dn, qunductfk => eycsx);
  
  -- Multi-driven assignments
  sfma <= tdxpeefy;
  sfma <= tdxpeefy;
end ylill;



-- Seed after: 9678891029588970442,2511821214772927453
