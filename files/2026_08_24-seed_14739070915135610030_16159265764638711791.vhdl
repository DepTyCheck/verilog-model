-- Seed: 14739070915135610030,16159265764638711791

entity agqaz is
  port (vucf : linkage time; gwtglsibl : out real_vector(0 downto 4));
end agqaz;

architecture yvgclpem of agqaz is
  
begin
  
end yvgclpem;

entity jaqornkao is
  port (lvdfq : inout integer; ue : out integer; sijn : out string(5 to 1));
end jaqornkao;

architecture lgzkjuyleu of jaqornkao is
  signal ltfnyxzxkc : real_vector(0 downto 4);
  signal q : time;
  signal e : real_vector(0 downto 4);
  signal duulx : time;
  signal sgfoyzhke : real_vector(0 downto 4);
  signal adnv : time;
begin
  qjencdxf : entity work.agqaz
    port map (vucf => adnv, gwtglsibl => sgfoyzhke);
  cb : entity work.agqaz
    port map (vucf => duulx, gwtglsibl => e);
  qmjqxa : entity work.agqaz
    port map (vucf => q, gwtglsibl => ltfnyxzxkc);
  
  -- Single-driven assignments
  sijn <= (others => ' ');
  ue <= ue;
end lgzkjuyleu;

library ieee;
use ieee.std_logic_1164.all;

entity qniskd is
  port (nbty : linkage std_logic_vector(4 downto 1));
end qniskd;

architecture yjtsmloi of qniskd is
  signal dowxyuqsmr : real_vector(0 downto 4);
  signal skul : time;
  signal jcuaphcdrz : real_vector(0 downto 4);
  signal qc : time;
  signal jelrpgjdk : string(5 to 1);
  signal ppg : integer;
  signal dhocwd : integer;
  signal amhoa : real_vector(0 downto 4);
  signal vkivs : time;
begin
  ysbxgpv : entity work.agqaz
    port map (vucf => vkivs, gwtglsibl => amhoa);
  wuzxlnopak : entity work.jaqornkao
    port map (lvdfq => dhocwd, ue => ppg, sijn => jelrpgjdk);
  xrpyw : entity work.agqaz
    port map (vucf => qc, gwtglsibl => jcuaphcdrz);
  ad : entity work.agqaz
    port map (vucf => skul, gwtglsibl => dowxyuqsmr);
end yjtsmloi;

library ieee;
use ieee.std_logic_1164.all;

entity azkhrsg is
  port (kdnxn : inout integer; kkacrhl : linkage std_logic_vector(4 to 4); dwdf : out integer_vector(0 to 1));
end azkhrsg;

library ieee;
use ieee.std_logic_1164.all;

architecture lhgwmiev of azkhrsg is
  signal qcymj : std_logic_vector(4 downto 1);
  signal rvbniq : real_vector(0 downto 4);
  signal yzaf : time;
begin
  hljdjjal : entity work.agqaz
    port map (vucf => yzaf, gwtglsibl => rvbniq);
  qauttascl : entity work.qniskd
    port map (nbty => qcymj);
end lhgwmiev;



-- Seed after: 11251933442223593459,16159265764638711791
