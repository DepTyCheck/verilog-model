-- Seed: 6148213374692700465,4122021602305298647

library ieee;
use ieee.std_logic_1164.all;

entity rrpfhrd is
  port (aiowlrlq : in integer; igd : buffer std_logic_vector(2 to 3); xtefoj : buffer std_logic_vector(0 downto 2); h : linkage time_vector(0 to 2));
end rrpfhrd;

architecture fhfcvsblt of rrpfhrd is
  
begin
  
end fhfcvsblt;

library ieee;
use ieee.std_logic_1164.all;

entity eggclfce is
  port (awbip : linkage time; cc : out std_logic_vector(2 downto 0); zflsnwtfse : out bit; dofht : inout real);
end eggclfce;

library ieee;
use ieee.std_logic_1164.all;

architecture nkblnw of eggclfce is
  signal xl : time_vector(0 to 2);
  signal weghulycrx : integer;
  signal a : time_vector(0 to 2);
  signal tiiqi : time_vector(0 to 2);
  signal qweogpajfl : std_logic_vector(0 downto 2);
  signal ds : std_logic_vector(2 to 3);
  signal sm : integer;
begin
  sfduisalmp : entity work.rrpfhrd
    port map (aiowlrlq => sm, igd => ds, xtefoj => qweogpajfl, h => tiiqi);
  woed : entity work.rrpfhrd
    port map (aiowlrlq => sm, igd => ds, xtefoj => qweogpajfl, h => a);
  tqzvxlrp : entity work.rrpfhrd
    port map (aiowlrlq => weghulycrx, igd => ds, xtefoj => qweogpajfl, h => xl);
  
  -- Single-driven assignments
  sm <= 8#67#;
  dofht <= dofht;
  
  -- Multi-driven assignments
  cc <= ('U', '0', 'H');
  cc <= ('X', '1', 'L');
end nkblnw;

entity jv is
  port (jyspr : in real);
end jv;

library ieee;
use ieee.std_logic_1164.all;

architecture zdskgsxwon of jv is
  signal ebj : time_vector(0 to 2);
  signal zfkgmzwc : integer;
  signal cvwrhg : time_vector(0 to 2);
  signal edgisl : std_logic_vector(0 downto 2);
  signal jb : std_logic_vector(2 to 3);
  signal v : integer;
begin
  fnggrfdkxv : entity work.rrpfhrd
    port map (aiowlrlq => v, igd => jb, xtefoj => edgisl, h => cvwrhg);
  axjvnswtcv : entity work.rrpfhrd
    port map (aiowlrlq => zfkgmzwc, igd => jb, xtefoj => edgisl, h => ebj);
  
  -- Single-driven assignments
  v <= v;
  zfkgmzwc <= 2_2;
  
  -- Multi-driven assignments
  jb <= "LW";
end zdskgsxwon;

library ieee;
use ieee.std_logic_1164.all;

entity bata is
  port (b : in real; uwdqc : in std_logic);
end bata;

library ieee;
use ieee.std_logic_1164.all;

architecture cbqyc of bata is
  signal avwkjliix : time_vector(0 to 2);
  signal obabuvzagh : std_logic_vector(0 downto 2);
  signal bjpitg : std_logic_vector(2 to 3);
  signal kbygjijhr : time_vector(0 to 2);
  signal cyppxdtjur : std_logic_vector(2 to 3);
  signal nspawk : time_vector(0 to 2);
  signal winyf : std_logic_vector(0 downto 2);
  signal yegpdvz : std_logic_vector(2 to 3);
  signal nahwwjmto : integer;
begin
  tyawcesylu : entity work.rrpfhrd
    port map (aiowlrlq => nahwwjmto, igd => yegpdvz, xtefoj => winyf, h => nspawk);
  l : entity work.rrpfhrd
    port map (aiowlrlq => nahwwjmto, igd => cyppxdtjur, xtefoj => winyf, h => kbygjijhr);
  wjjy : entity work.rrpfhrd
    port map (aiowlrlq => nahwwjmto, igd => bjpitg, xtefoj => obabuvzagh, h => avwkjliix);
  
  -- Single-driven assignments
  nahwwjmto <= 16#C#;
  
  -- Multi-driven assignments
  yegpdvz <= ('U', 'W');
  winyf <= "";
  obabuvzagh <= (others => '0');
  winyf <= winyf;
end cbqyc;



-- Seed after: 11048225451327936183,4122021602305298647
