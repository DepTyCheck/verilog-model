-- Seed: 8569113884822948097,1630680796402093529

library ieee;
use ieee.std_logic_1164.all;

entity nu is
  port (uapwhgff : in std_logic_vector(4 downto 2); mnbfsitra : buffer time; blsbp : inout time);
end nu;



architecture jkmlyva of nu is
  
begin
  
end jkmlyva;

library ieee;
use ieee.std_logic_1164.all;

entity iuwm is
  port (hntoeyk : in severity_level; m : inout time; fntkof : buffer std_logic; lc : out integer);
end iuwm;

library ieee;
use ieee.std_logic_1164.all;

architecture x of iuwm is
  signal hax : time;
  signal b : time;
  signal zeqzuc : time;
  signal f : time;
  signal oauw : time;
  signal yhcck : std_logic_vector(4 downto 2);
  signal ahqnwdk : time;
  signal bmenkjvy : time;
  signal puble : std_logic_vector(4 downto 2);
begin
  bphyx : entity work.nu
    port map (uapwhgff => puble, mnbfsitra => bmenkjvy, blsbp => ahqnwdk);
  dnjrmzb : entity work.nu
    port map (uapwhgff => yhcck, mnbfsitra => oauw, blsbp => f);
  myhnjeh : entity work.nu
    port map (uapwhgff => puble, mnbfsitra => zeqzuc, blsbp => b);
  akjou : entity work.nu
    port map (uapwhgff => puble, mnbfsitra => hax, blsbp => m);
end x;



entity t is
  port (mjihe : inout boolean; xmqs : in integer);
end t;

library ieee;
use ieee.std_logic_1164.all;

architecture qwil of t is
  signal cwtalxtud : time;
  signal pqlgbopyk : time;
  signal pnktgko : std_logic_vector(4 downto 2);
  signal hvpx : time;
  signal k : time;
  signal xia : time;
  signal tzd : time;
  signal ez : time;
  signal wbzcm : time;
  signal nmeg : std_logic_vector(4 downto 2);
begin
  oqutqj : entity work.nu
    port map (uapwhgff => nmeg, mnbfsitra => wbzcm, blsbp => ez);
  ugh : entity work.nu
    port map (uapwhgff => nmeg, mnbfsitra => tzd, blsbp => xia);
  oaghjqxw : entity work.nu
    port map (uapwhgff => nmeg, mnbfsitra => k, blsbp => hvpx);
  xzwmisege : entity work.nu
    port map (uapwhgff => pnktgko, mnbfsitra => pqlgbopyk, blsbp => cwtalxtud);
end qwil;



entity ethlqsdfx is
  port (vbzeime : buffer integer);
end ethlqsdfx;

library ieee;
use ieee.std_logic_1164.all;

architecture aiqppacvsf of ethlqsdfx is
  signal raoe : time;
  signal abwggjosh : time;
  signal kahz : std_logic_vector(4 downto 2);
  signal bxexh : time;
  signal gysus : time;
  signal kuefviio : std_logic_vector(4 downto 2);
  signal csfkhobqpr : integer;
  signal fsqtlvplal : boolean;
  signal qgxcklxmad : boolean;
begin
  luau : entity work.t
    port map (mjihe => qgxcklxmad, xmqs => vbzeime);
  j : entity work.t
    port map (mjihe => fsqtlvplal, xmqs => csfkhobqpr);
  pjwbh : entity work.nu
    port map (uapwhgff => kuefviio, mnbfsitra => gysus, blsbp => bxexh);
  d : entity work.nu
    port map (uapwhgff => kahz, mnbfsitra => abwggjosh, blsbp => raoe);
end aiqppacvsf;



-- Seed after: 11537443473364260800,1630680796402093529
