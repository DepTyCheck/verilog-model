-- Seed: 18269787294175683338,10754487200446211253

library ieee;
use ieee.std_logic_1164.all;

entity ancb is
  port (owsxuok : buffer real; ajo : in std_logic_vector(1 to 0); ripxeluixg : buffer time_vector(1 downto 4); uhbkmgigw : in character);
end ancb;

architecture fcerviaa of ancb is
  
begin
  -- Single-driven assignments
  ripxeluixg <= ripxeluixg;
  owsxuok <= owsxuok;
end fcerviaa;

entity mnqfyfvhb is
  port (tpftodzc : buffer integer_vector(2 to 1); sue : out time);
end mnqfyfvhb;

library ieee;
use ieee.std_logic_1164.all;

architecture jyoaxeiz of mnqfyfvhb is
  signal ldhvllgl : character;
  signal lopwbn : time_vector(1 downto 4);
  signal fp : std_logic_vector(1 to 0);
  signal crvukr : real;
begin
  ksqnc : entity work.ancb
    port map (owsxuok => crvukr, ajo => fp, ripxeluixg => lopwbn, uhbkmgigw => ldhvllgl);
  
  -- Single-driven assignments
  ldhvllgl <= ldhvllgl;
  sue <= sue;
  tpftodzc <= (others => 0);
  
  -- Multi-driven assignments
  fp <= "";
  fp <= "";
  fp <= fp;
  fp <= (others => '0');
end jyoaxeiz;

entity wippnsvsc is
  port (jxn : inout integer);
end wippnsvsc;

library ieee;
use ieee.std_logic_1164.all;

architecture eqk of wippnsvsc is
  signal jgnc : character;
  signal w : time_vector(1 downto 4);
  signal gyrgep : std_logic_vector(1 to 0);
  signal cva : real;
  signal vqvri : time;
  signal e : integer_vector(2 to 1);
  signal ptqldlz : character;
  signal ltsyazcsm : time_vector(1 downto 4);
  signal ubtlcjij : std_logic_vector(1 to 0);
  signal jeyst : real;
begin
  luxha : entity work.ancb
    port map (owsxuok => jeyst, ajo => ubtlcjij, ripxeluixg => ltsyazcsm, uhbkmgigw => ptqldlz);
  tgkrxgxu : entity work.mnqfyfvhb
    port map (tpftodzc => e, sue => vqvri);
  f : entity work.ancb
    port map (owsxuok => cva, ajo => gyrgep, ripxeluixg => w, uhbkmgigw => jgnc);
  
  -- Single-driven assignments
  jxn <= 8#73335#;
  jgnc <= ptqldlz;
  ptqldlz <= 'w';
  
  -- Multi-driven assignments
  gyrgep <= "";
end eqk;

library ieee;
use ieee.std_logic_1164.all;

entity hvuwrlx is
  port (xa : in std_logic; mgilu : inout std_logic_vector(0 downto 4); p : out real_vector(2 downto 1); ro : out time);
end hvuwrlx;

library ieee;
use ieee.std_logic_1164.all;

architecture xjcapfgc of hvuwrlx is
  signal aozzpfsx : time;
  signal xxsrrntlj : integer_vector(2 to 1);
  signal vbvwikcq : time_vector(1 downto 4);
  signal dtwl : std_logic_vector(1 to 0);
  signal g : real;
  signal xjlay : character;
  signal dxwsrnfso : time_vector(1 downto 4);
  signal ktfbryzwls : std_logic_vector(1 to 0);
  signal mbdiiydin : real;
  signal ufq : integer;
begin
  umhzzprov : entity work.wippnsvsc
    port map (jxn => ufq);
  jshk : entity work.ancb
    port map (owsxuok => mbdiiydin, ajo => ktfbryzwls, ripxeluixg => dxwsrnfso, uhbkmgigw => xjlay);
  tbcp : entity work.ancb
    port map (owsxuok => g, ajo => dtwl, ripxeluixg => vbvwikcq, uhbkmgigw => xjlay);
  ozpt : entity work.mnqfyfvhb
    port map (tpftodzc => xxsrrntlj, sue => aozzpfsx);
  
  -- Single-driven assignments
  ro <= ro;
  p <= (2_4_4.402, 8#7.4_4#);
  
  -- Multi-driven assignments
  dtwl <= "";
  dtwl <= "";
  mgilu <= (others => '0');
  mgilu <= mgilu;
end xjcapfgc;



-- Seed after: 9318178809790674112,10754487200446211253
