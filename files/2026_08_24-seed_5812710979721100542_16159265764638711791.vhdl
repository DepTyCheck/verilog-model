-- Seed: 5812710979721100542,16159265764638711791

entity wyfwtlru is
  port (kpfz : inout bit; qodc : inout time_vector(1 downto 1); uj : linkage integer);
end wyfwtlru;

architecture vbxunk of wyfwtlru is
  
begin
  -- Single-driven assignments
  qodc <= (others => 2#11001# us);
  kpfz <= kpfz;
end vbxunk;

library ieee;
use ieee.std_logic_1164.all;

entity gjgikm is
  port (vr : linkage real; rezhey : out std_logic; o : inout time_vector(2 to 2));
end gjgikm;

architecture tefaaqte of gjgikm is
  signal auivle : integer;
  signal pxcezpya : time_vector(1 downto 1);
  signal nkfyfx : bit;
  signal zi : integer;
  signal q : bit;
  signal krm : integer;
  signal qltoeernd : time_vector(1 downto 1);
  signal hfw : bit;
begin
  toc : entity work.wyfwtlru
    port map (kpfz => hfw, qodc => qltoeernd, uj => krm);
  gipzhc : entity work.wyfwtlru
    port map (kpfz => q, qodc => o, uj => zi);
  saioifdzrj : entity work.wyfwtlru
    port map (kpfz => nkfyfx, qodc => pxcezpya, uj => auivle);
  
  -- Multi-driven assignments
  rezhey <= 'Z';
  rezhey <= rezhey;
end tefaaqte;



-- Seed after: 14152673723726550022,16159265764638711791
