-- Seed: 3279589128060396911,13592003931158285879

library ieee;
use ieee.std_logic_1164.all;

entity vrgj is
  port (ywetpcg : linkage std_logic_vector(4 to 1); kw : inout std_logic; wbisyalmm : inout time);
end vrgj;

architecture xa of vrgj is
  
begin
  -- Single-driven assignments
  wbisyalmm <= 8#5# ns;
  
  -- Multi-driven assignments
  kw <= 'H';
  kw <= '-';
  kw <= 'U';
  kw <= kw;
end xa;

library ieee;
use ieee.std_logic_1164.all;

entity vritgkkra is
  port (ddibas : buffer character; wxul : out std_logic);
end vritgkkra;

library ieee;
use ieee.std_logic_1164.all;

architecture qh of vritgkkra is
  signal guu : time;
  signal v : std_logic;
  signal vdpwaphv : std_logic_vector(4 to 1);
  signal vdhdypkjf : time;
  signal rjkoyzxn : std_logic_vector(4 to 1);
  signal qpav : time;
  signal tcxozkcl : std_logic;
  signal apg : std_logic_vector(4 to 1);
begin
  xl : entity work.vrgj
    port map (ywetpcg => apg, kw => tcxozkcl, wbisyalmm => qpav);
  ajxb : entity work.vrgj
    port map (ywetpcg => rjkoyzxn, kw => tcxozkcl, wbisyalmm => vdhdypkjf);
  fbyktcl : entity work.vrgj
    port map (ywetpcg => vdpwaphv, kw => v, wbisyalmm => guu);
  
  -- Single-driven assignments
  ddibas <= ddibas;
end qh;

library ieee;
use ieee.std_logic_1164.all;

entity enc is
  port (eiovti : linkage std_logic_vector(1 downto 3); dmydlvf : out std_logic; krhz : buffer integer);
end enc;

library ieee;
use ieee.std_logic_1164.all;

architecture l of enc is
  signal ewpcxovavh : time;
  signal ph : std_logic_vector(4 to 1);
  signal pivpwrfs : time;
  signal fpjbitce : std_logic_vector(4 to 1);
  signal dav : character;
begin
  wryxjreyt : entity work.vritgkkra
    port map (ddibas => dav, wxul => dmydlvf);
  ch : entity work.vrgj
    port map (ywetpcg => fpjbitce, kw => dmydlvf, wbisyalmm => pivpwrfs);
  lj : entity work.vrgj
    port map (ywetpcg => ph, kw => dmydlvf, wbisyalmm => ewpcxovavh);
  
  -- Multi-driven assignments
  dmydlvf <= dmydlvf;
  dmydlvf <= dmydlvf;
  dmydlvf <= 'W';
end l;

library ieee;
use ieee.std_logic_1164.all;

entity urep is
  port (yq : inout std_logic_vector(3 to 4); yqtgbb : inout time; yf : buffer std_logic; mxr : inout std_logic);
end urep;

architecture dgvr of urep is
  
begin
  -- Single-driven assignments
  yqtgbb <= yqtgbb;
  
  -- Multi-driven assignments
  mxr <= mxr;
  mxr <= 'W';
  yq <= "X0";
end dgvr;



-- Seed after: 18052279896674503997,13592003931158285879
