-- Seed: 14630264676761192595,2338584220606314193

library ieee;
use ieee.std_logic_1164.all;

entity boc is
  port (ilbpibu : buffer std_logic_vector(1 to 1); trguppfh : linkage real; a : linkage std_logic; nhxlbcip : in std_logic);
end boc;

architecture fcgunvvl of boc is
  
begin
  -- Multi-driven assignments
  ilbpibu <= ilbpibu;
  ilbpibu <= ilbpibu;
  ilbpibu <= ilbpibu;
end fcgunvvl;

entity v is
  port (mzp : linkage character);
end v;

library ieee;
use ieee.std_logic_1164.all;

architecture dyncml of v is
  signal d : std_logic;
  signal sjpnjoi : real;
  signal gfdrxsl : std_logic;
  signal xlj : std_logic;
  signal sddmwzqhh : real;
  signal ccdhhsy : std_logic;
  signal nlah : real;
  signal fziupj : std_logic;
  signal w : std_logic;
  signal pqlurf : real;
  signal usozqevbmk : std_logic_vector(1 to 1);
begin
  c : entity work.boc
    port map (ilbpibu => usozqevbmk, trguppfh => pqlurf, a => w, nhxlbcip => fziupj);
  wiwusmppl : entity work.boc
    port map (ilbpibu => usozqevbmk, trguppfh => nlah, a => ccdhhsy, nhxlbcip => fziupj);
  fjy : entity work.boc
    port map (ilbpibu => usozqevbmk, trguppfh => sddmwzqhh, a => xlj, nhxlbcip => gfdrxsl);
  arrolhj : entity work.boc
    port map (ilbpibu => usozqevbmk, trguppfh => sjpnjoi, a => d, nhxlbcip => w);
end dyncml;

library ieee;
use ieee.std_logic_1164.all;

entity nvimgzcjui is
  port (g : inout std_logic_vector(0 downto 2); c : linkage real);
end nvimgzcjui;

library ieee;
use ieee.std_logic_1164.all;

architecture r of nvimgzcjui is
  signal jwyzxh : std_logic;
  signal xzku : std_logic;
  signal h : real;
  signal bqbzvsx : std_logic_vector(1 to 1);
  signal haqhcj : character;
begin
  e : entity work.v
    port map (mzp => haqhcj);
  rpmhlul : entity work.boc
    port map (ilbpibu => bqbzvsx, trguppfh => h, a => xzku, nhxlbcip => jwyzxh);
  
  -- Multi-driven assignments
  bqbzvsx <= bqbzvsx;
  bqbzvsx <= bqbzvsx;
  xzku <= 'H';
  g <= "";
end r;

library ieee;
use ieee.std_logic_1164.all;

entity m is
  port (t : linkage string(1 to 3); kycfxlons : in std_logic);
end m;

library ieee;
use ieee.std_logic_1164.all;

architecture aa of m is
  signal fos : std_logic;
  signal jxcjqok : real;
  signal ejij : std_logic_vector(1 to 1);
begin
  yonrhie : entity work.boc
    port map (ilbpibu => ejij, trguppfh => jxcjqok, a => kycfxlons, nhxlbcip => fos);
  
  -- Multi-driven assignments
  ejij <= ejij;
  fos <= fos;
  ejij <= ejij;
end aa;



-- Seed after: 12843613575285726099,2338584220606314193
