-- Seed: 16363003172822590923,8412319452373742525

library ieee;
use ieee.std_logic_1164.all;

entity w is
  port (dnurp : inout std_logic; i : in real_vector(2 to 0); fdast : linkage bit);
end w;

architecture hogpo of w is
  
begin
  -- Multi-driven assignments
  dnurp <= dnurp;
end hogpo;

entity okotwi is
  port (gajjltj : buffer bit; yz : linkage severity_level; vzjadkyp : inout time; vh : out time);
end okotwi;

library ieee;
use ieee.std_logic_1164.all;

architecture ya of okotwi is
  signal krs : std_logic;
  signal cdixb : bit;
  signal silrjmfb : std_logic;
  signal gksapvdoj : bit;
  signal t : real_vector(2 to 0);
  signal yqidzi : std_logic;
  signal btbjibluz : bit;
  signal v : real_vector(2 to 0);
  signal cf : std_logic;
begin
  smldoehi : entity work.w
    port map (dnurp => cf, i => v, fdast => btbjibluz);
  uq : entity work.w
    port map (dnurp => yqidzi, i => t, fdast => gksapvdoj);
  xezjbqe : entity work.w
    port map (dnurp => silrjmfb, i => v, fdast => cdixb);
  uowuuvxt : entity work.w
    port map (dnurp => krs, i => v, fdast => gajjltj);
  
  -- Single-driven assignments
  vh <= vh;
  vzjadkyp <= vh;
end ya;



-- Seed after: 9462489493566740443,8412319452373742525
