-- Seed: 16525565865046494401,13196211255131729027

library ieee;
use ieee.std_logic_1164.all;

entity lvuf is
  port (luxfsm : buffer std_logic; au : in std_logic);
end lvuf;

architecture cag of lvuf is
  
begin
  
end cag;

library ieee;
use ieee.std_logic_1164.all;

entity al is
  port (v : buffer std_logic_vector(4 downto 2));
end al;

library ieee;
use ieee.std_logic_1164.all;

architecture heiyoqxa of al is
  signal rpswn : std_logic;
  signal dc : std_logic;
  signal cyra : std_logic;
  signal gojhovwx : std_logic;
begin
  hzwlepl : entity work.lvuf
    port map (luxfsm => gojhovwx, au => gojhovwx);
  etc : entity work.lvuf
    port map (luxfsm => gojhovwx, au => cyra);
  vkupmhegn : entity work.lvuf
    port map (luxfsm => dc, au => dc);
  gysrbtyh : entity work.lvuf
    port map (luxfsm => rpswn, au => gojhovwx);
  
  -- Multi-driven assignments
  cyra <= 'U';
  v <= v;
  rpswn <= 'H';
end heiyoqxa;

library ieee;
use ieee.std_logic_1164.all;

entity cokpf is
  port (bxnlga : in std_logic; lmcgiarvjr : in integer; yr : out std_logic);
end cokpf;

architecture lmr of cokpf is
  
begin
  -- Multi-driven assignments
  yr <= 'W';
  yr <= bxnlga;
  yr <= yr;
  yr <= 'X';
end lmr;



-- Seed after: 18021759690373037855,13196211255131729027
