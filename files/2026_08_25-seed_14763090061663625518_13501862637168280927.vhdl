-- Seed: 14763090061663625518,13501862637168280927

entity u is
  port (yd : in real_vector(1 to 4); ikqoxci : inout boolean);
end u;

architecture tpzkhadd of u is
  
begin
  -- Single-driven assignments
  ikqoxci <= ikqoxci;
end tpzkhadd;

library ieee;
use ieee.std_logic_1164.all;

entity y is
  port (l : out std_logic; kevxh : linkage integer_vector(1 to 3));
end y;

architecture w of y is
  
begin
  
end w;

entity txrgopl is
  port (cyy : linkage bit; nkhlqgr : in real; gcks : out boolean; nmkcptr : buffer time_vector(2 to 0));
end txrgopl;

library ieee;
use ieee.std_logic_1164.all;

architecture fyadno of txrgopl is
  signal bjtbtsk : boolean;
  signal curdkchd : real_vector(1 to 4);
  signal dnmy : boolean;
  signal vyomzmja : integer_vector(1 to 3);
  signal wkpkvsrvd : std_logic;
  signal g : boolean;
  signal d : real_vector(1 to 4);
begin
  rpppkxoebv : entity work.u
    port map (yd => d, ikqoxci => g);
  nye : entity work.y
    port map (l => wkpkvsrvd, kevxh => vyomzmja);
  drbhykipgp : entity work.u
    port map (yd => d, ikqoxci => dnmy);
  lmwf : entity work.u
    port map (yd => curdkchd, ikqoxci => bjtbtsk);
  
  -- Single-driven assignments
  nmkcptr <= (others => 0 ns);
  
  -- Multi-driven assignments
  wkpkvsrvd <= '-';
  wkpkvsrvd <= '0';
  wkpkvsrvd <= 'Z';
end fyadno;



-- Seed after: 15504260776167342390,13501862637168280927
