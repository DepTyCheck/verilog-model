-- Seed: 5553856054721803674,5415160250146859793

library ieee;
use ieee.std_logic_1164.all;

entity bwo is
  port (tmegmvovw : linkage integer; fuxqle : linkage std_logic; y : out time_vector(0 to 0); jyerqsrpc : buffer integer);
end bwo;



architecture nircwgmffh of bwo is
  
begin
  
end nircwgmffh;



entity ocu is
  port (vpnnf : linkage real; ouqzudwv : in bit_vector(4 downto 3); xuimc : in real; ipuax : in real);
end ocu;

library ieee;
use ieee.std_logic_1164.all;

architecture jejflmovpc of ocu is
  signal yak : integer;
  signal lvqpflppu : time_vector(0 to 0);
  signal t : std_logic;
  signal f : time_vector(0 to 0);
  signal yoprqvs : integer;
  signal b : time_vector(0 to 0);
  signal w : std_logic;
  signal otjdxydc : integer;
  signal a : time_vector(0 to 0);
  signal ksefs : std_logic;
  signal ffkdcln : integer;
begin
  gkf : entity work.bwo
    port map (tmegmvovw => ffkdcln, fuxqle => ksefs, y => a, jyerqsrpc => ffkdcln);
  plbdnsvzie : entity work.bwo
    port map (tmegmvovw => otjdxydc, fuxqle => w, y => b, jyerqsrpc => otjdxydc);
  ezisg : entity work.bwo
    port map (tmegmvovw => yoprqvs, fuxqle => ksefs, y => f, jyerqsrpc => yoprqvs);
  v : entity work.bwo
    port map (tmegmvovw => yoprqvs, fuxqle => t, y => lvqpflppu, jyerqsrpc => yak);
end jejflmovpc;



-- Seed after: 11485146503437007977,5415160250146859793
