-- Seed: 1392772616814836840,12143220691580258643

library ieee;
use ieee.std_logic_1164.all;

entity wjxauvy is
  port (z : out time; jldku : out std_logic_vector(1 to 1); kyvbcjhfhm : inout real; uhghtcqlv : inout real);
end wjxauvy;

architecture eiigcwbo of wjxauvy is
  
begin
  -- Single-driven assignments
  uhghtcqlv <= kyvbcjhfhm;
  kyvbcjhfhm <= kyvbcjhfhm;
  
  -- Multi-driven assignments
  jldku <= "-";
  jldku <= jldku;
  jldku <= jldku;
  jldku <= "W";
end eiigcwbo;

library ieee;
use ieee.std_logic_1164.all;

entity azkewr is
  port (px : out time; tocaset : in std_logic_vector(1 to 4); rxrlutl : out std_logic);
end azkewr;

library ieee;
use ieee.std_logic_1164.all;

architecture owdztxdj of azkewr is
  signal pkoesdoyw : real;
  signal atmnkgtzkw : real;
  signal xjcxaskss : time;
  signal ahtlajp : real;
  signal kq : real;
  signal vfqeom : real;
  signal ozw : real;
  signal dank : std_logic_vector(1 to 1);
  signal cmrixgo : time;
begin
  ral : entity work.wjxauvy
    port map (z => cmrixgo, jldku => dank, kyvbcjhfhm => ozw, uhghtcqlv => vfqeom);
  ym : entity work.wjxauvy
    port map (z => px, jldku => dank, kyvbcjhfhm => kq, uhghtcqlv => ahtlajp);
  pn : entity work.wjxauvy
    port map (z => xjcxaskss, jldku => dank, kyvbcjhfhm => atmnkgtzkw, uhghtcqlv => pkoesdoyw);
  
  -- Multi-driven assignments
  rxrlutl <= rxrlutl;
end owdztxdj;

entity tnionx is
  port (cvuwen : inout integer; gs : buffer integer; xsra : buffer integer);
end tnionx;

library ieee;
use ieee.std_logic_1164.all;

architecture widqfx of tnionx is
  signal sceqg : real;
  signal qdptgsl : real;
  signal yfygb : std_logic_vector(1 to 1);
  signal ngixpc : time;
  signal q : real;
  signal gabs : real;
  signal nynrutzno : std_logic_vector(1 to 1);
  signal viojsydaq : time;
  signal eyb : real;
  signal zphbay : real;
  signal cko : std_logic_vector(1 to 1);
  signal pq : time;
  signal n : real;
  signal lw : real;
  signal awgs : std_logic_vector(1 to 1);
  signal fz : time;
begin
  wfzcop : entity work.wjxauvy
    port map (z => fz, jldku => awgs, kyvbcjhfhm => lw, uhghtcqlv => n);
  rdwidmnzio : entity work.wjxauvy
    port map (z => pq, jldku => cko, kyvbcjhfhm => zphbay, uhghtcqlv => eyb);
  i : entity work.wjxauvy
    port map (z => viojsydaq, jldku => nynrutzno, kyvbcjhfhm => gabs, uhghtcqlv => q);
  jlb : entity work.wjxauvy
    port map (z => ngixpc, jldku => yfygb, kyvbcjhfhm => qdptgsl, uhghtcqlv => sceqg);
  
  -- Single-driven assignments
  xsra <= 21222;
  gs <= 3;
  
  -- Multi-driven assignments
  awgs <= "H";
  awgs <= (others => 'H');
  yfygb <= awgs;
end widqfx;



-- Seed after: 14894807701684642578,12143220691580258643
