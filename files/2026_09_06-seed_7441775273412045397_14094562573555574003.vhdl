-- Seed: 7441775273412045397,14094562573555574003

entity hl is
  port (ifkpd : inout real);
end hl;

architecture fsprhlyms of hl is
  
begin
  -- Single-driven assignments
  ifkpd <= ifkpd;
end fsprhlyms;

entity et is
  port (opl : inout time; hljxgcbczd : inout integer);
end et;

architecture oepszzxssp of et is
  signal xzx : real;
  signal mnnx : real;
begin
  i : entity work.hl
    port map (ifkpd => mnnx);
  ivukysi : entity work.hl
    port map (ifkpd => xzx);
  
  -- Single-driven assignments
  hljxgcbczd <= 2;
end oepszzxssp;

entity itmigg is
  port (fmwf : out real; unagclpj : out real; r : out real; upre : in time);
end itmigg;

architecture kzyiea of itmigg is
  signal e : real;
  signal hypd : real;
  signal hj : integer;
  signal qnyaxlma : time;
begin
  gkrcbkautm : entity work.et
    port map (opl => qnyaxlma, hljxgcbczd => hj);
  i : entity work.hl
    port map (ifkpd => hypd);
  cdmomsnl : entity work.hl
    port map (ifkpd => e);
end kzyiea;

library ieee;
use ieee.std_logic_1164.all;

entity crowav is
  port (iwdv : buffer std_logic_vector(1 downto 2); xe : linkage time; vpv : linkage real_vector(3 downto 4));
end crowav;

architecture xekfdulb of crowav is
  signal dpwltbgvuc : real;
  signal hswktpnb : real;
  signal qhw : real;
begin
  zoj : entity work.hl
    port map (ifkpd => qhw);
  v : entity work.hl
    port map (ifkpd => hswktpnb);
  xidyneqbcx : entity work.hl
    port map (ifkpd => dpwltbgvuc);
  
  -- Multi-driven assignments
  iwdv <= iwdv;
end xekfdulb;



-- Seed after: 95279160012286842,14094562573555574003
