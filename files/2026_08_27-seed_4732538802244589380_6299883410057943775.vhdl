-- Seed: 4732538802244589380,6299883410057943775

entity u is
  port (vl : in integer; wtkqyre : in integer_vector(4 downto 2));
end u;

architecture f of u is
  
begin
  
end f;

library ieee;
use ieee.std_logic_1164.all;

entity hgmo is
  port (kwysnwnix : out std_logic_vector(1 downto 0));
end hgmo;

architecture iariiemoy of hgmo is
  signal zty : integer_vector(4 downto 2);
  signal amcomjmbyv : integer;
begin
  eb : entity work.u
    port map (vl => amcomjmbyv, wtkqyre => zty);
  
  -- Single-driven assignments
  amcomjmbyv <= 03;
  
  -- Multi-driven assignments
  kwysnwnix <= kwysnwnix;
  kwysnwnix <= "1W";
  kwysnwnix <= kwysnwnix;
  kwysnwnix <= "UU";
end iariiemoy;

entity skurmhp is
  port (e : in time; uskcx : buffer integer);
end skurmhp;

architecture uqhftx of skurmhp is
  signal owwoei : integer_vector(4 downto 2);
  signal sgflpyl : integer_vector(4 downto 2);
  signal hholzzh : integer;
  signal tyf : integer_vector(4 downto 2);
  signal pf : integer;
begin
  yy : entity work.u
    port map (vl => pf, wtkqyre => tyf);
  dyni : entity work.u
    port map (vl => hholzzh, wtkqyre => sgflpyl);
  zr : entity work.u
    port map (vl => pf, wtkqyre => owwoei);
  
  -- Single-driven assignments
  uskcx <= 8#3_7#;
  tyf <= tyf;
  pf <= 16#0#;
  sgflpyl <= sgflpyl;
  hholzzh <= uskcx;
end uqhftx;



-- Seed after: 3455472958151806226,6299883410057943775
