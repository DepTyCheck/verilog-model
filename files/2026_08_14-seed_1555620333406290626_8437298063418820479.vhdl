-- Seed: 1555620333406290626,8437298063418820479

library ieee;
use ieee.std_logic_1164.all;

entity wnqnqoy is
  port (pc : inout real; sy : buffer std_logic_vector(2 to 3); pmla : inout integer);
end wnqnqoy;

architecture jbq of wnqnqoy is
  
begin
  -- Single-driven assignments
  pmla <= pmla;
  pc <= pc;
  
  -- Multi-driven assignments
  sy <= sy;
  sy <= sy;
  sy <= sy;
  sy <= sy;
end jbq;

entity gvfsoopk is
  port (onesrhz : out boolean_vector(0 downto 0));
end gvfsoopk;

library ieee;
use ieee.std_logic_1164.all;

architecture equ of gvfsoopk is
  signal hdxikym : integer;
  signal xaw : real;
  signal umykbiv : integer;
  signal z : std_logic_vector(2 to 3);
  signal ymvten : real;
  signal wfhpa : integer;
  signal uw : std_logic_vector(2 to 3);
  signal iouxcsk : real;
begin
  qlyqqqqahi : entity work.wnqnqoy
    port map (pc => iouxcsk, sy => uw, pmla => wfhpa);
  vvo : entity work.wnqnqoy
    port map (pc => ymvten, sy => z, pmla => umykbiv);
  junjqvibri : entity work.wnqnqoy
    port map (pc => xaw, sy => uw, pmla => hdxikym);
  
  -- Single-driven assignments
  onesrhz <= onesrhz;
  
  -- Multi-driven assignments
  uw <= "XZ";
  uw <= uw;
  uw <= "UU";
  uw <= ('W', '-');
end equ;



-- Seed after: 1514092249004664804,8437298063418820479
