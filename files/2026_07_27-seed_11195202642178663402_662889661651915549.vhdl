-- Seed: 11195202642178663402,662889661651915549

library ieee;
use ieee.std_logic_1164.all;

entity mgfmh is
  port (rtl : out std_logic; vzeiei : linkage boolean; iicuy : inout integer_vector(0 downto 4));
end mgfmh;

architecture bq of mgfmh is
  
begin
  -- Single-driven assignments
  iicuy <= iicuy;
  
  -- Multi-driven assignments
  rtl <= 'Z';
  rtl <= rtl;
  rtl <= 'W';
  rtl <= 'W';
end bq;

entity od is
  port (fbjooxuxt : linkage real; xxpd : in integer);
end od;

library ieee;
use ieee.std_logic_1164.all;

architecture ipknhgkap of od is
  signal q : integer_vector(0 downto 4);
  signal zshlpgxpi : boolean;
  signal yhjmmdhw : std_logic;
  signal wnx : integer_vector(0 downto 4);
  signal lzkb : boolean;
  signal qsrb : integer_vector(0 downto 4);
  signal vqsdvoy : boolean;
  signal rju : integer_vector(0 downto 4);
  signal jnwbjuu : boolean;
  signal cxnyan : std_logic;
begin
  g : entity work.mgfmh
    port map (rtl => cxnyan, vzeiei => jnwbjuu, iicuy => rju);
  jo : entity work.mgfmh
    port map (rtl => cxnyan, vzeiei => vqsdvoy, iicuy => qsrb);
  udcakvgqo : entity work.mgfmh
    port map (rtl => cxnyan, vzeiei => lzkb, iicuy => wnx);
  pdxix : entity work.mgfmh
    port map (rtl => yhjmmdhw, vzeiei => zshlpgxpi, iicuy => q);
end ipknhgkap;



-- Seed after: 5527858884444194816,662889661651915549
