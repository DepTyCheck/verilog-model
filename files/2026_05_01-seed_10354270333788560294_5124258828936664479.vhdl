-- Seed: 10354270333788560294,5124258828936664479



entity azfjdpxb is
  port (bqkuzkmkib : out real; vf : inout real; xlhdykhj : out real);
end azfjdpxb;



architecture pe of azfjdpxb is
  
begin
  
end pe;

library ieee;
use ieee.std_logic_1164.all;

entity e is
  port (txfb : out std_logic; sa : in real; ur : in time);
end e;



architecture dvk of e is
  
begin
  
end dvk;

library ieee;
use ieee.std_logic_1164.all;

entity jtgxexsccq is
  port (j : out time; c : buffer time; qbipabeyo : in std_logic; rc : out std_logic);
end jtgxexsccq;



architecture co of jtgxexsccq is
  signal njentvey : real;
  signal nxdigdph : real;
  signal jl : real;
  signal md : real;
  signal ekrpvh : real;
  signal cdszmyw : real;
  signal cuytm : real;
  signal soju : real;
  signal njgmdqefq : real;
begin
  cozs : entity work.e
    port map (txfb => rc, sa => njgmdqefq, ur => c);
  rgjtcebm : entity work.azfjdpxb
    port map (bqkuzkmkib => soju, vf => cuytm, xlhdykhj => cdszmyw);
  cifz : entity work.azfjdpxb
    port map (bqkuzkmkib => ekrpvh, vf => md, xlhdykhj => jl);
  eyuuoe : entity work.azfjdpxb
    port map (bqkuzkmkib => nxdigdph, vf => njgmdqefq, xlhdykhj => njentvey);
end co;

library ieee;
use ieee.std_logic_1164.all;

entity sbqskikaug is
  port (qivwxmg : buffer integer; sv : buffer real; xkj : out real; sctqs : out std_logic);
end sbqskikaug;

library ieee;
use ieee.std_logic_1164.all;

architecture rbagks of sbqskikaug is
  signal nzzsk : real;
  signal cefl : real;
  signal ihfwckajsv : std_logic;
  signal riverelrx : time;
  signal ub : time;
  signal nzi : real;
  signal fxqhev : real;
begin
  ojveiup : entity work.azfjdpxb
    port map (bqkuzkmkib => fxqhev, vf => xkj, xlhdykhj => sv);
  jfbrvoqtcp : entity work.e
    port map (txfb => sctqs, sa => nzi, ur => ub);
  scqcfpm : entity work.jtgxexsccq
    port map (j => ub, c => riverelrx, qbipabeyo => sctqs, rc => ihfwckajsv);
  ut : entity work.azfjdpxb
    port map (bqkuzkmkib => cefl, vf => nzzsk, xlhdykhj => nzi);
end rbagks;



-- Seed after: 9055188343459337483,5124258828936664479
