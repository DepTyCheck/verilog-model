-- Seed: 11930889107775814089,13501862637168280927

library ieee;
use ieee.std_logic_1164.all;

entity ppcs is
  port (xet : inout std_logic; wfonrhwwmm : out real; kmcrujub : inout severity_level; jmubk : inout boolean);
end ppcs;

architecture jfybbm of ppcs is
  
begin
  -- Single-driven assignments
  jmubk <= jmubk;
  wfonrhwwmm <= wfonrhwwmm;
  kmcrujub <= kmcrujub;
end jfybbm;

library ieee;
use ieee.std_logic_1164.all;

entity xftrpxuhe is
  port (bcml : buffer integer; uwkjafdd : in std_logic; xg : inout std_logic_vector(1 downto 3); yhlohqwgnj : linkage integer_vector(3 downto 1));
end xftrpxuhe;

library ieee;
use ieee.std_logic_1164.all;

architecture uhhxyi of xftrpxuhe is
  signal xchisiudgi : boolean;
  signal lgpkroti : severity_level;
  signal bsgnvx : real;
  signal fjkigfn : boolean;
  signal dag : severity_level;
  signal nxgbwwq : real;
  signal xcrtedabk : std_logic;
  signal qkzz : boolean;
  signal lwga : severity_level;
  signal sfxox : real;
  signal vulhfqg : std_logic;
  signal alpulip : boolean;
  signal esfwmminca : severity_level;
  signal b : real;
  signal ifx : std_logic;
begin
  ejcpcopb : entity work.ppcs
    port map (xet => ifx, wfonrhwwmm => b, kmcrujub => esfwmminca, jmubk => alpulip);
  enkxpbuk : entity work.ppcs
    port map (xet => vulhfqg, wfonrhwwmm => sfxox, kmcrujub => lwga, jmubk => qkzz);
  exoncccjep : entity work.ppcs
    port map (xet => xcrtedabk, wfonrhwwmm => nxgbwwq, kmcrujub => dag, jmubk => fjkigfn);
  iqckrd : entity work.ppcs
    port map (xet => ifx, wfonrhwwmm => bsgnvx, kmcrujub => lgpkroti, jmubk => xchisiudgi);
  
  -- Multi-driven assignments
  xg <= xg;
end uhhxyi;

library ieee;
use ieee.std_logic_1164.all;

entity kvxagofv is
  port (qdmfccpkn : in std_logic_vector(3 to 4); vxbsesytxc : inout boolean_vector(2 to 2); kmukwe : buffer bit);
end kvxagofv;

library ieee;
use ieee.std_logic_1164.all;

architecture zgl of kvxagofv is
  signal qky : boolean;
  signal ntsxnfqgsq : severity_level;
  signal gqhhifeg : real;
  signal oxnwy : boolean;
  signal qxwxvno : severity_level;
  signal inbtg : real;
  signal owip : boolean;
  signal ovip : severity_level;
  signal yoauv : real;
  signal u : std_logic;
  signal rwwav : boolean;
  signal mgxnhndd : severity_level;
  signal zkiwpxrjs : real;
  signal umomts : std_logic;
begin
  xqpmdzz : entity work.ppcs
    port map (xet => umomts, wfonrhwwmm => zkiwpxrjs, kmcrujub => mgxnhndd, jmubk => rwwav);
  eijwhekmu : entity work.ppcs
    port map (xet => u, wfonrhwwmm => yoauv, kmcrujub => ovip, jmubk => owip);
  znwfxdgj : entity work.ppcs
    port map (xet => u, wfonrhwwmm => inbtg, kmcrujub => qxwxvno, jmubk => oxnwy);
  go : entity work.ppcs
    port map (xet => u, wfonrhwwmm => gqhhifeg, kmcrujub => ntsxnfqgsq, jmubk => qky);
  
  -- Multi-driven assignments
  umomts <= umomts;
  u <= umomts;
  umomts <= '0';
  umomts <= umomts;
end zgl;



-- Seed after: 4126623230381150371,13501862637168280927
