-- Seed: 2170744492079441073,1112937151005418631

entity cj is
  port (olmedhxg : linkage character; g : buffer time; gbc : linkage boolean);
end cj;

architecture wufet of cj is
  
begin
  -- Single-driven assignments
  g <= g;
end wufet;

entity hzbn is
  port (vufsffk : buffer integer_vector(2 downto 4));
end hzbn;

architecture g of hzbn is
  signal yarmrxsd : boolean;
  signal yoslizhaqs : time;
  signal hlvdsvaoa : character;
  signal i : boolean;
  signal bcnzwxccxe : time;
  signal tqykb : character;
  signal ghenxjzt : boolean;
  signal yrq : time;
  signal mulg : character;
  signal wpzi : boolean;
  signal ld : time;
  signal eoyoqy : character;
begin
  qcrsfky : entity work.cj
    port map (olmedhxg => eoyoqy, g => ld, gbc => wpzi);
  lka : entity work.cj
    port map (olmedhxg => mulg, g => yrq, gbc => ghenxjzt);
  xvuroqvyh : entity work.cj
    port map (olmedhxg => tqykb, g => bcnzwxccxe, gbc => i);
  p : entity work.cj
    port map (olmedhxg => hlvdsvaoa, g => yoslizhaqs, gbc => yarmrxsd);
end g;



-- Seed after: 10267909057056461951,1112937151005418631
