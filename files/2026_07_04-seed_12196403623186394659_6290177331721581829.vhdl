-- Seed: 12196403623186394659,6290177331721581829

use std.reflection.all;

entity zavut is
  port (p : inout file_value_mirror; gzdds : buffer character; rstrua : out real; omukgvig : inout integer_value_mirror);
end zavut;

architecture arjbuyee of zavut is
  
begin
  -- Single-driven assignments
  rstrua <= rstrua;
  gzdds <= 'l';
end arjbuyee;

use std.reflection.all;

entity xpwz is
  port (kmba : inout protected_subtype_mirror; seuvr : linkage boolean_vector(2 to 4));
end xpwz;

use std.reflection.all;

architecture wdix of xpwz is
  shared variable tedtsgps : integer_value_mirror;
  signal heswib : real;
  signal yytrkssuep : character;
  shared variable junylh : file_value_mirror;
  shared variable buq : integer_value_mirror;
  signal bmmkbhy : real;
  signal e : character;
  shared variable ewhkg : file_value_mirror;
  shared variable vm : integer_value_mirror;
  signal xfqcqxbwnn : real;
  signal htun : character;
  shared variable rfiyno : file_value_mirror;
  shared variable s : integer_value_mirror;
  signal mqfvm : real;
  signal kirq : character;
  shared variable iauiqqr : file_value_mirror;
begin
  mcidijow : entity work.zavut
    port map (p => iauiqqr, gzdds => kirq, rstrua => mqfvm, omukgvig => s);
  dlweerskd : entity work.zavut
    port map (p => rfiyno, gzdds => htun, rstrua => xfqcqxbwnn, omukgvig => vm);
  nclvvesce : entity work.zavut
    port map (p => ewhkg, gzdds => e, rstrua => bmmkbhy, omukgvig => buq);
  ojzdexagmd : entity work.zavut
    port map (p => junylh, gzdds => yytrkssuep, rstrua => heswib, omukgvig => tedtsgps);
end wdix;



-- Seed after: 6148213880689226878,6290177331721581829
