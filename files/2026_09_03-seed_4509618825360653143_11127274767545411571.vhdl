-- Seed: 4509618825360653143,11127274767545411571

entity wj is
  port (mklm : out string(2 to 1));
end wj;

architecture byytjtmyye of wj is
  
begin
  -- Single-driven assignments
  mklm <= mklm;
end byytjtmyye;

entity qcvr is
  port (xn : in severity_level; iniwmpgacu : linkage bit);
end qcvr;

architecture uvt of qcvr is
  signal pvnjs : string(2 to 1);
  signal uavdjnn : string(2 to 1);
  signal jjmnkvp : string(2 to 1);
  signal p : string(2 to 1);
begin
  fkylgap : entity work.wj
    port map (mklm => p);
  eotkxccskz : entity work.wj
    port map (mklm => jjmnkvp);
  vh : entity work.wj
    port map (mklm => uavdjnn);
  leqezjf : entity work.wj
    port map (mklm => pvnjs);
end uvt;



-- Seed after: 10927414131041173803,11127274767545411571
