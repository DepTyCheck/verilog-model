// Seed: 8498387399752071416,8577527857029547875

module kcwdohr (input shortreal luq, inout trior logic [3:0][1:2] l, output bit [4:0] mlllua);
  
  not cmunodppq(mlllua, sn);
  // warning: implicit conversion of port connection truncates from 5 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [4:0] mlllua -> logic mlllua
  
  nand gcq(sswk, sswk, sn);
  
  xor bnxzs(sswk, mghkoq, sswk);
  
  
  // Single-driven assigns
  
  // Multi-driven assigns
  assign l = l;
  assign sswk = 'bx1;
  assign sn = 'bx;
endmodule: kcwdohr

module bv (input trireg logic k [3:1]);
  
  xor mzsir(ejng, eev, of);
  
  xnor ke(bb, p, ialulw);
  
  kcwdohr ulewmjn(.luq(iqytwjus), .l(bb), .mlllua(adsuq));
  // warning: implicit conversion of port connection expands from 1 to 32 bits
  // warning: implicit conversion changes signedness from unsigned to signed
  //   wire logic iqytwjus -> shortreal luq
  //
  // warning: implicit conversion of port connection expands from 1 to 8 bits
  //   wire logic bb -> trior logic [3:0][1:2] l
  //
  // warning: implicit conversion of port connection expands from 1 to 5 bits
  // warning: implicit conversion changes possible bit states from 4-state to 2-state
  //   wire logic adsuq -> bit [4:0] mlllua
  
  xor ghk(n, bkbhnu, pf);
  
  
  // Single-driven assigns
  
  // Multi-driven assigns
  assign k = k;
  assign pf = bb;
endmodule: bv



// Seed after: 17899399111670710827,8577527857029547875
