-- Seed: 273171500723521646,4080032123900078489

library ieee;
use ieee.std_logic_1164.all;

entity kicpmb is
  port (ywsn : in real; azzyrp : linkage severity_level; mizmauf : inout std_logic; xwh : in std_logic_vector(1 downto 4));
end kicpmb;

architecture tsfao of kicpmb is
  
begin
  -- Multi-driven assignments
  mizmauf <= mizmauf;
  mizmauf <= 'H';
  mizmauf <= mizmauf;
end tsfao;

library ieee;
use ieee.std_logic_1164.all;

entity wlz is
  port ( ifzytqncfu : out std_logic_vector(3 downto 1)
  ; zrfptqbhun : linkage std_logic
  ; fxirlgayh : linkage std_logic_vector(2 downto 1)
  ; wstcpjlwaa : out integer
  );
end wlz;

architecture hfpak of wlz is
  
begin
  -- Single-driven assignments
  wstcpjlwaa <= 1404;
  
  -- Multi-driven assignments
  ifzytqncfu <= ifzytqncfu;
  ifzytqncfu <= ifzytqncfu;
  ifzytqncfu <= "WL1";
  ifzytqncfu <= ifzytqncfu;
end hfpak;

library ieee;
use ieee.std_logic_1164.all;

entity tiu is
  port (sizb : in std_logic_vector(3 downto 0); rifht : in boolean_vector(2 to 0); ucca : buffer real);
end tiu;

library ieee;
use ieee.std_logic_1164.all;

architecture gfrihtpbsw of tiu is
  signal hg : integer;
  signal xlkafey : std_logic_vector(2 downto 1);
  signal lrsqtafqdx : std_logic_vector(3 downto 1);
  signal ndjhs : std_logic_vector(1 downto 4);
  signal mejaja : std_logic;
  signal udpktshnwy : severity_level;
  signal hher : real;
  signal dt : std_logic_vector(1 downto 4);
  signal y : std_logic;
  signal vcdolvrnt : severity_level;
begin
  whbdff : entity work.kicpmb
    port map (ywsn => ucca, azzyrp => vcdolvrnt, mizmauf => y, xwh => dt);
  aqhkwvj : entity work.kicpmb
    port map (ywsn => hher, azzyrp => udpktshnwy, mizmauf => mejaja, xwh => ndjhs);
  jjshgzchr : entity work.wlz
    port map (ifzytqncfu => lrsqtafqdx, zrfptqbhun => y, fxirlgayh => xlkafey, wstcpjlwaa => hg);
  
  -- Single-driven assignments
  ucca <= ucca;
  hher <= ucca;
  
  -- Multi-driven assignments
  y <= y;
  ndjhs <= dt;
  y <= 'Z';
  y <= 'U';
end gfrihtpbsw;



-- Seed after: 10509099194528711437,4080032123900078489
