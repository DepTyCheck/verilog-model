-- Seed: 4331302815479138058,10463297573877745897

entity swspjhbsif is
  port (vfboqaqt : buffer boolean; yqsyjmqd : inout severity_level; eznjju : linkage real);
end swspjhbsif;

architecture hrnflizvzc of swspjhbsif is
  
begin
  
end hrnflizvzc;

entity phnb is
  port (cxguoz : linkage real);
end phnb;

architecture jwfucuuwz of phnb is
  signal m : severity_level;
  signal xc : boolean;
  signal j : real;
  signal jfdtphmzmg : severity_level;
  signal w : boolean;
  signal ummo : real;
  signal u : severity_level;
  signal cfn : boolean;
  signal vrayl : real;
  signal nxd : severity_level;
  signal ca : boolean;
begin
  nfbsbqotm : entity work.swspjhbsif
    port map (vfboqaqt => ca, yqsyjmqd => nxd, eznjju => vrayl);
  ruaeupakmd : entity work.swspjhbsif
    port map (vfboqaqt => cfn, yqsyjmqd => u, eznjju => ummo);
  z : entity work.swspjhbsif
    port map (vfboqaqt => w, yqsyjmqd => jfdtphmzmg, eznjju => j);
  bpqzikf : entity work.swspjhbsif
    port map (vfboqaqt => xc, yqsyjmqd => m, eznjju => cxguoz);
end jwfucuuwz;



-- Seed after: 9743930037584219085,10463297573877745897
