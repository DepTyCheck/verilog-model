-- Seed: 4223615952785843527,7726014785203345639

use std.reflection.all;

entity wb is
  port (xcymft : inout record_value_mirror);
end wb;

architecture qmnujzxki of wb is
  
begin
  
end qmnujzxki;

use std.reflection.all;

entity lnpcl is
  port (ogsyh : inout subtype_mirror; z : inout physical_subtype_mirror; q : inout file_subtype_mirror; qsxdcyckib : in integer);
end lnpcl;

use std.reflection.all;

architecture xcofryey of lnpcl is
  shared variable nbbwld : record_value_mirror;
  shared variable b : record_value_mirror;
  shared variable enoayl : record_value_mirror;
  shared variable hrknhdjcb : record_value_mirror;
begin
  nc : entity work.wb
    port map (xcymft => hrknhdjcb);
  mxdonfhdj : entity work.wb
    port map (xcymft => enoayl);
  curjfbkpfp : entity work.wb
    port map (xcymft => b);
  zb : entity work.wb
    port map (xcymft => nbbwld);
end xcofryey;

library ieee;
use ieee.std_logic_1164.all;

entity j is
  port (uhlby : out real; rznvutoc : in std_logic_vector(0 to 1); utratqt : out real; rerlnqtnj : out time);
end j;

use std.reflection.all;

architecture yj of j is
  shared variable zcakpzbu : record_value_mirror;
begin
  mlboloyca : entity work.wb
    port map (xcymft => zcakpzbu);
  
  -- Single-driven assignments
  rerlnqtnj <= 1 hr;
end yj;



-- Seed after: 3271578141592592466,7726014785203345639
