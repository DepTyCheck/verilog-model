-- Seed: 10766236730614178949,7726014785203345639

use std.reflection.all;

entity rkn is
  port (h : buffer severity_level; e : inout integer_value_mirror);
end rkn;

architecture qls of rkn is
  
begin
  -- Single-driven assignments
  h <= WARNING;
end qls;

library ieee;
use ieee.std_logic_1164.all;

entity ljo is
  port (ht : linkage std_logic_vector(1 downto 3));
end ljo;

use std.reflection.all;

architecture ie of ljo is
  shared variable bndlj : integer_value_mirror;
  signal pebcsn : severity_level;
  shared variable foxcv : integer_value_mirror;
  signal z : severity_level;
  shared variable rbwilokbn : integer_value_mirror;
  signal rhberbqyap : severity_level;
begin
  k : entity work.rkn
    port map (h => rhberbqyap, e => rbwilokbn);
  cjvajjalj : entity work.rkn
    port map (h => z, e => foxcv);
  xzeip : entity work.rkn
    port map (h => pebcsn, e => bndlj);
end ie;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity gzbxymc is
  port (cbxjouaw : in std_logic; hz : inout record_value_mirror; twhozh : out std_logic_vector(3 to 2); pdecj : inout floating_value_mirror);
end gzbxymc;

architecture xmmrugeac of gzbxymc is
  
begin
  -- Multi-driven assignments
  twhozh <= "";
  twhozh <= twhozh;
end xmmrugeac;



-- Seed after: 15574090491349610219,7726014785203345639
