-- Seed: 4067498773256221520,14629254427735353553

entity g is
  port (xdyuzn : inout time; v : buffer integer_vector(3 to 2); zxn : in integer);
end g;

architecture qjunikb of g is
  
begin
  -- Single-driven assignments
  v <= (others => 0);
  xdyuzn <= 20.41 ps;
end qjunikb;

library ieee;
use ieee.std_logic_1164.all;

entity baam is
  port (qd : buffer real; ilcqegcdx : inout std_logic_vector(3 downto 0); cyeqq : inout real; w : in boolean);
end baam;

architecture zzyphvnkx of baam is
  signal gwr : integer_vector(3 to 2);
  signal zkye : time;
  signal ybjvpnnvq : integer_vector(3 to 2);
  signal horjcjib : time;
  signal ym : integer_vector(3 to 2);
  signal gzpzgx : time;
  signal oakaeq : integer;
  signal ylltnkk : integer_vector(3 to 2);
  signal oed : time;
begin
  cwgax : entity work.g
    port map (xdyuzn => oed, v => ylltnkk, zxn => oakaeq);
  gaiihwg : entity work.g
    port map (xdyuzn => gzpzgx, v => ym, zxn => oakaeq);
  bs : entity work.g
    port map (xdyuzn => horjcjib, v => ybjvpnnvq, zxn => oakaeq);
  lsjnom : entity work.g
    port map (xdyuzn => zkye, v => gwr, zxn => oakaeq);
  
  -- Multi-driven assignments
  ilcqegcdx <= "Z-UX";
  ilcqegcdx <= ('L', '1', 'X', 'W');
  ilcqegcdx <= "HZ-L";
end zzyphvnkx;



-- Seed after: 12813103526806105363,14629254427735353553
