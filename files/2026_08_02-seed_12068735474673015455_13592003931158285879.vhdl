-- Seed: 12068735474673015455,13592003931158285879

library ieee;
use ieee.std_logic_1164.all;

entity ld is
  port (mtovrqnlhm : in string(1 downto 2); ncnz : linkage real; zyqq : inout std_logic);
end ld;

architecture wnj of ld is
  
begin
  -- Multi-driven assignments
  zyqq <= zyqq;
  zyqq <= 'W';
  zyqq <= 'W';
  zyqq <= '0';
end wnj;

entity jfy is
  port (ipftibzek : inout time);
end jfy;

library ieee;
use ieee.std_logic_1164.all;

architecture nctqhg of jfy is
  signal gdp : real;
  signal o : std_logic;
  signal kgfxxlje : real;
  signal f : std_logic;
  signal tpz : real;
  signal nxgziizikk : string(1 downto 2);
begin
  rr : entity work.ld
    port map (mtovrqnlhm => nxgziizikk, ncnz => tpz, zyqq => f);
  mbtwibz : entity work.ld
    port map (mtovrqnlhm => nxgziizikk, ncnz => kgfxxlje, zyqq => o);
  ipfd : entity work.ld
    port map (mtovrqnlhm => nxgziizikk, ncnz => gdp, zyqq => f);
  
  -- Single-driven assignments
  ipftibzek <= ipftibzek;
  
  -- Multi-driven assignments
  o <= f;
  o <= '0';
end nctqhg;



-- Seed after: 329706413157388621,13592003931158285879
