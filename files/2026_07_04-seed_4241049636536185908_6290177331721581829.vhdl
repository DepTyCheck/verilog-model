-- Seed: 4241049636536185908,6290177331721581829

use std.reflection.all;

entity orc is
  port (lrvfnzf : inout enumeration_subtype_mirror; za : inout real);
end orc;

architecture we of orc is
  
begin
  -- Single-driven assignments
  za <= 8#2_4_7.0#;
end we;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity bshe is
  port (ro : inout physical_value_mirror; ltsbpazgv : buffer std_logic_vector(4 downto 3); duwzsyr : inout subtype_mirror);
end bshe;

use std.reflection.all;

architecture wxozo of bshe is
  signal eqifmeqc : real;
  shared variable h : enumeration_subtype_mirror;
  signal useglhgyc : real;
  shared variable mvkqdemq : enumeration_subtype_mirror;
  signal tcxwbfe : real;
  shared variable vc : enumeration_subtype_mirror;
  signal wz : real;
  shared variable lhi : enumeration_subtype_mirror;
begin
  ocofddjhh : entity work.orc
    port map (lrvfnzf => lhi, za => wz);
  vsyjk : entity work.orc
    port map (lrvfnzf => vc, za => tcxwbfe);
  vmise : entity work.orc
    port map (lrvfnzf => mvkqdemq, za => useglhgyc);
  htnac : entity work.orc
    port map (lrvfnzf => h, za => eqifmeqc);
end wxozo;

use std.reflection.all;

entity ycq is
  port (f : inout character; vmxgkabfia : inout record_value_mirror; trmfvv : inout enumeration_subtype_mirror; g : inout array_value_mirror);
end ycq;

use std.reflection.all;

architecture pdsn of ycq is
  signal hvjln : real;
  shared variable nzpy : enumeration_subtype_mirror;
begin
  pdgpes : entity work.orc
    port map (lrvfnzf => nzpy, za => hvjln);
end pdsn;



-- Seed after: 7523307899308345608,6290177331721581829
