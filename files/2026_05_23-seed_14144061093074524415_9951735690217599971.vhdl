-- Seed: 14144061093074524415,9951735690217599971



entity onz is
  port (n : inout time; jvtoy : in integer_vector(0 downto 2));
end onz;



architecture sdjvs of onz is
  
begin
  
end sdjvs;

library ieee;
use ieee.std_logic_1164.all;

entity z is
  port (s : buffer real; bthzwtnj : in std_logic_vector(1 to 2); ghde : buffer time);
end z;



architecture qjbe of z is
  signal clb : integer_vector(0 downto 2);
  signal gkgmnucwh : time;
  signal k : integer_vector(0 downto 2);
  signal n : integer_vector(0 downto 2);
  signal ezpwkp : time;
begin
  qyigwdbd : entity work.onz
    port map (n => ezpwkp, jvtoy => n);
  eh : entity work.onz
    port map (n => ghde, jvtoy => k);
  urwsctuge : entity work.onz
    port map (n => gkgmnucwh, jvtoy => clb);
end qjbe;

library ieee;
use ieee.std_logic_1164.all;

entity dwgamr is
  port (ybkneklcf : linkage time; ezve : in std_logic; y : buffer std_logic);
end dwgamr;



architecture xwgir of dwgamr is
  signal kqppebl : integer_vector(0 downto 2);
  signal wtdjz : time;
  signal jhahdmw : integer_vector(0 downto 2);
  signal h : time;
begin
  yd : entity work.onz
    port map (n => h, jvtoy => jhahdmw);
  psfxoac : entity work.onz
    port map (n => wtdjz, jvtoy => kqppebl);
end xwgir;

library ieee;
use ieee.std_logic_1164.all;

entity yicygtum is
  port (ljplsuhn : inout std_logic; ixth : out real_vector(1 to 1); dd : in std_logic_vector(4 downto 0));
end yicygtum;

library ieee;
use ieee.std_logic_1164.all;

architecture ybzk of yicygtum is
  signal mqueyhuuky : time;
  signal m : std_logic_vector(1 to 2);
  signal fmru : real;
  signal jxvpj : time;
  signal k : real;
  signal bafaxu : time;
  signal w : std_logic_vector(1 to 2);
  signal dxqvzl : real;
begin
  i : entity work.z
    port map (s => dxqvzl, bthzwtnj => w, ghde => bafaxu);
  juxwsgrd : entity work.z
    port map (s => k, bthzwtnj => w, ghde => jxvpj);
  ylwm : entity work.z
    port map (s => fmru, bthzwtnj => m, ghde => mqueyhuuky);
end ybzk;



-- Seed after: 661957402679346929,9951735690217599971
