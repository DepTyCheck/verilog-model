-- Seed: 2923397089909238296,14426950258250697445

use std.reflection.all;

entity jatkhptl is
  port (flouk : inout protected_subtype_mirror; rrnpf : inout physical_value_mirror; ndz : inout subtype_mirror; rvotm : buffer time);
end jatkhptl;

architecture iobtme of jatkhptl is
  
begin
  -- Single-driven assignments
  rvotm <= rvotm;
end iobtme;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity wgsuluj is
  port (hig : inout time; klswlapr : linkage std_logic; s : inout array_subtype_mirror);
end wgsuluj;

use std.reflection.all;

architecture yynkpdx of wgsuluj is
  shared variable pkzztz : subtype_mirror;
  shared variable sbzllcizt : physical_value_mirror;
  shared variable ajrkprz : protected_subtype_mirror;
  signal ia : time;
  shared variable pafb : subtype_mirror;
  shared variable ztawit : physical_value_mirror;
  shared variable ehyprdfi : protected_subtype_mirror;
  signal rrrvoth : time;
  shared variable ttt : subtype_mirror;
  shared variable fiste : physical_value_mirror;
  shared variable cxbv : protected_subtype_mirror;
  signal d : time;
  shared variable nmjhxzaqc : subtype_mirror;
  shared variable xsixvawpj : physical_value_mirror;
  shared variable ehuycg : protected_subtype_mirror;
begin
  kqkpg : entity work.jatkhptl
    port map (flouk => ehuycg, rrnpf => xsixvawpj, ndz => nmjhxzaqc, rvotm => d);
  dcfoeyoba : entity work.jatkhptl
    port map (flouk => cxbv, rrnpf => fiste, ndz => ttt, rvotm => rrrvoth);
  epjkehno : entity work.jatkhptl
    port map (flouk => ehyprdfi, rrnpf => ztawit, ndz => pafb, rvotm => ia);
  y : entity work.jatkhptl
    port map (flouk => ajrkprz, rrnpf => sbzllcizt, ndz => pkzztz, rvotm => hig);
end yynkpdx;



-- Seed after: 303686412119900609,14426950258250697445
