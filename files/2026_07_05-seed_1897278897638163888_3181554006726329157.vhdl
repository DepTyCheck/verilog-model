-- Seed: 1897278897638163888,3181554006726329157

entity hpf is
  port (grcdnyio : in real);
end hpf;

architecture atlx of hpf is
  
begin
  
end atlx;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity tec is
  port (dxgkf : inout real; yjumghga : buffer std_logic_vector(0 to 1); k : inout severity_level; eoiwoqdjaq : inout integer_subtype_mirror);
end tec;

architecture hbjioybi of tec is
  
begin
  o : entity work.hpf
    port map (grcdnyio => dxgkf);
  r : entity work.hpf
    port map (grcdnyio => dxgkf);
  lrkdrjh : entity work.hpf
    port map (grcdnyio => dxgkf);
  lnxlb : entity work.hpf
    port map (grcdnyio => dxgkf);
  
  -- Single-driven assignments
  k <= WARNING;
  dxgkf <= 8#46354.1_5#;
  
  -- Multi-driven assignments
  yjumghga <= ('0', 'W');
  yjumghga <= ('-', 'L');
end hbjioybi;



-- Seed after: 6505096441608482678,3181554006726329157
