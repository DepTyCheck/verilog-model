-- Seed: 7900922681609587630,13479070923501788437

library ieee;
use ieee.std_logic_1164.all;

entity dnmwjexwv is
  port (k : buffer std_logic; qmsihhv : out bit_vector(3 to 0); xdu : buffer integer; ec : linkage time_vector(2 downto 2));
end dnmwjexwv;

architecture tddnmotkj of dnmwjexwv is
  
begin
  -- Single-driven assignments
  xdu <= 16#9#;
  qmsihhv <= (others => '0');
  
  -- Multi-driven assignments
  k <= 'H';
  k <= '-';
  k <= 'L';
  k <= 'Z';
end tddnmotkj;

entity i is
  port (v : out real);
end i;

library ieee;
use ieee.std_logic_1164.all;

architecture muefjzge of i is
  signal bf : time_vector(2 downto 2);
  signal bvnf : integer;
  signal b : bit_vector(3 to 0);
  signal xaqus : std_logic;
  signal wcfrkspbdp : time_vector(2 downto 2);
  signal swlpwtt : integer;
  signal psekte : bit_vector(3 to 0);
  signal kfvomk : std_logic;
  signal zjxyt : time_vector(2 downto 2);
  signal j : integer;
  signal qtwelrr : bit_vector(3 to 0);
  signal r : std_logic;
begin
  pjrp : entity work.dnmwjexwv
    port map (k => r, qmsihhv => qtwelrr, xdu => j, ec => zjxyt);
  liyhmk : entity work.dnmwjexwv
    port map (k => kfvomk, qmsihhv => psekte, xdu => swlpwtt, ec => wcfrkspbdp);
  owbgaque : entity work.dnmwjexwv
    port map (k => xaqus, qmsihhv => b, xdu => bvnf, ec => bf);
  
  -- Single-driven assignments
  v <= 16#7_2.C#;
  
  -- Multi-driven assignments
  xaqus <= 'Z';
  kfvomk <= 'U';
  xaqus <= 'L';
  r <= 'U';
end muefjzge;



-- Seed after: 13463174331946864403,13479070923501788437
