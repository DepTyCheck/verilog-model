-- Seed: 12276932537429849385,8067602802092121131

library ieee;
use ieee.std_logic_1164.all;

entity xvsrqso is
  port (irlgksbn : out integer_vector(3 to 4); tw : buffer real; xgrrq : buffer std_logic; by : in time);
end xvsrqso;

architecture nzateozqhm of xvsrqso is
  
begin
  -- Single-driven assignments
  tw <= 2.0_3_3;
  irlgksbn <= irlgksbn;
  
  -- Multi-driven assignments
  xgrrq <= 'H';
  xgrrq <= xgrrq;
  xgrrq <= '0';
end nzateozqhm;

library ieee;
use ieee.std_logic_1164.all;

entity riiwbqiey is
  port (tvkhewj : linkage std_logic_vector(3 downto 4); tcvnm : in real; ijgoqyqu : out time; dna : linkage real);
end riiwbqiey;

library ieee;
use ieee.std_logic_1164.all;

architecture hy of riiwbqiey is
  signal z : time;
  signal ywdj : real;
  signal ff : integer_vector(3 to 4);
  signal wamjktopul : time;
  signal fgismn : std_logic;
  signal wwy : real;
  signal snhyvi : integer_vector(3 to 4);
  signal vsjq : time;
  signal vhvpcaw : real;
  signal nvkbvsftdg : integer_vector(3 to 4);
  signal tuyvm : std_logic;
  signal ioqgmgx : real;
  signal yu : integer_vector(3 to 4);
begin
  evmnf : entity work.xvsrqso
    port map (irlgksbn => yu, tw => ioqgmgx, xgrrq => tuyvm, by => ijgoqyqu);
  cdml : entity work.xvsrqso
    port map (irlgksbn => nvkbvsftdg, tw => vhvpcaw, xgrrq => tuyvm, by => vsjq);
  yfoecesdje : entity work.xvsrqso
    port map (irlgksbn => snhyvi, tw => wwy, xgrrq => fgismn, by => wamjktopul);
  roauygb : entity work.xvsrqso
    port map (irlgksbn => ff, tw => ywdj, xgrrq => tuyvm, by => z);
  
  -- Single-driven assignments
  vsjq <= ijgoqyqu;
  z <= vsjq;
  wamjktopul <= ijgoqyqu;
  ijgoqyqu <= ijgoqyqu;
  
  -- Multi-driven assignments
  tuyvm <= tuyvm;
  tuyvm <= tuyvm;
  fgismn <= '0';
  tuyvm <= 'W';
end hy;

library ieee;
use ieee.std_logic_1164.all;

entity crnbpgubl is
  port (clrufv : in integer; dwaura : in real; mpqlssy : out time; qsorquo : buffer std_logic_vector(0 downto 4));
end crnbpgubl;

library ieee;
use ieee.std_logic_1164.all;

architecture hhkotpehs of crnbpgubl is
  signal jyag : real;
  signal zjg : integer_vector(3 to 4);
  signal lq : time;
  signal vz : real;
  signal kqmu : integer_vector(3 to 4);
  signal wbirgypkyo : time;
  signal j : std_logic;
  signal cl : real;
  signal lh : integer_vector(3 to 4);
begin
  dvvsgncts : entity work.xvsrqso
    port map (irlgksbn => lh, tw => cl, xgrrq => j, by => wbirgypkyo);
  etchvs : entity work.xvsrqso
    port map (irlgksbn => kqmu, tw => vz, xgrrq => j, by => lq);
  zkbmatwpni : entity work.xvsrqso
    port map (irlgksbn => zjg, tw => jyag, xgrrq => j, by => wbirgypkyo);
  
  -- Single-driven assignments
  mpqlssy <= mpqlssy;
  lq <= mpqlssy;
  wbirgypkyo <= mpqlssy;
  
  -- Multi-driven assignments
  j <= j;
  j <= j;
end hhkotpehs;

library ieee;
use ieee.std_logic_1164.all;

entity t is
  port (jkejsfwbe : in character; j : out real; hjile : out std_logic_vector(4 to 3));
end t;

library ieee;
use ieee.std_logic_1164.all;

architecture ta of t is
  signal zhguz : time;
  signal phbgycuimv : std_logic;
  signal tzc : integer_vector(3 to 4);
  signal mmhhq : time;
  signal se : integer_vector(3 to 4);
  signal uxyldn : real;
  signal smac : time;
  signal rlbhkgmzo : real;
  signal ctljshoxof : std_logic_vector(3 downto 4);
  signal wpissxlqp : time;
  signal hwhm : std_logic;
  signal lfhuaj : real;
  signal zel : integer_vector(3 to 4);
begin
  opprps : entity work.xvsrqso
    port map (irlgksbn => zel, tw => lfhuaj, xgrrq => hwhm, by => wpissxlqp);
  qon : entity work.riiwbqiey
    port map (tvkhewj => ctljshoxof, tcvnm => rlbhkgmzo, ijgoqyqu => smac, dna => uxyldn);
  mdvvf : entity work.xvsrqso
    port map (irlgksbn => se, tw => j, xgrrq => hwhm, by => mmhhq);
  kqrvds : entity work.xvsrqso
    port map (irlgksbn => tzc, tw => rlbhkgmzo, xgrrq => phbgycuimv, by => zhguz);
  
  -- Single-driven assignments
  wpissxlqp <= wpissxlqp;
  zhguz <= 1 sec;
  mmhhq <= 4 min;
  
  -- Multi-driven assignments
  phbgycuimv <= 'U';
end ta;



-- Seed after: 16643613386591200191,8067602802092121131
