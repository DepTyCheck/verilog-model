-- Seed: 12555599136012488318,7142793346053417159

library ieee;
use ieee.std_logic_1164.all;

entity ohdcpe is
  port (uurrko : inout std_logic_vector(4 to 0); gvcji : in bit_vector(4 downto 4); h : buffer time);
end ohdcpe;



architecture eilrbau of ohdcpe is
  
begin
  
end eilrbau;

library ieee;
use ieee.std_logic_1164.all;

entity wcgr is
  port (lqidt : out std_logic_vector(3 downto 4); fu : inout real_vector(4 to 0));
end wcgr;

library ieee;
use ieee.std_logic_1164.all;

architecture twqnkd of wcgr is
  signal tumglm : time;
  signal cqnas : std_logic_vector(4 to 0);
  signal svq : time;
  signal ykzaxcn : bit_vector(4 downto 4);
  signal lkahg : std_logic_vector(4 to 0);
  signal tnpfr : time;
  signal olmwdztyt : std_logic_vector(4 to 0);
  signal ukv : time;
  signal uvalm : bit_vector(4 downto 4);
begin
  hzlmnzczhv : entity work.ohdcpe
    port map (uurrko => lqidt, gvcji => uvalm, h => ukv);
  trevc : entity work.ohdcpe
    port map (uurrko => olmwdztyt, gvcji => uvalm, h => tnpfr);
  ev : entity work.ohdcpe
    port map (uurrko => lkahg, gvcji => ykzaxcn, h => svq);
  vkqajqhdi : entity work.ohdcpe
    port map (uurrko => cqnas, gvcji => uvalm, h => tumglm);
end twqnkd;

library ieee;
use ieee.std_logic_1164.all;

entity youutpoqx is
  port (wax : linkage boolean; dnlw : in std_logic_vector(3 downto 4));
end youutpoqx;



architecture vr of youutpoqx is
  
begin
  
end vr;



-- Seed after: 2703797565856109185,7142793346053417159
