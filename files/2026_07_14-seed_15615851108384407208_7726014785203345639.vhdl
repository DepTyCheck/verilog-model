-- Seed: 15615851108384407208,7726014785203345639

use std.reflection.all;

entity c is
  port (ozv : inout file_subtype_mirror; cuyqpcl : inout floating_value_mirror; bwnrymfw : inout array_value_mirror);
end c;

architecture kezieamo of c is
  
begin
  
end kezieamo;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity kyk is
  port ( raimqtbay : linkage std_logic_vector(4 downto 0)
  ; avaddaf : inout floating_value_mirror
  ; fzuwukfy : inout physical_subtype_mirror
  ; oze : inout record_subtype_mirror
  );
end kyk;

use std.reflection.all;

architecture mf of kyk is
  shared variable bvdm : array_value_mirror;
  shared variable iti : file_subtype_mirror;
  shared variable sfr : array_value_mirror;
  shared variable jutcdpk : floating_value_mirror;
  shared variable l : file_subtype_mirror;
  shared variable xos : array_value_mirror;
  shared variable zvc : floating_value_mirror;
  shared variable wlz : file_subtype_mirror;
  shared variable ibtf : array_value_mirror;
  shared variable k : floating_value_mirror;
  shared variable mv : file_subtype_mirror;
begin
  pfwu : entity work.c
    port map (ozv => mv, cuyqpcl => k, bwnrymfw => ibtf);
  evmd : entity work.c
    port map (ozv => wlz, cuyqpcl => zvc, bwnrymfw => xos);
  phz : entity work.c
    port map (ozv => l, cuyqpcl => jutcdpk, bwnrymfw => sfr);
  vcvzlpsdv : entity work.c
    port map (ozv => iti, cuyqpcl => avaddaf, bwnrymfw => bvdm);
end mf;

entity f is
  port (piz : buffer real_vector(2 downto 0));
end f;

architecture wh of f is
  
begin
  -- Single-driven assignments
  piz <= piz;
end wh;



-- Seed after: 2402147592279265871,7726014785203345639
