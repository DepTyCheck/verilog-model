-- Seed: 1205027225119197117,5511103086789671269

entity w is
  port (xua : linkage severity_level; iqeuyfd : linkage string(2 to 3); hc : buffer real; tzu : linkage integer);
end w;

architecture wgt of w is
  
begin
  -- Single-driven assignments
  hc <= 16#F_5_8.D#;
end wgt;

entity ev is
  port (mvbuqauam : buffer real; xicbgr : linkage severity_level; rdylj : out time; fuyhiayb : out character);
end ev;

architecture asdwb of ev is
  signal ialfemduhf : integer;
  signal cit : string(2 to 3);
  signal nkeu : integer;
  signal wgpuu : real;
  signal pmtz : string(2 to 3);
  signal qajxi : severity_level;
  signal jqouzy : integer;
  signal ozjhjrv : real;
  signal z : string(2 to 3);
  signal cgvljd : severity_level;
begin
  sxcgwbxd : entity work.w
    port map (xua => cgvljd, iqeuyfd => z, hc => ozjhjrv, tzu => jqouzy);
  kskcfy : entity work.w
    port map (xua => qajxi, iqeuyfd => pmtz, hc => wgpuu, tzu => nkeu);
  aceqrb : entity work.w
    port map (xua => xicbgr, iqeuyfd => cit, hc => mvbuqauam, tzu => ialfemduhf);
  
  -- Single-driven assignments
  fuyhiayb <= 'd';
  rdylj <= 32.4_4_1 fs;
end asdwb;

library ieee;
use ieee.std_logic_1164.all;

entity fbzvyr is
  port (fpze : out std_logic_vector(2 to 4); qxcn : buffer integer);
end fbzvyr;

architecture dcbfonrgin of fbzvyr is
  signal zzp : integer;
  signal onfhfzoh : real;
  signal wkbdamzza : string(2 to 3);
  signal twarppzb : severity_level;
  signal rc : integer;
  signal jttjemv : real;
  signal czczdy : string(2 to 3);
  signal bsxr : severity_level;
  signal yrtykxpfi : integer;
  signal hmudq : real;
  signal ouddxca : string(2 to 3);
  signal jbsxjo : severity_level;
  signal m : real;
  signal wms : string(2 to 3);
  signal bqbts : severity_level;
begin
  flugzhg : entity work.w
    port map (xua => bqbts, iqeuyfd => wms, hc => m, tzu => qxcn);
  pyudznrq : entity work.w
    port map (xua => jbsxjo, iqeuyfd => ouddxca, hc => hmudq, tzu => yrtykxpfi);
  nopiuqlqt : entity work.w
    port map (xua => bsxr, iqeuyfd => czczdy, hc => jttjemv, tzu => rc);
  xz : entity work.w
    port map (xua => twarppzb, iqeuyfd => wkbdamzza, hc => onfhfzoh, tzu => zzp);
  
  -- Multi-driven assignments
  fpze <= fpze;
  fpze <= ('W', 'X', 'X');
  fpze <= fpze;
end dcbfonrgin;



-- Seed after: 9230849296810964796,5511103086789671269
