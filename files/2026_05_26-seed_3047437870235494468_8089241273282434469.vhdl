-- Seed: 3047437870235494468,8089241273282434469

library ieee;
use ieee.std_logic_1164.all;

entity lapn is
  port (p : buffer real_vector(2 downto 1); d : buffer std_logic_vector(0 to 4); xhi : inout real);
end lapn;



architecture jcbwfhr of lapn is
  
begin
  
end jcbwfhr;

library ieee;
use ieee.std_logic_1164.all;

entity vftdogdid is
  port (jqkpfe : buffer std_logic_vector(0 to 4));
end vftdogdid;

library ieee;
use ieee.std_logic_1164.all;

architecture o of vftdogdid is
  signal pgvqze : real;
  signal ocahwal : std_logic_vector(0 to 4);
  signal wvogznlxih : real_vector(2 downto 1);
  signal jv : real;
  signal eoeakua : real_vector(2 downto 1);
  signal upmqsmyt : real;
  signal kcf : real_vector(2 downto 1);
begin
  cbm : entity work.lapn
    port map (p => kcf, d => jqkpfe, xhi => upmqsmyt);
  vkir : entity work.lapn
    port map (p => eoeakua, d => jqkpfe, xhi => jv);
  exx : entity work.lapn
    port map (p => wvogznlxih, d => ocahwal, xhi => pgvqze);
end o;

library ieee;
use ieee.std_logic_1164.all;

entity fk is
  port (vfdvlhv : inout real; hjphrul : inout integer; i : linkage integer; xddp : inout std_logic);
end fk;



architecture iy of fk is
  
begin
  
end iy;

library ieee;
use ieee.std_logic_1164.all;

entity d is
  port (h : out real_vector(3 downto 3); v : buffer std_logic_vector(3 downto 4));
end d;

library ieee;
use ieee.std_logic_1164.all;

architecture rjo of d is
  signal xuai : real;
  signal mquovh : std_logic_vector(0 to 4);
  signal gp : real_vector(2 downto 1);
begin
  ahiedjkfxg : entity work.lapn
    port map (p => gp, d => mquovh, xhi => xuai);
end rjo;



-- Seed after: 4929044135803884157,8089241273282434469
