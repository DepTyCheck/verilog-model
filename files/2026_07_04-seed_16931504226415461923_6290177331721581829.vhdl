-- Seed: 16931504226415461923,6290177331721581829

use std.reflection.all;

entity kj is
  port (hqpiitz : inout floating_value_mirror; nmrnk : out bit);
end kj;

architecture mfbtypz of kj is
  
begin
  
end mfbtypz;

use std.reflection.all;

entity bzdrywpdw is
  port (tm : inout file_subtype_mirror);
end bzdrywpdw;

use std.reflection.all;

architecture zomuf of bzdrywpdw is
  signal orhqlpq : bit;
  shared variable b : floating_value_mirror;
begin
  uwjyxw : entity work.kj
    port map (hqpiitz => b, nmrnk => orhqlpq);
end zomuf;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity kjemfzfy is
  port (abpjxtlm : in time; jhcabz : inout enumeration_subtype_mirror; nwim : linkage real; lrq : in std_logic_vector(2 to 0));
end kjemfzfy;

use std.reflection.all;

architecture m of kjemfzfy is
  shared variable ahxfpxm : file_subtype_mirror;
  signal mgrzo : bit;
  shared variable cm : floating_value_mirror;
  signal ykymzkueej : bit;
  shared variable a : floating_value_mirror;
  signal cyq : bit;
  shared variable sqrakcldb : floating_value_mirror;
begin
  sfwk : entity work.kj
    port map (hqpiitz => sqrakcldb, nmrnk => cyq);
  myqlfiz : entity work.kj
    port map (hqpiitz => a, nmrnk => ykymzkueej);
  fyfmisoa : entity work.kj
    port map (hqpiitz => cm, nmrnk => mgrzo);
  way : entity work.bzdrywpdw
    port map (tm => ahxfpxm);
end m;



-- Seed after: 7729163749013403785,6290177331721581829
