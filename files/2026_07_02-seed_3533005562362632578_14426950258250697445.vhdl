-- Seed: 3533005562362632578,14426950258250697445

use std.reflection.all;

entity mck is
  port (vdpjlyw : linkage integer_vector(4 downto 0); i : inout real_vector(2 downto 1); buwmnnalw : inout record_subtype_mirror);
end mck;

architecture huhhazc of mck is
  
begin
  
end huhhazc;

library ieee;
use ieee.std_logic_1164.all;

entity qrbsk is
  port (vegx : linkage real; ukbyaple : buffer std_logic);
end qrbsk;

use std.reflection.all;

architecture xwfxwkzp of qrbsk is
  shared variable uqxekklrrs : record_subtype_mirror;
  signal u : real_vector(2 downto 1);
  signal gleikyorzh : integer_vector(4 downto 0);
  shared variable syx : record_subtype_mirror;
  signal k : real_vector(2 downto 1);
  signal pzokotg : integer_vector(4 downto 0);
  shared variable rpwcu : record_subtype_mirror;
  signal ah : real_vector(2 downto 1);
  signal rcxgirtpo : integer_vector(4 downto 0);
begin
  tuq : entity work.mck
    port map (vdpjlyw => rcxgirtpo, i => ah, buwmnnalw => rpwcu);
  ypfxat : entity work.mck
    port map (vdpjlyw => pzokotg, i => k, buwmnnalw => syx);
  nwlsjwut : entity work.mck
    port map (vdpjlyw => gleikyorzh, i => u, buwmnnalw => uqxekklrrs);
  
  -- Multi-driven assignments
  ukbyaple <= ukbyaple;
end xwfxwkzp;

use std.reflection.all;

entity ci is
  port (agy : inout integer; jdyn : inout physical_subtype_mirror; qoftnxaa : inout record_value_mirror);
end ci;

use std.reflection.all;

architecture gqahzirq of ci is
  shared variable g : record_subtype_mirror;
  signal qie : real_vector(2 downto 1);
  signal bhxrf : integer_vector(4 downto 0);
begin
  o : entity work.mck
    port map (vdpjlyw => bhxrf, i => qie, buwmnnalw => g);
end gqahzirq;

use std.reflection.all;

entity rwcuzyi is
  port (e : inout protected_value_mirror; b : out boolean_vector(0 to 2));
end rwcuzyi;

architecture oa of rwcuzyi is
  
begin
  
end oa;



-- Seed after: 10074079501954435998,14426950258250697445
