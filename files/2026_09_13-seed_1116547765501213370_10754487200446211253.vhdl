-- Seed: 1116547765501213370,10754487200446211253

library ieee;
use ieee.std_logic_1164.all;

entity aweyhdq is
  port (xkczd : inout integer_vector(3 to 4); k : out real; pchq : out std_logic_vector(2 to 0); wemkk : in bit);
end aweyhdq;

architecture gqunmmblr of aweyhdq is
  
begin
  -- Single-driven assignments
  k <= k;
  xkczd <= (2#0#, 44403);
  
  -- Multi-driven assignments
  pchq <= "";
end gqunmmblr;

library ieee;
use ieee.std_logic_1164.all;

entity bgxrdcomxe is
  port (x : in boolean_vector(1 to 0); vlsnsswzlf : in severity_level; ad : buffer integer; v : out std_logic);
end bgxrdcomxe;

library ieee;
use ieee.std_logic_1164.all;

architecture pw of bgxrdcomxe is
  signal f : bit;
  signal dlrega : std_logic_vector(2 to 0);
  signal p : real;
  signal dtirdkimp : integer_vector(3 to 4);
  signal hkp : bit;
  signal pytlplqahn : std_logic_vector(2 to 0);
  signal aggbo : real;
  signal c : integer_vector(3 to 4);
begin
  mccxoe : entity work.aweyhdq
    port map (xkczd => c, k => aggbo, pchq => pytlplqahn, wemkk => hkp);
  wp : entity work.aweyhdq
    port map (xkczd => dtirdkimp, k => p, pchq => dlrega, wemkk => f);
  
  -- Single-driven assignments
  hkp <= hkp;
  ad <= ad;
  f <= '0';
  
  -- Multi-driven assignments
  dlrega <= "";
  pytlplqahn <= "";
  pytlplqahn <= pytlplqahn;
end pw;

library ieee;
use ieee.std_logic_1164.all;

entity kb is
  port (uxlwyf : linkage string(3 to 2); mrxjkdjojo : buffer std_logic_vector(0 to 4); pslfo : in std_logic);
end kb;

architecture lscodqibzs of kb is
  
begin
  
end lscodqibzs;

entity p is
  port (bvjwukhzm : in integer; wehpjiegx : linkage time; wkwtezphi : in time);
end p;

library ieee;
use ieee.std_logic_1164.all;

architecture nfqawhrdx of p is
  signal v : std_logic_vector(2 to 0);
  signal yoaq : real;
  signal kbxvfsytfk : integer_vector(3 to 4);
  signal f : bit;
  signal kn : std_logic_vector(2 to 0);
  signal nxminngbxn : real;
  signal gvzl : integer_vector(3 to 4);
begin
  x : entity work.aweyhdq
    port map (xkczd => gvzl, k => nxminngbxn, pchq => kn, wemkk => f);
  ng : entity work.aweyhdq
    port map (xkczd => kbxvfsytfk, k => yoaq, pchq => v, wemkk => f);
  
  -- Multi-driven assignments
  kn <= (others => '0');
  kn <= "";
end nfqawhrdx;



-- Seed after: 1848177795020372087,10754487200446211253
