-- Seed: 13573560932366378363,7726014785203345639

use std.reflection.all;

entity x is
  port (xmrllrjidz : in integer; fvwymnum : inout subtype_mirror; vvem : inout record_value_mirror; u : linkage time);
end x;

architecture webeuzcmxd of x is
  
begin
  
end webeuzcmxd;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity wv is
  port (vcsxhc : buffer std_logic; xnk : inout file_subtype_mirror);
end wv;

use std.reflection.all;

architecture dgoxdocns of wv is
  signal kederl : time;
  shared variable adhyuul : record_value_mirror;
  shared variable q : subtype_mirror;
  signal dyx : integer;
  signal s : time;
  shared variable ypzlwte : record_value_mirror;
  shared variable xmspdjf : subtype_mirror;
  signal twwf : time;
  shared variable ph : record_value_mirror;
  shared variable ycpwsqwxlf : subtype_mirror;
  signal wro : integer;
  signal sudjwthgvs : time;
  shared variable tknyftm : record_value_mirror;
  shared variable qbfddhe : subtype_mirror;
  signal shs : integer;
begin
  e : entity work.x
    port map (xmrllrjidz => shs, fvwymnum => qbfddhe, vvem => tknyftm, u => sudjwthgvs);
  cyxutlrnpl : entity work.x
    port map (xmrllrjidz => wro, fvwymnum => ycpwsqwxlf, vvem => ph, u => twwf);
  l : entity work.x
    port map (xmrllrjidz => shs, fvwymnum => xmspdjf, vvem => ypzlwte, u => s);
  erfxexg : entity work.x
    port map (xmrllrjidz => dyx, fvwymnum => q, vvem => adhyuul, u => kederl);
  
  -- Single-driven assignments
  shs <= shs;
  dyx <= shs;
  wro <= shs;
  
  -- Multi-driven assignments
  vcsxhc <= 'H';
end dgoxdocns;

entity gixxtnhys is
  port (jtcbumrjt : inout severity_level);
end gixxtnhys;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture yibowmhw of gixxtnhys is
  shared variable dty : file_subtype_mirror;
  shared variable hebab : file_subtype_mirror;
  signal vurnfydh : std_logic;
  signal mabcwsq : time;
  shared variable yynbgwef : record_value_mirror;
  shared variable lzepy : subtype_mirror;
  signal maskjv : integer;
  signal yoaww : time;
  shared variable pkxbg : record_value_mirror;
  shared variable ww : subtype_mirror;
  signal s : integer;
begin
  ultjajtngb : entity work.x
    port map (xmrllrjidz => s, fvwymnum => ww, vvem => pkxbg, u => yoaww);
  khotljzom : entity work.x
    port map (xmrllrjidz => maskjv, fvwymnum => lzepy, vvem => yynbgwef, u => mabcwsq);
  rkwiv : entity work.wv
    port map (vcsxhc => vurnfydh, xnk => hebab);
  bzlss : entity work.wv
    port map (vcsxhc => vurnfydh, xnk => dty);
  
  -- Single-driven assignments
  jtcbumrjt <= jtcbumrjt;
  maskjv <= s;
  s <= 8#2_6#;
  
  -- Multi-driven assignments
  vurnfydh <= vurnfydh;
  vurnfydh <= vurnfydh;
  vurnfydh <= 'H';
  vurnfydh <= vurnfydh;
end yibowmhw;

use std.reflection.all;

entity dqqjzmzps is
  port (lgmcnf : inout integer_value_mirror; wmazj : out real; qvvjtauuta : linkage real; pxfre : inout floating_subtype_mirror);
end dqqjzmzps;

architecture nj of dqqjzmzps is
  
begin
  -- Single-driven assignments
  wmazj <= wmazj;
end nj;



-- Seed after: 7842804138159065160,7726014785203345639
