-- Seed: 9162033024848648914,6329330932550885447

library ieee;
use ieee.std_logic_1164.all;

entity wujf is
  port (sylutle : in time; llta : inout real; fwmha : linkage std_logic_vector(4 downto 1));
end wujf;



architecture wqh of wujf is
  
begin
  
end wqh;

library ieee;
use ieee.std_logic_1164.all;

entity yz is
  port (dbgyrv : inout bit; jkvoad : buffer std_logic; smxh : buffer character; a : buffer std_logic_vector(1 downto 3));
end yz;

library ieee;
use ieee.std_logic_1164.all;

architecture ae of yz is
  signal xo : real;
  signal b : time;
  signal qfqymvg : real;
  signal rcu : time;
  signal v : std_logic_vector(4 downto 1);
  signal h : real;
  signal gavey : time;
begin
  jmvr : entity work.wujf
    port map (sylutle => gavey, llta => h, fwmha => v);
  khrsjne : entity work.wujf
    port map (sylutle => rcu, llta => qfqymvg, fwmha => v);
  djlrta : entity work.wujf
    port map (sylutle => b, llta => xo, fwmha => v);
end ae;

library ieee;
use ieee.std_logic_1164.all;

entity fhhmsbprz is
  port (xujylk : out std_logic_vector(4 to 0); nfzxfdbte : linkage std_logic; lvvsdggcz : in time; gpca : out std_logic);
end fhhmsbprz;

library ieee;
use ieee.std_logic_1164.all;

architecture o of fhhmsbprz is
  signal voufuo : std_logic_vector(4 downto 1);
  signal bftszn : real;
  signal hje : time;
  signal bssltk : std_logic_vector(4 downto 1);
  signal krhztqmi : real;
begin
  ij : entity work.wujf
    port map (sylutle => lvvsdggcz, llta => krhztqmi, fwmha => bssltk);
  rkgvuxcyli : entity work.wujf
    port map (sylutle => hje, llta => bftszn, fwmha => voufuo);
end o;

library ieee;
use ieee.std_logic_1164.all;

entity ctau is
  port (x : linkage integer_vector(3 downto 2); wkmazpuoz : out real; dlrqtsivq : buffer std_logic_vector(0 downto 0); zrsylzk : in integer);
end ctau;

library ieee;
use ieee.std_logic_1164.all;

architecture rpnzv of ctau is
  signal aaoojjg : std_logic_vector(4 downto 1);
  signal gppwwhxj : std_logic_vector(4 downto 1);
  signal u : real;
  signal m : time;
  signal hxuzeltks : character;
  signal sxeohvpe : bit;
  signal aqjmj : std_logic_vector(1 downto 3);
  signal otvexldi : character;
  signal i : std_logic;
  signal vpy : bit;
begin
  ej : entity work.yz
    port map (dbgyrv => vpy, jkvoad => i, smxh => otvexldi, a => aqjmj);
  tb : entity work.yz
    port map (dbgyrv => sxeohvpe, jkvoad => i, smxh => hxuzeltks, a => aqjmj);
  kwyygtc : entity work.wujf
    port map (sylutle => m, llta => u, fwmha => gppwwhxj);
  eobfkxbx : entity work.wujf
    port map (sylutle => m, llta => wkmazpuoz, fwmha => aaoojjg);
end rpnzv;



-- Seed after: 15226954029778904308,6329330932550885447
