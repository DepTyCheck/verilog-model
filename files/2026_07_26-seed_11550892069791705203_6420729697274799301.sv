// Seed: 11550892069791705203,6420729697274799301

module dwbigri (output real fqfmda [0:1]);
  xnor jmpzli(qg, xjsodwzv, zjfrpdxsf);
  
  and tsbjvljne(xjsodwzv, xjsodwzv, zjfrpdxsf);
  
  not lykkzqoh(qg, qg);
  
  or q(buzvl, hhrszlxst, qg);
  
  
  // Single-driven assignments
  assign fqfmda = fqfmda;
  
  // Multi-driven assignments
  assign hhrszlxst = 'b0;
  assign hhrszlxst = 'b0;
  assign qg = hhrszlxst;
  assign buzvl = 'bx01x;
endmodule: dwbigri

module lkn (input real hwmjtxtucj [3:3], inout supply1 logic y, input shortint iqcut, input reg [3:0][4:0][3:2] vtwb);
  not vlij(a, xb);
  
  xor vwuo(ossws, mfymkjj, ossws);
  
  xnor fkvgiyhx(xb, vtwb, vtwb);
  // warning: implicit conversion of port connection truncates from 40 to 1 bits
  //   reg [3:0][4:0][3:2] vtwb -> logic vtwb
  //
  // warning: implicit conversion of port connection truncates from 40 to 1 bits
  //   reg [3:0][4:0][3:2] vtwb -> logic vtwb
  
  
  // Multi-driven assignments
  assign mfymkjj = xb;
endmodule: lkn

module nizuadspz (inout tri1 logic [2:1][1:0][1:1] btdqlc);
  // Multi-driven assignments
  assign btdqlc = '{'bxx11,'{'{'b1},'bz}};
  assign btdqlc = btdqlc;
  assign btdqlc = btdqlc;
endmodule: nizuadspz

module aiunjsb (input longint igv);
  // Unpacked net declarations
  real rflcfg [3:3];
  
  or fmzniii(ptbm, igv, ptbm);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   longint igv -> logic igv
  
  xor xy(ptbm, igv, ptbm);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   longint igv -> logic igv
  
  or tswmcrzzl(ptbm, igv, lbov);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   longint igv -> logic igv
  
  lkn wokqyrnstm(.hwmjtxtucj(rflcfg), .y(eq), .iqcut(igv), .vtwb(igv));
  // warning: implicit conversion of port connection truncates from 64 to 16 bits
  //   longint igv -> shortint iqcut
  //
  // warning: implicit conversion of port connection truncates from 64 to 40 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   longint igv -> reg [3:0][4:0][3:2] vtwb
  
  
  // Multi-driven assignments
  assign eq = 'bx;
  assign ptbm = 'bx1zz1;
  assign ptbm = 'b0;
endmodule: aiunjsb



// Seed after: 14427090341224476871,6420729697274799301
