-- Seed: 7266023915362657554,10871023049702252113

library ieee;
use ieee.std_logic_1164.all;

entity uzcofp is
  port (uphtf : in real; m : buffer std_logic_vector(0 downto 0); mdr : out time);
end uzcofp;

architecture acxjlassyt of uzcofp is
  
begin
  -- Single-driven assignments
  mdr <= mdr;
  
  -- Multi-driven assignments
  m <= (others => '1');
end acxjlassyt;

library ieee;
use ieee.std_logic_1164.all;

entity cccetl is
  port (bbj : inout real; mptkjfdxht : out std_logic; v : out std_logic_vector(2 to 0); hudktorog : buffer bit);
end cccetl;

library ieee;
use ieee.std_logic_1164.all;

architecture xwd of cccetl is
  signal sujn : time;
  signal dppebkhhp : std_logic_vector(0 downto 0);
  signal hnnskevp : time;
  signal qyab : std_logic_vector(0 downto 0);
  signal pujadon : time;
  signal cwyrfwnjt : std_logic_vector(0 downto 0);
begin
  jmuf : entity work.uzcofp
    port map (uphtf => bbj, m => cwyrfwnjt, mdr => pujadon);
  em : entity work.uzcofp
    port map (uphtf => bbj, m => qyab, mdr => hnnskevp);
  xbtrye : entity work.uzcofp
    port map (uphtf => bbj, m => dppebkhhp, mdr => sujn);
  
  -- Multi-driven assignments
  v <= v;
  dppebkhhp <= qyab;
  cwyrfwnjt <= (others => '1');
  v <= "";
end xwd;

entity xtc is
  port (yoji : in boolean; xinlivss : in bit_vector(4 downto 1); y : inout boolean_vector(1 to 1));
end xtc;

library ieee;
use ieee.std_logic_1164.all;

architecture uavzrgoa of xtc is
  signal lrkhv : bit;
  signal mlnjro : std_logic_vector(2 to 0);
  signal useq : std_logic;
  signal xgdihcnliy : time;
  signal orivj : real;
  signal tbju : time;
  signal kpcg : std_logic_vector(0 downto 0);
  signal ghehm : real;
  signal yzcyixh : time;
  signal irgk : std_logic_vector(0 downto 0);
  signal wjlre : real;
begin
  caz : entity work.uzcofp
    port map (uphtf => wjlre, m => irgk, mdr => yzcyixh);
  gcov : entity work.uzcofp
    port map (uphtf => ghehm, m => kpcg, mdr => tbju);
  aib : entity work.uzcofp
    port map (uphtf => orivj, m => irgk, mdr => xgdihcnliy);
  k : entity work.cccetl
    port map (bbj => wjlre, mptkjfdxht => useq, v => mlnjro, hudktorog => lrkhv);
  
  -- Single-driven assignments
  orivj <= ghehm;
  ghehm <= 4_2.2_3_4_3;
  y <= y;
  
  -- Multi-driven assignments
  irgk <= irgk;
end uavzrgoa;



-- Seed after: 6975198525960141617,10871023049702252113
