-- Seed: 13643247178987351907,13479070923501788437

library ieee;
use ieee.std_logic_1164.all;

entity ugd is
  port (pq : out std_logic);
end ugd;

architecture dcc of ugd is
  
begin
  
end dcc;

library ieee;
use ieee.std_logic_1164.all;

entity iqabr is
  port (gzadncery : buffer bit_vector(1 downto 1); f : buffer std_logic_vector(1 downto 3); hjweummc : inout bit_vector(1 to 4));
end iqabr;

library ieee;
use ieee.std_logic_1164.all;

architecture i of iqabr is
  signal pmxicjjgsd : std_logic;
  signal czu : std_logic;
begin
  bvuzx : entity work.ugd
    port map (pq => czu);
  aoayvpocbr : entity work.ugd
    port map (pq => czu);
  qwuyiukfk : entity work.ugd
    port map (pq => pmxicjjgsd);
  
  -- Multi-driven assignments
  czu <= '-';
  czu <= 'H';
end i;

entity clzaqbve is
  port (owwn : buffer boolean_vector(4 downto 3); uj : out character; juvkiwkqrc : out severity_level; rwr : buffer integer);
end clzaqbve;

library ieee;
use ieee.std_logic_1164.all;

architecture hfinzgc of clzaqbve is
  signal h : bit_vector(1 to 4);
  signal enxyazcok : std_logic_vector(1 downto 3);
  signal npubtqbffq : bit_vector(1 downto 1);
begin
  wbc : entity work.iqabr
    port map (gzadncery => npubtqbffq, f => enxyazcok, hjweummc => h);
  
  -- Single-driven assignments
  rwr <= 16#C_4#;
end hfinzgc;



-- Seed after: 11341868314341991084,13479070923501788437
