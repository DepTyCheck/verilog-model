-- Seed: 2203088275576427645,7142793346053417159



entity k is
  port (wlvub : in integer; w : linkage boolean; tkixl : out severity_level; wefeapfmy : buffer integer_vector(4 downto 1));
end k;



architecture cbeiydzz of k is
  
begin
  
end cbeiydzz;

library ieee;
use ieee.std_logic_1164.all;

entity obp is
  port (vb : in std_logic; kdmo : buffer time; ipkg : linkage time_vector(3 downto 2); eufkfapo : in real);
end obp;



architecture lyzobyvlq of obp is
  signal rl : integer_vector(4 downto 1);
  signal olc : severity_level;
  signal cflh : integer_vector(4 downto 1);
  signal e : severity_level;
  signal vysjpb : integer;
  signal a : integer_vector(4 downto 1);
  signal xbqptow : severity_level;
  signal deckrbz : integer;
  signal jytrszp : integer_vector(4 downto 1);
  signal zwfiawb : severity_level;
  signal ecx : boolean;
  signal wqusvarfbt : integer;
begin
  yaclsu : entity work.k
    port map (wlvub => wqusvarfbt, w => ecx, tkixl => zwfiawb, wefeapfmy => jytrszp);
  ffnlnjcnwj : entity work.k
    port map (wlvub => deckrbz, w => ecx, tkixl => xbqptow, wefeapfmy => a);
  pc : entity work.k
    port map (wlvub => vysjpb, w => ecx, tkixl => e, wefeapfmy => cflh);
  aouuzjdnaj : entity work.k
    port map (wlvub => deckrbz, w => ecx, tkixl => olc, wefeapfmy => rl);
end lyzobyvlq;

library ieee;
use ieee.std_logic_1164.all;

entity vpdqypznye is
  port (u : inout real; bw : buffer std_logic; nwlrghm : in std_logic_vector(2 to 0));
end vpdqypznye;



architecture ahjyqhs of vpdqypznye is
  signal uzl : time_vector(3 downto 2);
  signal ikopo : time;
  signal rqnukhq : integer_vector(4 downto 1);
  signal yssyd : severity_level;
  signal dkpeqc : integer;
  signal cdhbdorcw : integer_vector(4 downto 1);
  signal xpmiszhxg : severity_level;
  signal zkfqw : boolean;
  signal cewgkm : integer;
begin
  sxkejqzsdr : entity work.k
    port map (wlvub => cewgkm, w => zkfqw, tkixl => xpmiszhxg, wefeapfmy => cdhbdorcw);
  jcja : entity work.k
    port map (wlvub => dkpeqc, w => zkfqw, tkixl => yssyd, wefeapfmy => rqnukhq);
  rgiaq : entity work.obp
    port map (vb => bw, kdmo => ikopo, ipkg => uzl, eufkfapo => u);
end ahjyqhs;



-- Seed after: 2779767967930700750,7142793346053417159
