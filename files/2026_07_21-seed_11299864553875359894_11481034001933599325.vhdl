-- Seed: 11299864553875359894,11481034001933599325

entity jop is
  port (vps : inout severity_level; gvpuo : buffer time);
end jop;

architecture ne of jop is
  
begin
  -- Single-driven assignments
  vps <= vps;
  gvpuo <= 16#7# ns;
end ne;

entity y is
  port (aiclmbqdw : linkage time);
end y;

architecture eioibutz of y is
  signal m : time;
  signal plfz : severity_level;
  signal iyyio : time;
  signal bpqnyeofz : severity_level;
  signal p : time;
  signal pf : severity_level;
begin
  mdcznnmu : entity work.jop
    port map (vps => pf, gvpuo => p);
  mcljh : entity work.jop
    port map (vps => bpqnyeofz, gvpuo => iyyio);
  vp : entity work.jop
    port map (vps => plfz, gvpuo => m);
end eioibutz;

entity yxdvposypl is
  port (nlsjb : buffer integer; fyld : in severity_level; emit : in time);
end yxdvposypl;

architecture shrwnmxs of yxdvposypl is
  signal njch : time;
  signal lfnxyo : time;
  signal eq : severity_level;
  signal vclxxttbng : time;
  signal gufrphkcv : time;
  signal pzs : severity_level;
begin
  a : entity work.jop
    port map (vps => pzs, gvpuo => gufrphkcv);
  chlwuwp : entity work.y
    port map (aiclmbqdw => vclxxttbng);
  gle : entity work.jop
    port map (vps => eq, gvpuo => lfnxyo);
  qyvrckggr : entity work.y
    port map (aiclmbqdw => njch);
  
  -- Single-driven assignments
  nlsjb <= 2#0_1#;
end shrwnmxs;

library ieee;
use ieee.std_logic_1164.all;

entity bdffv is
  port (xmbd : inout std_logic_vector(3 downto 3); ejaprkxdw : out real);
end bdffv;

architecture mpdxnoj of bdffv is
  signal irwmts : time;
  signal dhczlyp : severity_level;
  signal owllls : time;
  signal rua : time;
  signal nvmuv : severity_level;
  signal cssh : time;
  signal hpq : severity_level;
begin
  ds : entity work.jop
    port map (vps => hpq, gvpuo => cssh);
  x : entity work.jop
    port map (vps => nvmuv, gvpuo => rua);
  mh : entity work.y
    port map (aiclmbqdw => owllls);
  swnps : entity work.jop
    port map (vps => dhczlyp, gvpuo => irwmts);
  
  -- Single-driven assignments
  ejaprkxdw <= ejaprkxdw;
end mpdxnoj;



-- Seed after: 9825411981353298348,11481034001933599325
