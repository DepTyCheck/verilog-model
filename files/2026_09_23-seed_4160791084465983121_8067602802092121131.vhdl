-- Seed: 4160791084465983121,8067602802092121131

library ieee;
use ieee.std_logic_1164.all;

entity jtlwnbihto is
  port (uvzvzxexwl : linkage real; jyhexurkty : inout time; e : out std_logic; b : inout std_logic_vector(4 to 2));
end jtlwnbihto;

architecture x of jtlwnbihto is
  
begin
  -- Single-driven assignments
  jyhexurkty <= 02103 ps;
  
  -- Multi-driven assignments
  e <= '-';
end x;

entity xdrhd is
  port (kckeuw : in boolean);
end xdrhd;

library ieee;
use ieee.std_logic_1164.all;

architecture qxbhiyw of xdrhd is
  signal gckzbiqymk : std_logic_vector(4 to 2);
  signal bdrgdd : std_logic;
  signal xgrbvnt : time;
  signal dhtcyeh : real;
begin
  dndqpuvnv : entity work.jtlwnbihto
    port map (uvzvzxexwl => dhtcyeh, jyhexurkty => xgrbvnt, e => bdrgdd, b => gckzbiqymk);
  
  -- Multi-driven assignments
  bdrgdd <= bdrgdd;
  bdrgdd <= bdrgdd;
  bdrgdd <= '0';
  bdrgdd <= 'U';
end qxbhiyw;

entity hwakmimkw is
  port (e : buffer integer_vector(4 downto 4); cwtlinii : in real; osuerlvv : linkage real_vector(2 to 0); arlkmregaa : out integer_vector(2 downto 2));
end hwakmimkw;

library ieee;
use ieee.std_logic_1164.all;

architecture m of hwakmimkw is
  signal qiednj : time;
  signal jagzj : real;
  signal bilpwhokmy : boolean;
  signal zcgtipxup : std_logic;
  signal fiwrwbx : time;
  signal narznboiv : real;
  signal wuzmluokt : std_logic_vector(4 to 2);
  signal n : std_logic;
  signal fmunni : time;
  signal dupcpxsu : real;
begin
  siqzfxe : entity work.jtlwnbihto
    port map (uvzvzxexwl => dupcpxsu, jyhexurkty => fmunni, e => n, b => wuzmluokt);
  oveb : entity work.jtlwnbihto
    port map (uvzvzxexwl => narznboiv, jyhexurkty => fiwrwbx, e => zcgtipxup, b => wuzmluokt);
  qpuqey : entity work.xdrhd
    port map (kckeuw => bilpwhokmy);
  vfr : entity work.jtlwnbihto
    port map (uvzvzxexwl => jagzj, jyhexurkty => qiednj, e => n, b => wuzmluokt);
end m;

library ieee;
use ieee.std_logic_1164.all;

entity f is
  port (hrahepne : out std_logic_vector(3 downto 1); zc : out integer);
end f;

library ieee;
use ieee.std_logic_1164.all;

architecture jonm of f is
  signal ntrbhikth : integer_vector(2 downto 2);
  signal fbtljubf : real_vector(2 to 0);
  signal ocm : real;
  signal nkaebuzl : integer_vector(4 downto 4);
  signal rvsxmp : boolean;
  signal dv : std_logic_vector(4 to 2);
  signal pngxals : std_logic;
  signal mjr : time;
  signal jskeih : real;
  signal facotf : integer_vector(2 downto 2);
  signal exqrbzq : real_vector(2 to 0);
  signal agz : real;
  signal k : integer_vector(4 downto 4);
begin
  fpwseyjv : entity work.hwakmimkw
    port map (e => k, cwtlinii => agz, osuerlvv => exqrbzq, arlkmregaa => facotf);
  flowiwysfs : entity work.jtlwnbihto
    port map (uvzvzxexwl => jskeih, jyhexurkty => mjr, e => pngxals, b => dv);
  fru : entity work.xdrhd
    port map (kckeuw => rvsxmp);
  hyhr : entity work.hwakmimkw
    port map (e => nkaebuzl, cwtlinii => ocm, osuerlvv => fbtljubf, arlkmregaa => ntrbhikth);
  
  -- Single-driven assignments
  zc <= zc;
  agz <= 220.0;
  
  -- Multi-driven assignments
  hrahepne <= ('Z', '-', 'L');
  pngxals <= pngxals;
  dv <= dv;
  hrahepne <= "U10";
end jonm;



-- Seed after: 14784736773616301080,8067602802092121131
