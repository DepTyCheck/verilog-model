-- Seed: 9735710024408630098,4292249356257567981

entity mdoainq is
  port (vunogoe : out time; jlvviiyivb : linkage real; h : buffer real);
end mdoainq;

architecture gghgjjywe of mdoainq is
  
begin
  
end gghgjjywe;

entity bpnxhmi is
  port (u : inout integer);
end bpnxhmi;

architecture foz of bpnxhmi is
  signal casgj : real;
  signal osegyvlc : real;
  signal tbqqojswnu : time;
  signal iaityzllps : real;
  signal nt : real;
  signal w : time;
begin
  evl : entity work.mdoainq
    port map (vunogoe => w, jlvviiyivb => nt, h => iaityzllps);
  coj : entity work.mdoainq
    port map (vunogoe => tbqqojswnu, jlvviiyivb => osegyvlc, h => casgj);
  
  -- Single-driven assignments
  u <= u;
end foz;

entity eugdcu is
  port (bkfesewke : in time);
end eugdcu;

architecture ildtk of eugdcu is
  signal r : real;
  signal xtlb : real;
  signal qjqdxjqucu : time;
  signal kym : real;
  signal mgh : real;
  signal nvlsjicr : time;
begin
  su : entity work.mdoainq
    port map (vunogoe => nvlsjicr, jlvviiyivb => mgh, h => kym);
  b : entity work.mdoainq
    port map (vunogoe => qjqdxjqucu, jlvviiyivb => xtlb, h => r);
end ildtk;

library ieee;
use ieee.std_logic_1164.all;

entity ntqigogzy is
  port (cpglaxafx : linkage std_logic; cinlgnwfqv : buffer real; arn : buffer integer; ksuvm : buffer time);
end ntqigogzy;

architecture gdyjms of ntqigogzy is
  
begin
  -- Single-driven assignments
  ksuvm <= ksuvm;
  cinlgnwfqv <= cinlgnwfqv;
  arn <= 0_1_3_2;
end gdyjms;



-- Seed after: 8979374466176954627,4292249356257567981
