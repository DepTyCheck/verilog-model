-- Seed: 5801725710601043037,14426950258250697445

use std.reflection.all;

entity ypil is
  port (ihssgmta : inout subtype_mirror);
end ypil;

architecture rkoa of ypil is
  
begin
  
end rkoa;

use std.reflection.all;

entity oxodr is
  port (efhw : inout integer_subtype_mirror);
end oxodr;

use std.reflection.all;

architecture xrqadiyemf of oxodr is
  shared variable pk : subtype_mirror;
  shared variable koujdx : subtype_mirror;
begin
  ky : entity work.ypil
    port map (ihssgmta => koujdx);
  cjxwa : entity work.ypil
    port map (ihssgmta => pk);
end xrqadiyemf;

library ieee;
use ieee.std_logic_1164.all;

entity byt is
  port (dtam : out time_vector(3 downto 2); cpqciol : linkage std_logic);
end byt;

use std.reflection.all;

architecture jty of byt is
  shared variable jqzkhhqyl : subtype_mirror;
  shared variable ah : integer_subtype_mirror;
  shared variable hldougx : subtype_mirror;
begin
  fptgyasq : entity work.ypil
    port map (ihssgmta => hldougx);
  q : entity work.oxodr
    port map (efhw => ah);
  otyih : entity work.ypil
    port map (ihssgmta => jqzkhhqyl);
end jty;



-- Seed after: 2001677094127412768,14426950258250697445
