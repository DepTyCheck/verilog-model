-- Seed: 3463760594350816165,14641901754878719179

entity sz is
  port (tj : in string(4 downto 2); pa : in real; e : linkage boolean_vector(3 to 1));
end sz;

architecture mxqldcsc of sz is
  
begin
  
end mxqldcsc;

entity pwfmjrxi is
  port (hl : linkage real; qzcvzseuuw : inout boolean; zehzwrmjdo : linkage real; bswpptsln : buffer time_vector(4 downto 3));
end pwfmjrxi;

architecture rntkljy of pwfmjrxi is
  signal dc : boolean_vector(3 to 1);
  signal gdfxhn : real;
  signal hwjkbqgc : string(4 downto 2);
  signal rzcfbd : boolean_vector(3 to 1);
  signal kzyytv : real;
  signal heru : string(4 downto 2);
  signal hz : boolean_vector(3 to 1);
  signal lgokm : real;
  signal zscw : string(4 downto 2);
  signal kmspjogfze : boolean_vector(3 to 1);
  signal ggtfefuudb : real;
  signal v : string(4 downto 2);
begin
  u : entity work.sz
    port map (tj => v, pa => ggtfefuudb, e => kmspjogfze);
  txvuam : entity work.sz
    port map (tj => zscw, pa => lgokm, e => hz);
  zoqxvwk : entity work.sz
    port map (tj => heru, pa => kzyytv, e => rzcfbd);
  budigwjx : entity work.sz
    port map (tj => hwjkbqgc, pa => gdfxhn, e => dc);
  
  -- Single-driven assignments
  ggtfefuudb <= 16#B.C_1_E_2_6#;
end rntkljy;



-- Seed after: 13718490652195796164,14641901754878719179
