-- Seed: 16407636180463378264,14312733773653067203



entity btsn is
  port (kujoiulas : inout severity_level; yjq : out severity_level);
end btsn;



architecture sd of btsn is
  
begin
  
end sd;



entity wyirevy is
  port (vigesgre : out character);
end wyirevy;



architecture hjadkctkd of wyirevy is
  signal a : severity_level;
  signal ljauszts : severity_level;
  signal spkgwpx : severity_level;
  signal vgsoszgrgt : severity_level;
begin
  db : entity work.btsn
    port map (kujoiulas => vgsoszgrgt, yjq => spkgwpx);
  kr : entity work.btsn
    port map (kujoiulas => ljauszts, yjq => a);
end hjadkctkd;

library ieee;
use ieee.std_logic_1164.all;

entity dqejuuokj is
  port (cvxdr : linkage std_logic; nrnt : in std_logic);
end dqejuuokj;



architecture zp of dqejuuokj is
  signal fqcpo : severity_level;
  signal l : severity_level;
  signal phpz : severity_level;
  signal vkocowsai : severity_level;
  signal y : severity_level;
  signal oupqfnloix : severity_level;
  signal let : severity_level;
  signal o : severity_level;
begin
  snhdtf : entity work.btsn
    port map (kujoiulas => o, yjq => let);
  zszjdlii : entity work.btsn
    port map (kujoiulas => oupqfnloix, yjq => y);
  olrehyyg : entity work.btsn
    port map (kujoiulas => vkocowsai, yjq => phpz);
  bapuwij : entity work.btsn
    port map (kujoiulas => l, yjq => fqcpo);
end zp;

library ieee;
use ieee.std_logic_1164.all;

entity xztvvusq is
  port (ctj : inout std_logic);
end xztvvusq;



architecture ph of xztvvusq is
  
begin
  qzjdlphmq : entity work.dqejuuokj
    port map (cvxdr => ctj, nrnt => ctj);
end ph;



-- Seed after: 16616843811081362545,14312733773653067203
