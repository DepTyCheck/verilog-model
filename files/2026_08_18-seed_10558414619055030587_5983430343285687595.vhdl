-- Seed: 10558414619055030587,5983430343285687595

library ieee;
use ieee.std_logic_1164.all;

entity s is
  port (qyafyg : in integer; paksnvms : linkage std_logic_vector(4 to 2); dkge : in real_vector(1 downto 2); qhn : buffer severity_level);
end s;

architecture vsgpde of s is
  
begin
  -- Single-driven assignments
  qhn <= NOTE;
end vsgpde;

entity mrppy is
  port (nl : in string(5 downto 2); dryqcbml : out boolean_vector(3 downto 0));
end mrppy;

library ieee;
use ieee.std_logic_1164.all;

architecture pzcswtxt of mrppy is
  signal qezrirkedj : severity_level;
  signal brdysuv : real_vector(1 downto 2);
  signal tdauswt : std_logic_vector(4 to 2);
  signal uldkx : severity_level;
  signal oohq : severity_level;
  signal xphiefz : real_vector(1 downto 2);
  signal ogspbsb : severity_level;
  signal wxs : real_vector(1 downto 2);
  signal bonblzm : std_logic_vector(4 to 2);
  signal ehindx : integer;
begin
  sfvylo : entity work.s
    port map (qyafyg => ehindx, paksnvms => bonblzm, dkge => wxs, qhn => ogspbsb);
  k : entity work.s
    port map (qyafyg => ehindx, paksnvms => bonblzm, dkge => xphiefz, qhn => oohq);
  akhpqejxpw : entity work.s
    port map (qyafyg => ehindx, paksnvms => bonblzm, dkge => xphiefz, qhn => uldkx);
  y : entity work.s
    port map (qyafyg => ehindx, paksnvms => tdauswt, dkge => brdysuv, qhn => qezrirkedj);
  
  -- Single-driven assignments
  brdysuv <= (others => 0.0);
  dryqcbml <= (FALSE, FALSE, FALSE, FALSE);
  ehindx <= 8#10734#;
  xphiefz <= (others => 0.0);
  wxs <= wxs;
  
  -- Multi-driven assignments
  tdauswt <= bonblzm;
  bonblzm <= "";
  bonblzm <= bonblzm;
  bonblzm <= bonblzm;
end pzcswtxt;

entity eloladz is
  port (h : in time; gcrsgwk : linkage bit_vector(3 downto 3); swnsv : linkage integer; lovetra : buffer severity_level);
end eloladz;

library ieee;
use ieee.std_logic_1164.all;

architecture e of eloladz is
  signal wfxohk : boolean_vector(3 downto 0);
  signal dn : string(5 downto 2);
  signal iwc : severity_level;
  signal plw : real_vector(1 downto 2);
  signal lgrpxoo : std_logic_vector(4 to 2);
  signal iqf : integer;
begin
  mcafzhn : entity work.s
    port map (qyafyg => iqf, paksnvms => lgrpxoo, dkge => plw, qhn => iwc);
  omeh : entity work.mrppy
    port map (nl => dn, dryqcbml => wfxohk);
  x : entity work.s
    port map (qyafyg => iqf, paksnvms => lgrpxoo, dkge => plw, qhn => lovetra);
  
  -- Single-driven assignments
  plw <= plw;
  dn <= dn;
  iqf <= iqf;
  
  -- Multi-driven assignments
  lgrpxoo <= lgrpxoo;
  lgrpxoo <= lgrpxoo;
  lgrpxoo <= (others => '0');
  lgrpxoo <= "";
end e;

library ieee;
use ieee.std_logic_1164.all;

entity ugufczz is
  port (rdyyt : buffer std_logic; fotvmqrgrh : buffer integer);
end ugufczz;

architecture xlefgytdwc of ugufczz is
  
begin
  -- Multi-driven assignments
  rdyyt <= '-';
  rdyyt <= rdyyt;
  rdyyt <= 'U';
end xlefgytdwc;



-- Seed after: 9090485503243713095,5983430343285687595
