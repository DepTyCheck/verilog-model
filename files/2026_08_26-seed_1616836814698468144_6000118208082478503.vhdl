-- Seed: 1616836814698468144,6000118208082478503

entity d is
  port (viyc : out boolean);
end d;

architecture u of d is
  
begin
  -- Single-driven assignments
  viyc <= viyc;
end u;

entity mmnk is
  port (putywedo : buffer time; psean : buffer string(5 downto 3));
end mmnk;

architecture ni of mmnk is
  signal fn : boolean;
  signal kvtp : boolean;
begin
  whirmkwp : entity work.d
    port map (viyc => kvtp);
  yofpapks : entity work.d
    port map (viyc => fn);
  
  -- Single-driven assignments
  psean <= psean;
  putywedo <= 422 fs;
end ni;

entity pjlykdqdr is
  port (dn : buffer integer);
end pjlykdqdr;

architecture uexo of pjlykdqdr is
  signal vptwkuek : boolean;
  signal ttx : boolean;
  signal u : string(5 downto 3);
  signal bisxmopy : time;
  signal wkscn : boolean;
begin
  nsgk : entity work.d
    port map (viyc => wkscn);
  hkjhghwy : entity work.mmnk
    port map (putywedo => bisxmopy, psean => u);
  ih : entity work.d
    port map (viyc => ttx);
  z : entity work.d
    port map (viyc => vptwkuek);
  
  -- Single-driven assignments
  dn <= dn;
end uexo;

library ieee;
use ieee.std_logic_1164.all;

entity ptiyf is
  port (ckcs : linkage std_logic_vector(3 downto 3); jdlszhsgho : inout real; vcu : out bit; nqpywh : inout boolean_vector(0 downto 1));
end ptiyf;

architecture epwsu of ptiyf is
  signal r : integer;
  signal yxkgpwqvwh : boolean;
begin
  rceehhz : entity work.d
    port map (viyc => yxkgpwqvwh);
  kq : entity work.pjlykdqdr
    port map (dn => r);
  
  -- Single-driven assignments
  nqpywh <= nqpywh;
  vcu <= '1';
  jdlszhsgho <= jdlszhsgho;
end epwsu;



-- Seed after: 8188105263953459535,6000118208082478503
