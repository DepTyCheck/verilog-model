-- Seed: 9803619822460010697,17234720251424330329

library ieee;
use ieee.std_logic_1164.all;

entity pnwa is
  port (jefnozdog : inout std_logic_vector(3 downto 1); ym : buffer real; dh : out real; svlb : buffer time);
end pnwa;

architecture nrfnx of pnwa is
  
begin
  -- Single-driven assignments
  svlb <= svlb;
  dh <= 0_4_4.2;
  
  -- Multi-driven assignments
  jefnozdog <= jefnozdog;
  jefnozdog <= "WW0";
  jefnozdog <= "WXZ";
  jefnozdog <= "UXZ";
end nrfnx;

entity cer is
  port (njubhcjpdw : out time; qxuqtiaq : in bit_vector(0 to 2); xwmv : linkage real; tuo : in integer_vector(3 downto 2));
end cer;

library ieee;
use ieee.std_logic_1164.all;

architecture xf of cer is
  signal ebnv : real;
  signal ax : real;
  signal gp : std_logic_vector(3 downto 1);
  signal ht : time;
  signal zuk : real;
  signal psewrihv : real;
  signal bqwxrv : time;
  signal zrtej : real;
  signal czca : real;
  signal swombqzia : time;
  signal loljpexgh : real;
  signal u : real;
  signal h : std_logic_vector(3 downto 1);
begin
  brafoha : entity work.pnwa
    port map (jefnozdog => h, ym => u, dh => loljpexgh, svlb => swombqzia);
  gopky : entity work.pnwa
    port map (jefnozdog => h, ym => czca, dh => zrtej, svlb => bqwxrv);
  asd : entity work.pnwa
    port map (jefnozdog => h, ym => psewrihv, dh => zuk, svlb => ht);
  aomx : entity work.pnwa
    port map (jefnozdog => gp, ym => ax, dh => ebnv, svlb => njubhcjpdw);
  
  -- Multi-driven assignments
  h <= "1UL";
  h <= "Z1X";
  h <= h;
  h <= h;
end xf;

entity jbtedw is
  port (dv : linkage time);
end jbtedw;

library ieee;
use ieee.std_logic_1164.all;

architecture v of jbtedw is
  signal azndp : integer_vector(3 downto 2);
  signal cw : real;
  signal vif : bit_vector(0 to 2);
  signal hinvr : time;
  signal sr : time;
  signal jzcg : real;
  signal pwcngibxbf : real;
  signal abnwmhk : std_logic_vector(3 downto 1);
begin
  sa : entity work.pnwa
    port map (jefnozdog => abnwmhk, ym => pwcngibxbf, dh => jzcg, svlb => sr);
  vzrvgsmj : entity work.cer
    port map (njubhcjpdw => hinvr, qxuqtiaq => vif, xwmv => cw, tuo => azndp);
  
  -- Single-driven assignments
  vif <= vif;
  
  -- Multi-driven assignments
  abnwmhk <= "WL0";
  abnwmhk <= abnwmhk;
end v;

entity oxfcn is
  port (kquyesb : inout integer; usepbazpu : in real);
end oxfcn;

library ieee;
use ieee.std_logic_1164.all;

architecture naclodqm of oxfcn is
  signal hplqvzrv : time;
  signal zbhztddu : real;
  signal pzqsj : real;
  signal zcoph : time;
  signal odwttj : real;
  signal zafzktfvs : real;
  signal rocpccguxv : time;
  signal k : real;
  signal oyortobzg : real;
  signal vmcnxwpll : time;
  signal yvuzvatr : real;
  signal uhqx : real;
  signal xk : std_logic_vector(3 downto 1);
begin
  bkxoo : entity work.pnwa
    port map (jefnozdog => xk, ym => uhqx, dh => yvuzvatr, svlb => vmcnxwpll);
  pak : entity work.pnwa
    port map (jefnozdog => xk, ym => oyortobzg, dh => k, svlb => rocpccguxv);
  z : entity work.pnwa
    port map (jefnozdog => xk, ym => zafzktfvs, dh => odwttj, svlb => zcoph);
  vsbdhze : entity work.pnwa
    port map (jefnozdog => xk, ym => pzqsj, dh => zbhztddu, svlb => hplqvzrv);
  
  -- Single-driven assignments
  kquyesb <= 8#6_0_4_5#;
end naclodqm;



-- Seed after: 15710788578589437531,17234720251424330329
