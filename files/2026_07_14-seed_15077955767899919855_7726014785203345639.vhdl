-- Seed: 15077955767899919855,7726014785203345639

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity r is
  port (uqksgq : linkage std_logic_vector(0 to 3); qk : inout floating_subtype_mirror; rsdr : out boolean; fkthg : buffer real);
end r;

architecture lsm of r is
  
begin
  -- Single-driven assignments
  fkthg <= fkthg;
  rsdr <= rsdr;
end lsm;

use std.reflection.all;

entity zzrfdwvug is
  port (qxrkic : inout floating_value_mirror; ofiyg : inout access_value_mirror);
end zzrfdwvug;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture vfmnpqshy of zzrfdwvug is
  signal ivv : real;
  signal zftnvu : boolean;
  shared variable xsfloritdp : floating_subtype_mirror;
  signal dalwwha : std_logic_vector(0 to 3);
  signal zw : real;
  signal n : boolean;
  shared variable ocxsjrc : floating_subtype_mirror;
  signal wmrlfvyo : std_logic_vector(0 to 3);
  signal ssznwadxmq : real;
  signal bivuhkodn : boolean;
  shared variable pjhrca : floating_subtype_mirror;
  signal ddhypiam : std_logic_vector(0 to 3);
begin
  ayutnr : entity work.r
    port map (uqksgq => ddhypiam, qk => pjhrca, rsdr => bivuhkodn, fkthg => ssznwadxmq);
  jfwjnwz : entity work.r
    port map (uqksgq => wmrlfvyo, qk => ocxsjrc, rsdr => n, fkthg => zw);
  ha : entity work.r
    port map (uqksgq => dalwwha, qk => xsfloritdp, rsdr => zftnvu, fkthg => ivv);
  
  -- Multi-driven assignments
  ddhypiam <= ('H', 'W', '0', '-');
  wmrlfvyo <= ddhypiam;
  wmrlfvyo <= ddhypiam;
  wmrlfvyo <= ddhypiam;
end vfmnpqshy;



-- Seed after: 18254789004766241697,7726014785203345639
