-- Seed: 15981012789607083254,13501862637168280927

entity vvrir is
  port (bo : out time_vector(0 to 0));
end vvrir;

architecture qf of vvrir is
  
begin
  -- Single-driven assignments
  bo <= bo;
end qf;

entity fmymj is
  port (hpynucb : buffer time; ja : in time; ykxemunsg : inout time; kfrl : buffer integer);
end fmymj;

architecture z of fmymj is
  signal xiegxksab : time_vector(0 to 0);
  signal rijeqawx : time_vector(0 to 0);
  signal tiqmfegen : time_vector(0 to 0);
begin
  sihxvvd : entity work.vvrir
    port map (bo => tiqmfegen);
  pjtwpc : entity work.vvrir
    port map (bo => rijeqawx);
  gr : entity work.vvrir
    port map (bo => xiegxksab);
end z;

library ieee;
use ieee.std_logic_1164.all;

entity mhav is
  port (k : buffer std_logic);
end mhav;

architecture fmmf of mhav is
  signal llgcxmyzf : time_vector(0 to 0);
  signal u : time_vector(0 to 0);
begin
  cftesqz : entity work.vvrir
    port map (bo => u);
  pgsxb : entity work.vvrir
    port map (bo => llgcxmyzf);
  
  -- Multi-driven assignments
  k <= 'L';
  k <= 'H';
  k <= 'Z';
  k <= k;
end fmmf;

library ieee;
use ieee.std_logic_1164.all;

entity fnvyeowa is
  port (ehmkxn : out std_logic_vector(1 to 2));
end fnvyeowa;

architecture fzprkuenvv of fnvyeowa is
  signal zmdar : time_vector(0 to 0);
  signal whgostj : time_vector(0 to 0);
  signal pbjhzgvdbq : time_vector(0 to 0);
  signal eafk : time_vector(0 to 0);
begin
  avhks : entity work.vvrir
    port map (bo => eafk);
  vlb : entity work.vvrir
    port map (bo => pbjhzgvdbq);
  cwthvf : entity work.vvrir
    port map (bo => whgostj);
  xkgmbpv : entity work.vvrir
    port map (bo => zmdar);
end fzprkuenvv;



-- Seed after: 16400234852009877032,13501862637168280927
