-- Seed: 11949288464407899367,1641934135882347475

library ieee;
use ieee.std_logic_1164.all;

entity cck is
  port (ziixfc : linkage real; e : inout integer; pwzibrv : in std_logic_vector(4 downto 0));
end cck;



architecture t of cck is
  
begin
  
end t;

library ieee;
use ieee.std_logic_1164.all;

entity yp is
  port ( hwgyh : linkage std_logic_vector(2 downto 3)
  ; hbnqqldk : in integer
  ; rrjhmmx : inout std_logic_vector(3 to 3)
  ; tdekog : linkage real_vector(1 downto 1)
  );
end yp;

library ieee;
use ieee.std_logic_1164.all;

architecture o of yp is
  signal sun : std_logic_vector(4 downto 0);
  signal drwajh : integer;
  signal stvqzm : real;
  signal zai : integer;
  signal qsipptaq : integer;
  signal nim : real;
  signal snibrk : std_logic_vector(4 downto 0);
  signal fbxho : integer;
  signal r : real;
begin
  h : entity work.cck
    port map (ziixfc => r, e => fbxho, pwzibrv => snibrk);
  eeptrty : entity work.cck
    port map (ziixfc => nim, e => qsipptaq, pwzibrv => snibrk);
  tw : entity work.cck
    port map (ziixfc => nim, e => zai, pwzibrv => snibrk);
  t : entity work.cck
    port map (ziixfc => stvqzm, e => drwajh, pwzibrv => sun);
end o;



entity eyzlrlww is
  port (whpgrt : linkage time_vector(2 to 3); ba : in boolean; kyj : buffer real);
end eyzlrlww;



architecture alczqaslow of eyzlrlww is
  
begin
  
end alczqaslow;



entity wywleosw is
  port (kkgn : inout time; bw : in character);
end wywleosw;



architecture xpfjxlhwy of wywleosw is
  signal aejhp : real;
  signal dajs : boolean;
  signal qz : time_vector(2 to 3);
begin
  t : entity work.eyzlrlww
    port map (whpgrt => qz, ba => dajs, kyj => aejhp);
end xpfjxlhwy;



-- Seed after: 2429309226154603803,1641934135882347475
