-- Seed: 6775634741616411197,18037650846010261179

library ieee;
use ieee.std_logic_1164.all;

entity bqsgjpz is
  port (nybspsr : inout real; svlqarui : buffer std_logic);
end bqsgjpz;

architecture ajcxxjwz of bqsgjpz is
  
begin
  
end ajcxxjwz;

library ieee;
use ieee.std_logic_1164.all;

entity osbagfs is
  port (fkqi : out time; voknlpsbvw : buffer real_vector(4 to 4); q : in std_logic_vector(0 downto 2); rrz : in std_logic_vector(3 to 4));
end osbagfs;

library ieee;
use ieee.std_logic_1164.all;

architecture ijdybsbp of osbagfs is
  signal bvy : std_logic;
  signal ni : real;
begin
  yrvkri : entity work.bqsgjpz
    port map (nybspsr => ni, svlqarui => bvy);
  
  -- Single-driven assignments
  voknlpsbvw <= (others => 2#0_1.0#);
  fkqi <= 8#3.231# fs;
  
  -- Multi-driven assignments
  bvy <= bvy;
  bvy <= 'H';
  bvy <= 'W';
end ijdybsbp;

entity ubsro is
  port (bnoghn : out integer);
end ubsro;

library ieee;
use ieee.std_logic_1164.all;

architecture zxh of ubsro is
  signal kwoujfjjwf : real;
  signal jvdjik : real;
  signal xzzpffslh : std_logic;
  signal zrjtrpvetl : real;
  signal twwhuw : std_logic;
  signal p : real;
begin
  th : entity work.bqsgjpz
    port map (nybspsr => p, svlqarui => twwhuw);
  cm : entity work.bqsgjpz
    port map (nybspsr => zrjtrpvetl, svlqarui => xzzpffslh);
  mxmwcn : entity work.bqsgjpz
    port map (nybspsr => jvdjik, svlqarui => twwhuw);
  uzvbgscopy : entity work.bqsgjpz
    port map (nybspsr => kwoujfjjwf, svlqarui => twwhuw);
  
  -- Single-driven assignments
  bnoghn <= 2#0_0_1#;
  
  -- Multi-driven assignments
  twwhuw <= twwhuw;
  twwhuw <= 'Z';
  twwhuw <= twwhuw;
  twwhuw <= 'X';
end zxh;

library ieee;
use ieee.std_logic_1164.all;

entity omhhe is
  port (x : inout time; sjikcsf : in std_logic_vector(2 to 0); jeqq : inout real; kab : in integer_vector(4 to 3));
end omhhe;

library ieee;
use ieee.std_logic_1164.all;

architecture gwpijc of omhhe is
  signal coyswlyzge : std_logic;
  signal anuvwndxc : std_logic;
  signal rvqoyiepdf : real;
begin
  eurhsfbqs : entity work.bqsgjpz
    port map (nybspsr => rvqoyiepdf, svlqarui => anuvwndxc);
  idrngrqza : entity work.bqsgjpz
    port map (nybspsr => jeqq, svlqarui => coyswlyzge);
  
  -- Single-driven assignments
  x <= x;
  
  -- Multi-driven assignments
  anuvwndxc <= 'Z';
  anuvwndxc <= 'L';
end gwpijc;



-- Seed after: 9467154599832324151,18037650846010261179
