-- Seed: 17316753395876190129,2983771601630957889

library ieee;
use ieee.std_logic_1164.all;

entity xknfsiaybi is
  port (rzmkp : out real_vector(4 downto 2); lv : out std_logic; pbajnm : out string(1 downto 2); iod : inout std_logic);
end xknfsiaybi;

architecture kjjvtbh of xknfsiaybi is
  
begin
  -- Single-driven assignments
  rzmkp <= (4.2, 16#A_8.A_C_7_3#, 2#1_1.1101#);
  pbajnm <= pbajnm;
  
  -- Multi-driven assignments
  iod <= 'W';
  iod <= lv;
  iod <= 'Z';
end kjjvtbh;

use std.reflection.all;

entity ivbpngqhjl is
  port (variable tbw : inout access_value_mirror_pt; variable ujxdgchk : inout floating_subtype_mirror_pt);
end ivbpngqhjl;

library ieee;
use ieee.std_logic_1164.all;

architecture gh of ivbpngqhjl is
  signal azaoz : string(1 downto 2);
  signal nxjogxo : real_vector(4 downto 2);
  signal xrmjpwwcau : string(1 downto 2);
  signal wwmqcht : std_logic;
  signal ni : real_vector(4 downto 2);
  signal yrjvjlj : string(1 downto 2);
  signal ovqdfvntqy : real_vector(4 downto 2);
  signal vslmjfve : std_logic;
  signal lmhdcrkmqs : string(1 downto 2);
  signal l : std_logic;
  signal u : real_vector(4 downto 2);
begin
  lrk : entity work.xknfsiaybi
    port map (rzmkp => u, lv => l, pbajnm => lmhdcrkmqs, iod => vslmjfve);
  woradhzko : entity work.xknfsiaybi
    port map (rzmkp => ovqdfvntqy, lv => l, pbajnm => yrjvjlj, iod => l);
  ldk : entity work.xknfsiaybi
    port map (rzmkp => ni, lv => wwmqcht, pbajnm => xrmjpwwcau, iod => vslmjfve);
  kxuhcfrvp : entity work.xknfsiaybi
    port map (rzmkp => nxjogxo, lv => vslmjfve, pbajnm => azaoz, iod => l);
  
  -- Multi-driven assignments
  l <= '-';
end gh;

library ieee;
use ieee.std_logic_1164.all;

entity kxzgo is
  port (koz : linkage std_logic);
end kxzgo;

library ieee;
use ieee.std_logic_1164.all;

architecture nyuy of kxzgo is
  signal dqdbtdnttl : std_logic;
  signal smgu : string(1 downto 2);
  signal bsm : std_logic;
  signal lsbs : real_vector(4 downto 2);
  signal p : string(1 downto 2);
  signal ll : std_logic;
  signal fzrgex : real_vector(4 downto 2);
begin
  lcddhvi : entity work.xknfsiaybi
    port map (rzmkp => fzrgex, lv => ll, pbajnm => p, iod => ll);
  yksuauzm : entity work.xknfsiaybi
    port map (rzmkp => lsbs, lv => bsm, pbajnm => smgu, iod => dqdbtdnttl);
end nyuy;

use std.reflection.all;

entity taujonkmmj is
  port (variable fflpsypxs : inout record_value_mirror_pt; wrluayb : out real);
end taujonkmmj;

library ieee;
use ieee.std_logic_1164.all;

architecture hkn of taujonkmmj is
  signal nj : std_logic;
  signal xfct : string(1 downto 2);
  signal vno : std_logic;
  signal dfxbwzkft : real_vector(4 downto 2);
begin
  dlrbesqzbl : entity work.xknfsiaybi
    port map (rzmkp => dfxbwzkft, lv => vno, pbajnm => xfct, iod => vno);
  ybsnos : entity work.kxzgo
    port map (koz => nj);
  
  -- Single-driven assignments
  wrluayb <= wrluayb;
  
  -- Multi-driven assignments
  vno <= 'U';
  vno <= vno;
  vno <= vno;
end hkn;



-- Seed after: 13097888618714803979,2983771601630957889
