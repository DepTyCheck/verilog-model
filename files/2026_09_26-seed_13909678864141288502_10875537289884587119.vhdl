-- Seed: 13909678864141288502,10875537289884587119

library ieee;
use ieee.std_logic_1164.all;

entity gxwveeds is
  port (kachmf : in time; w : in severity_level; da : inout std_logic_vector(0 to 2));
end gxwveeds;

architecture ofykxkijs of gxwveeds is
  
begin
  
end ofykxkijs;

library ieee;
use ieee.std_logic_1164.all;

entity a is
  port (uxghrlth : in std_logic);
end a;

library ieee;
use ieee.std_logic_1164.all;

architecture jyffo of a is
  signal mourpq : severity_level;
  signal owmghyia : time;
  signal wazrqskj : severity_level;
  signal yfsabalta : std_logic_vector(0 to 2);
  signal bbp : severity_level;
  signal htpspuoy : time;
begin
  smrasvw : entity work.gxwveeds
    port map (kachmf => htpspuoy, w => bbp, da => yfsabalta);
  kigpuusmjb : entity work.gxwveeds
    port map (kachmf => htpspuoy, w => wazrqskj, da => yfsabalta);
  lsdpp : entity work.gxwveeds
    port map (kachmf => owmghyia, w => bbp, da => yfsabalta);
  ylskqnlzm : entity work.gxwveeds
    port map (kachmf => htpspuoy, w => mourpq, da => yfsabalta);
  
  -- Single-driven assignments
  bbp <= bbp;
  owmghyia <= 2#1110.1# ns;
  wazrqskj <= bbp;
  htpspuoy <= 0 min;
  
  -- Multi-driven assignments
  yfsabalta <= yfsabalta;
  yfsabalta <= ('-', 'H', 'L');
end jyffo;

library ieee;
use ieee.std_logic_1164.all;

entity oddzfufq is
  port (jzmbiyzgm : out std_logic; hawgwo : inout std_logic_vector(4 to 3); ouch : out real);
end oddzfufq;

library ieee;
use ieee.std_logic_1164.all;

architecture fwkb of oddzfufq is
  signal lvoyzsmez : std_logic_vector(0 to 2);
  signal k : severity_level;
  signal pja : std_logic;
  signal jvwvu : severity_level;
  signal lzworqkjj : std_logic_vector(0 to 2);
  signal u : severity_level;
  signal jbodpbgu : time;
begin
  npcyafjtw : entity work.gxwveeds
    port map (kachmf => jbodpbgu, w => u, da => lzworqkjj);
  zejaxiiiuh : entity work.gxwveeds
    port map (kachmf => jbodpbgu, w => jvwvu, da => lzworqkjj);
  x : entity work.a
    port map (uxghrlth => pja);
  c : entity work.gxwveeds
    port map (kachmf => jbodpbgu, w => k, da => lvoyzsmez);
  
  -- Single-driven assignments
  k <= ERROR;
  ouch <= 2#0.1_0_0#;
  u <= NOTE;
  jvwvu <= FAILURE;
  jbodpbgu <= 0 sec;
  
  -- Multi-driven assignments
  lvoyzsmez <= "U-L";
  hawgwo <= (others => '0');
  hawgwo <= hawgwo;
  lvoyzsmez <= ('X', 'W', 'Z');
end fwkb;

library ieee;
use ieee.std_logic_1164.all;

entity kl is
  port (itqs : linkage std_logic);
end kl;

library ieee;
use ieee.std_logic_1164.all;

architecture raq of kl is
  signal ycmqs : std_logic_vector(0 to 2);
  signal dbuckqvw : time;
  signal vohre : std_logic_vector(0 to 2);
  signal sqnwpcq : severity_level;
  signal ktorymyrx : time;
begin
  vwbu : entity work.gxwveeds
    port map (kachmf => ktorymyrx, w => sqnwpcq, da => vohre);
  hqws : entity work.gxwveeds
    port map (kachmf => dbuckqvw, w => sqnwpcq, da => ycmqs);
  
  -- Single-driven assignments
  ktorymyrx <= ktorymyrx;
  dbuckqvw <= dbuckqvw;
  sqnwpcq <= NOTE;
  
  -- Multi-driven assignments
  ycmqs <= "10W";
  ycmqs <= vohre;
  vohre <= "0HU";
end raq;



-- Seed after: 14375602668418457636,10875537289884587119
