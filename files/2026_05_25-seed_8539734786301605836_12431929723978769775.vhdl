-- Seed: 8539734786301605836,12431929723978769775

library ieee;
use ieee.std_logic_1164.all;

entity ywijvva is
  port (y : buffer time; cwhiwoonc : in real; ndxne : out std_logic; r : inout bit);
end ywijvva;



architecture gxawbehjft of ywijvva is
  
begin
  
end gxawbehjft;

library ieee;
use ieee.std_logic_1164.all;

entity delxtun is
  port (rirwbgfgoy : buffer integer; pcwoiggq : linkage real; xzhnukzal : out std_logic; jdnavz : linkage time);
end delxtun;

library ieee;
use ieee.std_logic_1164.all;

architecture usg of delxtun is
  signal rpzoajgpay : bit;
  signal nophmgvpnz : std_logic;
  signal zjbngurgo : real;
  signal pbzeiw : time;
  signal t : bit;
  signal df : time;
  signal vdpejsdkxf : bit;
  signal kurx : real;
  signal hscpggpm : time;
begin
  gkiefi : entity work.ywijvva
    port map (y => hscpggpm, cwhiwoonc => kurx, ndxne => xzhnukzal, r => vdpejsdkxf);
  q : entity work.ywijvva
    port map (y => df, cwhiwoonc => kurx, ndxne => xzhnukzal, r => t);
  sct : entity work.ywijvva
    port map (y => pbzeiw, cwhiwoonc => zjbngurgo, ndxne => nophmgvpnz, r => rpzoajgpay);
end usg;

library ieee;
use ieee.std_logic_1164.all;

entity v is
  port (iiczcdmuoa : inout time; itvxhb : in real; wg : linkage real; exhh : inout std_logic);
end v;

library ieee;
use ieee.std_logic_1164.all;

architecture rpvgpu of v is
  signal ykxviyova : bit;
  signal jbexqymzk : std_logic;
  signal i : real;
  signal wvmbnvejh : time;
  signal dmxreoqddq : bit;
  signal l : real;
begin
  pvoriz : entity work.ywijvva
    port map (y => iiczcdmuoa, cwhiwoonc => l, ndxne => exhh, r => dmxreoqddq);
  yoern : entity work.ywijvva
    port map (y => wvmbnvejh, cwhiwoonc => i, ndxne => jbexqymzk, r => ykxviyova);
end rpvgpu;



entity vsv is
  port (tr : out integer);
end vsv;

library ieee;
use ieee.std_logic_1164.all;

architecture cdzenmzs of vsv is
  signal k : bit;
  signal vecvipww : std_logic;
  signal fcgev : time;
  signal jxtisvg : bit;
  signal irg : time;
  signal ryr : bit;
  signal wlayt : real;
  signal r : time;
  signal sidyhae : bit;
  signal qhsogrejbo : std_logic;
  signal kzwmxtdaj : real;
  signal egltot : time;
begin
  xkhf : entity work.ywijvva
    port map (y => egltot, cwhiwoonc => kzwmxtdaj, ndxne => qhsogrejbo, r => sidyhae);
  tcvyam : entity work.ywijvva
    port map (y => r, cwhiwoonc => wlayt, ndxne => qhsogrejbo, r => ryr);
  bndtpg : entity work.ywijvva
    port map (y => irg, cwhiwoonc => wlayt, ndxne => qhsogrejbo, r => jxtisvg);
  x : entity work.ywijvva
    port map (y => fcgev, cwhiwoonc => kzwmxtdaj, ndxne => vecvipww, r => k);
end cdzenmzs;



-- Seed after: 12668667908065597163,12431929723978769775
