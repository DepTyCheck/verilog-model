-- Seed: 10036161607339963078,17234720251424330329

entity hikg is
  port (knca : buffer time; ktykxvxxy : linkage time);
end hikg;

architecture gcyiqcrxu of hikg is
  
begin
  -- Single-driven assignments
  knca <= 1_1 us;
end gcyiqcrxu;

entity uqyaaqb is
  port (euzlkekn : in time_vector(1 downto 1));
end uqyaaqb;

architecture mb of uqyaaqb is
  signal f : time;
  signal w : time;
  signal wyhcp : time;
  signal buchbn : time;
  signal u : time;
  signal dqkigexfo : time;
begin
  baz : entity work.hikg
    port map (knca => dqkigexfo, ktykxvxxy => u);
  umcqtasayc : entity work.hikg
    port map (knca => buchbn, ktykxvxxy => wyhcp);
  ohwppwas : entity work.hikg
    port map (knca => w, ktykxvxxy => f);
end mb;

library ieee;
use ieee.std_logic_1164.all;

entity gobd is
  port (awatbqbf : inout std_logic_vector(3 to 4));
end gobd;

architecture g of gobd is
  signal xulvnwbnpv : time_vector(1 downto 1);
begin
  zk : entity work.uqyaaqb
    port map (euzlkekn => xulvnwbnpv);
  
  -- Single-driven assignments
  xulvnwbnpv <= xulvnwbnpv;
end g;



-- Seed after: 16992368034929927744,17234720251424330329
