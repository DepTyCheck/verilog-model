-- Seed: 17744866620190472179,12143220691580258643

entity zuoc is
  port (vzftcds : buffer time; u : inout integer_vector(1 downto 3); b : in character; hbnpzvia : in time);
end zuoc;

architecture chcxpmv of zuoc is
  
begin
  -- Single-driven assignments
  vzftcds <= 4_3_0_4_0.4_1_1_4 ms;
  u <= u;
end chcxpmv;

library ieee;
use ieee.std_logic_1164.all;

entity oi is
  port (s : buffer std_logic_vector(0 to 4); xtsyqhr : in real; cd : inout std_logic);
end oi;

architecture lii of oi is
  signal uaojxum : time;
  signal tqeiqouw : character;
  signal ygluwkdwsn : integer_vector(1 downto 3);
  signal zjsqf : time;
begin
  jxmurvmmr : entity work.zuoc
    port map (vzftcds => zjsqf, u => ygluwkdwsn, b => tqeiqouw, hbnpzvia => uaojxum);
  
  -- Single-driven assignments
  tqeiqouw <= tqeiqouw;
  uaojxum <= 0_2_2_0.0_1_0 ns;
end lii;

entity pyyixzfnky is
  port (xmdecnychr : in character; tbmjqq : buffer time; yvdriv : out time);
end pyyixzfnky;

library ieee;
use ieee.std_logic_1164.all;

architecture oidfssvak of pyyixzfnky is
  signal ctb : time;
  signal kwcnjmp : integer_vector(1 downto 3);
  signal ybtovwll : time;
  signal vbauzrtof : integer_vector(1 downto 3);
  signal b : time;
  signal higmpg : std_logic;
  signal fphspklih : real;
  signal kozknb : std_logic_vector(0 to 4);
  signal bghulqzwy : time;
  signal rzhvx : character;
  signal nrhzgdrku : integer_vector(1 downto 3);
  signal k : time;
begin
  jadpuik : entity work.zuoc
    port map (vzftcds => k, u => nrhzgdrku, b => rzhvx, hbnpzvia => bghulqzwy);
  wcaaxq : entity work.oi
    port map (s => kozknb, xtsyqhr => fphspklih, cd => higmpg);
  qdlvl : entity work.zuoc
    port map (vzftcds => b, u => vbauzrtof, b => rzhvx, hbnpzvia => yvdriv);
  smxifguh : entity work.zuoc
    port map (vzftcds => ybtovwll, u => kwcnjmp, b => rzhvx, hbnpzvia => ctb);
  
  -- Single-driven assignments
  bghulqzwy <= 1 hr;
  tbmjqq <= 8#15# us;
  fphspklih <= fphspklih;
  yvdriv <= ctb;
  ctb <= tbmjqq;
  
  -- Multi-driven assignments
  kozknb <= ('L', 'W', '1', 'Z', 'L');
  kozknb <= "XHZUZ";
  higmpg <= higmpg;
end oidfssvak;

entity sebcnkh is
  port (phpwzpsoq : inout character);
end sebcnkh;

architecture lwqvcbiqe of sebcnkh is
  signal riiey : time;
  signal ocpnfazza : integer_vector(1 downto 3);
  signal m : time;
begin
  vp : entity work.zuoc
    port map (vzftcds => m, u => ocpnfazza, b => phpwzpsoq, hbnpzvia => riiey);
  
  -- Single-driven assignments
  phpwzpsoq <= 't';
  riiey <= 00034 ps;
end lwqvcbiqe;



-- Seed after: 11598721048683033661,12143220691580258643
