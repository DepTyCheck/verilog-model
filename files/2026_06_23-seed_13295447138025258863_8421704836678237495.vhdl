-- Seed: 13295447138025258863,8421704836678237495

library ieee;
use ieee.std_logic_1164.all;

entity b is
  port (degntb : buffer time; z : out std_logic_vector(4 to 2); sxokr : buffer time; izir : in std_logic_vector(1 to 3));
end b;

architecture wqy of b is
  
begin
  -- Single-driven assignments
  sxokr <= 1 hr;
  degntb <= 16#0_5_8# ms;
  
  -- Multi-driven assignments
  z <= "";
  z <= "";
end wqy;

entity vf is
  port (h : buffer integer; htlmlkud : buffer real; rssgilee : linkage string(1 downto 2));
end vf;

library ieee;
use ieee.std_logic_1164.all;

architecture imtbi of vf is
  signal qzvplg : time;
  signal kfm : std_logic_vector(4 to 2);
  signal ejfo : time;
  signal kkd : std_logic_vector(1 to 3);
  signal d : time;
  signal offnked : std_logic_vector(4 to 2);
  signal kfkbxcqwd : time;
  signal dvsvj : time;
  signal ppub : time;
  signal auglujr : std_logic_vector(1 to 3);
  signal hrn : time;
  signal gqgdfzcaey : std_logic_vector(4 to 2);
  signal kaepzdhqs : time;
begin
  fxdvyn : entity work.b
    port map (degntb => kaepzdhqs, z => gqgdfzcaey, sxokr => hrn, izir => auglujr);
  ehc : entity work.b
    port map (degntb => ppub, z => gqgdfzcaey, sxokr => dvsvj, izir => auglujr);
  spve : entity work.b
    port map (degntb => kfkbxcqwd, z => offnked, sxokr => d, izir => kkd);
  iftuliot : entity work.b
    port map (degntb => ejfo, z => kfm, sxokr => qzvplg, izir => auglujr);
  
  -- Single-driven assignments
  htlmlkud <= 2#1.1_0_1_1_1#;
  
  -- Multi-driven assignments
  auglujr <= "HZH";
  gqgdfzcaey <= "";
  kfm <= "";
  auglujr <= ('H', '1', 'U');
end imtbi;

entity mbs is
  port (czlbxsqlf : linkage boolean_vector(2 to 3));
end mbs;

architecture rjasiqs of mbs is
  
begin
  
end rjasiqs;



-- Seed after: 6499232829780733568,8421704836678237495
