-- Seed: 9394724680422200760,662889661651915549

entity puxwp is
  port (jmufkcabkg : in integer_vector(0 downto 2); otudzovxx : inout integer; skyic : linkage real; iqapydm : linkage real_vector(2 to 2));
end puxwp;

architecture wwlckkr of puxwp is
  
begin
  -- Single-driven assignments
  otudzovxx <= otudzovxx;
end wwlckkr;

library ieee;
use ieee.std_logic_1164.all;

entity kkuabp is
  port (mcmobbivm : in time; bjvznfiejg : in std_logic; gtiqj : linkage std_logic_vector(0 downto 4));
end kkuabp;

architecture rct of kkuabp is
  signal io : real_vector(2 to 2);
  signal zxhakz : real;
  signal pt : integer;
  signal act : integer_vector(0 downto 2);
  signal vrqvhi : real_vector(2 to 2);
  signal ysqmvcf : real;
  signal yux : integer;
  signal mgqaen : real_vector(2 to 2);
  signal ftzhabj : real;
  signal fwnr : integer;
  signal nofvahyb : integer_vector(0 downto 2);
begin
  hmdvftdqa : entity work.puxwp
    port map (jmufkcabkg => nofvahyb, otudzovxx => fwnr, skyic => ftzhabj, iqapydm => mgqaen);
  btyekzv : entity work.puxwp
    port map (jmufkcabkg => nofvahyb, otudzovxx => yux, skyic => ysqmvcf, iqapydm => vrqvhi);
  sm : entity work.puxwp
    port map (jmufkcabkg => act, otudzovxx => pt, skyic => zxhakz, iqapydm => io);
  
  -- Single-driven assignments
  nofvahyb <= (others => 0);
  act <= (others => 0);
end rct;



-- Seed after: 11590381410304916054,662889661651915549
