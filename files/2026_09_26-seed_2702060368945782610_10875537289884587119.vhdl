-- Seed: 2702060368945782610,10875537289884587119

entity h is
  port (ueck : linkage real_vector(1 to 0); qins : linkage time);
end h;

architecture qabzofb of h is
  
begin
  
end qabzofb;

entity r is
  port (lgybzzz : out real; oog : out character);
end r;

architecture cfqsezpq of r is
  signal llcx : time;
  signal alvviva : real_vector(1 to 0);
  signal serdwdpyvq : time;
  signal yvypwefgd : real_vector(1 to 0);
  signal fr : time;
  signal nd : real_vector(1 to 0);
  signal sejybzljmt : time;
  signal s : real_vector(1 to 0);
begin
  owpx : entity work.h
    port map (ueck => s, qins => sejybzljmt);
  dpwa : entity work.h
    port map (ueck => nd, qins => fr);
  pvtweh : entity work.h
    port map (ueck => yvypwefgd, qins => serdwdpyvq);
  rrbecujook : entity work.h
    port map (ueck => alvviva, qins => llcx);
  
  -- Single-driven assignments
  oog <= 'w';
  lgybzzz <= lgybzzz;
end cfqsezpq;



-- Seed after: 10520836565649379815,10875537289884587119
