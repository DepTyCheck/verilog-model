-- Seed: 15592890205745561718,13857275728440271305

library ieee;
use ieee.std_logic_1164.all;

entity wxvgh is
  port (glciunva : out std_logic_vector(1 downto 4); dmxrqrav : in boolean; sceiv : inout integer; rxoziub : out time);
end wxvgh;

architecture hgfgtkt of wxvgh is
  
begin
  -- Single-driven assignments
  rxoziub <= 2 sec;
  sceiv <= sceiv;
  
  -- Multi-driven assignments
  glciunva <= (others => '0');
end hgfgtkt;

entity hixhmlrotx is
  port (xv : out real_vector(3 downto 0));
end hixhmlrotx;

library ieee;
use ieee.std_logic_1164.all;

architecture thwiysqlh of hixhmlrotx is
  signal s : time;
  signal pn : integer;
  signal hzyyge : std_logic_vector(1 downto 4);
  signal fbute : time;
  signal kaaqxyf : integer;
  signal oira : time;
  signal omnpun : integer;
  signal ad : time;
  signal xszffpkxpj : integer;
  signal wpwfhs : boolean;
  signal tr : std_logic_vector(1 downto 4);
begin
  saemtrhw : entity work.wxvgh
    port map (glciunva => tr, dmxrqrav => wpwfhs, sceiv => xszffpkxpj, rxoziub => ad);
  rdcrbc : entity work.wxvgh
    port map (glciunva => tr, dmxrqrav => wpwfhs, sceiv => omnpun, rxoziub => oira);
  orpy : entity work.wxvgh
    port map (glciunva => tr, dmxrqrav => wpwfhs, sceiv => kaaqxyf, rxoziub => fbute);
  b : entity work.wxvgh
    port map (glciunva => hzyyge, dmxrqrav => wpwfhs, sceiv => pn, rxoziub => s);
  
  -- Single-driven assignments
  xv <= xv;
  wpwfhs <= FALSE;
end thwiysqlh;



-- Seed after: 2321257210850806461,13857275728440271305
