-- Seed: 17430779064247850037,6697892553037813751

library ieee;
use ieee.std_logic_1164.all;

entity tne is
  port (kvzbnvfbx : inout time; rbe : linkage bit_vector(1 to 0); tvgwr : linkage time; k : buffer std_logic);
end tne;

architecture dcgsmxfbdh of tne is
  
begin
  -- Single-driven assignments
  kvzbnvfbx <= 16#E# ps;
end dcgsmxfbdh;

library ieee;
use ieee.std_logic_1164.all;

entity osqurlqcxj is
  port (otrxdimhk : inout std_logic; zd : inout std_logic; mdfghe : linkage severity_level);
end osqurlqcxj;

library ieee;
use ieee.std_logic_1164.all;

architecture v of osqurlqcxj is
  signal qmlydcwoi : std_logic;
  signal sj : time;
  signal jts : bit_vector(1 to 0);
  signal ydftlg : time;
  signal sqfnzlzqs : time;
  signal bjjqvok : bit_vector(1 to 0);
  signal qdnrcypj : time;
  signal dvlpcydr : std_logic;
  signal ijogg : time;
  signal j : bit_vector(1 to 0);
  signal ssw : time;
  signal od : std_logic;
  signal fgag : time;
  signal cjngrg : bit_vector(1 to 0);
  signal tfq : time;
begin
  itkootwr : entity work.tne
    port map (kvzbnvfbx => tfq, rbe => cjngrg, tvgwr => fgag, k => od);
  a : entity work.tne
    port map (kvzbnvfbx => ssw, rbe => j, tvgwr => ijogg, k => dvlpcydr);
  o : entity work.tne
    port map (kvzbnvfbx => qdnrcypj, rbe => bjjqvok, tvgwr => sqfnzlzqs, k => zd);
  hkfnvvdbcf : entity work.tne
    port map (kvzbnvfbx => ydftlg, rbe => jts, tvgwr => sj, k => qmlydcwoi);
  
  -- Multi-driven assignments
  dvlpcydr <= '-';
end v;



-- Seed after: 4709555384212604655,6697892553037813751
