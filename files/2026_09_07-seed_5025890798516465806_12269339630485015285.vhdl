-- Seed: 5025890798516465806,12269339630485015285

entity nmprhm is
  port (wavgvyqvbm : linkage time; rthjkwycrv : in character);
end nmprhm;

architecture awiuxdyv of nmprhm is
  
begin
  
end awiuxdyv;

library ieee;
use ieee.std_logic_1164.all;

entity smkgsfiu is
  port (qc : out std_logic_vector(0 downto 2); fon : inout integer; djqlfhjc : in real_vector(4 to 4));
end smkgsfiu;

architecture yhqhwh of smkgsfiu is
  signal nl : character;
  signal k : time;
  signal xza : time;
  signal gpg : character;
  signal embldx : time;
  signal numpqeqhc : character;
  signal giyxn : time;
begin
  tqdfdfhgxe : entity work.nmprhm
    port map (wavgvyqvbm => giyxn, rthjkwycrv => numpqeqhc);
  gyqrqvbt : entity work.nmprhm
    port map (wavgvyqvbm => embldx, rthjkwycrv => gpg);
  ffq : entity work.nmprhm
    port map (wavgvyqvbm => xza, rthjkwycrv => numpqeqhc);
  ovuklftiat : entity work.nmprhm
    port map (wavgvyqvbm => k, rthjkwycrv => nl);
  
  -- Single-driven assignments
  fon <= fon;
  
  -- Multi-driven assignments
  qc <= "";
  qc <= qc;
end yhqhwh;

entity vxgdz is
  port (nrwml : in string(4 to 2));
end vxgdz;

library ieee;
use ieee.std_logic_1164.all;

architecture r of vxgdz is
  signal tv : time;
  signal vd : character;
  signal cjyffzs : time;
  signal a : real_vector(4 to 4);
  signal qie : integer;
  signal av : std_logic_vector(0 downto 2);
  signal ad : character;
  signal airoud : time;
begin
  zgnff : entity work.nmprhm
    port map (wavgvyqvbm => airoud, rthjkwycrv => ad);
  eisyoqo : entity work.smkgsfiu
    port map (qc => av, fon => qie, djqlfhjc => a);
  zvdxlbtg : entity work.nmprhm
    port map (wavgvyqvbm => cjyffzs, rthjkwycrv => vd);
  tvzqbkv : entity work.nmprhm
    port map (wavgvyqvbm => tv, rthjkwycrv => ad);
  
  -- Multi-driven assignments
  av <= av;
end r;



-- Seed after: 14228508366428270297,12269339630485015285
