-- Seed: 15732647664076377360,7198033922882419595

library ieee;
use ieee.std_logic_1164.all;

entity jdpttijye is
  port (saoiqto : inout integer_vector(4 to 1); lvjmgisz : out real; wq : in std_logic);
end jdpttijye;

architecture qsrcub of jdpttijye is
  
begin
  -- Single-driven assignments
  lvjmgisz <= lvjmgisz;
  saoiqto <= (others => 0);
end qsrcub;

library ieee;
use ieee.std_logic_1164.all;

entity fu is
  port (gxmje : linkage severity_level; u : in std_logic);
end fu;

library ieee;
use ieee.std_logic_1164.all;

architecture occnsh of fu is
  signal x : std_logic;
  signal n : real;
  signal pb : integer_vector(4 to 1);
  signal lmzjclz : std_logic;
  signal idvyyp : real;
  signal ixm : integer_vector(4 to 1);
  signal s : std_logic;
  signal zmaissbum : real;
  signal vcnjzos : integer_vector(4 to 1);
begin
  cdd : entity work.jdpttijye
    port map (saoiqto => vcnjzos, lvjmgisz => zmaissbum, wq => s);
  oakegwmefk : entity work.jdpttijye
    port map (saoiqto => ixm, lvjmgisz => idvyyp, wq => lmzjclz);
  lthzelnr : entity work.jdpttijye
    port map (saoiqto => pb, lvjmgisz => n, wq => x);
  
  -- Multi-driven assignments
  lmzjclz <= 'H';
end occnsh;

library ieee;
use ieee.std_logic_1164.all;

entity g is
  port (lofgmdz : linkage std_logic_vector(0 downto 1));
end g;

library ieee;
use ieee.std_logic_1164.all;

architecture kaq of g is
  signal i : real;
  signal qonl : integer_vector(4 to 1);
  signal ncwguqdw : std_logic;
  signal x : real;
  signal kyurbsb : integer_vector(4 to 1);
  signal zvg : std_logic;
  signal iqaeq : real;
  signal gyceamltp : integer_vector(4 to 1);
begin
  vhnlmiflrb : entity work.jdpttijye
    port map (saoiqto => gyceamltp, lvjmgisz => iqaeq, wq => zvg);
  tlkp : entity work.jdpttijye
    port map (saoiqto => kyurbsb, lvjmgisz => x, wq => ncwguqdw);
  xcip : entity work.jdpttijye
    port map (saoiqto => qonl, lvjmgisz => i, wq => zvg);
  
  -- Multi-driven assignments
  zvg <= zvg;
end kaq;



-- Seed after: 7186392509011670579,7198033922882419595
