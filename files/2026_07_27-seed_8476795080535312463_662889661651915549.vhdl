-- Seed: 8476795080535312463,662889661651915549

entity ivnd is
  port (dpcixooqud : out time_vector(2 downto 1); heweynmrbz : out real);
end ivnd;

architecture lwuzezyhar of ivnd is
  
begin
  
end lwuzezyhar;

library ieee;
use ieee.std_logic_1164.all;

entity kvsd is
  port (ktumy : in bit; lczefldc : in std_logic);
end kvsd;

architecture ahkqymrhi of kvsd is
  signal tfhwsum : real;
  signal kdnyxev : time_vector(2 downto 1);
  signal yuasrwc : real;
  signal viqdgsdild : time_vector(2 downto 1);
  signal ibsajhli : real;
  signal dwschrfoev : time_vector(2 downto 1);
  signal k : real;
  signal kddoetye : time_vector(2 downto 1);
begin
  lnctqxn : entity work.ivnd
    port map (dpcixooqud => kddoetye, heweynmrbz => k);
  wxftpzdhrz : entity work.ivnd
    port map (dpcixooqud => dwschrfoev, heweynmrbz => ibsajhli);
  nqr : entity work.ivnd
    port map (dpcixooqud => viqdgsdild, heweynmrbz => yuasrwc);
  wskixwys : entity work.ivnd
    port map (dpcixooqud => kdnyxev, heweynmrbz => tfhwsum);
end ahkqymrhi;



-- Seed after: 3884788687403920476,662889661651915549
