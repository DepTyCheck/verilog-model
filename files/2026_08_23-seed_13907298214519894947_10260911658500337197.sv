// Seed: 13907298214519894947,10260911658500337197

module juprizjh (output logic [1:4] zggyydf, output bit [0:3][2:2] azln);
  xnor umkv(azln, zggyydf, azln);
  // warning: implicit conversion of port connection truncates from 4 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [0:3][2:2] azln -> logic azln
  //
  // warning: implicit conversion of port connection truncates from 4 to 1 bits
  //   logic [1:4] zggyydf -> logic zggyydf
  //
  // warning: implicit conversion of port connection truncates from 4 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [0:3][2:2] azln -> logic azln
  
endmodule: juprizjh

module lzfben (input logic kgakzkpbmh [4:4], output reg k, inout tri0 logic [3:0][3:1][4:0] a);
  xnor agwxp(g, k, k);
  
  xnor lszhwcv(g, jk, k);
  
  xnor idohwnla(k, iplbvxug, vk);
  
  juprizjh wwzm(.zggyydf(wurongvlw), .azln(xjfrdiqup));
  // warning: implicit conversion of port connection expands from 1 to 4 bits
  //   wire logic wurongvlw -> logic [1:4] zggyydf
  //
  // warning: implicit conversion of port connection expands from 1 to 4 bits
  // warning: implicit conversion changes possible bit states from 4-state to 2-state
  //   wire logic xjfrdiqup -> bit [0:3][2:2] azln
  
  
  // Multi-driven assignments
  assign vk = 'bx;
  assign a = a;
  assign vk = 'bz;
  assign a = a;
endmodule: lzfben

module pl (input reg [1:4][2:0] fkl, inout supply1 logic [4:3][2:4] ebs [2:2]);
  // Unpacked net declarations
  logic bz [4:4];
  
  and j(yi, fkl, qmn);
  // warning: implicit conversion of port connection truncates from 12 to 1 bits
  //   reg [1:4][2:0] fkl -> logic fkl
  
  and ed(x, x, x);
  
  lzfben ih(.kgakzkpbmh(bz), .k(vxdcog), .a(rpxl));
  // warning: implicit conversion of port connection expands from 1 to 60 bits
  //   wire logic rpxl -> tri0 logic [3:0][3:1][4:0] a
  
  
  // Single-driven assignments
  assign bz = '{'bx};
  
  // Multi-driven assignments
  assign qmn = rpxl;
endmodule: pl

module nccclgir (input wor logic [1:1] srj [3:1][0:1][4:0][1:0]);
  // Unpacked net declarations
  logic x [4:4];
  
  lzfben gsfyaljut(.kgakzkpbmh(x), .k(a), .a(a));
  // warning: implicit conversion of port connection expands from 1 to 60 bits
  //   wire logic a -> tri0 logic [3:0][3:1][4:0] a
  
  or xdpfsnk(tsb, ypsa, jgjsga);
  
  or vejxoatt(tsb, jgjsga, fizn);
  
  
  // Single-driven assignments
  assign x = x;
  
  // Multi-driven assignments
  assign a = tsb;
  assign ypsa = a;
  assign a = fizn;
  assign srj = srj;
endmodule: nccclgir



// Seed after: 14103076416471822857,10260911658500337197
