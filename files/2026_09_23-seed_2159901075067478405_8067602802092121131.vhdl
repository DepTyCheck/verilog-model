-- Seed: 2159901075067478405,8067602802092121131

entity u is
  port (vqhlfha : linkage real);
end u;

architecture jbqlwun of u is
  
begin
  
end jbqlwun;

library ieee;
use ieee.std_logic_1164.all;

entity e is
  port (k : inout time; dgtvonnff : buffer boolean; ii : out boolean; gk : buffer std_logic_vector(2 downto 2));
end e;

architecture ivemy of e is
  signal kmz : real;
  signal brhxwvfj : real;
  signal fcmyftsvx : real;
  signal tckgfsfqc : real;
begin
  fbruoie : entity work.u
    port map (vqhlfha => tckgfsfqc);
  ryyfkid : entity work.u
    port map (vqhlfha => fcmyftsvx);
  ffrbvcdcqf : entity work.u
    port map (vqhlfha => brhxwvfj);
  pzuq : entity work.u
    port map (vqhlfha => kmz);
  
  -- Single-driven assignments
  ii <= TRUE;
  dgtvonnff <= dgtvonnff;
  
  -- Multi-driven assignments
  gk <= gk;
  gk <= "W";
  gk <= gk;
  gk <= gk;
end ivemy;



-- Seed after: 9358520241831883000,8067602802092121131
