-- Seed: 12784809565583499282,5415160250146859793

library ieee;
use ieee.std_logic_1164.all;

entity pdbv is
  port (ooz : linkage boolean; dxp : linkage bit_vector(3 downto 4); nhsi : inout std_logic; cxluexrvh : buffer severity_level);
end pdbv;



architecture ygfihbicnk of pdbv is
  
begin
  
end ygfihbicnk;



entity cqdaiymh is
  port (ppdywhnu : out real_vector(2 to 0); yi : out time);
end cqdaiymh;

library ieee;
use ieee.std_logic_1164.all;

architecture zxvlral of cqdaiymh is
  signal hxevrzf : severity_level;
  signal bxknqbc : bit_vector(3 downto 4);
  signal ildcu : severity_level;
  signal gvdwtsdss : std_logic;
  signal dtamuy : bit_vector(3 downto 4);
  signal yt : boolean;
begin
  teemdsd : entity work.pdbv
    port map (ooz => yt, dxp => dtamuy, nhsi => gvdwtsdss, cxluexrvh => ildcu);
  hn : entity work.pdbv
    port map (ooz => yt, dxp => bxknqbc, nhsi => gvdwtsdss, cxluexrvh => hxevrzf);
end zxvlral;



-- Seed after: 10097537818311574232,5415160250146859793
