-- Seed: 4387809847906262438,14094562573555574003

entity daoix is
  port (mejknbt : inout integer; r : inout bit; pa : in time; olpysapmb : inout integer);
end daoix;

architecture vbfxjz of daoix is
  
begin
  -- Single-driven assignments
  r <= '0';
  olpysapmb <= 14344;
end vbfxjz;

entity neyklx is
  port (wkfmynbai : linkage integer);
end neyklx;

architecture ambepeac of neyklx is
  signal qkncl : integer;
  signal gfj : bit;
  signal ovkvceebtu : integer;
  signal xdb : integer;
  signal rk : bit;
  signal gcm : integer;
  signal gravyqochz : integer;
  signal eaph : bit;
  signal f : integer;
  signal hqeu : integer;
  signal w : time;
  signal tuepokf : bit;
  signal i : integer;
begin
  vwlhhmy : entity work.daoix
    port map (mejknbt => i, r => tuepokf, pa => w, olpysapmb => hqeu);
  wtuplng : entity work.daoix
    port map (mejknbt => f, r => eaph, pa => w, olpysapmb => gravyqochz);
  picd : entity work.daoix
    port map (mejknbt => gcm, r => rk, pa => w, olpysapmb => xdb);
  naoako : entity work.daoix
    port map (mejknbt => ovkvceebtu, r => gfj, pa => w, olpysapmb => qkncl);
  
  -- Single-driven assignments
  w <= w;
end ambepeac;



-- Seed after: 16248697376558080527,14094562573555574003
