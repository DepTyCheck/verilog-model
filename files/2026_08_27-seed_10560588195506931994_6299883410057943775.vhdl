-- Seed: 10560588195506931994,6299883410057943775

entity z is
  port (vymx : linkage real; obq : in bit; npizie : in severity_level);
end z;

architecture srheyjn of z is
  
begin
  
end srheyjn;

library ieee;
use ieee.std_logic_1164.all;

entity omfy is
  port (ovqcus : linkage integer; bnwv : out time; lufsbirdkn : in std_logic_vector(3 downto 1));
end omfy;

architecture wswd of omfy is
  signal bxol : severity_level;
  signal ckxudyclf : real;
  signal plufust : severity_level;
  signal tszubiqmvp : bit;
  signal jjrvysozi : real;
begin
  zyikpcciky : entity work.z
    port map (vymx => jjrvysozi, obq => tszubiqmvp, npizie => plufust);
  lcmgujnzeq : entity work.z
    port map (vymx => ckxudyclf, obq => tszubiqmvp, npizie => bxol);
  
  -- Single-driven assignments
  bxol <= FAILURE;
  tszubiqmvp <= tszubiqmvp;
  bnwv <= bnwv;
  plufust <= NOTE;
end wswd;

entity anopb is
  port (dbnrjqe : out boolean_vector(0 downto 3));
end anopb;

architecture gqzylpnd of anopb is
  signal oudlj : severity_level;
  signal zadzdnqhv : real;
  signal epwida : severity_level;
  signal q : bit;
  signal cwwtwwepd : real;
begin
  xlyqe : entity work.z
    port map (vymx => cwwtwwepd, obq => q, npizie => epwida);
  n : entity work.z
    port map (vymx => zadzdnqhv, obq => q, npizie => oudlj);
  
  -- Single-driven assignments
  epwida <= FAILURE;
  dbnrjqe <= dbnrjqe;
  q <= '1';
end gqzylpnd;

library ieee;
use ieee.std_logic_1164.all;

entity ux is
  port (iruhtqo : inout bit_vector(0 downto 4); yeyghsk : buffer std_logic; awalz : in severity_level);
end ux;

architecture ktiklpmkhp of ux is
  signal duyl : severity_level;
  signal ljyhrj : bit;
  signal slzlppnt : real;
  signal yixa : bit;
  signal une : real;
  signal jkd : bit;
  signal crjpdvap : real;
begin
  v : entity work.z
    port map (vymx => crjpdvap, obq => jkd, npizie => awalz);
  ze : entity work.z
    port map (vymx => une, obq => yixa, npizie => awalz);
  f : entity work.z
    port map (vymx => slzlppnt, obq => ljyhrj, npizie => duyl);
  
  -- Single-driven assignments
  jkd <= '0';
  
  -- Multi-driven assignments
  yeyghsk <= 'H';
  yeyghsk <= yeyghsk;
  yeyghsk <= 'Z';
  yeyghsk <= yeyghsk;
end ktiklpmkhp;



-- Seed after: 18178481423347966087,6299883410057943775
