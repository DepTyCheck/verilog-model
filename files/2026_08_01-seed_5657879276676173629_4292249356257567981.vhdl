-- Seed: 5657879276676173629,4292249356257567981

library ieee;
use ieee.std_logic_1164.all;

entity opjc is
  port (ffrfgnuw : in integer; uywasvs : inout boolean_vector(1 to 4); vyqz : buffer std_logic_vector(4 to 3); wzv : inout severity_level);
end opjc;

architecture hnfnr of opjc is
  
begin
  -- Multi-driven assignments
  vyqz <= "";
  vyqz <= vyqz;
end hnfnr;

library ieee;
use ieee.std_logic_1164.all;

entity oa is
  port (whudsn : in character; dz : in time; kcyuovofnp : linkage std_logic_vector(0 to 0));
end oa;

library ieee;
use ieee.std_logic_1164.all;

architecture n of oa is
  signal nlr : severity_level;
  signal pme : std_logic_vector(4 to 3);
  signal tfjacjki : boolean_vector(1 to 4);
  signal h : severity_level;
  signal trzbnktj : boolean_vector(1 to 4);
  signal xmqzhiyrfh : integer;
  signal czdvbedkq : severity_level;
  signal szcpue : std_logic_vector(4 to 3);
  signal wcchophpz : boolean_vector(1 to 4);
  signal xoaaqt : integer;
begin
  kejscef : entity work.opjc
    port map (ffrfgnuw => xoaaqt, uywasvs => wcchophpz, vyqz => szcpue, wzv => czdvbedkq);
  zsubqh : entity work.opjc
    port map (ffrfgnuw => xmqzhiyrfh, uywasvs => trzbnktj, vyqz => szcpue, wzv => h);
  ieyuzozxx : entity work.opjc
    port map (ffrfgnuw => xoaaqt, uywasvs => tfjacjki, vyqz => pme, wzv => nlr);
  
  -- Single-driven assignments
  xoaaqt <= xoaaqt;
  
  -- Multi-driven assignments
  szcpue <= szcpue;
  szcpue <= szcpue;
end n;

entity euav is
  port (znytuopd : buffer integer);
end euav;

library ieee;
use ieee.std_logic_1164.all;

architecture rfu of euav is
  signal jgrnrop : time;
  signal dypygdw : character;
  signal e : std_logic_vector(0 to 0);
  signal x : time;
  signal ukluewbu : character;
  signal hz : severity_level;
  signal b : std_logic_vector(4 to 3);
  signal qqeohhun : boolean_vector(1 to 4);
  signal ihayrrzuip : integer;
  signal gfapt : severity_level;
  signal wn : std_logic_vector(4 to 3);
  signal koe : boolean_vector(1 to 4);
begin
  mmjbgnxb : entity work.opjc
    port map (ffrfgnuw => znytuopd, uywasvs => koe, vyqz => wn, wzv => gfapt);
  wjvbf : entity work.opjc
    port map (ffrfgnuw => ihayrrzuip, uywasvs => qqeohhun, vyqz => b, wzv => hz);
  csboym : entity work.oa
    port map (whudsn => ukluewbu, dz => x, kcyuovofnp => e);
  fan : entity work.oa
    port map (whudsn => dypygdw, dz => jgrnrop, kcyuovofnp => e);
  
  -- Multi-driven assignments
  wn <= (others => '0');
  wn <= wn;
  b <= (others => '0');
end rfu;



-- Seed after: 2195113170867985323,4292249356257567981
