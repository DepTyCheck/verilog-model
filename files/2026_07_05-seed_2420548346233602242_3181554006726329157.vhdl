-- Seed: 2420548346233602242,3181554006726329157

use std.reflection.all;

entity zwhbg is
  port (cvvdlkaix : inout time; tgueiqku : inout array_value_mirror; kw : inout time; yyp : out integer_vector(1 downto 1));
end zwhbg;

architecture l of zwhbg is
  
begin
  -- Single-driven assignments
  yyp <= yyp;
  kw <= 13 ms;
  cvvdlkaix <= 1 hr;
end l;

use std.reflection.all;

entity sjnxlpsf is
  port ( figsh : inout physical_subtype_mirror
  ; qbixts : linkage bit_vector(4 to 0)
  ; misoxdigj : inout array_subtype_mirror
  ; l : buffer time_vector(0 to 3)
  );
end sjnxlpsf;

use std.reflection.all;

architecture dkj of sjnxlpsf is
  signal iauepoxuxx : integer_vector(1 downto 1);
  signal ts : time;
  shared variable zdqjel : array_value_mirror;
  signal dj : time;
  signal ahgq : integer_vector(1 downto 1);
  signal igakg : time;
  shared variable iphdl : array_value_mirror;
  signal zcvuxxmz : time;
  signal csfstneoo : integer_vector(1 downto 1);
  signal etitth : time;
  shared variable okfxlhzl : array_value_mirror;
  signal p : time;
  signal fezk : integer_vector(1 downto 1);
  signal lbbodkjba : time;
  shared variable xjnlyfuu : array_value_mirror;
  signal xpv : time;
begin
  sqqonta : entity work.zwhbg
    port map (cvvdlkaix => xpv, tgueiqku => xjnlyfuu, kw => lbbodkjba, yyp => fezk);
  ht : entity work.zwhbg
    port map (cvvdlkaix => p, tgueiqku => okfxlhzl, kw => etitth, yyp => csfstneoo);
  jygzf : entity work.zwhbg
    port map (cvvdlkaix => zcvuxxmz, tgueiqku => iphdl, kw => igakg, yyp => ahgq);
  nihtwnzocs : entity work.zwhbg
    port map (cvvdlkaix => dj, tgueiqku => zdqjel, kw => ts, yyp => iauepoxuxx);
  
  -- Single-driven assignments
  l <= l;
end dkj;

use std.reflection.all;

entity tyjxx is
  port (dtgozbggf : out character; omacsrwt : inout record_subtype_mirror);
end tyjxx;

use std.reflection.all;

architecture iadxogvssk of tyjxx is
  signal dhyjdiwl : integer_vector(1 downto 1);
  signal vdzdskhtn : time;
  shared variable abspfakjve : array_value_mirror;
  signal p : time;
  signal qdxc : integer_vector(1 downto 1);
  signal zo : time;
  shared variable ivk : array_value_mirror;
  signal l : time;
  signal ltw : integer_vector(1 downto 1);
  signal mqhq : time;
  shared variable aqblvqadtv : array_value_mirror;
  signal urlr : time;
  signal eqqvqcc : integer_vector(1 downto 1);
  signal vjwr : time;
  shared variable zqhilulpf : array_value_mirror;
  signal rhmjuo : time;
begin
  gfjqciqhvo : entity work.zwhbg
    port map (cvvdlkaix => rhmjuo, tgueiqku => zqhilulpf, kw => vjwr, yyp => eqqvqcc);
  tbkpwuvng : entity work.zwhbg
    port map (cvvdlkaix => urlr, tgueiqku => aqblvqadtv, kw => mqhq, yyp => ltw);
  lsqzmpfuq : entity work.zwhbg
    port map (cvvdlkaix => l, tgueiqku => ivk, kw => zo, yyp => qdxc);
  mqdfviwm : entity work.zwhbg
    port map (cvvdlkaix => p, tgueiqku => abspfakjve, kw => vdzdskhtn, yyp => dhyjdiwl);
  
  -- Single-driven assignments
  dtgozbggf <= dtgozbggf;
end iadxogvssk;

use std.reflection.all;

entity pznaogzb is
  port (yddiepd : inout enumeration_subtype_mirror);
end pznaogzb;

use std.reflection.all;

architecture vutzrqu of pznaogzb is
  signal ugfgwky : integer_vector(1 downto 1);
  signal ou : time;
  shared variable lqwk : array_value_mirror;
  signal xwia : time;
  signal spoixdbo : integer_vector(1 downto 1);
  signal jcnlngjm : time;
  shared variable gaozjafa : array_value_mirror;
  signal nnm : time;
  signal cxdcrvtjyw : time_vector(0 to 3);
  shared variable tfuxh : array_subtype_mirror;
  signal jf : bit_vector(4 to 0);
  shared variable luxbiniwr : physical_subtype_mirror;
  signal iydkorqw : integer_vector(1 downto 1);
  signal tmjiufu : time;
  shared variable vmnplcv : array_value_mirror;
  signal bqfq : time;
begin
  smawkrw : entity work.zwhbg
    port map (cvvdlkaix => bqfq, tgueiqku => vmnplcv, kw => tmjiufu, yyp => iydkorqw);
  ibhgge : entity work.sjnxlpsf
    port map (figsh => luxbiniwr, qbixts => jf, misoxdigj => tfuxh, l => cxdcrvtjyw);
  hbpsvezd : entity work.zwhbg
    port map (cvvdlkaix => nnm, tgueiqku => gaozjafa, kw => jcnlngjm, yyp => spoixdbo);
  bzjxmu : entity work.zwhbg
    port map (cvvdlkaix => xwia, tgueiqku => lqwk, kw => ou, yyp => ugfgwky);
end vutzrqu;



-- Seed after: 5649490692817187558,3181554006726329157
