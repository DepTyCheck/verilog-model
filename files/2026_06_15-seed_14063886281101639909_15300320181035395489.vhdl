-- Seed: 14063886281101639909,15300320181035395489

entity qp is
  port (svcmkp : inout real; lk : inout boolean; qzyhr : in character);
end qp;

architecture xft of qp is
  
begin
  -- Single-driven assignments
  lk <= TRUE;
  svcmkp <= 16#0_9_E_3_8.CCD#;
end xft;

entity alh is
  port (exocjbxk : inout boolean);
end alh;

architecture x of alh is
  signal uk : character;
  signal ruqtme : boolean;
  signal k : real;
  signal lhctqt : character;
  signal bnjvqz : boolean;
  signal ziwtrtx : real;
  signal gzwt : character;
  signal ivpj : boolean;
  signal eqxfcwnmpc : real;
begin
  l : entity work.qp
    port map (svcmkp => eqxfcwnmpc, lk => ivpj, qzyhr => gzwt);
  ssgcv : entity work.qp
    port map (svcmkp => ziwtrtx, lk => bnjvqz, qzyhr => lhctqt);
  lewuqraf : entity work.qp
    port map (svcmkp => k, lk => ruqtme, qzyhr => uk);
  
  -- Single-driven assignments
  exocjbxk <= TRUE;
  lhctqt <= 'e';
  gzwt <= 't';
end x;

entity uefr is
  port (hyywlt : in time_vector(3 to 3));
end uefr;

architecture zalxbe of uefr is
  signal oxw : character;
  signal pysplpct : boolean;
  signal sqsht : real;
begin
  badc : entity work.qp
    port map (svcmkp => sqsht, lk => pysplpct, qzyhr => oxw);
  
  -- Single-driven assignments
  oxw <= 'a';
end zalxbe;



-- Seed after: 4190172080447889525,15300320181035395489
