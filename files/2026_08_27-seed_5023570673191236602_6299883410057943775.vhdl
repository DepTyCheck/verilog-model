-- Seed: 5023570673191236602,6299883410057943775

library ieee;
use ieee.std_logic_1164.all;

entity mpwgkdrq is
  port (q : in time; gtympbblts : out integer; fqqtyehd : inout severity_level; ashwyl : out std_logic_vector(4 to 0));
end mpwgkdrq;

architecture yicdiyate of mpwgkdrq is
  
begin
  -- Single-driven assignments
  fqqtyehd <= WARNING;
  gtympbblts <= 8#2#;
  
  -- Multi-driven assignments
  ashwyl <= "";
end yicdiyate;

entity hpcrwicgvs is
  port (mhimlo : buffer time_vector(3 to 0));
end hpcrwicgvs;

library ieee;
use ieee.std_logic_1164.all;

architecture s of hpcrwicgvs is
  signal b : std_logic_vector(4 to 0);
  signal tvlf : severity_level;
  signal mwquj : integer;
  signal zri : severity_level;
  signal xprqu : integer;
  signal nowilbg : std_logic_vector(4 to 0);
  signal gdnrqx : severity_level;
  signal z : integer;
  signal awonpbwccv : time;
  signal beiobogc : std_logic_vector(4 to 0);
  signal gzdjxl : severity_level;
  signal livhmlrtbe : integer;
  signal lac : time;
begin
  degt : entity work.mpwgkdrq
    port map (q => lac, gtympbblts => livhmlrtbe, fqqtyehd => gzdjxl, ashwyl => beiobogc);
  msrckednkf : entity work.mpwgkdrq
    port map (q => awonpbwccv, gtympbblts => z, fqqtyehd => gdnrqx, ashwyl => nowilbg);
  owjuo : entity work.mpwgkdrq
    port map (q => lac, gtympbblts => xprqu, fqqtyehd => zri, ashwyl => nowilbg);
  asxbp : entity work.mpwgkdrq
    port map (q => lac, gtympbblts => mwquj, fqqtyehd => tvlf, ashwyl => b);
  
  -- Single-driven assignments
  awonpbwccv <= lac;
  mhimlo <= mhimlo;
  lac <= 16#1_9_2# ps;
  
  -- Multi-driven assignments
  beiobogc <= (others => '0');
  b <= "";
end s;

library ieee;
use ieee.std_logic_1164.all;

entity piygnm is
  port (yrtvpmkv : buffer std_logic; scwusbr : buffer real; tkfsozugy : in severity_level; zhii : buffer time_vector(4 to 0));
end piygnm;

library ieee;
use ieee.std_logic_1164.all;

architecture zvlhoii of piygnm is
  signal qtzx : severity_level;
  signal qnpkog : integer;
  signal awfxg : time;
  signal mxudccp : std_logic_vector(4 to 0);
  signal vfjhegf : severity_level;
  signal ihmblyzxze : integer;
  signal mmsrjee : time;
begin
  bqc : entity work.mpwgkdrq
    port map (q => mmsrjee, gtympbblts => ihmblyzxze, fqqtyehd => vfjhegf, ashwyl => mxudccp);
  anvkyyjm : entity work.mpwgkdrq
    port map (q => awfxg, gtympbblts => qnpkog, fqqtyehd => qtzx, ashwyl => mxudccp);
  
  -- Single-driven assignments
  awfxg <= mmsrjee;
  mmsrjee <= awfxg;
  zhii <= zhii;
  scwusbr <= scwusbr;
  
  -- Multi-driven assignments
  yrtvpmkv <= yrtvpmkv;
end zvlhoii;



-- Seed after: 17129722685233095634,6299883410057943775
