-- Seed: 11500352266184169740,3566912872917928779

use std.reflection.all;

entity vx is
  port (hrcim : inout bit_vector(0 downto 0); wsijcurkhi : linkage real; t : buffer real; pmkodtczf : inout integer_subtype_mirror);
end vx;

architecture szhvoarzvx of vx is
  
begin
  -- Single-driven assignments
  t <= t;
  hrcim <= (others => '0');
end szhvoarzvx;

use std.reflection.all;

entity ntkydzp is
  port (bogm : out time; xt : inout floating_subtype_mirror; ymat : inout floating_value_mirror);
end ntkydzp;

use std.reflection.all;

architecture ghbetz of ntkydzp is
  shared variable ptzaxomco : integer_subtype_mirror;
  signal e : real;
  signal wpnqzxwrx : real;
  signal akqwg : bit_vector(0 downto 0);
  shared variable ajikze : integer_subtype_mirror;
  signal nd : real;
  signal dqkwfvbt : real;
  signal dycuclj : bit_vector(0 downto 0);
begin
  h : entity work.vx
    port map (hrcim => dycuclj, wsijcurkhi => dqkwfvbt, t => nd, pmkodtczf => ajikze);
  wc : entity work.vx
    port map (hrcim => akqwg, wsijcurkhi => wpnqzxwrx, t => e, pmkodtczf => ptzaxomco);
  
  -- Single-driven assignments
  bogm <= 01433 ps;
end ghbetz;



-- Seed after: 11279886669471103342,3566912872917928779
