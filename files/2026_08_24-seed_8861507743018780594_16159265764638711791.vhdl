-- Seed: 8861507743018780594,16159265764638711791

entity wkns is
  port (xnr : inout real; jdhweshsua : buffer time; kcvigctudy : buffer bit);
end wkns;

architecture ysp of wkns is
  
begin
  -- Single-driven assignments
  jdhweshsua <= 4 sec;
end ysp;

library ieee;
use ieee.std_logic_1164.all;

entity zy is
  port (zcrswppnlk : out real; dprlmg : linkage time; ieluh : linkage std_logic);
end zy;

architecture gsdkytz of zy is
  signal uc : bit;
  signal xnrix : time;
  signal ladks : real;
  signal ciy : bit;
  signal boyeyiy : time;
  signal zxcmtoi : bit;
  signal q : time;
  signal kjzuj : real;
begin
  w : entity work.wkns
    port map (xnr => kjzuj, jdhweshsua => q, kcvigctudy => zxcmtoi);
  i : entity work.wkns
    port map (xnr => zcrswppnlk, jdhweshsua => boyeyiy, kcvigctudy => ciy);
  idrwopie : entity work.wkns
    port map (xnr => ladks, jdhweshsua => xnrix, kcvigctudy => uc);
end gsdkytz;

library ieee;
use ieee.std_logic_1164.all;

entity l is
  port (cbbiappr : out std_logic_vector(3 downto 4); hc : in real; lub : in real_vector(1 to 1));
end l;

architecture ojbmda of l is
  signal qhl : bit;
  signal seaedm : time;
  signal m : real;
  signal vwfaevdp : bit;
  signal rxz : time;
  signal gm : real;
  signal jovbndx : bit;
  signal ezol : time;
  signal anfe : real;
  signal gei : bit;
  signal fpwaggeurx : time;
  signal pfetswbqr : real;
begin
  wthrzys : entity work.wkns
    port map (xnr => pfetswbqr, jdhweshsua => fpwaggeurx, kcvigctudy => gei);
  qed : entity work.wkns
    port map (xnr => anfe, jdhweshsua => ezol, kcvigctudy => jovbndx);
  orflgukft : entity work.wkns
    port map (xnr => gm, jdhweshsua => rxz, kcvigctudy => vwfaevdp);
  ivzbr : entity work.wkns
    port map (xnr => m, jdhweshsua => seaedm, kcvigctudy => qhl);
end ojbmda;



-- Seed after: 16181399084388998807,16159265764638711791
