-- Seed: 880193698230857833,10875537289884587119

entity r is
  port (wu : in real; i : inout bit_vector(3 to 2); tkdwkfayyv : in character; dkyemjvl : inout boolean);
end r;

architecture pidbbi of r is
  
begin
  
end pidbbi;

entity pbkr is
  port (qdfswoox : inout time);
end pbkr;

architecture tbrz of pbkr is
  signal jeurbw : boolean;
  signal pwnv : character;
  signal uqqsv : bit_vector(3 to 2);
  signal zdgdkn : real;
  signal gn : boolean;
  signal mgdwdnqhcm : character;
  signal qhfetrqp : bit_vector(3 to 2);
  signal ebbtdi : real;
begin
  fuxrkk : entity work.r
    port map (wu => ebbtdi, i => qhfetrqp, tkdwkfayyv => mgdwdnqhcm, dkyemjvl => gn);
  nedsxz : entity work.r
    port map (wu => zdgdkn, i => uqqsv, tkdwkfayyv => pwnv, dkyemjvl => jeurbw);
  
  -- Single-driven assignments
  qdfswoox <= 3 min;
  mgdwdnqhcm <= mgdwdnqhcm;
  ebbtdi <= 2#10101.1#;
end tbrz;

library ieee;
use ieee.std_logic_1164.all;

entity tfwqjpmq is
  port (qrhevjh : out severity_level; pvqfkirsdt : buffer std_logic; lxbq : linkage std_logic_vector(4 to 3); fqjf : buffer real);
end tfwqjpmq;

architecture tc of tfwqjpmq is
  signal xnbgbhin : boolean;
  signal ycmkgvdr : character;
  signal d : bit_vector(3 to 2);
  signal kstysbhy : boolean;
  signal przw : character;
  signal stqobeiuxr : bit_vector(3 to 2);
  signal xyyc : real;
  signal keff : boolean;
  signal mzdfahg : character;
  signal mqujo : bit_vector(3 to 2);
  signal klrcabyje : boolean;
  signal vtqdysxgo : character;
  signal l : bit_vector(3 to 2);
  signal wftv : real;
begin
  kwqhpju : entity work.r
    port map (wu => wftv, i => l, tkdwkfayyv => vtqdysxgo, dkyemjvl => klrcabyje);
  ppsxxadt : entity work.r
    port map (wu => fqjf, i => mqujo, tkdwkfayyv => mzdfahg, dkyemjvl => keff);
  sutuk : entity work.r
    port map (wu => xyyc, i => stqobeiuxr, tkdwkfayyv => przw, dkyemjvl => kstysbhy);
  ldzi : entity work.r
    port map (wu => xyyc, i => d, tkdwkfayyv => ycmkgvdr, dkyemjvl => xnbgbhin);
  
  -- Single-driven assignments
  qrhevjh <= qrhevjh;
  fqjf <= 8#0_1.51573#;
  
  -- Multi-driven assignments
  pvqfkirsdt <= 'W';
  pvqfkirsdt <= '1';
end tc;

entity rkip is
  port (ga : in character; p : linkage string(1 to 3); thfoz : linkage integer; flf : out integer_vector(0 to 2));
end rkip;

library ieee;
use ieee.std_logic_1164.all;

architecture edlntvdrco of rkip is
  signal uhjfetb : time;
  signal gmlcv : real;
  signal gvmuzjgxto : std_logic_vector(4 to 3);
  signal ihnx : std_logic;
  signal qa : severity_level;
  signal tbjgvy : real;
  signal dfvxdxu : std_logic_vector(4 to 3);
  signal pkznad : std_logic;
  signal ngxmrgv : severity_level;
begin
  vrxlkrmr : entity work.tfwqjpmq
    port map (qrhevjh => ngxmrgv, pvqfkirsdt => pkznad, lxbq => dfvxdxu, fqjf => tbjgvy);
  nsiqk : entity work.tfwqjpmq
    port map (qrhevjh => qa, pvqfkirsdt => ihnx, lxbq => gvmuzjgxto, fqjf => gmlcv);
  mbh : entity work.pbkr
    port map (qdfswoox => uhjfetb);
  
  -- Single-driven assignments
  flf <= flf;
  
  -- Multi-driven assignments
  pkznad <= 'H';
  pkznad <= 'W';
end edlntvdrco;



-- Seed after: 3524944809614843718,10875537289884587119
