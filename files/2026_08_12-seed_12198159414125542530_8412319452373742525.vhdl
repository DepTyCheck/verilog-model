-- Seed: 12198159414125542530,8412319452373742525

library ieee;
use ieee.std_logic_1164.all;

entity kc is
  port (yjfujneem : out boolean; byekeha : buffer integer; v : out std_logic_vector(4 to 2));
end kc;

architecture sawrkifvyn of kc is
  
begin
  -- Single-driven assignments
  byekeha <= byekeha;
  yjfujneem <= yjfujneem;
  
  -- Multi-driven assignments
  v <= v;
  v <= "";
  v <= "";
end sawrkifvyn;

entity m is
  port (memtlrhpw : inout integer; ysd : out real; xhpdboh : inout character);
end m;

library ieee;
use ieee.std_logic_1164.all;

architecture lgughc of m is
  signal ioouoefhs : std_logic_vector(4 to 2);
  signal t : integer;
  signal kx : boolean;
  signal cyf : boolean;
  signal i : std_logic_vector(4 to 2);
  signal uttbdykvmp : integer;
  signal esjh : boolean;
begin
  uid : entity work.kc
    port map (yjfujneem => esjh, byekeha => uttbdykvmp, v => i);
  biv : entity work.kc
    port map (yjfujneem => cyf, byekeha => memtlrhpw, v => i);
  hfdxwfz : entity work.kc
    port map (yjfujneem => kx, byekeha => t, v => ioouoefhs);
  
  -- Multi-driven assignments
  i <= (others => '0');
  i <= i;
end lgughc;

library ieee;
use ieee.std_logic_1164.all;

entity hhxht is
  port (c : inout std_logic_vector(4 downto 2); i : in integer; bdcv : buffer std_logic; kacb : buffer string(5 to 1));
end hhxht;

architecture ctyqdy of hhxht is
  
begin
  -- Single-driven assignments
  kacb <= kacb;
  
  -- Multi-driven assignments
  c <= c;
  bdcv <= bdcv;
  bdcv <= bdcv;
end ctyqdy;

entity rcazmjj is
  port (fdrv : out real);
end rcazmjj;

library ieee;
use ieee.std_logic_1164.all;

architecture cxpzm of rcazmjj is
  signal ytc : character;
  signal zsjlwa : real;
  signal tmotsh : integer;
  signal aolr : std_logic_vector(4 to 2);
  signal nrwxq : boolean;
  signal ti : string(5 to 1);
  signal zb : std_logic;
  signal rvpwxuvf : integer;
  signal urazhyo : std_logic_vector(4 downto 2);
begin
  gesvo : entity work.hhxht
    port map (c => urazhyo, i => rvpwxuvf, bdcv => zb, kacb => ti);
  ugervxtadc : entity work.kc
    port map (yjfujneem => nrwxq, byekeha => rvpwxuvf, v => aolr);
  cn : entity work.m
    port map (memtlrhpw => tmotsh, ysd => zsjlwa, xhpdboh => ytc);
  
  -- Single-driven assignments
  fdrv <= fdrv;
end cxpzm;



-- Seed after: 17031681285151531719,8412319452373742525
