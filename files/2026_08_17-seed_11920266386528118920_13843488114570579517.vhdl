-- Seed: 11920266386528118920,13843488114570579517

library ieee;
use ieee.std_logic_1164.all;

entity qzgreln is
  port (cukmo : out bit_vector(3 to 0); bfvktpfn : linkage std_logic_vector(4 to 2); fvb : linkage real; vefppg : buffer integer);
end qzgreln;

architecture gxaeumb of qzgreln is
  
begin
  -- Single-driven assignments
  vefppg <= 16#D_6_5_A#;
  cukmo <= (others => '0');
end gxaeumb;

library ieee;
use ieee.std_logic_1164.all;

entity bchf is
  port (qhlwizoy : out std_logic_vector(1 downto 3));
end bchf;

library ieee;
use ieee.std_logic_1164.all;

architecture okfmc of bchf is
  signal mjzmdfnbtb : integer;
  signal olzv : real;
  signal vfhdmf : std_logic_vector(4 to 2);
  signal wmfrdrii : bit_vector(3 to 0);
  signal iagw : integer;
  signal cuembxw : real;
  signal dsbfaobd : bit_vector(3 to 0);
  signal g : integer;
  signal uhcplwsqf : real;
  signal feussh : std_logic_vector(4 to 2);
  signal lnimafodyd : bit_vector(3 to 0);
  signal lkhpgdft : integer;
  signal nmjlzvsh : real;
  signal pyqshdb : bit_vector(3 to 0);
begin
  rje : entity work.qzgreln
    port map (cukmo => pyqshdb, bfvktpfn => qhlwizoy, fvb => nmjlzvsh, vefppg => lkhpgdft);
  idbs : entity work.qzgreln
    port map (cukmo => lnimafodyd, bfvktpfn => feussh, fvb => uhcplwsqf, vefppg => g);
  jwiqnds : entity work.qzgreln
    port map (cukmo => dsbfaobd, bfvktpfn => qhlwizoy, fvb => cuembxw, vefppg => iagw);
  xnaadj : entity work.qzgreln
    port map (cukmo => wmfrdrii, bfvktpfn => vfhdmf, fvb => olzv, vefppg => mjzmdfnbtb);
  
  -- Multi-driven assignments
  qhlwizoy <= qhlwizoy;
  feussh <= qhlwizoy;
  feussh <= qhlwizoy;
  vfhdmf <= qhlwizoy;
end okfmc;

entity dtjbhje is
  port (yzmsg : buffer string(1 downto 4); eivpthxkf : linkage character);
end dtjbhje;

library ieee;
use ieee.std_logic_1164.all;

architecture juhfdsxf of dtjbhje is
  signal joclsfwyti : std_logic_vector(1 downto 3);
begin
  ke : entity work.bchf
    port map (qhlwizoy => joclsfwyti);
  
  -- Multi-driven assignments
  joclsfwyti <= joclsfwyti;
  joclsfwyti <= (others => '0');
  joclsfwyti <= (others => '0');
end juhfdsxf;

library ieee;
use ieee.std_logic_1164.all;

entity vzlti is
  port (annqhjtobz : buffer std_logic; adlnw : inout boolean; rrsx : inout std_logic);
end vzlti;

library ieee;
use ieee.std_logic_1164.all;

architecture yaxrxwufks of vzlti is
  signal kbyye : integer;
  signal kglikp : real;
  signal uydyithwku : bit_vector(3 to 0);
  signal vebjv : integer;
  signal c : real;
  signal otca : std_logic_vector(4 to 2);
  signal ruernzib : bit_vector(3 to 0);
begin
  cbic : entity work.qzgreln
    port map (cukmo => ruernzib, bfvktpfn => otca, fvb => c, vefppg => vebjv);
  zatdyllo : entity work.qzgreln
    port map (cukmo => uydyithwku, bfvktpfn => otca, fvb => kglikp, vefppg => kbyye);
  
  -- Multi-driven assignments
  annqhjtobz <= rrsx;
  annqhjtobz <= 'H';
  rrsx <= '1';
  otca <= otca;
end yaxrxwufks;



-- Seed after: 7781109169589465659,13843488114570579517
