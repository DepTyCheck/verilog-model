-- Seed: 17181786855616427930,13854332967471039201

library ieee;
use ieee.std_logic_1164.all;

entity oownoyppd is
  port (yu : linkage time; aqxij : linkage std_logic; og : inout std_logic_vector(2 downto 1); ellkourp : out std_logic);
end oownoyppd;



architecture xzzyuxut of oownoyppd is
  
begin
  
end xzzyuxut;



entity filhbrl is
  port (adtskjtcq : in time);
end filhbrl;

library ieee;
use ieee.std_logic_1164.all;

architecture rabdezsi of filhbrl is
  signal pf : std_logic;
  signal egm : std_logic_vector(2 downto 1);
  signal mktlf : std_logic_vector(2 downto 1);
  signal yrusztae : std_logic;
  signal gruym : time;
  signal vta : std_logic_vector(2 downto 1);
  signal pmdmibp : std_logic;
  signal seyhxssl : time;
begin
  izyjrmy : entity work.oownoyppd
    port map (yu => seyhxssl, aqxij => pmdmibp, og => vta, ellkourp => pmdmibp);
  lmf : entity work.oownoyppd
    port map (yu => gruym, aqxij => yrusztae, og => mktlf, ellkourp => pmdmibp);
  jhrrsefk : entity work.oownoyppd
    port map (yu => seyhxssl, aqxij => pmdmibp, og => egm, ellkourp => pf);
end rabdezsi;



entity dbjkdcdc is
  port (jiryza : out severity_level; rjaa : buffer integer);
end dbjkdcdc;

library ieee;
use ieee.std_logic_1164.all;

architecture w of dbjkdcdc is
  signal b : std_logic;
  signal vxrlizg : std_logic;
  signal bywrxgyec : time;
  signal liqprajbmv : std_logic;
  signal hp : std_logic_vector(2 downto 1);
  signal nrjkr : time;
  signal he : std_logic_vector(2 downto 1);
  signal zgeec : std_logic;
  signal ygbelweb : time;
begin
  myp : entity work.oownoyppd
    port map (yu => ygbelweb, aqxij => zgeec, og => he, ellkourp => zgeec);
  sjtkrxpspz : entity work.oownoyppd
    port map (yu => nrjkr, aqxij => zgeec, og => hp, ellkourp => liqprajbmv);
  odyn : entity work.filhbrl
    port map (adtskjtcq => bywrxgyec);
  rjaivxi : entity work.oownoyppd
    port map (yu => ygbelweb, aqxij => vxrlizg, og => he, ellkourp => b);
end w;



-- Seed after: 1525333286603861718,13854332967471039201
