-- Seed: 14346173125959585931,1112937151005418631

library ieee;
use ieee.std_logic_1164.all;

entity qts is
  port (pkvohglpcf : linkage integer; yvtvem : inout std_logic; tc : inout real_vector(2 downto 0));
end qts;

architecture uikfjswhm of qts is
  
begin
  -- Single-driven assignments
  tc <= (2#1.01#, 16#D_6_1_0.8CF2#, 16#95D47.9AFA#);
  
  -- Multi-driven assignments
  yvtvem <= yvtvem;
  yvtvem <= 'W';
  yvtvem <= '-';
  yvtvem <= yvtvem;
end uikfjswhm;

entity yr is
  port (l : out time);
end yr;

library ieee;
use ieee.std_logic_1164.all;

architecture mivnj of yr is
  signal cpjnyqu : real_vector(2 downto 0);
  signal fxceqom : integer;
  signal qgy : real_vector(2 downto 0);
  signal mjogbgc : std_logic;
  signal xdagvpd : integer;
begin
  e : entity work.qts
    port map (pkvohglpcf => xdagvpd, yvtvem => mjogbgc, tc => qgy);
  x : entity work.qts
    port map (pkvohglpcf => fxceqom, yvtvem => mjogbgc, tc => cpjnyqu);
  
  -- Single-driven assignments
  l <= l;
  
  -- Multi-driven assignments
  mjogbgc <= 'L';
  mjogbgc <= 'U';
  mjogbgc <= mjogbgc;
end mivnj;

library ieee;
use ieee.std_logic_1164.all;

entity zxen is
  port (dgvkpyguy : out integer; bcehv : out severity_level; mijxrmooxv : in time_vector(0 to 3); nxm : inout std_logic);
end zxen;

library ieee;
use ieee.std_logic_1164.all;

architecture gqdflzyin of zxen is
  signal ngejzv : real_vector(2 downto 0);
  signal vwo : std_logic;
  signal z : real_vector(2 downto 0);
  signal opshasltf : std_logic;
  signal asqin : integer;
  signal o : time;
begin
  fdtnwgu : entity work.yr
    port map (l => o);
  wsbhlmbumf : entity work.qts
    port map (pkvohglpcf => asqin, yvtvem => opshasltf, tc => z);
  nm : entity work.qts
    port map (pkvohglpcf => dgvkpyguy, yvtvem => vwo, tc => ngejzv);
  
  -- Single-driven assignments
  bcehv <= bcehv;
  
  -- Multi-driven assignments
  nxm <= opshasltf;
  nxm <= 'U';
  opshasltf <= opshasltf;
  nxm <= 'Z';
end gqdflzyin;

library ieee;
use ieee.std_logic_1164.all;

entity tngltr is
  port (xaagrus : linkage std_logic_vector(4 downto 1); xiofcqqdgs : in integer);
end tngltr;

library ieee;
use ieee.std_logic_1164.all;

architecture mdjfffqn of tngltr is
  signal pw : time;
  signal uyjwkmyyhk : real_vector(2 downto 0);
  signal vrfddbt : integer;
  signal l : time;
  signal wmhoeah : real_vector(2 downto 0);
  signal lznzgpsx : std_logic;
  signal mkbc : integer;
begin
  iduyo : entity work.qts
    port map (pkvohglpcf => mkbc, yvtvem => lznzgpsx, tc => wmhoeah);
  oas : entity work.yr
    port map (l => l);
  wdjtmejvc : entity work.qts
    port map (pkvohglpcf => vrfddbt, yvtvem => lznzgpsx, tc => uyjwkmyyhk);
  hgcqh : entity work.yr
    port map (l => pw);
  
  -- Multi-driven assignments
  lznzgpsx <= 'U';
  lznzgpsx <= lznzgpsx;
  lznzgpsx <= '0';
end mdjfffqn;



-- Seed after: 12506379508699553237,1112937151005418631
