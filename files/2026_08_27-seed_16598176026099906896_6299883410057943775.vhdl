-- Seed: 16598176026099906896,6299883410057943775

entity puzrtrw is
  port (ittr : linkage bit; i : inout integer; lwdmbvpfw : inout real);
end puzrtrw;

architecture k of puzrtrw is
  
begin
  -- Single-driven assignments
  i <= 23321;
end k;

entity ulph is
  port (cmazccuhc : buffer boolean; wxghr : buffer character; zzwicjqgax : out boolean);
end ulph;

architecture aws of ulph is
  
begin
  -- Single-driven assignments
  zzwicjqgax <= TRUE;
  cmazccuhc <= TRUE;
  wxghr <= 'q';
end aws;

library ieee;
use ieee.std_logic_1164.all;

entity lpnwt is
  port (ryztfaltp : inout bit; cm : in bit_vector(2 to 1); olpqwibb : out std_logic);
end lpnwt;

architecture d of lpnwt is
  signal zehs : real;
  signal oei : integer;
  signal gowgjv : real;
  signal x : integer;
  signal tmh : bit;
  signal lzljovng : real;
  signal mlxwsmwaf : integer;
  signal apa : bit;
  signal e : boolean;
  signal ha : character;
  signal sthgkm : boolean;
begin
  chcyw : entity work.ulph
    port map (cmazccuhc => sthgkm, wxghr => ha, zzwicjqgax => e);
  sixy : entity work.puzrtrw
    port map (ittr => apa, i => mlxwsmwaf, lwdmbvpfw => lzljovng);
  slkzony : entity work.puzrtrw
    port map (ittr => tmh, i => x, lwdmbvpfw => gowgjv);
  njomss : entity work.puzrtrw
    port map (ittr => ryztfaltp, i => oei, lwdmbvpfw => zehs);
  
  -- Multi-driven assignments
  olpqwibb <= olpqwibb;
end d;



-- Seed after: 8348004426864695311,6299883410057943775
