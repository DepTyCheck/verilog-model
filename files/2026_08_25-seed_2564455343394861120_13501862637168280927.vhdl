-- Seed: 2564455343394861120,13501862637168280927

entity my is
  port (eltfwt : linkage integer; vhsa : linkage integer_vector(3 to 0));
end my;

architecture pjwiv of my is
  
begin
  
end pjwiv;

entity njtepmk is
  port (lzv : inout integer);
end njtepmk;

architecture wwmpobtgk of njtepmk is
  signal gfj : integer_vector(3 to 0);
  signal wbvxixbt : integer;
  signal ydgycddapn : integer_vector(3 to 0);
  signal yktepmpvos : integer_vector(3 to 0);
  signal jloztk : integer;
  signal l : integer_vector(3 to 0);
  signal ocnpwok : integer;
begin
  dsvemongxo : entity work.my
    port map (eltfwt => ocnpwok, vhsa => l);
  jvhzgqqiv : entity work.my
    port map (eltfwt => jloztk, vhsa => yktepmpvos);
  nzaincujsb : entity work.my
    port map (eltfwt => lzv, vhsa => ydgycddapn);
  nqmzzmfwzo : entity work.my
    port map (eltfwt => wbvxixbt, vhsa => gfj);
end wwmpobtgk;

entity uicrcefu is
  port (jilfwidxih : buffer time);
end uicrcefu;

architecture jxwcdth of uicrcefu is
  signal jjl : integer_vector(3 to 0);
  signal unhyp : integer;
  signal bryfdvnm : integer_vector(3 to 0);
  signal hefigumb : integer;
begin
  ig : entity work.my
    port map (eltfwt => hefigumb, vhsa => bryfdvnm);
  kzgxgpzfy : entity work.my
    port map (eltfwt => unhyp, vhsa => jjl);
  
  -- Single-driven assignments
  jilfwidxih <= 1 hr;
end jxwcdth;



-- Seed after: 10067273476679373585,13501862637168280927
