-- Seed: 6340483033689227798,11127274767545411571

entity mvrw is
  port (z : linkage integer_vector(2 downto 2); zywc : in real; ltme : out string(5 downto 1); tnylxu : linkage boolean);
end mvrw;

architecture l of mvrw is
  
begin
  -- Single-driven assignments
  ltme <= ltme;
end l;

library ieee;
use ieee.std_logic_1164.all;

entity sqbuqpq is
  port (felmpinb : buffer std_logic; jfedvzsejn : buffer std_logic; xu : inout std_logic_vector(0 to 2));
end sqbuqpq;

architecture ixsgqlp of sqbuqpq is
  signal at : boolean;
  signal h : string(5 downto 1);
  signal mxdrexmdbd : real;
  signal vjlvwkxwh : integer_vector(2 downto 2);
  signal exjwuqwa : boolean;
  signal lrxongt : string(5 downto 1);
  signal iuhqhbb : real;
  signal tbtq : integer_vector(2 downto 2);
  signal vle : boolean;
  signal ncvflaj : string(5 downto 1);
  signal ztihqzcmwm : integer_vector(2 downto 2);
  signal fj : boolean;
  signal hh : string(5 downto 1);
  signal qaao : real;
  signal cwqgrqralb : integer_vector(2 downto 2);
begin
  ondwot : entity work.mvrw
    port map (z => cwqgrqralb, zywc => qaao, ltme => hh, tnylxu => fj);
  elbfi : entity work.mvrw
    port map (z => ztihqzcmwm, zywc => qaao, ltme => ncvflaj, tnylxu => vle);
  euslkebn : entity work.mvrw
    port map (z => tbtq, zywc => iuhqhbb, ltme => lrxongt, tnylxu => exjwuqwa);
  llb : entity work.mvrw
    port map (z => vjlvwkxwh, zywc => mxdrexmdbd, ltme => h, tnylxu => at);
  
  -- Single-driven assignments
  mxdrexmdbd <= 8#22013.7_0#;
  iuhqhbb <= qaao;
  qaao <= 4_2_0_3.2;
  
  -- Multi-driven assignments
  xu <= xu;
  xu <= xu;
  xu <= xu;
end ixsgqlp;



-- Seed after: 16212764743253608996,11127274767545411571
