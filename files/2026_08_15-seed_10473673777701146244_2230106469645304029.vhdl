-- Seed: 10473673777701146244,2230106469645304029

library ieee;
use ieee.std_logic_1164.all;

entity uibt is
  port (z : inout time; l : in time; udzwftsm : linkage std_logic_vector(2 downto 3); yf : inout time_vector(1 downto 0));
end uibt;

architecture weivwkofv of uibt is
  
begin
  -- Single-driven assignments
  yf <= yf;
end weivwkofv;

entity wljuw is
  port (cs : in time; abk : in real; wposxyya : out bit; pxndnlxkol : out integer);
end wljuw;

library ieee;
use ieee.std_logic_1164.all;

architecture kawvlvoww of wljuw is
  signal r : time_vector(1 downto 0);
  signal rppkjjpm : time_vector(1 downto 0);
  signal roe : std_logic_vector(2 downto 3);
  signal gslsnweho : time;
  signal zyf : time;
begin
  oxkrdsjaq : entity work.uibt
    port map (z => zyf, l => gslsnweho, udzwftsm => roe, yf => rppkjjpm);
  qecfn : entity work.uibt
    port map (z => gslsnweho, l => zyf, udzwftsm => roe, yf => r);
  
  -- Single-driven assignments
  wposxyya <= '1';
  pxndnlxkol <= 16#C327#;
  
  -- Multi-driven assignments
  roe <= roe;
  roe <= roe;
  roe <= (others => '0');
  roe <= roe;
end kawvlvoww;

library ieee;
use ieee.std_logic_1164.all;

entity ouwydl is
  port (mbckjcjenv : buffer std_logic);
end ouwydl;

library ieee;
use ieee.std_logic_1164.all;

architecture xlffr of ouwydl is
  signal wv : integer;
  signal qyd : bit;
  signal pjyxas : real;
  signal s : time_vector(1 downto 0);
  signal yyjy : std_logic_vector(2 downto 3);
  signal bl : time;
  signal lxw : time_vector(1 downto 0);
  signal dukmfgrlsa : std_logic_vector(2 downto 3);
  signal ou : time;
begin
  eksr : entity work.uibt
    port map (z => ou, l => ou, udzwftsm => dukmfgrlsa, yf => lxw);
  xam : entity work.uibt
    port map (z => bl, l => ou, udzwftsm => yyjy, yf => s);
  lif : entity work.wljuw
    port map (cs => ou, abk => pjyxas, wposxyya => qyd, pxndnlxkol => wv);
  
  -- Single-driven assignments
  pjyxas <= 0.24;
end xlffr;



-- Seed after: 10813261997258074050,2230106469645304029
