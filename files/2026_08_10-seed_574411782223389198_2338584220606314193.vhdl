-- Seed: 574411782223389198,2338584220606314193

library ieee;
use ieee.std_logic_1164.all;

entity izvxviut is
  port (bntqucdgh : linkage std_logic; mto : in std_logic; jkhawps : buffer string(3 downto 3));
end izvxviut;

architecture oabw of izvxviut is
  
begin
  -- Single-driven assignments
  jkhawps <= jkhawps;
end oabw;

entity v is
  port (uluhbahidc : linkage real; atqfekla : inout real);
end v;

library ieee;
use ieee.std_logic_1164.all;

architecture xuzyr of v is
  signal bolavp : string(3 downto 3);
  signal qkhk : std_logic;
  signal gcnhrsdacg : std_logic;
  signal agxpau : string(3 downto 3);
  signal frg : std_logic;
  signal st : string(3 downto 3);
  signal id : std_logic;
begin
  u : entity work.izvxviut
    port map (bntqucdgh => id, mto => id, jkhawps => st);
  afga : entity work.izvxviut
    port map (bntqucdgh => frg, mto => id, jkhawps => agxpau);
  r : entity work.izvxviut
    port map (bntqucdgh => gcnhrsdacg, mto => qkhk, jkhawps => bolavp);
  
  -- Single-driven assignments
  atqfekla <= atqfekla;
  
  -- Multi-driven assignments
  id <= 'X';
  id <= id;
  id <= qkhk;
end xuzyr;

library ieee;
use ieee.std_logic_1164.all;

entity x is
  port (ufsfrlri : linkage std_logic_vector(0 to 1));
end x;

library ieee;
use ieee.std_logic_1164.all;

architecture u of x is
  signal qjwjw : string(3 downto 3);
  signal vajynvaym : string(3 downto 3);
  signal zzedhg : std_logic;
begin
  bfl : entity work.izvxviut
    port map (bntqucdgh => zzedhg, mto => zzedhg, jkhawps => vajynvaym);
  ixbjwtj : entity work.izvxviut
    port map (bntqucdgh => zzedhg, mto => zzedhg, jkhawps => qjwjw);
end u;

entity qwaajzi is
  port (lgsxjvhko : out time);
end qwaajzi;

library ieee;
use ieee.std_logic_1164.all;

architecture skkbmy of qwaajzi is
  signal wnuwjxdn : real;
  signal loqsbtyh : real;
  signal swgq : std_logic_vector(0 to 1);
  signal rc : string(3 downto 3);
  signal vfkep : std_logic;
  signal vbslsov : std_logic;
begin
  xsqqsxqb : entity work.izvxviut
    port map (bntqucdgh => vbslsov, mto => vfkep, jkhawps => rc);
  mre : entity work.x
    port map (ufsfrlri => swgq);
  rzopu : entity work.v
    port map (uluhbahidc => loqsbtyh, atqfekla => wnuwjxdn);
  
  -- Multi-driven assignments
  swgq <= swgq;
  vbslsov <= '1';
end skkbmy;



-- Seed after: 620236214602813900,2338584220606314193
