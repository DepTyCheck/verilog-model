-- Seed: 1149749704571932012,1112937151005418631

library ieee;
use ieee.std_logic_1164.all;

entity ooko is
  port (cildtiq : linkage std_logic; ngtjh : in std_logic_vector(4 to 1); xtt : inout boolean);
end ooko;

architecture bihyxyopcc of ooko is
  
begin
  -- Single-driven assignments
  xtt <= xtt;
end bihyxyopcc;

entity upsic is
  port (twerjpyneo : linkage time; pzfr : buffer boolean; vacskqts : inout bit_vector(1 to 2));
end upsic;

library ieee;
use ieee.std_logic_1164.all;

architecture ddd of upsic is
  signal rrtok : boolean;
  signal przlnko : std_logic_vector(4 to 1);
  signal znrnjnq : boolean;
  signal dysobvi : boolean;
  signal nzacb : std_logic;
  signal rqhcza : boolean;
  signal b : std_logic_vector(4 to 1);
  signal frgjoyp : std_logic;
begin
  tm : entity work.ooko
    port map (cildtiq => frgjoyp, ngtjh => b, xtt => rqhcza);
  bxbo : entity work.ooko
    port map (cildtiq => nzacb, ngtjh => b, xtt => dysobvi);
  dgs : entity work.ooko
    port map (cildtiq => frgjoyp, ngtjh => b, xtt => znrnjnq);
  xorlpfwm : entity work.ooko
    port map (cildtiq => nzacb, ngtjh => przlnko, xtt => rrtok);
  
  -- Single-driven assignments
  vacskqts <= vacskqts;
  pzfr <= FALSE;
  
  -- Multi-driven assignments
  nzacb <= 'Z';
  frgjoyp <= frgjoyp;
end ddd;

library ieee;
use ieee.std_logic_1164.all;

entity wjgfsaryj is
  port (ohm : inout std_logic; qcjhpeuubu : buffer integer_vector(2 to 4));
end wjgfsaryj;

library ieee;
use ieee.std_logic_1164.all;

architecture pjdntkydp of wjgfsaryj is
  signal h : boolean;
  signal acvsdn : std_logic_vector(4 to 1);
  signal nslvmirify : bit_vector(1 to 2);
  signal ph : boolean;
  signal fvclqlq : time;
  signal szvlx : bit_vector(1 to 2);
  signal rl : boolean;
  signal wtbcqde : time;
begin
  eofwq : entity work.upsic
    port map (twerjpyneo => wtbcqde, pzfr => rl, vacskqts => szvlx);
  rhqhuh : entity work.upsic
    port map (twerjpyneo => fvclqlq, pzfr => ph, vacskqts => nslvmirify);
  c : entity work.ooko
    port map (cildtiq => ohm, ngtjh => acvsdn, xtt => h);
  
  -- Multi-driven assignments
  acvsdn <= "";
  ohm <= ohm;
  ohm <= 'Z';
end pjdntkydp;



-- Seed after: 12188219911287520802,1112937151005418631
