-- Seed: 15050253019696962253,2983771601630957889

use std.reflection.all;

entity uyvhtuv is
  port (dnvpav : linkage real; variable wypgitw : inout protected_value_mirror_pt);
end uyvhtuv;

architecture wppegxr of uyvhtuv is
  
begin
  
end wppegxr;

use std.reflection.all;

entity idutdyu is
  port (variable wamom : inout floating_subtype_mirror_pt);
end idutdyu;

use std.reflection.all;

architecture pvjwspd of idutdyu is
  shared variable sqfucopml : protected_value_mirror_pt;
  signal qbmrcvx : real;
  shared variable anpwnfnp : protected_value_mirror_pt;
  signal ybotujlr : real;
  shared variable ausgtzv : protected_value_mirror_pt;
  signal zt : real;
begin
  mstrxlkd : entity work.uyvhtuv
    port map (dnvpav => zt, wypgitw => ausgtzv);
  hcdxsfztua : entity work.uyvhtuv
    port map (dnvpav => ybotujlr, wypgitw => anpwnfnp);
  g : entity work.uyvhtuv
    port map (dnvpav => qbmrcvx, wypgitw => sqfucopml);
end pvjwspd;

use std.reflection.all;

entity kpy is
  port (variable bf : inout file_value_mirror_pt; st : out time; x : out character);
end kpy;

use std.reflection.all;

architecture xvuzumof of kpy is
  shared variable ichlj : floating_subtype_mirror_pt;
  shared variable zywvd : protected_value_mirror_pt;
  signal ld : real;
begin
  jksqwko : entity work.uyvhtuv
    port map (dnvpav => ld, wypgitw => zywvd);
  nhczq : entity work.idutdyu
    port map (wamom => ichlj);
end xvuzumof;



-- Seed after: 2576071966714090834,2983771601630957889
