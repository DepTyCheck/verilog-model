-- Seed: 15884509570678585432,13854332967471039201

library ieee;
use ieee.std_logic_1164.all;

entity l is
  port (cqmkbrknul : out character; a : inout real; d : linkage std_logic);
end l;



architecture tgpwposgz of l is
  
begin
  
end tgpwposgz;



entity poaa is
  port (ctcdi : buffer bit_vector(1 to 4); jpdlvgt : in integer);
end poaa;

library ieee;
use ieee.std_logic_1164.all;

architecture fnsjlv of poaa is
  signal ajj : std_logic;
  signal dgmnpwohc : real;
  signal iikbbtyyw : character;
  signal ackvlfdou : std_logic;
  signal pgime : real;
  signal h : character;
begin
  ftnlpt : entity work.l
    port map (cqmkbrknul => h, a => pgime, d => ackvlfdou);
  im : entity work.l
    port map (cqmkbrknul => iikbbtyyw, a => dgmnpwohc, d => ajj);
end fnsjlv;

library ieee;
use ieee.std_logic_1164.all;

entity vc is
  port (zwhm : out time; gxok : buffer std_logic; vhf : linkage time; hloaxgx : inout integer);
end vc;



architecture epb of vc is
  signal ybxlgo : real;
  signal lzaarvdt : character;
begin
  tj : entity work.l
    port map (cqmkbrknul => lzaarvdt, a => ybxlgo, d => gxok);
end epb;

library ieee;
use ieee.std_logic_1164.all;

entity wpzasqzy is
  port (skuxokemxd : in bit; cd : inout std_logic_vector(3 to 4));
end wpzasqzy;

library ieee;
use ieee.std_logic_1164.all;

architecture sz of wpzasqzy is
  signal g : std_logic;
  signal ftjby : real;
  signal i : character;
  signal okc : integer;
  signal mfux : bit_vector(1 to 4);
begin
  zbrbfwiil : entity work.poaa
    port map (ctcdi => mfux, jpdlvgt => okc);
  msgr : entity work.l
    port map (cqmkbrknul => i, a => ftjby, d => g);
end sz;



-- Seed after: 3895271322838508163,13854332967471039201
