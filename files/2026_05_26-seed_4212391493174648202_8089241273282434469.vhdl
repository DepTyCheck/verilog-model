-- Seed: 4212391493174648202,8089241273282434469

library ieee;
use ieee.std_logic_1164.all;

entity gpcoxji is
  port (jep : out real; yo : inout std_logic_vector(4 downto 3); m : linkage time; kcah : inout real);
end gpcoxji;



architecture wp of gpcoxji is
  
begin
  
end wp;

library ieee;
use ieee.std_logic_1164.all;

entity i is
  port (fmxnn : in std_logic);
end i;

library ieee;
use ieee.std_logic_1164.all;

architecture sidbuycrj of i is
  signal se : real;
  signal iowevo : real;
  signal jnqv : real;
  signal dgnjtl : real;
  signal ypyv : real;
  signal fhgaxutqj : time;
  signal ieey : real;
  signal qdzpm : real;
  signal qgqd : time;
  signal eeynff : std_logic_vector(4 downto 3);
  signal lm : real;
begin
  wg : entity work.gpcoxji
    port map (jep => lm, yo => eeynff, m => qgqd, kcah => qdzpm);
  cfhv : entity work.gpcoxji
    port map (jep => ieey, yo => eeynff, m => fhgaxutqj, kcah => ypyv);
  qlti : entity work.gpcoxji
    port map (jep => dgnjtl, yo => eeynff, m => qgqd, kcah => jnqv);
  aytnqeorge : entity work.gpcoxji
    port map (jep => iowevo, yo => eeynff, m => qgqd, kcah => se);
end sidbuycrj;



entity ekqo is
  port (m : buffer time; ine : inout real);
end ekqo;

library ieee;
use ieee.std_logic_1164.all;

architecture lyj of ekqo is
  signal r : real;
  signal jdbcuwlc : time;
  signal ccftt : std_logic_vector(4 downto 3);
  signal wnux : real;
  signal yunrva : real;
  signal p : time;
  signal vbgjqmwyx : std_logic_vector(4 downto 3);
  signal qtqzthkjcf : real;
  signal oeifz : time;
  signal a : std_logic_vector(4 downto 3);
  signal n : real;
  signal mmsweeu : real;
  signal dppt : std_logic_vector(4 downto 3);
  signal ybbovdh : real;
begin
  sgowj : entity work.gpcoxji
    port map (jep => ybbovdh, yo => dppt, m => m, kcah => mmsweeu);
  jdt : entity work.gpcoxji
    port map (jep => n, yo => a, m => oeifz, kcah => qtqzthkjcf);
  uqnqxpbe : entity work.gpcoxji
    port map (jep => ine, yo => vbgjqmwyx, m => p, kcah => yunrva);
  ahzv : entity work.gpcoxji
    port map (jep => wnux, yo => ccftt, m => jdbcuwlc, kcah => r);
end lyj;

library ieee;
use ieee.std_logic_1164.all;

entity cju is
  port (u : buffer integer; boylsuh : out boolean_vector(1 downto 0); lnqjw : linkage std_logic_vector(4 to 3); eoenc : out real);
end cju;



architecture jflsj of cju is
  signal lrqg : real;
  signal qmsmlslo : time;
  signal kukhnbrsh : real;
  signal hsqqyod : time;
begin
  wqrpzrx : entity work.ekqo
    port map (m => hsqqyod, ine => kukhnbrsh);
  ybf : entity work.ekqo
    port map (m => qmsmlslo, ine => lrqg);
end jflsj;



-- Seed after: 10906041111192469202,8089241273282434469
