-- Seed: 6736237607441493540,5380108556942160549

library ieee;
use ieee.std_logic_1164.all;

entity neu is
  port (llw : inout time; gvud : buffer std_logic; vdmw : out boolean; rfmkcpnl : out integer);
end neu;



architecture nm of neu is
  
begin
  
end nm;

library ieee;
use ieee.std_logic_1164.all;

entity nn is
  port (hc : linkage time; dxszlrckhi : in real; hlghwr : linkage real; tmys : in std_logic);
end nn;

library ieee;
use ieee.std_logic_1164.all;

architecture bsouxsqw of nn is
  signal yyqim : integer;
  signal aqwbplnjpv : boolean;
  signal moftnrd : time;
  signal b : integer;
  signal gqiaol : boolean;
  signal jwifz : time;
  signal jcryp : integer;
  signal ul : boolean;
  signal zsucd : time;
  signal bqntp : integer;
  signal ppnx : boolean;
  signal tcn : std_logic;
  signal nev : time;
begin
  phwfglxd : entity work.neu
    port map (llw => nev, gvud => tcn, vdmw => ppnx, rfmkcpnl => bqntp);
  oqiljyp : entity work.neu
    port map (llw => zsucd, gvud => tcn, vdmw => ul, rfmkcpnl => jcryp);
  pfgcayge : entity work.neu
    port map (llw => jwifz, gvud => tcn, vdmw => gqiaol, rfmkcpnl => b);
  xuqeo : entity work.neu
    port map (llw => moftnrd, gvud => tcn, vdmw => aqwbplnjpv, rfmkcpnl => yyqim);
end bsouxsqw;

library ieee;
use ieee.std_logic_1164.all;

entity kyykopacfa is
  port (z : out time; gqs : linkage std_logic);
end kyykopacfa;

library ieee;
use ieee.std_logic_1164.all;

architecture b of kyykopacfa is
  signal kjmmt : integer;
  signal kxwians : boolean;
  signal nxlseauune : std_logic;
  signal ojucbsuyks : real;
  signal bfy : time;
  signal dvlvees : real;
  signal aj : time;
  signal uxetaan : std_logic;
  signal jfozm : real;
begin
  nareuu : entity work.nn
    port map (hc => z, dxszlrckhi => jfozm, hlghwr => jfozm, tmys => uxetaan);
  nsk : entity work.nn
    port map (hc => aj, dxszlrckhi => jfozm, hlghwr => dvlvees, tmys => uxetaan);
  tdezjd : entity work.nn
    port map (hc => bfy, dxszlrckhi => ojucbsuyks, hlghwr => dvlvees, tmys => uxetaan);
  geqzjgc : entity work.neu
    port map (llw => bfy, gvud => nxlseauune, vdmw => kxwians, rfmkcpnl => kjmmt);
end b;



entity wmvnomgf is
  port (iefe : buffer integer; p : linkage integer; bxtn : in severity_level);
end wmvnomgf;

library ieee;
use ieee.std_logic_1164.all;

architecture yaxewomaj of wmvnomgf is
  signal snwv : std_logic;
  signal qwq : time;
  signal bquckil : std_logic;
  signal c : real;
  signal blgppvfh : real;
  signal ef : boolean;
  signal dpcjdq : std_logic;
  signal jkaahj : time;
begin
  yrs : entity work.neu
    port map (llw => jkaahj, gvud => dpcjdq, vdmw => ef, rfmkcpnl => iefe);
  lvebcplx : entity work.nn
    port map (hc => jkaahj, dxszlrckhi => blgppvfh, hlghwr => c, tmys => bquckil);
  m : entity work.kyykopacfa
    port map (z => qwq, gqs => snwv);
end yaxewomaj;



-- Seed after: 16003580139192840473,5380108556942160549
