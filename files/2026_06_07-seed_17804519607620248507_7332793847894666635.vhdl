-- Seed: 17804519607620248507,7332793847894666635

library ieee;
use ieee.std_logic_1164.all;

entity aubfql is
  port (tx : out std_logic; ssyjecc : linkage real_vector(1 downto 2); yysrpd : out boolean);
end aubfql;



architecture zsvaiimp of aubfql is
  
begin
  
end zsvaiimp;

library ieee;
use ieee.std_logic_1164.all;

entity fovjle is
  port (oumhz : linkage std_logic);
end fovjle;

library ieee;
use ieee.std_logic_1164.all;

architecture aweinhsxvx of fovjle is
  signal efqhgq : boolean;
  signal xnago : std_logic;
  signal jequhzhp : boolean;
  signal iwwliww : real_vector(1 downto 2);
  signal lbetkphy : std_logic;
begin
  odoxxndots : entity work.aubfql
    port map (tx => lbetkphy, ssyjecc => iwwliww, yysrpd => jequhzhp);
  h : entity work.aubfql
    port map (tx => xnago, ssyjecc => iwwliww, yysrpd => efqhgq);
end aweinhsxvx;

library ieee;
use ieee.std_logic_1164.all;

entity xergmzbmnl is
  port (topqp : inout integer; bogvysn : out std_logic_vector(3 downto 3); waq : out integer; lcvvmnuanf : in integer);
end xergmzbmnl;

library ieee;
use ieee.std_logic_1164.all;

architecture obmpq of xergmzbmnl is
  signal hhhukjtswr : boolean;
  signal joveyky : real_vector(1 downto 2);
  signal bic : boolean;
  signal dnfq : real_vector(1 downto 2);
  signal iyqu : std_logic;
  signal alfhf : std_logic;
begin
  m : entity work.fovjle
    port map (oumhz => alfhf);
  vivartbig : entity work.aubfql
    port map (tx => iyqu, ssyjecc => dnfq, yysrpd => bic);
  cexz : entity work.aubfql
    port map (tx => alfhf, ssyjecc => joveyky, yysrpd => hhhukjtswr);
end obmpq;

library ieee;
use ieee.std_logic_1164.all;

entity fm is
  port (pi : out time; nms : out std_logic_vector(1 to 4));
end fm;

library ieee;
use ieee.std_logic_1164.all;

architecture h of fm is
  signal zzosplgijl : integer;
  signal axo : std_logic_vector(3 downto 3);
  signal yklzohhcl : integer;
  signal auazrci : integer;
  signal dzxjluril : integer;
  signal tdcowm : std_logic_vector(3 downto 3);
  signal lec : integer;
  signal ss : std_logic;
begin
  wsogya : entity work.fovjle
    port map (oumhz => ss);
  ju : entity work.xergmzbmnl
    port map (topqp => lec, bogvysn => tdcowm, waq => dzxjluril, lcvvmnuanf => auazrci);
  giiqwf : entity work.xergmzbmnl
    port map (topqp => yklzohhcl, bogvysn => axo, waq => auazrci, lcvvmnuanf => zzosplgijl);
end h;



-- Seed after: 11324348429508817127,7332793847894666635
