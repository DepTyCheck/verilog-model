-- Seed: 16010018627373800594,17234720251424330329

entity mrikhccp is
  port (lbskjtugey : buffer real; tzplu : inout integer; kylc : buffer real; fcheoraefb : out time);
end mrikhccp;

architecture gr of mrikhccp is
  
begin
  -- Single-driven assignments
  fcheoraefb <= 3 ns;
  tzplu <= 16#1_F_4#;
  kylc <= kylc;
end gr;

library ieee;
use ieee.std_logic_1164.all;

entity uxoipzblea is
  port (nytzpyal : out time; td : buffer time; y : linkage std_logic_vector(4 to 3));
end uxoipzblea;

architecture qcye of uxoipzblea is
  signal utuzxqp : real;
  signal vuwj : integer;
  signal hpmmmlpymj : real;
  signal ldh : real;
  signal avggnhbz : integer;
  signal rfhxoxk : real;
begin
  ivxkzqze : entity work.mrikhccp
    port map (lbskjtugey => rfhxoxk, tzplu => avggnhbz, kylc => ldh, fcheoraefb => td);
  dpurdbqjj : entity work.mrikhccp
    port map (lbskjtugey => hpmmmlpymj, tzplu => vuwj, kylc => utuzxqp, fcheoraefb => nytzpyal);
end qcye;

library ieee;
use ieee.std_logic_1164.all;

entity olitxy is
  port (lxhx : in std_logic; zwvlejilo : in character; jvwxbhi : out bit);
end olitxy;

architecture sha of olitxy is
  signal oiwkzxowaj : time;
  signal vhrlcunuz : real;
  signal uy : integer;
  signal cbgn : real;
  signal cdbfxwx : time;
  signal fgwi : real;
  signal etixmdc : integer;
  signal cqrr : real;
begin
  kp : entity work.mrikhccp
    port map (lbskjtugey => cqrr, tzplu => etixmdc, kylc => fgwi, fcheoraefb => cdbfxwx);
  cvkwumvdzx : entity work.mrikhccp
    port map (lbskjtugey => cbgn, tzplu => uy, kylc => vhrlcunuz, fcheoraefb => oiwkzxowaj);
  
  -- Single-driven assignments
  jvwxbhi <= '0';
end sha;



-- Seed after: 1829046597252538759,17234720251424330329
