-- Seed: 18224125929162306521,8118127366649987907

library ieee;
use ieee.std_logic_1164.all;

entity dbaenwmxt is
  port (za : out std_logic_vector(0 to 4); dbofulz : out time);
end dbaenwmxt;

architecture fbannff of dbaenwmxt is
  
begin
  
end fbannff;

entity ergtev is
  port (clxecaq : linkage integer);
end ergtev;

library ieee;
use ieee.std_logic_1164.all;

architecture a of ergtev is
  signal rfznrcr : time;
  signal uwx : std_logic_vector(0 to 4);
  signal zmywhdz : time;
  signal z : time;
  signal kmporstnu : time;
  signal wb : std_logic_vector(0 to 4);
begin
  x : entity work.dbaenwmxt
    port map (za => wb, dbofulz => kmporstnu);
  k : entity work.dbaenwmxt
    port map (za => wb, dbofulz => z);
  cxq : entity work.dbaenwmxt
    port map (za => wb, dbofulz => zmywhdz);
  pbopf : entity work.dbaenwmxt
    port map (za => uwx, dbofulz => rfznrcr);
end a;

entity bkjos is
  port (ruphbum : buffer severity_level; kobuigv : inout character);
end bkjos;

architecture vlzqhw of bkjos is
  
begin
  -- Single-driven assignments
  kobuigv <= 'z';
  ruphbum <= ERROR;
end vlzqhw;

entity hbclp is
  port (verwkmq : buffer integer);
end hbclp;

library ieee;
use ieee.std_logic_1164.all;

architecture yzoovxhfe of hbclp is
  signal gdecefzdeh : time;
  signal rbotihwqm : std_logic_vector(0 to 4);
  signal ml : time;
  signal atgh : time;
  signal ltnrfb : time;
  signal xiw : std_logic_vector(0 to 4);
begin
  xtcxfdh : entity work.dbaenwmxt
    port map (za => xiw, dbofulz => ltnrfb);
  he : entity work.dbaenwmxt
    port map (za => xiw, dbofulz => atgh);
  alvmsholm : entity work.dbaenwmxt
    port map (za => xiw, dbofulz => ml);
  wpvk : entity work.dbaenwmxt
    port map (za => rbotihwqm, dbofulz => gdecefzdeh);
  
  -- Single-driven assignments
  verwkmq <= 2#01011#;
  
  -- Multi-driven assignments
  xiw <= ('H', 'W', 'L', 'Z', 'X');
  xiw <= "ZUHZZ";
  xiw <= "1ZLWZ";
  xiw <= "0U-1-";
end yzoovxhfe;



-- Seed after: 12946690075803332326,8118127366649987907
