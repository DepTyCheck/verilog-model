-- Seed: 1709486418150100193,13592003931158285879

library ieee;
use ieee.std_logic_1164.all;

entity ebleutx is
  port (tbhyps : in time; n : buffer std_logic_vector(1 to 0));
end ebleutx;

architecture ofoplfaaop of ebleutx is
  
begin
  -- Multi-driven assignments
  n <= n;
  n <= n;
  n <= n;
  n <= n;
end ofoplfaaop;

library ieee;
use ieee.std_logic_1164.all;

entity zryurtd is
  port (ka : buffer character; uqznampdhz : linkage real; bjlpvc : in std_logic; s : inout std_logic_vector(0 to 1));
end zryurtd;

architecture kcquesrjuu of zryurtd is
  
begin
  -- Multi-driven assignments
  s <= s;
  s <= ('-', 'U');
  s <= s;
  s <= s;
end kcquesrjuu;

library ieee;
use ieee.std_logic_1164.all;

entity zzcunk is
  port (nsfmutursf : inout std_logic; q : inout time; csld : buffer integer; lsxeijqvv : in boolean_vector(0 downto 3));
end zzcunk;

library ieee;
use ieee.std_logic_1164.all;

architecture xpo of zzcunk is
  signal quxenhsslz : std_logic_vector(1 to 0);
  signal ztxaepsm : std_logic_vector(0 to 1);
  signal tzfumkdtd : std_logic;
  signal kxmt : real;
  signal dosmbd : character;
  signal qx : std_logic_vector(1 to 0);
  signal lp : std_logic_vector(1 to 0);
  signal p : time;
begin
  lhqhao : entity work.ebleutx
    port map (tbhyps => p, n => lp);
  rioui : entity work.ebleutx
    port map (tbhyps => p, n => qx);
  vlucrt : entity work.zryurtd
    port map (ka => dosmbd, uqznampdhz => kxmt, bjlpvc => tzfumkdtd, s => ztxaepsm);
  laym : entity work.ebleutx
    port map (tbhyps => p, n => quxenhsslz);
  
  -- Single-driven assignments
  csld <= 04;
  p <= 3 min;
  q <= 16#3AD8# fs;
  
  -- Multi-driven assignments
  ztxaepsm <= ztxaepsm;
  qx <= lp;
  tzfumkdtd <= nsfmutursf;
  tzfumkdtd <= nsfmutursf;
end xpo;



-- Seed after: 12236301677136870150,13592003931158285879
