-- Seed: 10695188821302811618,8881251984038598933

library ieee;
use ieee.std_logic_1164.all;

entity rwska is
  port (boohsowons : linkage severity_level; apsfi : in time; sxlxca : out std_logic);
end rwska;



architecture yrwgn of rwska is
  
begin
  
end yrwgn;

library ieee;
use ieee.std_logic_1164.all;

entity csazom is
  port (b : out std_logic; qlfzzk : inout severity_level);
end csazom;



architecture u of csazom is
  signal o : severity_level;
  signal zikrgjou : severity_level;
  signal txihyrpic : time;
  signal ofvubnkdz : severity_level;
begin
  yksnf : entity work.rwska
    port map (boohsowons => ofvubnkdz, apsfi => txihyrpic, sxlxca => b);
  dcjc : entity work.rwska
    port map (boohsowons => zikrgjou, apsfi => txihyrpic, sxlxca => b);
  jifvcxpuzi : entity work.rwska
    port map (boohsowons => o, apsfi => txihyrpic, sxlxca => b);
end u;



entity bfpgyqos is
  port (ftar : in real; upm : inout real; ecwibcvwu : buffer real; mm : inout boolean);
end bfpgyqos;

library ieee;
use ieee.std_logic_1164.all;

architecture gkzdovgp of bfpgyqos is
  signal t : std_logic;
  signal ewhohndy : severity_level;
  signal go : severity_level;
  signal ygtlhuk : severity_level;
  signal utlrd : std_logic;
  signal vzdjcytmel : time;
  signal nejemwvzw : severity_level;
begin
  uvanwaabjf : entity work.rwska
    port map (boohsowons => nejemwvzw, apsfi => vzdjcytmel, sxlxca => utlrd);
  ioo : entity work.csazom
    port map (b => utlrd, qlfzzk => ygtlhuk);
  obnkz : entity work.csazom
    port map (b => utlrd, qlfzzk => go);
  eto : entity work.rwska
    port map (boohsowons => ewhohndy, apsfi => vzdjcytmel, sxlxca => t);
end gkzdovgp;



entity vg is
  port (pavcfxu : in real; fexcojov : buffer real; c : buffer time);
end vg;

library ieee;
use ieee.std_logic_1164.all;

architecture wdbihe of vg is
  signal zeiey : std_logic;
  signal beysuxr : severity_level;
begin
  pruouy : entity work.rwska
    port map (boohsowons => beysuxr, apsfi => c, sxlxca => zeiey);
end wdbihe;



-- Seed after: 8850610778057924291,8881251984038598933
