-- Seed: 9144950995887735808,9951735690217599971



entity eqdmviyv is
  port (gunuqihw : out real; vzyg : linkage severity_level; eib : out severity_level; wkmnq : inout time);
end eqdmviyv;



architecture wtsf of eqdmviyv is
  
begin
  
end wtsf;

library ieee;
use ieee.std_logic_1164.all;

entity cty is
  port (xapuls : out std_logic; thyobg : out real);
end cty;



architecture ytsilno of cty is
  signal pqnhjr : time;
  signal gnmildiye : severity_level;
  signal mkeva : real;
  signal xzsyxyg : time;
  signal v : severity_level;
  signal cmhiwd : real;
  signal zoagcvm : time;
  signal w : severity_level;
  signal jlrsgzqq : severity_level;
  signal nnxt : time;
  signal c : severity_level;
  signal ujjmwufrse : severity_level;
  signal cjealegbp : real;
begin
  x : entity work.eqdmviyv
    port map (gunuqihw => cjealegbp, vzyg => ujjmwufrse, eib => c, wkmnq => nnxt);
  xs : entity work.eqdmviyv
    port map (gunuqihw => thyobg, vzyg => jlrsgzqq, eib => w, wkmnq => zoagcvm);
  nbqqsux : entity work.eqdmviyv
    port map (gunuqihw => cmhiwd, vzyg => v, eib => ujjmwufrse, wkmnq => xzsyxyg);
  izogezmy : entity work.eqdmviyv
    port map (gunuqihw => mkeva, vzyg => jlrsgzqq, eib => gnmildiye, wkmnq => pqnhjr);
end ytsilno;



entity ingz is
  port (qnkvy : inout boolean; ysnivzavch : linkage time; nigog : linkage integer);
end ingz;

library ieee;
use ieee.std_logic_1164.all;

architecture k of ingz is
  signal akr : real;
  signal kf : std_logic;
  signal vbpmwjszp : time;
  signal m : severity_level;
  signal muwushwc : real;
  signal ukkuaay : time;
  signal wrfhjw : severity_level;
  signal sueajtjocb : severity_level;
  signal pietk : real;
begin
  ualfm : entity work.eqdmviyv
    port map (gunuqihw => pietk, vzyg => sueajtjocb, eib => wrfhjw, wkmnq => ukkuaay);
  aax : entity work.eqdmviyv
    port map (gunuqihw => muwushwc, vzyg => m, eib => sueajtjocb, wkmnq => vbpmwjszp);
  ovijfg : entity work.cty
    port map (xapuls => kf, thyobg => akr);
end k;

library ieee;
use ieee.std_logic_1164.all;

entity t is
  port (wqa : inout std_logic_vector(4 to 0); mujgvb : in real_vector(1 downto 4));
end t;

library ieee;
use ieee.std_logic_1164.all;

architecture glhkyrf of t is
  signal hmoo : integer;
  signal qtnxccplub : boolean;
  signal mq : real;
  signal nbssvganxd : std_logic;
  signal swlzudx : time;
  signal cztzhgw : severity_level;
  signal i : severity_level;
  signal djtfktmsqc : real;
  signal wdqn : time;
  signal eutpqomidd : severity_level;
  signal iw : real;
begin
  gtlxsmopl : entity work.eqdmviyv
    port map (gunuqihw => iw, vzyg => eutpqomidd, eib => eutpqomidd, wkmnq => wdqn);
  e : entity work.eqdmviyv
    port map (gunuqihw => djtfktmsqc, vzyg => i, eib => cztzhgw, wkmnq => swlzudx);
  qwgxky : entity work.cty
    port map (xapuls => nbssvganxd, thyobg => mq);
  zcaki : entity work.ingz
    port map (qnkvy => qtnxccplub, ysnivzavch => wdqn, nigog => hmoo);
end glhkyrf;



-- Seed after: 13715839049249187872,9951735690217599971
