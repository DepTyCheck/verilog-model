-- Seed: 18365850519892624287,7332793847894666635



entity xnbagemrgb is
  port (hmzcnos : inout time; jrelskib : inout integer_vector(4 to 1); cujde : out integer; e : out bit_vector(3 to 2));
end xnbagemrgb;



architecture enxmmbmohl of xnbagemrgb is
  
begin
  
end enxmmbmohl;

library ieee;
use ieee.std_logic_1164.all;

entity xgvcs is
  port (bhlshyw : in bit; sjtwxnfgr : in integer; cewswupw : out std_logic; ixysgm : linkage std_logic);
end xgvcs;



architecture ecqgec of xgvcs is
  signal rypyri : bit_vector(3 to 2);
  signal l : integer;
  signal rv : integer_vector(4 to 1);
  signal shbw : time;
  signal ram : bit_vector(3 to 2);
  signal cvnrmxmp : integer;
  signal jpehye : integer_vector(4 to 1);
  signal fjqugjnwc : time;
  signal kqqxstl : bit_vector(3 to 2);
  signal b : integer;
  signal qfnjk : integer_vector(4 to 1);
  signal ayrskd : time;
begin
  nenw : entity work.xnbagemrgb
    port map (hmzcnos => ayrskd, jrelskib => qfnjk, cujde => b, e => kqqxstl);
  tspabg : entity work.xnbagemrgb
    port map (hmzcnos => fjqugjnwc, jrelskib => jpehye, cujde => cvnrmxmp, e => ram);
  dov : entity work.xnbagemrgb
    port map (hmzcnos => shbw, jrelskib => rv, cujde => l, e => rypyri);
end ecqgec;



-- Seed after: 8371720195071026223,7332793847894666635
