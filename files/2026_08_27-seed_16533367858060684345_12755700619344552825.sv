// Seed: 16533367858060684345,12755700619344552825

module eooojwz (input bit [4:3] owltfiru [3:0], output logic [1:2][2:0] tnym, output reg amqt [4:1]);
  xnor qpevdbi(lg, tnym, tnym);
  // warning: implicit conversion of port connection truncates from 6 to 1 bits
  //   logic [1:2][2:0] tnym -> logic tnym
  //
  // warning: implicit conversion of port connection truncates from 6 to 1 bits
  //   logic [1:2][2:0] tnym -> logic tnym
  
  and mipfwg(tnym, libjaob, tugkv);
  // warning: implicit conversion of port connection truncates from 6 to 1 bits
  //   logic [1:2][2:0] tnym -> logic tnym
  
  or cm(lg, tugkv, ehsbyuavu);
  
  xnor d(libjaob, tugkv, fawwzt);
  
  
  // Single-driven assignments
  assign amqt = '{'bx,'b1z,'b000,'bx1x};
  
  // Multi-driven assignments
  assign fawwzt = 'bx;
  assign ehsbyuavu = tugkv;
  assign libjaob = libjaob;
endmodule: eooojwz

module bvhw (output time zomx, output byte tpkad [3:0]);
  or nzkkajqws(xsc, zomx, zomx);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  //   time zomx -> logic zomx
  //
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  //   time zomx -> logic zomx
  
  xor kf(qig, xsc, zomx);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  //   time zomx -> logic zomx
  
  nand esstixumc(zomx, xkpmxynuc, ethtcs);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  //   time zomx -> logic zomx
  
  
  // Single-driven assignments
  assign tpkad = tpkad;
  
  // Multi-driven assignments
  assign xsc = 'b0;
  assign xsc = 'b0x;
  assign ethtcs = xsc;
endmodule: bvhw

module tyha (input shortreal mjxm [1:4], output trireg logic [3:0][2:3] jbtvlykccz, input int mcktbtmyl);
  // Multi-driven assignments
  assign jbtvlykccz = jbtvlykccz;
endmodule: tyha



// Seed after: 17432163383417728379,12755700619344552825
