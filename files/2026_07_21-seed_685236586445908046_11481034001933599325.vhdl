-- Seed: 685236586445908046,11481034001933599325

library ieee;
use ieee.std_logic_1164.all;

entity prohjuolq is
  port (xf : buffer boolean; qzxypyvztu : linkage string(1 to 2); lzclnnnufg : inout std_logic_vector(3 downto 0));
end prohjuolq;

architecture aahdgm of prohjuolq is
  
begin
  -- Single-driven assignments
  xf <= xf;
  
  -- Multi-driven assignments
  lzclnnnufg <= ('U', 'Z', 'L', '0');
  lzclnnnufg <= ('L', '-', 'Z', 'U');
end aahdgm;

library ieee;
use ieee.std_logic_1164.all;

entity zdbjcyr is
  port (uttz : in bit; sbcrqecjc : in real_vector(1 downto 0); fh : linkage std_logic_vector(2 to 4); rugal : in std_logic_vector(4 downto 3));
end zdbjcyr;

library ieee;
use ieee.std_logic_1164.all;

architecture zggckjei of zdbjcyr is
  signal xxpqcsmh : string(1 to 2);
  signal juwh : boolean;
  signal ldcslryqsy : std_logic_vector(3 downto 0);
  signal mh : string(1 to 2);
  signal tuttvnac : boolean;
  signal ydvkctz : string(1 to 2);
  signal p : boolean;
  signal vkqizahqwk : std_logic_vector(3 downto 0);
  signal b : string(1 to 2);
  signal zsbzkf : boolean;
begin
  gxmmxoiz : entity work.prohjuolq
    port map (xf => zsbzkf, qzxypyvztu => b, lzclnnnufg => vkqizahqwk);
  qqnyfqnuka : entity work.prohjuolq
    port map (xf => p, qzxypyvztu => ydvkctz, lzclnnnufg => vkqizahqwk);
  cqbb : entity work.prohjuolq
    port map (xf => tuttvnac, qzxypyvztu => mh, lzclnnnufg => ldcslryqsy);
  phjiufwjva : entity work.prohjuolq
    port map (xf => juwh, qzxypyvztu => xxpqcsmh, lzclnnnufg => vkqizahqwk);
  
  -- Multi-driven assignments
  vkqizahqwk <= vkqizahqwk;
  vkqizahqwk <= "ZWL-";
end zggckjei;



-- Seed after: 16306197338028668485,11481034001933599325
