-- Seed: 13338352682291431331,10463297573877745897

library ieee;
use ieee.std_logic_1164.all;

entity lz is
  port (hovjznv : in std_logic_vector(4 downto 3); u : out real; qknidziql : in time);
end lz;

architecture t of lz is
  
begin
  -- Single-driven assignments
  u <= u;
end t;

library ieee;
use ieee.std_logic_1164.all;

entity vh is
  port ( gqjs : inout std_logic_vector(2 to 2)
  ; ll : out real_vector(4 to 1)
  ; robavv : linkage boolean_vector(2 to 4)
  ; ynvytrni : in std_logic_vector(4 downto 2)
  );
end vh;

library ieee;
use ieee.std_logic_1164.all;

architecture hixgk of vh is
  signal wgjd : time;
  signal fphm : real;
  signal ieyvnhv : std_logic_vector(4 downto 3);
begin
  swzoksw : entity work.lz
    port map (hovjznv => ieyvnhv, u => fphm, qknidziql => wgjd);
  
  -- Single-driven assignments
  ll <= ll;
  wgjd <= wgjd;
  
  -- Multi-driven assignments
  gqjs <= (others => 'Z');
  gqjs <= gqjs;
  ieyvnhv <= ieyvnhv;
end hixgk;

library ieee;
use ieee.std_logic_1164.all;

entity gb is
  port (mjfdtks : buffer std_logic; lj : buffer integer; lwfthdvsd : linkage std_logic_vector(4 downto 3));
end gb;

library ieee;
use ieee.std_logic_1164.all;

architecture bendu of gb is
  signal cynaibj : std_logic_vector(4 downto 2);
  signal p : boolean_vector(2 to 4);
  signal replm : real_vector(4 to 1);
  signal eskgl : std_logic_vector(2 to 2);
  signal huinkevzdc : boolean_vector(2 to 4);
  signal uxgojucpug : real_vector(4 to 1);
  signal vgei : std_logic_vector(2 to 2);
  signal ztizu : std_logic_vector(4 downto 2);
  signal xijrwvbuiv : boolean_vector(2 to 4);
  signal eetbzq : real_vector(4 to 1);
  signal jnegjso : std_logic_vector(2 to 2);
  signal jr : time;
  signal fpt : real;
  signal s : std_logic_vector(4 downto 3);
begin
  vtvrf : entity work.lz
    port map (hovjznv => s, u => fpt, qknidziql => jr);
  sxyhm : entity work.vh
    port map (gqjs => jnegjso, ll => eetbzq, robavv => xijrwvbuiv, ynvytrni => ztizu);
  erftcourx : entity work.vh
    port map (gqjs => vgei, ll => uxgojucpug, robavv => huinkevzdc, ynvytrni => ztizu);
  zjjugiwew : entity work.vh
    port map (gqjs => eskgl, ll => replm, robavv => p, ynvytrni => cynaibj);
  
  -- Single-driven assignments
  lj <= lj;
  jr <= 1 hr;
end bendu;

library ieee;
use ieee.std_logic_1164.all;

entity oiuuvzbwph is
  port (ko : in boolean; gl : buffer std_logic; ruvdnnw : in std_logic; ceitlex : inout real_vector(4 to 2));
end oiuuvzbwph;

architecture sqrskdh of oiuuvzbwph is
  
begin
  -- Multi-driven assignments
  gl <= 'U';
  gl <= gl;
  gl <= '0';
end sqrskdh;



-- Seed after: 11228015284543713950,10463297573877745897
