-- Seed: 7193999964719609137,8437298063418820479

library ieee;
use ieee.std_logic_1164.all;

entity ghtyrmngvt is
  port (ebuqwd : linkage std_logic_vector(0 to 3); gm : in severity_level; krcxtsr : in time; g : linkage time);
end ghtyrmngvt;

architecture ncl of ghtyrmngvt is
  
begin
  
end ncl;

library ieee;
use ieee.std_logic_1164.all;

entity lpb is
  port (lymugnptv : linkage std_logic);
end lpb;

library ieee;
use ieee.std_logic_1164.all;

architecture rffnivxiv of lpb is
  signal fmirmisk : time;
  signal vaxycbb : severity_level;
  signal xpdyct : time;
  signal dda : std_logic_vector(0 to 3);
  signal gfqsyal : time;
  signal xoqq : std_logic_vector(0 to 3);
  signal zryrvvqlpm : time;
  signal cb : time;
  signal dbrgoth : severity_level;
  signal piu : std_logic_vector(0 to 3);
begin
  hwdn : entity work.ghtyrmngvt
    port map (ebuqwd => piu, gm => dbrgoth, krcxtsr => cb, g => zryrvvqlpm);
  cmoen : entity work.ghtyrmngvt
    port map (ebuqwd => xoqq, gm => dbrgoth, krcxtsr => cb, g => gfqsyal);
  qpobzuhui : entity work.ghtyrmngvt
    port map (ebuqwd => dda, gm => dbrgoth, krcxtsr => cb, g => xpdyct);
  nmb : entity work.ghtyrmngvt
    port map (ebuqwd => piu, gm => vaxycbb, krcxtsr => fmirmisk, g => cb);
  
  -- Multi-driven assignments
  piu <= "WHL0";
  piu <= "U0U1";
  piu <= xoqq;
end rffnivxiv;



-- Seed after: 11119218070633565031,8437298063418820479
