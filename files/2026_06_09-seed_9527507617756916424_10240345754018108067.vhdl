-- Seed: 9527507617756916424,10240345754018108067

library ieee;
use ieee.std_logic_1164.all;

entity dqiku is
  port (mmdewll : buffer severity_level; krjgcafle : out bit_vector(2 to 2); nillpv : in std_logic; gvtwmkr : linkage real);
end dqiku;



architecture kernqnjtj of dqiku is
  
begin
  
end kernqnjtj;



entity fsjxyf is
  port (zenosnfdzs : in time);
end fsjxyf;

library ieee;
use ieee.std_logic_1164.all;

architecture pywiikybcp of fsjxyf is
  signal p : std_logic;
  signal akfkisvat : bit_vector(2 to 2);
  signal acda : severity_level;
  signal ysv : real;
  signal lcfs : std_logic;
  signal wm : bit_vector(2 to 2);
  signal flkkircy : severity_level;
  signal ix : real;
  signal b : std_logic;
  signal rqzyv : bit_vector(2 to 2);
  signal jihazn : severity_level;
begin
  lhuyslj : entity work.dqiku
    port map (mmdewll => jihazn, krjgcafle => rqzyv, nillpv => b, gvtwmkr => ix);
  nxtyje : entity work.dqiku
    port map (mmdewll => flkkircy, krjgcafle => wm, nillpv => lcfs, gvtwmkr => ysv);
  amhsxeddga : entity work.dqiku
    port map (mmdewll => acda, krjgcafle => akfkisvat, nillpv => p, gvtwmkr => ix);
end pywiikybcp;



-- Seed after: 2900807414708151096,10240345754018108067
