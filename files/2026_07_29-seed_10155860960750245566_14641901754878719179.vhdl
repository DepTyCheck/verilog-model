-- Seed: 10155860960750245566,14641901754878719179

entity ceuoxxt is
  port (mwopgqm : buffer real; e : buffer boolean; kcfy : in severity_level; rdasmativ : out bit);
end ceuoxxt;

architecture bmmswx of ceuoxxt is
  
begin
  -- Single-driven assignments
  mwopgqm <= mwopgqm;
end bmmswx;

library ieee;
use ieee.std_logic_1164.all;

entity q is
  port (s : linkage std_logic; eockhzzfb : in std_logic; wpxxcfs : linkage time);
end q;

architecture flhea of q is
  signal z : bit;
  signal myy : severity_level;
  signal pa : boolean;
  signal xxqslb : real;
  signal cushc : bit;
  signal whejmm : boolean;
  signal lbcfeu : real;
  signal lyfqdrzmgw : bit;
  signal nkmb : severity_level;
  signal ay : boolean;
  signal jtn : real;
  signal vrnk : bit;
  signal cqmj : severity_level;
  signal efilftotkz : boolean;
  signal ukvtz : real;
begin
  touvblndse : entity work.ceuoxxt
    port map (mwopgqm => ukvtz, e => efilftotkz, kcfy => cqmj, rdasmativ => vrnk);
  o : entity work.ceuoxxt
    port map (mwopgqm => jtn, e => ay, kcfy => nkmb, rdasmativ => lyfqdrzmgw);
  nt : entity work.ceuoxxt
    port map (mwopgqm => lbcfeu, e => whejmm, kcfy => nkmb, rdasmativ => cushc);
  rrwdyzizm : entity work.ceuoxxt
    port map (mwopgqm => xxqslb, e => pa, kcfy => myy, rdasmativ => z);
  
  -- Single-driven assignments
  nkmb <= WARNING;
  myy <= WARNING;
  cqmj <= FAILURE;
end flhea;

entity txphfsvh is
  port (qvb : inout integer; nv : out real);
end txphfsvh;

library ieee;
use ieee.std_logic_1164.all;

architecture dgfmofzk of txphfsvh is
  signal xgemltqp : bit;
  signal mpxpew : severity_level;
  signal n : boolean;
  signal a : real;
  signal cnvylr : bit;
  signal f : severity_level;
  signal lcbc : boolean;
  signal imxeecyv : real;
  signal mkkonjlpcv : time;
  signal nwfcq : std_logic;
  signal x : std_logic;
begin
  age : entity work.q
    port map (s => x, eockhzzfb => nwfcq, wpxxcfs => mkkonjlpcv);
  ifkvyc : entity work.ceuoxxt
    port map (mwopgqm => imxeecyv, e => lcbc, kcfy => f, rdasmativ => cnvylr);
  peeqhvxjh : entity work.ceuoxxt
    port map (mwopgqm => a, e => n, kcfy => mpxpew, rdasmativ => xgemltqp);
  
  -- Single-driven assignments
  mpxpew <= WARNING;
  qvb <= qvb;
  nv <= a;
  
  -- Multi-driven assignments
  x <= '-';
  x <= 'X';
  x <= x;
end dgfmofzk;

entity bridmdwxi is
  port (uwyefok : in boolean_vector(4 to 0); kzslmmsk : linkage time);
end bridmdwxi;

architecture etm of bridmdwxi is
  signal aftt : bit;
  signal wxe : boolean;
  signal qx : real;
  signal gy : bit;
  signal ienib : boolean;
  signal h : real;
  signal nbmd : bit;
  signal furortbriy : severity_level;
  signal xnj : boolean;
  signal pc : real;
begin
  w : entity work.ceuoxxt
    port map (mwopgqm => pc, e => xnj, kcfy => furortbriy, rdasmativ => nbmd);
  kepu : entity work.ceuoxxt
    port map (mwopgqm => h, e => ienib, kcfy => furortbriy, rdasmativ => gy);
  xbawdv : entity work.ceuoxxt
    port map (mwopgqm => qx, e => wxe, kcfy => furortbriy, rdasmativ => aftt);
  
  -- Single-driven assignments
  furortbriy <= ERROR;
end etm;



-- Seed after: 8334956333329164838,14641901754878719179
