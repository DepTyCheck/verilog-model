-- Seed: 2094102415917664140,11181851762153539145



entity i is
  port (fn : buffer time; umnv : inout severity_level; bpfs : in real);
end i;



architecture vl of i is
  
begin
  
end vl;

library ieee;
use ieee.std_logic_1164.all;

entity etbge is
  port (djvludiv : in std_logic; wmndgyiq : inout bit; wvbiqtzeah : buffer integer);
end etbge;



architecture kihyrr of etbge is
  signal zf : severity_level;
  signal efegbv : time;
  signal dhr : real;
  signal lalktpjr : severity_level;
  signal ploit : time;
  signal q : real;
  signal s : severity_level;
  signal dwyylhhds : time;
  signal sclbxc : real;
  signal mhpb : severity_level;
  signal iixfattp : time;
begin
  lvpnwbkici : entity work.i
    port map (fn => iixfattp, umnv => mhpb, bpfs => sclbxc);
  tcqlfrfvy : entity work.i
    port map (fn => dwyylhhds, umnv => s, bpfs => q);
  wwfqhefwlc : entity work.i
    port map (fn => ploit, umnv => lalktpjr, bpfs => dhr);
  ofm : entity work.i
    port map (fn => efegbv, umnv => zf, bpfs => sclbxc);
end kihyrr;



entity g is
  port (revqnuynvg : out real_vector(3 downto 2));
end g;



architecture bxntqylgm of g is
  
begin
  
end bxntqylgm;



-- Seed after: 7569131409287810647,11181851762153539145
