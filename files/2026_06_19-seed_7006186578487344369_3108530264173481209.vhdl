-- Seed: 7006186578487344369,3108530264173481209

entity eg is
  port (fryg : in boolean);
end eg;

architecture zvpfek of eg is
  
begin
  
end zvpfek;

library ieee;
use ieee.std_logic_1164.all;

entity n is
  port (aircyn : linkage time_vector(3 downto 2); qrt : in std_logic; uskauw : in character; el : out time);
end n;

architecture mfr of n is
  signal r : boolean;
  signal yc : boolean;
  signal ffgtcwen : boolean;
  signal fxb : boolean;
begin
  zvgbze : entity work.eg
    port map (fryg => fxb);
  oqyw : entity work.eg
    port map (fryg => ffgtcwen);
  knrcshk : entity work.eg
    port map (fryg => yc);
  ioeo : entity work.eg
    port map (fryg => r);
  
  -- Single-driven assignments
  fxb <= TRUE;
  el <= 8#4.035# ms;
  yc <= FALSE;
end mfr;

library ieee;
use ieee.std_logic_1164.all;

entity ctxindwxu is
  port (pdgu : in std_logic_vector(2 to 1));
end ctxindwxu;

architecture ac of ctxindwxu is
  signal moiofs : boolean;
  signal spqfylgofn : boolean;
  signal ednccbbyzb : boolean;
begin
  hcvxmmbo : entity work.eg
    port map (fryg => ednccbbyzb);
  ctna : entity work.eg
    port map (fryg => spqfylgofn);
  dinogtmjxj : entity work.eg
    port map (fryg => ednccbbyzb);
  hgi : entity work.eg
    port map (fryg => moiofs);
end ac;

entity otgnwk is
  port (kvgrayrdgq : in bit_vector(1 downto 2); tkbzs : out boolean_vector(4 downto 3); vy : inout time);
end otgnwk;

library ieee;
use ieee.std_logic_1164.all;

architecture yk of otgnwk is
  signal fc : character;
  signal xdm : time_vector(3 downto 2);
  signal jyvjdpicuz : boolean;
  signal blgslldmd : time;
  signal vtjh : character;
  signal ewmpxittqr : std_logic;
  signal rsocayfvn : time_vector(3 downto 2);
begin
  vlvzzhlav : entity work.n
    port map (aircyn => rsocayfvn, qrt => ewmpxittqr, uskauw => vtjh, el => blgslldmd);
  wwxcy : entity work.eg
    port map (fryg => jyvjdpicuz);
  vzgrijflia : entity work.n
    port map (aircyn => xdm, qrt => ewmpxittqr, uskauw => fc, el => vy);
  
  -- Single-driven assignments
  tkbzs <= (FALSE, TRUE);
  
  -- Multi-driven assignments
  ewmpxittqr <= 'U';
  ewmpxittqr <= '0';
  ewmpxittqr <= 'W';
  ewmpxittqr <= 'Z';
end yk;



-- Seed after: 4078019820334140936,3108530264173481209
