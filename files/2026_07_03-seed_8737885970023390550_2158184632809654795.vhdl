-- Seed: 8737885970023390550,2158184632809654795

use std.reflection.all;

entity v is
  port (pv : inout protected_value_mirror);
end v;

architecture mw of v is
  
begin
  
end mw;

use std.reflection.all;

entity urknlb is
  port (vhu : inout integer_subtype_mirror; lnmtxif : linkage time; twgwb : inout enumeration_value_mirror; cge : inout record_value_mirror);
end urknlb;

use std.reflection.all;

architecture qxvycnhqm of urknlb is
  shared variable skhnjx : protected_value_mirror;
  shared variable erwzv : protected_value_mirror;
  shared variable qoiktmkxm : protected_value_mirror;
  shared variable lx : protected_value_mirror;
begin
  bk : entity work.v
    port map (pv => lx);
  mux : entity work.v
    port map (pv => qoiktmkxm);
  qxbr : entity work.v
    port map (pv => erwzv);
  icfgo : entity work.v
    port map (pv => skhnjx);
end qxvycnhqm;



-- Seed after: 5534064418370520204,2158184632809654795
