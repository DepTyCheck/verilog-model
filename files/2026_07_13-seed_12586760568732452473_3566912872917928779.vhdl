-- Seed: 12586760568732452473,3566912872917928779

use std.reflection.all;

entity endm is
  port (njzo : inout protected_subtype_mirror; faxlujl : inout subtype_mirror; xglfy : buffer time);
end endm;

architecture cjdol of endm is
  
begin
  
end cjdol;

use std.reflection.all;

entity rgo is
  port ( vincknmud : inout enumeration_subtype_mirror
  ; qdzs : inout boolean_vector(2 downto 0)
  ; qkysktalq : inout character
  ; rfvkaugk : inout record_value_mirror
  );
end rgo;

use std.reflection.all;

architecture jhi of rgo is
  signal mm : time;
  shared variable udcdql : subtype_mirror;
  shared variable koqfsfgznv : protected_subtype_mirror;
  signal boprgrzgu : time;
  shared variable ljblcia : subtype_mirror;
  shared variable e : protected_subtype_mirror;
begin
  lbjqwekux : entity work.endm
    port map (njzo => e, faxlujl => ljblcia, xglfy => boprgrzgu);
  xgfkppcv : entity work.endm
    port map (njzo => koqfsfgznv, faxlujl => udcdql, xglfy => mm);
  
  -- Single-driven assignments
  qkysktalq <= qkysktalq;
  qdzs <= (FALSE, FALSE, FALSE);
end jhi;



-- Seed after: 3608260862426840448,3566912872917928779
