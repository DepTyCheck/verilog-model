-- Seed: 18408359775855096061,1852660963590380551

library ieee;
use ieee.std_logic_1164.all;

entity opql is
  port (rfxayasf : in std_logic; cty : linkage real; dodc : linkage std_logic_vector(3 to 2); djbxa : inout bit_vector(3 to 2));
end opql;



architecture kujkfz of opql is
  
begin
  
end kujkfz;

library ieee;
use ieee.std_logic_1164.all;

entity naulxfiwde is
  port (uf : linkage std_logic; bcexpcdzbw : out time; xpkue : in real; kogmaquwv : buffer integer);
end naulxfiwde;

library ieee;
use ieee.std_logic_1164.all;

architecture vxid of naulxfiwde is
  signal ytrrbapf : bit_vector(3 to 2);
  signal bbrlt : std_logic_vector(3 to 2);
  signal i : real;
  signal mulvt : std_logic;
begin
  rblwxdc : entity work.opql
    port map (rfxayasf => mulvt, cty => i, dodc => bbrlt, djbxa => ytrrbapf);
end vxid;



entity w is
  port (gkgombim : linkage bit_vector(0 downto 4); qbsoyuiat : buffer real; oxhdaaraq : inout boolean_vector(0 to 3); k : linkage bit_vector(0 to 1));
end w;

library ieee;
use ieee.std_logic_1164.all;

architecture vper of w is
  signal zknlvwfzh : integer;
  signal enjrztxcf : real;
  signal gsaqjerin : time;
  signal upkogymbwm : bit_vector(3 to 2);
  signal jektpqsvxt : real;
  signal hqoniljtpz : integer;
  signal alaocba : time;
  signal ez : bit_vector(3 to 2);
  signal b : std_logic_vector(3 to 2);
  signal dxno : std_logic;
begin
  tgafof : entity work.opql
    port map (rfxayasf => dxno, cty => qbsoyuiat, dodc => b, djbxa => ez);
  ujg : entity work.naulxfiwde
    port map (uf => dxno, bcexpcdzbw => alaocba, xpkue => qbsoyuiat, kogmaquwv => hqoniljtpz);
  cztiq : entity work.opql
    port map (rfxayasf => dxno, cty => jektpqsvxt, dodc => b, djbxa => upkogymbwm);
  oe : entity work.naulxfiwde
    port map (uf => dxno, bcexpcdzbw => gsaqjerin, xpkue => enjrztxcf, kogmaquwv => zknlvwfzh);
end vper;



entity vm is
  port (m : buffer severity_level; bh : inout real);
end vm;

library ieee;
use ieee.std_logic_1164.all;

architecture im of vm is
  signal pyz : bit_vector(3 to 2);
  signal qtzuwdm : real;
  signal qmctnmzzhm : std_logic;
  signal hlvfo : bit_vector(3 to 2);
  signal yklswq : std_logic_vector(3 to 2);
  signal sfhqjrkek : real;
  signal skldfoa : std_logic;
begin
  llbx : entity work.opql
    port map (rfxayasf => skldfoa, cty => sfhqjrkek, dodc => yklswq, djbxa => hlvfo);
  n : entity work.opql
    port map (rfxayasf => qmctnmzzhm, cty => qtzuwdm, dodc => yklswq, djbxa => pyz);
end im;



-- Seed after: 11166250218534473237,1852660963590380551
