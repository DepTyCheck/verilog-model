-- Seed: 12742839672092895414,7332793847894666635

library ieee;
use ieee.std_logic_1164.all;

entity pszown is
  port (zfpktbkjek : inout std_logic; l : in std_logic; pihwye : linkage integer_vector(3 to 2); tlmkylgllr : out time);
end pszown;



architecture kw of pszown is
  
begin
  
end kw;



entity pth is
  port (zfy : out real_vector(4 to 0); vch : inout real_vector(2 to 2));
end pth;

library ieee;
use ieee.std_logic_1164.all;

architecture lvtpxlg of pth is
  signal cklrjrp : time;
  signal aian : integer_vector(3 to 2);
  signal ujgwn : std_logic;
  signal uddqwqlrbs : time;
  signal qj : std_logic;
  signal c : time;
  signal twy : integer_vector(3 to 2);
  signal mgfkgvgyd : time;
  signal gbupvtsfj : integer_vector(3 to 2);
  signal uejzzyat : std_logic;
  signal qohrieo : std_logic;
begin
  foes : entity work.pszown
    port map (zfpktbkjek => qohrieo, l => uejzzyat, pihwye => gbupvtsfj, tlmkylgllr => mgfkgvgyd);
  vhigvmxh : entity work.pszown
    port map (zfpktbkjek => qohrieo, l => qohrieo, pihwye => twy, tlmkylgllr => c);
  oph : entity work.pszown
    port map (zfpktbkjek => qohrieo, l => qj, pihwye => twy, tlmkylgllr => uddqwqlrbs);
  ptlbkufb : entity work.pszown
    port map (zfpktbkjek => ujgwn, l => qohrieo, pihwye => aian, tlmkylgllr => cklrjrp);
end lvtpxlg;



-- Seed after: 12738687446097282424,7332793847894666635
