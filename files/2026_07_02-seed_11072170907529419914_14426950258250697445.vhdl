-- Seed: 11072170907529419914,14426950258250697445

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity rgli is
  port (kh : inout std_logic_vector(1 to 2); vexe : inout string(2 to 2); eakybpndkg : in std_logic; mmz : inout access_subtype_mirror);
end rgli;

architecture bvstgfcrpj of rgli is
  
begin
  -- Multi-driven assignments
  kh <= kh;
  kh <= ('-', 'L');
  kh <= kh;
end bvstgfcrpj;

library ieee;
use ieee.std_logic_1164.all;

entity axm is
  port (swuuls : linkage std_logic_vector(2 to 0));
end axm;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture kvqcrtorxq of axm is
  shared variable pbgfq : access_subtype_mirror;
  signal itp : std_logic;
  signal umpp : string(2 to 2);
  shared variable gzk : access_subtype_mirror;
  signal zfvhu : string(2 to 2);
  shared variable ljsljblt : access_subtype_mirror;
  signal vhqgy : std_logic;
  signal u : string(2 to 2);
  signal lf : std_logic_vector(1 to 2);
begin
  zyddhicgj : entity work.rgli
    port map (kh => lf, vexe => u, eakybpndkg => vhqgy, mmz => ljsljblt);
  uqndbvfjj : entity work.rgli
    port map (kh => lf, vexe => zfvhu, eakybpndkg => vhqgy, mmz => gzk);
  wezph : entity work.rgli
    port map (kh => lf, vexe => umpp, eakybpndkg => itp, mmz => pbgfq);
  
  -- Multi-driven assignments
  lf <= lf;
  lf <= ('0', '1');
end kvqcrtorxq;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity dsukanzj is
  port ( bexumptbkd : inout floating_value_mirror
  ; k : inout enumeration_subtype_mirror
  ; ookoxybzy : inout std_logic_vector(2 downto 3)
  ; r : inout array_subtype_mirror
  );
end dsukanzj;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture fhbrrh of dsukanzj is
  shared variable ashpxoclo : access_subtype_mirror;
  signal vxklhysytc : std_logic;
  signal hdkla : string(2 to 2);
  signal j : std_logic_vector(1 to 2);
begin
  wmohkwr : entity work.rgli
    port map (kh => j, vexe => hdkla, eakybpndkg => vxklhysytc, mmz => ashpxoclo);
  
  -- Multi-driven assignments
  ookoxybzy <= "";
end fhbrrh;



-- Seed after: 15827435699640127010,14426950258250697445
