-- Seed: 16491355185562399064,14426950258250697445

use std.reflection.all;

entity fidkmqmj is
  port (af : inout subtype_mirror);
end fidkmqmj;

architecture rdpxrck of fidkmqmj is
  
begin
  
end rdpxrck;

use std.reflection.all;

entity waavlb is
  port (ztgbsaet : linkage bit_vector(2 downto 4); setatq : inout record_value_mirror; djrwcmia : out time_vector(4 downto 3));
end waavlb;

use std.reflection.all;

architecture tilkn of waavlb is
  shared variable z : subtype_mirror;
  shared variable dlkl : subtype_mirror;
  shared variable tedjjaw : subtype_mirror;
  shared variable pc : subtype_mirror;
begin
  tccmir : entity work.fidkmqmj
    port map (af => pc);
  cjdhmp : entity work.fidkmqmj
    port map (af => tedjjaw);
  gwacii : entity work.fidkmqmj
    port map (af => dlkl);
  mzznf : entity work.fidkmqmj
    port map (af => z);
  
  -- Single-driven assignments
  djrwcmia <= (3_2_1_0_0.2 ns, 1 hr);
end tilkn;

library ieee;
use ieee.std_logic_1164.all;

entity kkrra is
  port (f : buffer std_logic);
end kkrra;

use std.reflection.all;

architecture rkswaa of kkrra is
  shared variable fxtore : subtype_mirror;
  shared variable ocikbd : subtype_mirror;
  shared variable wib : subtype_mirror;
begin
  rrqnnvwtd : entity work.fidkmqmj
    port map (af => wib);
  oeh : entity work.fidkmqmj
    port map (af => ocikbd);
  n : entity work.fidkmqmj
    port map (af => fxtore);
  
  -- Multi-driven assignments
  f <= f;
  f <= 'W';
end rkswaa;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity tdo is
  port ( ilefmcvqc : linkage time
  ; bmihzysp : inout subtype_mirror
  ; begrw : inout boolean_vector(4 downto 0)
  ; hxqdwutzq : inout std_logic_vector(0 to 4)
  );
end tdo;

use std.reflection.all;

architecture fo of tdo is
  shared variable wz : subtype_mirror;
begin
  d : entity work.fidkmqmj
    port map (af => wz);
  
  -- Single-driven assignments
  begrw <= begrw;
end fo;



-- Seed after: 8453283925088248637,14426950258250697445
