-- Seed: 2296230613556697837,2983771601630957889

use std.reflection.all;

entity qpdkaarqs is
  port (variable paxvpjytov : inout record_value_mirror_pt; bvudkammi : buffer real; zkcsmmr : out boolean);
end qpdkaarqs;

architecture lyde of qpdkaarqs is
  
begin
  -- Single-driven assignments
  zkcsmmr <= zkcsmmr;
end lyde;

use std.reflection.all;

entity fxfqr is
  port ( variable fjyncroelz : inout enumeration_subtype_mirror_pt
  ; variable kivjqpvitt : inout physical_subtype_mirror_pt
  ; variable octsimf : inout integer_value_mirror_pt
  ; n : inout boolean_vector(3 downto 3)
  );
end fxfqr;

use std.reflection.all;

architecture ykbnfmixe of fxfqr is
  signal g : boolean;
  signal lbmvlmjq : real;
  shared variable t : record_value_mirror_pt;
  signal ejqzk : boolean;
  signal oknsxaxyiz : real;
  shared variable wb : record_value_mirror_pt;
begin
  kex : entity work.qpdkaarqs
    port map (paxvpjytov => wb, bvudkammi => oknsxaxyiz, zkcsmmr => ejqzk);
  qytwnue : entity work.qpdkaarqs
    port map (paxvpjytov => t, bvudkammi => lbmvlmjq, zkcsmmr => g);
end ykbnfmixe;

use std.reflection.all;

entity uwsik is
  port (variable hbe : inout physical_subtype_mirror_pt; variable b : inout file_value_mirror_pt; variable x : inout floating_subtype_mirror_pt);
end uwsik;

use std.reflection.all;

architecture sppiw of uwsik is
  signal qtq : boolean;
  signal avgk : real;
  shared variable r : record_value_mirror_pt;
  signal unimosox : boolean;
  signal lpkz : real;
  shared variable nbvgt : record_value_mirror_pt;
  signal katernm : boolean;
  signal rnlyqstkb : real;
  shared variable pc : record_value_mirror_pt;
begin
  ocdohvk : entity work.qpdkaarqs
    port map (paxvpjytov => pc, bvudkammi => rnlyqstkb, zkcsmmr => katernm);
  pjzcltteij : entity work.qpdkaarqs
    port map (paxvpjytov => nbvgt, bvudkammi => lpkz, zkcsmmr => unimosox);
  xbgymm : entity work.qpdkaarqs
    port map (paxvpjytov => r, bvudkammi => avgk, zkcsmmr => qtq);
end sppiw;

entity d is
  port (x : inout time);
end d;

use std.reflection.all;

architecture sp of d is
  signal tvig : boolean;
  signal chkwg : real;
  shared variable kagslym : record_value_mirror_pt;
begin
  drctahtys : entity work.qpdkaarqs
    port map (paxvpjytov => kagslym, bvudkammi => chkwg, zkcsmmr => tvig);
  
  -- Single-driven assignments
  x <= x;
end sp;



-- Seed after: 10996924987854197244,2983771601630957889
