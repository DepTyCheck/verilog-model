-- Seed: 1051060194145111638,5511103086789671269

entity gbmmr is
  port (y : linkage integer);
end gbmmr;

architecture cbsuxrom of gbmmr is
  
begin
  
end cbsuxrom;

entity nue is
  port (kgyjv : in character);
end nue;

architecture lba of nue is
  
begin
  
end lba;

library ieee;
use ieee.std_logic_1164.all;

entity xnauznyivq is
  port (erdwdj : inout character; h : inout std_logic; pklyxmeai : linkage boolean_vector(0 to 4));
end xnauznyivq;

architecture gv of xnauznyivq is
  signal rcr : integer;
  signal nwcihacnnt : integer;
  signal s : integer;
  signal aahzzuw : integer;
begin
  ted : entity work.gbmmr
    port map (y => aahzzuw);
  arizim : entity work.gbmmr
    port map (y => s);
  tkkc : entity work.gbmmr
    port map (y => nwcihacnnt);
  mgnjmp : entity work.gbmmr
    port map (y => rcr);
  
  -- Single-driven assignments
  erdwdj <= erdwdj;
  
  -- Multi-driven assignments
  h <= h;
  h <= h;
  h <= h;
  h <= 'Z';
end gv;

entity lujuci is
  port (gbiqc : linkage string(4 to 4));
end lujuci;

library ieee;
use ieee.std_logic_1164.all;

architecture eoyyhrpq of lujuci is
  signal qoucvufk : integer;
  signal u : boolean_vector(0 to 4);
  signal wf : std_logic;
  signal kk : integer;
  signal rcnm : character;
begin
  v : entity work.nue
    port map (kgyjv => rcnm);
  p : entity work.gbmmr
    port map (y => kk);
  tm : entity work.xnauznyivq
    port map (erdwdj => rcnm, h => wf, pklyxmeai => u);
  xf : entity work.gbmmr
    port map (y => qoucvufk);
  
  -- Multi-driven assignments
  wf <= wf;
end eoyyhrpq;



-- Seed after: 1776498740408542356,5511103086789671269
