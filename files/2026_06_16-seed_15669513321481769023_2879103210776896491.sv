// Seed: 15669513321481769023,2879103210776896491

module wabyqu ();
  and hqgosgtfb(pexafnx, pexafnx, pexafnx);
  
  
  // Multi-driven assignments
  assign pexafnx = 'b0;
endmodule: wabyqu

module wpjs (output bit [3:1] iackcrdc, inout trireg logic pklgw);
  xnor vttnokel(gngyzzvgof, ax, lxtkdirr);
  
  and zdmdr(lxtkdirr, z, iackcrdc);
  // warning: implicit conversion of port connection truncates from 3 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [3:1] iackcrdc -> logic iackcrdc
  
  or zcpmonv(lxtkdirr, bntez, a);
  
  xnor vtglzoeu(iackcrdc, iackcrdc, ax);
  // warning: implicit conversion of port connection truncates from 3 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [3:1] iackcrdc -> logic iackcrdc
  //
  // warning: implicit conversion of port connection truncates from 3 to 1 bits
  // warning: implicit conversion changes possible bit states from 2-state to 4-state
  //   bit [3:1] iackcrdc -> logic iackcrdc
  
  
  // Multi-driven assignments
  assign pklgw = pklgw;
  assign a = 'bz;
endmodule: wpjs

module mo ();
  nand ibsr(peastvg, uej, vqrmgexr);
  
  or xdilapaje(uej, qatkifjepd, qatkifjepd);
  
  wpjs i(.iackcrdc(ufetvy), .pklgw(mqn));
  // warning: implicit conversion of port connection expands from 1 to 3 bits
  // warning: implicit conversion changes possible bit states from 4-state to 2-state
  //   wire logic ufetvy -> bit [3:1] iackcrdc
  
  
  // Multi-driven assignments
  assign vqrmgexr = vqrmgexr;
  assign vqrmgexr = vqrmgexr;
  assign qatkifjepd = 'b10;
endmodule: mo



// Seed after: 8762271690120921060,2879103210776896491
