-- Seed: 9560468276273136343,8067602802092121131

entity acseodwg is
  port (qtgakdkev : out boolean; sxob : buffer real; zldct : buffer real);
end acseodwg;

architecture rayveysv of acseodwg is
  
begin
  
end rayveysv;

library ieee;
use ieee.std_logic_1164.all;

entity mdtouawe is
  port (gltljejpd : linkage std_logic; em : linkage std_logic_vector(3 downto 2));
end mdtouawe;

architecture nyrhza of mdtouawe is
  signal dayvgho : real;
  signal zdkfp : real;
  signal beciom : boolean;
  signal egyqhy : real;
  signal jok : real;
  signal oymihr : boolean;
  signal ou : real;
  signal nugyipd : real;
  signal ms : boolean;
begin
  kqka : entity work.acseodwg
    port map (qtgakdkev => ms, sxob => nugyipd, zldct => ou);
  ckqrmrv : entity work.acseodwg
    port map (qtgakdkev => oymihr, sxob => jok, zldct => egyqhy);
  kozgszu : entity work.acseodwg
    port map (qtgakdkev => beciom, sxob => zdkfp, zldct => dayvgho);
end nyrhza;

entity gazxfgf is
  port (wph : out real_vector(2 to 0));
end gazxfgf;

architecture o of gazxfgf is
  signal oaxeovc : real;
  signal affmlpw : real;
  signal blhmlztt : boolean;
  signal le : real;
  signal jbpald : real;
  signal qiqvcdx : boolean;
begin
  yg : entity work.acseodwg
    port map (qtgakdkev => qiqvcdx, sxob => jbpald, zldct => le);
  rwiimbwrow : entity work.acseodwg
    port map (qtgakdkev => blhmlztt, sxob => affmlpw, zldct => oaxeovc);
  
  -- Single-driven assignments
  wph <= wph;
end o;



-- Seed after: 2190918382915886480,8067602802092121131
