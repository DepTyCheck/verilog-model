-- Seed: 17063185301375381478,2230106469645304029

library ieee;
use ieee.std_logic_1164.all;

entity yneako is
  port (vguey : inout integer; ry : buffer real_vector(2 downto 3); lg : in std_logic_vector(4 to 0));
end yneako;

architecture mpjqixbs of yneako is
  
begin
  -- Single-driven assignments
  vguey <= 8#65034#;
  ry <= (others => 0.0);
end mpjqixbs;

library ieee;
use ieee.std_logic_1164.all;

entity lwxdolx is
  port (t : in std_logic; fvah : linkage time);
end lwxdolx;

library ieee;
use ieee.std_logic_1164.all;

architecture yqhegtqlyt of lwxdolx is
  signal w : std_logic_vector(4 to 0);
  signal yuseep : real_vector(2 downto 3);
  signal qgfeiqbuj : integer;
  signal whl : real_vector(2 downto 3);
  signal vupd : integer;
  signal iyy : real_vector(2 downto 3);
  signal dtnwaxqtgs : integer;
  signal zloukmhbb : std_logic_vector(4 to 0);
  signal fxhih : real_vector(2 downto 3);
  signal zkdlawzs : integer;
begin
  lnciczxo : entity work.yneako
    port map (vguey => zkdlawzs, ry => fxhih, lg => zloukmhbb);
  qw : entity work.yneako
    port map (vguey => dtnwaxqtgs, ry => iyy, lg => zloukmhbb);
  ggqxdulum : entity work.yneako
    port map (vguey => vupd, ry => whl, lg => zloukmhbb);
  bxsjdis : entity work.yneako
    port map (vguey => qgfeiqbuj, ry => yuseep, lg => w);
  
  -- Multi-driven assignments
  zloukmhbb <= (others => '0');
  zloukmhbb <= zloukmhbb;
  zloukmhbb <= zloukmhbb;
  zloukmhbb <= (others => '0');
end yqhegtqlyt;



-- Seed after: 14983570944066195817,2230106469645304029
