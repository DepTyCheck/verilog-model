-- Seed: 9491807404669476305,4404421571376382767

entity uyjfwgb is
  port (apuzsbnt : in character; mu : buffer time; uwqlxrhm : in severity_level; yjg : linkage real);
end uyjfwgb;

architecture nftdjjthvj of uyjfwgb is
  
begin
  -- Single-driven assignments
  mu <= 2#00.1_0_1_0# us;
end nftdjjthvj;

entity hvar is
  port (sxxiz : buffer boolean_vector(0 to 0); ltasztty : in bit_vector(4 downto 4));
end hvar;

architecture st of hvar is
  signal cwenz : real;
  signal varsz : severity_level;
  signal qptuqmiaxz : time;
  signal pem : character;
  signal drlu : real;
  signal xxdjpethp : time;
  signal bevuwpbxzx : real;
  signal zvawdwsgu : severity_level;
  signal lfrbv : time;
  signal trstpk : character;
begin
  iiavslewi : entity work.uyjfwgb
    port map (apuzsbnt => trstpk, mu => lfrbv, uwqlxrhm => zvawdwsgu, yjg => bevuwpbxzx);
  haz : entity work.uyjfwgb
    port map (apuzsbnt => trstpk, mu => xxdjpethp, uwqlxrhm => zvawdwsgu, yjg => drlu);
  xbqk : entity work.uyjfwgb
    port map (apuzsbnt => pem, mu => qptuqmiaxz, uwqlxrhm => varsz, yjg => cwenz);
  
  -- Single-driven assignments
  pem <= trstpk;
  trstpk <= trstpk;
  varsz <= WARNING;
end st;

library ieee;
use ieee.std_logic_1164.all;

entity uipazmbks is
  port (c : in integer; nvi : buffer std_logic);
end uipazmbks;

architecture icj of uipazmbks is
  
begin
  -- Multi-driven assignments
  nvi <= 'H';
  nvi <= nvi;
end icj;

library ieee;
use ieee.std_logic_1164.all;

entity wlzvwed is
  port (selawrib : buffer std_logic; iqflfg : in bit_vector(2 to 2); oxygyybh : buffer std_logic_vector(2 to 0));
end wlzvwed;

architecture lzxjs of wlzvwed is
  signal llf : real;
  signal ff : severity_level;
  signal izcghrz : time;
  signal h : character;
begin
  bqeg : entity work.uyjfwgb
    port map (apuzsbnt => h, mu => izcghrz, uwqlxrhm => ff, yjg => llf);
  
  -- Single-driven assignments
  h <= 'g';
  ff <= ff;
  
  -- Multi-driven assignments
  selawrib <= 'Z';
  selawrib <= 'Z';
  selawrib <= selawrib;
end lzxjs;



-- Seed after: 2100276390936781064,4404421571376382767
