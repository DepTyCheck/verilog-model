-- Seed: 17665900367224150069,16159265764638711791

library ieee;
use ieee.std_logic_1164.all;

entity ozsnipme is
  port (k : inout integer; yytefrovnz : inout integer; nv : buffer std_logic; heei : buffer character);
end ozsnipme;

architecture mecreuby of ozsnipme is
  
begin
  -- Single-driven assignments
  heei <= heei;
  k <= 8#710#;
  yytefrovnz <= 1_3_1_2;
  
  -- Multi-driven assignments
  nv <= 'L';
  nv <= nv;
end mecreuby;

entity srxavqnzn is
  port (zlmk : inout real; todanz : linkage real_vector(1 downto 3));
end srxavqnzn;

library ieee;
use ieee.std_logic_1164.all;

architecture wtul of srxavqnzn is
  signal lx : character;
  signal bfrnkitjaf : std_logic;
  signal ewqll : integer;
  signal e : integer;
  signal jgps : character;
  signal j : integer;
  signal ews : integer;
  signal cqykhdtlos : character;
  signal mfwgckeb : std_logic;
  signal zcpstwsfm : integer;
  signal jrxgke : integer;
  signal oxbrny : character;
  signal hu : std_logic;
  signal edffyu : integer;
  signal lcc : integer;
begin
  olhnhdnm : entity work.ozsnipme
    port map (k => lcc, yytefrovnz => edffyu, nv => hu, heei => oxbrny);
  cnxq : entity work.ozsnipme
    port map (k => jrxgke, yytefrovnz => zcpstwsfm, nv => mfwgckeb, heei => cqykhdtlos);
  slhxxvab : entity work.ozsnipme
    port map (k => ews, yytefrovnz => j, nv => hu, heei => jgps);
  z : entity work.ozsnipme
    port map (k => e, yytefrovnz => ewqll, nv => bfrnkitjaf, heei => lx);
  
  -- Single-driven assignments
  zlmk <= zlmk;
  
  -- Multi-driven assignments
  bfrnkitjaf <= 'W';
end wtul;

library ieee;
use ieee.std_logic_1164.all;

entity uep is
  port (abc : out std_logic; w : buffer bit_vector(1 downto 0); ryt : buffer time; bvtuz : inout integer);
end uep;

library ieee;
use ieee.std_logic_1164.all;

architecture rwte of uep is
  signal wydvhs : character;
  signal qouybcric : integer;
  signal b : character;
  signal obckwgiar : std_logic;
  signal lf : integer;
  signal zjcqmuenr : integer;
  signal uldtpy : character;
  signal tu : integer;
  signal krm : integer;
begin
  jk : entity work.ozsnipme
    port map (k => krm, yytefrovnz => tu, nv => abc, heei => uldtpy);
  dl : entity work.ozsnipme
    port map (k => zjcqmuenr, yytefrovnz => lf, nv => obckwgiar, heei => b);
  ibmm : entity work.ozsnipme
    port map (k => qouybcric, yytefrovnz => bvtuz, nv => abc, heei => wydvhs);
  
  -- Multi-driven assignments
  abc <= abc;
  abc <= abc;
  obckwgiar <= abc;
end rwte;

entity h is
  port (xhql : inout time_vector(3 to 2); lxjfghzrdk : linkage boolean; jtkjc : in time);
end h;

library ieee;
use ieee.std_logic_1164.all;

architecture d of h is
  signal e : character;
  signal qeydx : std_logic;
  signal dnm : integer;
  signal nh : integer;
  signal u : real_vector(1 downto 3);
  signal qagorupash : real;
  signal itbhro : real_vector(1 downto 3);
  signal yethnrbzec : real;
  signal obzwljb : real_vector(1 downto 3);
  signal fyttqasf : real;
begin
  vuym : entity work.srxavqnzn
    port map (zlmk => fyttqasf, todanz => obzwljb);
  bioinub : entity work.srxavqnzn
    port map (zlmk => yethnrbzec, todanz => itbhro);
  ctfy : entity work.srxavqnzn
    port map (zlmk => qagorupash, todanz => u);
  bcs : entity work.ozsnipme
    port map (k => nh, yytefrovnz => dnm, nv => qeydx, heei => e);
  
  -- Single-driven assignments
  xhql <= (others => 0 ns);
  
  -- Multi-driven assignments
  qeydx <= qeydx;
  qeydx <= '0';
end d;



-- Seed after: 14062378442320676450,16159265764638711791
