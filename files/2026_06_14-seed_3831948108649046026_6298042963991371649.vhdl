-- Seed: 3831948108649046026,6298042963991371649



entity f is
  port (qt : in time; mvtn : out bit_vector(2 downto 4));
end f;



architecture texmriwmqz of f is
  
begin
  
end texmriwmqz;

library ieee;
use ieee.std_logic_1164.all;

entity ttflc is
  port (fqz : buffer std_logic; dgaxff : out real; yfknz : out time; nekkelep : out time_vector(1 to 3));
end ttflc;



architecture vzhff of ttflc is
  signal tft : bit_vector(2 downto 4);
  signal x : time;
  signal tdoy : bit_vector(2 downto 4);
  signal pbhnlt : time;
  signal drwsudhx : bit_vector(2 downto 4);
  signal xcqtipdqep : bit_vector(2 downto 4);
  signal ub : time;
begin
  ygtg : entity work.f
    port map (qt => ub, mvtn => xcqtipdqep);
  y : entity work.f
    port map (qt => yfknz, mvtn => drwsudhx);
  qwizywk : entity work.f
    port map (qt => pbhnlt, mvtn => tdoy);
  bkp : entity work.f
    port map (qt => x, mvtn => tft);
end vzhff;



-- Seed after: 14030530385849839561,6298042963991371649
