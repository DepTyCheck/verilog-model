-- Seed: 2349358022308545218,8437298063418820479

entity qbsmembhq is
  port (er : buffer time; rfgrhm : buffer real; kwh : out character);
end qbsmembhq;

architecture rosexpuajv of qbsmembhq is
  
begin
  -- Single-driven assignments
  kwh <= 'z';
end rosexpuajv;

entity f is
  port (gaydhkl : out severity_level; xbnkal : inout boolean_vector(2 downto 1); dfvppmrviq : linkage integer_vector(0 downto 1));
end f;

architecture ptxdouikds of f is
  signal ob : character;
  signal vss : real;
  signal ljgey : time;
  signal chbk : character;
  signal al : real;
  signal t : time;
  signal zvbjltmv : character;
  signal gserzmlju : real;
  signal fzdevh : time;
begin
  kiioukdvvw : entity work.qbsmembhq
    port map (er => fzdevh, rfgrhm => gserzmlju, kwh => zvbjltmv);
  b : entity work.qbsmembhq
    port map (er => t, rfgrhm => al, kwh => chbk);
  p : entity work.qbsmembhq
    port map (er => ljgey, rfgrhm => vss, kwh => ob);
  
  -- Single-driven assignments
  gaydhkl <= ERROR;
end ptxdouikds;



-- Seed after: 2808981241492901114,8437298063418820479
