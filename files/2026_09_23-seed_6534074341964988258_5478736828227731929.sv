// Seed: 6534074341964988258,5478736828227731929

module mhpsxyr (output real wupa);
  or lbyse(wzdorwnq, wupa, wupa);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   real wupa -> logic wupa
  //
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   real wupa -> logic wupa
  
  xor vix(wupa, tqqwsm, wupa);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   real wupa -> logic wupa
  //
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   real wupa -> logic wupa
  
  xnor xt(wzdorwnq, wzdorwnq, lvpii);
  
  and rffdo(wzdorwnq, v, p);
  
  
  // Multi-driven assignments
  assign wzdorwnq = v;
  assign wzdorwnq = wzdorwnq;
  assign tqqwsm = wzdorwnq;
  assign wzdorwnq = wzdorwnq;
endmodule: mhpsxyr

module xxz ();
  xnor salj(ithq, iponawzapz, ithq);
  
  mhpsxyr twln(.wupa(s));
  // warning: implicit conversion of port connection expands from 1 to 64 bits
  // warning: implicit conversion changes signedness from unsigned to signed
  //   wire logic s -> real wupa
  
  
  // Multi-driven assignments
  assign s = 'b0x;
  assign iponawzapz = ithq;
  assign iponawzapz = iponawzapz;
endmodule: xxz



// Seed after: 14146764534047175700,5478736828227731929
