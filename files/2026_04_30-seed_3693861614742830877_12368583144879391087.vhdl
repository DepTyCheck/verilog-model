-- Seed: 3693861614742830877,12368583144879391087



entity jrl is
  port (gpjmn : linkage real; qe : linkage real; stlau : out character; gm : inout time);
end jrl;



architecture jsbqorwnpx of jrl is
  
begin
  
end jsbqorwnpx;

library ieee;
use ieee.std_logic_1164.all;

entity mwxu is
  port (meupo : linkage std_logic);
end mwxu;



architecture n of mwxu is
  signal bwtyrwclcl : time;
  signal gksy : character;
  signal p : real;
  signal jshzcvi : time;
  signal d : character;
  signal fuxcxspoyy : real;
begin
  h : entity work.jrl
    port map (gpjmn => fuxcxspoyy, qe => fuxcxspoyy, stlau => d, gm => jshzcvi);
  r : entity work.jrl
    port map (gpjmn => fuxcxspoyy, qe => p, stlau => gksy, gm => bwtyrwclcl);
end n;

library ieee;
use ieee.std_logic_1164.all;

entity mr is
  port (expgxwy : in std_logic; b : buffer std_logic; eivtdjrdww : linkage real; bhqpsd : inout real);
end mr;

library ieee;
use ieee.std_logic_1164.all;

architecture oz of mr is
  signal adwpqvayjm : std_logic;
  signal mkudw : time;
  signal pzfomgmaf : character;
  signal mmupv : real;
  signal iqvmo : time;
  signal wpeebi : character;
  signal ex : real;
  signal l : real;
  signal x : time;
  signal zgeu : character;
begin
  fpkbi : entity work.jrl
    port map (gpjmn => bhqpsd, qe => bhqpsd, stlau => zgeu, gm => x);
  ejhwoh : entity work.jrl
    port map (gpjmn => l, qe => ex, stlau => wpeebi, gm => iqvmo);
  fpjxpkrpa : entity work.jrl
    port map (gpjmn => ex, qe => mmupv, stlau => pzfomgmaf, gm => mkudw);
  zncas : entity work.mwxu
    port map (meupo => adwpqvayjm);
end oz;



-- Seed after: 5699376398364384662,12368583144879391087
