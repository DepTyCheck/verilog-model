-- Seed: 14706842683106380124,13857275728440271305

library ieee;
use ieee.std_logic_1164.all;

entity kklig is
  port (tjo : out severity_level; yi : out std_logic);
end kklig;

architecture sg of kklig is
  
begin
  -- Multi-driven assignments
  yi <= yi;
  yi <= yi;
  yi <= yi;
end sg;

entity e is
  port (yb : inout time);
end e;

architecture oxtbdkn of e is
  
begin
  
end oxtbdkn;

library ieee;
use ieee.std_logic_1164.all;

entity f is
  port (hlwu : out std_logic; cl : out real; llcw : linkage boolean_vector(2 to 3); ytdusi : linkage integer);
end f;

architecture wey of f is
  signal aagmhvg : time;
  signal ihsnajrtsp : severity_level;
  signal zdwlhpvjdi : time;
begin
  zmovija : entity work.e
    port map (yb => zdwlhpvjdi);
  nrrqc : entity work.kklig
    port map (tjo => ihsnajrtsp, yi => hlwu);
  rgyyfdl : entity work.e
    port map (yb => aagmhvg);
  
  -- Single-driven assignments
  cl <= cl;
end wey;

entity gelkghq is
  port (ifxcsvke : linkage integer);
end gelkghq;

library ieee;
use ieee.std_logic_1164.all;

architecture pwzbivptxs of gelkghq is
  signal dzx : integer;
  signal swdnuuzq : boolean_vector(2 to 3);
  signal qsddlws : real;
  signal wivvogny : std_logic;
  signal kjipc : severity_level;
  signal ofe : std_logic;
  signal ogjenqdw : severity_level;
begin
  vjsypvmvfy : entity work.kklig
    port map (tjo => ogjenqdw, yi => ofe);
  lt : entity work.kklig
    port map (tjo => kjipc, yi => wivvogny);
  opwqincy : entity work.f
    port map (hlwu => ofe, cl => qsddlws, llcw => swdnuuzq, ytdusi => dzx);
  
  -- Multi-driven assignments
  ofe <= ofe;
  ofe <= 'Z';
  ofe <= 'Z';
  ofe <= ofe;
end pwzbivptxs;



-- Seed after: 2368828930590761074,13857275728440271305
