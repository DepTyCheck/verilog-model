-- Seed: 1383862741706834955,12359743974512393525

library ieee;
use ieee.std_logic_1164.all;

entity utvmae is
  port (wtpolpary : inout std_logic; ctymny : linkage string(5 downto 3); cbsdkv : buffer severity_level; gftptak : buffer time);
end utvmae;

architecture gkydfpc of utvmae is
  
begin
  -- Single-driven assignments
  cbsdkv <= cbsdkv;
  gftptak <= 16#B_B_9.17609# us;
  
  -- Multi-driven assignments
  wtpolpary <= 'L';
  wtpolpary <= wtpolpary;
end gkydfpc;

entity ws is
  port (rbvhtpdivk : linkage time; cey : buffer bit_vector(3 to 2); h : inout real);
end ws;

library ieee;
use ieee.std_logic_1164.all;

architecture bnypqfzpch of ws is
  signal msw : time;
  signal otxrzj : severity_level;
  signal dmthz : string(5 downto 3);
  signal r : std_logic;
  signal bzpgxpxxl : time;
  signal yrsqbex : severity_level;
  signal llnpkycckd : string(5 downto 3);
  signal vpi : time;
  signal yflw : severity_level;
  signal qepag : string(5 downto 3);
  signal qzhm : std_logic;
  signal ipjr : time;
  signal eijni : severity_level;
  signal he : string(5 downto 3);
  signal nysp : std_logic;
begin
  witj : entity work.utvmae
    port map (wtpolpary => nysp, ctymny => he, cbsdkv => eijni, gftptak => ipjr);
  bzmbdsv : entity work.utvmae
    port map (wtpolpary => qzhm, ctymny => qepag, cbsdkv => yflw, gftptak => vpi);
  hwd : entity work.utvmae
    port map (wtpolpary => nysp, ctymny => llnpkycckd, cbsdkv => yrsqbex, gftptak => bzpgxpxxl);
  atlfu : entity work.utvmae
    port map (wtpolpary => r, ctymny => dmthz, cbsdkv => otxrzj, gftptak => msw);
  
  -- Single-driven assignments
  h <= h;
  cey <= (others => '0');
  
  -- Multi-driven assignments
  nysp <= '0';
  qzhm <= 'U';
  nysp <= nysp;
end bnypqfzpch;



-- Seed after: 4292455330586142890,12359743974512393525
