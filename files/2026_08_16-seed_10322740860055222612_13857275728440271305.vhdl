-- Seed: 10322740860055222612,13857275728440271305

library ieee;
use ieee.std_logic_1164.all;

entity mweue is
  port (htb : buffer boolean; yullceievl : buffer std_logic; cbv : out real; mfblforvqx : inout std_logic_vector(0 to 1));
end mweue;

architecture x of mweue is
  
begin
  -- Single-driven assignments
  cbv <= 2#0_0_0_0_1.1011#;
  htb <= htb;
  
  -- Multi-driven assignments
  yullceievl <= yullceievl;
end x;

entity zm is
  port (mujob : linkage boolean_vector(3 downto 3); e : inout boolean; xtpfqxqzr : buffer integer; glmuyzr : buffer time);
end zm;

library ieee;
use ieee.std_logic_1164.all;

architecture kmdbogk of zm is
  signal nltfs : std_logic_vector(0 to 1);
  signal cjzdaz : real;
  signal ndwfi : std_logic;
begin
  hkrzo : entity work.mweue
    port map (htb => e, yullceievl => ndwfi, cbv => cjzdaz, mfblforvqx => nltfs);
  
  -- Single-driven assignments
  glmuyzr <= glmuyzr;
  xtpfqxqzr <= 2_1;
  
  -- Multi-driven assignments
  nltfs <= ('L', '-');
  nltfs <= nltfs;
  ndwfi <= ndwfi;
end kmdbogk;

entity hmwo is
  port (ipcuyzc : inout time; z : linkage integer; sfeiqbhlv : out real; l : buffer boolean_vector(1 to 4));
end hmwo;

architecture j of hmwo is
  
begin
  -- Single-driven assignments
  ipcuyzc <= 1 hr;
end j;



-- Seed after: 12888794180912439150,13857275728440271305
