-- Seed: 17936451842979741454,16715549879197889543



entity qbcdbh is
  port (thvw : inout bit; fwzbgpu : out real);
end qbcdbh;



architecture ycodlmfbc of qbcdbh is
  
begin
  
end ycodlmfbc;

library ieee;
use ieee.std_logic_1164.all;

entity qrws is
  port (zedyetndz : out time; lunaufilrh : inout integer; dfwuxaan : in std_logic_vector(2 to 4); p : in std_logic_vector(0 to 2));
end qrws;



architecture h of qrws is
  signal fhu : real;
  signal sakfikw : bit;
begin
  hncttlnhnj : entity work.qbcdbh
    port map (thvw => sakfikw, fwzbgpu => fhu);
end h;

library ieee;
use ieee.std_logic_1164.all;

entity sfdom is
  port (zsingsczhx : inout boolean; gdhjwff : in character; idaeuwixn : buffer std_logic_vector(3 to 1); jkl : out severity_level);
end sfdom;

library ieee;
use ieee.std_logic_1164.all;

architecture wjaki of sfdom is
  signal ehqyaler : std_logic_vector(0 to 2);
  signal cbeobyyzn : std_logic_vector(2 to 4);
  signal ayuibhyhsk : integer;
  signal tkpncpwvg : time;
  signal nin : real;
  signal mqbos : bit;
  signal cjzhmwy : real;
  signal j : bit;
  signal l : real;
  signal acpod : bit;
begin
  c : entity work.qbcdbh
    port map (thvw => acpod, fwzbgpu => l);
  jnbeedli : entity work.qbcdbh
    port map (thvw => j, fwzbgpu => cjzhmwy);
  fofmbm : entity work.qbcdbh
    port map (thvw => mqbos, fwzbgpu => nin);
  hlh : entity work.qrws
    port map (zedyetndz => tkpncpwvg, lunaufilrh => ayuibhyhsk, dfwuxaan => cbeobyyzn, p => ehqyaler);
end wjaki;



-- Seed after: 6886807615221628466,16715549879197889543
