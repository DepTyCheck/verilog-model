-- Seed: 11057979410812556625,5983430343285687595

library ieee;
use ieee.std_logic_1164.all;

entity vmb is
  port (svpsh : out severity_level; vcus : buffer character; c : in severity_level; yvggmc : buffer std_logic_vector(0 downto 1));
end vmb;

architecture etumyc of vmb is
  
begin
  -- Multi-driven assignments
  yvggmc <= (others => '0');
  yvggmc <= yvggmc;
end etumyc;

library ieee;
use ieee.std_logic_1164.all;

entity cvlgnbtsia is
  port (plg : linkage std_logic_vector(0 to 4); w : inout std_logic_vector(4 to 0); jwssbivsrl : out bit_vector(0 to 0));
end cvlgnbtsia;

library ieee;
use ieee.std_logic_1164.all;

architecture zasaoa of cvlgnbtsia is
  signal owupk : std_logic_vector(0 downto 1);
  signal cepzhrbpxs : character;
  signal j : character;
  signal hm : severity_level;
  signal mghjlchvq : severity_level;
  signal zoj : character;
  signal zidqvt : severity_level;
begin
  rmh : entity work.vmb
    port map (svpsh => zidqvt, vcus => zoj, c => mghjlchvq, yvggmc => w);
  psbn : entity work.vmb
    port map (svpsh => hm, vcus => j, c => mghjlchvq, yvggmc => w);
  bcynkvwh : entity work.vmb
    port map (svpsh => mghjlchvq, vcus => cepzhrbpxs, c => mghjlchvq, yvggmc => owupk);
end zasaoa;

entity r is
  port (roi : inout time; eqebpxocb : inout severity_level);
end r;

library ieee;
use ieee.std_logic_1164.all;

architecture bbxujml of r is
  signal uynloi : std_logic_vector(0 downto 1);
  signal ppomxr : severity_level;
  signal nokdp : character;
  signal hxmecyyxaw : severity_level;
  signal ikkcyzli : std_logic_vector(0 downto 1);
  signal ausxxrdugy : severity_level;
  signal jgtckvu : character;
begin
  yzhtdiv : entity work.vmb
    port map (svpsh => eqebpxocb, vcus => jgtckvu, c => ausxxrdugy, yvggmc => ikkcyzli);
  cxmvnh : entity work.vmb
    port map (svpsh => hxmecyyxaw, vcus => nokdp, c => ppomxr, yvggmc => uynloi);
  
  -- Multi-driven assignments
  ikkcyzli <= "";
  ikkcyzli <= ikkcyzli;
end bbxujml;



-- Seed after: 2982479723238790903,5983430343285687595
