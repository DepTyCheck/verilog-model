-- Seed: 14951771135422829600,2511821214772927453

library ieee;
use ieee.std_logic_1164.all;

entity pji is
  port (qqls : inout std_logic_vector(1 to 4); uksscu : inout real);
end pji;

architecture lxypdh of pji is
  
begin
  -- Single-driven assignments
  uksscu <= 16#9126F.F_3#;
  
  -- Multi-driven assignments
  qqls <= qqls;
  qqls <= ('0', '-', 'Z', '0');
end lxypdh;

entity osgiuoz is
  port (ep : inout time);
end osgiuoz;

library ieee;
use ieee.std_logic_1164.all;

architecture pa of osgiuoz is
  signal payb : real;
  signal hnpejygpj : std_logic_vector(1 to 4);
  signal ujyylk : real;
  signal utp : std_logic_vector(1 to 4);
begin
  ewij : entity work.pji
    port map (qqls => utp, uksscu => ujyylk);
  xo : entity work.pji
    port map (qqls => hnpejygpj, uksscu => payb);
  
  -- Single-driven assignments
  ep <= 8#00167# fs;
  
  -- Multi-driven assignments
  utp <= ('L', 'H', 'H', '1');
end pa;

entity kliyzrm is
  port (cdjwhcoxf : in time; kxymjv : linkage severity_level; eosvnosnxi : in severity_level; u : in time);
end kliyzrm;

library ieee;
use ieee.std_logic_1164.all;

architecture rpala of kliyzrm is
  signal jyuddcfec : time;
  signal eftepregrr : real;
  signal euczxxnupv : std_logic_vector(1 to 4);
  signal am : time;
  signal csglpmzsi : real;
  signal t : std_logic_vector(1 to 4);
begin
  wfemkezf : entity work.pji
    port map (qqls => t, uksscu => csglpmzsi);
  wutr : entity work.osgiuoz
    port map (ep => am);
  rhlmxuh : entity work.pji
    port map (qqls => euczxxnupv, uksscu => eftepregrr);
  znandug : entity work.osgiuoz
    port map (ep => jyuddcfec);
  
  -- Multi-driven assignments
  t <= "LZXU";
  t <= "-X1W";
  t <= t;
end rpala;



-- Seed after: 11076857512455235506,2511821214772927453
