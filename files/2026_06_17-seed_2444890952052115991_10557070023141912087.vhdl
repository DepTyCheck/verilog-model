-- Seed: 2444890952052115991,10557070023141912087

library ieee;
use ieee.std_logic_1164.all;

entity nfbgsh is
  port (g : linkage real; wflychowdx : buffer real; rxstgvpgb : inout std_logic; dyzhbdczx : out boolean_vector(3 to 0));
end nfbgsh;

architecture zeh of nfbgsh is
  
begin
  
end zeh;

entity wlawwtbglj is
  port (dujhwjx : linkage character; zbr : in real; rp : linkage time);
end wlawwtbglj;

library ieee;
use ieee.std_logic_1164.all;

architecture untwufm of wlawwtbglj is
  signal slawrp : boolean_vector(3 to 0);
  signal dtdrztau : real;
  signal ou : real;
  signal ck : boolean_vector(3 to 0);
  signal zrmktetp : real;
  signal flouzu : real;
  signal upjcouen : boolean_vector(3 to 0);
  signal rwl : std_logic;
  signal mj : real;
  signal y : real;
  signal od : boolean_vector(3 to 0);
  signal bxlhcep : std_logic;
  signal carcsvbx : real;
  signal qckqhdwq : real;
begin
  otl : entity work.nfbgsh
    port map (g => qckqhdwq, wflychowdx => carcsvbx, rxstgvpgb => bxlhcep, dyzhbdczx => od);
  bbv : entity work.nfbgsh
    port map (g => y, wflychowdx => mj, rxstgvpgb => rwl, dyzhbdczx => upjcouen);
  mhqfpn : entity work.nfbgsh
    port map (g => flouzu, wflychowdx => zrmktetp, rxstgvpgb => bxlhcep, dyzhbdczx => ck);
  qcpdstjypb : entity work.nfbgsh
    port map (g => ou, wflychowdx => dtdrztau, rxstgvpgb => bxlhcep, dyzhbdczx => slawrp);
  
  -- Multi-driven assignments
  rwl <= 'W';
  rwl <= '0';
  rwl <= 'U';
end untwufm;



-- Seed after: 10606476925073266173,10557070023141912087
