-- Seed: 17139110135409520871,10557070023141912087

entity janouqxqt is
  port (mhzsjijmi : inout real_vector(2 to 1); dcftykcv : buffer time; qqp : linkage character; chqp : inout real);
end janouqxqt;

architecture hnwciv of janouqxqt is
  
begin
  -- Single-driven assignments
  dcftykcv <= 2#1# ms;
  chqp <= 40124.33;
end hnwciv;

library ieee;
use ieee.std_logic_1164.all;

entity w is
  port (uxidudl : linkage std_logic_vector(0 to 1));
end w;

architecture otnjw of w is
  signal ofafpv : real;
  signal blxv : character;
  signal tbytvq : time;
  signal kbclotk : real_vector(2 to 1);
  signal aewogvtdoi : real;
  signal wjecipmmg : character;
  signal iwrjrosf : time;
  signal papw : real_vector(2 to 1);
  signal tbwihop : real;
  signal pz : character;
  signal wdj : time;
  signal jbqvupa : real_vector(2 to 1);
  signal qbm : real;
  signal hgkznfo : character;
  signal ezc : time;
  signal cneelov : real_vector(2 to 1);
begin
  wtbb : entity work.janouqxqt
    port map (mhzsjijmi => cneelov, dcftykcv => ezc, qqp => hgkznfo, chqp => qbm);
  ovtddfxf : entity work.janouqxqt
    port map (mhzsjijmi => jbqvupa, dcftykcv => wdj, qqp => pz, chqp => tbwihop);
  sa : entity work.janouqxqt
    port map (mhzsjijmi => papw, dcftykcv => iwrjrosf, qqp => wjecipmmg, chqp => aewogvtdoi);
  sy : entity work.janouqxqt
    port map (mhzsjijmi => kbclotk, dcftykcv => tbytvq, qqp => blxv, chqp => ofafpv);
end otnjw;

library ieee;
use ieee.std_logic_1164.all;

entity ix is
  port (dld : inout std_logic);
end ix;

library ieee;
use ieee.std_logic_1164.all;

architecture ui of ix is
  signal kkpa : real;
  signal b : character;
  signal aalo : time;
  signal juevsajudu : real_vector(2 to 1);
  signal wcr : std_logic_vector(0 to 1);
  signal yd : std_logic_vector(0 to 1);
begin
  jtnswg : entity work.w
    port map (uxidudl => yd);
  vmqfxuhfh : entity work.w
    port map (uxidudl => wcr);
  l : entity work.janouqxqt
    port map (mhzsjijmi => juevsajudu, dcftykcv => aalo, qqp => b, chqp => kkpa);
  
  -- Multi-driven assignments
  dld <= 'X';
  dld <= 'L';
end ui;



-- Seed after: 12537345860814527316,10557070023141912087
