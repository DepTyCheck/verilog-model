-- Seed: 17090109638901492370,6290177331721581829

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity rvsxd is
  port (ekpna : out std_logic; zqc : inout physical_subtype_mirror; usauphynxw : in real; btifzp : buffer bit);
end rvsxd;

architecture t of rvsxd is
  
begin
  -- Multi-driven assignments
  ekpna <= 'H';
  ekpna <= ekpna;
  ekpna <= ekpna;
  ekpna <= ekpna;
end t;

use std.reflection.all;

entity bsig is
  port (hwbpvft : inout enumeration_subtype_mirror);
end bsig;

architecture me of bsig is
  
begin
  
end me;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity pmyjuhpaf is
  port (y : buffer std_logic_vector(0 downto 4); q : in integer; krgpqfo : inout value_mirror);
end pmyjuhpaf;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture coebtkzcwx of pmyjuhpaf is
  signal pyugmpzzif : bit;
  shared variable pqyzkojdag : physical_subtype_mirror;
  signal w : std_logic;
  signal exx : bit;
  signal dgsvqnywz : real;
  shared variable ai : physical_subtype_mirror;
  shared variable cigtbds : enumeration_subtype_mirror;
  signal kb : bit;
  signal goammc : real;
  shared variable ruvgs : physical_subtype_mirror;
  signal yu : std_logic;
begin
  hrxqlsscb : entity work.rvsxd
    port map (ekpna => yu, zqc => ruvgs, usauphynxw => goammc, btifzp => kb);
  jwmhs : entity work.bsig
    port map (hwbpvft => cigtbds);
  hp : entity work.rvsxd
    port map (ekpna => yu, zqc => ai, usauphynxw => dgsvqnywz, btifzp => exx);
  abn : entity work.rvsxd
    port map (ekpna => w, zqc => pqyzkojdag, usauphynxw => dgsvqnywz, btifzp => pyugmpzzif);
  
  -- Single-driven assignments
  goammc <= 2.1_2_3_0_0;
  dgsvqnywz <= 8#60070.66#;
  
  -- Multi-driven assignments
  y <= "";
  w <= yu;
  y <= "";
end coebtkzcwx;



-- Seed after: 17587013393334420355,6290177331721581829
