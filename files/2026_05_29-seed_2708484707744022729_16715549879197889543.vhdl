-- Seed: 2708484707744022729,16715549879197889543



entity jabgbng is
  port (tifhrcxu : in time; hrfikjal : linkage real; yzkozc : buffer boolean_vector(1 to 0); mprmieicc : buffer boolean);
end jabgbng;



architecture gm of jabgbng is
  
begin
  
end gm;

library ieee;
use ieee.std_logic_1164.all;

entity lswlcsq is
  port (mromphbby : buffer severity_level; bnrqkewdn : inout integer_vector(0 downto 3); w : buffer std_logic_vector(2 downto 1));
end lswlcsq;



architecture oidnin of lswlcsq is
  signal t : boolean;
  signal gtob : boolean_vector(1 to 0);
  signal vyx : real;
  signal lec : time;
  signal ogs : boolean;
  signal kuc : boolean_vector(1 to 0);
  signal x : boolean;
  signal q : boolean_vector(1 to 0);
  signal skcjrnbqjt : time;
  signal phiiwj : boolean;
  signal ciflsituv : boolean_vector(1 to 0);
  signal ksrqkvurqq : real;
  signal gho : time;
begin
  dp : entity work.jabgbng
    port map (tifhrcxu => gho, hrfikjal => ksrqkvurqq, yzkozc => ciflsituv, mprmieicc => phiiwj);
  gpw : entity work.jabgbng
    port map (tifhrcxu => skcjrnbqjt, hrfikjal => ksrqkvurqq, yzkozc => q, mprmieicc => x);
  guku : entity work.jabgbng
    port map (tifhrcxu => skcjrnbqjt, hrfikjal => ksrqkvurqq, yzkozc => kuc, mprmieicc => ogs);
  chpxnms : entity work.jabgbng
    port map (tifhrcxu => lec, hrfikjal => vyx, yzkozc => gtob, mprmieicc => t);
end oidnin;

library ieee;
use ieee.std_logic_1164.all;

entity owosseeyio is
  port (ttetl : inout integer; pfdgcgrw : buffer time; gkddya : out std_logic);
end owosseeyio;

library ieee;
use ieee.std_logic_1164.all;

architecture hdzqqpv of owosseeyio is
  signal xw : integer_vector(0 downto 3);
  signal qjiskdtto : severity_level;
  signal r : boolean;
  signal cving : boolean_vector(1 to 0);
  signal cphvhcovwd : time;
  signal ltjqpmecnx : std_logic_vector(2 downto 1);
  signal zsgtkljs : integer_vector(0 downto 3);
  signal fujqlt : severity_level;
  signal qckctiq : boolean;
  signal cgwtlfpjv : boolean_vector(1 to 0);
  signal kcdk : real;
begin
  aedw : entity work.jabgbng
    port map (tifhrcxu => pfdgcgrw, hrfikjal => kcdk, yzkozc => cgwtlfpjv, mprmieicc => qckctiq);
  rtw : entity work.lswlcsq
    port map (mromphbby => fujqlt, bnrqkewdn => zsgtkljs, w => ltjqpmecnx);
  pgx : entity work.jabgbng
    port map (tifhrcxu => cphvhcovwd, hrfikjal => kcdk, yzkozc => cving, mprmieicc => r);
  ez : entity work.lswlcsq
    port map (mromphbby => qjiskdtto, bnrqkewdn => xw, w => ltjqpmecnx);
end hdzqqpv;



-- Seed after: 8266692255519018875,16715549879197889543
