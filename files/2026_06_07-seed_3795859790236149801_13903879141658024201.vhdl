-- Seed: 3795859790236149801,13903879141658024201



entity ganw is
  port (q : in severity_level; olfvl : inout time; drcqpyethh : out time);
end ganw;



architecture lnxe of ganw is
  
begin
  
end lnxe;



entity jazvgu is
  port (gsxqqqu : out time; t : linkage boolean_vector(2 to 4); cwq : buffer integer; rzhop : buffer real);
end jazvgu;



architecture yl of jazvgu is
  signal ccftn : time;
  signal ad : severity_level;
  signal tpobujs : time;
  signal wjeobkk : time;
  signal ugysfse : severity_level;
  signal jomozqfhb : time;
  signal gewnwsh : time;
  signal umbp : time;
  signal qrofgznahf : time;
  signal x : severity_level;
begin
  ghtfcz : entity work.ganw
    port map (q => x, olfvl => qrofgznahf, drcqpyethh => umbp);
  piqq : entity work.ganw
    port map (q => x, olfvl => gewnwsh, drcqpyethh => jomozqfhb);
  nhmxli : entity work.ganw
    port map (q => ugysfse, olfvl => wjeobkk, drcqpyethh => tpobujs);
  wuh : entity work.ganw
    port map (q => ad, olfvl => gsxqqqu, drcqpyethh => ccftn);
end yl;



-- Seed after: 16965075112248844383,13903879141658024201
