-- Seed: 7427724327049788971,662889661651915549

entity tzydmxrot is
  port (gwystwq : in real);
end tzydmxrot;

architecture c of tzydmxrot is
  
begin
  
end c;

library ieee;
use ieee.std_logic_1164.all;

entity z is
  port (lpldka : inout std_logic; qu : inout integer; pbwjzeuge : in std_logic_vector(2 to 4); noywbfnj : linkage std_logic);
end z;

architecture pess of z is
  signal hnf : real;
begin
  ddano : entity work.tzydmxrot
    port map (gwystwq => hnf);
  
  -- Single-driven assignments
  qu <= qu;
  
  -- Multi-driven assignments
  lpldka <= lpldka;
  lpldka <= 'U';
  lpldka <= lpldka;
end pess;

library ieee;
use ieee.std_logic_1164.all;

entity dq is
  port (fxp : in std_logic_vector(0 downto 1); bw : buffer real);
end dq;

architecture oah of dq is
  signal km : real;
  signal ukyftvr : real;
  signal iwp : real;
begin
  leoojzmzte : entity work.tzydmxrot
    port map (gwystwq => bw);
  ci : entity work.tzydmxrot
    port map (gwystwq => iwp);
  ctonolb : entity work.tzydmxrot
    port map (gwystwq => ukyftvr);
  hfpqnjw : entity work.tzydmxrot
    port map (gwystwq => km);
  
  -- Single-driven assignments
  ukyftvr <= 8#34311.6_3#;
  km <= km;
  iwp <= bw;
  bw <= 2#10101.0_0_1#;
end oah;

library ieee;
use ieee.std_logic_1164.all;

entity md is
  port (cdhwv : out boolean; ypqwdqcwzp : out std_logic_vector(1 downto 2));
end md;

library ieee;
use ieee.std_logic_1164.all;

architecture fstpakdl of md is
  signal gelms : real;
  signal bewaivdzv : real;
  signal irenucur : std_logic;
  signal g : std_logic_vector(2 to 4);
  signal iw : integer;
  signal bqie : std_logic;
  signal qi : std_logic_vector(2 to 4);
  signal njwvkrq : integer;
  signal avjhujggb : std_logic;
begin
  rwyib : entity work.z
    port map (lpldka => avjhujggb, qu => njwvkrq, pbwjzeuge => qi, noywbfnj => avjhujggb);
  pzjvpahip : entity work.z
    port map (lpldka => bqie, qu => iw, pbwjzeuge => g, noywbfnj => irenucur);
  vnpupbqtay : entity work.tzydmxrot
    port map (gwystwq => bewaivdzv);
  urmvgznwtf : entity work.tzydmxrot
    port map (gwystwq => gelms);
  
  -- Single-driven assignments
  cdhwv <= FALSE;
end fstpakdl;



-- Seed after: 5631176402370721626,662889661651915549
