-- Seed: 11890877851170883239,16715549879197889543

library ieee;
use ieee.std_logic_1164.all;

entity h is
  port (ol : buffer std_logic_vector(1 to 0); hogog : linkage std_logic_vector(3 downto 3));
end h;



architecture extkl of h is
  
begin
  
end extkl;



entity wnug is
  port (nuab : linkage real);
end wnug;

library ieee;
use ieee.std_logic_1164.all;

architecture vpc of wnug is
  signal ulmayyq : std_logic_vector(1 to 0);
  signal ym : std_logic_vector(3 downto 3);
  signal rjsfk : std_logic_vector(1 to 0);
  signal ziavs : std_logic_vector(1 to 0);
  signal frfemfopc : std_logic_vector(3 downto 3);
  signal lxwy : std_logic_vector(1 to 0);
begin
  lroa : entity work.h
    port map (ol => lxwy, hogog => frfemfopc);
  g : entity work.h
    port map (ol => ziavs, hogog => frfemfopc);
  nntg : entity work.h
    port map (ol => rjsfk, hogog => ym);
  puqjsw : entity work.h
    port map (ol => ulmayyq, hogog => frfemfopc);
end vpc;



-- Seed after: 6920997605614591578,16715549879197889543
