-- Seed: 240443138736641840,10754487200446211253

library ieee;
use ieee.std_logic_1164.all;

entity t is
  port (wlqjeyhz : out std_logic_vector(1 to 3); pyzz : buffer real; upi : in integer; khhytnmwf : linkage character);
end t;

architecture qygaaqa of t is
  
begin
  -- Multi-driven assignments
  wlqjeyhz <= "ZXZ";
  wlqjeyhz <= wlqjeyhz;
  wlqjeyhz <= "UW0";
end qygaaqa;

entity gmc is
  port (bqonq : in boolean_vector(1 to 1); wlaj : out integer; tlijtz : in real; pb : inout severity_level);
end gmc;

library ieee;
use ieee.std_logic_1164.all;

architecture a of gmc is
  signal dzkxjadq : character;
  signal vg : real;
  signal xxpimmuzso : std_logic_vector(1 to 3);
begin
  xpqqj : entity work.t
    port map (wlqjeyhz => xxpimmuzso, pyzz => vg, upi => wlaj, khhytnmwf => dzkxjadq);
  
  -- Single-driven assignments
  wlaj <= 2004;
  pb <= FAILURE;
  
  -- Multi-driven assignments
  xxpimmuzso <= xxpimmuzso;
  xxpimmuzso <= xxpimmuzso;
end a;

entity isrweoqn is
  port (jayivju : linkage integer; puwxbsyfzu : out real);
end isrweoqn;

library ieee;
use ieee.std_logic_1164.all;

architecture z of isrweoqn is
  signal cyerwmrq : severity_level;
  signal eodpit : real;
  signal sbfslga : boolean_vector(1 to 1);
  signal syabe : character;
  signal uiixk : integer;
  signal bpjrepdt : real;
  signal ywox : character;
  signal div : integer;
  signal pd : real;
  signal rjjsstr : std_logic_vector(1 to 3);
begin
  xnwxgpf : entity work.t
    port map (wlqjeyhz => rjjsstr, pyzz => pd, upi => div, khhytnmwf => ywox);
  gqirbnhl : entity work.t
    port map (wlqjeyhz => rjjsstr, pyzz => bpjrepdt, upi => uiixk, khhytnmwf => syabe);
  uvqwdibhhc : entity work.gmc
    port map (bqonq => sbfslga, wlaj => div, tlijtz => eodpit, pb => cyerwmrq);
  
  -- Single-driven assignments
  uiixk <= 0;
  puwxbsyfzu <= 0.30;
  eodpit <= puwxbsyfzu;
  
  -- Multi-driven assignments
  rjjsstr <= rjjsstr;
  rjjsstr <= rjjsstr;
end z;

entity kwf is
  port (rdwdl : out real; iwqq : in time; rz : in character);
end kwf;

architecture qt of kwf is
  
begin
  -- Single-driven assignments
  rdwdl <= rdwdl;
end qt;



-- Seed after: 7645474070450487141,10754487200446211253
