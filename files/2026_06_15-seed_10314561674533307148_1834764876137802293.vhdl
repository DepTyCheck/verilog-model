-- Seed: 10314561674533307148,1834764876137802293

library ieee;
use ieee.std_logic_1164.all;

entity c is
  port (krvyldnjed : out time; l : buffer severity_level; sycmcview : out std_logic_vector(1 downto 1));
end c;

architecture vd of c is
  
begin
  -- Single-driven assignments
  l <= FAILURE;
  krvyldnjed <= 8#01# ns;
  
  -- Multi-driven assignments
  sycmcview <= (others => 'H');
end vd;

entity jgrwz is
  port (itzqc : linkage integer_vector(4 downto 4); kyxdhfl : out character; rgvc : linkage string(1 to 3));
end jgrwz;

architecture gsbtfz of jgrwz is
  
begin
  -- Single-driven assignments
  kyxdhfl <= 'f';
end gsbtfz;

library ieee;
use ieee.std_logic_1164.all;

entity bkfwl is
  port (zbsdgmcgvc : inout std_logic_vector(3 to 1));
end bkfwl;

library ieee;
use ieee.std_logic_1164.all;

architecture nfhphfvmvn of bkfwl is
  signal qerrzryplm : std_logic_vector(1 downto 1);
  signal aejfhksk : severity_level;
  signal fnbj : time;
  signal hnmvnnhni : std_logic_vector(1 downto 1);
  signal ae : severity_level;
  signal xfnlslvdzm : time;
  signal gctpn : std_logic_vector(1 downto 1);
  signal msrkjizy : severity_level;
  signal vomirr : time;
begin
  hypylenbl : entity work.c
    port map (krvyldnjed => vomirr, l => msrkjizy, sycmcview => gctpn);
  cnjqyzjm : entity work.c
    port map (krvyldnjed => xfnlslvdzm, l => ae, sycmcview => hnmvnnhni);
  vrpimyyum : entity work.c
    port map (krvyldnjed => fnbj, l => aejfhksk, sycmcview => qerrzryplm);
  
  -- Multi-driven assignments
  zbsdgmcgvc <= (others => '0');
end nfhphfvmvn;

entity cqqbzrkq is
  port (xbln : inout real);
end cqqbzrkq;

library ieee;
use ieee.std_logic_1164.all;

architecture t of cqqbzrkq is
  signal lthtebrono : std_logic_vector(1 downto 1);
  signal cyrq : severity_level;
  signal u : time;
  signal jpypizmix : std_logic_vector(3 to 1);
  signal djxgps : std_logic_vector(1 downto 1);
  signal px : severity_level;
  signal e : time;
begin
  ql : entity work.c
    port map (krvyldnjed => e, l => px, sycmcview => djxgps);
  evwqqgs : entity work.bkfwl
    port map (zbsdgmcgvc => jpypizmix);
  ooeatw : entity work.c
    port map (krvyldnjed => u, l => cyrq, sycmcview => lthtebrono);
  
  -- Single-driven assignments
  xbln <= 16#1_E.0_A_F#;
  
  -- Multi-driven assignments
  lthtebrono <= "L";
  lthtebrono <= (others => '0');
end t;



-- Seed after: 11419631168208787237,1834764876137802293
