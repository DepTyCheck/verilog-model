-- Seed: 15796341436914987076,10871023049702252113

library ieee;
use ieee.std_logic_1164.all;

entity royfubqxo is
  port (ydosx : inout std_logic_vector(1 downto 3); i : linkage real; vghdwqlods : inout severity_level; esacab : buffer std_logic_vector(3 to 3));
end royfubqxo;

architecture mo of royfubqxo is
  
begin
  -- Single-driven assignments
  vghdwqlods <= FAILURE;
  
  -- Multi-driven assignments
  esacab <= esacab;
end mo;

library ieee;
use ieee.std_logic_1164.all;

entity u is
  port (tps : in time; pwissybme : buffer real_vector(3 to 3); amrq : inout std_logic_vector(0 downto 0));
end u;

library ieee;
use ieee.std_logic_1164.all;

architecture gczkkbwhyd of u is
  signal wussbi : std_logic_vector(3 to 3);
  signal kzvcabslp : severity_level;
  signal yahdy : real;
  signal pvsufbn : severity_level;
  signal vovjlmz : real;
  signal rb : std_logic_vector(1 downto 3);
  signal tksomcno : std_logic_vector(3 to 3);
  signal b : severity_level;
  signal vvuqvggnjp : real;
  signal k : std_logic_vector(1 downto 3);
begin
  bjhvrnam : entity work.royfubqxo
    port map (ydosx => k, i => vvuqvggnjp, vghdwqlods => b, esacab => tksomcno);
  cuszrm : entity work.royfubqxo
    port map (ydosx => rb, i => vovjlmz, vghdwqlods => pvsufbn, esacab => amrq);
  rlyxxbl : entity work.royfubqxo
    port map (ydosx => rb, i => yahdy, vghdwqlods => kzvcabslp, esacab => wussbi);
  
  -- Single-driven assignments
  pwissybme <= (others => 0.1);
  
  -- Multi-driven assignments
  amrq <= (others => '0');
  tksomcno <= "H";
end gczkkbwhyd;

entity poowi is
  port (rksfgr : inout time; bhkck : buffer real);
end poowi;

library ieee;
use ieee.std_logic_1164.all;

architecture n of poowi is
  signal yzrjxkx : std_logic_vector(3 to 3);
  signal bkxqgnpp : severity_level;
  signal rjtqmnxc : std_logic_vector(1 downto 3);
  signal hcg : std_logic_vector(3 to 3);
  signal vkvimfbajo : severity_level;
  signal lzwkznks : real;
  signal dtgfzk : std_logic_vector(1 downto 3);
  signal qyyccq : severity_level;
  signal mqyv : real;
  signal kkwk : std_logic_vector(1 downto 3);
  signal gzqesvv : std_logic_vector(3 to 3);
  signal ltzqgfbbr : severity_level;
  signal mscwmhf : real;
  signal ns : std_logic_vector(1 downto 3);
begin
  yfhanscm : entity work.royfubqxo
    port map (ydosx => ns, i => mscwmhf, vghdwqlods => ltzqgfbbr, esacab => gzqesvv);
  qi : entity work.royfubqxo
    port map (ydosx => kkwk, i => mqyv, vghdwqlods => qyyccq, esacab => gzqesvv);
  ydsq : entity work.royfubqxo
    port map (ydosx => dtgfzk, i => lzwkznks, vghdwqlods => vkvimfbajo, esacab => hcg);
  vbhtogo : entity work.royfubqxo
    port map (ydosx => rjtqmnxc, i => bhkck, vghdwqlods => bkxqgnpp, esacab => yzrjxkx);
  
  -- Multi-driven assignments
  dtgfzk <= "";
  ns <= kkwk;
  rjtqmnxc <= ns;
end n;

library ieee;
use ieee.std_logic_1164.all;

entity bcayqn is
  port (erbozvyka : inout integer; op : in std_logic_vector(1 downto 1); yjruudbxah : linkage std_logic; pu : inout std_logic);
end bcayqn;

library ieee;
use ieee.std_logic_1164.all;

architecture odw of bcayqn is
  signal w : std_logic_vector(0 downto 0);
  signal pieeyuc : real_vector(3 to 3);
  signal l : time;
  signal oqyhexpsm : std_logic_vector(3 to 3);
  signal hwzfux : severity_level;
  signal kgxzc : real;
  signal q : std_logic_vector(1 downto 3);
begin
  cpcwtvbvxj : entity work.royfubqxo
    port map (ydosx => q, i => kgxzc, vghdwqlods => hwzfux, esacab => oqyhexpsm);
  vykmcfaywv : entity work.u
    port map (tps => l, pwissybme => pieeyuc, amrq => w);
end odw;



-- Seed after: 8137777693967239982,10871023049702252113
