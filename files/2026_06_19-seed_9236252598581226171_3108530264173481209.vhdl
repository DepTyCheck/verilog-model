-- Seed: 9236252598581226171,3108530264173481209

entity ty is
  port (rcuhrcq : inout severity_level; dfj : in integer; lrzgtmdweh : buffer real; cifrxnes : linkage real_vector(4 to 4));
end ty;

architecture harzq of ty is
  
begin
  -- Single-driven assignments
  lrzgtmdweh <= 3_1.1_4_0_1;
  rcuhrcq <= FAILURE;
end harzq;

library ieee;
use ieee.std_logic_1164.all;

entity lbvomyzvi is
  port (nb : inout real_vector(2 downto 0); yb : inout integer; hjouywizvz : out std_logic; ynovnmgwp : inout string(1 downto 1));
end lbvomyzvi;

architecture ilp of lbvomyzvi is
  signal lajekmpf : real_vector(4 to 4);
  signal gvwvzvzeei : real;
  signal mekffa : integer;
  signal tvhszkv : severity_level;
  signal v : real_vector(4 to 4);
  signal xlbjqzatu : real;
  signal sokxlbekq : severity_level;
  signal zsi : real_vector(4 to 4);
  signal lfxwbtnvrb : real;
  signal f : severity_level;
  signal jc : real_vector(4 to 4);
  signal mzyiher : real;
  signal qhq : integer;
  signal ktctygopm : severity_level;
begin
  pnerakeisg : entity work.ty
    port map (rcuhrcq => ktctygopm, dfj => qhq, lrzgtmdweh => mzyiher, cifrxnes => jc);
  yjwplmmog : entity work.ty
    port map (rcuhrcq => f, dfj => yb, lrzgtmdweh => lfxwbtnvrb, cifrxnes => zsi);
  hpa : entity work.ty
    port map (rcuhrcq => sokxlbekq, dfj => yb, lrzgtmdweh => xlbjqzatu, cifrxnes => v);
  qpzj : entity work.ty
    port map (rcuhrcq => tvhszkv, dfj => mekffa, lrzgtmdweh => gvwvzvzeei, cifrxnes => lajekmpf);
  
  -- Single-driven assignments
  ynovnmgwp <= (others => 'o');
  mekffa <= 02433;
  qhq <= 32;
  nb <= (1_2.3, 43220.0, 2_4_2.3_3);
  yb <= 2#10000#;
  
  -- Multi-driven assignments
  hjouywizvz <= '0';
end ilp;

library ieee;
use ieee.std_logic_1164.all;

entity ofhkksms is
  port (tn : inout std_logic; jtpiwlir : linkage real; sqx : in std_logic; uikiaditb : out std_logic_vector(4 to 2));
end ofhkksms;

architecture lignsrual of ofhkksms is
  signal gdpaz : real_vector(4 to 4);
  signal cribt : real;
  signal rhmrktb : severity_level;
  signal d : real_vector(4 to 4);
  signal igicvqvqg : real;
  signal dom : severity_level;
  signal zeuwonlth : real_vector(4 to 4);
  signal ukuikdt : real;
  signal tdrjh : integer;
  signal fspn : severity_level;
  signal nya : real_vector(4 to 4);
  signal xdhys : real;
  signal ayemxudccs : integer;
  signal w : severity_level;
begin
  a : entity work.ty
    port map (rcuhrcq => w, dfj => ayemxudccs, lrzgtmdweh => xdhys, cifrxnes => nya);
  su : entity work.ty
    port map (rcuhrcq => fspn, dfj => tdrjh, lrzgtmdweh => ukuikdt, cifrxnes => zeuwonlth);
  mqfsdvwq : entity work.ty
    port map (rcuhrcq => dom, dfj => ayemxudccs, lrzgtmdweh => igicvqvqg, cifrxnes => d);
  i : entity work.ty
    port map (rcuhrcq => rhmrktb, dfj => ayemxudccs, lrzgtmdweh => cribt, cifrxnes => gdpaz);
end lignsrual;

library ieee;
use ieee.std_logic_1164.all;

entity ritfeogdjg is
  port (r : out integer; n : buffer std_logic_vector(0 downto 4); litfgkl : linkage integer_vector(4 to 0); fy : in time);
end ritfeogdjg;

library ieee;
use ieee.std_logic_1164.all;

architecture lojlf of ritfeogdjg is
  signal jevvfmga : std_logic_vector(4 to 2);
  signal nn : std_logic;
  signal ywnrpvrt : real;
  signal yohy : std_logic;
  signal vqnaays : real_vector(4 to 4);
  signal fvl : real;
  signal jgjy : integer;
  signal y : severity_level;
begin
  idibhyaa : entity work.ty
    port map (rcuhrcq => y, dfj => jgjy, lrzgtmdweh => fvl, cifrxnes => vqnaays);
  vyxbof : entity work.ofhkksms
    port map (tn => yohy, jtpiwlir => ywnrpvrt, sqx => nn, uikiaditb => jevvfmga);
  
  -- Single-driven assignments
  jgjy <= 16#B_0_6_5#;
  r <= 2#01000#;
  
  -- Multi-driven assignments
  n <= (others => '0');
  n <= "";
  yohy <= 'U';
end lojlf;



-- Seed after: 3286419057779864730,3108530264173481209
