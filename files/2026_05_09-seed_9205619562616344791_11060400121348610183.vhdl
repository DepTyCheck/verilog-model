-- Seed: 9205619562616344791,11060400121348610183

library ieee;
use ieee.std_logic_1164.all;

entity e is
  port (vhzqye : buffer time; hrcevjs : buffer character; zgr : in std_logic; kn : linkage integer);
end e;



architecture ewzeg of e is
  
begin
  
end ewzeg;



entity gupbeqgw is
  port (olpenydipp : buffer boolean; ogtzugz : out real; jcygddetwa : out integer; vevk : buffer time);
end gupbeqgw;

library ieee;
use ieee.std_logic_1164.all;

architecture zorbh of gupbeqgw is
  signal omnfnvpp : integer;
  signal eodntw : std_logic;
  signal vzjpfto : character;
  signal ejeg : integer;
  signal klaf : std_logic;
  signal cqkxx : character;
  signal infm : time;
begin
  wvxiewhl : entity work.e
    port map (vhzqye => infm, hrcevjs => cqkxx, zgr => klaf, kn => ejeg);
  qda : entity work.e
    port map (vhzqye => vevk, hrcevjs => vzjpfto, zgr => eodntw, kn => omnfnvpp);
end zorbh;

library ieee;
use ieee.std_logic_1164.all;

entity wtedkrunfo is
  port (qddbqju : in std_logic; eutmybzymr : inout std_logic; gy : linkage time; vdahnwip : buffer boolean);
end wtedkrunfo;

library ieee;
use ieee.std_logic_1164.all;

architecture nbe of wtedkrunfo is
  signal jfertzpln : character;
  signal hklejav : time;
  signal sst : time;
  signal aby : integer;
  signal tire : real;
  signal ne : boolean;
  signal kxzkcp : integer;
  signal d : std_logic;
  signal cccdn : character;
  signal ix : time;
begin
  obzrsw : entity work.e
    port map (vhzqye => ix, hrcevjs => cccdn, zgr => d, kn => kxzkcp);
  lgku : entity work.gupbeqgw
    port map (olpenydipp => ne, ogtzugz => tire, jcygddetwa => aby, vevk => sst);
  xnwaau : entity work.e
    port map (vhzqye => hklejav, hrcevjs => jfertzpln, zgr => d, kn => kxzkcp);
end nbe;



entity wlhorjsidi is
  port (uwj : buffer severity_level; sriunypa : inout integer);
end wlhorjsidi;

library ieee;
use ieee.std_logic_1164.all;

architecture cnocvhu of wlhorjsidi is
  signal y : character;
  signal imzrs : time;
  signal ddpuagg : std_logic;
  signal o : character;
  signal ett : time;
  signal ki : integer;
  signal eavewgf : std_logic;
  signal iappgipqr : character;
  signal iokkiwbv : time;
begin
  rstxee : entity work.e
    port map (vhzqye => iokkiwbv, hrcevjs => iappgipqr, zgr => eavewgf, kn => ki);
  rcxovur : entity work.e
    port map (vhzqye => ett, hrcevjs => o, zgr => ddpuagg, kn => sriunypa);
  fabi : entity work.e
    port map (vhzqye => imzrs, hrcevjs => y, zgr => eavewgf, kn => sriunypa);
end cnocvhu;



-- Seed after: 16599566421539284362,11060400121348610183
