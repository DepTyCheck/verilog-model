-- Seed: 11667705117416210825,4292249356257567981

entity tklc is
  port (yvy : linkage real_vector(1 downto 0); ntcdax : linkage bit; h : linkage real);
end tklc;

architecture ctstnrelam of tklc is
  
begin
  
end ctstnrelam;

library ieee;
use ieee.std_logic_1164.all;

entity fbaduvxf is
  port (g : out std_logic; qqshla : out time; zwokef : buffer std_logic);
end fbaduvxf;

architecture kajlmql of fbaduvxf is
  signal ntqslttp : real;
  signal jk : bit;
  signal encafnss : real_vector(1 downto 0);
  signal gudn : real;
  signal gkra : bit;
  signal it : real_vector(1 downto 0);
  signal iqdjamsw : real;
  signal lpeevp : bit;
  signal fwwztdwlmn : real_vector(1 downto 0);
begin
  mffjxeuz : entity work.tklc
    port map (yvy => fwwztdwlmn, ntcdax => lpeevp, h => iqdjamsw);
  ibwexd : entity work.tklc
    port map (yvy => it, ntcdax => gkra, h => gudn);
  p : entity work.tklc
    port map (yvy => encafnss, ntcdax => jk, h => ntqslttp);
  
  -- Single-driven assignments
  qqshla <= 1 sec;
  
  -- Multi-driven assignments
  zwokef <= '0';
  zwokef <= zwokef;
  zwokef <= zwokef;
end kajlmql;

entity rmbtak is
  port (kqggr : out real; v : inout time; yam : in integer);
end rmbtak;

architecture sepwvd of rmbtak is
  
begin
  -- Single-driven assignments
  v <= 0 sec;
  kqggr <= kqggr;
end sepwvd;

library ieee;
use ieee.std_logic_1164.all;

entity mrcqyp is
  port (aldcz : out boolean_vector(0 downto 0); v : buffer std_logic_vector(4 to 2); aql : in severity_level);
end mrcqyp;

library ieee;
use ieee.std_logic_1164.all;

architecture mbowf of mrcqyp is
  signal rrgxvdin : time;
  signal lazfluqnjn : std_logic;
begin
  hobxkldmzw : entity work.fbaduvxf
    port map (g => lazfluqnjn, qqshla => rrgxvdin, zwokef => lazfluqnjn);
  
  -- Single-driven assignments
  aldcz <= (others => TRUE);
  
  -- Multi-driven assignments
  lazfluqnjn <= lazfluqnjn;
  v <= v;
  v <= "";
  lazfluqnjn <= lazfluqnjn;
end mbowf;



-- Seed after: 11054247300410117139,4292249356257567981
