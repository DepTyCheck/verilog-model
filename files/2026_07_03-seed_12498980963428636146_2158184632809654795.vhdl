-- Seed: 12498980963428636146,2158184632809654795

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity nuxpzfpg is
  port (a : buffer time; pk : out std_logic_vector(3 to 0); bkhc : in real; hp : inout record_value_mirror);
end nuxpzfpg;

architecture czwwy of nuxpzfpg is
  
begin
  -- Multi-driven assignments
  pk <= (others => '0');
  pk <= "";
  pk <= (others => '0');
  pk <= (others => '0');
end czwwy;

use std.reflection.all;

entity xpgyj is
  port (bf : inout enumeration_subtype_mirror);
end xpgyj;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture cmkvvhsjnr of xpgyj is
  shared variable k : record_value_mirror;
  signal kzznj : real;
  signal b : time;
  shared variable emg : record_value_mirror;
  signal nboenuf : real;
  signal iryueu : std_logic_vector(3 to 0);
  signal cyozybyv : time;
  shared variable mdtv : record_value_mirror;
  signal mjvacuutr : real;
  signal m : std_logic_vector(3 to 0);
  signal px : time;
begin
  gnsvlyqze : entity work.nuxpzfpg
    port map (a => px, pk => m, bkhc => mjvacuutr, hp => mdtv);
  hzxijr : entity work.nuxpzfpg
    port map (a => cyozybyv, pk => iryueu, bkhc => nboenuf, hp => emg);
  owxqdyt : entity work.nuxpzfpg
    port map (a => b, pk => m, bkhc => kzznj, hp => k);
  
  -- Single-driven assignments
  mjvacuutr <= 3.4_0_0_4;
  kzznj <= nboenuf;
  nboenuf <= 16#364F.1FD1D#;
  
  -- Multi-driven assignments
  iryueu <= "";
  m <= (others => '0');
end cmkvvhsjnr;

use std.reflection.all;

entity vdigf is
  port (osrpa : inout floating_subtype_mirror);
end vdigf;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture imfazqqz of vdigf is
  shared variable iw : enumeration_subtype_mirror;
  shared variable pekmk : record_value_mirror;
  signal frtexs : real;
  signal bn : std_logic_vector(3 to 0);
  signal eejked : time;
  shared variable jmnmb : enumeration_subtype_mirror;
begin
  wdhqedh : entity work.xpgyj
    port map (bf => jmnmb);
  uscqpkq : entity work.nuxpzfpg
    port map (a => eejked, pk => bn, bkhc => frtexs, hp => pekmk);
  yd : entity work.xpgyj
    port map (bf => iw);
  
  -- Single-driven assignments
  frtexs <= frtexs;
  
  -- Multi-driven assignments
  bn <= bn;
  bn <= "";
  bn <= bn;
  bn <= bn;
end imfazqqz;

use std.reflection.all;

entity uv is
  port (pmvg : linkage integer; oc : inout integer_value_mirror);
end uv;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture dv of uv is
  shared variable bptx : record_value_mirror;
  signal sfwqfncxi : real;
  signal obzia : std_logic_vector(3 to 0);
  signal kjhetclky : time;
begin
  fkgolp : entity work.nuxpzfpg
    port map (a => kjhetclky, pk => obzia, bkhc => sfwqfncxi, hp => bptx);
  
  -- Multi-driven assignments
  obzia <= "";
  obzia <= (others => '0');
end dv;



-- Seed after: 67361453262194663,2158184632809654795
