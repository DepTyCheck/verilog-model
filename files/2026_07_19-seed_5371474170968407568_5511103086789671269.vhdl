-- Seed: 5371474170968407568,5511103086789671269

library ieee;
use ieee.std_logic_1164.all;

entity r is
  port (jpxi : buffer std_logic; tybddwp : buffer time; pde : inout string(3 to 1));
end r;

architecture dyucnur of r is
  
begin
  -- Single-driven assignments
  pde <= "";
end dyucnur;

entity alkikzqwsa is
  port (jauxkclljv : inout bit; zdrwppyo : out severity_level);
end alkikzqwsa;

library ieee;
use ieee.std_logic_1164.all;

architecture e of alkikzqwsa is
  signal vadr : string(3 to 1);
  signal co : time;
  signal ozyjp : string(3 to 1);
  signal dbdp : time;
  signal tzy : string(3 to 1);
  signal cgcexuso : time;
  signal xrxqvmkh : std_logic;
begin
  qujgd : entity work.r
    port map (jpxi => xrxqvmkh, tybddwp => cgcexuso, pde => tzy);
  rqfmgix : entity work.r
    port map (jpxi => xrxqvmkh, tybddwp => dbdp, pde => ozyjp);
  osivsh : entity work.r
    port map (jpxi => xrxqvmkh, tybddwp => co, pde => vadr);
  
  -- Multi-driven assignments
  xrxqvmkh <= xrxqvmkh;
  xrxqvmkh <= 'W';
  xrxqvmkh <= xrxqvmkh;
end e;

library ieee;
use ieee.std_logic_1164.all;

entity j is
  port (gkbt : buffer bit; siio : linkage std_logic; l : in integer);
end j;

library ieee;
use ieee.std_logic_1164.all;

architecture hnj of j is
  signal pksaiwauxu : string(3 to 1);
  signal pns : time;
  signal alewnnk : string(3 to 1);
  signal aasokz : time;
  signal ziypxcfurb : std_logic;
  signal xrvipkeg : string(3 to 1);
  signal rsxes : time;
  signal eqnehn : string(3 to 1);
  signal soiiqpfuy : time;
  signal lk : std_logic;
begin
  tczitcc : entity work.r
    port map (jpxi => lk, tybddwp => soiiqpfuy, pde => eqnehn);
  czlymumpo : entity work.r
    port map (jpxi => lk, tybddwp => rsxes, pde => xrvipkeg);
  wbllyid : entity work.r
    port map (jpxi => ziypxcfurb, tybddwp => aasokz, pde => alewnnk);
  pfml : entity work.r
    port map (jpxi => lk, tybddwp => pns, pde => pksaiwauxu);
  
  -- Single-driven assignments
  gkbt <= gkbt;
  
  -- Multi-driven assignments
  lk <= ziypxcfurb;
  lk <= 'W';
end hnj;



-- Seed after: 5949876237784371944,5511103086789671269
