-- Seed: 7062764785350604799,6329330932550885447

library ieee;
use ieee.std_logic_1164.all;

entity pab is
  port (mnszzyeawv : linkage integer; yboni : out std_logic_vector(1 to 0); nr : inout std_logic_vector(3 downto 0));
end pab;



architecture dxr of pab is
  
begin
  
end dxr;

library ieee;
use ieee.std_logic_1164.all;

entity xlbitmwtfw is
  port (h : out std_logic);
end xlbitmwtfw;



architecture uvdh of xlbitmwtfw is
  
begin
  
end uvdh;

library ieee;
use ieee.std_logic_1164.all;

entity y is
  port ( cejkzdgrx : in time_vector(1 to 4)
  ; latjjiumd : in std_logic_vector(2 downto 4)
  ; tgkhtqayi : in real
  ; leulkuneo : linkage std_logic_vector(4 to 1)
  );
end y;

library ieee;
use ieee.std_logic_1164.all;

architecture yumtfryp of y is
  signal hf : std_logic;
  signal eh : std_logic_vector(3 downto 0);
  signal gm : std_logic_vector(1 to 0);
  signal tul : std_logic_vector(3 downto 0);
  signal axkkevgd : std_logic_vector(1 to 0);
  signal optuprh : integer;
begin
  ebb : entity work.pab
    port map (mnszzyeawv => optuprh, yboni => axkkevgd, nr => tul);
  altcip : entity work.pab
    port map (mnszzyeawv => optuprh, yboni => gm, nr => eh);
  hhm : entity work.xlbitmwtfw
    port map (h => hf);
end yumtfryp;



entity eoqn is
  port (bf : inout real; i : inout real; aik : buffer bit);
end eoqn;

library ieee;
use ieee.std_logic_1164.all;

architecture xxe of eoqn is
  signal pexn : std_logic_vector(2 downto 4);
  signal b : time_vector(1 to 4);
  signal swpbooua : std_logic_vector(3 downto 0);
  signal kcvbfbe : std_logic_vector(1 to 0);
  signal feyd : integer;
  signal bgn : std_logic_vector(3 downto 0);
  signal nynzqbphmv : std_logic_vector(4 to 1);
  signal pufflj : integer;
begin
  bxx : entity work.pab
    port map (mnszzyeawv => pufflj, yboni => nynzqbphmv, nr => bgn);
  k : entity work.pab
    port map (mnszzyeawv => feyd, yboni => kcvbfbe, nr => swpbooua);
  tw : entity work.pab
    port map (mnszzyeawv => pufflj, yboni => nynzqbphmv, nr => bgn);
  paog : entity work.y
    port map (cejkzdgrx => b, latjjiumd => pexn, tgkhtqayi => bf, leulkuneo => nynzqbphmv);
end xxe;



-- Seed after: 913595397213378437,6329330932550885447
