-- Seed: 17037765669256259990,4080032123900078489

entity htnkoumx is
  port (cq : in time);
end htnkoumx;

architecture tqtofqbs of htnkoumx is
  
begin
  
end tqtofqbs;

library ieee;
use ieee.std_logic_1164.all;

entity nw is
  port (rg : out std_logic; zawtkesa : in time);
end nw;

architecture djddgej of nw is
  signal ojipu : time;
begin
  eqip : entity work.htnkoumx
    port map (cq => ojipu);
  w : entity work.htnkoumx
    port map (cq => zawtkesa);
  dij : entity work.htnkoumx
    port map (cq => zawtkesa);
  
  -- Single-driven assignments
  ojipu <= 2#0# us;
  
  -- Multi-driven assignments
  rg <= rg;
  rg <= '-';
  rg <= rg;
end djddgej;



-- Seed after: 12951020076339295884,4080032123900078489
