-- Seed: 15347292463596364254,7198033922882419595

entity hihcp is
  port (xpyntbl : out character; xc : in boolean);
end hihcp;

architecture i of hihcp is
  
begin
  -- Single-driven assignments
  xpyntbl <= 'f';
end i;

entity pgbvgwbi is
  port (zfxhocx : buffer real_vector(2 to 4));
end pgbvgwbi;

architecture pnki of pgbvgwbi is
  signal ps : character;
  signal scqq : boolean;
  signal ceqigrz : character;
begin
  h : entity work.hihcp
    port map (xpyntbl => ceqigrz, xc => scqq);
  zxijkbhdg : entity work.hihcp
    port map (xpyntbl => ps, xc => scqq);
  
  -- Single-driven assignments
  scqq <= scqq;
  zfxhocx <= zfxhocx;
end pnki;

entity kgqcdhkt is
  port (gypp : buffer integer_vector(1 downto 4));
end kgqcdhkt;

architecture utmi of kgqcdhkt is
  signal fwvxg : boolean;
  signal vwczpngp : character;
  signal brhi : real_vector(2 to 4);
  signal ewvyfkf : boolean;
  signal yurblsbdxq : character;
  signal teeaif : boolean;
  signal wi : character;
begin
  xaz : entity work.hihcp
    port map (xpyntbl => wi, xc => teeaif);
  wwqqgg : entity work.hihcp
    port map (xpyntbl => yurblsbdxq, xc => ewvyfkf);
  lrpconrpu : entity work.pgbvgwbi
    port map (zfxhocx => brhi);
  ulkhtusuzg : entity work.hihcp
    port map (xpyntbl => vwczpngp, xc => fwvxg);
end utmi;

entity rqolnunut is
  port (fzj : out integer; mdtnpni : out integer_vector(3 to 2); hennzhctcr : linkage integer; xl : out integer);
end rqolnunut;

architecture dkdvmc of rqolnunut is
  signal wfrdyqzww : character;
  signal ow : character;
  signal aklari : character;
  signal elfuecmv : boolean;
  signal wgardzbdzi : character;
begin
  s : entity work.hihcp
    port map (xpyntbl => wgardzbdzi, xc => elfuecmv);
  yqwrvvfgy : entity work.hihcp
    port map (xpyntbl => aklari, xc => elfuecmv);
  kghfgk : entity work.hihcp
    port map (xpyntbl => ow, xc => elfuecmv);
  yxrgtu : entity work.hihcp
    port map (xpyntbl => wfrdyqzww, xc => elfuecmv);
  
  -- Single-driven assignments
  xl <= 3_1_4_4_0;
end dkdvmc;



-- Seed after: 9882225936573206225,7198033922882419595
