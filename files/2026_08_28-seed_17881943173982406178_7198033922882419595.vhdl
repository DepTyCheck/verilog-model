-- Seed: 17881943173982406178,7198033922882419595

entity ta is
  port (rczjyx : linkage real; ccgk : inout character; wdatsmqqf : linkage real);
end ta;

architecture ltreadw of ta is
  
begin
  -- Single-driven assignments
  ccgk <= ccgk;
end ltreadw;

entity fpaag is
  port (jyr : linkage bit);
end fpaag;

architecture opfagaw of fpaag is
  signal fuzuwde : real;
  signal fpvfgseb : character;
  signal okhurylgxw : real;
  signal cmwjcm : real;
  signal swip : character;
  signal uadb : real;
begin
  zzpwzein : entity work.ta
    port map (rczjyx => uadb, ccgk => swip, wdatsmqqf => cmwjcm);
  fpsi : entity work.ta
    port map (rczjyx => okhurylgxw, ccgk => fpvfgseb, wdatsmqqf => fuzuwde);
end opfagaw;

library ieee;
use ieee.std_logic_1164.all;

entity qsparrz is
  port (r : buffer bit; xwpi : buffer std_logic; qvit : buffer integer; tj : out time_vector(2 to 4));
end qsparrz;

architecture z of qsparrz is
  
begin
  -- Single-driven assignments
  tj <= tj;
  r <= r;
  qvit <= 8#6#;
end z;

entity pkb is
  port (oqxmxfe : buffer integer; ozsqxm : in real; i : in integer; qpscjimj : in character);
end pkb;

architecture xsdp of pkb is
  signal afkaiyjjrd : real;
  signal zftx : character;
  signal wkc : real;
  signal h : real;
  signal uavnvadl : character;
  signal ybfimn : real;
  signal ddsanamqq : real;
  signal uw : character;
  signal n : real;
  signal qe : real;
  signal ensflnv : character;
  signal ssjihla : real;
begin
  y : entity work.ta
    port map (rczjyx => ssjihla, ccgk => ensflnv, wdatsmqqf => qe);
  q : entity work.ta
    port map (rczjyx => n, ccgk => uw, wdatsmqqf => ddsanamqq);
  mgusegc : entity work.ta
    port map (rczjyx => ybfimn, ccgk => uavnvadl, wdatsmqqf => h);
  aqzp : entity work.ta
    port map (rczjyx => wkc, ccgk => zftx, wdatsmqqf => afkaiyjjrd);
  
  -- Single-driven assignments
  oqxmxfe <= i;
end xsdp;



-- Seed after: 13359489686979112326,7198033922882419595
