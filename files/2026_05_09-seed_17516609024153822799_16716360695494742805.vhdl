-- Seed: 17516609024153822799,16716360695494742805



entity leztd is
  port (fisevaywu : buffer severity_level; xczhvihkv : out real; qwr : inout real; kijsunhagy : out time);
end leztd;



architecture dwbgcqa of leztd is
  
begin
  
end dwbgcqa;



entity edbcbmu is
  port (wkesvxd : buffer time; y : out real);
end edbcbmu;



architecture rinweiel of edbcbmu is
  signal o : time;
  signal rdddmscam : real;
  signal gfabrv : severity_level;
  signal gmwqp : time;
  signal xjf : real;
  signal ngsscohhv : real;
  signal dnfizzk : severity_level;
begin
  bquaerajht : entity work.leztd
    port map (fisevaywu => dnfizzk, xczhvihkv => ngsscohhv, qwr => xjf, kijsunhagy => gmwqp);
  gveisxxz : entity work.leztd
    port map (fisevaywu => gfabrv, xczhvihkv => rdddmscam, qwr => y, kijsunhagy => o);
end rinweiel;



entity phfwusmiiz is
  port (oohvocgir : linkage integer);
end phfwusmiiz;



architecture ykdirl of phfwusmiiz is
  
begin
  
end ykdirl;

library ieee;
use ieee.std_logic_1164.all;

entity kjevqtvto is
  port (oiopqbguzc : buffer std_logic);
end kjevqtvto;



architecture hs of kjevqtvto is
  signal piiqqy : time;
  signal hjwdctwqgh : real;
  signal ey : real;
  signal fygoyitj : severity_level;
  signal smsibul : time;
  signal mjyiup : real;
  signal oo : real;
  signal opeorftmz : severity_level;
  signal l : time;
  signal dyg : real;
  signal frlmnnv : real;
  signal iopurtbkt : severity_level;
begin
  wkroaxabng : entity work.leztd
    port map (fisevaywu => iopurtbkt, xczhvihkv => frlmnnv, qwr => dyg, kijsunhagy => l);
  q : entity work.leztd
    port map (fisevaywu => opeorftmz, xczhvihkv => oo, qwr => mjyiup, kijsunhagy => smsibul);
  mwgv : entity work.leztd
    port map (fisevaywu => fygoyitj, xczhvihkv => ey, qwr => hjwdctwqgh, kijsunhagy => piiqqy);
end hs;



-- Seed after: 5758994659682930436,16716360695494742805
