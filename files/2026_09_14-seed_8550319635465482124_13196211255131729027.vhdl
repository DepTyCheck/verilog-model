-- Seed: 8550319635465482124,13196211255131729027

library ieee;
use ieee.std_logic_1164.all;

entity xyekuy is
  port (jbsb : linkage severity_level; foganvb : buffer std_logic_vector(0 downto 2); cdatnxpotv : in real_vector(2 to 0));
end xyekuy;

architecture gw of xyekuy is
  
begin
  -- Multi-driven assignments
  foganvb <= "";
  foganvb <= "";
  foganvb <= (others => '0');
end gw;

library ieee;
use ieee.std_logic_1164.all;

entity u is
  port (veniyat : in std_logic_vector(0 to 4); cvdwv : buffer integer; arrguhpr : inout string(4 to 2); psradfq : in std_logic);
end u;

library ieee;
use ieee.std_logic_1164.all;

architecture hpcyr of u is
  signal crzgj : std_logic_vector(0 downto 2);
  signal pywswiehfc : severity_level;
  signal mbbyxmqps : real_vector(2 to 0);
  signal acg : std_logic_vector(0 downto 2);
  signal fasil : severity_level;
  signal oehd : real_vector(2 to 0);
  signal dchvlryinv : std_logic_vector(0 downto 2);
  signal dhelwwil : severity_level;
begin
  xwt : entity work.xyekuy
    port map (jbsb => dhelwwil, foganvb => dchvlryinv, cdatnxpotv => oehd);
  arjgzt : entity work.xyekuy
    port map (jbsb => fasil, foganvb => acg, cdatnxpotv => mbbyxmqps);
  k : entity work.xyekuy
    port map (jbsb => pywswiehfc, foganvb => crzgj, cdatnxpotv => oehd);
  
  -- Single-driven assignments
  arrguhpr <= arrguhpr;
  
  -- Multi-driven assignments
  dchvlryinv <= dchvlryinv;
  dchvlryinv <= "";
end hpcyr;



-- Seed after: 16363074753165057313,13196211255131729027
