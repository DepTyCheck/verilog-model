-- Seed: 12676067049428658885,2230106469645304029

library ieee;
use ieee.std_logic_1164.all;

entity ml is
  port (snz : in severity_level; xmi : linkage real; dfrcqvas : linkage time; v : out std_logic_vector(1 downto 3));
end ml;

architecture dar of ml is
  
begin
  -- Multi-driven assignments
  v <= v;
  v <= "";
  v <= v;
  v <= "";
end dar;

library ieee;
use ieee.std_logic_1164.all;

entity zjbuqqpzg is
  port (vgquu : linkage integer; frwdoiug : in std_logic_vector(0 to 4); edkgexy : out bit_vector(2 to 0));
end zjbuqqpzg;

library ieee;
use ieee.std_logic_1164.all;

architecture zdpd of zjbuqqpzg is
  signal jctbce : time;
  signal rkpdoytse : real;
  signal ws : severity_level;
  signal mpe : time;
  signal uhda : real;
  signal ho : severity_level;
  signal otzpwksvib : time;
  signal qtkfytzs : real;
  signal mlrhjzngv : severity_level;
  signal mazsybkmh : std_logic_vector(1 downto 3);
  signal lj : time;
  signal swj : real;
  signal kpjgaoy : severity_level;
begin
  empdrzkm : entity work.ml
    port map (snz => kpjgaoy, xmi => swj, dfrcqvas => lj, v => mazsybkmh);
  qhp : entity work.ml
    port map (snz => mlrhjzngv, xmi => qtkfytzs, dfrcqvas => otzpwksvib, v => mazsybkmh);
  tfdzbzs : entity work.ml
    port map (snz => ho, xmi => uhda, dfrcqvas => mpe, v => mazsybkmh);
  idoadsemr : entity work.ml
    port map (snz => ws, xmi => rkpdoytse, dfrcqvas => jctbce, v => mazsybkmh);
  
  -- Single-driven assignments
  kpjgaoy <= NOTE;
  ho <= mlrhjzngv;
  mlrhjzngv <= ERROR;
  edkgexy <= edkgexy;
  ws <= WARNING;
  
  -- Multi-driven assignments
  mazsybkmh <= (others => '0');
end zdpd;

entity cwgu is
  port (btzqseozs : out severity_level; tpkmhlj : buffer real_vector(0 to 1); tnkhvq : inout bit);
end cwgu;

library ieee;
use ieee.std_logic_1164.all;

architecture kjzrajprd of cwgu is
  signal absjdc : std_logic_vector(1 downto 3);
  signal e : time;
  signal c : real;
  signal avoh : std_logic_vector(1 downto 3);
  signal nvadh : time;
  signal xsky : real;
  signal azseesqrvq : std_logic_vector(1 downto 3);
  signal vj : time;
  signal vtff : real;
  signal rk : severity_level;
begin
  ssxwm : entity work.ml
    port map (snz => rk, xmi => vtff, dfrcqvas => vj, v => azseesqrvq);
  gdr : entity work.ml
    port map (snz => rk, xmi => xsky, dfrcqvas => nvadh, v => avoh);
  uwjkkfvmlr : entity work.ml
    port map (snz => rk, xmi => c, dfrcqvas => e, v => absjdc);
  
  -- Multi-driven assignments
  absjdc <= (others => '0');
  avoh <= (others => '0');
  azseesqrvq <= (others => '0');
end kjzrajprd;



-- Seed after: 9101593413762172153,2230106469645304029
