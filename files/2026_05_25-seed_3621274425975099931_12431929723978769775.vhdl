-- Seed: 3621274425975099931,12431929723978769775

library ieee;
use ieee.std_logic_1164.all;

entity r is
  port (rjiiuwbl : out std_logic_vector(0 to 0); mrnj : linkage character; tiabrx : out character; pfjsy : linkage bit_vector(1 to 0));
end r;



architecture uofkoietl of r is
  
begin
  
end uofkoietl;

library ieee;
use ieee.std_logic_1164.all;

entity arys is
  port (prkbsgj : in integer; bkygx : buffer std_logic_vector(1 to 0); mm : inout real; uw : inout integer);
end arys;



architecture fndml of arys is
  
begin
  
end fndml;



entity w is
  port (jiv : inout time; prkywko : buffer time);
end w;

library ieee;
use ieee.std_logic_1164.all;

architecture adwuar of w is
  signal gbos : bit_vector(1 to 0);
  signal lmc : character;
  signal j : character;
  signal voelcu : std_logic_vector(0 to 0);
  signal rxbxymp : integer;
  signal obbdmbjuo : real;
  signal qazshqa : std_logic_vector(1 to 0);
  signal yh : integer;
begin
  tauqshnuje : entity work.arys
    port map (prkbsgj => yh, bkygx => qazshqa, mm => obbdmbjuo, uw => rxbxymp);
  vanxjdlg : entity work.r
    port map (rjiiuwbl => voelcu, mrnj => j, tiabrx => lmc, pfjsy => gbos);
end adwuar;

library ieee;
use ieee.std_logic_1164.all;

entity cblmpgar is
  port (wwe : in real; fdl : out std_logic_vector(0 downto 3); neothrysrw : in std_logic_vector(2 to 1));
end cblmpgar;

library ieee;
use ieee.std_logic_1164.all;

architecture vcw of cblmpgar is
  signal pyo : bit_vector(1 to 0);
  signal mttcyh : character;
  signal jwmpcov : std_logic_vector(0 to 0);
begin
  riv : entity work.r
    port map (rjiiuwbl => jwmpcov, mrnj => mttcyh, tiabrx => mttcyh, pfjsy => pyo);
end vcw;



-- Seed after: 13868667961492156354,12431929723978769775
