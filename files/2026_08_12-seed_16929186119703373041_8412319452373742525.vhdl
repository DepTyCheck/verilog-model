-- Seed: 16929186119703373041,8412319452373742525

library ieee;
use ieee.std_logic_1164.all;

entity cr is
  port (twedauu : buffer integer; tugdcs : buffer string(3 to 3); radylgs : in real; esslgf : buffer std_logic_vector(3 to 3));
end cr;

architecture rabsys of cr is
  
begin
  -- Single-driven assignments
  tugdcs <= (others => 'f');
  twedauu <= twedauu;
  
  -- Multi-driven assignments
  esslgf <= esslgf;
end rabsys;

entity fbpuoztbo is
  port (hgxej : in boolean; qcoggksr : in time; jcodkij : linkage real);
end fbpuoztbo;

library ieee;
use ieee.std_logic_1164.all;

architecture smidmt of fbpuoztbo is
  signal aybzmnc : real;
  signal zn : string(3 to 3);
  signal nmkbttjk : integer;
  signal qwhtox : string(3 to 3);
  signal shqzv : integer;
  signal grqaqxuq : std_logic_vector(3 to 3);
  signal xbae : real;
  signal kvximj : string(3 to 3);
  signal ijr : integer;
  signal zfodxetot : std_logic_vector(3 to 3);
  signal imfra : real;
  signal fv : string(3 to 3);
  signal jj : integer;
begin
  bzwlfzencx : entity work.cr
    port map (twedauu => jj, tugdcs => fv, radylgs => imfra, esslgf => zfodxetot);
  ilt : entity work.cr
    port map (twedauu => ijr, tugdcs => kvximj, radylgs => xbae, esslgf => grqaqxuq);
  kwk : entity work.cr
    port map (twedauu => shqzv, tugdcs => qwhtox, radylgs => imfra, esslgf => zfodxetot);
  isdomy : entity work.cr
    port map (twedauu => nmkbttjk, tugdcs => zn, radylgs => aybzmnc, esslgf => grqaqxuq);
  
  -- Single-driven assignments
  imfra <= 16#6B.4384#;
end smidmt;

library ieee;
use ieee.std_logic_1164.all;

entity iafgcv is
  port (hvih : inout std_logic_vector(4 to 2); ro : linkage std_logic_vector(1 downto 0); cvoaskno : inout time);
end iafgcv;

library ieee;
use ieee.std_logic_1164.all;

architecture drothy of iafgcv is
  signal krbhmokc : real;
  signal a : string(3 to 3);
  signal kfxr : integer;
  signal g : real;
  signal kyp : boolean;
  signal ij : std_logic_vector(3 to 3);
  signal pgaqfhblf : real;
  signal cjr : string(3 to 3);
  signal qdsyb : integer;
begin
  zmtrr : entity work.cr
    port map (twedauu => qdsyb, tugdcs => cjr, radylgs => pgaqfhblf, esslgf => ij);
  iqkauu : entity work.fbpuoztbo
    port map (hgxej => kyp, qcoggksr => cvoaskno, jcodkij => g);
  tdyhvi : entity work.cr
    port map (twedauu => kfxr, tugdcs => a, radylgs => krbhmokc, esslgf => ij);
  
  -- Single-driven assignments
  krbhmokc <= 8#4_1_5_6_3.4_1_1#;
  
  -- Multi-driven assignments
  hvih <= hvih;
end drothy;



-- Seed after: 10599182246184047085,8412319452373742525
