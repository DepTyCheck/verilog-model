// Seed: 13545088643485442407,4842319300033127013

module cba (input supply1 logic [3:3][1:1][0:2] pv [4:3], output real y);
  
  xnor zalrk(y, nhr, nhr);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   real y -> logic y
  
  xor zdjibhxeu(ons, y, y);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   real y -> logic y
  //
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   real y -> logic y
  
  and crchfkikus(nhr, hl, dwnlkfslbc);
  
  xor oigtgaquxh(dwnlkfslbc, thkuyachn, ude);
  
  
  // Single-driven assigns
  
  // Multi-driven assigns
  assign ons = thkuyachn;
  assign thkuyachn = 'bxx0x0;
  assign nhr = nhr;
  assign hl = 'bx10z0;
endmodule: cba



// Seed after: 18404616924823582139,4842319300033127013
