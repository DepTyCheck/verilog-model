-- Seed: 14511291160050465601,3687118713772291287

entity xhvxn is
  port (mgpfwm : buffer real_vector(1 downto 1); ahsyf : buffer boolean_vector(2 to 1));
end xhvxn;

architecture dbogixuwjd of xhvxn is
  
begin
  -- Single-driven assignments
  ahsyf <= (others => TRUE);
  mgpfwm <= (others => 2#110.1#);
end dbogixuwjd;

library ieee;
use ieee.std_logic_1164.all;

entity bgepl is
  port (d : out time; bvmd : linkage real; rvlsjl : inout severity_level; ngnod : buffer std_logic_vector(3 downto 1));
end bgepl;

architecture zlkbhabta of bgepl is
  signal sdiigfi : boolean_vector(2 to 1);
  signal uclla : real_vector(1 downto 1);
  signal roetuialhw : boolean_vector(2 to 1);
  signal sbxcri : real_vector(1 downto 1);
  signal xlhcoyphpm : boolean_vector(2 to 1);
  signal gsffet : real_vector(1 downto 1);
  signal m : boolean_vector(2 to 1);
  signal klqywfr : real_vector(1 downto 1);
begin
  rxpp : entity work.xhvxn
    port map (mgpfwm => klqywfr, ahsyf => m);
  lqzdv : entity work.xhvxn
    port map (mgpfwm => gsffet, ahsyf => xlhcoyphpm);
  bmunegu : entity work.xhvxn
    port map (mgpfwm => sbxcri, ahsyf => roetuialhw);
  y : entity work.xhvxn
    port map (mgpfwm => uclla, ahsyf => sdiigfi);
  
  -- Single-driven assignments
  rvlsjl <= NOTE;
  d <= 2#111.0011# ms;
  
  -- Multi-driven assignments
  ngnod <= ('1', '1', 'H');
  ngnod <= ('1', 'H', '-');
  ngnod <= ('H', 'L', 'U');
end zlkbhabta;

library ieee;
use ieee.std_logic_1164.all;

entity nmlain is
  port (yufekngafd : buffer std_logic);
end nmlain;

library ieee;
use ieee.std_logic_1164.all;

architecture jxwpfetjn of nmlain is
  signal ueit : std_logic_vector(3 downto 1);
  signal pwfkum : severity_level;
  signal mphxg : real;
  signal qibsx : time;
  signal bh : boolean_vector(2 to 1);
  signal ovfxpd : real_vector(1 downto 1);
begin
  bgu : entity work.xhvxn
    port map (mgpfwm => ovfxpd, ahsyf => bh);
  ujoirwaaf : entity work.bgepl
    port map (d => qibsx, bvmd => mphxg, rvlsjl => pwfkum, ngnod => ueit);
  
  -- Multi-driven assignments
  yufekngafd <= 'U';
  ueit <= "U1Z";
  yufekngafd <= 'L';
end jxwpfetjn;

library ieee;
use ieee.std_logic_1164.all;

entity fwqkvaeghs is
  port (yfgc : in std_logic_vector(4 to 2); yiywhlpe : in time; qracci : out real);
end fwqkvaeghs;

architecture tw of fwqkvaeghs is
  
begin
  -- Single-driven assignments
  qracci <= 3_2_2_2.300;
end tw;



-- Seed after: 17187578319612671968,3687118713772291287
