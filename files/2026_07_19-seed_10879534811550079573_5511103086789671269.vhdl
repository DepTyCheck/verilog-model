-- Seed: 10879534811550079573,5511103086789671269

entity nxpkrq is
  port (bpjo : linkage real; rvszl : inout boolean);
end nxpkrq;

architecture f of nxpkrq is
  
begin
  
end f;

entity mabpcbgh is
  port (mogynsn : in integer);
end mabpcbgh;

architecture shvwcfnit of mabpcbgh is
  signal xvl : boolean;
  signal zq : real;
  signal yyypocb : boolean;
  signal g : real;
begin
  gcb : entity work.nxpkrq
    port map (bpjo => g, rvszl => yyypocb);
  k : entity work.nxpkrq
    port map (bpjo => zq, rvszl => xvl);
end shvwcfnit;

entity gjnppukoyp is
  port (q : linkage string(1 to 1); dbh : inout real; amfzbu : in boolean_vector(4 to 0); rzdchota : linkage integer_vector(4 to 4));
end gjnppukoyp;

architecture dvgvjkdkfv of gjnppukoyp is
  signal ijrpeyuyfy : boolean;
  signal qwmqd : real;
  signal jdpzw : integer;
begin
  ji : entity work.mabpcbgh
    port map (mogynsn => jdpzw);
  amuyjtyxt : entity work.nxpkrq
    port map (bpjo => qwmqd, rvszl => ijrpeyuyfy);
end dvgvjkdkfv;

library ieee;
use ieee.std_logic_1164.all;

entity dtq is
  port ( gzcn : out std_logic_vector(4 to 0)
  ; jiyks : out string(2 downto 3)
  ; semouesc : linkage std_logic_vector(1 to 0)
  ; wpl : out std_logic_vector(0 to 0)
  );
end dtq;

architecture herxscrd of dtq is
  signal kcoyhankh : boolean;
  signal ubsm : real;
  signal zg : integer;
begin
  bx : entity work.mabpcbgh
    port map (mogynsn => zg);
  trvkigdr : entity work.mabpcbgh
    port map (mogynsn => zg);
  txapmhujm : entity work.nxpkrq
    port map (bpjo => ubsm, rvszl => kcoyhankh);
  
  -- Multi-driven assignments
  wpl <= wpl;
  gzcn <= gzcn;
  wpl <= wpl;
  gzcn <= "";
end herxscrd;



-- Seed after: 4610367222777482369,5511103086789671269
