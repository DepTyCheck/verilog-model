-- Seed: 7842804138159065160,7726014785203345639

use std.reflection.all;

entity dmzbletpw is
  port (tkwsuksm : out real; nnsfybk : inout protected_value_mirror; q : linkage boolean);
end dmzbletpw;

architecture yozfdknpir of dmzbletpw is
  
begin
  -- Single-driven assignments
  tkwsuksm <= tkwsuksm;
end yozfdknpir;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity hkmg is
  port (nxnbfh : buffer std_logic; ojlaojcj : inout enumeration_value_mirror; sg : inout record_value_mirror);
end hkmg;

architecture wd of hkmg is
  
begin
  -- Multi-driven assignments
  nxnbfh <= 'H';
  nxnbfh <= nxnbfh;
  nxnbfh <= '1';
end wd;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity yajugnofb is
  port (e : inout std_logic_vector(4 to 1); dhc : inout array_value_mirror);
end yajugnofb;

use std.reflection.all;

architecture rrko of yajugnofb is
  signal tdzpbybihi : boolean;
  shared variable uqzxoxeer : protected_value_mirror;
  signal xir : real;
  signal aduenqgn : boolean;
  shared variable h : protected_value_mirror;
  signal qexrcza : real;
begin
  dghki : entity work.dmzbletpw
    port map (tkwsuksm => qexrcza, nnsfybk => h, q => aduenqgn);
  vdlelp : entity work.dmzbletpw
    port map (tkwsuksm => xir, nnsfybk => uqzxoxeer, q => tdzpbybihi);
  
  -- Multi-driven assignments
  e <= (others => '0');
  e <= "";
  e <= (others => '0');
end rrko;

use std.reflection.all;

entity ssupyi is
  port (ttykf : inout severity_level; udsrglc : inout record_subtype_mirror);
end ssupyi;

use std.reflection.all;

architecture irivvucr of ssupyi is
  signal nrct : boolean;
  shared variable rqzsnoqvw : protected_value_mirror;
  signal d : real;
  signal lwgkiph : boolean;
  shared variable tkheqhd : protected_value_mirror;
  signal vkwbo : real;
begin
  xgxfbhpggl : entity work.dmzbletpw
    port map (tkwsuksm => vkwbo, nnsfybk => tkheqhd, q => lwgkiph);
  ri : entity work.dmzbletpw
    port map (tkwsuksm => d, nnsfybk => rqzsnoqvw, q => nrct);
  
  -- Single-driven assignments
  ttykf <= ttykf;
end irivvucr;



-- Seed after: 644738628426096758,7726014785203345639
