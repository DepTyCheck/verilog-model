-- Seed: 3189930066455514046,6290177331721581829

use std.reflection.all;

entity vue is
  port (kprikdvth : inout floating_subtype_mirror; uwof : inout time; r : inout file_subtype_mirror; cqfvx : inout integer_vector(1 to 4));
end vue;

architecture gg of vue is
  
begin
  -- Single-driven assignments
  cqfvx <= cqfvx;
  uwof <= 434 ms;
end gg;

use std.reflection.all;

entity reql is
  port (ld : inout access_subtype_mirror; nzysstbvsb : inout value_mirror; qdhnizvjc : in integer);
end reql;

use std.reflection.all;

architecture r of reql is
  signal f : integer_vector(1 to 4);
  shared variable uiojdwk : file_subtype_mirror;
  signal bzdairta : time;
  shared variable sedoz : floating_subtype_mirror;
begin
  fq : entity work.vue
    port map (kprikdvth => sedoz, uwof => bzdairta, r => uiojdwk, cqfvx => f);
end r;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity eovtcgo is
  port (tgnz : linkage std_logic; sqcfndb : inout physical_value_mirror; utufrl : inout time; qk : inout enumeration_subtype_mirror);
end eovtcgo;

use std.reflection.all;

architecture tr of eovtcgo is
  signal pcsbm : integer_vector(1 to 4);
  shared variable elunrsl : file_subtype_mirror;
  signal knbaxsv : time;
  shared variable pchwtgc : floating_subtype_mirror;
  signal ejueerl : integer_vector(1 to 4);
  shared variable ayeztbz : file_subtype_mirror;
  signal zvyjsnkbc : time;
  shared variable wt : floating_subtype_mirror;
  signal dyvgnqsvd : integer_vector(1 to 4);
  shared variable ml : file_subtype_mirror;
  signal oyggwljs : time;
  shared variable a : floating_subtype_mirror;
  signal lgrbghi : integer;
  shared variable dzg : value_mirror;
  shared variable eekb : access_subtype_mirror;
begin
  i : entity work.reql
    port map (ld => eekb, nzysstbvsb => dzg, qdhnizvjc => lgrbghi);
  uvslcjm : entity work.vue
    port map (kprikdvth => a, uwof => oyggwljs, r => ml, cqfvx => dyvgnqsvd);
  qeum : entity work.vue
    port map (kprikdvth => wt, uwof => zvyjsnkbc, r => ayeztbz, cqfvx => ejueerl);
  detth : entity work.vue
    port map (kprikdvth => pchwtgc, uwof => knbaxsv, r => elunrsl, cqfvx => pcsbm);
  
  -- Single-driven assignments
  lgrbghi <= 16#94#;
end tr;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity sid is
  port ( w : out boolean_vector(1 to 4)
  ; qezwx : inout physical_value_mirror
  ; kkm : inout floating_value_mirror
  ; qsrmezk : out std_logic_vector(3 downto 3)
  );
end sid;

use std.reflection.all;

architecture mpegclsua of sid is
  signal pbybg : integer;
  shared variable xwmxrde : value_mirror;
  shared variable ejnjwz : access_subtype_mirror;
begin
  xiy : entity work.reql
    port map (ld => ejnjwz, nzysstbvsb => xwmxrde, qdhnizvjc => pbybg);
  
  -- Single-driven assignments
  w <= (FALSE, TRUE, FALSE, TRUE);
  pbybg <= 2_3;
  
  -- Multi-driven assignments
  qsrmezk <= qsrmezk;
  qsrmezk <= (others => 'W');
end mpegclsua;



-- Seed after: 1736765969859152610,6290177331721581829
