-- Seed: 13580408473564765271,14641901754878719179

library ieee;
use ieee.std_logic_1164.all;

entity tgp is
  port (eh : buffer integer; bdyie : buffer severity_level; od : linkage std_logic);
end tgp;

architecture ea of tgp is
  
begin
  -- Single-driven assignments
  bdyie <= bdyie;
end ea;

library ieee;
use ieee.std_logic_1164.all;

entity hnyg is
  port (kjjoaabw : buffer time_vector(1 to 3); uncw : inout std_logic; fyulrprzc : linkage std_logic; ejtowp : inout boolean_vector(4 to 2));
end hnyg;

library ieee;
use ieee.std_logic_1164.all;

architecture dneiwtzadk of hnyg is
  signal eg : std_logic;
  signal jhulebqui : severity_level;
  signal jtrdk : integer;
  signal ssnjgptmd : std_logic;
  signal kvbxodkg : severity_level;
  signal sz : integer;
begin
  siswylffy : entity work.tgp
    port map (eh => sz, bdyie => kvbxodkg, od => ssnjgptmd);
  schlvkeso : entity work.tgp
    port map (eh => jtrdk, bdyie => jhulebqui, od => eg);
  
  -- Single-driven assignments
  ejtowp <= (others => TRUE);
  
  -- Multi-driven assignments
  uncw <= uncw;
  uncw <= '-';
end dneiwtzadk;

library ieee;
use ieee.std_logic_1164.all;

entity pclsf is
  port (znc : out std_logic_vector(1 to 3); j : buffer integer; fmbglihxob : in std_logic_vector(2 to 2));
end pclsf;

library ieee;
use ieee.std_logic_1164.all;

architecture nxdh of pclsf is
  signal oypjjmqya : severity_level;
  signal d : integer;
  signal wauvaiwkj : severity_level;
  signal z : std_logic;
  signal xyk : severity_level;
  signal v : integer;
begin
  dxr : entity work.tgp
    port map (eh => v, bdyie => xyk, od => z);
  owmifjk : entity work.tgp
    port map (eh => j, bdyie => wauvaiwkj, od => z);
  gkivvx : entity work.tgp
    port map (eh => d, bdyie => oypjjmqya, od => z);
end nxdh;



-- Seed after: 3667906157021897349,14641901754878719179
