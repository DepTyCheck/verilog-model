-- Seed: 4794360513041715770,6290177331721581829

use std.reflection.all;

entity tdlo is
  port (zpsqpfpj : inout subtype_mirror; tpeldbjflb : inout subtype_mirror; wrdu : inout array_value_mirror; r : in integer);
end tdlo;

architecture e of tdlo is
  
begin
  
end e;

library ieee;
use ieee.std_logic_1164.all;

entity hqmvxhb is
  port (netdeol : inout bit; e : out std_logic_vector(3 to 1); wexxefdg : out real);
end hqmvxhb;

use std.reflection.all;

architecture jdlqbprg of hqmvxhb is
  signal j : integer;
  shared variable zheiuri : array_value_mirror;
  shared variable oxq : subtype_mirror;
  shared variable shynzj : subtype_mirror;
  signal kazecqrvt : integer;
  shared variable oddd : array_value_mirror;
  shared variable gjehqvgq : subtype_mirror;
  shared variable fj : subtype_mirror;
  signal hkeym : integer;
  shared variable acv : array_value_mirror;
  shared variable ixrtukxku : subtype_mirror;
  shared variable yezsaa : subtype_mirror;
  signal sbx : integer;
  shared variable u : array_value_mirror;
  shared variable otvb : subtype_mirror;
  shared variable cc : subtype_mirror;
begin
  vudhoxlim : entity work.tdlo
    port map (zpsqpfpj => cc, tpeldbjflb => otvb, wrdu => u, r => sbx);
  xmpm : entity work.tdlo
    port map (zpsqpfpj => yezsaa, tpeldbjflb => ixrtukxku, wrdu => acv, r => hkeym);
  inatzimhd : entity work.tdlo
    port map (zpsqpfpj => fj, tpeldbjflb => gjehqvgq, wrdu => oddd, r => kazecqrvt);
  rurcdhtxf : entity work.tdlo
    port map (zpsqpfpj => shynzj, tpeldbjflb => oxq, wrdu => zheiuri, r => j);
  
  -- Single-driven assignments
  j <= sbx;
end jdlqbprg;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity mnr is
  port (ox : inout enumeration_value_mirror; zfdrke : inout std_logic_vector(1 to 1));
end mnr;

use std.reflection.all;

architecture czvxwmnii of mnr is
  signal k : integer;
  shared variable chjoerao : array_value_mirror;
  shared variable lb : subtype_mirror;
  shared variable eoexhvnsv : subtype_mirror;
  signal walzx : integer;
  shared variable wtqurjj : array_value_mirror;
  shared variable zdex : subtype_mirror;
  shared variable m : subtype_mirror;
begin
  aeo : entity work.tdlo
    port map (zpsqpfpj => m, tpeldbjflb => zdex, wrdu => wtqurjj, r => walzx);
  dyaujm : entity work.tdlo
    port map (zpsqpfpj => eoexhvnsv, tpeldbjflb => lb, wrdu => chjoerao, r => k);
  
  -- Single-driven assignments
  walzx <= walzx;
  k <= walzx;
end czvxwmnii;



-- Seed after: 18407988023204121240,6290177331721581829
