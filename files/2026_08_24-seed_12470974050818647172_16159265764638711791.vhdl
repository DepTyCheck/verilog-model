-- Seed: 12470974050818647172,16159265764638711791

entity wmh is
  port (l : buffer string(1 downto 4));
end wmh;

architecture oygf of wmh is
  
begin
  -- Single-driven assignments
  l <= "";
end oygf;

library ieee;
use ieee.std_logic_1164.all;

entity d is
  port (dhos : inout real; h : linkage integer; gmikszlx : linkage std_logic_vector(4 to 2));
end d;

architecture tdriu of d is
  signal n : string(1 downto 4);
  signal rxl : string(1 downto 4);
  signal clrio : string(1 downto 4);
begin
  fepjeugfaj : entity work.wmh
    port map (l => clrio);
  ad : entity work.wmh
    port map (l => rxl);
  wfnsgq : entity work.wmh
    port map (l => n);
  
  -- Single-driven assignments
  dhos <= dhos;
end tdriu;

entity fw is
  port (wyatp : buffer real; ug : inout real);
end fw;

architecture cyf of fw is
  signal o : string(1 downto 4);
  signal lxic : string(1 downto 4);
  signal jgwyyjvi : string(1 downto 4);
  signal cwa : string(1 downto 4);
begin
  v : entity work.wmh
    port map (l => cwa);
  bszxeclx : entity work.wmh
    port map (l => jgwyyjvi);
  s : entity work.wmh
    port map (l => lxic);
  rdhn : entity work.wmh
    port map (l => o);
  
  -- Single-driven assignments
  ug <= ug;
  wyatp <= 0.41;
end cyf;



-- Seed after: 12590232575117127632,16159265764638711791
