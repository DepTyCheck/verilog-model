-- Seed: 74787538619899918,13843488114570579517

library ieee;
use ieee.std_logic_1164.all;

entity xikj is
  port (lqf : inout severity_level; s : in integer; w : buffer time; vmyheko : linkage std_logic_vector(2 downto 0));
end xikj;

architecture wmfdifya of xikj is
  
begin
  -- Single-driven assignments
  w <= w;
  lqf <= ERROR;
end wmfdifya;

library ieee;
use ieee.std_logic_1164.all;

entity ushrznmfwu is
  port (meg : buffer boolean_vector(3 to 0); qv : inout integer; nklgxgegod : inout integer; omi : in std_logic);
end ushrznmfwu;

library ieee;
use ieee.std_logic_1164.all;

architecture fvbqniowxm of ushrznmfwu is
  signal nkp : std_logic_vector(2 downto 0);
  signal jknyvqfuqg : time;
  signal ifroiyczg : integer;
  signal ohv : severity_level;
  signal zgszbin : std_logic_vector(2 downto 0);
  signal rjsbv : time;
  signal ilu : severity_level;
  signal crb : std_logic_vector(2 downto 0);
  signal bgyzsan : time;
  signal pwreqbpf : severity_level;
begin
  b : entity work.xikj
    port map (lqf => pwreqbpf, s => nklgxgegod, w => bgyzsan, vmyheko => crb);
  awpdh : entity work.xikj
    port map (lqf => ilu, s => nklgxgegod, w => rjsbv, vmyheko => zgszbin);
  qq : entity work.xikj
    port map (lqf => ohv, s => ifroiyczg, w => jknyvqfuqg, vmyheko => nkp);
end fvbqniowxm;

library ieee;
use ieee.std_logic_1164.all;

entity m is
  port ( deuhrkj : linkage std_logic_vector(4 downto 1)
  ; uiyd : buffer time
  ; sasamowumt : buffer std_logic_vector(3 to 2)
  ; tsjemgziqg : out std_logic_vector(4 to 4)
  );
end m;

library ieee;
use ieee.std_logic_1164.all;

architecture i of m is
  signal rejagqap : severity_level;
  signal mfsturmqp : std_logic_vector(2 downto 0);
  signal exlge : time;
  signal pxnkaceoge : integer;
  signal sp : severity_level;
begin
  qrje : entity work.xikj
    port map (lqf => sp, s => pxnkaceoge, w => exlge, vmyheko => mfsturmqp);
  lww : entity work.xikj
    port map (lqf => rejagqap, s => pxnkaceoge, w => uiyd, vmyheko => mfsturmqp);
end i;

library ieee;
use ieee.std_logic_1164.all;

entity fnlen is
  port (ixqhigy : out std_logic; jjqgpil : out real);
end fnlen;

architecture hlw of fnlen is
  
begin
  -- Single-driven assignments
  jjqgpil <= jjqgpil;
end hlw;



-- Seed after: 17903921239628891918,13843488114570579517
