-- Seed: 5273297992794365391,10594830431004325987

entity gjaxvohp is
  port (mbkyn : buffer time; zkfut : linkage boolean; qao : in boolean_vector(2 to 0));
end gjaxvohp;

architecture e of gjaxvohp is
  
begin
  -- Single-driven assignments
  mbkyn <= 16#DE.6D1B3# ns;
end e;

library ieee;
use ieee.std_logic_1164.all;

entity lylqkbqal is
  port (vrunoikmfu : out time; fub : out std_logic_vector(3 to 0); xnp : out real);
end lylqkbqal;

architecture fgwahbakx of lylqkbqal is
  signal wur : boolean_vector(2 to 0);
  signal jygpp : boolean;
  signal lqd : boolean_vector(2 to 0);
  signal sela : boolean;
  signal jit : time;
begin
  isotfviiot : entity work.gjaxvohp
    port map (mbkyn => jit, zkfut => sela, qao => lqd);
  adusex : entity work.gjaxvohp
    port map (mbkyn => vrunoikmfu, zkfut => jygpp, qao => wur);
  
  -- Single-driven assignments
  xnp <= 16#54.D494#;
  
  -- Multi-driven assignments
  fub <= fub;
  fub <= "";
  fub <= fub;
end fgwahbakx;



-- Seed after: 4953279928003400146,10594830431004325987
