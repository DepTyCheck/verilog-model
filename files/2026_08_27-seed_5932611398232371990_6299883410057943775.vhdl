-- Seed: 5932611398232371990,6299883410057943775

library ieee;
use ieee.std_logic_1164.all;

entity ydmwc is
  port (pq : inout boolean; f : inout std_logic; fadirper : in integer_vector(2 to 0); pnywddgeq : linkage time);
end ydmwc;

architecture io of ydmwc is
  
begin
  -- Single-driven assignments
  pq <= TRUE;
  
  -- Multi-driven assignments
  f <= f;
  f <= f;
end io;

library ieee;
use ieee.std_logic_1164.all;

entity t is
  port (skuf : inout string(1 downto 4); gznhcejad : linkage std_logic; nmsrhhehq : buffer real);
end t;

library ieee;
use ieee.std_logic_1164.all;

architecture hybjnos of t is
  signal rh : time;
  signal gz : std_logic;
  signal iizpqhjo : boolean;
  signal qyjsbb : time;
  signal hnu : integer_vector(2 to 0);
  signal rulv : std_logic;
  signal qhpgkxwwcy : boolean;
  signal uihlxhwfp : time;
  signal dmiivq : std_logic;
  signal fkeliyy : boolean;
  signal zjzot : time;
  signal qpprmwobfo : integer_vector(2 to 0);
  signal tz : std_logic;
  signal ghwfll : boolean;
begin
  c : entity work.ydmwc
    port map (pq => ghwfll, f => tz, fadirper => qpprmwobfo, pnywddgeq => zjzot);
  gliya : entity work.ydmwc
    port map (pq => fkeliyy, f => dmiivq, fadirper => qpprmwobfo, pnywddgeq => uihlxhwfp);
  jk : entity work.ydmwc
    port map (pq => qhpgkxwwcy, f => rulv, fadirper => hnu, pnywddgeq => qyjsbb);
  i : entity work.ydmwc
    port map (pq => iizpqhjo, f => gz, fadirper => hnu, pnywddgeq => rh);
  
  -- Single-driven assignments
  nmsrhhehq <= 8#6_3_4.4_4_6_1_2#;
  qpprmwobfo <= qpprmwobfo;
  skuf <= (others => ' ');
  
  -- Multi-driven assignments
  rulv <= '1';
  tz <= tz;
end hybjnos;

entity uyhf is
  port (ql : out real);
end uyhf;

library ieee;
use ieee.std_logic_1164.all;

architecture kbwetcr of uyhf is
  signal lmakt : time;
  signal torvhrbhr : integer_vector(2 to 0);
  signal uwidj : std_logic;
  signal s : boolean;
  signal kmtixavqw : time;
  signal pvz : boolean;
  signal xpx : time;
  signal fqnod : integer_vector(2 to 0);
  signal bbg : std_logic;
  signal v : boolean;
begin
  ztufy : entity work.ydmwc
    port map (pq => v, f => bbg, fadirper => fqnod, pnywddgeq => xpx);
  wzfaql : entity work.ydmwc
    port map (pq => pvz, f => bbg, fadirper => fqnod, pnywddgeq => kmtixavqw);
  mqqkypkudu : entity work.ydmwc
    port map (pq => s, f => uwidj, fadirper => torvhrbhr, pnywddgeq => lmakt);
  
  -- Multi-driven assignments
  bbg <= 'W';
end kbwetcr;



-- Seed after: 4035399768720946146,6299883410057943775
