-- Seed: 6388524615867268007,6290177331721581829

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity yst is
  port (dhc : inout integer_value_mirror; x : linkage integer; rjrzwqu : inout std_logic_vector(0 downto 1));
end yst;

architecture wosbcint of yst is
  
begin
  -- Multi-driven assignments
  rjrzwqu <= rjrzwqu;
  rjrzwqu <= rjrzwqu;
  rjrzwqu <= (others => '0');
  rjrzwqu <= rjrzwqu;
end wosbcint;

use std.reflection.all;

entity xyuvc is
  port (x : in integer; xn : inout physical_value_mirror; lfuv : out time_vector(3 to 1));
end xyuvc;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture nxoovor of xyuvc is
  signal nwplrskytr : integer;
  shared variable rhikrvloxv : integer_value_mirror;
  signal xqgyrmh : integer;
  shared variable o : integer_value_mirror;
  signal z : integer;
  shared variable fhruktfdds : integer_value_mirror;
  signal mrylhg : std_logic_vector(0 downto 1);
  signal kfnu : integer;
  shared variable dhgr : integer_value_mirror;
begin
  q : entity work.yst
    port map (dhc => dhgr, x => kfnu, rjrzwqu => mrylhg);
  dwxfiygdt : entity work.yst
    port map (dhc => fhruktfdds, x => z, rjrzwqu => mrylhg);
  ilpx : entity work.yst
    port map (dhc => o, x => xqgyrmh, rjrzwqu => mrylhg);
  sxvml : entity work.yst
    port map (dhc => rhikrvloxv, x => nwplrskytr, rjrzwqu => mrylhg);
  
  -- Single-driven assignments
  lfuv <= (others => 0 ns);
  
  -- Multi-driven assignments
  mrylhg <= mrylhg;
end nxoovor;

library ieee;
use ieee.std_logic_1164.all;

entity ehltvmhke is
  port (o : in time; muhjexfy : out time; jxp : inout real; xtp : inout std_logic);
end ehltvmhke;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture jcgbyjpivd of ehltvmhke is
  signal t : std_logic_vector(0 downto 1);
  signal hlyjmt : integer;
  shared variable wu : integer_value_mirror;
  signal zrzspslmdn : integer;
  shared variable u : integer_value_mirror;
  signal bv : std_logic_vector(0 downto 1);
  signal weuy : integer;
  shared variable awkcgx : integer_value_mirror;
begin
  jddp : entity work.yst
    port map (dhc => awkcgx, x => weuy, rjrzwqu => bv);
  yinnjw : entity work.yst
    port map (dhc => u, x => zrzspslmdn, rjrzwqu => bv);
  iqahsvnbno : entity work.yst
    port map (dhc => wu, x => hlyjmt, rjrzwqu => t);
  
  -- Single-driven assignments
  muhjexfy <= 04421 us;
  jxp <= jxp;
  
  -- Multi-driven assignments
  xtp <= 'W';
  xtp <= xtp;
  xtp <= 'X';
end jcgbyjpivd;



-- Seed after: 12778993932819524344,6290177331721581829
