-- Seed: 8247130518227780095,13694093582652240945

library ieee;
use ieee.std_logic_1164.all;

entity lqqij is
  port (paou : linkage std_logic_vector(2 downto 3); tqgbhoho : buffer severity_level; afa : out character);
end lqqij;

architecture iohesi of lqqij is
  
begin
  -- Single-driven assignments
  tqgbhoho <= FAILURE;
  afa <= 'z';
end iohesi;

library ieee;
use ieee.std_logic_1164.all;

entity udpguttxug is
  port (bmycsxbr : out time; yh : linkage std_logic; nei : in time; mmz : out integer);
end udpguttxug;

library ieee;
use ieee.std_logic_1164.all;

architecture ghoqktp of udpguttxug is
  signal brypig : character;
  signal rhwe : severity_level;
  signal sst : character;
  signal pdcfkpsh : severity_level;
  signal zhrjvewps : std_logic_vector(2 downto 3);
begin
  tncnkx : entity work.lqqij
    port map (paou => zhrjvewps, tqgbhoho => pdcfkpsh, afa => sst);
  x : entity work.lqqij
    port map (paou => zhrjvewps, tqgbhoho => rhwe, afa => brypig);
  
  -- Single-driven assignments
  mmz <= 16#664#;
  bmycsxbr <= 2#0_1_1# ms;
end ghoqktp;

entity pfeagwwp is
  port (udibtqqcx : linkage string(3 downto 2));
end pfeagwwp;

library ieee;
use ieee.std_logic_1164.all;

architecture dskvgiqdfp of pfeagwwp is
  signal euett : integer;
  signal zmc : std_logic;
  signal nkoeschv : time;
  signal zrl : character;
  signal ufushojowh : severity_level;
  signal qaxtumghyh : character;
  signal bqaj : severity_level;
  signal pdpibx : std_logic_vector(2 downto 3);
  signal sxdc : character;
  signal nlxwsmv : severity_level;
  signal qfjil : std_logic_vector(2 downto 3);
begin
  q : entity work.lqqij
    port map (paou => qfjil, tqgbhoho => nlxwsmv, afa => sxdc);
  ubjgxa : entity work.lqqij
    port map (paou => pdpibx, tqgbhoho => bqaj, afa => qaxtumghyh);
  zhlkwar : entity work.lqqij
    port map (paou => qfjil, tqgbhoho => ufushojowh, afa => zrl);
  juddvpewmp : entity work.udpguttxug
    port map (bmycsxbr => nkoeschv, yh => zmc, nei => nkoeschv, mmz => euett);
end dskvgiqdfp;



-- Seed after: 17638760023872232059,13694093582652240945
