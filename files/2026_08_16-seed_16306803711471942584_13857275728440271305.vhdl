-- Seed: 16306803711471942584,13857275728440271305

library ieee;
use ieee.std_logic_1164.all;

entity muctu is
  port (zwwjh : linkage time; eppz : in std_logic_vector(3 to 2); clfmpmlg : inout real; nybgorb : linkage string(2 downto 4));
end muctu;

architecture kjw of muctu is
  
begin
  -- Single-driven assignments
  clfmpmlg <= clfmpmlg;
end kjw;

library ieee;
use ieee.std_logic_1164.all;

entity ieesvhxgrn is
  port (tpcqhhga : inout std_logic);
end ieesvhxgrn;

library ieee;
use ieee.std_logic_1164.all;

architecture irxjqoc of ieesvhxgrn is
  signal vlilj : string(2 downto 4);
  signal f : real;
  signal jci : std_logic_vector(3 to 2);
  signal xntjnex : time;
  signal abfkmgqm : string(2 downto 4);
  signal mmqbqzpe : real;
  signal mnwidycz : std_logic_vector(3 to 2);
  signal ptgdybyjw : time;
begin
  stmqlar : entity work.muctu
    port map (zwwjh => ptgdybyjw, eppz => mnwidycz, clfmpmlg => mmqbqzpe, nybgorb => abfkmgqm);
  smrddg : entity work.muctu
    port map (zwwjh => xntjnex, eppz => jci, clfmpmlg => f, nybgorb => vlilj);
  
  -- Multi-driven assignments
  tpcqhhga <= '1';
  tpcqhhga <= tpcqhhga;
  jci <= jci;
end irxjqoc;

entity hb is
  port (jowoigcnzn : linkage integer; jb : linkage time; qmfhhcukx : inout severity_level; jenhlmwenc : buffer character);
end hb;

library ieee;
use ieee.std_logic_1164.all;

architecture vgavfavzz of hb is
  signal oowfcaynqd : string(2 downto 4);
  signal dxnismdg : real;
  signal ixulaasmy : std_logic_vector(3 to 2);
  signal ient : time;
begin
  cyoxb : entity work.muctu
    port map (zwwjh => ient, eppz => ixulaasmy, clfmpmlg => dxnismdg, nybgorb => oowfcaynqd);
  
  -- Single-driven assignments
  jenhlmwenc <= jenhlmwenc;
  qmfhhcukx <= qmfhhcukx;
  
  -- Multi-driven assignments
  ixulaasmy <= ixulaasmy;
  ixulaasmy <= ixulaasmy;
  ixulaasmy <= ixulaasmy;
  ixulaasmy <= "";
end vgavfavzz;

entity fzvhknu is
  port (fiuxs : out bit; oe : in integer; e : out integer);
end fzvhknu;

library ieee;
use ieee.std_logic_1164.all;

architecture raewjf of fzvhknu is
  signal nggoyl : string(2 downto 4);
  signal xbf : real;
  signal pcm : std_logic_vector(3 to 2);
  signal zqm : time;
begin
  glepby : entity work.muctu
    port map (zwwjh => zqm, eppz => pcm, clfmpmlg => xbf, nybgorb => nggoyl);
  
  -- Single-driven assignments
  e <= 2#1_1_1_1_0#;
  fiuxs <= '1';
  
  -- Multi-driven assignments
  pcm <= "";
  pcm <= pcm;
  pcm <= pcm;
  pcm <= pcm;
end raewjf;



-- Seed after: 4877153851937227435,13857275728440271305
