-- Seed: 5036242937420436325,17234720251424330329

library ieee;
use ieee.std_logic_1164.all;

entity s is
  port (ma : linkage std_logic_vector(1 downto 3); c : in time);
end s;

architecture pzaccya of s is
  
begin
  
end pzaccya;

library ieee;
use ieee.std_logic_1164.all;

entity fsotigktla is
  port (run : out integer; pvm : inout time; qf : buffer std_logic_vector(4 downto 1); fvtv : linkage std_logic_vector(3 downto 4));
end fsotigktla;

library ieee;
use ieee.std_logic_1164.all;

architecture lu of fsotigktla is
  signal zrnkeh : time;
  signal ejkao : std_logic_vector(1 downto 3);
  signal jxn : std_logic_vector(1 downto 3);
begin
  oswrkkd : entity work.s
    port map (ma => fvtv, c => pvm);
  klhjmv : entity work.s
    port map (ma => jxn, c => pvm);
  tspaju : entity work.s
    port map (ma => ejkao, c => zrnkeh);
  iz : entity work.s
    port map (ma => jxn, c => pvm);
  
  -- Single-driven assignments
  pvm <= 8#3# ns;
  
  -- Multi-driven assignments
  qf <= qf;
  qf <= qf;
  jxn <= "";
end lu;

entity nsgoclhdnt is
  port (axb : in boolean_vector(2 downto 4));
end nsgoclhdnt;

library ieee;
use ieee.std_logic_1164.all;

architecture gpkwooipmy of nsgoclhdnt is
  signal avrd : std_logic_vector(4 downto 1);
  signal ktsifrcmu : integer;
  signal id : time;
  signal xpquwwnvsj : std_logic_vector(3 downto 4);
begin
  vxubykbzh : entity work.s
    port map (ma => xpquwwnvsj, c => id);
  omyjffo : entity work.fsotigktla
    port map (run => ktsifrcmu, pvm => id, qf => avrd, fvtv => xpquwwnvsj);
  
  -- Multi-driven assignments
  avrd <= "WZWW";
end gpkwooipmy;



-- Seed after: 14708733896163713090,17234720251424330329
