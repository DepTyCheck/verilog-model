-- Seed: 5147392442036241611,7808623373429384027

entity qlmh is
  port (h : out boolean; hznxpmbhnq : linkage integer; l : out time);
end qlmh;

architecture nfsbwypjw of qlmh is
  
begin
  -- Single-driven assignments
  h <= h;
  l <= 2#1.0011# us;
end nfsbwypjw;

entity vhj is
  port (fb : buffer real; qqskqgn : linkage time);
end vhj;

architecture aplynu of vhj is
  signal v : time;
  signal b : integer;
  signal plbkyilfgd : boolean;
  signal was : time;
  signal smcsze : integer;
  signal lkl : boolean;
  signal e : time;
  signal ketu : integer;
  signal fpndacy : boolean;
begin
  ctkk : entity work.qlmh
    port map (h => fpndacy, hznxpmbhnq => ketu, l => e);
  kfkysawwvs : entity work.qlmh
    port map (h => lkl, hznxpmbhnq => smcsze, l => was);
  rsjhtfe : entity work.qlmh
    port map (h => plbkyilfgd, hznxpmbhnq => b, l => v);
  
  -- Single-driven assignments
  fb <= fb;
end aplynu;

library ieee;
use ieee.std_logic_1164.all;

entity xf is
  port (frgbeaqrpi : in std_logic);
end xf;

architecture ylfeb of xf is
  signal oee : time;
  signal yawrlsne : integer;
  signal ylkxt : boolean;
  signal hwioym : time;
  signal vohlme : integer;
  signal eqknovpxen : boolean;
begin
  owwdc : entity work.qlmh
    port map (h => eqknovpxen, hznxpmbhnq => vohlme, l => hwioym);
  exdyfj : entity work.qlmh
    port map (h => ylkxt, hznxpmbhnq => yawrlsne, l => oee);
end ylfeb;

library ieee;
use ieee.std_logic_1164.all;

entity rngxclvh is
  port (pkfanssm : linkage bit_vector(1 to 1); ibkstpbrmw : out string(2 downto 1); eyqvh : in std_logic; lgnfbivxgh : in bit);
end rngxclvh;

architecture i of rngxclvh is
  signal nj : time;
  signal qiuyckoph : integer;
  signal kttbx : boolean;
  signal quadzqlhu : time;
  signal lclnclk : integer;
  signal ebiix : boolean;
  signal zcmjscg : time;
  signal wfptbxikw : integer;
  signal oglyj : boolean;
begin
  las : entity work.xf
    port map (frgbeaqrpi => eyqvh);
  aicwsdmw : entity work.qlmh
    port map (h => oglyj, hznxpmbhnq => wfptbxikw, l => zcmjscg);
  v : entity work.qlmh
    port map (h => ebiix, hznxpmbhnq => lclnclk, l => quadzqlhu);
  ny : entity work.qlmh
    port map (h => kttbx, hznxpmbhnq => qiuyckoph, l => nj);
  
  -- Single-driven assignments
  ibkstpbrmw <= ('a', 'h');
end i;



-- Seed after: 14339772310141300708,7808623373429384027
