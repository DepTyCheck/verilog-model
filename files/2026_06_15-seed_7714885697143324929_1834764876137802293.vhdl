-- Seed: 7714885697143324929,1834764876137802293

entity bzodw is
  port (emf : in character; ev : linkage real_vector(2 to 2));
end bzodw;

architecture jaqyaaunw of bzodw is
  
begin
  
end jaqyaaunw;

entity xlb is
  port (bqendzova : out bit; ob : in time; t : out integer);
end xlb;

architecture kekozfdx of xlb is
  signal m : real_vector(2 to 2);
  signal k : character;
begin
  ycd : entity work.bzodw
    port map (emf => k, ev => m);
  
  -- Single-driven assignments
  t <= 2241;
  k <= 'j';
  bqendzova <= '1';
end kekozfdx;

entity lzqysynip is
  port (rkv : linkage integer; ntdoddcg : inout character; yew : inout real_vector(0 downto 2));
end lzqysynip;

architecture dfb of lzqysynip is
  signal pobb : real_vector(2 to 2);
  signal aggutihywc : character;
  signal hlkrowtbz : integer;
  signal zrin : time;
  signal zbt : bit;
begin
  ajdmk : entity work.xlb
    port map (bqendzova => zbt, ob => zrin, t => hlkrowtbz);
  pqy : entity work.bzodw
    port map (emf => aggutihywc, ev => pobb);
  
  -- Single-driven assignments
  yew <= (others => 0.0);
  zrin <= 22.4311 ns;
  ntdoddcg <= 'b';
end dfb;



-- Seed after: 9126250715840520688,1834764876137802293
