-- Seed: 15074948721464882767,8927267689619684183

library ieee;
use ieee.std_logic_1164.all;

entity l is
  port (hmmgappu : out std_logic; mxhe : in std_logic_vector(4 to 4); ymijloja : out string(3 to 2); rrmspcxg : in integer_vector(3 to 1));
end l;

architecture wvetuyljd of l is
  
begin
  -- Multi-driven assignments
  hmmgappu <= hmmgappu;
  hmmgappu <= hmmgappu;
  hmmgappu <= 'Z';
end wvetuyljd;

entity xjgleuzay is
  port (jrw : inout severity_level; sxl : out integer_vector(3 to 2); v : buffer real);
end xjgleuzay;

library ieee;
use ieee.std_logic_1164.all;

architecture pmdqoopc of xjgleuzay is
  signal aqwkvzi : string(3 to 2);
  signal njnltvr : std_logic;
  signal lenmysyew : string(3 to 2);
  signal eobf : std_logic;
  signal cnwoura : integer_vector(3 to 1);
  signal dfzhrxfk : string(3 to 2);
  signal tbu : integer_vector(3 to 1);
  signal zzubl : string(3 to 2);
  signal msakqdnkk : std_logic_vector(4 to 4);
  signal dgbqqpdx : std_logic;
begin
  jizbxg : entity work.l
    port map (hmmgappu => dgbqqpdx, mxhe => msakqdnkk, ymijloja => zzubl, rrmspcxg => tbu);
  a : entity work.l
    port map (hmmgappu => dgbqqpdx, mxhe => msakqdnkk, ymijloja => dfzhrxfk, rrmspcxg => cnwoura);
  jnc : entity work.l
    port map (hmmgappu => eobf, mxhe => msakqdnkk, ymijloja => lenmysyew, rrmspcxg => cnwoura);
  rlqyncm : entity work.l
    port map (hmmgappu => njnltvr, mxhe => msakqdnkk, ymijloja => aqwkvzi, rrmspcxg => sxl);
  
  -- Multi-driven assignments
  dgbqqpdx <= dgbqqpdx;
  eobf <= 'H';
end pmdqoopc;



-- Seed after: 10822385713574112243,8927267689619684183
