-- Seed: 10236605527884396919,1834764876137802293

library ieee;
use ieee.std_logic_1164.all;

entity fufib is
  port (pfgunciff : in std_logic_vector(0 to 0); vu : buffer bit; xdtfv : buffer string(1 to 5); og : linkage time);
end fufib;

architecture mx of fufib is
  
begin
  -- Single-driven assignments
  xdtfv <= ('p', 's', 'i', 'e', 'c');
  vu <= '1';
end mx;

library ieee;
use ieee.std_logic_1164.all;

entity nwkw is
  port (tadzpess : out integer; nczrh : linkage std_logic; zhdv : inout real; usnsgqcjx : out integer);
end nwkw;

library ieee;
use ieee.std_logic_1164.all;

architecture gvift of nwkw is
  signal lsxihi : time;
  signal ogrfsgf : string(1 to 5);
  signal wyfec : bit;
  signal nq : time;
  signal kbzizjz : string(1 to 5);
  signal lzphfi : bit;
  signal y : time;
  signal glzri : string(1 to 5);
  signal ck : bit;
  signal qydqoa : time;
  signal mcyvxtblr : string(1 to 5);
  signal xymc : bit;
  signal dyr : std_logic_vector(0 to 0);
begin
  xlitnynmk : entity work.fufib
    port map (pfgunciff => dyr, vu => xymc, xdtfv => mcyvxtblr, og => qydqoa);
  u : entity work.fufib
    port map (pfgunciff => dyr, vu => ck, xdtfv => glzri, og => y);
  qklzif : entity work.fufib
    port map (pfgunciff => dyr, vu => lzphfi, xdtfv => kbzizjz, og => nq);
  yusgif : entity work.fufib
    port map (pfgunciff => dyr, vu => wyfec, xdtfv => ogrfsgf, og => lsxihi);
  
  -- Single-driven assignments
  zhdv <= 8#20723.5#;
  usnsgqcjx <= 44320;
  tadzpess <= 4_4;
  
  -- Multi-driven assignments
  dyr <= (others => 'Z');
  dyr <= (others => 'Z');
  dyr <= (others => 'X');
  dyr <= "H";
end gvift;

library ieee;
use ieee.std_logic_1164.all;

entity ba is
  port (qlwpx : out std_logic_vector(3 downto 0));
end ba;

library ieee;
use ieee.std_logic_1164.all;

architecture vwavrajia of ba is
  signal a : time;
  signal iucmeglqfw : string(1 to 5);
  signal zxrrlrwgt : bit;
  signal mxx : std_logic_vector(0 to 0);
  signal yxn : time;
  signal n : string(1 to 5);
  signal sin : bit;
  signal z : std_logic_vector(0 to 0);
begin
  swqhyyceqx : entity work.fufib
    port map (pfgunciff => z, vu => sin, xdtfv => n, og => yxn);
  tjqfturfy : entity work.fufib
    port map (pfgunciff => mxx, vu => zxrrlrwgt, xdtfv => iucmeglqfw, og => a);
  
  -- Multi-driven assignments
  z <= "0";
  qlwpx <= ('L', '-', 'W', 'H');
end vwavrajia;



-- Seed after: 12741022178237451428,1834764876137802293
