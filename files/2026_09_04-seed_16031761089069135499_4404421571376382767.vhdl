-- Seed: 16031761089069135499,4404421571376382767

entity mxd is
  port (gdws : in severity_level; vbbjeo : inout real; vi : buffer time);
end mxd;

architecture nq of mxd is
  
begin
  -- Single-driven assignments
  vi <= 3_3_2_0_3.10142 ms;
  vbbjeo <= vbbjeo;
end nq;

library ieee;
use ieee.std_logic_1164.all;

entity kh is
  port (xfambpcda : inout time; yypkhti : buffer std_logic_vector(1 to 4); iskfqkmp : linkage character; sfqhe : buffer integer);
end kh;

architecture rszgk of kh is
  signal byukmvxnqi : real;
  signal rsxbewa : severity_level;
  signal zb : time;
  signal mykvqgcbo : real;
  signal ko : severity_level;
  signal ryuumbrca : time;
  signal lqkds : real;
  signal rq : time;
  signal rvbsp : real;
  signal orje : severity_level;
begin
  wkicvduw : entity work.mxd
    port map (gdws => orje, vbbjeo => rvbsp, vi => rq);
  ioqrpowgv : entity work.mxd
    port map (gdws => orje, vbbjeo => lqkds, vi => ryuumbrca);
  bnilh : entity work.mxd
    port map (gdws => ko, vbbjeo => mykvqgcbo, vi => zb);
  n : entity work.mxd
    port map (gdws => rsxbewa, vbbjeo => byukmvxnqi, vi => xfambpcda);
  
  -- Single-driven assignments
  sfqhe <= 20;
  orje <= orje;
  
  -- Multi-driven assignments
  yypkhti <= yypkhti;
  yypkhti <= yypkhti;
  yypkhti <= "-0Z-";
  yypkhti <= ('0', '1', 'W', '0');
end rszgk;

library ieee;
use ieee.std_logic_1164.all;

entity hissta is
  port (hfiqmbowpa : inout std_logic; gridormb : out integer);
end hissta;

library ieee;
use ieee.std_logic_1164.all;

architecture lw of hissta is
  signal wybialqwlw : time;
  signal xczk : real;
  signal m : severity_level;
  signal wqra : character;
  signal r : std_logic_vector(1 to 4);
  signal veniqtpwwa : time;
begin
  rhsdkwqsr : entity work.kh
    port map (xfambpcda => veniqtpwwa, yypkhti => r, iskfqkmp => wqra, sfqhe => gridormb);
  rdtey : entity work.mxd
    port map (gdws => m, vbbjeo => xczk, vi => wybialqwlw);
  
  -- Single-driven assignments
  m <= m;
  
  -- Multi-driven assignments
  hfiqmbowpa <= 'Z';
  hfiqmbowpa <= hfiqmbowpa;
  hfiqmbowpa <= 'W';
end lw;



-- Seed after: 5531471655741457626,4404421571376382767
