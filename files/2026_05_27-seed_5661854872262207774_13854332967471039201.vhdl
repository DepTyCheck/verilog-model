-- Seed: 5661854872262207774,13854332967471039201



entity j is
  port (gafopvc : inout real; zy : buffer real; berjpitf : out time);
end j;



architecture iprqb of j is
  
begin
  
end iprqb;

library ieee;
use ieee.std_logic_1164.all;

entity hfytapu is
  port (ndx : buffer std_logic_vector(3 to 2); mfdsxsql : buffer std_logic);
end hfytapu;



architecture gaf of hfytapu is
  signal m : time;
  signal o : real;
  signal g : real;
  signal ooppx : time;
  signal zopndvmntz : real;
  signal azhiega : real;
  signal nknrhnclp : time;
  signal h : real;
  signal uyvvid : real;
begin
  qrpyrrdqgk : entity work.j
    port map (gafopvc => uyvvid, zy => h, berjpitf => nknrhnclp);
  x : entity work.j
    port map (gafopvc => azhiega, zy => zopndvmntz, berjpitf => ooppx);
  sopolfvm : entity work.j
    port map (gafopvc => g, zy => o, berjpitf => m);
end gaf;



-- Seed after: 5967641031461381026,13854332967471039201
