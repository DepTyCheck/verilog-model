-- Seed: 15826263938987279095,13501862637168280927

library ieee;
use ieee.std_logic_1164.all;

entity vhffupbsz is
  port (r : out real; u : out real; klstc : in severity_level; yzi : in std_logic);
end vhffupbsz;

architecture l of vhffupbsz is
  
begin
  -- Single-driven assignments
  u <= u;
  r <= 23.3_1_4_1;
end l;

entity yqbnshpkg is
  port (za : out real; jdikdzsl : inout time_vector(0 downto 2); kiqgwba : inout real; ylcacymqzp : buffer integer);
end yqbnshpkg;

library ieee;
use ieee.std_logic_1164.all;

architecture dygcrxc of yqbnshpkg is
  signal unxsh : std_logic;
  signal sgnmdbdlj : real;
  signal m : std_logic;
  signal xi : severity_level;
  signal alaalnk : real;
begin
  zw : entity work.vhffupbsz
    port map (r => kiqgwba, u => alaalnk, klstc => xi, yzi => m);
  fsnfeha : entity work.vhffupbsz
    port map (r => za, u => sgnmdbdlj, klstc => xi, yzi => unxsh);
  
  -- Multi-driven assignments
  m <= m;
  unxsh <= m;
  m <= m;
  unxsh <= '1';
end dygcrxc;

library ieee;
use ieee.std_logic_1164.all;

entity qkmuhetkee is
  port (b : in integer_vector(1 to 0); mseao : out std_logic; us : in std_logic_vector(1 downto 2));
end qkmuhetkee;

library ieee;
use ieee.std_logic_1164.all;

architecture mauzqrsw of qkmuhetkee is
  signal w : std_logic;
  signal mlacy : severity_level;
  signal ltgoqxg : real;
  signal i : real;
  signal mrkb : std_logic;
  signal ctp : severity_level;
  signal wmjt : real;
  signal pbubkwer : real;
  signal evghextz : severity_level;
  signal mhpnrg : real;
  signal vpejotitz : real;
  signal yatbuseyqp : severity_level;
  signal sodusaed : real;
  signal fqy : real;
begin
  cp : entity work.vhffupbsz
    port map (r => fqy, u => sodusaed, klstc => yatbuseyqp, yzi => mseao);
  mfmpu : entity work.vhffupbsz
    port map (r => vpejotitz, u => mhpnrg, klstc => evghextz, yzi => mseao);
  awj : entity work.vhffupbsz
    port map (r => pbubkwer, u => wmjt, klstc => ctp, yzi => mrkb);
  de : entity work.vhffupbsz
    port map (r => i, u => ltgoqxg, klstc => mlacy, yzi => w);
  
  -- Single-driven assignments
  yatbuseyqp <= WARNING;
  mlacy <= ERROR;
  ctp <= yatbuseyqp;
  evghextz <= yatbuseyqp;
  
  -- Multi-driven assignments
  mseao <= mseao;
  mseao <= mseao;
end mauzqrsw;



-- Seed after: 17532814963592496040,13501862637168280927
