-- Seed: 7108834995750599124,16188444798499499427

library ieee;
use ieee.std_logic_1164.all;

entity inokoin is
  port (cysichltcs : in std_logic_vector(1 to 4); ckulqu : buffer std_logic; s : linkage real_vector(4 to 4); gija : buffer integer);
end inokoin;

architecture muperw of inokoin is
  
begin
  -- Single-driven assignments
  gija <= 16#3#;
  
  -- Multi-driven assignments
  ckulqu <= ckulqu;
  ckulqu <= ckulqu;
  ckulqu <= ckulqu;
end muperw;

entity luyofu is
  port (nvws : in time);
end luyofu;

library ieee;
use ieee.std_logic_1164.all;

architecture yq of luyofu is
  signal ylg : integer;
  signal iij : real_vector(4 to 4);
  signal qqnwz : std_logic;
  signal myng : std_logic_vector(1 to 4);
  signal omsgpc : integer;
  signal bsr : real_vector(4 to 4);
  signal ydxazwq : integer;
  signal bu : real_vector(4 to 4);
  signal cp : std_logic;
  signal eceqb : std_logic_vector(1 to 4);
begin
  cqaiggmpp : entity work.inokoin
    port map (cysichltcs => eceqb, ckulqu => cp, s => bu, gija => ydxazwq);
  sa : entity work.inokoin
    port map (cysichltcs => eceqb, ckulqu => cp, s => bsr, gija => omsgpc);
  gupbnh : entity work.inokoin
    port map (cysichltcs => myng, ckulqu => qqnwz, s => iij, gija => ylg);
  
  -- Multi-driven assignments
  cp <= cp;
  myng <= "UU0Z";
end yq;

entity nvgpvfn is
  port (xzulvvg : linkage integer; y : linkage time);
end nvgpvfn;

library ieee;
use ieee.std_logic_1164.all;

architecture jvkp of nvgpvfn is
  signal umwbvxasu : integer;
  signal afpknaa : real_vector(4 to 4);
  signal mkhomgplf : std_logic;
  signal canjsfcuri : integer;
  signal yazmorldwf : real_vector(4 to 4);
  signal ymvswpidl : std_logic;
  signal cucpzng : integer;
  signal hc : real_vector(4 to 4);
  signal pdfe : std_logic;
  signal rsqdn : std_logic_vector(1 to 4);
begin
  fiubraj : entity work.inokoin
    port map (cysichltcs => rsqdn, ckulqu => pdfe, s => hc, gija => cucpzng);
  qqzpibgo : entity work.inokoin
    port map (cysichltcs => rsqdn, ckulqu => ymvswpidl, s => yazmorldwf, gija => canjsfcuri);
  ht : entity work.inokoin
    port map (cysichltcs => rsqdn, ckulqu => mkhomgplf, s => afpknaa, gija => umwbvxasu);
  
  -- Multi-driven assignments
  rsqdn <= ('-', '1', 'Z', 'X');
  ymvswpidl <= pdfe;
end jvkp;

entity m is
  port (qx : inout severity_level; uycxjolp : in boolean);
end m;

architecture uedapzx of m is
  signal tbd : time;
begin
  whm : entity work.luyofu
    port map (nvws => tbd);
  
  -- Single-driven assignments
  qx <= qx;
  tbd <= 0_0_3_2_0 ns;
end uedapzx;



-- Seed after: 9467727371702519325,16188444798499499427
