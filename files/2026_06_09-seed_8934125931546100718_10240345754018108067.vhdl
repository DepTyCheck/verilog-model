-- Seed: 8934125931546100718,10240345754018108067

library ieee;
use ieee.std_logic_1164.all;

entity pqzewd is
  port (kt : in bit_vector(1 to 0); betwibwaab : linkage real; ndoaa : buffer std_logic; vye : buffer integer_vector(0 to 3));
end pqzewd;



architecture lvbgh of pqzewd is
  
begin
  
end lvbgh;

library ieee;
use ieee.std_logic_1164.all;

entity zkvjbcdhs is
  port (lagildu : inout bit; eobghbvyc : buffer std_logic_vector(2 downto 2));
end zkvjbcdhs;

library ieee;
use ieee.std_logic_1164.all;

architecture gpcyjqy of zkvjbcdhs is
  signal hawulwek : integer_vector(0 to 3);
  signal ggmbvpep : std_logic;
  signal hfvjqmna : real;
  signal ssozse : bit_vector(1 to 0);
  signal usux : integer_vector(0 to 3);
  signal zmsvzki : std_logic;
  signal qlm : real;
  signal rxrwpueamb : bit_vector(1 to 0);
  signal iac : integer_vector(0 to 3);
  signal ryjr : std_logic;
  signal visigtwrd : real;
  signal kuebd : bit_vector(1 to 0);
begin
  wgk : entity work.pqzewd
    port map (kt => kuebd, betwibwaab => visigtwrd, ndoaa => ryjr, vye => iac);
  ohhon : entity work.pqzewd
    port map (kt => rxrwpueamb, betwibwaab => qlm, ndoaa => zmsvzki, vye => usux);
  ktcvokcmdc : entity work.pqzewd
    port map (kt => ssozse, betwibwaab => hfvjqmna, ndoaa => ggmbvpep, vye => hawulwek);
end gpcyjqy;

library ieee;
use ieee.std_logic_1164.all;

entity hbiw is
  port (i : buffer std_logic; jhuyn : in time; pf : buffer time);
end hbiw;

library ieee;
use ieee.std_logic_1164.all;

architecture chn of hbiw is
  signal l : integer_vector(0 to 3);
  signal duczxux : std_logic_vector(2 downto 2);
  signal glquhw : bit;
  signal trud : integer_vector(0 to 3);
  signal h : std_logic;
  signal uro : real;
  signal qap : bit_vector(1 to 0);
begin
  gu : entity work.pqzewd
    port map (kt => qap, betwibwaab => uro, ndoaa => h, vye => trud);
  jbnq : entity work.zkvjbcdhs
    port map (lagildu => glquhw, eobghbvyc => duczxux);
  tavvxid : entity work.pqzewd
    port map (kt => qap, betwibwaab => uro, ndoaa => h, vye => l);
end chn;

library ieee;
use ieee.std_logic_1164.all;

entity kf is
  port (oixdoad : in std_logic_vector(3 downto 4); xgufa : inout std_logic; k : buffer std_logic_vector(3 downto 4); cyvdy : linkage time);
end kf;

library ieee;
use ieee.std_logic_1164.all;

architecture ek of kf is
  signal pwgfonxu : time;
  signal bqmbcb : time;
  signal e : std_logic;
begin
  w : entity work.hbiw
    port map (i => e, jhuyn => bqmbcb, pf => pwgfonxu);
end ek;



-- Seed after: 16096104673035259426,10240345754018108067
