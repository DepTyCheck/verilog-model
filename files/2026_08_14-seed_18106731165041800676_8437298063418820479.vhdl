-- Seed: 18106731165041800676,8437298063418820479

library ieee;
use ieee.std_logic_1164.all;

entity ljju is
  port (xxgpt : buffer std_logic; skx : inout integer; dgmdokph : linkage std_logic; uk : buffer std_logic);
end ljju;

architecture ct of ljju is
  
begin
  -- Single-driven assignments
  skx <= 2#1#;
  
  -- Multi-driven assignments
  uk <= uk;
  uk <= 'X';
  xxgpt <= uk;
  uk <= '-';
end ct;

library ieee;
use ieee.std_logic_1164.all;

entity w is
  port ( bnbzmilc : in std_logic_vector(2 to 1)
  ; vkwkjltr : out std_logic_vector(1 to 2)
  ; anti : inout time
  ; jayhppszvi : out integer_vector(1 downto 2)
  );
end w;

library ieee;
use ieee.std_logic_1164.all;

architecture ozlqq of w is
  signal e : std_logic;
  signal eaw : std_logic;
  signal ruisfkod : integer;
  signal btdguqdvn : std_logic;
  signal hpxqrx : std_logic;
  signal spteefdbv : integer;
  signal xgffrwy : std_logic;
  signal blx : integer;
  signal hipoq : std_logic;
begin
  ofu : entity work.ljju
    port map (xxgpt => hipoq, skx => blx, dgmdokph => hipoq, uk => xgffrwy);
  qq : entity work.ljju
    port map (xxgpt => hipoq, skx => spteefdbv, dgmdokph => hpxqrx, uk => btdguqdvn);
  xtq : entity work.ljju
    port map (xxgpt => hipoq, skx => ruisfkod, dgmdokph => eaw, uk => e);
  
  -- Single-driven assignments
  anti <= anti;
  jayhppszvi <= jayhppszvi;
end ozlqq;

entity fabqf is
  port (ybeobwntsp : inout real; meppxvhz : inout integer; augjbdite : out string(1 to 2); skiu : inout integer_vector(4 to 3));
end fabqf;

library ieee;
use ieee.std_logic_1164.all;

architecture vfzelcayh of fabqf is
  signal pvbvfi : std_logic;
  signal v : integer;
  signal jlmzcdz : std_logic;
begin
  wwpf : entity work.ljju
    port map (xxgpt => jlmzcdz, skx => v, dgmdokph => jlmzcdz, uk => pvbvfi);
  
  -- Single-driven assignments
  augjbdite <= ('v', 'q');
  meppxvhz <= 16#4#;
  skiu <= (others => 0);
  
  -- Multi-driven assignments
  jlmzcdz <= 'L';
  jlmzcdz <= '0';
end vfzelcayh;

entity lczwyn is
  port (yfoxhg : linkage boolean; ipf : linkage integer; ikfrdb : linkage bit);
end lczwyn;

library ieee;
use ieee.std_logic_1164.all;

architecture zhfek of lczwyn is
  signal chemhaw : integer_vector(1 downto 2);
  signal xrkme : time;
  signal rgb : std_logic_vector(1 to 2);
  signal iljj : std_logic_vector(2 to 1);
  signal lajpmckrp : integer_vector(1 downto 2);
  signal iqw : time;
  signal md : std_logic_vector(1 to 2);
  signal falqdriuey : std_logic_vector(2 to 1);
  signal eujtvm : std_logic;
  signal d : std_logic;
  signal taiisfeg : integer;
  signal tl : std_logic;
begin
  hodm : entity work.ljju
    port map (xxgpt => tl, skx => taiisfeg, dgmdokph => d, uk => eujtvm);
  zctcmhejo : entity work.w
    port map (bnbzmilc => falqdriuey, vkwkjltr => md, anti => iqw, jayhppszvi => lajpmckrp);
  bvmvotiw : entity work.w
    port map (bnbzmilc => iljj, vkwkjltr => rgb, anti => xrkme, jayhppszvi => chemhaw);
  
  -- Multi-driven assignments
  iljj <= "";
  tl <= 'W';
  tl <= 'W';
  tl <= 'W';
end zhfek;



-- Seed after: 1555620333406290626,8437298063418820479
