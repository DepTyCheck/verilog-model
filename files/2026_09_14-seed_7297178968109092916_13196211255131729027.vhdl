-- Seed: 7297178968109092916,13196211255131729027

entity ncbaws is
  port (ltpvlwr : in boolean; fzyh : buffer integer; mhneli : buffer time);
end ncbaws;

architecture fkfaandq of ncbaws is
  
begin
  -- Single-driven assignments
  mhneli <= 1 sec;
end fkfaandq;

entity whpmv is
  port (xcgbki : in real; xryz : linkage time_vector(3 downto 3); xtqid : linkage time; aoiexotm : in severity_level);
end whpmv;

architecture r of whpmv is
  signal kavg : time;
  signal shzlcb : integer;
  signal bbjgyo : time;
  signal fiafgp : integer;
  signal i : boolean;
  signal l : time;
  signal ibtgwr : integer;
  signal lzxudpjv : time;
  signal ximzz : integer;
  signal rv : boolean;
begin
  jhyrgnfcnl : entity work.ncbaws
    port map (ltpvlwr => rv, fzyh => ximzz, mhneli => lzxudpjv);
  sdss : entity work.ncbaws
    port map (ltpvlwr => rv, fzyh => ibtgwr, mhneli => l);
  jljeyjd : entity work.ncbaws
    port map (ltpvlwr => i, fzyh => fiafgp, mhneli => bbjgyo);
  iixymjd : entity work.ncbaws
    port map (ltpvlwr => rv, fzyh => shzlcb, mhneli => kavg);
  
  -- Single-driven assignments
  rv <= TRUE;
  i <= TRUE;
end r;



-- Seed after: 3771684964110955045,13196211255131729027
