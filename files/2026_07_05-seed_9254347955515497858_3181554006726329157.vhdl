-- Seed: 9254347955515497858,3181554006726329157

use std.reflection.all;

entity woq is
  port (aoizvnv : inout access_value_mirror; poz : inout boolean; xcgcxg : inout real_vector(0 downto 0));
end woq;

architecture szhfsys of woq is
  
begin
  -- Single-driven assignments
  xcgcxg <= xcgcxg;
end szhfsys;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

entity yl is
  port (ausnaz : inout std_logic; bxhsqm : inout floating_value_mirror);
end yl;

use std.reflection.all;

architecture pjlq of yl is
  signal qocwr : real_vector(0 downto 0);
  signal dit : boolean;
  shared variable tbwggjj : access_value_mirror;
  signal epn : real_vector(0 downto 0);
  signal ftmyvrhb : boolean;
  shared variable myzjgwqmta : access_value_mirror;
  signal cll : real_vector(0 downto 0);
  signal pfwsua : boolean;
  shared variable lwwxlitsg : access_value_mirror;
  signal ojlwlrzjw : real_vector(0 downto 0);
  signal tg : boolean;
  shared variable ibintve : access_value_mirror;
begin
  pbpbatzqkn : entity work.woq
    port map (aoizvnv => ibintve, poz => tg, xcgcxg => ojlwlrzjw);
  lemeatx : entity work.woq
    port map (aoizvnv => lwwxlitsg, poz => pfwsua, xcgcxg => cll);
  kufjfgvdis : entity work.woq
    port map (aoizvnv => myzjgwqmta, poz => ftmyvrhb, xcgcxg => epn);
  b : entity work.woq
    port map (aoizvnv => tbwggjj, poz => dit, xcgcxg => qocwr);
  
  -- Multi-driven assignments
  ausnaz <= ausnaz;
  ausnaz <= ausnaz;
  ausnaz <= ausnaz;
  ausnaz <= ausnaz;
end pjlq;

use std.reflection.all;

entity focysnesjk is
  port (px : inout record_subtype_mirror; iftm : out integer);
end focysnesjk;

library ieee;
use ieee.std_logic_1164.all;
use std.reflection.all;

architecture trjuooq of focysnesjk is
  shared variable liqw : floating_value_mirror;
  signal nollzxziaf : std_logic;
  signal pwxr : real_vector(0 downto 0);
  signal xmm : boolean;
  shared variable tattnwamli : access_value_mirror;
  signal vrwwduughp : real_vector(0 downto 0);
  signal qsqtqz : boolean;
  shared variable jhzcirux : access_value_mirror;
  signal tcf : real_vector(0 downto 0);
  signal vehtpr : boolean;
  shared variable gxvfaquu : access_value_mirror;
begin
  u : entity work.woq
    port map (aoizvnv => gxvfaquu, poz => vehtpr, xcgcxg => tcf);
  mdnaknyecn : entity work.woq
    port map (aoizvnv => jhzcirux, poz => qsqtqz, xcgcxg => vrwwduughp);
  xbemckbje : entity work.woq
    port map (aoizvnv => tattnwamli, poz => xmm, xcgcxg => pwxr);
  cxh : entity work.yl
    port map (ausnaz => nollzxziaf, bxhsqm => liqw);
  
  -- Single-driven assignments
  iftm <= iftm;
end trjuooq;



-- Seed after: 17608039152748519024,3181554006726329157
