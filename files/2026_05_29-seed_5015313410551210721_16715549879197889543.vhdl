-- Seed: 5015313410551210721,16715549879197889543

library ieee;
use ieee.std_logic_1164.all;

entity hwelay is
  port (svuggy : linkage std_logic_vector(3 to 0); ozlz : inout std_logic_vector(3 to 1));
end hwelay;



architecture zw of hwelay is
  
begin
  
end zw;

library ieee;
use ieee.std_logic_1164.all;

entity uya is
  port (byjdoccjhf : in real; hav : in std_logic);
end uya;

library ieee;
use ieee.std_logic_1164.all;

architecture ilxouigasx of uya is
  signal trbjiwhv : std_logic_vector(3 to 1);
begin
  sg : entity work.hwelay
    port map (svuggy => trbjiwhv, ozlz => trbjiwhv);
end ilxouigasx;

library ieee;
use ieee.std_logic_1164.all;

entity wrevkasqma is
  port (dpmqfxlx : in std_logic_vector(4 downto 0); lkmiyzqr : out time_vector(4 to 0); goe : inout bit; eokjqbbr : buffer real);
end wrevkasqma;

library ieee;
use ieee.std_logic_1164.all;

architecture hwuzkwe of wrevkasqma is
  signal bzlk : std_logic;
  signal khul : real;
  signal itc : std_logic_vector(3 to 1);
  signal u : std_logic;
begin
  tgrmnwekwc : entity work.uya
    port map (byjdoccjhf => eokjqbbr, hav => u);
  rk : entity work.hwelay
    port map (svuggy => itc, ozlz => itc);
  jomh : entity work.uya
    port map (byjdoccjhf => khul, hav => bzlk);
end hwuzkwe;

library ieee;
use ieee.std_logic_1164.all;

entity s is
  port (xkqu : inout std_logic);
end s;

library ieee;
use ieee.std_logic_1164.all;

architecture h of s is
  signal la : real;
  signal kjs : bit;
  signal oove : time_vector(4 to 0);
  signal pvuqrtlbv : std_logic_vector(4 downto 0);
  signal apk : std_logic_vector(3 to 1);
begin
  acl : entity work.hwelay
    port map (svuggy => apk, ozlz => apk);
  upxucjx : entity work.wrevkasqma
    port map (dpmqfxlx => pvuqrtlbv, lkmiyzqr => oove, goe => kjs, eokjqbbr => la);
end h;



-- Seed after: 4982290027078931626,16715549879197889543
