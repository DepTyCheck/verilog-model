-- Seed: 15198749164467623145,18037650846010261179

library ieee;
use ieee.std_logic_1164.all;

entity nmpwxbofe is
  port (wcxhcdznyj : buffer integer_vector(0 to 0); tsyolobw : inout std_logic; uvmv : inout std_logic; sqx : inout time);
end nmpwxbofe;

architecture bpvs of nmpwxbofe is
  
begin
  -- Single-driven assignments
  sqx <= sqx;
  wcxhcdznyj <= (others => 4043);
  
  -- Multi-driven assignments
  uvmv <= 'U';
  uvmv <= uvmv;
end bpvs;

library ieee;
use ieee.std_logic_1164.all;

entity nr is
  port (kiavnowuv : out time; qedtbmbvaq : out std_logic_vector(4 to 4); brwe : buffer integer; wkss : in std_logic_vector(3 to 3));
end nr;

library ieee;
use ieee.std_logic_1164.all;

architecture lqyoru of nr is
  signal lzj : std_logic;
  signal drqqvwdyuk : integer_vector(0 to 0);
begin
  i : entity work.nmpwxbofe
    port map (wcxhcdznyj => drqqvwdyuk, tsyolobw => lzj, uvmv => lzj, sqx => kiavnowuv);
  
  -- Single-driven assignments
  brwe <= brwe;
end lqyoru;

library ieee;
use ieee.std_logic_1164.all;

entity smigeosyow is
  port (u : inout std_logic_vector(1 to 2); av : in integer; saxor : buffer bit_vector(2 downto 1));
end smigeosyow;

library ieee;
use ieee.std_logic_1164.all;

architecture gnnmmjjxk of smigeosyow is
  signal hjx : time;
  signal qytte : integer_vector(0 to 0);
  signal vrqgtbgz : time;
  signal n : std_logic;
  signal zsmbx : integer_vector(0 to 0);
begin
  exgcfqlli : entity work.nmpwxbofe
    port map (wcxhcdznyj => zsmbx, tsyolobw => n, uvmv => n, sqx => vrqgtbgz);
  lg : entity work.nmpwxbofe
    port map (wcxhcdznyj => qytte, tsyolobw => n, uvmv => n, sqx => hjx);
  
  -- Multi-driven assignments
  u <= u;
  n <= n;
  u <= ('W', 'W');
end gnnmmjjxk;

library ieee;
use ieee.std_logic_1164.all;

entity fejopa is
  port (lkrn : inout string(2 to 3); xr : buffer std_logic_vector(1 to 4); x : in real; suzz : buffer real);
end fejopa;

library ieee;
use ieee.std_logic_1164.all;

architecture ttmzat of fejopa is
  signal ftebotgt : time;
  signal wrybu : integer_vector(0 to 0);
  signal ekjintywy : time;
  signal uliqdd : std_logic;
  signal hciis : std_logic;
  signal kydoqvkc : integer_vector(0 to 0);
begin
  tgsmtqjdud : entity work.nmpwxbofe
    port map (wcxhcdznyj => kydoqvkc, tsyolobw => hciis, uvmv => uliqdd, sqx => ekjintywy);
  qnd : entity work.nmpwxbofe
    port map (wcxhcdznyj => wrybu, tsyolobw => hciis, uvmv => uliqdd, sqx => ftebotgt);
  
  -- Single-driven assignments
  lkrn <= lkrn;
  suzz <= suzz;
  
  -- Multi-driven assignments
  xr <= "U0ZZ";
  xr <= xr;
  xr <= ('W', 'Z', '0', 'X');
end ttmzat;



-- Seed after: 12415863902041438855,18037650846010261179
