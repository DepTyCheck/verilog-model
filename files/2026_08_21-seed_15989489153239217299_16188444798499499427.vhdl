-- Seed: 15989489153239217299,16188444798499499427

entity xouriv is
  port (clplxyl : out bit_vector(3 to 0); wc : out real; irz : buffer severity_level);
end xouriv;

architecture ninplzc of xouriv is
  
begin
  -- Single-driven assignments
  irz <= irz;
  clplxyl <= clplxyl;
  wc <= wc;
end ninplzc;

entity y is
  port (nqvus : in time_vector(4 downto 0); lcvmrhfe : linkage time; aehj : inout time);
end y;

architecture cw of y is
  signal dpla : severity_level;
  signal tkb : real;
  signal grnhhyqlb : bit_vector(3 to 0);
  signal t : severity_level;
  signal cc : real;
  signal o : bit_vector(3 to 0);
  signal cyvvcf : severity_level;
  signal mpovdjzquf : real;
  signal lciv : bit_vector(3 to 0);
begin
  wbqisc : entity work.xouriv
    port map (clplxyl => lciv, wc => mpovdjzquf, irz => cyvvcf);
  yyhmk : entity work.xouriv
    port map (clplxyl => o, wc => cc, irz => t);
  gjoxdxrg : entity work.xouriv
    port map (clplxyl => grnhhyqlb, wc => tkb, irz => dpla);
  
  -- Single-driven assignments
  aehj <= aehj;
end cw;

library ieee;
use ieee.std_logic_1164.all;

entity ayeg is
  port (bkvbd : inout integer; kuoz : out std_logic);
end ayeg;

architecture kucpv of ayeg is
  signal kpcbirxkfe : severity_level;
  signal wgxcnx : real;
  signal im : bit_vector(3 to 0);
  signal ynesbpykkz : severity_level;
  signal lqbtfpy : real;
  signal pmaqreosf : bit_vector(3 to 0);
  signal b : severity_level;
  signal djsjycp : real;
  signal l : bit_vector(3 to 0);
  signal ebnfftmj : severity_level;
  signal tprjocbz : real;
  signal scsum : bit_vector(3 to 0);
begin
  kxohodvs : entity work.xouriv
    port map (clplxyl => scsum, wc => tprjocbz, irz => ebnfftmj);
  dsv : entity work.xouriv
    port map (clplxyl => l, wc => djsjycp, irz => b);
  scxqno : entity work.xouriv
    port map (clplxyl => pmaqreosf, wc => lqbtfpy, irz => ynesbpykkz);
  oq : entity work.xouriv
    port map (clplxyl => im, wc => wgxcnx, irz => kpcbirxkfe);
end kucpv;



-- Seed after: 10888678224466018590,16188444798499499427
