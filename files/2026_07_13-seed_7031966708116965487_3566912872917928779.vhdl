-- Seed: 7031966708116965487,3566912872917928779

use std.reflection.all;

entity jysjanac is
  port (xmenfaz : inout physical_subtype_mirror);
end jysjanac;

architecture itjltramzf of jysjanac is
  
begin
  
end itjltramzf;

entity yafke is
  port (n : buffer integer_vector(1 to 0));
end yafke;

use std.reflection.all;

architecture fmfcuw of yafke is
  shared variable kzsj : physical_subtype_mirror;
  shared variable zof : physical_subtype_mirror;
  shared variable z : physical_subtype_mirror;
  shared variable cwnqzva : physical_subtype_mirror;
begin
  opi : entity work.jysjanac
    port map (xmenfaz => cwnqzva);
  rhuwwvchxb : entity work.jysjanac
    port map (xmenfaz => z);
  ftrvw : entity work.jysjanac
    port map (xmenfaz => zof);
  ynot : entity work.jysjanac
    port map (xmenfaz => kzsj);
  
  -- Single-driven assignments
  n <= n;
end fmfcuw;

use std.reflection.all;

entity zdknent is
  port (gpjqnbp : inout integer_subtype_mirror; kkdmstmt : inout integer_subtype_mirror);
end zdknent;

use std.reflection.all;

architecture e of zdknent is
  shared variable oezpl : physical_subtype_mirror;
begin
  tkgnuddgin : entity work.jysjanac
    port map (xmenfaz => oezpl);
end e;



-- Seed after: 9628341256666477736,3566912872917928779
