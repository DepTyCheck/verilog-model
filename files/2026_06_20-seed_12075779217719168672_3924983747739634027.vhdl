-- Seed: 12075779217719168672,3924983747739634027

entity tj is
  port (fzxuh : inout real);
end tj;

architecture izlmldlq of tj is
  
begin
  -- Single-driven assignments
  fzxuh <= 23.14;
end izlmldlq;

library ieee;
use ieee.std_logic_1164.all;

entity lp is
  port (ftowmmng : out boolean; gaeydvvp : buffer std_logic_vector(2 downto 0));
end lp;

architecture jo of lp is
  signal ffybh : real;
  signal lyq : real;
  signal pjaimvshy : real;
  signal s : real;
begin
  clyrz : entity work.tj
    port map (fzxuh => s);
  dz : entity work.tj
    port map (fzxuh => pjaimvshy);
  c : entity work.tj
    port map (fzxuh => lyq);
  yxqsfjfbbe : entity work.tj
    port map (fzxuh => ffybh);
  
  -- Single-driven assignments
  ftowmmng <= TRUE;
end jo;



-- Seed after: 5455228067828142752,3924983747739634027
