-- Seed: 5084017723835077638,11127274767545411571

entity hiinnsw is
  port (jmui : out real_vector(2 downto 0); jfdyoo : buffer real; jfeh : inout severity_level; qxyoleegv : in boolean);
end hiinnsw;

architecture c of hiinnsw is
  
begin
  -- Single-driven assignments
  jfeh <= jfeh;
end c;

entity t is
  port (ukrmoqisn : inout integer; nhpmc : inout integer);
end t;

architecture ebrh of t is
  signal gwuvcahjm : severity_level;
  signal lxe : real;
  signal x : real_vector(2 downto 0);
  signal cqwtnfv : boolean;
  signal uw : severity_level;
  signal fsuz : real;
  signal awphlbafua : real_vector(2 downto 0);
begin
  zjfdii : entity work.hiinnsw
    port map (jmui => awphlbafua, jfdyoo => fsuz, jfeh => uw, qxyoleegv => cqwtnfv);
  oxhnec : entity work.hiinnsw
    port map (jmui => x, jfdyoo => lxe, jfeh => gwuvcahjm, qxyoleegv => cqwtnfv);
  
  -- Single-driven assignments
  nhpmc <= 32;
  ukrmoqisn <= ukrmoqisn;
end ebrh;

entity dmwdi is
  port (itwpfuez : out integer; u : linkage integer; fifjziktc : in time);
end dmwdi;

architecture emvrqvxsky of dmwdi is
  
begin
  -- Single-driven assignments
  itwpfuez <= 16#D#;
end emvrqvxsky;



-- Seed after: 16903184086394377190,11127274767545411571
