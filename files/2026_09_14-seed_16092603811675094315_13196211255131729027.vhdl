-- Seed: 16092603811675094315,13196211255131729027

entity tjsus is
  port (vk : inout severity_level);
end tjsus;

architecture kmbqae of tjsus is
  
begin
  -- Single-driven assignments
  vk <= vk;
end kmbqae;

entity puyirbtf is
  port (siswi : buffer boolean; kfhniqy : inout real; cnkfm : in time);
end puyirbtf;

architecture utwc of puyirbtf is
  signal gvnw : severity_level;
  signal wvqn : severity_level;
  signal sblmmdvuhx : severity_level;
begin
  qwypgxw : entity work.tjsus
    port map (vk => sblmmdvuhx);
  plwsbdofya : entity work.tjsus
    port map (vk => wvqn);
  w : entity work.tjsus
    port map (vk => gvnw);
  
  -- Single-driven assignments
  kfhniqy <= 430.0_3_4;
  siswi <= siswi;
end utwc;

entity ribjmlrb is
  port (ptljsdat : linkage boolean);
end ribjmlrb;

architecture etaxglfhhn of ribjmlrb is
  signal xzjgk : severity_level;
  signal rkrcgh : severity_level;
  signal kw : time;
  signal utkel : real;
  signal d : boolean;
  signal jclwuggf : severity_level;
begin
  et : entity work.tjsus
    port map (vk => jclwuggf);
  b : entity work.puyirbtf
    port map (siswi => d, kfhniqy => utkel, cnkfm => kw);
  qkkudafl : entity work.tjsus
    port map (vk => rkrcgh);
  cllqjtuddy : entity work.tjsus
    port map (vk => xzjgk);
  
  -- Single-driven assignments
  kw <= 16#0CDC# us;
end etaxglfhhn;

library ieee;
use ieee.std_logic_1164.all;

entity ge is
  port (k : in time; jhwej : buffer std_logic_vector(4 to 1));
end ge;

architecture vlzhhu of ge is
  signal xhljtyet : boolean;
  signal ta : severity_level;
begin
  ybgffrn : entity work.tjsus
    port map (vk => ta);
  phsfxlnpju : entity work.ribjmlrb
    port map (ptljsdat => xhljtyet);
end vlzhhu;



-- Seed after: 14193492912449825603,13196211255131729027
