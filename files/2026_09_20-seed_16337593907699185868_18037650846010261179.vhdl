-- Seed: 16337593907699185868,18037650846010261179

library ieee;
use ieee.std_logic_1164.all;

entity de is
  port (qcwpnmpi : in std_logic_vector(4 to 2); dfqlvxtsk : buffer std_logic_vector(0 to 3));
end de;

architecture g of de is
  
begin
  
end g;

library ieee;
use ieee.std_logic_1164.all;

entity ozmyoy is
  port (z : buffer string(2 downto 3); b : in std_logic; w : out integer; ys : out std_logic);
end ozmyoy;

library ieee;
use ieee.std_logic_1164.all;

architecture t of ozmyoy is
  signal rkslcy : std_logic_vector(0 to 3);
  signal khdayu : std_logic_vector(0 to 3);
  signal aovnvtqvgl : std_logic_vector(4 to 2);
  signal wkdi : std_logic_vector(0 to 3);
  signal d : std_logic_vector(0 to 3);
  signal vjvdajgrv : std_logic_vector(4 to 2);
begin
  jzcytdi : entity work.de
    port map (qcwpnmpi => vjvdajgrv, dfqlvxtsk => d);
  vh : entity work.de
    port map (qcwpnmpi => vjvdajgrv, dfqlvxtsk => wkdi);
  yzbqvt : entity work.de
    port map (qcwpnmpi => aovnvtqvgl, dfqlvxtsk => khdayu);
  vuzdqtmsuq : entity work.de
    port map (qcwpnmpi => vjvdajgrv, dfqlvxtsk => rkslcy);
end t;



-- Seed after: 5376952050543958308,18037650846010261179
