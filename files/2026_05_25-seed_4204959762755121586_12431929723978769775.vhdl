-- Seed: 4204959762755121586,12431929723978769775

library ieee;
use ieee.std_logic_1164.all;

entity iokwpfid is
  port (raacqe : buffer severity_level; by : in integer; vtsu : buffer std_logic; atzjvcqhcv : inout real);
end iokwpfid;



architecture e of iokwpfid is
  
begin
  
end e;

library ieee;
use ieee.std_logic_1164.all;

entity w is
  port (yyxqe : inout std_logic_vector(3 downto 4); vxtlrwa : in std_logic_vector(1 downto 3));
end w;

library ieee;
use ieee.std_logic_1164.all;

architecture ktawwor of w is
  signal l : real;
  signal cfp : std_logic;
  signal iill : integer;
  signal vvzhbjntni : severity_level;
begin
  wencj : entity work.iokwpfid
    port map (raacqe => vvzhbjntni, by => iill, vtsu => cfp, atzjvcqhcv => l);
end ktawwor;



entity fo is
  port (hx : buffer real; cemdbvqkn : in character; f : in time);
end fo;

library ieee;
use ieee.std_logic_1164.all;

architecture rqwqoresc of fo is
  signal dfyr : integer;
  signal d : severity_level;
  signal csruajjpgq : real;
  signal lz : std_logic;
  signal rri : integer;
  signal tuzjptxtc : severity_level;
  signal yfd : std_logic_vector(1 downto 3);
  signal ez : real;
  signal devpn : std_logic;
  signal txmfe : integer;
  signal rct : severity_level;
begin
  ivnteis : entity work.iokwpfid
    port map (raacqe => rct, by => txmfe, vtsu => devpn, atzjvcqhcv => ez);
  ncwfjivjdu : entity work.w
    port map (yyxqe => yfd, vxtlrwa => yfd);
  rfsm : entity work.iokwpfid
    port map (raacqe => tuzjptxtc, by => rri, vtsu => lz, atzjvcqhcv => csruajjpgq);
  rgyz : entity work.iokwpfid
    port map (raacqe => d, by => dfyr, vtsu => devpn, atzjvcqhcv => hx);
end rqwqoresc;

library ieee;
use ieee.std_logic_1164.all;

entity bosba is
  port (rj : out std_logic_vector(0 to 3));
end bosba;

library ieee;
use ieee.std_logic_1164.all;

architecture hmw of bosba is
  signal isacmm : real;
  signal hymkjbmyrv : integer;
  signal dewja : severity_level;
  signal imdrsm : real;
  signal kxl : integer;
  signal dg : severity_level;
  signal dgjoivly : std_logic_vector(1 downto 3);
  signal ysd : real;
  signal ku : std_logic;
  signal zidz : integer;
  signal abdyambays : severity_level;
begin
  u : entity work.iokwpfid
    port map (raacqe => abdyambays, by => zidz, vtsu => ku, atzjvcqhcv => ysd);
  exf : entity work.w
    port map (yyxqe => dgjoivly, vxtlrwa => dgjoivly);
  se : entity work.iokwpfid
    port map (raacqe => dg, by => kxl, vtsu => ku, atzjvcqhcv => imdrsm);
  qupbdgazpj : entity work.iokwpfid
    port map (raacqe => dewja, by => hymkjbmyrv, vtsu => ku, atzjvcqhcv => isacmm);
end hmw;



-- Seed after: 7233775565075057417,12431929723978769775
