-- Seed: 4608258132720140654,13857275728440271305

entity akgkfkp is
  port (bfusces : inout severity_level; x : linkage severity_level; mhcg : out time);
end akgkfkp;

architecture kytxxrlzov of akgkfkp is
  
begin
  -- Single-driven assignments
  mhcg <= 0_2_4_4_3 ns;
  bfusces <= NOTE;
end kytxxrlzov;

library ieee;
use ieee.std_logic_1164.all;

entity upnys is
  port (udlgqdzj : in std_logic_vector(0 to 2); mcpwk : linkage std_logic_vector(4 downto 2); zjskx : inout integer);
end upnys;

architecture ydszvy of upnys is
  signal zht : time;
  signal odmpcro : severity_level;
  signal vpfynnsjl : severity_level;
  signal agcslt : time;
  signal kysjmvuw : severity_level;
  signal lqqqrsh : severity_level;
  signal ejf : time;
  signal ndtf : severity_level;
  signal cvjrjkqzhz : severity_level;
  signal u : time;
  signal rdpw : severity_level;
  signal uyoilunsv : severity_level;
begin
  xphokfog : entity work.akgkfkp
    port map (bfusces => uyoilunsv, x => rdpw, mhcg => u);
  vklrhguad : entity work.akgkfkp
    port map (bfusces => cvjrjkqzhz, x => ndtf, mhcg => ejf);
  xogzfcznf : entity work.akgkfkp
    port map (bfusces => lqqqrsh, x => kysjmvuw, mhcg => agcslt);
  i : entity work.akgkfkp
    port map (bfusces => vpfynnsjl, x => odmpcro, mhcg => zht);
  
  -- Single-driven assignments
  zjskx <= zjskx;
end ydszvy;

library ieee;
use ieee.std_logic_1164.all;

entity nvjx is
  port (snesoxc : in std_logic_vector(2 downto 1); znosqmxtk : out boolean_vector(4 to 3));
end nvjx;

architecture fss of nvjx is
  signal qfhx : time;
  signal ta : severity_level;
  signal gp : severity_level;
  signal ty : time;
  signal bemjft : severity_level;
  signal wn : severity_level;
  signal kkvvmqaw : time;
  signal frmllribx : severity_level;
  signal axy : severity_level;
  signal wd : time;
  signal jhptwtaab : severity_level;
  signal xqujjftoj : severity_level;
begin
  xhlypugi : entity work.akgkfkp
    port map (bfusces => xqujjftoj, x => jhptwtaab, mhcg => wd);
  dhcmj : entity work.akgkfkp
    port map (bfusces => axy, x => frmllribx, mhcg => kkvvmqaw);
  e : entity work.akgkfkp
    port map (bfusces => wn, x => bemjft, mhcg => ty);
  q : entity work.akgkfkp
    port map (bfusces => gp, x => ta, mhcg => qfhx);
end fss;

library ieee;
use ieee.std_logic_1164.all;

entity lolpgix is
  port (cxfqcsn : inout std_logic_vector(3 to 2); ylixpkrsrc : linkage severity_level; xdis : in integer; gf : buffer real);
end lolpgix;

library ieee;
use ieee.std_logic_1164.all;

architecture q of lolpgix is
  signal gxscydgy : integer;
  signal y : std_logic_vector(4 downto 2);
  signal opbmnvub : std_logic_vector(0 to 2);
begin
  rerde : entity work.upnys
    port map (udlgqdzj => opbmnvub, mcpwk => y, zjskx => gxscydgy);
  
  -- Single-driven assignments
  gf <= gf;
  
  -- Multi-driven assignments
  cxfqcsn <= "";
  cxfqcsn <= cxfqcsn;
  opbmnvub <= opbmnvub;
end q;



-- Seed after: 9677703370677610266,13857275728440271305
