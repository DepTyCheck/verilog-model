-- Seed: 8826294809401853761,6329330932550885447

library ieee;
use ieee.std_logic_1164.all;

entity nsvzxtrx is
  port (aspc : linkage time; gbpgi : inout std_logic_vector(1 to 1); fxvrtbput : buffer std_logic; slauyny : linkage real);
end nsvzxtrx;



architecture v of nsvzxtrx is
  
begin
  
end v;



entity vdimxrqvor is
  port (nge : out severity_level);
end vdimxrqvor;

library ieee;
use ieee.std_logic_1164.all;

architecture x of vdimxrqvor is
  signal kmneuk : real;
  signal zomkqzdrs : time;
  signal ocvnhejl : time;
  signal g : real;
  signal upcyuhzdb : std_logic;
  signal fyrdlrv : std_logic_vector(1 to 1);
  signal kn : time;
begin
  cs : entity work.nsvzxtrx
    port map (aspc => kn, gbpgi => fyrdlrv, fxvrtbput => upcyuhzdb, slauyny => g);
  ure : entity work.nsvzxtrx
    port map (aspc => ocvnhejl, gbpgi => fyrdlrv, fxvrtbput => upcyuhzdb, slauyny => g);
  nz : entity work.nsvzxtrx
    port map (aspc => zomkqzdrs, gbpgi => fyrdlrv, fxvrtbput => upcyuhzdb, slauyny => kmneuk);
end x;



-- Seed after: 10803958613032031929,6329330932550885447
