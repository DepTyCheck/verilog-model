-- Seed: 3155507564289132423,5511103086789671269

library ieee;
use ieee.std_logic_1164.all;

entity xipicw is
  port (fjkwztk : linkage integer; rtbol : in severity_level; k : linkage std_logic; zkprp : out bit);
end xipicw;

architecture tisasgfm of xipicw is
  
begin
  
end tisasgfm;

entity dy is
  port (fzzobv : inout real; cvqzorvbpg : in integer; fraj : buffer character; stnddan : inout boolean);
end dy;

library ieee;
use ieee.std_logic_1164.all;

architecture prgliwtdfy of dy is
  signal ghhat : bit;
  signal ewpp : std_logic;
  signal ufie : severity_level;
  signal rntzbonbwl : integer;
  signal jzihi : bit;
  signal r : std_logic;
  signal pvsbji : severity_level;
  signal hfyuv : integer;
begin
  nc : entity work.xipicw
    port map (fjkwztk => hfyuv, rtbol => pvsbji, k => r, zkprp => jzihi);
  evuxtlaz : entity work.xipicw
    port map (fjkwztk => rntzbonbwl, rtbol => ufie, k => ewpp, zkprp => ghhat);
  
  -- Single-driven assignments
  fzzobv <= 16#C.E2#;
  stnddan <= stnddan;
  
  -- Multi-driven assignments
  r <= 'X';
  r <= r;
  r <= r;
end prgliwtdfy;

library ieee;
use ieee.std_logic_1164.all;

entity hcowhxtio is
  port (clpmhkt : buffer std_logic; yvii : linkage real);
end hcowhxtio;

library ieee;
use ieee.std_logic_1164.all;

architecture ztfgqdpst of hcowhxtio is
  signal wdgygdyr : bit;
  signal hj : integer;
  signal otlbtu : boolean;
  signal jdlmoqhav : character;
  signal oxmweiy : integer;
  signal ylvjw : real;
  signal a : bit;
  signal fedeoo : std_logic;
  signal nzvtgx : severity_level;
  signal hob : integer;
  signal z : bit;
  signal cbtvt : severity_level;
  signal ljxfc : integer;
begin
  xay : entity work.xipicw
    port map (fjkwztk => ljxfc, rtbol => cbtvt, k => clpmhkt, zkprp => z);
  owrn : entity work.xipicw
    port map (fjkwztk => hob, rtbol => nzvtgx, k => fedeoo, zkprp => a);
  sutebl : entity work.dy
    port map (fzzobv => ylvjw, cvqzorvbpg => oxmweiy, fraj => jdlmoqhav, stnddan => otlbtu);
  axxijicfe : entity work.xipicw
    port map (fjkwztk => hj, rtbol => cbtvt, k => fedeoo, zkprp => wdgygdyr);
  
  -- Single-driven assignments
  oxmweiy <= hj;
end ztfgqdpst;



-- Seed after: 13421990122185024297,5511103086789671269
