-- Seed: 6933042999054846429,12143220691580258643

entity qgzoelwyc is
  port (xlmhhnmnp : in severity_level; ndaod : in bit_vector(2 downto 4); qtdsspuhx : in bit; jpxoaiu : linkage real);
end qgzoelwyc;

architecture ajr of qgzoelwyc is
  
begin
  
end ajr;

library ieee;
use ieee.std_logic_1164.all;

entity ldwwyntef is
  port (zjnsp : linkage real; pxojpk : buffer std_logic_vector(3 to 2));
end ldwwyntef;

architecture fqrv of ldwwyntef is
  
begin
  -- Multi-driven assignments
  pxojpk <= (others => '0');
  pxojpk <= (others => '0');
  pxojpk <= pxojpk;
end fqrv;

library ieee;
use ieee.std_logic_1164.all;

entity trsmf is
  port (brdgtrzla : out std_logic_vector(0 to 2));
end trsmf;

library ieee;
use ieee.std_logic_1164.all;

architecture ttw of trsmf is
  signal jkpbdho : real;
  signal fqvcr : bit;
  signal l : severity_level;
  signal tc : real;
  signal qaizpmunme : bit;
  signal em : std_logic_vector(3 to 2);
  signal iqfnhh : real;
  signal qeckia : real;
  signal ejxlrisn : bit;
  signal cuk : bit_vector(2 downto 4);
  signal awicbl : severity_level;
begin
  ednmppvk : entity work.qgzoelwyc
    port map (xlmhhnmnp => awicbl, ndaod => cuk, qtdsspuhx => ejxlrisn, jpxoaiu => qeckia);
  kjfawc : entity work.ldwwyntef
    port map (zjnsp => iqfnhh, pxojpk => em);
  ydrd : entity work.qgzoelwyc
    port map (xlmhhnmnp => awicbl, ndaod => cuk, qtdsspuhx => qaizpmunme, jpxoaiu => tc);
  shhk : entity work.qgzoelwyc
    port map (xlmhhnmnp => l, ndaod => cuk, qtdsspuhx => fqvcr, jpxoaiu => jkpbdho);
  
  -- Single-driven assignments
  fqvcr <= ejxlrisn;
end ttw;



-- Seed after: 18042055506986566103,12143220691580258643
