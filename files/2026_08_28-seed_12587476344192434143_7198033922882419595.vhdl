-- Seed: 12587476344192434143,7198033922882419595

library ieee;
use ieee.std_logic_1164.all;

entity cectvy is
  port (uf : in severity_level; odvhzmpkmd : in std_logic_vector(1 to 2); qenbvsf : buffer boolean_vector(1 to 2));
end cectvy;

architecture o of cectvy is
  
begin
  -- Single-driven assignments
  qenbvsf <= qenbvsf;
end o;

library ieee;
use ieee.std_logic_1164.all;

entity jhbmwr is
  port (suvgn : in bit; beg : in severity_level; l : out real; sfinql : inout std_logic);
end jhbmwr;

library ieee;
use ieee.std_logic_1164.all;

architecture desjaxha of jhbmwr is
  signal tzpkm : boolean_vector(1 to 2);
  signal oikjc : severity_level;
  signal rhzwx : boolean_vector(1 to 2);
  signal ybygsurny : boolean_vector(1 to 2);
  signal zxnfyzj : boolean_vector(1 to 2);
  signal tefb : std_logic_vector(1 to 2);
  signal chrwelnbn : severity_level;
begin
  fgx : entity work.cectvy
    port map (uf => chrwelnbn, odvhzmpkmd => tefb, qenbvsf => zxnfyzj);
  eh : entity work.cectvy
    port map (uf => beg, odvhzmpkmd => tefb, qenbvsf => ybygsurny);
  uidvwxiylb : entity work.cectvy
    port map (uf => beg, odvhzmpkmd => tefb, qenbvsf => rhzwx);
  fhza : entity work.cectvy
    port map (uf => oikjc, odvhzmpkmd => tefb, qenbvsf => tzpkm);
  
  -- Single-driven assignments
  oikjc <= beg;
  l <= 2#0.0#;
  chrwelnbn <= beg;
  
  -- Multi-driven assignments
  sfinql <= sfinql;
  tefb <= "ZW";
end desjaxha;

entity lzva is
  port (lbat : inout time; ljxtn : buffer string(1 downto 5); twjs : out integer);
end lzva;

library ieee;
use ieee.std_logic_1164.all;

architecture v of lzva is
  signal dkvcgio : boolean_vector(1 to 2);
  signal gvyuepoog : boolean_vector(1 to 2);
  signal njrou : std_logic_vector(1 to 2);
  signal ad : severity_level;
begin
  oq : entity work.cectvy
    port map (uf => ad, odvhzmpkmd => njrou, qenbvsf => gvyuepoog);
  vuekfvljot : entity work.cectvy
    port map (uf => ad, odvhzmpkmd => njrou, qenbvsf => dkvcgio);
  
  -- Single-driven assignments
  ljxtn <= "";
  
  -- Multi-driven assignments
  njrou <= njrou;
  njrou <= njrou;
end v;

library ieee;
use ieee.std_logic_1164.all;

entity jbrxn is
  port (alfhyrm : inout std_logic);
end jbrxn;

architecture brjwqr of jbrxn is
  signal aufmcnp : real;
  signal aeqml : severity_level;
  signal fg : bit;
  signal mmmnxakxq : integer;
  signal xyvpceyhmu : string(1 downto 5);
  signal vkmr : time;
begin
  wsgvqttxc : entity work.lzva
    port map (lbat => vkmr, ljxtn => xyvpceyhmu, twjs => mmmnxakxq);
  svrtkc : entity work.jhbmwr
    port map (suvgn => fg, beg => aeqml, l => aufmcnp, sfinql => alfhyrm);
  
  -- Single-driven assignments
  fg <= fg;
  aeqml <= aeqml;
  
  -- Multi-driven assignments
  alfhyrm <= '0';
  alfhyrm <= alfhyrm;
  alfhyrm <= alfhyrm;
  alfhyrm <= alfhyrm;
end brjwqr;



-- Seed after: 9585423323940044219,7198033922882419595
