-- Seed: 5200489524335136181,499459191852795575

entity rehn is
  port (nrbshs : in real_vector(3 to 1); jel : out integer; desi : linkage real; xkxobao : linkage time);
end rehn;

architecture thhljnvm of rehn is
  
begin
  -- Single-driven assignments
  jel <= jel;
end thhljnvm;

entity vtax is
  port (y : buffer time);
end vtax;

architecture cvoguvi of vtax is
  signal nueyvubw : time;
  signal zdh : real;
  signal ifutv : integer;
  signal lzt : real_vector(3 to 1);
  signal godjthfs : real;
  signal l : integer;
  signal nbelt : real_vector(3 to 1);
begin
  bvoinnen : entity work.rehn
    port map (nrbshs => nbelt, jel => l, desi => godjthfs, xkxobao => y);
  x : entity work.rehn
    port map (nrbshs => lzt, jel => ifutv, desi => zdh, xkxobao => nueyvubw);
  
  -- Single-driven assignments
  nbelt <= nbelt;
  lzt <= nbelt;
end cvoguvi;



-- Seed after: 5528336857606828631,499459191852795575
