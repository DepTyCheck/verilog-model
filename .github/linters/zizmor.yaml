rules:
  excessive-permissions:
    disable: true
  template-injection:
    disable: true
  unpinned-uses:
    disable: true
  unpinned-images:
    disable: true
  secrets-outside-env:
    disable: true
  github-env:
    disable: true
  cache-poisoning:
    disable: true
